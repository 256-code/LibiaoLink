# 市场项目管理系统 · 技术设计 v0.1（选型分析篇）

- 版本：v0.1.8（讨论稿），2026-09-18
- 入仓：仓库根 技术设计v0.1-选型分析.md（2026-09-17 入仓，PR 评审、与《开发日志》同批同步；v0.2 同批入仓）
- 需求输入：《市场项目管理系统_功能设计与需求说明书_V1.0》（基于《市场项目.md》《自动化功能清单.md》整理）；功能口径对照《市场项目管理系统 · 系统功能书》（V1.0 评审稿）
- 参照文档：《技术设计v1.2.2》（下称"旧设计"，InPulse 项目）
- 本文范围：只做**技术选型分析与结论**；数据模型、接口契约、部署细节留给 v0.2 及后续篇章

**修订记录**

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.1 | 2026-09-17 | 首轮选型分析 |
| v0.1.1 | 2026-09-17 | 业务确认后修订：仅用企微；ERP、移动端、甘特图移出一期；三维查看保留需求、方案留白（禁止上传第三方云）；干系人纳入一期；在线编辑定位二期自托管；新增团队分工与轻量 CI 策略（7.5） |
| v0.1.2 | 2026-09-17 | 对齐 LibiaoLink 仓库现状：认证改为接入公司统一 SSO（Casdoor OIDC + PKCE）；前端按现状修正（React 19 + Vite 8 + TS 7 + Tailwind 4，改走 headless 方案）；CI 与仓库结构对齐；新增 1.5 现状快照与 Q14~Q16 |
| v0.1.3 | 2026-09-17 | 与《系统功能书 V1.0》评审对齐（选型侧）：邮件渠道一期不做、预留二期（4.14/4.16、Q17）；二维 CAD 条件性交付（PoC 入阶段 0、样本提前；4.8/7.3、Q4）；补齐可用性 99.9% 验收口径（4.17、Q19）；一期范围摘要补 全局搜索/审批中心/工作日历/回收站 并改引「范围矩阵」（1.3/7.2）；补 ERP 接口方式待确认（Q18）；完成 7.1 仓库结构与 Q14~Q16 收尾 |
| v0.1.4 | 2026-09-17 | 开会对齐的新需求落地：流程节点体系（固定模板、按模板增删节点、节点约束、成果文件门禁）与流程蓝图快速导入（自建 JSON schema，见 4.21）；变更管理一期不做审批（申请即通过）；预览明确为只读、系统内直接查看、对标 ONLYOFFICE 查看体验、不做在线编辑（4.7）；三维在线查看确定自建路线（开源优先、无合适组件则自研，见 4.8）；流程节点体系一期不做权限控制（不按角色区分、全程留痕），全系统权限设计不变（4.12）；首页分类侧边栏（地区/项目类型/负责人，7.2）；新增 Q20~Q28 |
| v0.1.5 | 2026-09-17 | 口径澄清：一期免审批仅指「变更最终交付文件」的审批（申请即通过、所有人平权，其余审批能力保留）；蓝图定义与自建格式经业务确认（4.21）；首页分类「地区」= 项目落地地区（7.2，Q24/Q26/Q27/Q28 同步） |
| v0.1.6 | 2026-09-17 | 蓝图口径收口（原 Q27）：蓝图全局一份；一期节点增删仅限模板节点池，自定义节点（可能牵涉管理员权限）留待后续版本；蓝图变更不影响已生成项目（导入即快照）；Q29 记录，8.2 Q27 关闭 |
| v0.1.7 | 2026-09-17 | 入仓：本文档入仓库根（技术设计v0.1-选型分析.md，与 v0.2 同批）。与代码 / 功能书对齐：星级进度→四格进度（1.1、7.2）；任务状态五态派生（待开始 / 进行中 / 已完成 / 提前完成 / 已延期，逾期标注留在实际完成日期，1.5）；文档路径与仓库结构按现状修订（1.5、7.1、8.2 Q16、9） |
| v0.1.8 | 2026-09-18 | 术语口径更正（Push 35）：首页分类侧边栏「负责人」维度按业务两角色口径更正为「项目经理」（项目级唯一责任人，`projects.manager_id` 必填，首页筛选 `filter[managerId]`）；任务级仍为「任务负责人」（`tasks.owner_id`）。与 技术设计v0.2 v0.2.2 / 系统功能书 A1-18 对齐；正文历史结论不静默改写（差异见本表） |
| v0.1.9 | 2026-09-18 | 现状快照（1.5）回写：登录后新增入口页与三层路由（`#/` 入口页、`#/projects` 项目空间列表、`#/project/{id}` 详情、`#/templates` / `#/files` 占位页），顶栏 Logo 改为「返回上一层」（Push 57，详见 `前端功能需求.md` §一.7） |
| v0.1.10 | 2026-09-19 | 现状快照（1.5）回写：任务模板页第一版（`#/templates?section=<板块>`：九个阶段板块标签栏 + 任务节点卡片区 + 毛玻璃模板面板，拖拽复制 / 去重 / 面板内排序，模板按阶段分开、一期内存草稿），另 `字段对照清单.md` 新增「任务模板草稿字段」口径（Push 58，详见 `前端功能需求.md` §一.7.2） |


---

## 0. 一页结论

### 0.1 选型结论表

| 层次 | 结论 | 一句话理由 |
|---|---|---|
| 交付形态 | 自研（延续旧项目路线） | 图纸/三维预览、字段级权限、自动化规则、ERP 集成这四件事恰是低代码与成品软件最弱处 |
| 总体架构 | 模块化单体 + 独立 worker 进程 + 独立转换容器 + 对象存储，Docker Compose 单机部署 | 300 人没有高并发压力；需要隔离的是 CPU 密集的文件处理与定时/消息可靠性，不是业务域 |
| 语言/框架 | TypeScript 严格模式：NestJS（api/worker）+ React（SPA） | 与旧设计同栈，团队与 AI 工作流零迁移成本；CPU 密集任务交给外部进程，语言不构成瓶颈 |
| 数据库 | PostgreSQL 18.x | JSONB 自定义字段、约束与部分唯一索引、窗口函数与物化视图、SKIP LOCKED 队列、PGroonga 中文检索，一库多能 |
| 数据访问 | Drizzle ORM + 显式 SQL 迁移 | 类型安全同时保留 SQL、锁与索引控制力（旧设计已验证） |
| 队列与调度 | 一期 PostgreSQL 原生：Outbox + SKIP LOCKED + advisory lock；不引入 Redis | 能用数据库解决的就不加中间件；同时给出 Redis 的明确切换阈值（见 4.5） |
| 文件存储 | S3 协议抽象 + MinIO 单节点（独立数据盘、开启版本控制）+ 异地增量同步 | 大文件直传、断点续传、版本、生命周期都要；云 OSS 作为后续替换项 |
| 在线预览 | 自建转换管道（LibreOffice headless + xlsx 结构化渲染 + PDF/图片），**只读、系统内直接查看、对标 ONLYOFFICE 查看体验**；三维在线查看走自建路线（一期保留上传/下载入口） | 已确认不允许上传第三方云（排除 Autodesk 等云方案），预览不经第三方软件/云、不支持在线编辑；三维以开源/现成组件优先，无合适组件则自研（见 4.7/4.8） |
| 在线编辑 | **明确不做**：系统只提供只读预览，不支持在线编辑 | 开会确认：预览在系统内直接看、不经第三方软件，且不支持在线编辑；ONLYOFFICE/Collabora 不采用（费用调研存档，见 4.7） |
| 流程节点与蓝图 | 自研流程节点体系（固定模板 + 按模板增删节点 + 节点约束 + 成果文件门禁）+ 自建蓝图格式（JSON schema + 蓝图库 + 版本号，支持快速导入） | 不引入 BPMN/工作流引擎；门禁规则必须服务端强制、可单测（见 4.21、ADR-015） |
| 首页分类 | 项目主数据扩展（地区、项目类型等）+ 首页侧边栏多维分类（负责人等），筛选态入 URL | 业务开会对齐：按地区/项目类型/负责人等分类查看项目（见 7.2） |
| 消息通道 | 企业微信（自建应用消息 + 群机器人）+ 站内信兜底；适配层预留钉钉/飞书/邮件 | 用户都在企微；外部渠道必须"至少一次 + 幂等"，因此 Outbox 强制（见 4.13/4.14） |
| 身份认证 | 接入公司统一 SSO（Casdoor，OIDC + PKCE）；企业微信为上游账号来源；本系统不自建账号与 IDP，会话与授权仍在本系统 | 公司 SSO 已上线并有接入标准与实测记录；离职在 SSO/企微侧禁用即失效 |
| 权限 | 自研策略层：RBAC + 数据范围 + 记录级 + 字段级 + 操作级（导出/下载/外链） | 需求中"指定内容不可见"包含字段级与脱敏；第三方引擎仍要自研大量胶水，自研可测可控 |
| 搜索 | PostgreSQL + PGroonga（脱敏搜索投影） | 沿用旧设计已验证方案；数据量级远未到 Elasticsearch |
| 前端 | 延续仓库现状（React 19 + Vite 8 + TS 7 + Tailwind 4 + 自研组件），补充 TanStack Query / TanStack Table+Virtual / RHF + Zod / ECharts | 与现有视觉体系零冲突；不用 AntD 等重型组件库（见 4.9 与 Q14） |
| 移动端 | 一期不做专项（Web 保持基础响应式，不做弱网草稿/拍照/企微 JS-SDK 集成） | 已确认一期先不考虑移动端，二期再评估（见 4.10） |
| 部署 | Docker Compose + Nginx（仅 Nginx 暴露端口），单机起步 | 无 K8s 需求；容量按实测校准（见 4.18） |
| 可观测 | pino 结构化日志 + live/ready 健康检查 + 队列/调度/备份告警（推企微群） | 继承旧设计，新增"自动化管道自身的可观测性" |
| 测试与 CI | 轻量分级：本地钩子（lint/typecheck/单测）+ 合并前检查（集成测试/从零迁移）+ 夜间任务（E2E/演练） | 3 人团队适配版；三条红线不可省（见 7.5） |

### 0.2 三句话总结

1. **300 人不是性能问题**。真正难的是三件事的正确性：文件（存储/版本/预览/备份）、自动化（定时正确、消息不丢不重）、权限（字段级、导出受控、全程留痕）。
2. **与旧设计的差异集中在四个新增技术面**：对象存储、转换/预览管道、Outbox 消息投递、企业微信集成。旧设计的工程规范（契约、约束、幂等、审计、版本锁定、CI 门禁）整体继承，但领域模型不继承。
3. **一期只服务实施部门，建议"功能裁剪、模型不裁剪"**：功能按最小闭环做，数据模型与权限模型按 300 人/多部门设计，否则二期结构性返工。
4. **本轮已确认的范围调整**：干系人纳入一期；甘特图、移动端专项、ERP 移出一期；**不允许上传第三方云**、**预览只读且不经第三方软件**、**不支持在线编辑**是硬约束；新增一期：流程节点体系（固定模板 + 按模板增删 + 节点约束 + 成果文件门禁）、流程蓝图快速导入（自建格式）、变更申请即通过（不做审批）、首页分类侧边栏；三维在线查看走自建路线（开源优先，无合适组件则自研）。

---

## 1. 输入、边界与判断依据

### 1.1 需求基线（技术相关要点提取）

需求说明书共 12 章 + 4 个附录，7 大功能域：

| 功能域 | 包含模块 | 优先级 |
|---|---|---|
| ① 项目与任务管理 | 项目总览、任务计划表、人员任务分配、任务进展看板、甘特图、里程碑与依赖 | P0 |
| ② 日报与问题闭环 | 日报填报、日报中心、问题跟踪、问题看板 | P0 |
| ③ 文件与变更管理 | 文件库与定档、在线预览、变更申请与审批、变更记录、归档导出 | P1 |
| ④ 干系人管理 | 干系人台账、销售录入端口、项目关联 | P1 |
| ⑤ 自动化与通知中心 | 规则引擎、消息中心、自定义待办 | P1 |
| ⑥ 仪表盘与统计分析 | 仪表盘、专项统计、四格进度、ERP 对比图 | P1 |
| ⑦ 平台与治理 | 权限、字典、模板、集成、审计 | P0 |

其中**直接决定技术选型的硬需求**：

- **在线预览**：Word/Excel/PDF 在线打开（**只读、系统内直接查看、对标 ONLYOFFICE 查看体验，不支持在线编辑**）；CAD（dwg/dxf）在线查看；三维文件可在线查看（**不上第三方云；自建路线：开源/现成组件优先，无合适组件则自研**）；支持离线下载；初期支持任意文件上传。
- **文件生命周期**：上传 → 定档（锁版）→ 变更申请（一期不做「变更最终交付文件」审批：申请即通过、所有人平权）→ 版本链 → 归档导出；变更需保留"变更前/变更后/原因/申请人/版本"。
- **流程节点与蓝图**：项目环节的流程节点来自固定模板（不可随意编辑），可按模板增删；每个节点有约束；节点要求输出成果文件、无文件时不允许完成；项目全流程支持快速蓝图导入（格式由本系统自建，见 4.21）。
- **自动化**：原文 7 条规则（R01~R07）+ 建议 18 条（A01~A18）；触发方式含"预计完成日期前 1 天 08:00""开始日期当天 10:00""每周一 09:30""记录创建/更新"；动作含"发送消息到群聊""回写字段（变更关联）""创建待办"。
- **权限**：字段级"指定内容不可见"、记录级、导出与下载受控且留痕、外链分享（限时/限权/可水印）。
- **集成**：统一 SSO（公司 Casdoor，已就绪）、ERP（二期，读取节点数据形成对比图）、IM（仅企业微信）、邮件；三维查看不得使用第三方云（Autodesk 等已排除）。
- **非功能**：核心工作时间可用性 ≥ 99.9%；定时提醒可重试与补发；百人级并发；日报高峰 18:00-19:00 不阻塞；单项目 1 万行表格操作流畅（滚动/筛选 < 2 秒）；每日自动备份保留 ≥ 6 个月；关键操作全量审计；预留国际化。
- **移动端**：现场日报、拍照上传、弱网草稿（需求文档 5.6.1 明确移动端优先）——**本轮确认：一期不做，二期再评估**。

### 1.2 规模画像："300 人"的真实含义

| 维度 | 数值/判断 | 说明 |
|---|---|---|
| 用户数（目标） | 约 300 人 | 全公司多部门 |
| 一期用户（实施部门） | 假设 30~80 人（待确认，见 8-Q9） | 只影响试点资源，不影响架构选型 |
| 峰值并发 | 30~60（按 10%~20% 同时在线估计） | 内部系统真实并发通常低于 5% |
| 日报高峰 | 18:00-19:00，假设 15 分钟 100~200 次提交 | 折合每秒不到 1 次请求，无压力 |
| 数据量（3 年） | 任务 5~20 万行、日报 5~15 万条、问题 1~5 万条、变更 1~3 万条 | 单表百万级以内，PG 单机绰绰有余 |
| 文件量（3 年） | 约 1.5~4 TB（含版本与备份），见 2.3 测算 | **唯一需要认真规划容量的维度** |
| 消息量 | 规则消息 5~20 万条/年（合并推送后实际外发更少） | PG 队列完全够用 |
| 预览转换量 | 假设峰值 200~500 次/天，单次 1~10 秒 | 需要并发控制，但远不需要集群 |

**结论：用户数不是容量问题，文件才是。**

### 1.3 一期范围（实施部门）：功能裁剪、模型不裁剪

用户已明确：现阶段只完成实施部门的使用，其余部门功能尚未涉及。建议：

- **一期实现**：统一 SSO 登录（企微为账号来源）与组织同步、项目与任务主表（表格 + 看板）、我的工作台、日报 + 日报中心（桌面端）、问题闭环（自动生成/分派/超期升级）、干系人管理（含销售录入）、文件上传/下载/定档/版本/回收站、预览（Office/PDF/图片，只读、系统内直接查看）+ 二维 CAD（条件性，PoC 达标才交付）、变更申请（一期不做「变更最终交付文件」的审批：申请即通过、所有人平权；审批中心保留延期审批位，A06 期次以功能书 V1.1 为准）、流程节点体系（固定模板、按模板增删节点、节点约束、成果文件门禁）与流程蓝图快速导入（自建格式，见 4.21）、首页分类侧边栏（地区/项目类型/负责人）、全局搜索、字典与模板、工作日历、自动化 R01~R07 + A01/A02/A03/A14、站内信 + 企微消息、基础仪表盘、权限与审计、备份恢复、历史数据迁移。
- **一期留接口不实现（或留白）**：甘特图（二期）、移动端专项（二期再评估）、三维在线查看（路线已定：自建，开源优先、无组件则自研；一期保留上传/下载与关联）、ERP（二期）、外链分享、数据大屏、归档包导出、多语言。
- **明确不做**：在线编辑（预览只读）；外部格式（Excel/WPS/Visio 等）的流程蓝图导入。
- **数据模型层面**：部门、角色、项目成员、干系人、权限策略按多部门设计，避免二期结构性返工。

（一期范围的唯一口径：待《系统功能书》V1.1 发布「范围矩阵」后以其为准；本节与 7.2 仅为摘要引用。）

### 1.4 与旧设计（InPulse v1.2.2）的关系

| 类别 | 内容 |
|---|---|
| 直接继承（工程不变量） | ① TS 严格模式 + monorepo + pnpm workspace；② Zod 契约 + Code-first OpenAPI + 生成客户端；③ 数据库约束兜底（NOT NULL、CHECK、复合外键、部分唯一索引）；④ 乐观锁"条件更新 + 版本递增"；⑤ 写接口幂等（同事务幂等表）；⑥ 事务内投影（搜索/活动/统计）；⑦ 审计"可追溯、抗篡改"；⑧ Session 安全设计（PG Store、Token 哈希、CSRF）；⑨ 权限矩阵 CI 门禁；⑩ 版本与镜像 digest 锁定；⑪ 备份以"能够恢复"为验收标准；⑫ AI 协作规范（仓库规则 + CI 强制） |
| 必须新增（新需求面） | ① 对象存储与文件生命周期；② 转换/预览管道（隔离、缓存、降级）；③ Outbox + 消息投递（外部副作用）；④ 规则引擎与调度（时间语义、补发）；⑤ 企业微信集成（OAuth/通讯录/消息）；⑥ 字段级权限与脱敏在导出/搜索/通知中的一致性；⑦ 移动端弱网与大文件上传；⑧ CAD/三维预览的合规与降级策略 |
| 不可照搬 | ① 领域模型（任务聚合/迭代记录/合并/遗留项属于旧项目业务）；② NestJS 11.2.3 等历史锁定（新项目重新定版，见 4.20）；③ "本期不实现 Outbox"的结论（本项目必须有）；④ 单进程形态（本项目拆 api/worker）；⑤ "所有副作用都是数据库写入"的前提假设 |

### 1.5 现状快照：LibiaoLink 仓库已有资产（2026-09-17）

| 项 | 现状 | 对本设计的影响 |
|---|---|---|
| 仓库 | github.com/256-code/LibiaoLink（公开；main 分支保护 + PR 评审 + CODEOWNERS + Dependabot） | 设计文档与代码都走 PR；文档同步有硬性约定（AGENTS.md） |
| 团队 | px / wmj / lan（3 人） | 与 7.5 的分工假设一致（2 后端 + 1 前端/运维/测试） |
| 统一登录 | 公司 SSO 已上线：Casdoor 自托管（正式 auth.libiaorobot.com；测试 authtest 仅内网），企微为 Provider，通讯录同步 + 离职禁用；标准 OIDC + PKCE，JWT-Custom 五字段（Name=工号） | 本系统作为 OIDC 客户端接入，不自建账号体系；业务授权仍在本系统（Casdoor 角色默认为空） |
| 前端原型 | React 19 + Vite 8 + TypeScript 7 + Tailwind CSS 4（无组件库）；登录后入口页（项目空间 / 任务模板 / 文件库 三入口，`#/`）、项目空间列表（`#/projects`）、项目详情（`#/project/{id}`，阶段导航 / 分组任务表 / 四格进度 / 抽屉 / 列筛选）、任务模板第一版（`#/templates?section=<板块>`：阶段板块标签栏 + 任务节点卡片区 + 毛玻璃模板面板，拖拽复制 / 去重 / 面板内排序，模板按阶段分开、一期内存草稿）已可用，数据为内存 mock；任务状态为派生展示 5 态（待开始 / 进行中 / 已完成 / 提前完成 / 已延期），「逾期」标注落在实际完成日期 | 组件库走 headless 路线（见 4.9）；现有交互与字段口径是后端契约的输入 |
| 数据样本 | 源表 1 个项目：44 条任务 / 18 列；干系人 6 条；变更 3 条；日报为空 | 迁移与数据模型的第一批真实样本（见 字段对照清单） |
| 接口约定 | 前端功能需求.md（仓库根）已给出 /api/v1 建议契约（项目 / 任务 / 进度 / 汇总 / 偏好） | 作为后端 v1 契约起点，落为 Zod 契约包 |
| 技术设计 | 技术设计v0.1-选型分析.md 与 技术设计v0.2-架构与数据模型.md 已在仓库根（2026-09-17 入仓） | 设计与代码同仓，随《开发日志》同步维护 |
| CI | GitHub Actions：frontend 的 typecheck + build + shared 的契约漂移检查 + server 门禁（typecheck / vitest / 边界规则，PR 与 main） | 已是轻量 CI 的 L2 雏形；集成测试与空库迁移按 7.5 继续扩展 |
| 部署 | deploy/casdoor（Casdoor + MySQL + Redis 本地沙箱模板） | 属公司 SSO 资产，不是本系统部署；本系统部署另建 compose（见 4.18） |

---

## 2. 负载与场景画像（选型的依据）

### 2.1 决定选型的八类技术特征

| # | 特征 | 强度 | 对选型的影响 |
|---|---|---|---|
| 1 | 文件密集：上传、版本、预览、归档、备份 | ★★★★★ | 对象存储 + 转换 worker + 直传与断点续传；备份策略必须覆盖文件 |
| 2 | 自动化与定时：规则引擎、提醒、补发 | ★★★★★ | 调度与投递必须自建可控；幂等与时间语义是正确性核心 |
| 3 | 权限密集：字段级、记录级、导出受控 | ★★★★☆ | 服务端统一策略层；没有"开箱即用"的第三方能省掉胶水 |
| 4 | 多视图重交互：表格、看板、甘特 | ★★★★☆ | 前端组件的自研/采购决策直接决定体验与工期 |
| 5 | 外部集成：企微、ERP、三维云 | ★★★★☆ | 需要适配层与降级路径；外部依赖不可控 |
| 6 | 移动现场：弱网、拍照、草稿（二期） | ★★★★☆ | H5 + 本地草稿 + 图片压缩 + 断点续传；一期仅保持基础响应式 |
| 7 | 审计与留存：导出/下载/变更全留痕 | ★★★☆☆ | 统一审计中间件 + 下载水印与日志 |
| 8 | 高并发与大数据分析 | ★★☆☆☆ | 不是驱动因素；PG 单机足以覆盖（除文件外） |

### 2.2 量化假设（假设值需用真实数据校准）

| 指标 | 假设值 | 校准方式 |
|---|---|---|
| 在管项目数 | 100~300（一期 20~50） | 从智能表格台账导出统计 |
| 单项目任务数 | 50~300，极端 ≤ 1 万（需求上限） | 现有总览表行数分布 |
| 单项目文件数 | 100~500 个（图纸/文档/照片/评审单） | 表格附件统计 + 抽样访谈 |
| 单文件大小 | 图纸 2~20 MB；Office/PDF 1~10 MB；三维 20~500 MB；照片 0.3~5 MB | 抽样盘点 |
| 活跃规则数 | 7 条内置 + 10~20 条扩展 | 需求清单 |
| 单日规则消息量 | 50~500 条（合并推送前） | R03/R04/R05 按在管任务量推演 |
| 转换任务量 | 200~500 次/天（峰值） | 预览点击率估算 |

### 2.3 文件存储测算

> 总容量 ≈ 项目数 × 单项目文件量 × 版本系数 × 副本系数（副本 = 备份 + 归档留存）

| 情景 | 参数 | 估算总容量 |
|---|---|---|
| 保守 | 300 项目 × 1 GB × 1.3 × 2.2 | 约 0.9 TB |
| 中性 | 500 项目 × 1.5 GB × 1.5 × 2.2 | 约 2.5 TB |
| 激进 | 800 项目 × 2.5 GB × 1.6 × 2.2 | 约 7 TB |

另需预留：转换/预览产物缓存（约文件量的 10%~20%）、归档包（导出后长期留存）、数据库备份。

**建议**：一期按 2~4 TB 可用容量规划（独立数据盘，可扩容），并制定保留策略（定档文件长期保留、过程文件 2 年后可归档清理）。**照片增长最容易被低估**，建议客户端压缩到长边 2048px、单张 300 KB~1 MB 后上传。

### 2.4 瓶颈排序（结论）

1. **文件管道**（最重，牵动存储、备份、预览、合规）
2. **自动化正确性**（最易出错、最影响口碑：漏发/重发/错发）
3. **权限与审计一致性**（越权与泄露是不可接受事故）
4. **前端重交互组件**（表格/看板/甘特，工期风险最高）
5. **数据迁移**（脏数据影响上线信任度）
6. **并发**（几乎无压力，不要为此过度设计）

---

## 3. 选型五原则

1. **延续优于重构**：团队与 AI 工作流的复用是最大成本杠杆，能继承的工程规范全部继承。
2. **只在"新需求面"引入新组件**：本项目的新需求面有四个（对象存储、转换管道、Outbox 投递、企微集成），其余一律复用旧栈。
3. **每个新组件必须能退得回去**：用接口隔离（S3 协议、ChannelAdapter、NotifyPort），避免深度绑定单一供应商。
4. **能用数据库解决的，不引入新中间件**：PostgreSQL 在本项目同时承担事务库、队列、调度、搜索、统计五种角色。
5. **决策留痕**：所有"采用/不采用"写入 ADR，版本、镜像 digest、切换阈值全部写死到文档与 CI。

---

## 4. 逐层选型

### 4.1 交付形态：自研 vs 低代码 vs 成品采购

| 维度 | 低代码平台（简道云/明道云/宜搭/氚云等） | 成品项目管理系统 | 自研（本方案） |
|---|---|---|---|
| 表单/表格/看板/甘特 | 现成，上手快 | 现成 | 需自研（甘特是重点） |
| 自动化规则 | 有，但复杂条件/回写语义受限 | 有限 | 完全可控 |
| 字段级权限 | 多数只能到"视图/表单"级 | 弱 | 完全可控 |
| 文件预览（CAD/三维） | 基本没有 | 少 | 必须私有化（本轮确认不上第三方云） |
| ERP/企微深度集成 | 受限 | 依赖厂商 | 完全可控 |
| 数据主权与合规 | 数据在第三方或私有化高价版 | 依厂商 | 完全自主 |
| 成本模型 | 按用户年订阅（300 人费用持续） | 高一次性 + 实施费 | 开发人力 + 服务器 |
| 长期演进 | 受平台边界限制 | 受厂商版本限制 | 自主 |
| 适用结论 | 适合轻量收集类场景 | 适合标准化管理场景 | **本项目四个硬需求（图纸预览、字段级权限、自动化语义、ERP 集成）恰是前两者最弱处** |

**结论：自研。** 触发改结论的条件：如果公司战略上要求"三个月内上线且允许数据在第三方"，可先用低代码承接日报与问题闭环作为过渡，但核心数据底座仍需自研。

### 4.2 总体架构形态

| 候选 | 结论 | 理由 |
|---|---|---|
| 纯单体单进程 | 不采用 | 文件转换是 CPU 密集任务，会阻塞 API；定时任务重启丢语义 |
| **模块化单体 + 多进程** | **采用** | api（HTTP）与 worker（队列/调度/投递）同代码库、不同入口；Compose 内独立服务、独立资源限制 |
| 微服务 | 不采用 | 300 人无独立扩缩容需求，分布式事务与联调成本纯属负收益 |
| Kubernetes | 不采用 | 无弹性伸缩与多租户需求；Compose + 单机运维成本更低 |

运行时拓扑（一期）：

~~~text
                    浏览器 / 企微工作台(H5)
                            │ HTTPS
                            ▼
                 ┌───────────────────────┐
                 │  Nginx（唯一暴露端口）  │
                 │  / → 静态资源 SPA      │
                 │  /api/v1/* → api      │
                 │  /files/* → 鉴权代理    │
                 └───────┬───────┬───────┘
                         │       │
              ┌──────────▼──┐  ┌─▼──────────────┐
              │  api        │  │  MinIO（S3）    │
              │  NestJS     │  │  独立数据盘      │
              └──┬───────┬──┘  └─▲──────────────┘
                 │       │       │
      ┌──────────▼──┐  ┌─▼───────┴──────┐   ┌─────────────────┐
      │ PostgreSQL  │  │  worker        │   │ converter       │
      │ 18.x        │◀─│  队列/调度/规则  │──▶│ LibreOffice 等  │
      │ 事务+队列    │  │  消息投递        │   │ 无外网/非 root   │
      └─────────────┘  └───────┬────────┘   └─────────────────┘
                               │ 出网（白名单）
                               ▼
                     企业微信 API / SMTP / ERP（二期）
~~~

进程职责：

- **api**：HTTP 接口、鉴权、权限判定、业务事务、生成预签名 URL；不做重活。
- **worker**：Outbox 投递、定时调度、规则执行、转换任务编排、归档打包、报表生成。
- **converter**：无外网、非 root、只读根文件系统的转换沙箱（LibreOffice 等）。
- **minio / postgres**：状态与文件，独立数据盘与备份。

触发升级的条件：需要多实例部署或独立扩缩容（届时先加 Redis 与共享调度，再评估拆分服务）。

### 4.3 语言与后端框架

| 维度（权重） | TS + NestJS | Java + Spring Boot | Go | Python |
|---|---|---|---|---|
| 团队与 AI 工作流连续性（25%） | ★★★★★ 同旧设计 | ★★☆ | ★★☆ | ★★★ |
| Web/后台工程生态（15%） | ★★★★★ | ★★★★★ | ★★★ | ★★★★ |
| 文件与 Office 生态（15%） | ★★★（exceljs/SheetJS 够用） | ★★★★★（POI 最全） | ★★ | ★★★★ |
| 企业集成（ERP/IM SDK）（15%） | ★★★★ | ★★★★★ | ★★★ | ★★★ |
| CPU 密集性能（10%） | ★★★（交给外部进程，权重低） | ★★★★ | ★★★★★ | ★★ |
| 部署与资源占用（10%） | ★★★★ | ★★★ | ★★★★★ | ★★★ |
| 招聘与长期维护（10%） | ★★★★ | ★★★★★ | ★★ | ★★★★ |

**结论：TypeScript + NestJS（api/worker 同栈）。** 核心理由是延续性：旧项目已验证的契约、迁移、权限矩阵、AI 规范可以整体平移，而 CPU 密集的部分（文档转换、打包、报表）本来就交给外部进程或容器，语言性能不构成瓶颈。

**改用 Java 的触发条件**：公司 IT 标准强制 Java 技术栈；或后续确认由 Java 团队接手维护与 ERP 中间件对接。若确认，建议架构形态不变（模块化单体 + worker），仅替换语言层实现。

**版本建议**：新项目不继承旧项目的 NestJS 11.2.3 锁定，在阶段 0 完成 NestJS 12.x 兼容性 PoC 后直接采用 12.x（见 4.20）。

### 4.4 数据库与数据访问

| 维度 | PostgreSQL 18.x（推荐） | MySQL 8.x |
|---|---|---|
| 约束能力（NOT NULL/CHECK/部分唯一索引/复合外键） | 完整 | 较弱（CHECK 8.0.16 起才支持，部分索引不支持） |
| JSONB 自定义字段（需求 5.12"自定义字段"） | 原生 + GIN 索引 | JSON 支持较晚且功能弱 |
| 窗口函数/物化视图（仪表盘与统计） | 强 | 8.0 起支持窗口函数，物化视图无 |
| 队列能力（SKIP LOCKED） | 原生支持 | 8.0 支持 SKIP LOCKED，但生态与经验少 |
| 中文全文检索 | PGroonga / zhparser（旧设计已做 PoC） | 需分词组插件，体验一般 |
| 运维与团队熟悉度 | 需确认公司是否有 PG 运维经验 | 国内更常见 |
| 迁移与备份工具链 | pg_dump/pg_restore + 时间点恢复 | 成熟 |

**结论：PostgreSQL 18.x**（与旧设计一致），原因不只是 SQL 能力，而是它在本项目里同时扮演事务库、队列、调度表、搜索索引、统计汇总五种角色。

**数据访问：Drizzle ORM + 显式 SQL 迁移**（继承）。备选 Prisma（开发体验好，但复杂 SQL、JSONB 与约束表达力弱）与 Kysely（纯查询构建器，缺迁移与关系建模约定）。

**自定义字段策略**：动态字段存 JSONB（带类型与校验的字段定义表驱动），高频过滤字段用生成列 + B-tree 索引，低频查询用 GIN；报表层通过视图/汇总表访问，不直接在 JSONB 上做复杂聚合。

**大表与归档**：审计日志、消息记录、日报三张表增长最快，触发条件（单表 > 500 万行）时按年分区；归档遵循旧设计的"expand/contract"与不可变历史原则。

### 4.5 队列、缓存与调度

| 候选 | 结论 | 理由与风险 |
|---|---|---|
| PostgreSQL 原生（outbox 表 + job 表 + FOR UPDATE SKIP LOCKED + advisory lock） | **一期采用** | 零新增中间件；事务一致（业务写入与待投递消息同事务）；SKIP LOCKED 并发领取无重复；满足本项目量级 |
| Redis + BullMQ | 暂不采用，设阈值 | 生态成熟、限速/延迟任务方便；代价是新增一个必须备份/监控/调优的有状态组件 |
| RabbitMQ / Kafka | 不采用 | 无跨系统事件流、无高吞吐需求，收益为零、成本为正 |

旧设计的教训与本次差异：旧项目"不实现 Outbox"的前提是**所有副作用都是数据库写入**；本项目有企业微信、邮件、短信等**外部副作用**，必须"至少一次投递 + 幂等消费"，因此 Outbox 强制（见 4.13）。

PostgreSQL 做队列的工程要点（必须写进实现规范）：

- 表结构：outbox(id, event_type, payload jsonb, status, attempts, next_attempt_at, locked_at, locked_by, last_error, created_at)；索引 (status, next_attempt_at)。
- 领取：批量 UPDATE ... WHERE id IN (SELECT ... FOR UPDATE SKIP LOCKED) RETURNING，避免全表扫描与锁等待。
- 可见性超时：locked_at 超时自动回到 pending，防止 worker 崩溃丢任务。
- 重试：指数退避 + 最大次数 + 死信标记（failed），死信必须触发管理员告警。
- 幂等：投递键 = 规则 ID + 实体 ID + 触发窗口（或业务幂等键）；重复领取只发送一次。
- 清理与膨胀：成功的 outbox 行按保留期（如 90 天）归档或删除，配合 autovacuum 参数观察表膨胀。
- 调度选主：pg_advisory_lock 选主，单活调度器；任务定义存 DB（rule/job 表），每分钟 tick 扫描到期项，支持"错过补发"（last_run_at 与窗口比对）。
- 限速：渠道级令牌桶（企微机器人 20 条/分钟等）在 worker 侧实现，跨进程用数据库计数或后续 Redis 实现。

**引入 Redis/Valkey 的切换阈值（写入 ADR）**：出现多实例部署且需要共享锁/缓存；持续外发速率超过 5 条/秒；或 PG 队列/锁竞争在压测中成为瓶颈。任一条成立再引入，并同步评估 BullMQ。

### 4.6 对象存储

| 候选 | 结论 | 说明 |
|---|---|---|
| MinIO（自建，S3 协议） | **一期采用** | 支持预签名直传、分片、版本控制、生命周期；私有化、数据自主 |
| 云对象存储 OSS/COS/OBS | 备选 | 运维最省；取决于公司云策略与数据合规（图纸可能不允许出内网） |
| NAS / 服务器本地盘 | 不单独采用 | 作为"异地副本"角色保留（rclone 同步目标），不做主存储 |

**版本与许可注意事项（阶段 0 必须核实）**：MinIO 社区版近年持续向企业版倾斜（控制台能力收缩、许可为 AGPLv3），需锁定具体版本并评估是否需要替换为 SeaweedFS（Apache-2.0）等 S3 兼容实现。因为全部代码只依赖 S3 协议接口，替换成本可控——这正是"能退得回去"原则的落地。

工程要点：

- **直传**：浏览器/手机向 api 申请预签名 URL（分片），直传 MinIO；api 只登记元数据，不代理大文件流量。
- **网络可达性（易踩坑）**：预签名 URL 中的 host 必须对客户端可达。若实施人员在外网、系统在内网，需要 Nginx 反向代理文件流量或使用双域名策略；此点必须在阶段 0 用真实网络环境验证。
- **目录与键设计**：按 项目/任务/版本/哈希 组织键；同一文件内容哈希去重；原文件名保留在元数据中（中文文件名必须兼容）。
- **版本控制**：开启桶版本控制；"定档"在数据库层锁版，物理层不覆盖。
- **生命周期**：未定档的临时/草稿文件 30 天清理；归档包转低频存储（若用云 OSS）。
- **备份**：异地增量同步（rclone）+ 定期抽检恢复；清单文件记录对象键、哈希、大小。**数据库备份成功不等于文件备份成功**。

### 4.7 在线预览：Office / PDF / 图片

| 维度 | A. 自建转换管道（LibreOffice headless + 结构化渲染） | B. KKFileView | C. ONLYOFFICE Docs | D. WPS 365 / 金山文档中台 |
|---|---|---|---|---|
| 许可 | 自有代码 + LibreOffice（MPL） | Apache-2.0 | AGPLv3 / 商业版 | 商业采购 |
| 部署形态 | 与现有栈同源（容器） | 额外 Java 服务 | 额外重型服务（建议 2C4G 起） | 私有化/云，采购门槛高 |
| Office 预览 | 转换 PDF，版式基本可用 | 开箱 | 体验最好 | 体验最好 |
| Excel 体验 | 转换 PDF 分页较差，需 xlsx 结构化渲染兜底 | 同 A | 接近原生 | 接近原生 |
| 在线编辑 | 不支持 | 不支持 | 支持（Word/Excel/PPT） | 支持 |
| CAD/三维 | 不支持，需另接 | 内置部分支持（覆盖度需 PoC） | 不支持 | 部分支持 |
| 手机端 | 可用（PDF/图片/HTML） | 可用 | 可用 | 可用 |
| 额外技术栈 | 无（转换进程） | Java | Node/C++ 服务 | 采购流程 |
| 结论 | **一期采用（只读预览，对标 ONLYOFFICE 查看体验）** | 作为快速覆盖 CAD/三维的候选（PoC 评估） | 不采用（不支持在线编辑；费用调研转存档） | 不采用（已确认不使用 WPS 等第三方应用/云） |

工程要点（A 方案落地细节）：

- **转换沙箱**：converter 容器无外网、非 root、只读根文件系统、CPU/内存/时间上限、超时杀进程；不可访问数据库与内网（解析不可信文件是高风险面）。
- **产物与缓存**：转换产物键 = 内容哈希 + 参数版本；同一文件只转换一次；定档时预生成（P1），其余按需懒生成。
- **字体**：容器内必须安装中文字体（含常用宋体/黑体映射），否则 PDF 中文乱码或方块。
- **Excel 兜底**：xlsx 走前端结构化渲染（SheetJS 解析 + 表格组件展示），保留多 Sheet、列宽与基础格式；复杂图表以转换 PDF 作为补充视图。
- **失败降级**：转换失败不影响下载；预览页显示"暂不支持在线预览，请下载查看"，并记录失败原因供运维排查。
- **安全**：预览访问必须走鉴权（短时签名 URL + 审计），不允许 MinIO 匿名读取；预览同样计入"下载/查看"审计。

**在线编辑：明确不做（2026-09-17 开会对齐）**

预览在系统中直接查看、不经过第三方软件，且**不支持在线编辑**——在线编辑由此移出范围，ONLYOFFICE Docs / Collabora Online 均不采用。以下内容仅作费用存档（回答此前"第三方组件怎么收费"的评审问题）：

| 候选 | 说明 | 注意 |
|---|---|---|
| ONLYOFFICE Docs（自托管） | Office 兼容性好、与 S3/回调集成成熟；Word/Excel/PPT 在线编辑 | AGPLv3/商业许可需法务确认；资源建议 2C4G 起 |
| Collabora Online（LibreOffice 自托管） | 开源（MPL 系）与商业支持版并存；与 LibreOffice 同源 | 并发与资源占用需 PoC；移动端体验一般 |

设计影响（仅存档，若未来重启在线编辑才适用）：编辑保存的文件**不能覆盖已定档版本**；保存须产生新的草稿版本并走定档/变更流程；编辑会话的权限、并发锁与留痕必须纳入审计。

**（存档）费用参考：第三方编辑组件怎么收费（2026-09-17 官方公开信息，实际以报价为准）**

| 方案 | 免费档 | 商业档 | 计费维度 |
|---|---|---|---|
| ONLYOFFICE Docs | 社区版：免费（AGPLv3）；同时连接数上限约 20；无移动网页编辑、无官方支持 | 企业版：询价制，按同时连接数分档，可选 1 年期或终身授权（含 1 年或 3 年更新与支持）；开发者版（白标/SaaS 集成）配置样例约 3500 美元 | 同时打开的编辑连接数（同一文档两人打开算 2 个连接；达到上限后新文档只读打开） |
| Collabora Online | CODE 开发版：免费，但仅限测试/家用，不适合生产 | 商业版（≤99 用户）：€3 / £2.60 / $3.40 每人每月；100+ 用户走企业版，量价折扣需询价 | 按用户数（不是并发连接数） |

- 折算参考：80 人用 Collabora 商业版约 €2,880/年（约 2.3 万元人民币/年）；ONLYOFFICE 企业版按并发连接数报价，成本与"同时编辑人数"直接相关。
- 结论：本项目**不做在线编辑、不采购、不询价**；以上仅存档备查。若未来业务重启该需求，先评估 ONLYOFFICE 社区版（≤20 并发连接）是否够用，再决定是否询价。


### 4.8 CAD 与三维预览（全项目技术风险最高的单点）

网页端原生查看 DWG 与三维装配体，没有"随手可得"的方案：要么上云，要么买 SDK，要么改流程。候选：

| 候选 | 能力 | 代价与风险 | 结论 |
|---|---|---|---|
| A. Autodesk APS（Model Derivative + Viewer） | 覆盖 DWG/STEP/IGES/STL 等主流格式；查看体验好 | 文件需上传 Autodesk 云 | **已排除**（业务确认：不允许上传第三方云，泄密风险） |
| B. 本地转换（KKFileView 或专用转换组件） | dwg → PDF/SVG 预览；可控、不出内网 | 覆盖度与渲染质量需 PoC；额外服务与调优；三维支持弱 | 二维图纸的优先验证路线 |
| C. 流程约定：设计部门导出预览件 | 二维导出 PDF/PNG、三维导出 glTF 后平台预览 | 零技术风险；增加设计人员操作成本；版本一致性靠流程约束 | 兜底方案，所有项目都可用的保底 |
| D. 商业 SDK 自建查看器（ODA / CAD Exchanger / HOOPS 等） | 能力最强、完全私有化 | 许可费高、开发量大、维护成本高 | 仅当开源方案不满足且自研不现实时再评估 |

**本轮确认的约束与建议**：

1. **硬约束**：数据不出内网，排除一切需要上传第三方云的方案（Autodesk APS 等）。
2. **技术路线已定：自建**（2026-09-17 开会对齐）。3D 建模在线查看优先用开源或现成组件自建查看器（候选：three.js + 开源解析组件，如 occt-import-js / OpenCASCADE WASM、glTF/STL/OBJ 加载器，覆盖格式以样本 PoC 为准）；**若没有满足要求的开源或现成组件，则自研转换与渲染管道**（成本最高，需先用真实样本做可行性验证并单独排期）。
3. **一期保留能力入口**：三维文件支持上传、下载与关联管理（与二维图纸一致）；在线查看按「条件性交付」处理（PoC 达标才承诺上线），默认随二期实施。并列候选路线（供 ADR-008 拍板）：
   - a. 开源组件自建查看器（首选验证）：three.js + 开源解析（glTF/STEP/STL 等）；零外部依赖、数据不出内网；
   - b. 自研转换 + 渲染管道：无合适开源组件时启用，工作量需单独立项评估；
   - c. 设计端导出轻量预览件（glTF/PDF/图片）+ 自建查看器（流程兜底）；
   - d. 仅提供下载，由本地专业软件查看（保底姿态）。
4. 二维图纸：优先用本地转换（B）PoC 验证渲染质量（图层、线宽、中文字体、打印样式）；达标即上线，不达标退化为 c。
5. 触发明确方案的前置条件：拿到格式清单与样本文件、明确交互要求（旋转/剖切/测量）、确认可接受的成本与工具链。
**评审对齐（一期落地决策）**：二维 CAD（dwg/dxf）按「条件性交付」执行——本地转换 PoC 移到阶段 0 完成，样本与格式清单提前到阶段 0 提供（见 8.2 Q4）；达标即一期上线，不达标自动退化为「仅下载」（D2-05 降级路径），不阻塞上线；三维在线查看路线已定（自建，开源优先、无合适组件则自研），默认二期实施。
**待确认清单**：三维文件格式清单与样本文件、交互要求（旋转/剖切/测量）、开源组件评估结论（是否有组件满足覆盖与体验要求）、成本预算（见 8.2 的 Q4）。

### 4.9 前端技术选型

基础栈（继承旧设计，团队零迁移）：

| 层 | 选择 | 说明 |
|---|---|---|
| 框架 | React 19 + Vite 8 + TypeScript 7 | 内部重交互 SPA，无 SSR/SEO 需求；与仓库现状一致 |
| UI | Tailwind CSS 4（仓库现状）+ 自研组件；按需引入 headless primitives（Radix / Headless UI） | 与现有视觉体系一致；避免引入第二套设计语言 |
| 服务端状态 | TanStack Query 5 | 不建立服务端实体副本（继承约束） |
| 表单 | React Hook Form + Zod | 契约与后端同源 |
| 图表 | Apache ECharts | 仪表盘、对比图、大屏一站解决；中文文档与生态最好 |
| 拖拽 | dnd-kit | 看板拖拽（React 19 兼容、可访问性好） |
| 日期 | dayjs | 与 AntD 一致 |
| 国际化 | i18next（预留，不在一期启用） | 需求预留中英文 |

专项组件决策：

**（1）表格（任务主表，需求上限 1 万行）**

| 候选 | 结论 | 说明 |
|---|---|---|
| TanStack Table + TanStack Virtual（headless）+ 自研单元格编辑/粘贴 | **一期采用** | 与 Tailwind 自研视觉零冲突；支持 1 万行虚拟滚动；行内编辑、批量粘贴、列配置自研（约 1~2 周） |
| AG Grid Community | 备选 | 功能强，但社区版缺分组/透视/Excel 导出（属商业版），深度定制反而受约束 |
| Ant Design Table | 备选（需评估主题改造） | 组件完整，但会引入第二套设计语言 |
| Handsontable | 不采用 | 商业授权限制（非商业许可不可用于内部经营系统） |

触发升级条件：出现多级表头 + 列冻结 + 公式 + 透视等重需求时，再评估 AG Grid 商业版。

**（2）看板**：dnd-kit + 虚拟长列表（列内虚拟），卡片状态变更走乐观更新 + 409 冲突提示（继承旧设计的"更新冲突"处理约定）。

**（3）甘特图（二期，已确认移出一期）**

| 候选 | 能力 | 许可/成本 | 结论 |
|---|---|---|---|
| 自研（SVG，内嵌 React） | 计划/实际双条、里程碑、依赖箭头、缩放、拖动改期、延期高亮、导出 | 自有人力 | **二期实施**；一期任务进度以表格与看板呈现，不交付甘特 |
| DHTMLX Gantt | 功能全（依赖、关键路径、资源、基线） | 商业（按开发者收费） | 若二期确认需要基线/资源平衡则采购 |
| Bryntum Gantt | 能力最强 | 商业，价格最高 | 不采用 |
| frappe-gantt | 简单轻量 | MIT | 起点参考，但缺关键路径与虚拟化 |
| SVAR React Gantt | 中等 | GPLv3（商用需商业许可） | 不采用（许可不便） |

自研范围（二期启动时）：行虚拟化、日/周/月缩放、计划与实际双条、里程碑菱形、FS 依赖箭头、拖动改期（含依赖校验）、延期高亮、导出图片。关键路径（CPM）与资源平衡在前述范围内一并评估。

**（4）大屏**：独立全屏路由 + ECharts + 自动轮播，不引入独立技术栈。

工程约束（继承旧设计）：服务端数据唯一真相；静态资源哈希缓存；写操作冲突返回明确 409 并保持用户输入；错误边界与骨架屏；组件级权限只做展示控制（服务端强制）。

### 4.10 移动端
> 已确认：**一期不做移动端专项**，桌面端优先；本节内容作为二期评估输入保留。

| 候选 | 结论 | 理由 |
|---|---|---|
| 响应式 H5 + 企微工作台 | **二期再评估（一期不做专项）** | 一期桌面端优先；Web 保持基础响应式即可，不做移动专项优化 |
| 原生 App | 不采用 | 无推送/离线强需求，维护成本数倍 |
| 小程序 | 备选 | 需要额外主体、审核与开发；若后续要求"拍完即传、扫码开单"再评估 |

关键能力（二期清单，需求文档已明确）：

- 移动优先页面：日报填报（拍照、多图、施工人数）、任务详情与状态更新、问题处理、审批、文件预览、我的工作台。
- 拍照上传：客户端压缩（长边 2048px）+ 并发上传 + 失败重试；缩略图先传后补原图。
- 弱网草稿：IndexedDB 本地草稿 + 后台重试队列（需求 5.6.1 明确"弱网下草稿本地保存"）。
- 大文件：S3 分片上传 + 断点续传（图纸/三维常见几百 MB）。
- 企微集成：网页授权免登、JS-SDK（拍照/定位/扫码按需申请）、工作台应用入口；需企业微信管理员配合配置可信域名与 IP 白名单。
- 兼容性测试基线：企微内置浏览器（Android X5、iOS WKWebView）+ Chrome/Edge + Safari。

### 4.11 认证与组织（按仓库现状修订）

| 候选 | 结论 | 说明 |
|---|---|---|
| 公司统一 SSO（Casdoor，OIDC + PKCE） | **一期唯一登录方式** | 公司已上线并完成本地沙箱与公司环境实测（见仓库 SSO 方案 / 接入标准 / 实测报告）；本系统只做 OIDC 客户端 |
| 企业微信登录 | 由 Casdoor 承接（Provider） | 企微是账号来源之一，本系统不直连企微做登录 |
| 本地账号 / 密码 | 不建 | 统一由 SSO 管理；管理员应急通道由 Casdoor 负责 |
| AD/LDAP、钉钉、飞书 | 由 Casdoor 侧对接 | 本系统无感，不增加开发面 |

设计要点：

- 后端实现标准 OIDC 客户端（authorization_code + PKCE + state，JWT-Custom 五字段：Name=工号 / DisplayName / Email / Id / Owner）；前端当前由 Vite 中间件承载的 /auth/login|callback|me|logout，在生产必须迁到后端实现（仓库文档已明确）。
- 会话：生产用 HttpOnly Cookie + CSRF 同步 Token；会话表在 PostgreSQL（不存原始凭据）；登出同时清理本地会话并跳转 Casdoor 统一登出。
- 授权独立：Casdoor 的角色 / 权限默认为空，业务权限（项目、任务、字段级）仍由本系统权限模型负责，不依赖 SSO 下发角色。
- 组织同步与离职回收：依托 Casdoor + 企微通讯录（离职自动禁用），本系统按 工号 / 用户 ID 建立本地用户映射表。
- 未认证一律 401 并前端自动跳转 SSO；本系统不出现自建登录页。
### 4.12 权限模型

模型分层（需求 8.1 的落地）：

~~~text
角色（Role）
  ├─ 功能权限：模块/操作（查看、编辑、删除、导出、下载、审批）
  ├─ 数据范围：全部 / 本部门 / 我参与的 / 我负责的 / 自定义项目集
  ├─ 记录权限：项目成员制（非成员不可见，404 语义）
  ├─ 字段权限：字段可见 / 只读 / 隐藏（脱敏）
  └─ 时效：临时授权（到期自动收回）、外链（限时/限权/水印）
~~~

| 候选 | 结论 | 理由 |
|---|---|---|
| 自研策略层（PolicyDecisionPoint + 策略表） | **采用** | 字段级 + 脱敏 + 导出控制 + 审计留痕本来就是自研胶水；自研可单测、可 CI 矩阵化 |
| Casbin 等策略引擎 | 不采用 | DSL 学习与调试成本高；省下的代码量有限；与本项目的"数据范围/字段策略"模型对齐成本高 |

必须固化的不变量（写进仓库规则与 CI）：

1. 权限判定只在服务端执行，前端隐藏只是体验优化。
2. 无权限的字段**不返回**（不做"前端打码"）；导出、搜索投影、消息内容、统计接口全部走同一策略。
3. 资源不存在与无权访问统一返回 404（防 IDOR 探测）。
4. 导出/下载/预览/外链生成全部写审计；越权尝试单独留痕并告警。
5. 权限矩阵为 CI 门禁：每条路由至少要有一组允许用例与一组拒绝用例（继承旧设计的 permissions.matrix 做法）。

**范围例外（2026-09-17 开会对齐）**：一期流程节点体系不做权限控制——节点操作不按角色区分（项目内可见即可操作），但所有节点完成/回退/增删全程留痕；其余权限（项目/文件/字段/导出）按本节不变量执行。权限模型为节点操作预留策略位，二期随审批场景启用。

性能说明：本项目策略数量小（角色 < 30、字段策略 < 200），策略全量加载内存 + 版本号失效即可，不需要复杂缓存层。

### 4.13 自动化引擎（本项目正确性的核心）

结构：触发器 → 条件 → 动作 → 投递，四段式，管理员可配置（需求 4.5）。

| 环节 | 设计 |
|---|---|
| 触发器 | ①记录事件（创建/更新/状态变化/字段变化，附变更前后 diff）；②定时（每日 08:00/10:00、每周一 09:30、日期前后 N 天）；③手动/批量补跑 |
| 条件 | 字段非空/为空、集合包含、枚举、数值/日期比较，与或非组合；变量引用记录字段 |
| 动作 | 发消息（渠道）、更新字段（R01 变更关联）、创建待办、触发审批（延期申请 A06）、调用 Webhook（预留） |
| 投递 | Outbox → worker 发送 → 结果回写；失败重试、降级、告警 |

执行保证（逐条对应需求与非功能要求）：

- **幂等**：执行键 = 规则 ID + 实体 ID + 触发窗口（如日期窗口、状态版本），重复触发只执行一次；状态回退可再生新窗口（解决"重复轰炸"）。
- **补发**：调度器记录 last_run_at；服务重启或错过窗口时按"应执行清单"补齐；提醒类规则允许在配置窗口内补发（需求：错过的提醒可补发）。
- **重试**：Outbox 指数退避 + 最大次数 + 死信告警。
- **合并与免打扰**：同一人同时间段多条消息合并；免打扰时段静默（次日补发）；每人每日上限。
- **可解释**：每条规则保存命中记录、条件求值结果、动作结果、发送渠道与时间（需求 6.4"全量留痕"）。
- **时间语义**：统一按 Asia/Shanghai 计算，存储用 UTC；节假日/调休日历表参与"顺延"判断；T-1/T+1 以任务当前日期字段实时求值（日期被修改后，未发送的提醒按新日期重新计算，已发送的不撤回）。

**为什么必须上 Outbox（与旧设计的关键差异）**：旧项目所有副作用都是数据库写入，事务即可保证；本项目的消息要出网，进程崩溃、渠道限流、网络抖动都可能丢消息。正确做法是"业务事务内写 outbox，worker 至少一次投递 + 消费幂等"，宁可重复一次也不丢失（重复由幂等键消除）。

可测试性（工程要求）：时钟注入（禁止在规则代码里直接取系统时间）、规则回放（给定数据集与时间点，输出应发送清单）、金标用例（原文 7 条规则的消息文案逐字比对，需求附录 C.1 已给出模板）。

### 4.14 消息通道

| 通道 | 用途 | 结论与前提 |
|---|---|---|
| 企微自建应用消息 | 个人提醒（到期/启动/超时/成果文件/审批） | **一期主渠道**；需企业管理员创建应用、配置可信域名与 IP 白名单 |
| 企微群机器人 Webhook | 群播报（喜报、阶段达成、日报汇总） | **一期采用**；每个群机器人有频率限制（文档为 20 条/分钟，需实测确认），必须队列限速 |
| 站内信 | 全量兜底与审计 | 必须有；轮询模式（继承旧设计） |
| 邮件 | 周报/风险推送（A12，二期）、导出外发、外部干系人 | **一期不做**（预留适配位）：发信涉及域名、退信与运维成本，随二期周报/风险推送一起启用（优先评估公司邮箱 SMTP 中继） |
| 短信 | 紧急升级 | 一期不做（成本与合规），保留接口 |
| 钉钉 / 飞书 | 其他部门未来可能使用 | 不实现，仅保留 ChannelAdapter 适配位 |

投递语义：至少一次 + 幂等 + 渠道限速 + 失败降级（企微失败 → 站内信保底）+ 管理员告警（连续失败/死信）。

配套管理能力：项目 ↔ 群绑定维护（需求待确认第 10 条）、消息模板管理（文案以需求附录 C 为准）、发送记录查询、管理员重发。

### 4.15 搜索

**结论：PostgreSQL + PGroonga + 脱敏搜索投影**（继承旧设计）。

- 范围：项目、任务、文件（名称/类型/关联）、日报、问题、干系人、变更记录。
- 架构：各业务模块写入时同事务维护统一投影表（含权限归属列），搜索只查投影；无权限数据在 SQL 层过滤。
- 中文检索：TokenBigram 满足短词与编号检索；旧设计已建立"金标查询 + Recall@20 门禁"的验收方式，直接复用。
- 不引入 Elasticsearch：数据量级（百万行以内）与并发都不构成理由，额外集群的运维成本纯属浪费。

### 4.16 外部集成

| 集成对象 | 方向 | 结论 |
|---|---|---|
| ERP | 读取（采购/发货/验收节点） | 一期只留适配层与快照表，不实现；待确认 ERP 型号与接口方式（见第 8 章） |
| 企业微信 | 双向（登录/通讯录/消息） | 一期实现（见 4.11、4.14） |
| 邮件 | 推送 | 预留（二期启用，见 4.14） |
| Autodesk | 嵌入查看 | **已排除**（业务确认：不允许上传第三方云）；三维方案见 4.8 |
| SSO/OIDC | 预留 | 不实现，接口预留 |

ERP 集成设计要点（即使二期做也要先定边界）：只读、不改写 ERP；同步到本地快照表（erp_project_node），对比图基于快照渲染；同步失败不影响主系统；接口方式按 ERP 能力排序：开放 API > 只读数据库视图 > 中间表 > 导出文件导入。

### 4.17 可观测性与运维

| 关注点 | 方案 |
|---|---|
| 应用日志 | pino JSON → stdout（继承）；包含 requestId、用户、路由、耗时；严禁记录 Cookie/密码/Token |
| 健康检查 | live（不查 DB）与 ready（DB + 迁移版本）；继承旧设计 |
| 业务指标 | 新增：Outbox 积压量与最老消息年龄、投递成功率、规则执行失败数、转换队列长度与失败率、存储容量与增长 |
| 系统指标 | CPU/内存/磁盘/服务存活；一期用轻量采集 + 企微群告警，二期可上 Prometheus + Grafana |
| 告警通道 | 管理员企微群（复用消息通道，避免再造一套） |
| 运维页面 | 仅管理员：备份状态、队列积压、调度健康、存储用量、最近失败任务 |

**备份与恢复（比旧设计多一层"文件一致性"）**：

1. 数据库：每 12 小时加密备份（pg_dump custom + 排除 Session 数据），异机保留，SHA-256 校验；RPO ≤ 24h、RTO ≤ 2h（以实测为准）。
2. 文件：MinIO 版本控制 + 每日增量同步（rclone）到独立位置（NAS/另一台机器/云），清单文件记录对象键、哈希、大小。
3. 一致性：恢复演练必须同时恢复数据库与文件，并抽检"任务 → 附件 → 内容哈希"链路；**只恢复数据库的备份不算合格备份**。
4. 演练：每月一次恢复演练（与旧设计相同节奏），记录实测 RPO/RTO。

**可用性口径（把「核心工作时间可用性 ≥99.9%」变成可验收指标）**：

- 统计窗口：工作日 8:30–18:30（按 D5 工作日历），按月统计；剔除提前 ≥24 小时通知的计划维护窗口（每月 ≤1 次）。
- 单机现实：单次故障恢复按 RTO ≤2h 计，一次计划外中断即可击穿当月 0.1%（约 13 分钟）余量；建议按「计划外中断次数 + 恢复时长」联合验收，而不是纯时长占比。
- 该口径需在验收前与业务/IT 书面确认（见 8.2 Q19）。

### 4.18 部署与容量起点

| 环境 | 配置建议 | 说明 |
|---|---|---|
| 试点（实施部门，≤ 80 用户） | 1 台 8C16G + 500G SSD 系统盘 + 2TB 数据盘 | 全部组件（Nginx/api/worker/converter/PG/MinIO）同机，先用起来 |
| 生产（300 用户） | 应用节点 8C16G + SSD；存储节点 4C8G + 2~4TB（MinIO 独立） | 数据库可与应用同机，文件必须独立盘并异地备份 |
| 转换资源 | converter 限制 2C4G、并发行 2~4 | 防止 LibreOffice 转换吃光整机 |

约束（继承旧设计的生产门禁）：仅 Nginx 暴露端口；容器非 root；镜像精确版本 + digest；迁移独立任务、API 不自动迁移；Secrets 走文件挂载；CPU/内存/pids 限额；数据库连接池有界。

**待确认（IT 环境）**：Linux 发行版与版本、是否允许 Docker/Compose、虚机申请流程、域名与证书、出口网络策略（企微需要出网；ERP 二期再说）、是否已有 NAS 或对象存储、备份介质与异地存放点。

### 4.19 测试与 CI

测试金字塔继承旧设计：Vitest 单元 + PostgreSQL 集成测试（真实约束与并发）+ Playwright 关键路径。CI 采用面向 3 人团队的轻量分级策略，完整方案见 7.5；仓库现状已有 GitHub Actions 对 frontend 做 typecheck + build、对 shared 做契约漂移检查、对 server 做门禁（typecheck + vitest + 边界规则）（PR/main），即 L2 雏形，集成测试与空库迁移按 7.5 继续扩展。

本项目新增的必测项：

| 新增测试 | 内容 |
|---|---|
| 文件管道 | 上传 → 转换 → 预览 → 下载 → 归档全链路；中文文件名；大文件分片；断点续传 |
| 恶意文件 | 带宏/畸形/超大文件的隔离与超时；转换容器不触网、不访问内网 |
| 规则时间语义 | 时钟注入；T-1/T+1、跨天、跨月、节假日顺延、错过补发；文案逐字断言 |
| 消息投递 | 渠道限流、失败重试、死信、幂等去重、免打扰、合并推送、降级站内信 |
| 权限矩阵扩展 | 字段级可见性、记录级、导出/下载/预览/通知内容的一致性（无权限字段绝不出现） |
| 弱网与大文件 | Playwright 网络节流下的日报提交与文件上传 |
| 恢复演练 | 数据库 + 文件 + 一致性抽检的脚本化演练 |

### 4.20 版本基线（新项目重新定版，不照搬历史锁定）

| 组件 | 建议基线 | 说明 |
|---|---|---|
| Node.js | 24.x | 与旧设计一致；engines 与 CI 固定 |
| pnpm | 11.x | packageManager 固定，lockfile 提交 |
| NestJS | 12.x | **新项目直接评估 12.x**（旧项目锁 11.2.3 是历史原因，不继承） |
| React / Vite / Ant Design | 19.x / 8.x / 6.x | 阶段 0 做兼容矩阵验证 |
| TypeScript | 5.9.x 严格模式 | 全文同上 |
| Zod / OpenAPI | 4.x / 3.1 | Code-first，Schema 唯一真相 |
| PostgreSQL | 18.x（精确补丁） | 与旧设计一致 |
| Drizzle ORM | 阶段 0 评估最新稳定版（含 1.x GA 状态） | 不受旧项目 0.45.x 锁定约束 |
| Nginx | 1.30.x | 生产锁定补丁与 digest |
| MinIO / LibreOffice / ECharts / dnd-kit | 锁定具体版本 | 纳入镜像 digest 门禁 |

规则：禁止 latest/next/beta/rc 进生产；依赖升级走 PR + 人工确认；主版本升级必须新增 ADR 并跑完整回归（继承旧设计 0.1 节）。

### 4.21 流程节点体系与流程蓝图（2026-09-17 开会对齐，一期）

**先回答：这里的「蓝图」是什么**——指**项目全流程蓝图**：把公司标准的项目执行过程（九阶段：售前规划 → 设计开发 → 加工采购 → 组装发货 → 硬件实施 → 软件部署 → 试运行 → 生产阶段 → 验收）连同每个阶段下的**流程节点、节点顺序、节点约束（含必交成果物）**整体定义成一份可复用的标准模板。不是 CAD/工程图纸意义上的「蓝图」，也不是外部工具（Visio/WPS/Excel）的流程图文件——**格式由本系统自建**。

| 维度 | 结论 |
|---|---|
| 模板与实例 | 蓝图是标准定义（模板），**不可在项目里随意编辑**；项目按蓝图导入后生成自己的流程节点实例，节点可按模板规则**增删**（增删留痕、受约束校验）；一期增删仅限模板节点池，自定义节点后续版本再议（可能牵涉管理员权限） |
| 快速蓝图导入 | 新建项目时从蓝图库一键导入，自动生成整套阶段/节点/约束；支持导入自建的蓝图 JSON 文件（导入蓝图库或直接建项目） |
| 蓝图格式（自建） | 自定义 JSON schema：schemaVersion + 蓝图版本号 + 适用条件（字段保留；一期全局一份、暂不启用）+ 阶段数组 + 节点数组（含顺序、约束、必交成果物引用）+ 校验规则；导出/导入同格式、round-trip 无损 |
| 导入校验 | schema 校验 + 引用校验（成果物类型、字典项必须存在）+ 幂等（同一蓝图版本重复导入不重复生成节点）；导入留痕（谁、何时、从哪个蓝图版本） |
| 节点约束 | 每个节点至少有一类约束（必交成果物 / 完成条件 / 前置节点 / 时限）；约束是数据（可配置），不写死在代码里 |
| 完成门禁 | 节点声明了必交成果文件时：**未满足不允许点「完成」**——前端置灰并给出原因，服务端在事务内强校验并拒绝（防绕过），绕过尝试留痕；完成/回退/增删节点全部写审计 |
| 为什么自研 | 节点模型是业务数据 + 校验规则，不是通用流程编排；不引入 BPMN/工作流引擎（见 ADR-015），门禁规则可直接单测与金标回放 |
| 权限 | 一期该体系**不做权限控制**（不区分角色、只留痕）；权限位预留、二期可加（见 4.12 范围例外） |
| 与自动化关系 | 节点状态变化（完成/逾期/缺件）是自动化规则触发器之一；门禁管「不能完成」，提醒规则管「催办」，两者互补 |

**口径确认（2026-09-17）**：蓝图即本节定义的项目全流程蓝图（定义与自建格式已确认）、**全局一份**；首页分类的「地区」= 项目落地地区；一期节点增删仅限模板节点池（自定义节点后续再议）；**蓝图变更不影响已生成项目**（导入即快照，项目内调整走增删并留痕）。

**待确认（业务侧，见 8.2 Q28）**：节点约束细则（哪些节点必交哪类成果文件、能否回退）。

---

## 5. 决策汇总表（含切换触发条件）

| # | 层次 | 选择 | 备选 | 切换触发条件 | ADR |
|---|---|---|---|---|---|
| 1 | 交付形态 | 自研 | 低代码过渡 | 战略要求 3 个月内上线且允许数据在第三方 | ADR-001 |
| 2 | 架构形态 | 模块化单体 + api/worker 双进程 + 转换容器 | 单进程 / 微服务 | 需要多实例部署或独立扩缩容 | ADR-002 |
| 3 | 语言框架 | TypeScript + NestJS 12 | Java + Spring Boot | 公司 IT 标准强制或团队交接给 Java 团队 | ADR-003 |
| 4 | 数据库 | PostgreSQL 18 | MySQL 8 | 公司 DBA 标准强制 MySQL 且不可协商 | ADR-004 |
| 5 | 数据访问 | Drizzle + 显式迁移 | Prisma / Kysely | 阶段 0 评估后确认（含 1.x GA 状态） | ADR-004 |
| 6 | 队列与调度 | PG 原生（Outbox + SKIP LOCKED + advisory lock） | Redis + BullMQ | 多实例部署 / 外发 > 5 条/秒 / PG 竞争成瓶颈 | ADR-005 |
| 7 | 对象存储 | S3 抽象 + MinIO | 云 OSS / SeaweedFS | MinIO 许可或功能不满足 / 公司云策略变化 | ADR-006 |
| 8 | 文件预览 | 自建转换管道，只读、系统内直接查看（对标 ONLYOFFICE 查看体验）；不支持在线编辑 | KKFileView | CAD 覆盖需求扩大且 PoC 优于自建 | ADR-007 |
| 9 | 三维/CAD | 二维 CAD：条件性交付（阶段 0 PoC 达标即上线，否则退化仅下载）；三维在线查看：**自建路线**（开源/现成组件优先，无合适组件则自研；硬约束：数据不出内网） | 设计端轻量预览件 / 商业 SDK（仅无开源方案时评估） | 阶段 0 CAD PoC 不达标 → 一期降级「仅下载」；三维样本评估无合适开源组件 → 启动自研评估或暂缓 | ADR-008 |
| 10 | 消息通道 | 企微应用消息 + 群机器人 + 站内信 | 钉钉/飞书/邮件扩展 | 其他部门使用不同 IM | ADR-009 |
| 11 | 认证 | 接入公司统一 SSO（Casdoor，OIDC + PKCE；企微为账号来源） | 自建企微 OAuth + 本地账号兜底 | SSO 不可用或不覆盖外部干系人时启用兜底 | ADR-010 |
| 12 | 权限 | 自研策略层 | Casbin | 策略规模爆炸（角色 > 100 或出现策略 DSL 需求） | ADR-011 |
| 13 | 前端 | React 19 + Vite 8 + Tailwind 4 + 自研组件（headless：TanStack Query/Table + RHF/Zod + ECharts + dnd-kit）；甘特二期 | AG Grid（表格）/ DHTMLX（甘特） | 表格需求超出自研范围；甘特二期启动 | ADR-012 |
| 14 | 部署 | Docker Compose 单机 | 云托管 / K8s | 用户量或可用性要求显著提升 | ADR-013 |
| 15 | CI 与质量门禁 | 轻量分级（本地钩子 + 合并前检查 + 夜间任务） | 自托管 runner / 纯本地脚本 | 团队扩大或发布频率提升 | ADR-014 |
| 16 | 流程节点与蓝图 | 自研节点体系 + 自建蓝图 JSON schema；不引入 BPMN/工作流引擎；节点约束与成果文件门禁由服务端事务内强制 | 轻量工作流引擎 / 外部流程格式导入 | 出现跨系统流程编排、多套蓝图并行编辑冲突或需要可视化流程设计器 | ADR-015 |
| 17 | 首页分类 | 项目主数据扩展（地区/项目类型）+ 首页侧边栏多维筛选（筛选态入 URL）；不建独立报表门户 | 独立统计门户 | 分类维度超过 5 个或业务要求自定义维度 | ADR-016 |

---

## 6. 与旧设计的关键差异与风险

| # | 差异 | 风险 | 对策 |
|---|---|---|---|
| 1 | **Outbox 必须实现**（旧设计明确不实现） | 消息丢失或重复轰炸 | 同事务写 outbox + 至少一次投递 + 幂等键 + 死信告警 |
| 2 | **对象存储与文件生命周期是新领域** | 备份缺口、磁盘打满、越权访问 | S3 抽象、版本控制、独立数据盘、异地同步、容量与审计告警 |
| 3 | **外部渠道不可控**（企微限速/审核/变更） | 提醒大面积失败 | 速率限制、降级站内信、失败告警、渠道适配层 |
| 4 | **预览转换是重资产** | 转换服务打满 CPU 或成为攻击面 | 独立容器、资源限额、无外网、超时、缓存、失败降级 |
| 5 | **定时任务正确性是新的验收维度** | 漏发/错发引发业务不信任 | 时钟注入、补发机制、金标用例、执行留痕 |
| 6 | **字段级权限贯穿导出/搜索/通知** | 隐性越权泄露 | 服务端统一策略、无权限字段不返回、CI 矩阵覆盖各出口 |
| 7 | **移动端一期不做，但现场填报需求仍在** | 一期桌面端先行；二期补现场体验 | 页面与接口保持响应式基础与移动友好约束，避免二期返工 |
| 8 | **流程节点门禁是业务正确性新维度**（无成果文件不能完成） | 绕过门禁或规则不齐导致管理失真 | 服务端事务内强校验 + 留痕 + 金标用例；节点约束与门禁细则一次对齐《系统功能书》V1.1 |

---

## 7. 一期落地建议（实施部门）

### 7.1 仓库结构（按仓库现状修订）

现状：仓库已有 frontend/（React + Vite），文档在仓库根（系统功能书 / 前端功能需求 / 字段对照清单 / 开发日志）与 docs/（SSO 等专题），CI 只覆盖 frontend。建议在原仓库上原地演进，不另起 monorepo、不做目录大搬迁（是否引入 pnpm workspace 统一前后端脚本，见 8.2 Q15）：

~~~text
libiaolink/
├── frontend/      现有 SPA 原地演进（React 19 + Vite 8 + TS 7 + Tailwind 4）；接口对接 /api/v1
├── server/        NestJS 双入口：api（HTTP 服务）与 worker（Outbox、调度、规则、转换编排、报表）
├── shared/        Zod 契约与生成客户端、跨进程领域模型与常量
├── database/      schema / migrations / seeds
├── infra/         Dockerfile、compose、nginx、minio、converter
└── docs/          SSO 等专题文档、ADR、权限矩阵、Runbook
~~~

说明：文档按用途分层——仓库根：AI 常读四件套（《系统功能书》《前端功能需求》《字段对照清单》《开发日志》）＋两篇技术设计（技术设计v0.1-选型分析.md、技术设计v0.2-架构与数据模型.md，2026-09-17 入仓）；docs/：SSO 等专题文档、ADR、权限矩阵、Runbook。

注意：仓库 frontend/server/ 下的 Vite 中间件（本地 OIDC 联调用）仅服务开发，生产由 server 承担 OIDC 回调与会话（见 4.11）。

### 7.2 一期范围裁剪

| 功能域 | 一期做 | 一期不做（留接口/模型） |
|---|---|---|
| 项目与任务 | 项目主数据（含地区/项目类型，供首页分类）、任务主表（表格/筛选/批量/分组）、流程节点体系（固定模板、按模板增删、节点约束、成果文件门禁）、流程蓝图快速导入（自建格式，见 4.21）、首页分类侧边栏（地区/项目类型/负责人）、我的工作台、状态看板 | 甘特图、关键路径、人员负荷热力、任务模板（蓝图/节点体系已覆盖模板诉求） |
| 日报与问题 | 日报填报（桌面端）、日报中心、问题自动生成与四态看板、自动分派、超期升级 | 移动端专项、周报/月报自动生成、问题统计深化 |
| 文件 | 上传/下载/版本/定档/回收站、预览（Office/PDF/图片，只读、系统内直接查看）、二维 CAD 预览（条件性，PoC 达标才交付）、变更申请（一期不做变更文件审批：申请即通过、所有人平权）、三维文件上传与下载、下载审计 | 三维在线查看实现（方案已定：自建）、在线编辑（明确不做）、归档包导出、外链分享、水印 |
| 自动化 | R01~R07 + A01/A02/A03/A14；站内信 + 企微消息；规则管理界面 | 其余 A 类规则、短信、Webhook |
| 干系人 | **纳入一期**：台账、销售录入（内部账号）、项目关联、批量导入、隐私字段权限 | 去重合并、维护提醒（二期） |
| 仪表盘 | 项目/任务/问题基础统计与下钻 | 大屏、ERP 对比图、进度统计深化 |
| 平台 | 组织/角色/权限、字典与模板（含流程蓝图库与版本管理：JSON schema 导入/导出、后台维护）、全局搜索、工作日历、审计、备份恢复、运维页 | 自定义字段（二期）、多语言、模板市场、蓝图可视化设计器 |

（本表仅为摘要；一期范围唯一口径 =《系统功能书》V1.1 的「范围矩阵」，发布后本节改为纯引用。）

**首页分类侧边栏（本轮新增）实现要点**：项目主数据补 地区、项目类型 字段（字典化；地区 = 项目落地地区）与常用组合索引；首页侧边栏按 地区 / 项目类型 / 负责人 等维度分类筛选（多选、可组合、计数实时）；筛选态写入 URL 可分享、可收藏；历史项目按规则补录（缺省「未分类」，不阻塞上线）；统计与导出沿用同一筛选口径。

### 7.3 阶段 0 的 8 个必过 PoC + 1 个条件性 PoC（每个都有验收标准）

| # | PoC | 验收标准 |
|---|---|---|
| 1 | 文件直传 + 断点续传 + 转换预览 | 桌面端在上传中断后可续传（含 200MB 级文件）；xlsx/docx/pdf 转换成功率 ≥ 95%，中文不乱码 |
| 2 | 企微打通 | 免登可用；应用消息与群机器人消息均可送达；实测渠道限速并配置队列限速 |
| 3 | 规则时间语义 | R03~R07 金标回放通过（含跨天、节假日、改期、补发）；文案与需求逐字一致 |
| 4 | Outbox 投递语义 | 杀死 worker 不丢消息；重复领取不重复发送；失败重试与死信告警可用 |
| 5 | PG 队列并发 | 50 并发领取无重复无死锁；表膨胀与 vacuum 行为符合预期 |
| 6 | 权限矩阵与脱敏 | 字段级/记录级/导出/搜索/通知五类出口的自动化用例全绿 |
| 7 | 历史数据迁移（Q10 已确认要迁移） | 用真实导出的智能表格数据 + 附件包跑通全量导入；校验报告可读；重复/缺失数据有处理记录 |
| 8（条件性） | 二维 CAD（dwg/dxf）本地转换与渲染 | 真实样本 ≥10 个：图层/线宽/中文字体/打印样式达标率 ≥95%，单页转换 ≤10s；**不达标不阻塞上线**，对应功能降级为「仅下载」 |
| 9 | 流程节点门禁与蓝图导入 | 用真实蓝图（阶段/节点/约束/必交成果物）导入生成项目流程；节点缺必交成果文件时完成操作被拒绝（UI 置灰 + 服务端强校验 + 留痕）；蓝图导出→导入 round-trip 无损；蓝图版本号与来源可追溯 |

### 7.4 工期提示

- 最大不确定项：**文件预览管道**（转换质量与性能）与**数据迁移的脏数据清理**；甘特与三维已移出一期；二维 CAD 为条件性交付（PoC 不达标即降级，不构成工期风险，但样本必须在阶段 0 到位）。
- 次大不确定项：企微配置审批（IT 流程）与 ERP 接口对接（二期）。
- 其余模块（项目/任务/日报/问题/权限/自动化框架）属于旧设计已验证的模式平移，风险可控。
- 本轮新增范围（流程节点体系、蓝图导入、节点门禁、首页分类）属于既有技术栈内的常规开发，不改变工期量级；但节点约束/门禁细则需与《系统功能书》V1.1 的节点与成果物字典一次对齐，避免返工。

---


### 7.5 团队分工与轻量 CI 策略（2 后端 + 1 前端/运维/测试）

> 分工到人、模块归属、契约与文档责任、阶段 0 PoC 主责与值班安排见仓库根 `团队分工.md`（v1.0，2026-09-17）；本节保留角色层面的分工假设与 CI 策略。

#### 7.5.1 分工建议

| 角色 | 主要负责人 | 关键交付 |
|---|---|---|
| 后端 A（领域负责人，兼技术负责人） | 项目、任务、流程节点与蓝图、日报、问题、干系人、权限与审计、自动化规则引擎 | 领域模型与事务、权限矩阵、ADR 与评审、规则时间语义 |
| 后端 B（平台负责人） | 文件与对象存储、预览转换、Outbox/队列/调度、企微集成、迁移工具、统计导出 | 文件管道、投递可靠性、迁移工具链、运维脚本支持 |
| 前端 C（交付负责人） | SPA、表格/看板、图表、文件/干系人/流程节点/首页分类界面、部署脚本、E2E 与发布流程 | 界面交付、部署与备份脚本、冒烟与回归用例、发布检查单 |

针对该分工的调整建议：

1. **C 一人兼三职，接近上线时会过载**：运维必须脚本化（发布/备份/恢复各一条命令），E2E 框架由 C 维护、用例由功能开发者在提测前补齐，避免测试成为单点。
2. **没有专职测试岗**：用内建质量替代——每张功能卡必须有验收标准与自动化测试（至少集成级）；发布前由非作者完成交叉验收并留记录。
3. **消除 Bus factor = 1**：备份恢复与发布流程至少两人会操作，季度演练轮流执行；Runbook 必须是文字而非口头。
4. **设合并窗口与评审权**：后端 A 兼技术负责人，掌握合并权与 ADR；建议每天 2 个合并窗口、周五冻结，降低 3 人并行冲突。
5. **前端减负有红利**：甘特与移动端移出一期后，C 的产能应优先投入文件管理、干系人界面与仪表盘。
6. **工期预期**：3 人 + AI 代理，建议按 4~6 个月规划一期（含迁移与上线；本轮新增的流程节点/蓝图/首页分类已计入），其中文件管道与自动化是并行瓶颈，尽早启动。

#### 7.5.2 CI：建议保留，但按轻量分级做

结论：**不建议取消 CI，但同意不应该让 CI 拖慢迭代**。团队规模越小、AI 产出越多，越需要机器守住回归底线——真正的解法是把 CI 做小、做快，而不是删掉。仓库现状：GitHub Actions 已在 PR/main 上跑 frontend 的 typecheck + build、shared 的契约漂移检查与 server 门禁（typecheck / vitest / 边界规则）（即下表 L2 雏形）；集成测试与空库迁移按同一分层继续扩展。

| 层级 | 时机 | 内容 | 时间预算 |
|---|---|---|---|
| L1 本地钩子 | 每次提交（pre-push） | 格式化、lint、typecheck、受影响包单测（pnpm 过滤） | 30 秒~2 分钟 |
| L2 合并前 | PR / 合并窗口 | 完整单测 + 从零数据库迁移 + 集成测试（约束、并发、文件管道冒烟） | 5~15 分钟 |
| L3 夜间/定期 | 不阻塞开发 | E2E 关键路径、权限矩阵、依赖漏洞扫描；恢复演练每月一次 | 夜间执行 |

三条不可省的红线（其余都可按需裁剪）：

1. **空库迁移能跑通**：这是生产故障时的救火能力，必须每次合并前验证。
2. **类型与测试基线**：类型错误与回归由机器拦截，而不是上线后由用户发现。
3. **镜像可重复构建**：保证测试通过的版本等于部署的版本。

提速手段（替代砍掉 CI）：只跑受影响范围（monorepo 过滤）、依赖与构建缓存、测试分片并行；E2E 只保留冒烟路径在 L2，全量放 L3；若不想用云 CI，把同一套命令放在公司服务器上跑（自托管 runner 或定时脚本），效果等价、成本更低。

如果确实完全不接入 CI，最低限度是：**合并前必须在一台干净环境执行同一条命令（迁移 + 集成测试）并留痕**——这是不可再退的底线。

---

## 8. 决策状态（2026-09-17 业务确认）

### 8.1 已确认

| # | 结论 | 对方案的影响 |
|---|---|---|
| Q1 | IM 只用企业微信 | 一期只实现企微（应用消息 + 群机器人）+ 站内信；钉钉/飞书仅留适配位 |
| Q2 | ERP 一期不考虑 | 从一期与阶段 0 移除；适配层与快照表仅保留设计位 |
| Q3 | 不允许上传第三方云 | 排除 Autodesk 等云方案；三维与图纸方案必须私有化 |
| Q7 | **已更新（v0.1.4）**：不做在线编辑；预览只读、系统内直接查看，不使用 WPS 等外部应用/云 | 原二期自托管方案作废（见 4.7）；ONLYOFFICE/Collabora 不采用，费用调研存档 |
| Q8 | 账号来源企微 + 接入公司统一 SSO（企微为 Provider）；干系人不开账号 | SSO 登录与组织同步（Casdoor OIDC）；业务授权在本系统；外部干系人只作为台账数据 |
| Q10 | 历史数据要迁移 | 一期纳入迁移工作量；需业务提供智能表格全量导出与附件包 |
| Q11 | 等保/信创暂不考虑，后续接入公司 | 不引入国产化强制约束；保持部署可迁移 |
| Q12 | 默认自研 | 甘特、预览、权限等全部自研；商业组件仅作后续备选 |
| Q17 | 邮件渠道一期不做（预留，二期启用） | 一期通知 = 企微 + 站内信；邮件随管理层周报/风险推送（二期）启用，优先评估公司邮箱 SMTP 中继（见 4.14/4.16） |
| Q20 | 流程节点：有固定模板、不可随意编辑，可按模板增删节点；每个节点有约束；节点要求输出成果文件时，无文件不允许完成 | 一期新增流程节点体系与节点门禁（见 4.21）；完成校验在服务端事务内强制，UI 置灰仅作提示；增删/完成/回退全程留痕 |
| Q21 | 流程蓝图快速导入，格式以系统自建形式 | 自建 JSON schema + 蓝图库 + 版本号；支持导出/导入与后台维护，不做 Excel/外部工具格式导入（见 4.21） |
| Q22 | 预览：系统内直接看、不经第三方软件；不支持在线编辑；查看体验对标 ONLYOFFICE | 预览 = 自建转换管道只读输出（4.7）；在线编辑移出范围；ONLYOFFICE/Collabora 不采用 |
| Q23 | 3D 建模在线查看：开源或现成组件优先，无合适组件则自研 | 自建查看器/转换路线，数据不出内网（4.8、ADR-008） |
| Q24 | 变更管理一期不做审批——明确为「变更最终交付文件」的审批暂且不做（申请即通过、所有人平权） | 变更提交后新版本直接生效、全程留痕，不设审批角色差异；多级审批能力保留模型、二期可启用；C8 审批范围收敛为延期审批（A06 期次待《系统功能书》V1.1 拍板）（见 7.2） |
| Q25 | 权限：流程节点体系一期不做权限控制（不区分角色、只留痕）；全系统权限维持既定设计 | 4.12 增加范围例外；权限模型为节点操作预留策略位，二期可加 |
| Q26 | 首页分类侧边栏：按地区（口径 = 项目落地地区）、项目类型、负责人等分类 | 项目主数据扩展 + 多维筛选（筛选态入 URL；见 7.2） |
| Q29 | 蓝图全局一份；一期节点增删仅限模板节点池；自定义节点（可能牵涉管理员权限）留待后续版本；蓝图变更不影响已生成项目（导入即快照，项目内调整走增删并留痕） | 收口原 Q27（见 4.21）；自定义节点与管理员权限不在一期技术设计范围 |

### 8.2 仍待确认

| # | 事项 | 需要谁 | 时间点 |
|---|---|---|---|
| Q4 | 格式清单与样本文件：二维 CAD 需阶段 0 提供（PoC 前置）；三维含交互要求（旋转/剖切/测量） | 设计部门 + 业务 | 二维：阶段 0（1~2 周内）；三维：二期启动前 |
| Q5 | 服务器与运维标准（发行版、Docker、规格、证书、出口网络） | IT | 部署前 |
| Q6 | 文件体量与保留期（默认按 2~4TB、定档文件长期保留执行） | 业务 + IT | 试点上线前 |
| Q9 | 一期实施部门人数与角色清单 | 业务 | 试点上线前 |
| Q13 | ~~在线编辑许可与资源~~（**v0.1.4 起作废**：已确认不做在线编辑；费用调研转 4.7 存档） | — | — |
| Q14 | 组件库路线：headless 自研（Tailwind + TanStack）为基线，是否引入 Radix 等 primitives、是否回退 AntD 主题化 | 前端 C + 技术负责人 A | 阶段 0 评估后 |
| Q15 | 仓库结构：frontend/ 原地演进（推荐）还是升级 pnpm workspace 统一前后端脚本 | 团队（A 牵头） | 阶段 1 启动前 |
| ~~Q16~~ | 已办（2026-09-17）：两篇技术设计入仓仓库根（技术设计v0.1-选型分析.md / 技术设计v0.2-架构与数据模型.md），走 PR 评审、与《开发日志》同批同步 | — | — |
| Q18 | ERP 型号与接口方式（优先级：开放 API > 只读库视图 > 中间表 > 导出文件） | IT + 业务 | 二期启动前 |
| Q19 | 可用性 99.9% 的统计口径（维护窗口剔除、按「中断次数 + 恢复时长」联合验收） | 业务 + IT | 验收前 |
| ~~Q27~~ | 已收口（v0.1.6）：见 8.1 Q29——蓝图全局一份；一期节点增删仅限模板节点池；蓝图变更不影响已生成项目 | — | — |
| Q28 | 节点约束细则：哪些节点必交哪类成果文件、能否回退/需不需要理由；首页分类：项目类型枚举口径与历史项目补录规则（地区已确认 = 项目落地地区） | 业务 | 阶段 1 |

## 9. 下一步

1. **建立 ADR 草案**：ADR-001 交付形态、ADR-002 架构形态、ADR-003 语言框架、ADR-004 数据库与数据访问、ADR-005 队列与调度、ADR-006 对象存储、ADR-007 预览管道、ADR-008 三维策略（路线已定：自建，开源优先、无合适组件则自研）、ADR-009 消息通道、ADR-010 认证、ADR-011 权限、ADR-012 前端与甘特节奏、ADR-013 部署与容量、ADR-014 CI 与质量门禁、ADR-015 流程节点与蓝图（自研、不引入 BPMN 引擎）、ADR-016 首页分类（主数据扩展 + 侧边栏筛选）。
2. **阶段 0 PoC 启动**（7.3 的 8 项必过 + 1 项条件性 CAD PoC），优先：文件管道、企微打通、历史数据迁移演练；二维 CAD 需先向设计部门取样本（8.2 Q4）。
3. **输出 v0.2（架构与数据模型篇）**：总体架构与进程边界、领域模型与实体关系（含干系人）、流程节点模型与蓝图 JSON schema（含节点约束/成果文件门禁）、首页分类维度与筛选契约、权限模型细化（含节点权限预留位）、文件生命周期状态机、自动化规则 DSL、接口契约与错误模型。（初稿已输出：技术设计v0.2-架构与数据模型.md，2026-09-17）
4. **按 8.2 收集待确认信息**（本轮新增 Q27/Q28 优先），拿到后更新本文档并冻结为 v1.0 选型基线；同步向《系统功能书》V1.1 提出对应修订（流程节点/蓝图/门禁、变更免审批、预览只读、首页分类）。
5. **文档入仓（已办，2026-09-17）**：两篇技术设计已按仓库约定走 PR 进入仓库根目录（与《系统功能书》同级），并同步《开发日志》（见 8.2 Q16）。

---

（本文档为技术选型讨论稿，所有容量数字为估算起点，需用真实数据校准后再做容量承诺。）
