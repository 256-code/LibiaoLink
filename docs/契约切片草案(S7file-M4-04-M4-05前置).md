# 契约切片草案 · S7·file（M4-04 变更 / M4-05 预览前置）

> 提出：lan（后端平台线）· 2026-09-22 · 分支 `codex/contract-preview-audit`
> 评审定案：wmj（PR #103 评审 `pullrequestreview-5273532272`，COMMENT；§15 高风险需非作者批准）——**本文件已按定案改写**，草案与定案的差异见 §三。
> 归属：契约（`shared/`）属 wmj 线 —— 本 PR 是 lan 提请的契约切片，按定案调整后由评审人批准并入（CONTRIBUTING §5 / §15）。

## 一、本切片改了什么（四项，均为「扩枚举 / 加 schema」，无破坏性变更）

| # | 位置 | 改动 | 依据 |
|---|---|---|---|
| 1 | `shared/src/modules/audits.ts` `AUDIT_ACTIONS` | 追加 `preview`（原八值 → 九值） | D2-07「预览计入『查看 / 下载』审计」；v0.2 §4.3 审计范围明列「预览」 |
| 2 | `shared/src/modules/audits.ts` `AUDIT_OBJECT_TYPES` | 追加 `change`（对象 id = changeRequestId） | A4-13~A4-15 变更记录留痕；预览**不新开对象类型**（定案口径：沿用 `file` + `action = preview` + metadata，见 §三） |
| 3 | `shared/src/modules/files.ts` | 新增 `PREVIEW_TARGETS` / `PreviewTargetSchema`（pdf / image / structured，**渲染通道**口径）、`PREVIEW_STATUSES` / `PreviewStatusSchema`（ready / not_ready / failed）、`FilePreviewQuerySchema`（可选 `versionId`）、`FilePreviewResponseSchema` | A4-11 在线预览、A4-06 任意历史版本可预览、D2-01~D2-07；v0.2 §7.2 已登记 `PREVIEW_NOT_READY` / `PREVIEW_FAILED`（200 语义） |
| 4 | `shared/src/openapi.ts` | 注册 `GET /api/v1/files/{id}/preview`（tags=files；查询参数 `versionId` 可选；200 / 404） | 技术设计v0.3 §3.5「GET /files/{id}/preview 预览状态 + 短时签名 URL」 |

**未改**：`ErrorCode` 与 `HTTP_STATUS_BY_ERROR_CODE` —— `PREVIEW_NOT_READY` / `PREVIEW_FAILED` 早已入枚举（200 语义），本切片只把它们变成有契约承载的状态字段。

## 二、状态语义口径（定案后）

| 状态 | 语义 | 响应 | 前端行为 |
|---|---|---|---|
| `ready` | 产物就绪 | 200 + `url` + `expiresAt` + `target` + `pipelineVersion` + `generatedAt` | 内嵌查看器（PDF / 图片） |
| `not_ready` | 尚未生成 | 200 + URL 相关字段全空 | 占位 + 轮询；服务端幂等补投生成任务（按三元组去重），**不引入请求约定** |
| `failed` | 转换失败 | 200 + `reason`（≤ 500 字） | 失败态 + 「暂不支持在线预览，请下载查看」（D2-05） |

- **版本**：查询参数 `versionId` 缺省 = 当前版本；不属于该文件 / 不存在 → 404（A4-06）。
- **缓存键 = 内容哈希 + `pipelineVersion` + `target`**（ADR-007 / v0.2 §5.4 三元组）；`pipelineVersion` = 转换管线版本（转换器实现 + 影响产物的参数 / 模板 / 字体 / 兜底策略，服务端配置下发如 `PREVIEW_PIPELINE_VERSION`；客户端不解析，仅作缓存失效 / 排障）；产物按三元组唯一，转换器或参数变更 → 版本递增 → 缓存整体失效、按需重转。
- **鉴权**：对象存储禁止匿名读取（D2-04）；`url` 一律短时签名（`S3_DOWNLOAD_URL_TTL_SECONDS`，默认 300）。读取面记录级语义随 M4-05 实现（非成员 404）。
- **审计**：`object_type = file`（object id = fileId）+ `action = preview` + `metadata`（versionId / target / pipelineVersion）；**仅「返回 `ready`（下发签名 URL）」的读取写审计** —— `not_ready` 轮询与 `failed` 读取不写（避免轮询噪声）；生成失败记 `preview_artifacts.error`。

## 三、评审定案摘要（wmj · PR #103）

- **三项提请口径**：① `target` 三值够用 —— 按渲染通道定义、**不新开 `svg`**（CAD 产物若为 SVG 归 `image`、为 PDF 归 `pdf`；M4-06 PoC 报告须写明产物格式与通道归属，不达标即降级仅下载）；② `pipelineVersion` + 三元组缓存键按草案表述成立；③ `not_ready` **不引入请求约定**（不采用 `Accept-Preview: generate` —— 懒生成 = GET 的幂等副作用）。
- **两处必须调整（已改）**：① `AUDIT_OBJECT_TYPES` **不加 `preview`**（沿用 `object_type = file` + `action = preview` + metadata，避免「同一 fileId 两种对象类型」的两套检索口径；`change` 保留）；② 补可选查询参数 `versionId`（A4-06）。
- **命名 / 描述（已改）**：`previewedAt` → `generatedAt`（对齐 `preview_artifacts.generated_at`）；`structured` 描述改「一期未启用时 xlsx 走 `pdf` 通道」；`reason` 限长 `max(500)`；`image` 描述补「含矢量 SVG 产物」，`PreviewTarget` 描述不再把 CAD 写死在 `pdf`。
- **文档回写（wmj 线 · PR #105 / Push 135）**：ADR-007 决策区、v0.2 §5.4、v0.3 §3.5 的缓存键已统一为三元组，并补 `target` 三值定义与 `pipelineVersion` 语义 —— 本 PR 不重复改 `技术设计*` / `docs/adr/`。

## 四、连带影响

1. **库侧 CHECK 需随迁移扩值**（M4-05 实现切片，lan 线）：`audit_logs.action` 现约束 `ck_audit_logs_action`（八值，`database/migrations/0013_admin_dict_audit.sql:58`）—— `preview` 落库前需一条只追加迁移 `drop constraint` + 重建（含 `preview`）。`object_type` 侧无枚举约束（仅长度 CHECK），`change` 不需迁移。
2. **生成物**：因审计枚举与预览契约变更而重出（`npm run generate`）—— paths = 60、schemas = 145（含 h6~h8 / M4-01~03 / #106 多项目经理与多负责人的累计，非本切片独占）。
3. **`FileDownloadUrlResponse` 未复用**：预览响应独立成 schema（字段集不同：`versionId` / `status` / `target` / `reason`），避免把下载响应撑成联合体。
4. **前端**（`前端功能需求.md`，px 线）：§3 预览错误码与占位口径已登记「随预览切片补契约」，本切片即该登记项的落地；wmj 建议把「失败态 + 重试」一行改为「失败态 + 降级『请下载』（D2-05）」——用户触发重试一期不做，若要保留请 px 登记为后续卡片（显式接口如 `POST /files/{id}/preview/regenerate`）；联调由 px 按 `FilePreviewResponse` 接线（M4-07 / u4）。

## 五、本切片不含（避免越界）

- `preview_artifacts` 建表、converter 沙箱、worker 投递循环 → **M4-05 实现切片**（lan 线）；converter 运行形态与 `deploy/` 落点（px 线）在实现前对齐。
- 变更（M4-04）的写入 / 读面实现 —— 契约已就位（`ChangeRequest*` 三 schema + 两条路径 + `intent=change`），本切片只补 `change` 审计对象类型。
- `tasks.change_ref`（R01）回写 —— 列与外键已在 0001，实现随 M4-04；**订正（Push 144）**：业务要求「变更关联」列展示多条，已由 `0019_task_change_refs.sql` 改为多值 `tasks.change_refs uuid[]`（追加 + 去重），契约 `Task.changeRef` → `Task.changeLinks`（多条）。

## 六、验证证据

- `shared`：`npm run typecheck` 通过；`npm run generate`（paths = 60 / schemas = 145）→ `npm run check` 零漂移；`npm run build` 通过。
- 契约回放 `shared/scripts/preview-status-replay.mjs`：**15 / 15 PASS**（响应：三态 + `image` 通道接受，非法状态 / 非法目标 / 缺 fileId / 缺 generatedAt / reason 超 500 字拒绝；查询：`versionId` 合法与缺省接受、非 UUID 拒绝；审计：动作含 `preview`、对象类型含 `change`、对象类型**不含** `preview`）。
- `server`：`npm run typecheck` 通过；`npm test` 全绿（无回归）；`check:boundaries` / `check:permission-matrix` 通过。
- CI：本 PR 四 job（frontend / shared / server / database）全绿。

## 七、后续

- 实现顺序：**M4-04 写入 → M4-04 读面 → M4-05 数据层（含 `ck_audit_logs_action` 扩值迁移）→ M4-05 转换器与队列 → M4-05 读 API**。
- 若评审再有命名调整，契约与生成物在同一 PR 同步；lan 侧实现随定案对齐。