# LibiaoLink 团队分工（2 后端 + 1 前端/运维）

- 版本：v1.1（2026-09-17）
- 适用范围：一期（实施部门）的设计、开发、测试、部署与上线支持
- 依据：`CONTRIBUTING.md`（第 5 / 11 / 15 / 16 / 17 节）、`技术设计v0.1-选型分析.md` §7.5（分工假设与轻量 CI）、`技术设计v0.2-架构与数据模型.md` §1.2（模块划分）、`系统功能书.md`（模块 A1~D5）、`前端功能需求.md`

- 维护：由 px 维护；分工、人员或模块归属变化时按第 9 节更新，并同步 `开发日志.md`

## 1. 角色与人

| 角色 | 人 | GitHub | 常驻分支 | 一句话职责 |
|---|---|---|---|---|
| 前端 + 运维（交付负责人） | px | @256-code | `px` | 前端界面交付、SSO 前端接入、部署 / CI / 备份 / 发布、E2E 与冒烟 |
| 后端 A（领域负责人，兼技术负责人） | wmj | @suikiiovo | `wmj` | 领域模型与事务、流程节点与门禁、权限与审计、自动化规则引擎、ADR 与架构评审 |
| 后端 B（平台负责人） | lan | @XSZR341 | `lan` | 文件与对象存储、预览转换、Outbox / 队列 / 调度、企微集成、迁移工具链、统计导出 |

> 角色与人名的绑定于 2026-09-17 确认；如两人互换（后端 A ↔ 后端 B），按第 9 节修订后生效，模块归属以本表为准。

## 2. 后端模块归属（按 `技术设计v0.2-架构与数据模型.md` §1.2）

| 模块 | 职责要点 | 主责 | 评审 |
|---|---|---|---|
| identity/org | SSO 接入、组织与通讯录同步、用户与角色 | wmj | lan |
| project | 项目主数据、成员、阶段、首页分类字段（地区 / 项目类型 / 负责人） | wmj | lan |
| blueprint / node | 蓝图版本化与导入、节点实例、节点约束、完成门禁（缺件拒绝） | wmj | lan |
| task | 任务主表、进度与状态（五态派生：待开始 / 进行中 / 已完成 / 提前完成 / 已延期） | wmj | lan |
| report / issue | 日报填报与中心、问题四态闭环、问题归类字典 | wmj | lan |
| stakeholder | 干系人台账（隐私字段走字段级权限）、项目关联 | wmj | lan |
| automation | 规则定义（R01~R07 + A01/A02/A03/A14）、执行、回放与留痕 | wmj | lan |
| admin（字典 / 审计） | 字典唯一口径、审计留痕、备份恢复、运维页 | wmj | lan |
| file | 文件与版本、定档锁版、变更（申请即通过）、回收站 | lan | wmj |
| preview | converter 沙箱、Office / PDF / 图片 / 二维 CAD 转换与降级（不达标仅下载） | lan | wmj |
| notify | 站内信、企微投递、模板、合并与免打扰 | lan | wmj |
| outbox / 队列 / 调度 | Outbox 投递语义（去重键）、SKIP LOCKED 领取、重试与死信告警 | lan | wmj |
| search / dashboard | 搜索投影、基础仪表盘统计与下钻 | lan | wmj |
| 迁移工具链 | 智能表格历史数据导入、校验报告、脏数据处理记录 | lan | wmj |

依赖规则（写进仓库规则与 CI）：业务模块可依赖平台模块，反向禁止；跨模块禁止直接改表，副作用走同事务 Outbox（见 v0.2 §1.2）。

## 3. 前端与功能书模块的交付人

| 功能书模块 | 交付内容 | 前端 | 服务端 / 其他 |
|---|---|---|---|
| A1 项目总览、A2 任务计划 | 15 列表格、列显隐、四格 Tracker、任务详情抽屉、任务状态五态展示 | px | wmj（任务与状态接口） |
| A3 日报与问题跟踪、A7 日报中心 | 填报、中心、问题看板与详情 | px | wmj（闭环与规则事件） |
| A4 文件与变更管理 | 文件库、预览入口、变更申请与记录 | px | lan（文件管道与变更服务） |
| A5 干系人管理、A6 我的工作台 | 台账、销售录入、工作台待办 | px | wmj（干系人）、lan（统计） |
| B1 人员任务分配表、B2 任务进展看板 | 分配表、看板与拖拽状态 | px | wmj（状态与校验） |
| B4 仪表盘、B5 专项统计 | 图表与下钻界面 | px | lan（统计接口） |
| B3 甘特图、B6 ERP 对比（二期） | 一期只留入口位 | px | — |
| C2 自动化与通知中心、C5 消息中心、C6 全局搜索 | 规则管理界面、消息中心、搜索框 | px | wmj（规则引擎）、lan（投递 / 搜索投影） |
| C3 权限管理、C7 审计与操作日志、C9 字典与模板 | 权限、审计、字典管理界面 | px | wmj（服务端） |
| C1 表单收集引擎、C4 归档与导出、C8 审批中心（一期不启用） | 一期仅保留入口 / 结构 | px | wmj |
| D1 用户与组织、D5 工作日历 | 组织与日历界面 | px | wmj（组织同步、日历） |
| D2 在线预览服务、D3 三维与图纸查看 | 查看器前端、降级提示 | px | lan（转换与预览管道） |

## 4. 契约、文档与评审责任

| 事项 | 主责 | 协办 / 会签 |
|---|---|---|
| API 契约（Zod 契约包 → OpenAPI → 生成客户端，唯一来源，禁止手工改生成物） | wmj | lan、px |
| `前端功能需求.md`（页面交互、数据模型、接口需求） | px | wmj（契约可行性）、lan（文件与预览相关） |
| `字段对照清单.md`（源表 → 页面字段口径） | px | wmj（落库口径） |
| `系统功能书.md`（定档口径；与源文档双处同步） | 变更提出人 | wmj（技术可行性）、px（实现一致性） |
| `技术设计v0.1-选型分析.md`、§7.5 分工与 CI 策略 | wmj | lan、px |
| `技术设计v0.2-架构与数据模型.md`、DDL 与迁移 | wmj | lan、px |
| `开发日志.md`（每次推送一条，格式：日期 时间 \| 作者 \| 分支 \| Push 序号） | 当次推送人 | — |
| 部署 / 备份 / 发布 Runbook、`deploy/` 与 `.github/` | px | lan |
| ADR（架构决策记录） | wmj | 全员评审 |

高风险变更（`CONTRIBUTING.md` 第 15 节：数据库迁移、鉴权与会话、Secrets、对外契约、容器 / CI / 分支保护、依赖引入、测试基座）必须取得另一名合格评审者批准后才能合并；3 人互为评审人，禁止自审自合。

## 5. 协作流程与节奏

- 分支与推送：各自常驻分支（`px` / `wmj` / `lan`）→ PR → `main`（squash-only，源分支保留）；禁止直接推送 `main`；常驻分支不删除。
- 开发日志：每次推送前更新，与改动同一次推送提交；一个 PR 至少一条记录。
- 评审：每个 PR 至少 1 名非作者评审；高风险变更按第 4 节要求批准；PR 描述按 `.github/pull_request_template.md`。
- 合并窗口：建议每日 2 个（11:00 / 17:00），周五 17:00 后冻结非紧急合并；主干保持可发布。
- 联调顺序：契约先行（`shared/` 落地后）→ 服务端实现 → 前端对接；契约变更必须同一 PR 更新 `前端功能需求.md` 与生成客户端。
- 提测与验收：功能开发者在提测前补齐该卡片的自动化用例（至少集成级），交叉验收由非作者执行并留记录；无专职测试岗，用内建质量替代。
- CI 分层：L1 本地钩子（pre-push：格式化 / lint / typecheck / 受影响单测，30 秒~2 分钟）→ L2 合并前（完整单测 + 空库迁移 + 集成测试，5~15 分钟）→ L3 夜间（E2E 关键路径、权限矩阵、依赖漏洞扫描、恢复演练）。三条红线不可省：空库迁移能跑通、类型与测试基线、镜像可重复构建。
- 工期预期：3 人 + AI 代理，一期按 4~6 个月规划（含迁移与上线）；文件管道与自动化是并行瓶颈，尽早启动（`技术设计v0.1-选型分析.md` §7.5.1）。

### 5.1 代码评审路由（`.github/CODEOWNERS`）

- 已按模块细化（2026-09-17 随 Push 25 落地）：`frontend/`、`assets/`、`docs/`、根目录文档（README / 开发日志 / 团队分工 / 前端功能需求 / 字段对照清单 / 系统功能书）→ px（+wmj）；`deploy/`、`.github/` → px、lan；`server/`、`database/`、`shared/` → wmj、lan；`技术设计*.md` → wmj（+px / lan）；`AGENTS.md`、`CONTRIBUTING.md` → 全员。
- 兜底 `*` 仍为全员；每条具体路径至少 2 名所有者，保证「非作者评审」始终有人被自动请求。
- 说明：CODEOWNERS 只做评审路由，不等于合并权限；高风险变更（`CONTRIBUTING.md` 第 15 节）仍需另一名合格评审者批准。
- 后端目录（`server/` / `database/` / `shared/`）尚未创建，对应规则先行登记，落地后自动生效；`AGENTS.md`「分工与路由」章节与本节同口径，AI 代理按此归属。

## 6. 阶段 0 PoC 分工（`技术设计v0.1-选型分析.md` §7.3）

| # | PoC | 主责 | 协办 | 验收证据 |
|---|---|---|---|---|
| 1 | 文件直传 + 断点续传 + 转换预览 | lan | px | 续传演示（含 200MB 级）+ 转换成功率报告（≥95%、中文不乱码） |
| 2 | 企微打通 | lan | px | 免登可用 + 应用消息与群机器人消息送达记录 + 限速配置 |
| 3 | 规则时间语义 | wmj | lan | R03~R07 金标回放记录（跨天、节假日、改期、补发；文案逐字一致） |
| 4 | Outbox 投递语义 | lan | wmj | 杀 worker 不丢消息、重复领取不重发、重试与死信告警演示 |
| 5 | PG 队列并发 | lan | wmj | 50 并发领取无重复无死锁报告 + 表膨胀与 vacuum 观察 |
| 6 | 权限矩阵与脱敏 | wmj | px | 字段级 / 记录级 / 导出 / 搜索 / 通知五类出口自动化用例全绿 |
| 7 | 历史数据迁移 | lan | wmj、px | 真实导出数据 + 附件包全量导入；校验报告可读；重复 / 缺失有处理记录 |
| 8（条件性） | 二维 CAD 本地转换与渲染 | lan | px | ≥10 真实样本达标率 ≥95%、单页 ≤10s；不达标降级为「仅下载」，不阻塞上线 |
| 9 | 流程节点门禁与蓝图导入 | wmj | px | 蓝图导出 → 导入 round-trip 无损；缺必交成果文件时完成被拒（UI 置灰 + 服务端强校验 + 留痕） |

## 7. 运维与值班

| 事项 | 主责 | 备份人 | 说明 |
|---|---|---|---|
| 本地沙箱（`deploy/casdoor/`）维护 | px | lan | 起停、账号、配置快照与公司环境对照见 `docs/` 内 SSO 文档 |
| 环境部署与发布 | px | lan | 按 `CONTRIBUTING.md` 第 16 节：标签指向通过全部门禁的主干 commit |
| 备份与恢复 | px 执行 | lan、wmj | 每日自动备份；恢复演练每季度一次、两人会操作、轮流执行 |
| 告警与故障响应 | px（服务 / 部署类） | lan（数据与投递类）、wmj（领域逻辑类） | 一级故障先止损再复盘，复盘结论进 `开发日志.md` 或 ADR |
| SSO / 企微配置变更 | px 提变更 | wmj 复核权限影响 | 属高风险变更，需他人评审后方可执行 |
| 依赖与漏洞处置 | px | lan | Dependabot 提醒；依赖升级只走独立 PR（`CONTRIBUTING.md` 第 13 节） |

## 8. 单点风险与消减

| 风险 | 消减措施 |
|---|---|
| px 一人兼前端 + 运维，接近上线易过载 | 运维脚本化（发布 / 备份 / 恢复各一条命令）；E2E 用例由功能开发者在提测前补齐 |
| Bus factor = 1（部署、备份恢复、迁移工具） | 每项至少两人会操作；Runbook 文字化；季度演练轮流执行 |
| 契约漂移（前后端口径不一致） | 契约唯一来源；生成物按 `CONTRIBUTING.md` 第 14 节做「无漂移」检查 |
| 文件管道不确定（转换质量与性能） | 阶段 0 最优先启动（lan）；不达标按 v0.1 §7.3 降级为「仅下载」 |
| 权限与审计被绕过 | 服务端是唯一可信校验点；五类出口自动化用例 + 高风险变更人工评审 |

## 9. 修订流程

- 本文件由 px 维护；分工、人员、模块归属或值班安排变化时更新本文件，并在 `开发日志.md` 追加一条记录（`docs` 类型）。
- 与 `CONTRIBUTING.md` 冲突时，以 `CONTRIBUTING.md` 为准（更严格者优先）；本文件只细化「谁负责什么」，不改变分支、评审、发布规则。

## 10. 变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-09-17 | 首版：角色到人（px / wmj / lan）、后端模块归属、前端与功能书模块交付人、契约与文档责任、阶段 0 PoC 分工、运维值班与单点风险 |
| v1.1 | 2026-09-17 | 评审路由落地：`.github/CODEOWNERS` 按模块细化；`AGENTS.md` 新增「分工与路由」章节（AI 代理按此归属），随 Push 25 |

