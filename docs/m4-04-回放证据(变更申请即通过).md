# M4-04 回放证据（S7·file 变更 · 申请即通过）

> 卡片：M4-04「变更（申请即通过）」一期写入切片（主责 lan，评审 wmj）｜口径来源：系统功能书 A4-13（定档后变更须走变更流程，申请即通过、平权）/ A4-14（变更 = 新版本 + 变更记录）/ A4-17（变更统计，读面随后续切片）｜技术设计v0.2 §2.3（`change_requests`）与 §5.1-5.3｜ADR-024 §R01（输出成果文件 = 变更文件 → 自动关联任务）｜契约 shared/src/modules/files.ts（`ChangeIntentBody` / `ChangeRequest`，本卡零改动）。
> 落点说明：`docs/` 属 px 线；本文件由 lan 随 M4-04 写入切片代记（回放脚本与断言同 PR，请 px 复核）。

| 项 | 值 |
|---|---|
| 回放时间 | 2026-09-22 11:19:06 +08:00 |
| 目标 | http://127.0.0.1:3011 |
| 数据库 | postgresql://libiaolink_migrator@127.0.0.1:55432/libiaolink |
| 对象存储 | libiaolink（本地 MinIO 沙箱：仅沙箱、不代表生产选型） |
| 代码版本 | e9c744f（回放时工作区含本卡未提交改动） |
| 执行账号 | 管理员（建项目 / 建任务 / 上传 / 变更）+ 非成员（404 反例） |
| 脚本 | server/scripts/m4-change-replay.mjs |

## 断言明细

| PASS | S0 | api 可用（/healthz + /readyz：含存储探针） | 
  - 期望：200 / 200
  - 实际：200 / 200
| PASS | P1 | 建回放项目（为 file / change 提供 projectId） | 
  - 期望：201 + 项目可见
  - 实际：201 {"id":"7adf2462-c18b-4483-aef9-0b6191a6c464","code":"M4CHG-20260922031904"}
| PASS | P2 | 建第二个项目（跨项目 400 / 非成员 404 反例用） | 
  - 期望：201
  - 实际：201
| PASS | P3 | 快照节点可见（文件挂 nodeId 用；变更阶段默认取节点所属阶段） | 
  - 期望：节点与阶段 key 非空
  - 实际：{"node":"d8d2b47f-2348-45fe-abb5-49222310c6ab","stageKey":"presale"}
| PASS | P4 | 建 3 个任务：两个输出成果文件 = CAD图纸（R01 多值命中）、一个 = 合同（对照） | 
  - 期望：201 × 3
  - 实际：{"a":"665d827b-9abd-49ee-a0dc-d17c39cdc5c6","b":"d46a943a-589e-462d-ae8e-d47a42b5f525","other":"53d5b5e1-7b67-4e2d-badf-5fc325e1ef75"}
| PASS | U1 | 上传文件 A（docType=CAD图纸、挂节点）+ 定档 → 变更目标就绪 | 
  - 期望：200（v1）+ 200（status=final）
  - 实际：{"upload":200,"seq":1,"finalize":200,"status":"final"}
| PASS | C1 | 变更入口放开：intent=change + fileId（目标 final）→ 201 会话 + change_payload 落库 + 不新建文件行 | 
  - 期望：201（intent=change、change_payload 带 reason/摘要）+ files 行数不变
  - 实际：{"status":201,"intent":"change","payload":{"reason":"设计变更（回放）：孔位按变更单 CR-2026-0918 调整","stageKey":null,"afterSummary":"按变更单调整孔位","beforeSummary":"v1 按初版施工"},"filesDelta":0}
| PASS | C2 | 变更目标门禁：非定档 409 FILE_STATE_INVALID / 不存在 404 / 跨项目 400 invalid_file / 非成员 404（防 IDOR） | 
  - 期望：409 FILE_STATE_INVALID / 404 NOT_FOUND / 400 VALIDATION_FAILED（invalid_file）/ 404
  - 实际：{"draft":{"status":409,"code":"FILE_STATE_INVALID"},"missing":{"status":404,"code":"NOT_FOUND"},"cross":{"status":400,"code":"VALIDATION_FAILED","detail":"invalid_file"},"member":{"status":404,"code":"NOT_FOUND"}}
| PASS | C3 | 完成变更上传：200 + changeRequest 视图（applied / 同事务版本）+ 版本挂 change_request_id + 文件 final → changed | 
  - 期望：200 + status=applied + 版本 change_request_id = 变更 id + files.status=changed
  - 实际：{"status":200,"seq":2,"changeRequest":{"id":"1ad94806-cfac-471f-a84e-304ad7204dc9","projectId":"7adf2462-c18b-4483-aef9-0b6191a6c464","nodeId":"d8d2b47f-2348-45fe-abb5-49222310c6ab","stageKey":"presale","reason":"设计变更（回放）：孔位按变更单 CR-2026-0918 调整","beforeSummary":"v1 按初版施工","afterSummary":"按变更单调整孔位","status":"applied","appliedBy":"caa8d763-4b6a-4967-9b26-7d1086272c9c","appliedAt":"2026-09-22T03:19:05.169Z","createdAt":…
| PASS | C4 | change_requests 落库：reason / 摘要 / 申请人 / 变更阶段默认取文件节点所属阶段（stageKey 缺省） | 
  - 期望：status=applied、stage_key=presale、applied_by=管理员、reason / 摘要与请求一致
  - 实际：{"id":"1ad94806-cfac-471f-a84e-304ad7204dc9","project_id":"7adf2462-c18b-4483-aef9-0b6191a6c464","node_id":"d8d2b47f-2348-45fe-abb5-49222310c6ab","stage_key":"presale","reason":"设计变更（回放）：孔位按变更单 CR-2026-0918 调整","before_summary":"v1 按初版施工","after_summary":"按变更单调整孔位","status":"applied","applied_by":"caa8d763-4b6a-4967-9b…
| PASS | C5 | 多态关联写入 change 行（object_type=change，object_id=变更 id） | 
  - 期望：1 行 change:1ad94806-cfac-471f-a84e-304ad7204dc9
  - 实际：[{"object_type":"change","object_id":"1ad94806-cfac-471f-a84e-304ad7204dc9"}]
| PASS | C6 | R01 自动关联：输出成果文件 = 变更文件成果类型（CAD图纸）的任务全部回写 change_ref = 本次变更；不命中任务保持空 | 
  - 期望：两个 CAD图纸 任务 change_ref = 变更 id；合同任务 change_ref = null
  - 实际：{"a":true,"b":true,"other":null}
| PASS | C7 | 变更审计（object_type=change / action=create）+ outbox change.applied（同事务） | 
  - 期望：审计 1 条（changes: final → changed、metadata.linkedTasks=2）+ outbox 1 条（dedupeKey=change.applied:{id}）
  - 实际：{"outbox":{"topic":"change.applied","dedupeKey":"change.applied:1ad94806-cfac-471f-a84e-304ad7204dc9","matchedTasks":2},"audit":{"action":"create","changes":[{"to":"changed","from":"final","field":"status"}],"linkedTasks":2,"stageKey":"presale"}}
| PASS | C8 | R01 无匹配（成果类型 = 评审单，无对应任务）→ 变更仍生效、change_ref 不回写（提示申请人随 M5） | 
  - 期望：200 + changeRequest 非空 + 既有任务 change_ref 均未变
  - 实际：{"status":200,"changeRequest":"70bfafcf-945d-478f-a114-c52df5ef0f12","refs":[null,"1ad94806-cfac-471f-a84e-304ad7204dc9","1ad94806-cfac-471f-a84e-304ad7204dc9"]}
| PASS | C9 | 定档后回溯 = 变更（不再 400）：changeRequest 非空 + 新版本挂 change_request_id + 状态 final → changed + 合同任务 change_ref 回写 | 
  - 期望：200 + changeRequest.status=applied + 版本 change_request_id 一致 + files.status=changed + 合同任务 change_ref=本次变更
  - 实际：{"status":200,"changeRequest":{"id":"a5f15e9f-8b73-4482-9dc1-014743f9da15","projectId":"7adf2462-c18b-4483-aef9-0b6191a6c464","nodeId":"d8d2b47f-2348-45fe-abb5-49222310c6ab","stageKey":"presale","reason":"定档后回退到 v1（走变更）","beforeSummary":null,"afterSummary":null,"status":"applied","appliedBy":"caa8d763-4b6a-4967-9b26-7d1086272c9c","appliedAt":"2026-09-22T03:19:05.998Z","createdAt":"2026-09-22T03:19:05.998Z","fileId":"…
| PASS | C10 | 定档后管控：intent=version 对 changed 文件 → 409 FILE_STATE_INVALID（修改须走变更） | 
  - 期望：409 FILE_STATE_INVALID
  - 实际：409 {"code":"FILE_STATE_INVALID"}

## 汇总

- ✅ 全部断言通过（16 项）：变更入口与载荷落库 / 目标门禁 / 生效链路（变更记录 + 版本 + 状态 + 关联 + 审计 + outbox）/ R01 多值与无匹配 / 定档后回溯 = 变更 / 定档后管控。

## 验收对照（M4-04 写入切片）

- 变更入口 = C1 / C2：`intent=change` 随上传管道提交（技术设计v0.3 §3.5 口径），`ChangeIntentBody` 落 `upload_sessions.change_payload`；目标须 final / changed，其余状态 409 / 不存在 404 / 跨项目 400 / 无可见性 404。
- 变更生效 = C3 / C4 / C5 / C7：完成上传同一事务写 `change_requests`（status=applied、stage_key 缺省取节点阶段）+ 版本挂 `change_request_id` + `files.status=changed` + `file_links`(change) + 审计（object_type=change）+ outbox `change.applied`。
- R01 自动关联 = C6 / C8 / C9：按「任务输出成果文件 = 变更文件成果类型」命中全部回写 `tasks.change_ref`（最近一次变更，覆盖式）；无匹配只记日志、不阻断（提示申请人随 M5 通知）。
- 定档后回溯 = C9（A4-13）：`POST /files/{id}/rollback` 对 final 文件即变更（生成新版本 + 变更记录 + 状态 changed），不再 400。
- 定档后管控 = C10：`intent=version` 对非 draft 文件 409 `FILE_STATE_INVALID`（修改须走变更）。
- 单测回归（不连库）：server/test/file-service.test.ts 随 npm test 常跑：change 入口（载荷规范化 / 目标门禁 / 不新建文件）、change 完成链路（变更记录 / 版本挂接 / 状态 / 关联 / R01 / 审计 / outbox）、R01 无匹配只告警、定档后回溯 = 变更。

## 与后续卡片的关系

- 本切片只落**写入面**（变更申请即通过 + 生效链路）；**读面**（`GET /projects/{id}/change-requests` / `GET /change-requests/{id}`）与**统计**（A4-17）随 M4-04 后续切片，通知（A4-18）随 M5（outbox `change.applied` 已埋点）。
- 既有回放同步切换：`scripts/m4-lifecycle-replay.mjs` 的 L5（定档后回溯）与 `scripts/m4-upload-replay.mjs` 的 U22（change 入口）随本卡改动同步更新，避免旧断言把新行为判失败。
- `server/scripts/m4-lifecycle-replay.mjs` 收尾新增 `change_requests` 清理（定档后回溯会产变更记录）。
- 复跑：cd server && node --env-file-if-exists=.env scripts/m4-change-replay.mjs --out "../docs/m4-04-回放证据(变更申请即通过).md"
