# 字段对照清单：源表 → 项目总览页

- 对照时间：2026-09-17
- 源表：《[XXXX-XXXX] XXX项目名称.xlsx》（4 个 sheet：项目名称 / 日报及问题汇总 / 干系人 / 变更管理）
- 对照对象：项目总览页表格（15 列，支持「字段」按钮勾选显隐，任务描述常显；预计所需天数随开始/预计完成日期联动）+ 任务详情抽屉（始终显示全部字段）+ 首页项目卡片（项目序号徽标）
- 口径补充：2026-09-18 新增项目序号（`seqNo`）定案；同日 Q8 定案——施工人数 / 紧急重要度任务创建 / 编辑均可填（非必填），文件走站内文件库（多类型、不通过第三方应用打开）；2026-09-19 新增「任务模板草稿字段」口径（Push 58，见 §四；无源表对应、当前原型前端本地草稿、正式版落库）；2026-09-19 Push 60 起 §四 的初始内容由业务写死的模板预设生成（`frontend/src/data/templatePresets.ts` 的 `STAGE_TEMPLATE_PRESETS`，九个阶段各一套、硬件实施两套 18 / 11 条）；2026-09-19 Push 61 起项目总览可「从任务模板添加任务」（入口 = 点任务表的阶段标签 → 右侧卡片：标签导航「任务节点 + 各模板」，逐条 / 整套添加）：阶段 = 模板所属阶段，项目经理 = 项目卡片上的经理（项目级字段，不是任务字段），新任务字段默认值 = `待开始` / 进度 0 / 紧急重要度「中」，负责人 / 日期 / 成果文件 / 人数留空待补（接口需求见 `前端功能需求.md` §3.8 A10）；2026-09-19 Push 63 任务编辑弹窗落地（入口 = 任务行悬停铅笔 + 详情抽屉「编辑任务」）：可编辑 = 项目经理（**项目级字段**，保存回写项目、全项目同步）/ 任务负责人 / 开始与预计完成日期（与分类筛选同一套日期区间选择器）/ 预计所需施工人数 / 紧急重要度 / 项目进展描述，预计所需天数随日期区间联动（只读）；任务描述 / 成果文件按 A1-17 锁定，状态 / 进度 / 实际完成日期为派生只读；同一批把演示人员（任务负责人）替换为虚构成员，源表真实姓名只保留在本文档的源表对照说明里；2026-09-19 Push 64 行内编辑落地：同一批可编辑字段（项目经理 / 任务负责人 / 紧急重要度 / 开始日期 / 预计完成日期 / 预计所需施工人数 / 项目进展描述）在任务表里**点单元格直接改**（入口见 `前端功能需求.md` §6.9），改日期同时联动预计所需天数并按新日期重派生状态，字段口径与弹窗完全一致（只是入口不同）；2026-09-19 Push 65 任务状态可编辑 + 与四格进度条双向联动：状态列改色标签（已延期 / 进行中 / 已完成 / 待开始 / 提前完成）+ 行内下拉，点进度条 = 0 格待开始 / 1~3 格进行中 / 4 格已完成（按工期可能提前完成、未填预计完成时间只显示已完成），选状态 = 反向写进度（进行中至少 1 格、已完成与提前完成四格全亮、已延期保持当前格数），手动状态优先于派生（新增任务字段 `statusOverride`）；接口口径待 wmj 决议（见 `前端功能需求.md` §3.8 A12）；2026-09-19 Push 67 实际完成日期可在表格行内直接填 / 清（填 = 完成、四格全亮；清 = 退回进行中、进度 3 格），且把任务状态改成非完成态（待开始 / 进行中 / 已延期）或把进度条退回 1~3 格时**实际完成日期一并清空**（业务定案，避免状态与完成日期自相矛盾）；同批把任务表列对齐改为「表头与列内容居中」（任务描述列保持左对齐），行内可编辑单元格静止态改为白底 + 淡灰描边小胶囊；接口口径见 `前端功能需求.md` §3.8 A13（待 wmj 决议）；2026-09-19 Push 67 起表格默认列按业务截图调整：默认只隐藏「预计所需施工人数」，「文件」「项目进展描述」改为默认显示（默认列见 `前端功能需求.md` §二.4 / §6.6）；同日 Push 67 逾期口径调整：「逾期」标注从「实际完成日期」列移到「是否按时交付」列（未完成超期 = 逾期未交付、完成晚于预计完成日期 = 逾期已交付），且点四格进度条不再把已过预计完成日期的任务从「已延期」改成「待开始 / 进行中」（接口口径见 `前端功能需求.md` §3.8 A14，已于 Push 70 定案）；2026-09-20 Push 70 任务写入口径定案（A12~A14）：任务状态基础三态可写（服务端同事务联动进度 / 完成日期，「已延期 / 提前完成」派生不可写且派生优先）、进度离散五档（写入只收 0 / 0.25 / 0.5 / 0.75 / 1，迁移 / 演示数据小数先归一）、清除实际完成日期 = 进度写回 <1 档（服务端清 `actual_end`）、`onTime` 与「逾期未交付 / 逾期已交付」由服务端读时派生回填（前端不再本地派生）；2026-09-20 Push 91 任务编辑入口收口（业务反馈「这个不需要了 要修改直接在抽屉里面修改即可」）：删除任务编辑弹窗与任务详情抽屉头部的「编辑任务」按钮，**可编辑字段 / 控件 / 保存口径（任务覆盖表）不变**，改为在任务详情抽屉里直接改 —— 选择类控件选完即存、日期区间确定即存、施工人数与进展描述失焦即存（值没变不写，避免无谓刷新项目时间）；2026-09-20 Push 98 进度与卡片口径（业务反馈「改成已完成 / 抽屉里面这个进度也要可以修改 / 卡片里面显示的也是中文表述 / 卡片里面也要可以修改实际完成日期」）：四格进度第 4 档中文档位名由「完成了」改为**已完成**（任务表 / 任务详情抽屉 / 看板卡片三处一致）；任务详情抽屉里的项目进度改为**可改**（进度条长度不变，四颗点**平均分布在条上** = 刚开工 / 完成一半 / 快完成了 / 已完成，联动口径同任务表）；看板卡片的进度由百分比改中文档位，并新增「实际完成日期」一栏 —— 卡片上直接点选小日历改（填 = 完成、清 = 退回进行中，口径同表格行内）。字段本身无新增 / 改名：进度仍是 `0 / 0.25 / 0.5 / 0.75 / 1` 五档，`doneDate` 仍走任务更新接口（`TaskUpdateBody`，A13）；2026-09-20 Push 101 抽屉可编辑性补齐 + 进度条统一绿色（业务反馈「抽屉里仍不能改任务状态 / 实际完成日期 这个也要可以修改」与「为什么是灰的 应该也要绿的」）：任务详情抽屉的**任务状态**改为五态色标签下拉（联动口径同任务表行内 —— 完成态 = 四格全亮、进行中 = 至少一格、待开始 = 清零、已延期 = 保持格数；改成非完成态时实际完成日期一并清空），抽屉的**实际完成日期**改为单日期小日历（填 = 完成、清 = 退回进行中，与行内 / 看板卡片同口径）—— 抽屉里这两项不再是只读派生，只读口径收窄为 是否按时交付（读时派生）/ 输出成果文件 / 文件 / 变更关联；抽屉进度条与条上四颗点统一改成**绿色**（与任务表四格点同色，不再按状态取蓝 / 灰）。字段本身无新增 / 改名（仍是 `statusOverride` / `doneDate` / `progress` 三个既有字段；接口需求见 `前端功能需求.md` §3.8 A17）；2026-09-20 Push 105 看板拖动落点（业务反馈「要有合适的交互显示拖动后的位置」；「拖动要实体化 / 不要虚化」一条同日修正为**不做成实体化**）：拖动中的卡片**样式保持原样**（不淡出、不做抬起的实体态、不换拖影，只换抓手光标）、看板落点改成**列内插入位**（拖到哪两格之间就在哪里浮出绿色虚线槽位「放开：移到「X」」/ 同列「放开：放到这里」，同一列内也能拖着排序），列内顺序存前端看板顺序表（`ProjectDetail` 的 `taskOrder`，原型内存态，与任务覆盖表同一层；任务表同阶段内顺序跟着走）—— **源表没有「顺序」这一列**，任务字段本身仍无新增 / 改名；服务端落库口径（`sort_index` 列或批量排序接口）见 `前端功能需求.md` §3.8 A19，待 wmj 决议；2026-09-22 Push 136 多项目经理 / 多任务负责人（业务口径「一个项目可以有多个项目经理和任务负责人」）：**项目级**「项目经理」由单个改为数组（`Project.managerIds`，至少一位、顺序 = 展示顺序），**任务级**「任务负责人」由单个改为两组同下标数组（`ProjectTask.owners` / `ownersEn`，可多位、空数组 = 待分配）—— 卡片 / 详情顶栏 / 任务表列多位按「、」连接展示，新建 / 编辑项目弹窗与任务表列 / 任务详情抽屉三处行内多选（取消最后一位不生效），首页侧栏计数每位各计一次、筛选「任一位命中即命中」、搜索任一姓名命中即命中，人员任务分配看板按每位负责人各出一列（同一任务多列出现；拖动换列 = 负责人整体换成拖入列那一位、拖入「待分配」= 清空），日报及问题面板关联任务多位「、」展示；**契约 / 落库多位口径已定案并落地**（数组列方案，见 `0017_multi_manager_and_owner.sql`；`前端功能需求.md` §3.8 A22 / A23、第六章 Q16 已关闭；本文档 §七「任务负责人」一行已按此增补）；2026-09-22 Push 143 M3-03 完成门禁 + 成果文件多选后端落地（wmj 线代记，请 px 复核）：「输出成果文件」由单值 `deliverable` 改多选 `deliverableTypes: DocType[]`（落库 `tasks.deliverable_types text[]`、空数组 = 不要求、生成后锁定），完成任务 / 改状态为已完成 / 进度拉满三入口走同一门禁（有节点任务按节点要求、无节点任务按自身类型，缺件 422 `TASK_REQUIRED_DOC_MISSING` + 缺件清单；已上传未定档放行但提醒定档 R02）；前端表格 / 抽屉多值展示与 422 缺件提示随 M3-07 联调
- 数据归属（2026-09-19，Push 59）：源表那一个项目的 44 条任务**只挂在印度项目 `inmu-0010`**（前端 `tasksForProject()`），其余项目打开只出阶段骨架（阶段分组头只显示阶段名、没有任务行；Push 61 起骨架常显，加出任务后其余阶段分组头不消失）；字段口径本身不变，接后端后按 `GET /api/v1/projects/{id}/tasks` 的 `{id}` 取数。

## 一、sheet「项目名称」：44 条任务 / 18 列

| 源表列 | 源表数据量 | 表格 | 抽屉 | 说明 |
| --- | --- | --- | --- | --- |
| 任务描述 | 44 | 有（第 1 列） | 有（标题） | 拆成中英文两行显示 |
| 项目经理 | 40（均为贾海洋） | 有（取项目级值） | 有 | 项目级必填字段；**Push 136 起可多位**（提案 `projects.manager_ids`，契约 `Project.managerIds` 数组、至少一位、顺序 = 展示顺序；页面多位按「、」连接展示，新建 / 编辑弹窗与任务表行内 / 任务详情抽屉三处都是多选、取消最后一位不生效；首页侧栏计数每位各计一次、筛选「任一位命中即命中」）—— **已落库 / 契约（Push 136 同批，`0017_multi_manager_and_owner.sql`）：`projects.manager_ids uuid[]`（至少一位）、`Project.managerIds` + `managerNames`、`ProjectCreateBody` / `ProjectUpdateBody.managerIds`、`filter[managerId]` 任一位命中、facets 每位各计一次**（`前端功能需求.md` §3.8 A22）；源表 4 行空取同项目非空值回填（小批量实物测试 / 客户培训、上线 / 产能爬坡 / 验收交付）；演示双经理样例：印度项目 = 冯雪 + 李伟、美国项目 = 王芳 + 陈晨 |
| 任务负责人 | 44 | 有 | 有 | 中英文合并显示（姓名 + 拼音名）；Push 63 起演示数据改用**虚构成员目录**（`frontend/src/data/members.ts`，项目经理 20 人 + 交付成员 11 人），编辑入口见 `前端功能需求.md` §6.8（弹窗）/ §6.9（表格行内）；**Push 136 起可多位**（`ProjectTask.owners` / `ownersEn` 两组同下标数组，可多位、空数组 = 待分配，多位按「、」连接展示；任务表列与任务详情抽屉都是多选；人员任务分配看板按每位负责人各出一列、同一任务多列出现，拖动换列 = 负责人整体换成拖入列那一位、拖入「待分配」= 清空）—— **已落库 / 契约（Push 136 同批，`0017_multi_manager_and_owner.sql`）：`tasks.owner_ids uuid[]`（空数组 = 待分配）、`Task.ownerIds` + `ownerNames`、`TaskCreateBody` / `TaskUpdateBody.ownerIds`、`filter[ownerId]` 任一位命中**（`前端功能需求.md` §3.8 A23、本文档 §七）；演示双负责人样例：任务「包装」= 谢遥 + 石昀、「运输」= 苏珩 + 彭砚 |
| 任务状态 | 44（已完成 15 / 进行中 1 / 待开始 28） | 有 | 有 | 展示态 5 态（已完成 / 提前完成 / 进行中 / 待开始 / 已延期）；**Push 65 起表格列可手动改**（色标签 + 行内下拉）并与四格进度条双向联动：点进度条 0 格 = 待开始、1~3 格 = 进行中、4 格 = 已完成（按工期派生提前完成，未填预计完成时间只显示已完成）；选状态则反向写进度（进行中 ≥ 1 格、已完成 / 提前完成 = 4 格、已延期保持当前格数），手动值优先于派生（`statusOverride`）；「逾期」不进入状态枚举 —— 【Push 67 起】红色「逾期未交付」/ 琥珀「逾期已交付」标注在「是否按时交付」列与抽屉字段，实际完成日期列只显示日期或「—」（接口口径已定案：A12 · Push 70 —— 基础三态可写、派生优先、`statusOverride` 可删） |
| 项目进度 | 22 行有值（1×16、0.49×1、0×5），22 行空 | 无（列已删） | 有（档位文字） | 表格改为四格 Tracker；阶段标题改为液体水位容器 |
| 是否按时交付 | 16 | 有 | 有 | 源表值 = 「按时交付」或空；**Push 67 起是逾期标注位**：未完成且已过预计完成日期显示红色「逾期未交付」，完成但晚于预计完成日期显示琥珀「逾期已交付」，其余照源表值（绿）|
| 输出成果文件 | 10 | 有 | 有 | **Push 143 起多选**（ADR-024 · wmj 线代记，请 px 复核）：契约 `Task.deliverableTypes: DocType[]`（数组、去重、空数组 = 不要求）、落库 `tasks.deliverable_types text[]`（迁移 0018）；随模板节点生成后锁定（A1-17，编辑接口不含该字段；例外调整随 M3-05）；前端表格 / 抽屉多值展示随 M3-07 联调 |
| 项目进展描述 | 2 | 有 | 有 | |
| 开始日期 | 44 | 有 | 有 | |
| 预计完成日期 | 44 | 有 | 有 | |
| 预计所需施工人数 | 0（全空） | 有（默认隐藏，可在「筛选」里勾选） | 有 | 页面为演示值，源表为空；2026-09-18 定案：任务创建 / 编辑可填、非必填（Q8） |
| 预计所需天数 | 44 | 无（列已删） | 有 | 表格改为两个日期之间横线上方的数字 |
| 实际完成日期 | 16 | 有 | 有 | Push 67 起只显示完成日期或「—」（逾期标注已移到「是否按时交付」列）；**可在表格行内直接选日期 / 清除** —— 填 = 完成（四格全亮、按工期派生 已完成 / 提前完成），清 = 退回进行中（进度 3 格）；与非完成态互为反向联动（任务状态改成 待开始 / 进行中 / 已延期，或进度条退回 1~3 格时自动清空）（接口口径已定案：A13 · Push 70 —— 清除 = 进度写回 <1 档，服务端清 `actual_end`） |
| 阶段性里程 | 44（9 个阶段） | 无（列已删） | 有（徽标） | 用作分组标题，未单独成列 |
| 紧急重要度 | 0（全空） | 有（可选列） | 有（徽标） | 页面为演示值（高/中/低），源表为空；2026-09-18 定案：任务创建 / 编辑可填（项目经理 / 任务负责人均可）、非必填（Q8） |
| 结束时间 | 0（全空） | 无 | 无 | 未接入 |
| 文件 | 0（全空） | 有（Push 67 起默认显示） | 有（文件标签） | 页面为演示值，源表为空；2026-09-18 定案：走站内文件库任务关联（多类型、不通过第三方应用打开、站内预览）（Q8） |
| 变更关联 | 2（均为 2026/8/11） | 有（标签） | 有 | 涉及任务：包装 Packing、物料清单完整版 |

## 二、完全未接入的 3 个 sheet

### 1. 干系人（6 条记录，7 列）— 页面无入口

- 字段：干系人姓名/name、电话或whatsapp或微信号、邮箱Email、职务Position/责任板块、所属公司company、填写者、提交时间
- 示例：Christian Winkler（项目经理 / 客户customer）等 6 人

### 2. 变更管理（3 条记录，13 列）— 表格和抽屉都没有

- 字段：变更时间、变更阶段、变更文件、变更内容描述、变更前、变更后、变更原因、变更申请人、填写者、变更后文件版本、变更后文件、进度、关联
- 数据：3 条均为 2026/8/11，关联任务：包装 Packing、物料清单完整版
- 附件：附件下载_[XXXX-XXXX] XXX项目名称/变更管理/变更后文件/（3 个 xlsx）
- 现状：页面只有任务行上的「变更 2026/8/11」小标签

### 3. 日报及问题汇总（11 列）— 未接入（源表无数据）

- 字段：时间time、填写者、当日完成工作、明日计划、现场工作附图、现场发现问题、问题归类、解决方案或建议、问题附图、问题是否处理、今日施工人数

## 三、系统内部生成字段（无源表对应）

| 字段 | 位置 | 说明 |
| --- | --- | --- |
| 项目序号 seqNo | 首页项目卡片右上角徽标（`projects.seq_no`） | 源表无对应列：创建项目时由系统分配（全库唯一正整数、不可修改、不回收；与项目编号一一对应同一项目）；卡片两位补零展示（≥100 原样），列表可按 seqNo 排序、可按序号检索。2026-09-18 定案 |
| 文件生命周期字段 | `files` 表（无源表对应：文件走站内文件库） | M4-02（PR-5 · Push 131）落库口径：定档 `finalized_at` / `finalized_by`、回收站 `recycled_at` / `recycled_by` / `recycled_from_status`、到期彻底删除 `purge_after`（保留期默认 30 天、可配 `FILE_RECYCLE_RETENTION_DAYS`，1~3650）；源表「文件」列全空、无字段对标。 |
| 文件多态关联（file_links） | `file_links` 表（无源表对应：文件走站内文件库） | M4-03（PR-6 · Push 132）落库口径：`file_id`（→ `files.id`，on delete cascade）/ `object_type`（六值：project / task / node / report / issue / change）/ `object_id`（多态无外键）/ `created_by` / `created_at`；联合唯一 `（file_id, object_type, object_id）` 保重复写入幂等，反查索引 `（object_type, object_id）` 支撑对象侧双向跳转（一处关联、多处可见）；写入口径 = 上传完成（project 必写，node / task 有则写），report / issue / change 随对应模块落地后写入。源表「文件」列全空、无字段对标。 |

## 四、任务模板草稿字段（无源表对应，Push 58 新增；Push 60 起初始内容取业务预设，当前原型前端本地、正式版落库）

> 任务模板页（`frontend/src/PlaceholderPage.tsx`）右侧每一块面板 = 一份模板草稿（最左的「任务节点」列是节点来源，不是模板）。当前原型全部存在浏览器内存里（正式版由后端持久化）：**面板初始内容与左列节点池都由业务写死的模板预设生成**（`frontend/src/data/templatePresets.ts` 的 `STAGE_TEMPLATE_PRESETS` —— 九个阶段各一套，硬件实施两套：18 条 / 11 条；软件部署一套 4 条），刷新即回到这份预设；下表先按本地草稿定义字段口径，落库时与 wmj 对齐 `templates` / `template_nodes`。

| 字段 | 位置 | 说明 |
| --- | --- | --- |
| 模板 id `id` | 面板列表 key / 拖拽定位（`TemplateDraft.id`） | 当前原型前端自增序号 `tpl-N`（跨阶段不重复），刷新即重排；落库建议 `task_templates.id`（全库唯一，UUID / 雪花） |
| 模板名称 `name` | 面板列头（可直接改名） | 当前原型默认取预设名（多数阶段「{阶段}模板」如「设计开发模板」，硬件实施「硬件实施模板一 / 模板二」），可改成「英国订单」这类业务名；本地草稿字段、未接模板列表；落库建议 `task_templates.name`（必填；长度与「同阶段内唯一」是否约束待业务定） |
| 所属阶段 `stage` | 板块标签栏选中态（`templatesByStage` 的 key） | 九个阶段（售前规划 / 设计开发 / 加工采购 / 组装发货 / 硬件实施 / 软件部署 / 试运行 / 生产阶段 / 验收）＝ `PROJECT_STAGES` 去掉「项目总览」；当前原型按阶段分开存储（每个阶段一套草稿、互不影响；正式版落库）；落库建议 `task_templates.stage_key`（枚举同上，或阶段表外键） |
| 节点列表 `nodes` | 面板内卡片列表，顺序即模板内顺序 | 元素引用任务节点（当前原型写死在 `frontend/src/data/templatePresets.ts`：多数阶段沿用「项目总览」对应阶段的任务顺序；硬件实施模板二 `hx01`~`hx08`、软件部署 `sw01`~`sw04` 是业务给的新节点；左列节点池 = 该阶段全部预设节点按出现顺序去重，硬件实施 26 个）；拖拽复制、叉号移除、拖动排序都改这份列表；同一模板内按节点 id 去重（同一节点只留一份），不同模板之间可重复引用同一节点；落库建议 `task_template_nodes(template_id, node_id, seq)` 关联表（`seq` 顺序敏感）+ 节点库 `task_nodes`（id / 阶段 / 中英文名 / 库内排序） |

**预设来源（Push 60）**：面板初始名称与节点顺序 = `STAGE_TEMPLATE_PRESETS`（九个阶段各一套，硬件实施两套：长清单 18 条 / 格口-滑槽型 11 条）；新增节点 `hx01`~`hx08` / `sw01`~`sw04` 的英文名按业务截图补全、**待业务核对**；软件部署第二套模板待业务提供。
| 保存快照 `saved` | 列头「保存 / 已保存」按钮的判据 | 当前原型是前端内存里的「上次保存时的样子」（名称 + 节点 id 顺序的 JSON 序列化）：与当前值不一致 → 按钮显示「保存」（＝有未保存改动）；落库后由服务端 `updated_at` / 版本号取代，前端只做对比展示 |
| 「保存」动作 | 面板列头 | 当前原型只写浏览器内存（不落库、刷新即回到该阶段默认空白模板）；正式版落库 = `PATCH /api/v1/task-templates/{id}`（改名 / `nodeIds` 全量替换，`version` 乐观锁；Push 62 已入契约） |
| 板块导航 `section` | 地址 `#/templates?section=设计开发` | 板块名进 URL query，非法值回落到第一个板块并纠正地址；纯前端导航态、不入库 |

**契约（Push 62 已入，实现在后；`shared/src/modules/templates.ts` + `tasks.ts`）**：① 任务节点库 `GET /api/v1/task-nodes?stage=`；② 模板列表 / 详情 `GET /api/v1/task-templates?stage=`、`GET /api/v1/task-templates/{id}`；③ 模板增删改 `POST /api/v1/task-templates`、`PATCH`（改名 + `nodeIds` 全量替换、去重、排序，`version` 乐观锁）、`DELETE`（带 `version`）；④ 实例化 `POST /api/v1/projects/{id}/tasks/from-template`（`templateId` + 可选 `nodeIds`，`skipExisting` 默认 true → `created` + `skipped`）。落库表建议 `task_nodes` / `task_templates` / `task_template_nodes`；细节口径见 `前端功能需求.md` §3.8 A11。

## 五、数据质量提醒

1. 项目经理：项目级必填字段、**Push 136 起可多位（数组，至少一位）**，源表 4 行空取同项目非空值回填，不留空值；**页面已按项目取经理姓名展示（Push 61：项目卡片 / 任务表 / 任务详情都取经理字段 → 姓名，如印度项目 = 冯雪、美国项目 = 王芳；Push 136 起多位按「、」连接，印度项目 = 冯雪、李伟，美国项目 = 王芳、陈晨）**。
2. 状态与进度不一致（1 条）：任务「人员进场、场地检查、施工对接」源表状态=待开始，但实际完成日期=8月11日、进度=1；页面按日期派生为「提前完成」（8月11日早于预计完成日期 9月2日）。
3. 项目进度：22 行空（页面按 0 处理）——建议后端统一为数值或档位枚举。
4. 四列空置：预计所需施工人数、紧急重要度、结束时间、文件（源表全空）。2026-09-18 Q8 定案：施工人数 / 紧急重要度任务创建 / 编辑均可填、非必填；文件走站内文件库（多类型、站内预览，不通过第三方应用打开）；结束时间仍未接入。

## 六、后续补充建议（优先级）

1. 高：项目经理取项目级字段（源表非空值回填），任务表该列展示所属项目的项目经理，替换当前全局常量（Push 61 已落地；Push 136 起该项目级字段可多位，展示与编辑口径见 §一「项目经理」行）。
2. 高：变更管理明细进任务抽屉（变更前/后、原因、附件），替换目前只有日期的标签。
3. 中：项目级「干系人」面板（6 人：姓名/职务/公司/联系方式）。
4. 低：日报及问题汇总——源表暂无数据，等有数据再设计。

## 七、任务落库口径（2026-09-21 业务定案：1A / 2B / 3A）

> 与 `前端功能需求.md` §3.8 的 A15 / A18 / A19 / A20 是同一条口径；前端三条都已按定案实现（原型内存态），**落库 / 迁移 / 契约已由 wmj 落地（Push 124）** —— 迁移 `0015_task_order_and_nullable_scope.sql` + Drizzle `server/src/db/schema/tasks.ts` + 契约 `shared/src/modules/tasks.ts` 三处同步；真机证据 `docs/w2-回放证据(任务落库口径A15A18A19).md`（33 项断言）。
> Push 136 增补（2026-09-22）：「任务负责人」一行由「单个可空」升级为**可多位**（多位口径与落库见 `前端功能需求.md` §3.8 A23，已落 `0017_multi_manager_and_owner.sql`）；1A / 2B 两条口径不变。
> Push 138 增补（2026-09-22）：首页筛选侧栏新增「常用筛选」组合（自定义名称 + 地区 / 项目类型 / 项目经理 / 项目时间多项条件，跨设备记忆待定契约，见 `前端功能需求.md` §3.8 A24）—— **只复用既有筛选参数（`filter[...]`），不改任何源表字段口径**。

| 口径 | 业务定案 | 落库落地（Push 124 · 已实现） |
|---|---|---|
| 看板列内顺序（拖动排序 / 「插入位置」） | **1A：要持久化** —— 拖动后的顺序，刷新 / 换设备 / 换人打开都保持 | **已选 ① `tasks.sort_index` 位次列**（不新增批量排序接口）：一组 = 同一项目 + 同一阶段（null = 未分组，自成一组），组内 0 起、密集；创建 `sortIndex` = 插入位次 / 编辑 = 移到第 N 位（越界 = 组尾，同组顺延，组行锁）；默认读序 = 阶段序（未分组落最后）+ `sort_index` + id；契约 `Task.sortIndex` / `TaskCreateBody.sortIndex` / `TaskUpdateBody.sortIndex` |
| 「临时任务」的阶段（列底「添加 → 临时任务」） | **2B：允许没有阶段** —— 只填任务名也能建；卡片「所属阶段」显示「未分组」、项目总览落最后一组 | `tasks.stage_key` 放宽**可空**（不补「未分组」合法值：null 即未分组）；契约 `Task.stageKey` 可空、`TaskCreateBody.stageKey` 可选可空（带 `taskNodeId` 时缺省取节点阶段、显式不一致 400）；阶段完成度与阶段门禁只统计带阶段任务 |
| 任务负责人 | **3A：允许「待分配」** —— 负责人为空是合法中间状态；卡片拖进「待分配」列 = 清空负责人；**Push 136 起可多位**（一个任务多位负责人，见 `前端功能需求.md` §3.8 A23；空集 = 待分配） | `tasks.owner_id` 放宽**可空**（Push 124 已落地）为单值基准；**Push 136 已落库 / 契约（`0017_multi_manager_and_owner.sql`）**：`tasks.owner_ids uuid[] not null default 空数组`；读模型顺序下发 `ownerIds` / `ownerNames`（同下标）、`filter[ownerId]` 与名单有交集即命中、「待分配」= 空数组（不用 null）；创建 body 缺省 = 项目全部项目经理兜底 / 显式 `[]` = 待分配、更新 body 支持显式置空（`[]`）或整组替换（顺序 = 展示顺序）；看板「每位负责人各出一列、同一任务多列出现」为前端展示口径 |
