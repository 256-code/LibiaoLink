# M4-05e 回放证据（S7·file 预览产物清理）

> 卡片：M4-05「在线预览」的收口切片（主责 lan，评审 wmj）｜口径来源：迁移 `0027` 口径 3（彻底删除 / 回收站到期须按 `content_hash` 反查引用，无剩余引用才清对象与行）｜系统功能书 D2-06（同一内容只转换一次）/ A4-12（彻底删除仅管理员、操作留痕）｜ADR-007（三元组缓存键 / 沙箱 / 降级）｜ADR-006（对象键形态）。
> 落点说明：`docs/` 属 px 线；本文件由 lan 随 M4-05 收口卡片代记（回放脚本与断言同 PR，请 px 复核）。

| 项 | 值 |
|---|---|
| 回放时间 | 2026-09-23 16:06:44 +08:00 |
| 目标 | http://127.0.0.1:3011 |
| 数据库 | postgresql://libiaolink_migrator@127.0.0.1:55432/libiaolink |
| 对象存储 | libiaolink（本地 MinIO 沙箱：仅沙箱、不代表生产选型） |
| 转换沙箱 | http://127.0.0.1:9900（deploy/preview/，镜像 libiaolink/preview-converter:1.0.0） |
| 代码版本 | eecc9b3（回放时工作区含本卡未提交改动） |
| 执行账号 | 管理员（建项目 / 上传 / 读预览 / 回收 / 彻底删除） |
| 脚本 | server/scripts/m4-preview-cleanup-replay.mjs |

## 断言明细

| PASS | S0 | api 可用（/healthz + /readyz：含存储探针） | 
  - 期望：200 / 200
  - 实际：200 / 200
| PASS | S1 | 转换沙箱可用且管线版本与 server 配置同值（清理判定的三元组必须同版本） | 
  - 期望：ok=true 且 pipelineVersion=1.0.0
  - 实际：{"ok":true,"pipelineVersion":"1.0.0"}
| PASS | P1 | 建回放项目（为 file 提供 projectId） | 
  - 期望：201 + 项目可见
  - 实际：201 {"id":"d5214472-fa0b-40af-bb20-276b46980063","code":"M4PRD-20260923080641"}
| PASS | F1 | 上传两个同内容文件 A / B（真实上传管道；上传完成不投递预览任务） | 
  - 期望：200 + 两边版本 v1 + 同 contentHash + outbox 0 行
  - 实际：{"a":{"status":200,"seq":1},"b":{"status":200,"seq":1},"sameHash":true,"jobs":0}
| PASS | R1 | 读 A 预览 → not_ready + 补投（读取侧懒生成；产物的「首登版本」= A 的 v1） | 
  - 期望：200 + status=not_ready + outbox 1 行
  - 实际：{"status":"not_ready","jobs":1}
| PASS | R2 | worker 消费 → 三元组 ready（产物对象落 `previews/{contentHash}/{pipelineVersion}/{target}`） | 
  - 期望：status=ready + object_key = 契约键 + generated_at 非空
  - 实际：{"status":"ready","objectKey":"previews/d48fb4e3b3f0e032f42f117f48d10924022b347eddb170a3a330f7ae3ca4344e/1.0.0/pdf","generatedAt":"2026-09-23T08:06:43.686Z"}
| PASS | R3 | 读 B（同内容另一文件）→ 直接命中同一三元组（共享缓存：不重投、不重转） | 
  - 期望：status=ready + url 非空 + outbox 仍 1 行 + 对象存在
  - 实际：{"status":"ready","url":"http://127.0.0.1:9000/libiaolink/previews/d48fb4e3b3f0e032f42f117f48d10924022b34…","jobs":1,"objectInStorage":true}
| PASS | R4 | 产物行归属 = 首登版本（A 的 v1；D2-06 只转换一次的登记口径） | 
  - 期望：file_id = A + version_id = A.v1
  - 实际：{"fileId":"558426be-40da-4160-a2ac-135e2e4b7a5c","versionId":"70ded32a-1596-41a3-b113-e33943847e21"}
| PASS | X1 | 彻底删除 A（先回收后彻底删除） | 
  - 期望：200 + purgedAt 非空
  - 实际：200 {"fileId":"558426be-40da-4160-a2ac-135e2e4b7a5c","purgedAt":"2026-09-23T08:06:44.378Z"}
| PASS | X2 | 有存活引用（B 的 v1 同内容）→ **不清对象**，缓存行归属转移给 B 的版本 | 
  - 期望：行仍在 + file_id = B + version_id = B.v1 + 对象仍存在
  - 实际：{"row":{"fileId":"d8abf533-0247-4d94-80c7-42a0dc90b3e7","versionId":"ea0e7606-3773-45e5-98cc-47aa73a8ef56","status":"ready"},"objectInStorage":true}
| PASS | X3 | A 的 purge 审计：previewArtifactsReassigned = 1 / previewArtifactsPurged = 0（摘要把「转移」与「清对象」分开） | 
  - 期望：action=delete + metadata {purged: 0, reassigned: 1}
  - 实际：{"action":"delete","metadata":{"reason":"回放：清 A（B 仍引用同内容）","source":"api","purgedAt":"2026-09-23T08:06:44.378Z","objectKeys":["projects/d5214472-fa0b-40af-bb20-276b46980063/files/558426be-40da-4160-a2ac-135e2e4b7a5c/v1/d48fb4e3b3f0e032f42f117f48d10924022b347eddb170a3a330f7ae3ca4344e.html"],"deletedVersions":1,"previewA…
| PASS | X4 | 删 A 后 B 仍 ready（缓存未被打回 not_ready、不重投）；A 的版本对象照旧已清 | 
  - 期望：status=ready + outbox 仍 1 行 + A 版本对象不存在
  - 实际：{"status":"ready","jobs":1,"versionObjectA":null}
| PASS | X5 | 彻底删除 B（同内容已无存活引用） | 
  - 期望：200 + purgedAt 非空
  - 实际：200 {"fileId":"d8abf533-0247-4d94-80c7-42a0dc90b3e7","purgedAt":"2026-09-23T08:06:44.439Z"}
| PASS | X6 | 无剩余引用 → 产物对象与行一并清（`preview_artifacts` 行随版本级联，对象在持锁事务内同序清理） | 
  - 期望：rows = 0 + 对象不存在
  - 实际：{"rows":0,"objectInStorage":null}
| PASS | X7 | B 的 purge 审计：previewArtifactsPurged = 1 / previewArtifactsReassigned = 0 | 
  - 期望：action=delete + metadata {purged: 1, reassigned: 0}
  - 实际：{"action":"delete","metadata":{"reason":"回放：清 B（同内容已无引用）","source":"api","purgedAt":"2026-09-23T08:06:44.439Z","objectKeys":["projects/d5214472-fa0b-40af-bb20-276b46980063/files/d8abf533-0247-4d94-80c7-42a0dc90b3e7/v1/d48fb4e3b3f0e032f42f117f48d10924022b347eddb170a3a330f7ae3ca4344e.html"],"deletedVersions":1,"previewAr…
| PASS | X8 | 对象已清：清前签发的短时地址再取 → 404；文件已彻底删除 → 读预览 404（不复活产物） | 
  - 期望：签名地址 404 + 读预览 404
  - 实际：{"signedUrlStatus":404,"previewStatus":404}

## 汇总

- ✅ 全部断言通过（16 项）：共享三元组缓存 / 有引用则归属转移（不清对象）/ 无引用则清对象与行 / 审计记数 / 不重投 / 不复活。

## 未覆盖 / 风险登记

- **回收站到期清理（worker 定时档）走的是同一个 `purgeRecycled`**：真机回放按「管理员手动彻底删除」两条覆盖（A / B），定时档路径由单测 `test/file-service.test.ts` 的 sweep 用例覆盖（`RECYCLE_SWEEP_INTERVAL_MS` = 30 分钟，等真实定时档不现实）。
- **反查未加索引**：`findVersionByContentHash` 按 `file_versions.content_hash` 扫描（无索引）。彻底删除是低频批处理，暂可接受；量大后按需补一条只追加索引迁移（登记为后续项）。
- **跨项目同内容**：本脚本的 A / B 在同一项目内；跨项目复用同一三元组是同一逻辑（反查不带项目条件），未单独覆盖。
- **压测未做**：并发 2~4、200MB 级长跑、转换成功率 ≥95%（PoC-1 真实样本集）属 M4-05 压测 / M8 容量验证。

## 复跑

```bash
cd deploy/preview && docker compose up -d            # 转换沙箱（首次需 build）
cd server && npm run build && npm run start:api &     # api（BASE_URL）
OUTBOX_POLL_MS=1000 npm run start:worker &            # worker（消费 preview.job）
cd server && M4_DATABASE_URL=postgresql://libiaolink_migrator@127.0.0.1:55432/libiaolink \
  node --env-file-if-exists=.env scripts/m4-preview-cleanup-replay.mjs --out "../docs/m4-05e-回放证据(预览产物清理).md"
```
