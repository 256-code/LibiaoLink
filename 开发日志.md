# 开发日志

> 每完成一次推送，在这里追加一条记录：**做了什么、效果与验证、风险与后续**。
> 记录格式：`日期 时间 | 作者 | 分支 | Push 序号`；**作者 = 当次推送人**（目前团队：`px`、`wmj`、`lan`），下面是明细表；最新一条在最上面。

## 2026-09-18 10:36 +08:00 | px | px | Push 29

| 字段 | 内容 |
|---|---|
| 类型 | `feat`（前端：首页「分类筛选」开关 + 左侧筛选侧边栏 + 自适应布局；卡片「项目类型」徽标） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① 新增 `frontend/src/components/CategoryFilterSidebar.tsx`：左侧「分类筛选」侧边栏（地区 / 项目经理两组多选胶囊 + 计数、底部「已选 N 项」与「重置」、头部关闭按钮；Esc 与窄屏遮罩点击均可关闭）；② `CategorySwitch` 改为受控组件（`checked` / `onChange`），开关即侧边栏开合（开启时紫色轨道 + 滑块右移旋转；未引入 styled-components，用 Tailwind 任意值还原）；③ `Home.tsx` 接入筛选状态与组合过滤（地区 + 项目经理，与关键词搜索叠加生效）；计数在有筛选 / 搜索时显示「找到 N 个项目」，无结果时按情况给出「清空搜索 / 清空筛选」；④ 页面自适应：桌面端（≥md）侧边栏把内容推开 280px（`md:pl-[304px]`，300ms 过渡），卡片网格改为容器查询（`@container` + `@md/@4xl/@6xl`），随内容宽度自动 1 / 2 / 3 / 4 列（侧边栏展开时 4 列自动降为 3 列）；窄屏（<md）侧边栏为遮罩浮层、内容不移位；⑤ 数据模型新增 `region` 字段（`types.ts`、`projects.ts` 20 条演示数据，新建项目默认「未分类」），对齐 `技术设计v0.2-架构与数据模型.md` §8 的 `projects.region`；⑥ 搜索匹配范围新增地区；⑦ 新增项目类型：`types.ts` 定义 `PROJECT_TYPES`（T-sort / 3D分拣 / 飞箱）与 `Project.projectType`，20 条演示数据补齐类型，卡片左上角新增类型徽标（底色复用主题色 `accent`，与小车图标一致；amber 卡用深色字保证可读），新建项目弹窗新增类型下拉（默认 T-sort），`App.tsx` 创建时写入 `projectType`，搜索匹配范围同步新增项目类型；⑧ 左侧筛选侧边栏新增「项目类型」维度（T-sort / 3D分拣 / 飞箱，按 `PROJECT_TYPES` 固定顺序展示计数，与地区 / 项目经理多选叠加生效，「重置」与「已选 N 项」同时覆盖三组）；⑨ 分类筛选开关上移（内容区顶部、距顶栏约 16px）并改为 `sticky top-16` 滚动吸附（页面底色 `#f5f6f8` 打底、层级 z-10 低于侧边栏 z-20），下滑浏览时始终可见可点；⑩ 卡片配色改为按项目类型固定：`types.ts` 新增 `PROJECT_TYPE_ACCENTS`（T-sort=blue / 3D分拣=emerald / 飞箱=amber）并把 `CardAccent` 收窄为 blue / emerald / amber，20 条演示数据与新建项目统一按类型取色（`App.tsx` 去掉轮转配色），`Card.tsx` 中 rose / violet 样式改为注释保留（未来新增类型可直接启用）；⑪ 布局调整：`AppHeader` 新增可选 `title`（首页在 logo 右侧显示「项目空间」，竖线分隔、字号保持 `text-2xl`，<640px 隐藏，项目详情面包屑分支不变），`Home.tsx` 去掉内容区标题块，改为「分类筛选开关 + 项目计数 + 新建项目 + 搜索框」同层工具条，窄屏自动换行且按钮不再被拉通；⑫ 筛选侧边栏三个分组标题（地区 / 项目类型 / 项目经理）由 `text-xs font-medium text-zinc-400` 加重为 `text-sm font-semibold text-zinc-700`，层级更清晰；⑬ 筛选侧边栏新增「更新时间」维度：按 `updatedAt` 的 `YYYY-MM` 分月（倒序）生成胶囊并带计数，与其他维度同为多选叠加，纳入 `hasFilters`、「已选 N 项」与重置；⑭ 按反馈把时间维度换成日期区间选择器：新增 `frontend/src/components/DateRangePicker.tsx`（自研、无新增依赖；触发按钮显示「全部时间」或区间，弹层为固定定位月历，支持翻月、区间高亮、清除 / 确定，Esc 与外部点击关闭，默认打开到最近数据月份），侧边栏分组改名「项目时间」；列表改为按 `updatedAt` 降序排序（最新在前）；⑮ 工具条新增「降序 / 升序」分段按钮（紧随分类筛选开关，当前项深色高亮，切换即时重排）。⑯ 修复账号菜单被遮挡：顶栏层级由 `z-10` 提升为 `z-30`（高于内容区工具条 `z-10` 与筛选侧边栏 / 窄屏遮罩 `z-20`，低于日期区间弹层 `z-40`、任务抽屉与新建项目弹窗 `z-50`），内容区吸附工具条保持 `z-10`；账号菜单展开后不再被工具条或侧边栏遮住。⑰ 按反馈把两处侧边面板的滚动条改成「滚动才浮现」：新增 `frontend/src/components/ScrollArea.tsx`——隐藏原生滚动条并自绘悬浮滑块（`ResizeObserver` + `scroll` 监听同步滑块高度与位置，滚动时置 `opacity-100`、停止 900ms 后淡出，支持拖动滑块，悬停滑块时显现，内容未溢出时不渲染滑块），`app.css` 新增 `.scrollbar-hidden` 隐藏原生滚动条，应用到 `CategoryFilterSidebar` 主体与 `TaskDrawer` 字段区。注：Chrome 的自定义原生滚动条不随悬停 / 样式变化重绘（无头与真实窗口均实测），纯 CSS 方案无法做到「滚动才浮现」，故改为自绘滑块。⑱ 账号菜单观感加强：菜单面板由 `w-32` / `rounded-lg` / 单层阴影改为 `w-36` / `rounded-xl` / `border-zinc-200/80` / 双层柔和阴影（`0_18px_40px_-12px` + `0_4px_12px_-4px`）并加 `p-1`，菜单项改为圆角块（`rounded-lg px-3 py-2`）浅灰高亮；`app.css` 新增 `.menu-pop`（`ll-menu-in` 150ms 下移淡入，`transform-origin: top right`，并在 `prefers-reduced-motion` 下禁用）；头像按钮悬停加 `hover:text-zinc-900` 与浮起阴影、按下时收敛。 |
| 新增功能 | 首页「分类筛选」开关 + 左侧筛选侧边栏（地区 / 项目经理多选、计数、重置；桌面推挤、窄屏浮层、网格自动降列）；卡片「项目类型」徽标（T-sort / 3D分拣 / 飞箱）、新建表单类型下拉，筛选侧边栏新增「项目类型」维度（三组维度可叠加）；`ScrollArea` 组件（自绘悬浮滚动滑块，筛选侧边栏与任务抽屉共用）。 |
| 修复 / 重构 / 优化 | `Project` 模型补 `region`；网格由视口断点改为容器查询，实现侧边栏展开时自动降列。 |
| 测试与验证 | 1）`npm run typecheck` 通过；2）`npm run build` 通过（vite v8.3.0；产物 CSS 已核对含 `md:pl-[304px]`、`transition-property:padding-left`、容器查询规则与开关动画色值）；3）无头浏览器实测（Chrome headless + 本地代理挂演示会话，截图时注入 `transition:none` 保证确定性）：收起态 4 列 + 开关关闭；打开态侧边栏推挤内容且网格自动降为 3 列；在侧边栏选中「中国」后计数变为「找到 1 个项目」、仅剩 1 张卡片、底部显示「已选 1 项」；480px 窄屏为遮罩浮层；4）说明：无头虚拟时间下 CSS 过渡可能不推进，属测试环境现象（真实浏览器不受影响），故验证时禁用过渡；5）项目类型复验：`npm run typecheck`、`npm run build` 通过；无头浏览器截图（1600px 宽 4 列 + 放大视角）确认 20 张卡片左上角均显示类型徽标、底色与小车图标一致（五种主题色），类型分布 T-sort 7 / 3D分拣 7 / 飞箱 6；6）侧边栏复验：无头 Chrome 经 CDP 实测——侧边栏展开后展示地区 / 项目类型 / 项目经理三组（项目类型计数 T-sort 7 / 3D分拣 7 / 飞箱 6），选中「飞箱」后计数变为「找到 6 个项目」、仅剩 6 张卡片、底部显示「已选 1 项」；7）吸附复验：无头 Chrome 经 CDP 把页面滚到 545px 后，开关实测位置 top=80px / bottom=116px（视口内可见），点击仍可正常展开侧边栏；8）配色复验：`npm run typecheck` / `npm run build` 通过；无头浏览器截图确认 20 张卡片只出现蓝（T-sort）/ 绿（3D分拣）/ 橙（飞箱）三种颜色，小车图标与左上角类型徽标同色，无红 / 紫卡片；9）布局复验：1440px 宽实测顶栏「项目空间」与工具条四项同行（开关 + 计数在左、按钮 + 搜索在右），项目详情页顶栏面包屑未受影响；420px 宽实测工具条按行换行、无横向溢出；10）侧边栏标题复验：截图确认三个分组标题字号与字重提升、与胶囊区区分明显；11）时间维度复验：侧边栏滚到底部可见「更新时间」组（演示数据 2026-07 共 20 条），点击该胶囊后底部显示「已选 1 项」、列表按月份条件过滤；12）区间选择器复验：无头 Chrome 经 CDP 打开弹层（默认 2026 年 7 月）→ 点选 1 日与 8 日 → 「确定」后触发按钮显示 `2026/07/01 – 2026/07/08`、底部显示「已选 1 项」，列表按项目时间降序（10 / 20 / 16 / 09 …）。注：演示数据项目时间同为 2026-07-08，区间筛选只能演示到「全部命中」，需打散演示时间才能看出部分命中；13）排序按钮复验：默认「降序」时首位为 10（17:41），点「升序」后首位变为 01（08:15）且按钮高亮切换；14）账号菜单层级复验：无头 Chrome 经 CDP 在 1400×620 与 420×900（筛选侧边栏展开）两种场景点击账号菜单，对「退出登录」条目中心做 `elementFromPoint` 命中测试均返回条目自身（未被遮挡），截图确认菜单完整显示在工具条与侧边栏之上；15）滚动条复验：真实窗口 Chrome（远程调试 + CDP，窗口移出可视区）实测——两处滑块静止时 `opacity: 0`，悬停不动仍为 0（符合「滚动才浮现」口径），滚轮滚动后置 1 且位置随滚动更新（滑块高度按 `clientHeight / scrollHeight` 计算，抽屉 620px 视口下 217px），停止滚动 1.6 秒后回到 0；拖动滑块实测 `scrollTop` 由 160 增至 276（到底）且滑块同步下移；裁剪放大截图确认滑块为 6px 圆角细条；16）账号菜单复验：无头 Chrome 经 CDP 展开菜单（144×46）并截图放大 3 倍确认——面板为圆角 + 双层柔和阴影，菜单项悬停显示圆角浅灰高亮，动画结束后位置稳定；`npm run build`（含 typecheck）通过。 |
| 文档同步 | `前端功能需求.md`（§一.3 首页说明与网格口径与侧边栏三组维度、§一.4 搜索新增项目类型、§一.5 新建项目表单四项、§二.1 数据模型新增 `region` 与 `projectType`、§五 状态保持、§六 新增待确认项 9 / 10、变更记录 Push 29）；`开发日志.md`（本条目）。 |
| 风险与后续 | 1）筛选维度暂为地区 / 项目经理（前端演示数据口径）；后端 facets 与 `filter[...]` 契约（技术设计 v0.2 §8.2）就绪后切换为服务端过滤；2）筛选态尚未写入 URL query（v0.2 §8.2 要求可分享 / 收藏），列入后续；3）`region` 演示值为地区名，字典与「未分类」口径以后端为准；4）项目类型当前为前端枚举（T-sort / 3D分拣 / 飞箱），取值与中英文展示待 C9 字典确认；筛选侧边栏已含「项目类型」维度（地区 / 项目类型 / 项目经理），后端 facets 需按同口径返回三组计数；5）卡片配色与项目类型绑定（蓝 / 绿 / 橙），红 / 紫为预留未启用，新增类型时在 `PROJECT_TYPE_ACCENTS` 与 `ACCENT_STYLES` 放开注释即可。 |
| 备注 | 推送 `px`；未直接推送 `main`。其余滚动区域（详情页「筛选（列显隐）」下拉、首页令牌声明折叠区）按反馈暂保留原生滚动条。 |

## 2026-09-18 10:42 +08:00 | wmj | wmj | Push 28

| 字段 | 内容 |
|---|---|
| 类型 | `feat`+`docs`（g2 · S5·契约包：/api/v1 契约 + 统一错误模型 + 生成链路；含主干同步） |
| 分支 | `wmj` |
| PR / commit | 分两笔：① 主干同步 `chore`（`origin/main` `de49b06 → db97bd5`，PR #20 已 squash 合并后 wmj 跟进合并，消除「在后面 1」）；② g2 契约包提交。均推 `wmj`；未直接推送 `main`。 |
| 做了什么 | ① 新建 `shared/` 契约包（独立 npm 包，不引入根 package.json、不预设 workspace —— 待 v0.1 Q15 定案）：`src/common`（错误码与统一信封、Uuid/时间/版本/幂等键/分页/排序、阶段/成果文件/状态机枚举）+ `src/modules`（projects 含首页 facets、tasks 含四格进度、flow 含蓝图 JSON/节点/完成门禁）；② `src/openapi.ts` 注册 **18 个 /api/v1 操作（15 条路径）**，生成 `generated/openapi.json`（OpenAPI 3.1，47 个 schema）；③ 生成 `generated/api-types.d.ts`（前端类型，openapi-typescript）；④ `scripts/`：generate（生成物）与 check-drift（漂移 + 结构校验），全部 Node 24 原生 TS 运行（无需构建）；⑤ `shared/README.md`：目录、命令、生成链路、已定案口径表、后续切片；⑥ 根 `README.md` 文档索引同步。 |
| 新增功能 | 契约层：项目/任务/流程节点与蓝图三组接口契约；统一错误模型（15 个错误码 + HTTP 状态映射）；常量（Uuid/分页/排序/乐观锁/幂等键/时间）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）`npm run typecheck` 通过（tsc 严格模式）；2）`npm run generate` 输出 openapi.json（65,295 bytes）+ api-types.d.ts（49,511 bytes）；3）`npm run check` 通过——OpenAPI 3.1 结构校验（paths=15、schemas=47）与两项生成物漂移校验均为「一致」；4）生成质量抽查：错误码枚举、`filter[...]` 查询参数、TaskProgress 四格、Project.required 字段均正确；5）**新增依赖（需评审）**：zod 4.6、@asteasolutions/zod-to-openapi 9.1、openapi-typescript 7.13、typescript 5.9（均随 package-lock.json 提交）；6）本包无单测（纯 schema 与生成链路，验证以生成 + 漂移校验为准）。 |
| 文档同步 | 新增 `shared/`（README + 源码 + 生成物 + 锁文件）；根 `README.md` 文档索引；本条目（`开发日志.md`）。 |
| 风险与后续 | 1）契约漂移检查尚未接入 CI（阶段 5 · CI 扩展任务负责；在那之前作者手动跑 `npm run check`）；2）类型版本差异：shared 用 TypeScript 5.9（openapi-typescript peer 要求 ^5.x），前端为 7.0.2 —— 已登记 ADR-017「待复核差异」；3）契约确认的两项前端口径（项目编号服务端生成、主题色随字典）建议由 px 回写《前端功能需求.md》；4）后续切片：文件/预览、通知、搜索/统计、迁移、自动化与日报问题（随对应模块落地）；5）file 模块契约由 lan 主责实现、契约仍归本线维护。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。契约包按《团队分工》§4（wmj 主责、lan/px 评审）；新增依赖按 CONTRIBUTING §15 属高风险变更，请在 PR 中重点评审。 |

## 2026-09-18 10:16 +08:00 | wmj | wmj | Push 27

| 字段 | 内容 |
|---|---|
| 类型 | `docs`（g1 · S5·ADR：技术选型定档 ADR-001~017 + 索引入库） |
| 分支 | `wmj` |
| PR / commit | 随本 PR（wmj → main）提交；未直接推送 `main`。推送前已同步主干：`origin/wmj` = `origin/main` = `de49b06`。 |
| 做了什么 | ① 新建 `docs/adr/`：索引 README（清单 + 状态流转 + 模板与编写规则）；② 按《技术设计v0.1-选型分析》§0.1 / §4 / §5 / §9 固化 16 条选型 ADR（交付形态、总体架构、语言框架、数据库与数据访问、队列与调度、对象存储、在线预览、三维与 CAD、消息通道、认证与组织、权限、前端、部署与容量、测试与 CI、流程节点与蓝图、首页分类）；③ 新增 ADR-017「版本基线与依赖升级策略」，收口 v0.1 §4.20 与 CONTRIBUTING §13/§14（版本基线表 + 升级规则）；④ 根 `README.md` 文档索引同步。 |
| 新增功能 | 无（纯文档）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）纯文档改动，按 AGENTS.md 约定未跑 frontend 门禁；2）逐条核对：ADR 的决策、备选与切换触发条件对照 v0.1 §0.1 / §4.x / §5 决策汇总表；3）发现并在 ADR-017「待复核差异」显式记录两处口径差异——TypeScript 版本（v0.1 §4.20 写 5.9.x vs 仓库现状 7.0.2）、包管理器（§4.20 写 pnpm vs 仓库现状 npm，Q15 未定案）——未静默选择，待评审定案；4）编码与行尾：新增 18 个文件均为 UTF-8 + LF，与仓库现状一致。 |
| 文档同步 | 新增 `docs/adr/`（18 个文件）；`README.md` 文档索引；本条目（`开发日志.md`）。 |
| 风险与后续 | 1）ADR 状态为「草案」：需 lan / px 评审，合并 main 后转「已采纳」；2）g2 契约包（Zod / OpenAPI / 生成客户端）以 ADR-003 / 004 / 017 为输入，下一步启动；3）Push 序号说明：远端当前最高为 26（`origin/lan`，未入 main），本次顺延取 27；若与其他分支重号，以先入 main 者为准、后合并方改号。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。ADR 按《团队分工》§4（wmj 主责、全员评审）；`docs/` 目录主理人为 px，本次改动请 px 一并评审。 |

## 2026-09-17 17:42 +08:00 | px | px | Push 25

| 字段 | 内容 |
|---|---|
| 类型 | `docs`（分工路由落地：AGENTS.md 分工章节 + CODEOWNERS 按模块细化） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。Push 24（PR #18）已 squash 合并，本分支已同步主干。 |
| 做了什么 | ① `AGENTS.md` 新增「分工与路由（谁负责哪块）」章节：团队角色一行（px = 前端 + 运维 / 交付负责人；wmj = 后端 A 领域负责人兼技术负责人；lan = 后端 B 平台负责人）+ 范围→主责→评审表（`frontend/`、`assets/`、`deploy/`、`.github/`、`docs/` 与根目录文档 → px；后端领域 identity/org、project、blueprint/node、task、report/issue、stakeholder、automation、admin → wmj；后端平台 file、preview、notify、outbox、search、dashboard、迁移工具链 → lan；API 契约（Zod / OpenAPI / 生成客户端）、ADR、`技术设计*.md` → wmj；`系统功能书.md` → 变更提出人），并写明代理动手前按表判断归属、跨线改动先对齐、后端目录未落地前先落文档与契约、高风险变更需他人评审；② `.github/CODEOWNERS` 由全员 `*` 兜底细化为模块规则：`frontend/`、`assets/`、`docs/` 与根目录 6 个文档 → px(+wmj)，`deploy/`、`.github/` → px+lan，`技术设计v0.1` → wmj(+px)、`技术设计v0.2` → wmj+lan(+px)，`AGENTS.md`、`CONTRIBUTING.md` → 全员，`server/`、`database/`、`shared/` → wmj+lan（目录落地后自动生效）；兜底 `*` 保持全员，每条具体路径 ≥2 名所有者；③ `团队分工.md` 升 v1.1：§5.1 由「建议细化」改为「已细化（随 Push 25 落地）」并列路由表与说明；④ `CONTRIBUTING.md` 附录 A 第 2 条补注「CODEOWNERS（评审自动路由；已按模块细化，路由表见 `团队分工.md` §5.1）」。 |
| 新增功能 | 无（文档与仓库配置）。 |
| 修复 / 重构 / 优化 | 无代码改动。 |
| 测试与验证 | 1）纯文档 / 仓库配置改动，按 AGENTS.md 约定未跑 frontend 门禁，PR 的 CI `frontend` 仍会执行；2）行尾 / 编码核对：`AGENTS.md`、`.github/CODEOWNERS`、`CONTRIBUTING.md`、`团队分工.md`、`开发日志.md` 均 CRLF、无松散 LF、UTF-8 无 BOM；3）内容核对：路由表与 `团队分工.md` §5.1 和第 3~4 节模块归属逐条一致；owner 账号与 `.github/CODEOWNERS` 现有成员（px=@256-code、wmj=@suikiiovo、lan=@XSZR341）一致；4）CODEOWNERS 语法：按「后匹配生效」排序，具体路径写在 `*` 之后。 |
| 文档同步 | `AGENTS.md`「分工与路由」；`.github/CODEOWNERS` 模块规则；`团队分工.md` v1.1（§5.1 + 变更记录）；`CONTRIBUTING.md` 附录 A 第 2 条；`开发日志.md`（本条目 Push 25）。 |
| 风险与后续 | 1）按模块路由后 PR 只自动请求对应模块所有者，兜底 `*` 仍请求全员；2）`server/`、`database/`、`shared/` 规则先行登记，后端落地首个 PR 时验证触发；3）`团队分工.md` §5.1 与 `AGENTS.md` 同口径，后续调整需两处同步。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 17:34 +08:00 | px | px | Push 24

| 字段 | 内容 |
|---|---|
| 类型 | `docs`（新增《团队分工》并入仓库根） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。注意：Push 23 随 PR #17 提交。 |
| 做了什么 | 新增 `团队分工.md`（v1.0，136 行）到仓库根：① 角色到人——px（前端 + 运维，交付负责人）、wmj（后端 A：领域负责人兼技术负责人）、lan（后端 B：平台负责人），含 GitHub 账号与常驻分支；② 后端 14 个模块的主责 / 评审（按 `技术设计v0.2-架构与数据模型.md` §1.2：wmj 负责 identity/org、project、blueprint/node、task、report/issue、stakeholder、automation、admin，lan 负责 file、preview、notify、outbox 队列调度、search/dashboard、迁移工具链）；③ 功能书 A1~D5 各模块的前端 / 服务端交付人；④ 契约与文档主责（API 契约 = wmj；前端功能需求 / 字段对照清单 = px；系统功能书 = 变更提出人；技术设计与 ADR = wmj；部署 Runbook = px）；⑤ 协作流程（分支 / 日志 / 评审 / 合并窗口 / 联调顺序 / 提测 / CI 分层 L1~L3 / 工期预期）与 CODEOWNERS 细化建议；⑥ 阶段 0 九项 PoC 的主责、协办与验收证据；⑦ 运维值班、单点风险与消减、修订流程。同步更新 `README.md` 文档表与 `CONTRIBUTING.md` 附录 A 第 3 条文档清单。 |
| 新增功能 | 无（文档）。 |
| 修复 / 重构 / 优化 | 无代码改动。 |
| 测试与验证 | 1）纯文档改动，按 AGENTS.md 约定（纯文档改动除外）未跑 frontend 门禁，PR 的 CI `frontend` 仍会执行；2）文件校验：UTF-8、CRLF 一致（无松散 LF）、无遗留占位符；3）内容核对：模块划分逐条对齐 `技术设计v0.2-架构与数据模型.md` §1.2；PoC 清单对齐 `技术设计v0.1-选型分析.md` §7.3；分支 / 评审 / 发布规则对齐 `CONTRIBUTING.md` 第 5 / 11 / 15 / 16 / 17 节；GitHub 账号取自 `.github/CODEOWNERS`。 |
| 文档同步 | 新增 `团队分工.md`；`README.md` 文档表 +1 行；`CONTRIBUTING.md` 附录 A 第 3 条补入；`开发日志.md`（本条目 Push 24）。 |
| 风险与后续 | 1）角色到人的绑定（后端 A = wmj、后端 B = lan）如与实际分工不符，按文档第 9 节修订并同步日志；2）CODEOWNERS 按模块细化（frontend → px；server/database → wmj、lan；deploy/.github → px、lan）待后端目录落地后作为独立 PR 执行；3）本文件只细化「谁负责什么」，与 `CONTRIBUTING.md` 冲突时以更严格者为准。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 17:25 +08:00 | px | px | Push 23

| 字段 | 内容 |
|---|---|
| 类型 | `docs`（两篇技术设计入仓仓库根） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。Push 22（含 Push 20 / 21 内容）已经 PR #16 squash 合并进 main（9627ab2，2026-09-17 17:24）；本条目为新的 PR。 |
| 做了什么 | 1）把《技术设计 v0.1（选型分析篇）》《技术设计 v0.2（架构与数据模型篇）》从微信文件缓存目录入仓到仓库根，文件名去掉下载副本后缀：`技术设计v0.1-选型分析.md`（入仓后版本 v0.1.7）、`技术设计v0.2-架构与数据模型.md`（入仓后版本 v0.2.1）（入仓复制时与源文件逐字节一致）；2）文档内按入仓现状更新 8 处（含修订记录 v0.1.7 / v0.2.1）：v0.1 头部补「入仓」行、1.5 现状快照补「技术设计」行、7.1 文档分层说明改写、8.2 Q16 标记已办、第 9 节「文档入仓」标记已办；v0.2 头部补「入仓」行；3）仓库文档索引同步：`README.md` 文档表新增两行，`CONTRIBUTING.md` §17 Definition of Done 的文档清单与附录 A 第 3 条补入《系统功能书》与两篇技术设计。 |
| 新增功能 | 无（文档入仓）。 |
| 修复 / 重构 / 优化 | 无代码改动；文档分层口径统一为「仓库根：系统功能书 / 前端功能需求 / 字段对照清单 / 开发日志 / 技术设计 v0.1、v0.2；docs/：SSO 等专题」。 |
| 测试与验证 | 1）入仓校验：复制后逐字节比对 `equals=true`（v0.1 85,641 bytes / v0.2 33,384 bytes）；2）编辑校验：8 处替换逐一断言「匹配次数=1」后才写入，其余内容零改动；3）门禁复跑（本 PR 含 Push 22 代码改动）：`npm run typecheck` 通过、`npm run build` 通过（vite v8.3.0，31 modules transformed，`index-c5yzkE9A.css` 37.65 kB / `index-D-vQS2vc.js` 274.00 kB，899ms——与 Push 22 产物哈希一致，无漂移）；CI `frontend` 在 PR #16 通过（Push 22）；本 PR 的 CI 结果以 PR 页面为准。 |
| 文档同步 | `README.md`、`CONTRIBUTING.md`、`技术设计v0.1-选型分析.md`、`技术设计v0.2-架构与数据模型.md`、`开发日志.md`（本条目 Push 23）。 |
| 风险与后续 | 1）微信缓存目录的源文件与仓库副本自此分叉，后续以仓库根版本为唯一口径（再分发从仓库导出）；2）两篇仍为讨论稿（v0.1.7 / v0.2.1），冻结 v1.0 基线时按 8.2 待确认项（如 Q28）更新；3）技术设计与代码的一致性自本次起纳入 §17 Definition of Done 维护范围。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 17:21 +08:00 | px | px | Push 22

| 字段 | 内容 |
|---|---|
| 类型 | `feat`（任务状态五态）+ `docs`（四份文档口径对齐） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 1）任务状态由 3 态扩为 5 态派生展示：`frontend/src/data/tasks.ts` 新增 `TaskStatus`（待开始 / 进行中 / 已完成 / 提前完成 / 已延期）、`isTaskDoneEarly()`、`taskStatus()`；`TaskBoard.tsx` 状态列改用 `taskStatus()` 并按态着色（已完成 / 提前完成 = 绿、进行中 = 蓝、已延期 = 红、待开始 = 灰）；`TaskDrawer.tsx` 状态胶囊同步 5 态，并移除与状态重复的头部「已逾期」胶囊。2）口径：逾期不进入任务状态枚举——红色「逾期」仍标注在实际完成日期（表格列 + 抽屉字段），抽屉进度区保留「已超过预计完成日期…」红色提示；「已延期」＝未完成且已过预计完成日期（派生展示态，不写回）；「提前完成」＝完成且实际完成日期早于预计完成日期；逾期补完按「已完成」（是否按时交付 = 否，不回退「提前完成」）。3）文档对齐：`前端功能需求.md`（§6.3 状态列、§6.5 抽屉、§二 ProjectTask.status 与状态派生规则、§四 status 规则、变更记录 Push 22）；`字段对照清单.md`（任务状态行 + 数据质量第 2 条）；`系统功能书.md` 与源文档同口径 6 处（A1-01、A1-06、A2 状态机图与口径说明、A2-09、B2-01）；技术设计 v0.2（§2.3 补 tasks / task_events DDL 与 projects.manager_id、§2.2 关系、§2.4 任务状态规则）；技术设计 v0.1（星级进度→四格进度、1.5 现状快照补任务状态口径、4 处文档路径与仓库结构按现状修订）。 |
| 新增功能 | 任务状态新增「提前完成」「已延期」两态（由日期字段派生展示，接口仍可只回传基础态 待开始 / 进行中 / 已完成）。 |
| 修复 / 重构 / 优化 | 修复状态与日期矛盾行的展示：t11「人员进场、场地检查、施工对接」源表状态=待开始但已完成（8月11日，早于预计完成日期 9月2日），现按「提前完成」显示，与《字段对照清单》数据质量记录一致。 |
| 测试与验证 | 1）`frontend/` 下 `npm run typecheck` 通过（tsc 两个工程无错误输出）；2）`npm run build` 通过（vite v8.3.0，31 modules transformed，`dist/assets/index-c5yzkE9A.css` 37.65 kB、`dist/assets/index-D-vQS2vc.js` 274.00 kB，909ms）；3）口径验证：直接用 node 加载 `src/data/tasks.ts`（`--experimental-strip-types`）对 44 条 mock 数据派生——已完成 15 / 提前完成 1 / 待开始 23 / 已延期 5（合计 44）；`isTaskDone` 16 条、`isTaskOverdue` 5 条，与改造前一致（仅改变状态展示口径，完成率与逾期计数不变）；4）产物核对：新配色类 `bg-red-500` / `text-red-600` / `text-emerald-700` 已进构建 CSS；5）文档核对：`系统功能书.md` 与源文档逐字节一致（53,500 bytes），源文档改动前已备份。 |
| 文档同步 | `前端功能需求.md`、`字段对照清单.md`、`系统功能书.md`（与源文档同步）、`开发日志.md`（本条目 Push 22）；两份技术设计文档在仓库外（WXWork 缓存目录），已按同一口径更新并留存 .bak 备份。 |
| 风险与后续 | 1）接口契约：后端建议回传基础态 `status`（待开始 / 进行中 / 已完成）＋ 三个日期字段，展示态由前端派生（当前实现），或由服务端直接回传 5 态——二选一需在接口对齐时定案；2）「提前完成」在 mock 中仅 1 条（t11，源表状态与日期矛盾行），真实数据接入后需复核派生结果；3）源文档（功能定档目录）与仓库 `系统功能书.md` 需双处同步维护，本次已同步。 |
| 备注 | 推送 `px`；未直接推送 `main`。备份：源功能点清单 → `市场项目管理系统-功能点清单-V1.1.修改前备份-20260917-任务状态口径.md`；技术设计 v0.1 / v0.2 → 同目录 .bak-任务状态口径。 |

## 2026-09-17 17:06 +08:00 | px | px | Push 21

| 字段 | 内容 |
|---|---|
| 类型 | `docs`（系统功能书入库 + 项目总览字段口径对齐） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 1）把《市场项目管理系统 · 功能点清单（定档版 V1.1）》以 `系统功能书.md` 入库到仓库根（与 `前端功能需求.md`、`字段对照清单.md` 同级），内容与源文档逐字符一致，作为系统功能书的仓库版本；2）按当前系统实现口径对齐「项目总览」字段并清理失效引用，共 27 处：① A1-01 字段范围改为当前 15 列（任务描述（中英文）、项目经理、任务负责人（中英文）、任务状态、紧急重要度、是否按时交付、输出成果文件、文件、项目进展描述、开始日期、预计所需天数、预计完成日期、预计所需施工人数、实际完成日期、变更关联）＋三项非独立列（阶段性里程＝阶段分组、项目进度＝四格进度、是否逾期），去掉 16 个已不存在字段、新增「预计所需施工人数」；② A1-06 自动计算字段改为四格档位 / 是否按时交付 / 是否逾期 / 阶段与整体完成率；③ A1-13 由「星级进度展示」改为「进度展示（四格进度）」；④ A1-11 里程碑与依赖、A2-03 部门维度移至二期；⑤ A1-17 模板映射与 C9-06 模板节点字段同步（去掉责任部门 / 协同部门、前置任务、是否里程碑、完成检查项，默认工期写入「预计所需天数」）；⑥ 清理其余引用已删字段的位置：A1-04、A1-12、A1-15、A1-16、A2-07、A2-09、B1-01、B2-03、B2-04、B3-01、B3-06、B3-09、B4-04、B4-08、B5-01、C9-07、D5-04（星级口径统一为四格）；⑦ 文档抬头补「修订」说明行。 |
| 新增功能 | 无（文档）。 |
| 修复 / 重构 / 优化 | 文档口径修正：去掉文档中当前系统已不存在的任务字段及其引用（星级进度、实际开始日期、责任部门 / 协同部门、前置 / 后置任务、剩余天数、任务健康度、完成检查项、任务类型、是否里程碑、关注人、关联日报数 / 问题数 / 变更次数、归档状态、审计字段等），避免与前端实现（项目总览 15 列 + 任务详情抽屉）不一致。 |
| 测试与验证 | 1）`frontend/` 下 `npm run typecheck` 通过（tsc 两个工程无错误输出）；2）`npm run build` 通过（vite v8.3.0，31 modules transformed，`index-CtSdJudy.css` 37.56 kB / `index-C2shZ_tC.js` 273.45 kB，与 Push 20 记录一致）——本次推送含 Push 20 的前端样式改动，按门禁要求执行；3）文档核对：改动前原件已备份，逐行比对（`git diff --no-index` 49 insertions / 44 deletions）全部落在预期的 27 处，其余内容零改动。 |
| 文档同步 | 新增 `系统功能书.md`；`README.md` 文档目录表新增该文件一行；`开发日志.md`（本条目 Push 21）。 |
| 风险与后续 | 1）字段口径以当前前端实现为准，后端建模请以 `系统功能书.md` A1-01 字段范围为准；2）A1-11、A2-03 已标为二期，B2 / B3 / B4 / B5、C9 中相关引用已按同口径改写，后续恢复字段需同步回改；3）星级进度统一为四格进度，客户如仍要求星级展示需重新定档；4）源文档（项目资料目录内的功能点清单）与仓库 `系统功能书.md` 需双处同步维护。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 15:52 +08:00 | px | px | Push 20

| 字段 | 内容 |
|---|---|
| 类型 | `style`（界面布局微调） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 首页「项目空间」标题区控件布局调整：`新建项目`按钮从搜索框上方（竖向堆叠）移到搜索框左侧；宽屏（≥640px）两者同一行、垂直居中，窄屏保持上下堆叠（按钮在上、搜索框通栏）。仅调整 `frontend/src/Home.tsx` 控件容器的一处响应式类名（`sm:flex-row sm:items-center`），交互、文案与数据均未变。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 界面优化：标题区右侧控件由两行收拢为一行，减少头部高度，按钮与搜索框的关联更直观。 |
| 测试与验证 | 在 `frontend/` 下执行：1）`npm run typecheck` 通过（tsc 两个工程均无错误输出）；2）`npm run build` 通过（vite v8.3.0，31 modules transformed，产物 `index-CtSdJudy.css` 37.56 kB / `index-C2shZ_tC.js` 273.45 kB，131ms）；3）产物 CSS 核对：`sm:flex-row` / `sm:items-center` 已生成（位于 `@media (width>=40rem)` 块）。未运行 `npm run smoke`：该冒烟依赖本地 Casdoor 沙箱与 3000 端口 dev server，本次改动不涉及认证链路与数据逻辑。 |
| 文档同步 | `前端功能需求.md`（§3 首页布局说明 + 变更记录 Push 20 行）；`开发日志.md`（本条目 Push 20）。 |
| 风险与后续 | 纯样式调整、无逻辑风险；如需回退只还原 `frontend/src/Home.tsx` 这一处类名即可。 |
| 备注 | 推送 `px`；未直接推送 `main`。曾试改阶段分组胶囊「液体」配色（emerald-500）与文字深绿，产物核对通过后按反馈于推送前整体回退（`frontend/src/app.css`、`frontend/src/components/TaskBoard.tsx` 已与 `main` 现有内容一致、diff 为空），最终交付仅含按钮位置调整。 |

## 2026-09-17 14:10 +08:00 | px | px | Push 19

| 字段 | 内容 |
|---|---|
| 类型 | `chore`（分支归属修正 + 规则补充） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 1）删除由 `px` 代建的 `wmj`、`lan` 两条常驻分支（删除前临时把规则集 `resident-branches-no-deletion` 条件收窄为仅 `px`，删除后已恢复为 `px` / `wmj` / `lan` 三分支覆盖）——改由本人创建，避免分支归属显示在他人账号下；2）`CONTRIBUTING.md` 第 5 节与附录 A 第 1 条补充「常驻分支由本人创建并维护」。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 修正 Push 11 中「由 `px` 代建 `wmj` / `lan` 分支」的归属问题（Push 11 条目如实保留，本条为后续修正记录）。 |
| 测试与验证 | `gh api` 实测：规则集条件收窄后 DELETE `wmj` / `lan` 均成功，远端复查两条分支已不存在；规则集 PUT 已恢复为三分支覆盖并确认 active（`deletion` 规则）；`git ls-remote` 复查远端当前仅剩 `main` / `px`（Dependabot 临时分支已随 #6/#7 合并、#8 关闭清理），`wmj` / `lan` 创建后将按名称自动受防删除保护。 |
| 文档同步 | `CONTRIBUTING.md`（第 5 节、附录 A 第 1 条）；`开发日志.md`（本条目 Push 19）。 |
| 风险与后续 | 1）待 `wmj` / `lan` 本人创建各自常驻分支（GitHub 网页 Branches → New branch，或本地 `git push origin main:wmj`），创建即受规则集保护；2）本 PR 无代码差异，CI（`frontend`）照常触发。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 14:02 +08:00 | px | px | Push 18

| 字段 | 内容 |
|---|---|
| 类型 | `docs`（文档结构 / 仓库布局） |
| 分支 | `codex/move-agent-docs-to-root` |
| PR / commit | 本条目随本 PR 合入 `main`；未直接推送 `main`。 |
| 做了什么 | 把 AI 代理每次工作都要读 / 写的三份文档从 `docs/` 上移到仓库根目录：`前端功能需求.md`、`开发日志.md`、`字段对照清单.md`（对应 `AGENTS.md`「文档同步」三条约定）。同步更新全部现行引用：`AGENTS.md`、`CONTRIBUTING.md`（§11 / §15 / §17 / 附录 A）、`README.md` 文档目录表、`.github/pull_request_template.md`、`前端功能需求.md` 内的字段对照指引。 |
| 新增功能 | 无（无代码改动）。 |
| 修复 / 重构 / 优化 | 仓库布局调整：高频读写文档从 `docs/` 子目录移到根目录，打开仓库即可定位，降低代理找错 / 写错路径的概率；文档内容本身不变。 |
| 测试与验证 | 纯文档改动，按 `AGENTS.md` 免跑 `npm run typecheck` / `npm run build`（PR 上 CI `frontend` 照常触发，结果见 PR 检查）。实际执行：`git grep -n -E "docs/(前端功能需求|开发日志|字段对照清单)\.md"` 全库核验，除 `开发日志.md` 历史条目（按 §11 保留）外无旧路径残留；`git status` / `git diff --stat -M` 核对改动仅涉及上述文档与模板、且三份文档按重命名识别。 |
| 文档同步 | 本次改动自身即文档同步：三份文档已位于仓库根；`AGENTS.md` / `CONTRIBUTING.md` / `README.md` / `.github/pull_request_template.md` / `前端功能需求.md` 引用已同步；`开发日志.md`（本条目 Push 18）。 |
| 风险与后续 | 1）`docs/前端功能需求.md` 等旧路径不再存在：仓库内已无站外 URL 引用（已全库扫描），历史 PR / Issue / 书签中的旧路径需按新路径访问；2）其他成员本地工作区 `git pull` 后按新路径使用；3）后续新增「AI 常读」文档沿用根目录口径，并在 `AGENTS.md` 登记。 |
| 备注 | 历史日志条目中的旧路径按 CONTRIBUTING §11「已合入记录不再修改」原样保留；`docs/` 继续承载 SSO / WeCom / 沙箱 / 实测 / 接入标准等按需查阅文档。 |

## 2026-09-17 13:58 +08:00 | px | px | Push 17

| 字段 | 内容 |
|---|---|
| 类型 | `chore`（依赖 / 平台） |
| 分支 | `codex/push17-devlog` |
| PR / commit | 本条目随本 PR 合入 `main`；依赖升级随 PR #6 / #7 已 squash 合并（`39d6dfd`、`b630635`）；未直接推送 `main`。 |
| 做了什么 | 处理 Dependabot 存量 PR（Push 13 暂停版本更新前自动开出的三条）：合并 `#6`（`actions/checkout` v4 → v7）与 `#7`（`actions/setup-node` v4 → v7），两条都只改 `.github/workflows/ci.yml` 的版本号、检查全绿；关闭 `#8`（`@types/node` 22.20.3 → 26.5.1），原因已写进关闭评论：CI 失败定位到 `frontend/server/oidc-plugin.ts:236` 使用 `node:crypto` 的 `JsonWebKey`，`@types/node` v26 不再导出该成员（error TS2694），需先改代码；且本仓库 CI / 本地运行在 Node 22，类型包不宜跳到 v26。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 无代码改动（仅工作流版本号）。 |
| 测试与验证 | 1）`#6` / `#7` 各自 PR 检查 `frontend` 均为 SUCCESS；2）合并后 `main` 上实测：run 35187791986（`39d6dfd`）success、run 35187810114（`b630635`）success（Install / Typecheck / Build 全绿，日志已核对）；3）`#8` 失败原因取自 run 35186214065 的失败日志；4）确认 `.github/workflows/ci.yml` 现为 `actions/checkout@v7` + `actions/setup-node@v7`（`node-version: 22` 未变）。 |
| 文档同步 | `docs/开发日志.md`（本条目 Push 17）；`.github/workflows/ci.yml` 由依赖 PR 自带变更记录。 |
| 风险与后续 | 1）checkout / setup-node 大版本升级仅在 CI 生效，不影响业务系统运行时；若后续 CI 异常，可把两个 action 回退到 v4；2）`@types/node` 待人工升级（需先处理 `JsonWebKey` 引用），升级前本地 / CI 保持 Node 22 口径；3）暂停版本更新期间不再自动新增依赖 PR。 |
| 备注 | `#6` / `#7` 合并后 Dependabot 源分支已由 GitHub 自动清理；`#8` 分支自动清理，本地无残留。 |

## 2026-09-17 13:52 +08:00 | px | px | Push 16

| 字段 | 内容 |
|---|---|
| 类型 | `fix`（安全 / 文档） |
| 分支 | `codex/fix-deploy-readme-secret` |
| PR / commit | 随本 PR 合入 `main`；未直接推送 `main`。 |
| 做了什么 | 补漏：Push 14 的凭据去明文遗漏两处 Casdoor 默认管理员口令 —— `deploy/casdoor/README.md` 第 24 行、`docs/公司统一登录(SSO)实施方案.md` 第 85 行（两份文件都不在上一轮扫描清单里）。现均改为指向本地 `deploy/casdoor/.env.local` 的 `CASDOOR_ADMIN_PASSWORD`，并说明本地联调需自备该文件；本轮扫描扩大到全部 `*.md`（含 `deploy/`）与 `docs/` 全量。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 修复：仓库内最后一处沙箱明文口令。 |
| 测试与验证 | 1）全仓 `rg`（排除 `node_modules` / `dist` / `.git` / `*.svg`）复查：管理员账号口令组合、文档中的「密码」字样、常见弱口令模式均 0 命中（除本条日志的描述性文字）；2）GitHub 上 `main` 的根 `README.md` 经 `raw.githubusercontent.com` 拉取复核为新内容（此前用户截图所见为旧缓存 / 旧提交）；3）`git log -S` 确认本次改动文件的历史无新增明文。 |
| 文档同步 | `deploy/casdoor/README.md`；`docs/公司统一登录(SSO)实施方案.md`；`docs/开发日志.md`（本条目 Push 16）。 |
| 风险与后续 | 1）`deploy/casdoor/.env.local` 为本机文件（不入库），其他人本地联调首次登录需向操作者获取管理员凭据；2）git 历史里的旧口令仍在（公开仓库不可撤回），建议轮换本地沙箱口令；3）本地联调文档已统一为「口令见 `.env.local`」口径。 |
| 备注 | 推送 `codex/fix-deploy-readme-secret`（临时分支，合并后删除）；未直接推送 `main`。 |

## 2026-09-17 13:47 +08:00 | px | px | Push 15

| 字段 | 内容 |
|---|---|
| 类型 | `docs` |
| 分支 | `px` |
| PR / commit | 本条目为 PR #9（px → main）合并后的补充记录，随后续 PR 合入 `main`；未直接推送 `main`。 |
| 做了什么 | 记录 Push 10–14 的合并落地：PR #9 以 squash 合并进 `main`，合并提交 `e1cd245`（操作方 `px`）；源分支 `px` 按常驻分支模型保留（未删除）。补充记录而不改写已合并条目（CONTRIBUTING §11「日志条目随 PR 提交、合并后视为历史」）。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）PR #9 必需检查 `frontend` 通过（job 105091339960）；2）合并后复查 `origin/main:frontend/scripts/smoke-test.mjs` 无明文口令（0 命中）；3）合并后 `git ls-remote --heads origin` 确认 `main` / `px` / `wmj` / `lan` 四条常驻分支均在（`delete_branch_on_merge` 保持 false）；4）本次合并未引入代码差异（Push 10–14 的验证结论见各条目，未重复执行 smoke）。 |
| 文档同步 | `docs/开发日志.md`（本条目 Push 15）；其余文档随 PR #9 已同步并入 `main`。 |
| 风险与后续 | 1）`main` 上仍有 Dependabot 历史 PR #6 / #7 / #8 待人工处理；2）git 历史中的旧口令仍在（公开仓库不可撤回），建议轮换本地沙箱口令；3）常驻分支 `wmj` / `lan` 仍指向 `51aa33d`，后续开发从各自分支开始即可。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 13:44 +08:00 | px | px | Push 14

| 字段 | 内容 |
|---|---|
| 类型 | `chore`（安全加固 / 文档） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 本地沙箱凭据去明文：1）此前写在仓库内的 `localhost:8000` 沙箱凭据——测试账号口令、管理员口令、DB / Redis 口令——全部从版本控制移出，只存本机 gitignore 的 `deploy/casdoor/.env.local`（新增；含 `TEST_USERNAME` / `TEST_PASSWORD` / `CASDOOR_ADMIN_USERNAME` / `CASDOOR_ADMIN_PASSWORD`）与 `frontend/.env.local`（含 smoke 用 `TEST_USERNAME` / `TEST_PASSWORD`）；2）`frontend/scripts/smoke-test.mjs` 去掉内置默认口令，改为读取上述 `.env.local`（已存在的环境变量优先），缺口令时明确报错退出；3）`README.md`、`frontend/README.md`、`docs/本地沙箱(LibiaoLink 演练环境).md`、`docs/开发者接入注意事项(SSO接入标准).md` 中的明文口令改为指向本地 `.env.local` 的引用。 |
| 新增功能 | 无（本地凭据管理方式变化）。 |
| 修复 / 重构 / 优化 | 修复：公开仓库内不应出现的沙箱口令明文；smoke 脚本不再内置口令。 |
| 测试与验证 | 1）密钥体检：`rg` 全仓（排除 `node_modules` / `dist` / `.git`）确认测试口令、管理员口令、DB / Redis 口令零命中；`git log -S` 复查历史，`frontend/.env.local`、`deploy/casdoor/.env.local` 从未入库，client secret（`b3ff1cc8…`）从未出现在任何提交；2）`git check-ignore -v` 确认两个 `.env.local` 均被忽略；3）`npm run smoke`（7 组断言，账号口令由本地 `.env.local` 提供）：全绿；4）`npm run typecheck`、`npm run build`：通过。 |
| 文档同步 | `README.md`；`frontend/README.md`；`docs/本地沙箱(LibiaoLink 演练环境).md`（新增「未入库说明」）；`docs/开发者接入注意事项(SSO接入标准).md`；`docs/开发日志.md`（本条目 Push 14）。`docs/前端功能需求.md` 不涉及（本次无页面 / 交互 / 数据模型 / 接口需求变化）。 |
| 风险与后续 | 1）本地开发 / CI 若要跑 smoke，需自备 `.env.local`（含 `TEST_USERNAME` / `TEST_PASSWORD`），否则脚本按缺凭据报错退出；2）git 历史里仍保留旧口令，按 CONTRIBUTING §12 视同已泄露，建议轮换本地沙箱三条口令（本地演示库，风险低）；3）公司环境（正式 / 测试）凭据不在本仓库，未受影响。 |
| 备注 | 推送 `px`；未直接推送 `main`；未改写 git 历史（仓库已公开，改写无法撤回已发生的曝光）。 |

## 2026-09-17 13:58 +08:00 | px | px | Push 13

| 字段 | 内容 |
|---|---|
| 类型 | `chore` |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 通知治理（团队反馈被 Dependabot 邮件刷屏）：`.github/dependabot.yml` 两个生态（npm `/frontend`、github-actions `/`）均设置 `open-pull-requests-limit: 0`，按官方文档写法暂停 Dependabot 版本更新 PR（不再每周批量开 PR）；Dependabot 漏洞提醒与安全更新保持不变（安全更新 PR 不受该上限约束，仓库安全设置未动）。 |
| 新增功能 | 无（平台配置）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 官方文档核对：`open-pull-requests-limit` 设为 0 即暂停该生态的版本更新（安全更新 PR 不受此限制、不计入该上限）；配置合入 `main` 后生效，若之后仍自动开出「版本更新」类 PR 视为未达标。推送前门禁：`frontend/` 下 `npm run typecheck`、`npm run build` 均通过（exit 0；本次无前端代码差异）。 |
| 文档同步 | `.github/dependabot.yml`（注释写明暂停原因与恢复方式）；`docs/开发日志.md`（本条目 Push 13）。 |
| 风险与后续 | 1）版本更新暂停后依赖升级改由人工发起；恢复自动升级 = 把两个 0 改回 1-5（建议同时改 monthly + groups 以降低打扰）；2）Dependabot 安全更新 PR 仍会零星开出，且仍会按 `CODEOWNERS` 自动请求三位评审——个人邮件彻底静音需三位开发者各自调整 GitHub 通知设置（`Settings → Notifications`），仓库侧无法代设；3）已开的 Dependabot PR #6 / #7 / #8 保持原样，待人工确认合并或关闭。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 13:53 +08:00 | px | px | Push 12

| 字段 | 内容 |
|---|---|
| 类型 | `docs` |
| 分支 | `px` |
| PR / commit | 随 PR #9（px → main）追加提交；未直接推送 `main`。 |
| 做了什么 | 规则补充：1）`CONTRIBUTING.md` 新增「5. Git 工作流（落地版）」——开发者先推自己的常驻分支（`px` / `wmj` / `lan`），再经 PR 合入 `main`，禁止直接推送 `main`；含常驻分支保留、squash-only、防删除规则集、日志与 PR 模板要求；2）抬头说明同步为「已落地：第 5 节、第 11–17 节」；3）`AGENTS.md` 新增「分支与推送」一节；4）附录 A 第 1 条补充指向第 5 节。 |
| 新增功能 | 无（规则文档）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 纯文档改动，无代码差异；规则与平台现状一致性核对：`main` 保护（require PR + 必需检查 `frontend`）、squash-only、常驻分支防删除规则集均为 Push 11 实测结论，未重复执行。 |
| 文档同步 | `CONTRIBUTING.md`（第 5 节 + 抬头 + 附录 A 第 1 条）；`AGENTS.md`；`docs/开发日志.md`（本条目 Push 12）。 |
| 风险与后续 | 1）第 1–4、6–10 节仍待补充；2）新增开发者时同步：建常驻分支 → 加入规则集与 `CODEOWNERS`。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 13:46 +08:00 | px | px | Push 11

| 字段 | 内容 |
|---|---|
| 类型 | `chore` |
| 分支 | `px` |
| PR / commit | 随 PR #9（px → main）追加提交；未直接推送 `main`。 |
| 做了什么 | 常驻分支模型落地：1）关闭「合并后自动删除源分支」（`delete_branch_on_merge=false`）——开发者分支合并后保留；2）新建 `wmj`、`lan` 常驻分支（基于 `main`），与 `px` 一起构成三条开发者分支；3）新增仓库规则集 `resident-branches-no-deletion`（id 23580071）：禁止删除 `refs/heads/px` / `wmj` / `lan`；4）`CONTRIBUTING.md` 附录 A 第 1 / 2 / 6 条与日志说明行同步更新（Push 序号不再写死）。 |
| 新增功能 | 无（分支与平台配置）。 |
| 修复 / 重构 / 优化 | 修正分支模型偏差：此前「合并后自动删除源分支」会在 PR 合并时删掉个人常驻分支（PR #5 合并时 `px` 曾被自动删除）。 |
| 测试与验证 | `gh api` 实测：`delete_branch_on_merge=false` 已生效；`git ls-remote` 确认 `main` / `px` / `wmj` / `lan` 四条常驻分支存在（`wmj` / `lan` 指向 `51aa33d`）；规则集创建返回 active（id 23580071，condition=三个 refs，rule=deletion），实测删除 `wmj` 被拒绝（422，分支保留）；CI 随本 PR 提交触发，结果见 PR 检查。 |
| 文档同步 | `CONTRIBUTING.md` 附录 A 第 1 / 2 / 6 条及日志说明行；`docs/开发日志.md`（本条目 Push 11）。 |
| 风险与后续 | 1）临时分支不再自动清理（如 Dependabot 合并后的分支），需要时手动删除；2）新增开发者时需同步：建常驻分支 → 加入规则集与 `CODEOWNERS`；3）`main` 保护 required approvals 仍为 0（待定是否调为 1）。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 13:37 +08:00 | px | px | Push 10

| 字段 | 内容 |
|---|---|
| 类型 | `chore` |
| 分支 | `px`（PR #5 合并后源分支被自动删除，本次按流程重建） |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 1）`.github/CODEOWNERS` 补齐团队账号：px=`@256-code`、wmj=`@suikiiovo`、lan=`@XSZR341`，移除「待补充」TODO 与已过时的套餐限制说明；2）按 CONTRIBUTING 第 11 节补本条目（Push 9 已随 PR #5 合入 `main`，按「合并后不再修改」原则不改写历史）。 |
| 新增功能 | 无（配置与文档）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 账号核对：`gh api repos/256-code/LibiaoLink/collaborators` 确认三账号均为协作者（`@256-code` admin；`@suikiiovo` / `@XSZR341` write）；本 PR 无代码差异，CI（`frontend`：锁定安装 → typecheck → build）随 PR 触发，结果见 PR 检查。 |
| 文档同步 | `.github/CODEOWNERS`；`docs/开发日志.md`（本条目 Push 10）。 |
| 风险与后续 | 1）`CODEOWNERS` 合入 `main` 后才对后续 PR 生效（自动请求评审）；2）`main` 分支保护 required approvals 仍为 0（作者可自合并），如需「非作者评审」硬门禁可调为 1，成员账号已就绪。 |
| 备注 | 推送 `px`（重建分支）；未直接推送 `main`。 |

## 2026-09-17 13:23 +08:00 | px | px | Push 9

| 字段 | 内容 |
|---|---|
| 类型 | `chore` |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 工程化与平台配置：1）仓库合并策略改为 squash-only、合并后自动删除源分支（`gh api` 已生效）；2）新增 `.github/workflows/ci.yml`（PR / `main` push 触发：锁定安装 → typecheck → build）；3）新增 PR 模板、`CODEOWNERS`、`dependabot.yml`（npm + github-actions 每周，人工确认，无自动合并）；4）开启 Dependabot 漏洞提醒与安全更新；5）仓库转公开后启用 `main` 分支保护（require PR、必需状态检查 `frontend`、conversation resolution、linear history、禁 force push / 删除、enforce admins、无绕过者）与 GitHub Secret scanning + push protection；6）按 `CONTRIBUTING.md` §12 清理示例环境文件：`frontend/.env.example`、`deploy/casdoor/.env.example` 移除占位 / 示例密码值，只保留变量名与说明注释；7）`CONTRIBUTING.md` 附录 A 按实测结果更新。 |
| 新增功能 | 无（工程化配置）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 平台项实测（`gh api`）：合并设置确认 `squash=true / merge=false / rebase=false / auto-delete=true`；Dependabot 漏洞提醒返回 204（已启用）；仓库转公开后复测：`main` 分支保护生效（require PR、必需检查 `frontend`、`enforce_admins=true`、linear history、禁 force push / 删除、conversation resolution、无 bypass）、Secret scanning 与 push protection 均 enabled、Dependabot 安全更新 enabled；受限项如实记录：Secret scanning 的 non-provider patterns / validity checks 仍为 disabled（响应确认需付费 SKU）；CI 工作流复跑成功（run 35185708338，job `frontend` success）；历史核查：`git log --all` 未发现真实 `.env` / 凭据 / Token 进入历史，两个 `.env.example` 已按 §12 清理；`npm run smoke` 依赖本地 Casdoor 沙箱，未纳入 CI。 |
| 文档同步 | `CONTRIBUTING.md` 附录 A（平台保护 / CI 状态实测更新）；`frontend/.env.example`、`deploy/casdoor/.env.example`（按 §12 清理占位值）；`docs/开发日志.md`（本条目 Push 9）。 |
| 风险与后续 | 1）CI 已通过并设为 `main` 必需检查，后续 PR 须保持全绿；2）Secret scanning 的 non-provider patterns / validity checks 需付费 SKU，暂不可用；3）`main` 分支保护当前 required approvals = 0（作者可自行合并），如需「非作者评审」硬门禁应调为 1，待 `wmj` / `lan` 的 GitHub 账号确认后执行；4）`CODEOWNERS` 待确认成员账号后补齐 / 调整；5）smoke 纳入 CI 需先解决 Casdoor 沙箱编排。 |
| 备注 | 本提交随本 PR 进入 `main`；未直接推送。 |

## 2026-09-17 13:18 +08:00 | px | px | Push 8

| 字段 | 内容 |
|---|---|
| 类型 | `docs` |
| 分支 | `px` |
| PR / commit | 随 PR #4（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 新增仓库根 `CONTRIBUTING.md`：《通用开发规则》第 11–17 节结合 LibiaoLink 现状的落地版（开发日志、Secrets 与通用安全、依赖与版本管理、生成物与不可变历史、高风险变更清单、发布与标签、Definition of Done、仓库落地清单）；`AGENTS.md` 增加「完整规则」章节指向 CONTRIBUTING 并声明冲突时从严。 |
| 新增功能 | 无（规则与文档）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 纯文档改动，无代码差异，未运行构建；落地前核对仓库现状：根目录结构、`frontend/package.json` 脚本（typecheck / build / smoke）、`frontend/.gitignore` 与根 `.gitignore` 实际忽略项（`git check-ignore` 验证 `.env` / `*.local` / `dist` / `files/*`（brand 除外）/ `logs` / `backup`）、确认无 `.github` 与 CI。 |
| 文档同步 | 新增 `CONTRIBUTING.md`；更新 `AGENTS.md`；`docs/开发日志.md`（本条目 Push 8）。 |
| 风险与后续 | 1）平台保护、CODEOWNERS、PR 模板、CI 门禁、ADR 目录尚未配置，已在 `CONTRIBUTING.md` 附录 A 标记待办；2）《通用开发规则》第 1–10 节尚未落地（章节编号已预留）；3）本 PR（#4）需非作者评审确认后合并。 |
| 备注 | 本提交进入 PR #4；`main` 未直接推送。 |

## 2026-09-17 12:26 +08:00 | px | px | Push 7

| 字段 | 内容 |
|---|---|
| 类型 | `docs` |
| 分支 | `px` |
| PR / commit | 直接推送 `px`（未创建 PR，未进 `main`）；本次为文档同步提交，不动前端代码。 |
| 做了什么 | 把 `docs/前端功能需求.md` 从 Push 5 状态补齐到 Push 6 实现：新增「项目详情页」全部功能描述（阶段标签导航、总览汇总卡、阶段分组任务表、四格进度条、任务详情抽屉、字段筛选、底部横向滑块）、任务数据模型与任务相关接口需求；并建立维护约定 —— 前端每次改动必须同步更新该需求文档（写入仓库根 `AGENTS.md` 与文档头部）。 |
| 新增功能 | 无（文档与约定）。 |
| 修复 / 重构 / 优化 | 修正需求文档滞后问题：此前文档停留在 Push 5（首页 + 搜索 + 新建项目），与含项目详情页的当前前端不一致。 |
| 测试与验证 | 文档类改动，无代码差异；内容按当前实现逐段核对（`TaskBoard` / `Tracker` / `TaskDrawer` / `ColumnPicker` / `TableScrollbar` / `ProjectDetail` / `AppHeader` / `data/tasks.ts`）。 |
| 文档同步 | `docs/前端功能需求.md`（全面更新 + 变更记录）；新增 `AGENTS.md`（文档同步约定）；`docs/开发日志.md`（本条目 Push 7）。 |
| 风险与后续 | 1）后续前端改动若未同步本文档，按 `AGENTS.md` 约定执行，避免再次滞后；2）任务进度 / 列显隐等内存态行为的接口建议已列入需求文档，待后端就绪后回填真实契约；3）`main` 未变更。 |
| 备注 | 推送 `px` 分支；本次仅文档与约定。 |

## 2026-09-17 12:20 +08:00 | px | px | Push 6

| 字段 | 内容 |
|---|---|
| 类型 | `feat` |
| 分支 | `px` |
| PR / commit | 直接推送 `px`（未创建 PR，未进 `main`）；本次含上一笔回退点快照 `9fc7e1f` 与本次功能提交。 |
| 做了什么 | 把项目详情页从占位骨架做成可用原型：源表（任务清单）落成可交互表格 —— 顶部阶段标签导航 + 「项目总览」汇总卡 + 阶段分组任务表（液体进度分组头、四格进度条、行点击抽屉、字段筛选、横向滑块），首页与详情页放开为全宽布局，认证门加载态换成无框 spinner。 |
| 新增功能 | 1）`data/tasks.ts`：44 条任务、16 个字段的数据模型与阶段派生逻辑（施工人数 / 紧急重要度 / 文件为演示值，源表对应列为空）；2）`components/TaskBoard.tsx`：阶段分组表格，列定义带最小宽 `min`、表宽 = 各列 `min` 之和，任务描述列锁定常显，阶段头内嵌淡绿色流动液体表示该阶段完成度；3）`components/Tracker.tsx`：四格进度条（灰=未开始、1 格绿=刚开工、2 格=完成一半、3 格=快完成、4 格=完成，悬停给文字提示、点击推进且不可回退）；4）`components/TaskDrawer.tsx`：行点击右侧抽屉展示全部字段；5）`components/ColumnPicker.tsx`：「筛选」按钮（列显隐 + 重置），默认显示项目经理 / 任务负责人 / 任务状态 / 紧急重要度 / 是否按时交付 / 输出成果文件 / 开始日期 / 预计完成日期 / 实际完成日期 / 变更关联，文件 / 项目进展描述 / 预计所需施工人数默认隐藏；6）`components/TableScrollbar.tsx` 与详情页底部 sticky 条：表格横向溢出时出现、不溢出自动隐藏，支持拖动滑块、点轨道跳转、键盘左右键；7）`components/Loader.tsx`：8 圆点脉冲加载动画，替换原先的加载卡片。 |
| 修复 / 重构 / 优化 | 1）主容器去掉 `max-w-[1440px]` 改全宽（Home / ProjectDetail）；2）顶栏面包屑副行去掉「项目经理：」；3）表头标签加 `truncate` + 悬浮全名，杜绝标签首尾相连；4）列宽体系由估算公式改为「每列最小宽之和」，`变更关联` 收窄（0.65fr/60px → 0.54fr/54px），任务描述列 460 → 340px（四格进度条位置随之左移且固定）；5）文件列文件名截到 4 个字加省略号，完整名保留在悬浮提示与抽屉；6）隐藏表格容器原生横向滚动条，避免与底部滑块重复；7）认证门拆开加载态与未登录态文案，避免未登录前的误提示。 |
| 测试与验证 | `npm run typecheck`（tsc 严格模式）与 `npm run build`（Vite 8 生产构建）通过；无头 Chrome 注入真实 SSO 令牌实测：默认列在 1324 与 1920 视口下 `scrollWidth = clientWidth`（滑块隐藏、15 个表头无一截断）；全列打开时「预计所需施工人数」与「实际完成日期」标签间距由 5px 提升到 46px；四格进度条 `barsX` 固定（437 → 377 → 317）且不随列筛选变动；文件列渲染为「技术协议…+1」；底部滑块拖动后表格 `scrollLeft` 0 → 173（到底）；2560 视口表格不溢出时滑块节点为空。 |
| 文档同步 | `docs/开发日志.md`（本条目 Push 6）；新增 `docs/字段对照清单.md`（源表字段与页面字段对照、默认显示列、待补字段）。 |
| 风险与后续 | 1）数据仍为前端内存态：进度点击、列显隐、新建项目刷新即回默认，待后端接口接管；2）源表数据瑕疵仍按原样呈现（人员进场 / 场地检查 / 施工对接状态为「待开始」但实际已完成；项目经理 4 行为空，页面用常量「贾海洋」）；3）字段筛选状态未持久化（刷新回默认列）；4）行内进度未写回后端，无操作审计；5）`main` 未变更，需评审后另建 PR。 |
| 备注 | 推送 `px` 分支；本次一并推送 `9fc7e1f` 快照（项目详情页骨架 + 顶栏头像菜单）。 |
## 2026-09-16 15:29 +08:00 | px | px | Push 5

| 字段 | 内容 |
|---|---|
| 类型 | `feat` |
| 分支 | `px` |
| PR / commit | 直接推送 `px`（未创建 PR，未进 `main`）；功能 commit `4446f33`，开发日志与需求文档随同一次推送提交。 |
| 做了什么 | 开发 LibiaoLink 首页「项目空间」：项目卡片列表、关键字搜索、新建项目弹窗的完整前端原型（`px` 分支），前端接入 Tailwind 4 原子化样式，统一卡片与控件视觉。 |
| 新增功能 | 1）`frontend/src/Home.tsx`：项目卡片网格（示例 20 个项目，4 列自适应降列），标题区含项目计数（共 N 个 / 搜索时显示找到 N 个）、新建项目按钮与搜索框；2）`frontend/src/components/Card.tsx`：序号徽标（浅灰底）+ 小车图标（CSS mask 上色、五色轮换）+ 项目编号 + 描述 + 项目经理 + 右下角更新时间，悬停缩放与主题色光晕；3）`frontend/src/components/SearchInput.tsx`：实时匹配项目编号 / 描述 / 更新时间 / 项目经理，支持一键清空与空态提示；4）`frontend/src/components/NewProjectModal.tsx`：居中弹窗表单（编号 / 描述 / 项目经理三项必填），取消、Esc、点遮罩均可关闭，创建后追加到列表（序号自增、时间取当前、颜色自动轮换）；5）小车图标素材入库（`assets/` 与 `frontend/public/`）。 |
| 修复 / 重构 / 优化 | 1）`app.css` 全局 `button` 样式收窄为 `.card button`（只作用于旧骨架页），修复 Tailwind 组件被全局按钮样式污染的问题；2）卡片宽度由固定 `w-64` 改为 `w-full`，随网格自适应；3）`App.tsx` 收敛为认证门（加载 / 未登录跳转 / 错误重试），登录后渲染首页。 |
| 测试与验证 | `npm run typecheck`（tsc 严格模式）与 `npm run build`（Vite 8 生产构建）通过；无头 Chrome 走完整 SSO 流程、注入真实令牌渲染首页实测：4 列 × 20 卡布局；搜索「日本」「08:15」「李伟」各命中 1 张，无匹配关键词出空态；新建弹窗打开 → 填 3 项 → 创建后列表变 21 张、计数同步、新卡片带当前时间。 |
| 文档同步 | `docs/开发日志.md`（本条目 Push 5）；新增 `docs/前端功能需求.md`（前端功能清单与后端接口需求，供后端开发参考）。 |
| 风险与后续 | 1）项目数据为前端内存态（刷新回到 20 张示例），待后端按需求文档提供接口后替换；2）项目经理与时间为示例数据；3）新建项目暂无权限控制与持久化；4）生产部署（构建产物托管、反向代理）尚未配置。 |
| 备注 | 本次推送至 `px` 分支；`main` 未变更。 |

## 2026-09-16 11:35 +08:00 | px | main | Push 4

| 字段 | 内容 |
|---|---|
| 类型 | `feat` |
| 分支 | `main` |
| PR / commit | 直接推送 `main`（未创建 PR）；commit `ad61736` |
| 做了什么 | 新增 LibiaoLink 前端接入参考（`frontend/`，React 19 + Vite 8 + TypeScript 严格模式），把「Casdoor 应用页点卡片 → 进入业务前端 → 自动单点登录 → 展示用户字段」整条链路在本地沙箱端到端跑通。 |
| 新增功能 | 1) 前端项目：`npm run dev` 起在 3000 端口，未登录自动跳 SSO，登录后展示 5 个标准字段（Name / DisplayName / Email / Id / Owner）与令牌声明；2) 开发用 OIDC 后端 `server/oidc-plugin.ts`：`/auth/login`（state + PKCE S256）、`/auth/callback`（code + verifier + secret 换令牌、HttpOnly 会话）、`/auth/me`（JWKS RS256 验签后返回用户字段）、`/auth/logout`（清理会话并跳 Casdoor 统一登出）；3) 冒烟测试 `scripts/smoke-test.mjs`（无浏览器，7 组断言）；4) `frontend/README.md`（接入说明、生产移植清单、常见问题）。 |
| 修复 / 重构 / 优化 | 本地 Casdoor 应用 `libiaolink` 补齐三项运行配置：Homepage URL = `http://localhost:3000`、Redirect URLs 增加 `http://localhost:3000/auth/callback`、开启 `enableAutoSignin`（已有会话免二次点击，对应接入标准第 4 条）。 |
| 测试与验证 | `npm run smoke` 全部通过（首页 / 登录入口 PKCE / 口令登录建会话 / 已有会话自动签发 / 回调换令牌 / 取用户字段 / 登出共 7 组）；`npm run typecheck`（tsc 严格模式）与 `npm run build`（Vite 8 生产构建）通过。 |
| 文档同步 | `README.md`（目录表与快速开始第 4 步）；`docs/本地沙箱(LibiaoLink 演练环境).md`（新增第九节「前端联调」，更新登录入口、配置快照、常见问题、与公司环境对照）；`docs/实测报告(SSO接入验证).md`（新增第六节：前端端到端验证结果 + 两条工程发现）；`docs/开发日志.md`（本文件，Push 1 起的历史见下方条目）。 |
| 风险与后续 | 1) 令牌交换目前放在 Vite 中间件（仅本地开发用），生产环境需把 4 个路由搬到后端（Java 可用 Spring Security oauth2Login），Cookie 加 `Secure`；2) 公司测试 / 正式环境的 `LibiaoLink` 应用仍待按接入标准调整（JWT-Custom、5 个 tokenFields、只留 `authorization_code`、替换 Logo）；3) 前端目前只是接入参考与起点，业务功能待开发。 |
| 备注 | 两个易踩点已记入实测报告：`/api/login` 的 `type=code` 不落地 SSO 会话（`type=login` 才建立）；「免二次点击」由登录页 JS 自动提交实现，服务端不会直接 302。 |

## 2026-09-16 11:11 +08:00 | px | main | Push 3

| 字段 | 内容 |
|---|---|
| 类型 | `docs` |
| 分支 | `main` |
| PR / commit | 直接推送 `main`；commit `26bfb56` |
| 做了什么 | 把公司测试环境的最新现状同步进文档：LibiaoLink 应用已由演练期的 `libiaolink`（clientId `75a4ed…`）更换为 `LibiaoLink`（clientId `41e162…`），本地沙箱联邦同步对齐；补充 `/callback` 报错说明。 |
| 新增功能 | 无（文档与运行环境同步）。 |
| 修复 / 重构 / 优化 | 定位并修复本地联邦登录的 `Invalid client_id`：根因是测试环境旧演练应用已被删除；本地认证源 `provider_authtest` 的运行配置改为指向新应用（clientId / secret），测试环境 `LibiaoLink` 应用的 Redirect URLs 补 `http://localhost:8000/callback`（联邦回跳，必须保留）。 |
| 测试与验证 | 管理员 API 复测联邦链路：测试环境出授权码 → 本地换令牌 `status: ok`；`panxing` 账号核对正常（displayName / email / owner / isAdmin）。 |
| 文档同步 | `docs/实测报告(SSO接入验证).md`（测试环境现状快照：13 个应用、18 个用户、待按接入标准调整的 4 项）；`docs/本地沙箱(LibiaoLink 演练环境).md`（联邦章节 clientId 更新、应用名修正、新增 `/callback` 常见问题）。 |
| 风险与后续 | 测试环境 `LibiaoLink` 应用仍未按接入标准配置：`tokenFormat=JWT`（应 JWT-Custom）、`tokenFields=[]`（应 5 字段）、Grant Types 开了 6 种（应只留 `authorization_code`）、Logo 为 Casdoor 默认图 —— 待调整。 |
| 备注 | 本地沙箱与测试环境的运行侧改动（不入 git）：认证源凭据、Redirect URLs。直接打开 `/callback` 报 `Unknown authentication type` 属正常现象（联邦回跳落地页，仅带 `code` / `state` 时有效）。 |

## 2026-09-15 14:53 +08:00 | px | main | Push 2

| 字段 | 内容 |
|---|---|
| 类型 | `feat` |
| 分支 | `main` |
| PR / commit | 直接推送 `main`；commit `9a43acb` |
| 做了什么 | LibiaoLink 统一登录（SSO）接入初版：基于开源 Casdoor 的方案设计、本地沙箱部署模板、应用图标与配套规范文档。 |
| 新增功能 | 1) `docs/公司统一登录(SSO)实施方案.md`：选型决策、部署、控制台配置、各系统接入方式、接口速查、上线清单、常见坑；2) `deploy/casdoor/`：本地联调部署模板（Docker Compose：Casdoor 4.4.0 + MySQL 8.0 + Redis 7，配置全部用环境变量注入，便于审计与回滚）；3) `assets/`：LibiaoLink 应用图标 SVG（浅色 / 深色两版，viewBox 收紧去掉四周空白，可直接填 Casdoor 应用 Logo 字段）；4) 文档：开发者接入标准（只用标准 OIDC、仅 authorization_code、JWT-Custom + Name / Owner / Id / DisplayName / Email 五个字段）、企业微信对接指南（登录通道 + 通讯录同步离职回收）、本地沙箱说明、实测报告。 |
| 修复 / 重构 / 优化 | 固化 MySQL DSN 拼接坑：`dataSourceName` 必须停在 `/` 结束（带上 `?charset=` 会与 dbName 拼出非法串、容器启动即 panic），字符集改由 MySQL 服务端参数保证 —— 已写进模板注释。 |
| 测试与验证 | 本地沙箱实测三轮：① 授权码 + PKCE 全链路（故意传错 `code_verifier` 被拒、`state` 防伪生效、单点登出可用、JWKS 本地验签 id_token 通过、refresh_token 换新令牌成功）；② 公司接入标准落地（JWT-Custom 五字段令牌、只留 authorization_code）；③ 合规改造。另做公司测试环境端到端演练（授权页 → 授权码 → 换令牌 → userinfo，令牌字段与登录名口径符合标准）与正式环境只读核对（企微 Provider 配置）。 |
| 文档同步 | README（目录表、公司环境对照、快速开始）、五份 docs 与 `deploy/casdoor/README.md` 一并上线。 |
| 风险与后续 | 默认令牌有效期偏长（内置应用 168 小时），生产需按应用改短并配合 refresh_token；角色 / 权限默认为空，做授权前需先建 Roles / Permissions；正式环境 `libiaolink` 应用尚不存在，需 SSO 管理员创建。 |
| 备注 | 本地登录入口：带应用 Logo 的 Casdoor 登录页（应用 `libiaolink`，clientId `libiaolink-a195b721bb30a7d4`），不依赖额外进程。 |

## 2026-09-15 10:47 +08:00 | px | main | Push 1

| 字段 | 内容 |
|---|---|
| 类型 | `chore` |
| 分支 | `main` |
| PR / commit | 直接推送 `main`；commit `f0eec6b` |
| 做了什么 | 创建 LibiaoLink 仓库（README 初版：仓库名与一句话定位）。 |
| 新增功能 | 无（仓库初始化）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 无。 |
| 文档同步 | README 初版。 |
| 风险与后续 | 待补充 SSO 接入方案与本地沙箱（见 Push 2）。 |
| 备注 | 仓库创建。 |
