# A01 / A02 / A03 / A14 扩展规则文案（一期四条）与金标用例

- 来源：系统功能书.md A3-14（超期升级）、A6-05 / C2-06（自定义待办提醒）、A7-03（日报推送）、A7-05（漏填提醒）、C2-03（一期实现 A01 / A02 / A03 / A14）；技术设计v0.2 §6.1 / §6.2；ADR-026（问题 SLA：T+1 提醒 / T+3 升级）；ADR-028（时区 Asia/Shanghai）。
- 状态：**建议稿** —— 需求说明书只给了 R01 ~ R07 的逐字文案，A 系列原文未给文案：触发时机 / 条件 / 收件人与正文由本文件先行约定并固化金标，业务回执后按结论改文案与断言（待确认清单见文末）。
- 评审：本文件由 **wmj** 起草（rules 归属），**请 px 复核**（docs / 前端呈现线），**lan 复核**规则与调度口径（M5-02 ~ M5-06 接线）。
- 口径：表中「消息内容」为逐字基准（变量占位符保持 `{字段}` 原文格式，不改字）；引擎模板见 `server/src/modules/automation/builtin-rules.ts`，金标回放见 `server/test/automation-a-series.test.ts`。
- 幂等键 = 规则 + 实体 + 触发窗口（技术设计v0.2 §6.2）；分组规则（A01 / R07）幂等键落**收件人粒度**。

---

## A01 日报应填未填提醒

| 项 | 设计说明（先行口径） |
|---|---|
| 触发时机 | 每日 19:30 扫描（原文未定时刻：先行 = A02 汇总推送后 30 分钟） |
| 触发条件 | 当日为工作日，且名册成员当日未提交日报（草稿未提交仍算未填） |
| 执行动作 | 向未填成员发提醒（站内信 + 企微应用消息）；同一人多项目合并为一条 |
| 消息标题 | 请及时填写今日日报 |
| 消息内容 | 你今天还有日报未提交：{项目清单}请尽快填写，辛苦啦！ |
| 补充设计 | ① 数据面 = `GET /api/v1/projects/{id}/reports/missing`（Push 162：名册 × 工作日历 × 当日状态），非工作日整列为空（不催报）；② 同一人多项目 → 一条清单式消息（`{项目清单}` = 项目名以「、」连接，顺序随数据面名册 / 入参顺序）；③ 单条命中同走清单形态（清单只有一项）；④ 正文**不附**他人日报内容（未填者只收清单 + 提示语）；⑤ 漏填名单的「已提醒」标记随 M5-06 发送记录（lan）。 |

- 模板码：`A01_INBOX_MERGED`（站内信）/ `A01_WECOM_MERGED`（企微应用消息）——同一文案，渠道各自一条便于后续差异化。
- 窗口与幂等：`SAME_DAY`（基准字段 `report.date`）→ 窗口键 = 业务日；幂等键 = `A01:<成员 id>:<日期>`（收件人粒度，跨项目合并不漏判）。
- 金标：工作日未提交命中（双渠道逐字 + 收件人 = 本人）；非工作日不催；已提交不进名单；同一人多项目合并为一条（清单顺序 = 入参顺序）；非当日槽位不触发；补跑（同窗口已发送）判 `duplicate`。

## A02 日报每日汇总群播报

| 项 | 设计说明（先行口径） |
|---|---|
| 触发时机 | 每日 19:00（系统功能书 A7-03） |
| 触发条件 | 当日已有提交（`entry_count > 0`） |
| 执行动作 | 推送项目群（企微群机器人）；推送失败降级站内信（投递层 · lan · M5-03 / M5-04） |
| 消息标题 | 日报汇总 \| {项目名}（{日期}） |
| 消息内容 | 今日 {提交人数} 人已提交（应填 {应填人数} 人），发现问题 {问题数} 条。{汇总正文} |
| 补充设计 | ① 数据面 = `GET /api/v1/projects/{id}/reports/summary`（Push 162）；② `{汇总正文}` 拼装口径（先行）= 逐条「{作者}：{当日完成工作}」，条目顺序随 `summary.entries`（提交时刻升序）—— 拼装在数据面完成，引擎只按字段 `report.summary_text` 取值；③ 当日无提交不发（不在群里刷空汇总）；④ 群播报按队列限速（C2-10）。 |

- 模板码：`A02_GROUP`（wecom_group）；窗口 `SAME_DAY`（`report.date`）；幂等键 = `A02:<项目 id>:<日期>`。
- 金标：有提交命中（标题与正文逐字，人数 / 问题数 / 正文取汇总数据面）；无提交不命中；项目未绑定群 → `recipient_missing`；补跑判 `duplicate`。

## A03 问题超期提醒与升级（T+1 / T+3）

| 项 | 设计说明（先行口径） |
|---|---|
| 触发时机 | 每日 09:00 扫描：超过处理时限 1 天（T+1）提醒责任人；超过 3 天（T+3）升级项目经理（ADR-026） |
| 触发条件 | 问题未闭环（`issue.state != done`，完成即止） |
| 执行动作 | T+1 → 通知责任人；T+3 → 通知项目经理（均为站内信 + 企微应用消息） |
| 消息标题 | T+1：问题处理超时提醒；T+3：问题超期升级 |
| 消息内容 | T+1：{责任人}，你的问题「{问题标题}」已超过处理时限 1 天，请尽快处理并更新进展！ |
|  | T+3：{项目经理}，问题「{问题标题}」超期 3 天仍未闭环，已升级给你，请关注并推动处理！ |
| 补充设计 | ① 时限 `due_at` 按归类配置（ADR-026），窗口基准字段 = `issue.due_at`；② 窗口是「**恰为** T+N 当天」（补发由 M5-02 调度承担，避免连续多日轰炸）；③ 责任人未分派时 T+1 不发（`recipient_missing`）、**T+3 仍升级项目经理**（未分派更需升级）；④ 升级记录可查随 M5-06 发送记录 / `automation_runs`（lan）。 |

- 模板码：`A03_T1_INBOX` / `A03_T1_APP`（同文）、`A03_T3_INBOX` / `A03_T3_APP`（同文）。
- 规则形态：A03 = **一条业务规则、两个触发窗口** → 引擎规则集内两行同 `code`（`name` 区分 T+1 / T+3；回落消息带 `ruleName`）；幂等键分别 = `A03:<问题 id>:<due+1>` / `A03:<问题 id>:<due+3>`；落库形态（`automation_rules` 是否同 code 两行）随 M5-06（lan）接线时定 —— 已登记差异。
- 金标：T+1 命中责任人、T+3 命中项目经理（两窗口互斥：T+3 当天不再重复提醒责任人）；已完成的问题两窗口都不发；责任人缺失 → T+1 不发 / T+3 仍升级；同窗口补跑判 `duplicate`。

## A14 自定义待办提醒

| 项 | 设计说明（先行口径） |
|---|---|
| 触发时机 | 待办提醒日当天 09:00（原文只说「提醒时间」，时分粒度随 M5-02 / M5-06） |
| 触发条件 | 待办未完成（`todo.status != done`） |
| 执行动作 | 按待办的提醒对象发送（站内信 + 企微应用消息） |
| 消息标题 | 待办提醒：{待办标题} |
| 消息内容 | {提醒对象}，你的待办「{待办标题}」已到提醒时间。{待办内容} |
| 补充设计 | ① 待办字段 = 标题 / 内容 / 关联项目或任务 / 提醒时间 / 提醒对象 / 重复规则（系统功能书 C2-06）；② 重复规则由待办模块**展开为逐次提醒日期**，引擎按 `todo.remind_date` 当天命中（`SAME_DAY`）；③ 内容为空 → 正文止于句号（不出现悬空标点）；④ 关联项目 / 任务的深链附在消息尾部（投递层 · M5-03 / M5-06），正文不含关联字段；⑤ **`todos` 表与重复展开尚未落** —— 本切片交付规则定义 + 文案 + 金标，落库随 M5-06 / M8（差异登记）。 |

- 模板码：`A14_INBOX` / `A14_APP`（同文）；窗口 `SAME_DAY`（`todo.remind_date`）；幂等键 = `A14:<待办 id>:<提醒日>`。
- 金标：提醒日命中（双渠道逐字，标题含待办标题）；非提醒日不命中；已完成待办不提醒；内容为空不出现悬空标点；补跑判 `duplicate`。
---

## 回放主体字段口径（唤醒层 → 引擎）

引擎只认「主体快照」（不连库）：`kind` 决定规则归属，`fields` 提供条件与模板变量，`recipients` 提供收件人解析。

| 主体 | kind | 规则 | 字段（引擎读取） | 收件人表 | 数据面 |
|---|---|---|---|---|---|
| 日报名册槽位（人员 × 项目 × 日期） | `report_slot` | A01 | `report.date` / `report.is_workday` / `report.submitted` / `report.user_id` / `project.name` | `report.member` | `GET /api/v1/projects/{id}/reports/missing`（Push 162） |
| 项目日报（项目 × 日期） | `project_day` | A02 | `report.date` / `report.entry_count` / `report.headcount_total` / `report.issue_count` / `report.summary_text` / `project.name` | `project.group` | `GET /api/v1/projects/{id}/reports/summary`（Push 162） |
| 问题 | `issue` | A03 | `issue.title` / `issue.state` / `issue.owner_name` / `issue.due_at` | `issue.owner`（T+1）/ `project.manager`（T+3） | issues（M6-01 / M6-02）+ `due_at`（ADR-026） |
| 自定义待办 | `todo` | A14 | `todo.title` / `todo.content` / `todo.status` / `todo.remind_date` | `rule.members` | `todos` 表（未落，差异登记） |

- 触发窗口基准字段（`trigger.baseField`）从 `fields` 取 `YYYY-MM-DD` 串；模板变量映射见 `builtin-rules.ts` 的 `SUBJECT_TEMPLATE_VARIABLES`（`@recipient` = 本次解析出的收件人名称）。
- 主体类型不匹配（如把问题主体喂给 R02）在回放中登记为 `subject_mismatch`，不静默命中 —— 见金标用例「主体类型不匹配」。

---

## 待确认清单（原文未定，按建议值先行、可修订）

| # | 规则 | 待确认点 | 先行口径 |
|---|---|---|---|
| 1 | A01 | 漏填提醒的每日时刻（原文未给） | 19:30（A02 汇总推送后 30 分钟） |
| 2 | A01 | 提醒粒度：按项目还是按人 | 按人合并为一条（列出未填项目），避免同人多项目重复轰炸 |
| 3 | A01 | 正文是否附当日汇总条目 | 不附（只列未填项目 + 提示语；不把他人日报内容推给未填者） |
| 4 | A02 | `{汇总正文}` 的拼装格式（连接符 / 是否换行 / 是否带日期） | 逐条「{作者}：{当日完成工作}」，数据面拼装后以 `report.summary_text` 注入 |
| 5 | A02 | 当日无提交是否仍播报 | 不播报（`entry_count > 0` 为条件） |
| 6 | A03 | T+3 升级时是否同时再提醒责任人 | 只发项目经理（T+3 当天不重复轰炸责任人） |
| 7 | A03 | 窗口语义：「恰为 T+N 当天」还是「≥ N 天每天提醒」 | 恰为当天（补发靠 M5-02 调度；避免连续多日提醒） |
| 8 | A14 | 重复规则（每日 / 每周 / 每月）的提醒展开方式 | 待办模块展开为逐次提醒日期，引擎按当天命中 |
| 9 | A14 | 提醒时刻是否精确到时分 | 每日 09:00 扫描当天到期待办；时分粒度随 M5-02 / M5-06 |
| 10 | A14 | 关联项目 / 任务是否进正文 | 不进正文（深链由投递层附在消息尾部） |

- 确认结果回写本文件与金标用例（断言见 `server/test/automation-a-series.test.ts`）；文案变更走独立评审（改文案 = 改金标）。

## 金标用例对照（`server/test/automation-a-series.test.ts`）

| 规则 | 断言要点 |
|---|---|
| A01 | 工作日未提交 → 双渠道逐字 + 收件人 = 本人 + 幂等键落收件人粒度；非工作日不催；已提交不命中；同一人多项目合并（清单顺序 / `mergedFrom`）；非当日 `window_mismatch`；补跑 `duplicate` |
| A02 | 有提交 → 标题与正文逐字（人数 / 问题数 / 正文取数据面）+ 收件人 = 项目群；无提交 `not_matched`；未绑定群 `recipient_missing` |
| A03 | T+1 提醒责任人（逐字 + 窗口键 = due+1）；T+3 升级项目经理（逐字 + 窗口键 = due+3）；两窗口互斥；已完成两窗口都不发；责任人缺失 → T+1 `recipient_missing` / T+3 仍升级；补跑 `duplicate` |
| A14 | 提醒日命中双渠道逐字；内容为空正文止于句号；非提醒日 `window_mismatch`；已完成 `not_matched`；补跑 `duplicate` |
| 引擎 | `T_PLUS_3` 窗口（due+3 09:00）；主体类型不匹配 `subject_mismatch`；规则集含 A 系列四条（A03 两窗口）与主体类型登记；**模板一致性**（动作模板存在、模板变量都能由主体字段 / 合并清单提供 —— 防「缺变量」运行时才炸） |

## 与其他文档的关系

- 系统功能书.md：A3-14 / A6-05 / A7-03 / A7-05 / C2-03 / C2-06 的规则侧逐字落点（文案为建议稿，见文首状态）。
- `docs/rules/R01-R07-内置规则文案.md`：R 系列（需求说明书原文逐字）；本文件为其 A 系列续篇。
- 技术设计v0.2-架构与数据模型.md §6：规则模型 / 执行保证（幂等、调度、投递、留痕）；技术设计v0.3-实施与验收.md §3.6（M5-05 出口：R01~R07 + A01/A02/A03/A14 金标回放全绿）。
- ADR-026（问题 SLA：T+1 / T+3）、ADR-028（时区）、ADR-005（队列与调度）。
