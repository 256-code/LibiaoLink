{
  "name": "@libiaolink/database",
  "version": "0.1.0",
  "private": true,
  "description": "LibiaoLink 数据库基线：只追加迁移 / 最小权限角色 / 带 advisory lock 与漂移校验的迁移器（g3 · S5）",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "migrate": "node scripts/migrate.mjs",
    "migrate:dry": "node scripts/migrate.mjs --dry-run"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
