# 开发日志

> 每完成一次推送，在这里追加一条记录：**做了什么、效果与验证、风险与后续**。
> 记录格式：`日期 时间 | 作者 | 分支 | Push 序号`；**作者 = 当次推送人**（目前团队：`px`、`wmj`、`lan`），下面是明细表；最新一条在最上面。

## 2026-09-20 15:26 +08:00 | px | px | Push 91

| 字段 | 内容 |
|---|---|
| 类型 | `refactor(frontend)`（任务编辑入口收口：删除任务编辑弹窗，改在任务详情抽屉里直接改） |
| 分支 | `px` |
| PR / commit | 随 PR（`px` → `main`，squash 合并）提交；未直接推送 `main`。 |
| 做了什么 | 业务反馈：**「这个不需要了 要修改直接在抽屉里面修改即可」**（截图 = 任务编辑弹窗 + 抽屉头部「编辑任务」按钮）。① **删除任务编辑弹窗**：`frontend/src/components/TaskEditModal.tsx` 整文件删除（`git rm`），保存值类型 `TaskEditSubmit` 挪到 `TaskDrawer.tsx` 导出；`TaskBoard.tsx` / `TaskKanban.tsx` 里的 `editingTask` 状态与弹窗渲染一并删除，`ProjectDetail.tsx` 的导入改指 `TaskDrawer`；② **任务详情抽屉改为可直接编辑**（`TaskDrawer.tsx` 重写字段区）：项目经理 / 任务负责人（`MemberSelect` 可搜索人员下拉，项目经理是项目级字段）/ 紧急重要度（`SelectMenu`）选完即存；开始 → 预计完成日期（`DateRangePicker`，与分类筛选同一套）确定即存，「预计所需天数」按区间联动只读；预计所需施工人数（数字输入，≥ 0，不合法时红字提示且不写）与项目进展描述（多行文本）失焦即存；**值没变不写**（避免无谓刷新项目 `updatedAt`）；存完抽屉右下角闪一下「已保存」（约 1.6 秒），页脚常驻「改动即时保存（原型暂存浏览器内存）· 点空白处或按 Esc 关闭」；③ **只读口径不变**：任务状态 / 项目进度 / 是否按时交付 / 实际完成日期（派生；状态与实际完成日期仍在任务表行内改，§6.9）、输出成果文件 / 文件 / 变更关联；④ **入口合并**：任务表整行点击与行内铅笔（`aria-label` = 「编辑任务」）都打开抽屉，两块钱看板卡片同理 —— 不再有第二层浮层；⑤ 保存路径不变：仍走 `handleSubmitTaskEdit`（同一张任务覆盖表 + 项目经理回写项目），任务行 / 抽屉 / 汇总卡即时同步，状态与逾期按新日期重新派生。 |
| 新增功能 | 无新业务功能（编辑能力从弹窗平移到抽屉内联）。 |
| 修复 / 重构 / 优化 | 删掉一个浮层组件与两处 `editingTask` 状态；顺带把抽屉的「失焦保存」做成读草稿 ref（`draftRef`）而不是渲染期取值 —— 输入与失焦落在同一批处理时不会漏存；输入过程也同步写 ref（`updateDraft`）。 |
| 测试与验证 | 1）`frontend/`：`npm run typecheck` 通过；`npm run build` 通过（`dist/assets/index-GvtXzJ-z.css` 65.15 kB、`dist/assets/index-sst8gNcQ.js` 371.56 kB —— 删掉弹窗后 CSS / JS 均比 Push 87 小）；2）无头 Chrome + CDP 实机（1500×1000，项目 `inmu-0010`）：任务行点击与行内铅笔两条入口都打开抽屉，抽屉里可编辑控件逐个核对（选择项目经理 / 选择任务负责人 / 选择紧急重要度 / 项目进展描述 / 选择开始与预计完成日期 / 预计所需施工人数），**抽屉内没有「编辑任务」按钮**、`[role=dialog]` 只有抽屉本身（无第二层浮层）；实改走查（真实键盘输入 + 失焦）：紧急重要度 高 → 低（任务行色签同步）、预计所需施工人数 2 → 7、项目进展描述 → 「等甲方确认布局口径」（任务表「项目进展描述」列由「—」变该文案）、日期 5/20 → 5/10–5/15（天数联动 6 天、关闭再开值仍在、是否按时交付按新日期派生为「逾期已交付」）；每次保存后右下角出现「已保存」；`Esc` 关闭抽屉；「人员任务分配」看板卡片同样打开可编辑抽屉；3）回归：任务表行内编辑仍在（44 个状态单元格可点开「修改任务状态」浮层、Esc 关闭）、抽屉 / 看板「添加」两条入口未受影响。 |
| 文档同步 | `前端功能需求.md`（§6.4 行交互、§6.5 任务详情抽屉新增「要改直接在抽屉里改」「只读字段」两条、§6.8 改为「任务编辑（Push 63 弹窗 → Push 91 起在任务详情抽屉里直接改）」、§6.9 / §6.10 / §6.11 措辞对齐 + 变更记录 Push 91）、`字段对照清单.md`（口径补充补 Push 91 一条），与代码**同一次推送**；本条目。`系统功能书.md` 不涉及定档口径、不改。 |
| 风险与后续 | 1）抽屉里**不能改**任务状态 / 实际完成日期（派生字段，口径不变）：要改去任务表行内（§6.9）；若业务希望抽屉里也能改，写入口径 A12~A14 已定案，前端接一下即可；2）任务负责人**不能在抽屉里清空**成「待分配」（人员下拉没有「空」选项，与任务表行内一致）—— 需要的话下拉里加一项；3）「已保存」是前端内存态提示，接后端后要按接口结果提示成功 / 失败；4）原型仍存浏览器内存（换项目 / 刷新回初始数据）。 |
| 备注 | 推送 `px`（未直接推送 `main`）。**Push 编号**：工作提交 `a327071` 于 15:26 推 `px` 并开 PR #66；按「先入 main 者为准」，wmj 的条目已先占 Push 88 / 89 / 90，本条顺延为 **Push 91**（`前端功能需求.md` / `字段对照清单.md` 里同一批的「Push 88」字样已同步改为 Push 91）。 |

## 2026-09-20 15:45 +08:00 | wmj | wmj | Push 90

| 字段 | 内容 |
|---|---|
| 类型 | `chore(merge)`（同步 main：px 的 [PR #62](https://github.com/256-code/LibiaoLink/pull/62)（frontend 侧 Push 85）、[PR #63](https://github.com/256-code/LibiaoLink/pull/63)（frontend 侧 Push 86）、[PR #65](https://github.com/256-code/LibiaoLink/pull/65)（frontend 侧 Push 87）合入后的 `f8efd17` → `fcb5f45`） |
| 分支 | `wmj` |
| PR / commit | 并入 [PR #64](https://github.com/256-code/LibiaoLink/pull/64)（`wmj` → `main`，h4 卡；本同步提交进入同一 PR）；未直接推送 `main`。 |
| 做了什么 | 把 `origin/main` 合并回 `wmj`（本 PR 内两次同步：`f8efd17` = px 的 PR #62 / #63，`fcb5f45` = px 的 PR #65；冲突都只在 `开发日志.md`，双方都在顶部插条目、解冲突口径一致）—— 按「先入 main 者为准」解决编号冲突：px 的 PR #62 / #63 / #65 已先入 main 占 **Push 85 / 86 / 87**，本分支此前两条未入 main 的条目顺延为 **Push 88**（h3 合入 main 的同步记录）、**Push 89**（h4 · S6·task），本同步条目占 **Push 90**；文件按编号倒序保留（90 → 89 → 88 → 87 → 86 → 85 → 84…），两条顺延条目的备注已注明。合入的 `frontend/src/ProjectDetail.tsx` / `frontend/src/components/TaskKanban.tsx` / `前端功能需求.md` 原样来自 main（px 侧），无本地改动。 |
| 新增功能 | 无（同步合并）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 纯文档冲突解决（两次同步同口径）：冲突标记已清、`git status` 无未解决冲突；h4 的代码与契约未变动（Push 89 条目的全链路验证仍有效：shared typecheck / generate / drift 0 / build + server typecheck / vitest 130 例 / nest build / check:boundaries 0 违规）。合入的 `frontend/` 三文件原样来自 main（PR #62 / #63 / #65 的 CI 已绿）；本地未装 `frontend/` 依赖，**未在本机复跑 frontend typecheck / build**，由本 PR 的 CI 四 job（frontend / server / database / shared）复核。 |
| 文档同步 | 本条目 + `开发日志.md` 冲突解决与编号顺延（88 / 89 / 90）。 |
| 风险与后续 | 1）**接口口径待对齐（跨线）**：px 的「添加 → 临时任务」（自填标题建任务）落到后端即 `POST /api/v1/projects/{id}/tasks` 的手工创建口径 —— 本卡按 A1-13 将其限定为**仅管理员**（非管理员 403）；前端目前是本地状态（未接接口），接线时需确认业务是否允许普通成员建临时任务（若要放开，改 `TaskService.create` 一处 + 契约注释即可）；2）本分支与 `main` 已同步，PR #64 可 squash 合并。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。 |

## 2026-09-20 15:30 +08:00 | wmj | wmj | Push 89

| 字段 | 内容 |
|---|---|
| 类型 | `feat(task)`（h4 · S6·task：M3-01 任务列表 / 详情 + M3-02 进度与状态 + 项目总览四格） |
| 分支 | `wmj`（本轮开 PR `wmj` → `main`；未直接推送 `main`） |
| PR / commit | 随 [PR #64](https://github.com/256-code/LibiaoLink/pull/64) 提交（`f159655`）；未直接推送 `main`。 |
| 做了什么 | ① 契约：`shared/src/modules/tasks.ts`（`TaskCreateBody.taskNodeId` 补 409 语义 + 节点须属本项目；`TaskProgressUpdateBody.note` 补写 note 并留痕语义）、错误码新增 409 `TASK_ALREADY_EXISTS`，生成物重跑（paths 49 / schemas 117，drift 0）；② server 新模块 `server/src/modules/task/`（`task.rules` 纯函数 / `task.query` 筛选排序解析 / `task.repository` / `task.service` / `task.stats` / `task.controller` / `task.module` + `index` 唯一出口）：`GET /api/v1/projects/{id}/summary` 项目总览四格、`GET …/{id}/tasks`（阶段 / 负责人 / 展示态多值（条件下推 SQL）/ `q` + 排序白名单（默认序 = 阶段序 + 预计开始 + 创建时间 + id））、`GET …/{id}/tasks/{taskId}` 详情（文件清单 + 免 N+1 行摘要）、`POST …/{id}/tasks`（任务节点生成判重 409 / 手工创建仅管理员）、`PATCH …/{id}/tasks/{taskId}`（乐观锁 409 + 三态联动）、`PATCH …/progress`（进度联动，响应同列表行形）；③ 规则：展示五态读时派生不写回存储（A1-06 / A12 / A14；`overdue` 派生优先、`early_done` 按实际早于预计）、`applyStatusWrite` / `applyProgressWrite` 联动、`shanghaiToday` 按 ADR-028 以参数进 SQL；④ 留痕 / 队列：`task_events` 四类型（status / progress / date / note，before → after JSON）+ outbox `task.created` / `task.updated` / `task.progress_changed`（dedupeKey 带版本）；⑤ h3 过渡口径收口：`node/gate.repository` 删直读 `tasks` 的 `countStageTasks` / `listStageProgress`（改只查节点），`GateService` 注入 `TaskStatsService`（阶段推进门禁「任务全 done」与阶段任务计数走对端出口；`StageProgressRow` 去任务字段；`files` 直读随 i1）；⑥ 归档项目写保护 409 `PROJECT_ARCHIVED`（ADR-027）、任务变更 touch 项目 `updated_at`（ADR-022 ④）。 |
| 新增功能 | 任务列表 / 详情 / 创建 / 编辑 / 进度五个 HTTP 入口（含展示五态筛选与排序）；项目总览四格；阶段任务统计出口（供 h3 门禁复用）。 |
| 修复 / 重构 / 优化 | h3 遗留过渡口径收口 —— node 模块不再直读 `tasks` 表（跨模块只经 index 出口，`check:boundaries` 83 文件 / 286 依赖 / 0 违规）；`server/test/flow-gate.test.ts` 适配注入 `TaskStatsService`。 |
| 测试与验证 | `shared`：`tsc` + `generate` + `check-drift`（漂移 0）+ `tsc -p tsconfig.build.json`；`server`：`typecheck` 通过、`vitest` 11 文件 130 例全绿（新增 `test/task-rules.test.ts` 17 例 + `test/task-service.test.ts` 13 例）、`nest build` 通过、`check:boundaries` 0 违规。本地实机（真 PG :55432 + 真 api :3011 + 桩会话）：建任务 201（预计完成已过 → `overdue` + `onTime=false`；自动带出负责人名与文件摘要 0/0/0）、同节点判重 409 `TASK_ALREADY_EXISTS`、非管理员手工创建 403 `FORBIDDEN`；进度 0.25 / 0.5 / 1（1 → done + 当天完成日期 + `early_done` / `onTime=true`）→ 写回 0.75 → active 并清完成日期；改 `status=done` 满格 + 缺省当天；改预计完成日期后翻 `overdue` 且 `filter[status]` 命中 / 多值筛选正确排除、`sort=title:asc` 生效；summary `{currentStage: design, overdue: 1, done: 1, total: 4}`；stages 任务计数与阶段推进 422 `STAGE_GATE_NOT_PASSED`（`task_not_done 1/3`）经新出口正常；stale version 409；归档项目 409 `PROJECT_ARCHIVED`；psql 核对 `task_events` 8 条（before → after 正确）+ outbox 9 条（created 3 / progress_changed 4 / updated 2）+ 项目 `updated_at` 已 touch。 |
| 文档同步 | `server/src/modules/task/README.md`（占位 → h4 实况 + 边界与后续）、`server/README.md`（任务接口段 + h3 过渡口径收口 + 测试计数 130 例 / 11 文件 + 后续卡片）、`shared/README.md`（M3-01 / M3-02 切片行标已入 + 现状行 Push 88）、`技术设计v0.3-实施与验收.md` §3.1 状态行、本条目。 |
| 风险与后续 | 1）**待复核差异**（评审后置，随 PR 描述）：记录级 404 与成员权限矩阵随 h6（当前读登录即可、写成员平权）、手工创建仅管理员的口径随 A1-13 复核、列表快捷筛选参数未做、任务软删 / 批量 / `deliverableTypes` / 门禁增强 M3-03 ~ M3-06 属后续卡、列表默认序暂无复合索引（随 M3-06 压测一起评）、`GateService` 仍直读 `files` 表随 i1；2）h4 验收（① 状态口径与系统功能书 A1-06 一致；② 进度 0 / 25 / 50 / 75 / 100 可更新）达成，卡片可置 done；3）frontend 依赖未在本机安装，前端本轮未改动，CI 四 job 复核。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。**Push 编号**：px 的 PR #62 / #63 / #65（看板列内滚动 / 添加两口径 / 横向滚动条与列底留白）已先入 main 占 Push 85 / 86 / 87，本卡按「先入 main 者为准」顺延为 **Push 89**（本分支先前的同步条目占 Push 88）。 |

## 2026-09-20 15:20 +08:00 | wmj | wmj | Push 88

| 字段 | 内容 |
|---|---|
| 类型 | `chore(merge)`（同步 main：[PR #61](https://github.com/256-code/LibiaoLink/pull/61) squash 合入后的 `407c66d`） |
| 分支 | `wmj` |
| PR / commit | 直属常驻分支 `wmj` 的同步合并（h3 已随 PR #61 入 main，无待合内容，不另开 PR）；未直接推送 `main`。 |
| 做了什么 | **h3 · S6·blueprint/node 已 squash 合入 main**（`407c66d`，PR [#61](https://github.com/256-code/LibiaoLink/pull/61)；合并前 CI 四 job 全绿：frontend / server / database / shared）。本次把 `origin/main` 合并回 `wmj`，使常驻分支与 main 对齐（squash 合入不留原始提交，合并后内容一致、仅历史分叉），下一张卡从 clean 出发。 |
| 新增功能 | 无（同步合并）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 同步合并为树等同合并（main 的 squash 提交内容 = 本分支 PR head `8cb5463` 的树），无冲突、无内容变化；`git status` 干净；纯文档（免 typecheck / build）。 |
| 文档同步 | 本条目。 |
| 风险与后续 | 1）h3 卡验收（蓝图 round-trip 无损 + 缺必交成果文件时完成被拒）达成，卡片可置 done；2）**待复核差异**（评审后置，见 PR #61 描述 10 条）：阶段回退范围、`GateService` 直读 `files` / `tasks` 表的过渡口径、首阶段回退 409、老项目 `blueprintVersion=0` 无回填等，评审意见到位后按新卡回改；3）下一张卡候选：`h4 · S6·task`（任务主表 / 五态派生 / 进度聚合）与 `h5 · S6·PoC-9`（蓝图 round-trip 与门禁拒绝的证据入库，本次实机已验证其内容）。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。**Push 编号**：px 的 PR #62 / #63 / #65 已先入 main 占 Push 85 / 86 / 87，本同步条目（h3 合入 main 的记录）按「先入 main 者为准」顺延为 **Push 88**；h4 卡本身占 Push 89。 |

## 2026-09-20 15:11 +08:00 | px | px | Push 87

| 字段 | 内容 |
|---|---|
| 类型 | `fix(frontend)` / `style(frontend)`（两块钱看板：底部横向滚动条改隐式 + 列底留白收口） |
| 分支 | `px` |
| PR / commit | [PR #65](https://github.com/256-code/LibiaoLink/pull/65)（`px` → `main`，squash 合并，CI 四项全绿：frontend / server / database / shared）；合并时间 2026-09-20 15:14 +08:00，合并提交 `fcb5f45`；未直接推送 `main`。 |
| 做了什么 | 业务反馈：**「这个底部滚动条以及留白太丑了」**（截图 = 人员任务分配看板列底：Windows 原生横杠带左右箭头 + 底下一大块空白）。① **看板横向滚动条改隐式**：列排布容器由裸 `overflow-x-auto` 换成 `ScrollArea axis="horizontal"`（`ScrollArea` 新增 `axis` 参数；原生滚动条隐藏 → 那条带箭头 / 灰凹槽的原生横杠不再出现，滑块默认透明、滚动 / 悬停时浮在列底预留的 12px 留白里，与列内纵向滑块同一套）；② **列高改按「铺满视口」算**：`COLUMN_SHELL` 由 `h-[calc(100vh-15.5rem)]` 改成 `h-[calc(100vh-12.75rem)]`（上限 52rem / 下限 22rem 不变）—— 列底与页面底之间不再留一大块空白；③ 看板 `pb-3` 从滚动视口挪到外层（给横向滑块留泳道）；卡片材质 / 卡片字段 / 列头 / 列底「添加」/ 两种添加入口（临时任务、阶段任务）一律未动。 |
| 新增功能 | 无新业务功能（看板横向滚动条改为隐式滑块）。 |
| 修复 / 重构 / 优化 | `ScrollArea` 支持 `axis`（默认 `vertical`，四处既有用法 —— 分类筛选侧栏 / 任务抽屉 / 任务编辑弹窗 / 看板列内 —— 类名与行为不变）；看板横向滚动改用同一组件，删掉裸 `overflow-x-auto pb-3`。 |
| 测试与验证 | 1）`frontend/`：`npm run typecheck` 通过；`npm run build` 通过（`dist/assets/index-CqP7b0np.css` 66.83 kB、`dist/assets/index-D-ldHdUr.js` 376.97 kB）；2）无头 Chrome + CDP 实机（1500×1000，项目 `inmu-0010`）：「人员任务分配」看板 —— 横向滚动视口原生滚动条占位 `0px`（Push 86 时是 15px 原生横杠）、10 列 / 列高 796px（= `100vh-204px`）、列底到视口底 56px（Push 86 约 100px）、`document.scrollHeight = 1000 = innerHeight`（页面不产生纵向滚动）；横向滑块 `role=scrollbar` / `aria-orientation=horizontal` 存在，滚到 `scrollLeft=700` 后浮现（宽 712px）；「任务进展」看板同口径（5 列，滑块宽 1432px）；3）回归：列内纵向滑块仍在（两块看板 6 / 3 个），项目列表页「分类筛选」侧栏（620px 高视口）滚动后纵向滑块浮现（高 223px）。 |
| 文档同步 | `前端功能需求.md`（§6.10 / §6.11 的滚动条与列高口径 + 变更记录 Push 87），与代码**同一次推送**；本条目。`系统功能书.md` 不涉及定档口径、不改。 |
| 风险与后续 | 1）横向滑块只在滚动 / 悬停时可见（与列内纵向滑块一致）；若业务希望「一眼看出还能往右滑」，改成常显即可（`thumbAlwaysVisible`）；2）「项目总览」表格底部的横向滚动条仍是老样式（常显轨道 + sticky 底栏，Push 45 口径），本次未动 —— 要统一成隐式滑块另开一条；3）列高按 `100vh-12.75rem` 算，视口很矮时仍由 `min-h-[22rem]` 兜底、很高时由 `max-h-[52rem]` 封顶；4）原型仍存浏览器内存，正式口径按后端任务列表 / 创建接口（A10 / A15 待 wmj 决议）。 |
| 备注 | 推送 `px`（未直接推送 `main`）。Push 编号顺延为 **Push 87**（Push 86 = 上一批「添加」固定列底 + 列内隐式滚动条，已入 main）。 |

## 2026-09-20 15:22 +08:00 | px | px | Push 86

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（两块钱看板：「添加」固定在列底 + 滚动条改隐式 + 添加拆成「临时任务 / 阶段任务」两种口径） |
| 分支 | `px` |
| PR / commit | [PR #63](https://github.com/256-code/LibiaoLink/pull/63)（`px` → `main`，squash 合并，CI 四项全绿：frontend / server / database / shared）；合并时间 2026-09-20 15:05 +08:00，合并提交 `f8efd17`；未直接推送 `main`。 |
| 做了什么 | 业务反馈（一）：**「添加按钮不是在最下方的 —— 添加按钮是固定的，不随滚消失出现」**、（二）：**「滑动栏做成隐式的」**、（三）：**「添加有两种任务：临时任务 和 阶段任务 —— 临时任务自己添加内容、阶段任务要从模板里面选择添加」**。① **「添加」固定在列底**：`frontend/src/components/TaskKanban.tsx` 新增 `COLUMN_FOOTER`，把按钮从滚动视口里挪到列壳内、滚动区之外（列结构 = 列头 / `ScrollArea` 卡片区 / 固定列底按钮）；② **滚动条改隐式**：列内滚动改用仓库既有 `ScrollArea` 组件（原生滚动条隐藏，滚动 / 悬停才浮出自绘滑块 —— 与分类筛选侧栏、任务抽屉同一套），同时删掉上一版（Push 85）自建的 `.kanban-scroll` 细滚动条与 `frontend/src/app.css` 里对应的 24 行 CSS；③ **两种添加**：点「添加」→ 列底浮层菜单（玻璃小面板，贴按钮向上展开、落在列宽内）——**临时任务** = 自己填内容（任务名称必填 + 英文名可空，`创建` 后挂到该列；标题不再写死「新任务」）/ **阶段任务** = 先选阶段（9 个施工阶段，顺序同「项目总览」）再打开该阶段的**「任务节点 + 模板」卡片**（复用 `StageAddCard`：节点池 + 各块模板 + 整套添加，已加过的节点按 id 判重显示「已添加」并置灰），卡片贴该列右侧、与列顶齐平（`Esc` / 点卡片外空白处关闭，可连点加多条）；④ 两条路径建出来的任务都带上**所在列的上下文**（负责人看板给负责人、进展看板给状态 + `progressAfterStatus` 联动进度），与任务表 / 抽屉 / 弹窗既有的字段覆盖表打通；⑤ 列宽 280px、卡片材质 / 字段（含「所属阶段」）、点开任务详情抽屉等一律未动。 |
| 新增功能 | 看板列内「临时任务」（自填标题）与「阶段任务」（从模板 / 节点池选）两种添加方式；列底「添加」固定可见；列内滚动条改隐式。 |
| 修复 / 重构 / 优化 | 删除 Push 85 自建的 `.kanban-scroll` 滚动条样式（改由 `ScrollArea` 统一）；`ProjectDetail.tsx` 的 `quickTask(group, title, titleEn)` 支持自填标题，新增 `handleKanbanAddNode(context, stage, node)`（按节点 id 判重 + 带列上下文）。 |
| 测试与验证 | 1）`frontend/`：`npm run typecheck` 通过；`npm run build` 通过（`dist/assets/index-CXS8YIlR.css` 66.83 kB、`dist/assets/index-BTlfh-ph.js` 376.59 kB）；2）无头 Chrome + CDP 实机走查（项目 `inmu-0010`，1500×1000）：列底「添加」按钮渲染 10 个（每列一个）且**固定在列底**（卡片滚动时按钮不动）；点「添加」→ 菜单文案 = 「添加任务 / 临时任务 自己填内容 / 阶段任务 从模板里选」；「临时任务」填「现场临时调试 / On-site debugging」→ 创建成功、该列计数 23 → **24项** 且新卡片出现；「阶段任务」→ 阶段列表（售前规划 … 验收 9 项）→ 选「硬件实施」→ 模板卡片 `aria-label = 硬件实施：任务节点与模板`、落点 `left 316 / top 148`（贴列右侧、与列顶齐平、完整落在视口内）→ 点一条节点 → 该列计数 24 → **25项**，已加过的节点显示「已添加」；3）滚动条：列内无原生滚动条（`ScrollArea` 隐藏），滚动 / 悬停浮出自绘滑块。 |
| 文档同步 | `前端功能需求.md`（§6.10 列底「添加」两种口径 + 隐式滚动条口径、§6.11 同口径、变更记录 Push 86），与代码**同一次推送**；本条目。`系统功能书.md` 不涉及定档口径（分组视图与字段口径未变）、不改。 |
| 风险与后续 | 1）**「阶段任务」目前不带「未分组」选项**：阶段列表 = 9 个施工阶段（要「无阶段」的用「临时任务」，两条路互补）；若业务要给阶段任务也留「未分组」，加一项即可；2）阶段里已加满的节点会全部显示「已添加」（与总览页口径一致，可换模板标签或「整套添加」看剩余项）；3）看板列内浮层用「贴列底向上展开」，列很窄时是单列菜单（宽度 = 列宽 280px）；若要更宽的浮层另开一张卡；4）原型仍存浏览器内存，正式口径按后端任务列表 / 创建接口（A10 / A15 待 wmj 决议：看板建任务的阶段口径）。 |
| 备注 | 推送 `px`（未直接推送 `main`）。Push 编号顺延为 **Push 86**（Push 85 = 本批的前一版列内滚动，已入 main）。 |

## 2026-09-20 14:56 +08:00 | px | px | Push 85

| 字段 | 内容 |
|---|---|
| 类型 | `fix(frontend)`（两块钱看板改**列内滚动** —— 业务反馈「项目卡片不能一直延伸下去」） |
| 分支 | `px` |
| PR / commit | [PR #62](https://github.com/256-code/LibiaoLink/pull/62)（`px` → `main`，squash 合并）—— **已于 2026-09-20 15:02 +08:00 合入 `main`**，合并提交 `3a78459`，CI 四项检查（frontend / server / database / shared）全绿；功能提交 `a2c9ca1`（文档随同一次提交），推送前先 `git merge origin/main`（`dcab4f5`，解 `开发日志.md` 冲突）；未直接推送 `main`。 |
| 做了什么 | 业务反馈：看板列里的卡片一多，整列就一直往下延伸（第二列第三列被拉得很长、页面要一直往下滚）。改成**列内滚动**（结构与材质一律未动，只改滚动容器）：① `frontend/src/components/TaskKanban.tsx` 新增两个常量 —— `COLUMN_SHELL`（列：`h-[calc(100vh-15.5rem)]` + 上限 `max-h-[52rem]` + 下限 `min-h-[22rem]` + 原 `w-[280px]`）与 `COLUMN_BODY`（列内滚动区：`min-h-0 flex-1 overflow-y-auto` + `kanban-scroll`）；`KanbanColumn` 的 `<section>` 用 `COLUMN_SHELL`、卡片列表容器由 `flex flex-col gap-3` 改 `COLUMN_BODY` —— **列头（头像 + 姓名 + 「N项」/ 状态色签 + 「N项」）留在滚动区之外固定不动**，卡片与列底「+ 添加」在列内滚；② `frontend/src/app.css` 新增 `.kanban-scroll` 细滚动条（WebKit：宽 8px、透明轨道、圆角灰拇指 `rgba(113,113,122,0.4)` + 悬停加深到 0.62；Firefox：`scrollbar-width: thin` + `scrollbar-color`），与仓库既有滚动条口径（`#task-board-scroll` / `.scrollbar-hidden`）并列、互不影响；③ 列宽、卡片材质（白壳 + 近白内板 + 细纹 + 投影）、卡片字段（含「所属阶段」）、点开抽屉 / 「+ 添加」/ 看板横向滚动等交互**一律未改**。 |
| 新增功能 | 无（交互 / 布局调整）—— 看板列改为「列头固定 + 列内纵向滚动」。 |
| 修复 / 重构 / 优化 | 看板列不再随卡片数量无限变长：页面高度稳定（实测 1500×1000 视口下页面 `scrollHeight = clientHeight = 1000`，无纵向滚动），列内 `scrollHeight 8847 / clientHeight 716`（人员看板）/ `1922 / 720`（进展看板）说明滚动落在列内。 |
| 测试与验证 | 1）`frontend/`：`npm run typecheck`（`tsc -p tsconfig.json` + `tsc -p tsconfig.node.json`）通过；`npm run build` 通过（`dist/assets/index-pjbhNno_.css` 66.41 kB、`dist/assets/index-EpoZQ1mA.js` 372.17 kB）；2）视觉复核（本地 dev `localhost:3000` + 后端会话，无头 Chrome + CDP，项目 `inmu-0010`，1500×1000）：切「人员任务分配」/「任务进展」两视图，逐列核对列高（752px）、列头固定、细滚动条可见、卡片字段与材质未变；把首列滚到底 → **「+ 添加」出现在列表末尾**、列头仍在原位；两视图页面均无纵向滚动（不再被列拉长）。 |
| 文档同步 | `前端功能需求.md`（§6.10 新增「列内滚动」条 + §6.11 列内滚动口径、变更记录 Push 85），与代码**同一次推送**；本条目。`系统功能书.md` 本次不涉及定档口径（分组视图与字段口径未变）、不改。 |
| 风险与后续 | 1）列高用 `calc(100vh-15.5rem)` 估算（顶栏 + 标签栏 + 页边距），不同分辨率下可能略松 / 略紧，真机观感如需微调只改 `COLUMN_SHELL` 一处；2）列内滚动条是原生细滚动条（非自绘滑块），与任务表的自绘 `TableScrollbar` 不是一套 —— 若业务要求统一成自绘，另开一张卡；3）原型仍存浏览器内存，正式口径按后端任务列表接口。 |
| 备注 | 推送 `px`（未直接推送 `main`）。Push 编号：**PR #61（wmj，h3）先入 main 占 Push 83**，wmj 紧随其后的同步合并条目占 **Push 84**；本批按「先入 main 者为准」顺延为 **Push 85**。 |

## 2026-09-20 15:05 +08:00 | wmj | wmj | Push 84

| 字段 | 内容 |
|---|---|
| 类型 | `chore(merge)`（同步 main：px 的 [PR #60](https://github.com/256-code/LibiaoLink/pull/60)（frontend 侧 Push 82）合入后的 `f3a74eb`） |
| 分支 | `wmj` |
| PR / commit | 并入 [PR #61](https://github.com/256-code/LibiaoLink/pull/61)（`wmj` → `main`，h3 卡；本同步提交进入同一 PR）；未直接推送 `main`。 |
| 做了什么 | 把 `origin/main`（`f3a74eb`）合并回 `wmj`：唯一冲突在 `开发日志.md`（双方都在顶部插条目）—— 按时间序保留三条：wmj Push 83（14:50）→ px Push 82（14:46：项目详情三视图 + 人员任务分配 / 任务进展看板）→ wmj Push 82（14:16：上一次同步合并）；`frontend/`、`系统功能书.md`、`前端功能需求.md` 的改动全部来自 main 侧，无冲突直接并入。 |
| 新增功能 | 无（同步合并）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 纯文档冲突解决：`git status` 确认无未解决冲突、文件中无冲突标记残留；h3 的代码与契约未改动。合入的 `frontend/` 三文件原样来自 main（PR #60 的 CI 已绿）；本地未装 `frontend/` 依赖，**未在本机复跑 frontend typecheck / build**，由本 PR 的 CI 四 job（frontend / server / database / shared）复核。 |
| 文档同步 | 本条目 + `开发日志.md` 冲突解决（wmj 83 / px 82 / wmj 82 三条并存，按时间倒序）。 |
| 风险与后续 | 1）**Push 编号说明**：px 的 #60 与 wmj 在同一天各有一条「Push 82」（双方按「先入 main 者为准」各自记录，px 条目备注已说明），本轮不改动既有编号；2）同步后 `wmj` 与 `main` 的差异仅为历史分叉（squash 合入不留原始提交），h3 的落地以 PR #61 为准。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。 |

## 2026-09-20 14:50 +08:00 | wmj | wmj | Push 83

| 字段 | 内容 |
|---|---|
| 类型 | `feat(flow)`（h3 · S6·blueprint/node：M2-02 建项目蓝图快照 + M2-03 阶段推进 / 回退 + 蓝图版本化 + 节点增删 / 完成门禁） |
| 分支 | `wmj`（本轮开 PR `wmj` → `main`；未直接推送 `main`） |
| PR / commit | 随 [PR #61](https://github.com/256-code/LibiaoLink/pull/61) 提交（`a587946`）；未直接推送 `main`。 |
| 做了什么 | ① 迁移 `0011_blueprints.sql`（`blueprints` 按 `project_type` 唯一 + 草稿 `draft_payload` + `published_version` + 乐观锁；`blueprint_versions` 版本快照 + 校验 issues + `unique(blueprint_id, blueprint_version)`）与 `0012_project_stages_tracking.sql`（`project_stages` 增 `advanced_at` / `advanced_by` / `rolled_back_at` / `rolled_back_by` / `rollback_reason`）+ Drizzle schema 同步；② 种子 #7 `database/seeds/blueprint.mjs`（default 模板 9 阶段 19 节点 + 版本 1；payload 比较前做键序归一化，库内已修订时不覆盖）；③ 契约：`flow.ts` 增 `BlueprintView.projectType / isDefault / publishedVersion`、`BlueprintQuerySchema`、`StageProgress` / `ProjectStageSummary` / `StageListResponse` / `StageAdvanceBody` / `StageRollbackBody`、`NodeCreateBody.reason`、`NodeDeleteBody.reason`，`openapi.ts` 补 blueprint 五条路径与 stages 三条路径（含 403 / 404 / 422 响应）；错误码新增 5 个（422 `STAGE_GATE_NOT_PASSED` / `BLUEPRINT_NOT_PUBLISHED`，409 `NODE_HAS_FILES` / `STAGE_STATE_INVALID` / `NODE_ALREADY_EXISTS`）；生成物重跑（paths 49 / schemas 117，drift 0）；④ blueprint 模块四层落地（`blueprint.validation.ts` 纯函数校验 / repository / service / controller + index 出口）：`GET / PUT /api/v1/blueprint`、`POST /publish`、`GET /export`、`POST /import`（写接口仅管理员；发布幂等；round-trip 无损；读 / 写按 `projectType` 精确取，default 只在项目侧解析时兜底）；⑤ project 模块新增 `flow.repository / flow.service / flow.controller / nodes.controller`：`createProject` 改单事务导入快照（M2-02）、`GET /projects/{id}/flow`、`GET /projects/{id}/stages`、阶段推进 / 回退（M2-03：门禁失败先回滚事务、再补写 outbox 留痕、最后 422，不部分推进）、节点增删（ADR-020 仅项目经理；模板节点池校验；`node_key` 项目内唯一 —— 已有未删节点 409，软删后再增补 = 还原同一行）、完成门禁与预检（成员平权）；⑥ node 模块 `gate.repository / gate.service / node.module`（门禁判定与缺项明细；独立模块避免 project ↔ blueprint 循环依赖）；⑦ 平台底座 `db/db-client.ts`（`Database` / `DbTransaction` / `DbClient` 事务类型）+ `db/outbox.ts`（`appendOutbox` 同事务写 outbox）+ `db/schema/blueprint.ts`。 |
| 新增功能 | 蓝图版本化 / 校验 / 导入导出 / 发布（ADR-019 按项目类型 + default 兜底）；建项目自动导入流程快照（M2-02）；阶段推进与回退（M2-03，服务端门禁 + 缺项明细 + 留痕）；项目内节点增补 / 删除（模板节点池 + 留痕，ADR-020）；节点完成门禁与预检（v0.2 §3.6）。 |
| 修复 / 重构 / 优化 | 实机发现并修 4 个真 bug：① 发布后草稿 jsonb 键序被 PG 重排 → 状态恒 `draft`、重复发布版本虚增（发布时把草稿同步归一为发布 payload）；② `updateStageAdvanced` 未置 `status=done`（推进后本阶段仍 `active`，本地库出现两个 active 阶段）；③ `findNodeTemplate` 未按 ADR-019 回落 default（项目类型无独立蓝图时，增补节点把合法 key 误判为 `BLUEPRINT_REF_UNKNOWN`）；④ 增补节点对同 `node_key` 直接插入 → 撞 DB 唯一索引 `project_nodes_project_id_node_key_key` 报 500（改为 409 `NODE_ALREADY_EXISTS` + 软删行还原语义）。 |
| 测试与验证 | `shared`：`tsc` + `generate` + `check-drift`（漂移 0）+ `tsc -p tsconfig.build.json`（dist 供 server 解析）；`server`：`typecheck` 通过、`vitest` 9 文件 100 例全绿（新增 `test/blueprint-validation.test.ts` 7 例、`test/flow-gate.test.ts` 6 例；`test/project-crud.test.ts` 的 `makeService` 改注入 fake DB + fake Flow）、`nest build` 通过、`check:boundaries` 75 文件 / 251 依赖 / 0 违规。本地实机（真 PG :55432 + 真 api :3011 + 桩会话）：迁移 0011 / 0012 执行成功、`check:db-schema` 21 表 / 217 列 / 59 索引 / 51 CHECK、种子 #7 幂等；蓝图 GET 200（default 9 阶段）→ export → import → export **round-trip 无损**、非管理员 403、管理员保存 / 导入 / 发布 200、无变更再发布不递增版本、有变更 +1（v4）；建项目 201 → 快照 9 阶段 19 节点（合同节点 2 条必交成果）+ `stage_key=presale`；完成门禁：无约束节点 200、重复完成 409 `NODE_ALREADY_DONE`、stale version 409、`can-complete` false + missing 两项、合同节点缺件 **422 `NODE_REQUIRED_DOC_MISSING` + missing 明细**、灌入定档文件后 200；阶段：推进 422 `STAGE_GATE_NOT_PASSED`（node_not_done ×2）且门禁失败不部分推进 → 补齐后推进 200（presale=done / design=active / `stage_key=design`）→ 未开始阶段推进 422（node_not_done ×3）→ 回退 200（design=pending / presale=active / `stage_key=presale` / 原因与时间留痕）→ 首阶段回退 409 `STAGE_STATE_INVALID` → stale version 409；节点：未知 key 422、同 key 增补 409、软删后增补还原同一行（`origin=blueprint`、`status=pending`）、有文件节点删除 409 `NODE_HAS_FILES`、重复删除 409；归档项目推进 409 `PROJECT_ARCHIVED`；非项目经理（lisi）推进 / 增补 / 删除 403 `FORBIDDEN`，完成节点未被权限拒绝（平权语义）；h2 老项目 `blueprintVersion=0`（stages / nodes 为空，增补 404 无阶段）。 |
| 文档同步 | `server/src/modules/blueprint/README.md`（占位 → 实现说明 + 边界）、`server/src/modules/node/README.md`（占位 → 门禁实现 + 过渡口径）、`server/src/modules/project/README.md`（h3 章节 + 对外接口 + 边界更新）、`server/README.md`（流程接口段 / 数据层 0011-0012 / 测试计数 100 例 / 后续卡片）、`database/README.md`（0011 / 0012 登记 + 种子 #7 + 验证行 21 表 + 迁移输出示例）、`database/seeds/README.md`（种子 #7 行）、`shared/README.md`（蓝图组织 / 阶段推进 / 节点增删三条业务口径 + M2-02 / M2-03 切片行标已入 + ADR-019/020/023 索引行 + paths/schemas 计数）、`技术设计v0.3-实施与验收.md` §3.1 状态行。 |
| 风险与后续 | 1）本批含契约新增（5 个错误码 + 3 条路径 + 若干 403 / 404 / 422 响应补全）、迁移 0011 / 0012 与门禁语义，属高风险面（CONTRIBUTING §15），需非作者评审（lan / px）——团队评审档期不足，经用户确认「评审后置、直接合」，差异在 PR 描述里列全，评审意见到位后按新卡回改；2）**待复核差异**：① M2-02 / M2-03 实做范围含阶段回退（ADR-023 同族接口，h3 卡未单列）；② `GateService` 直读 `files` / `tasks` 表 = 过渡口径（file / task 模块未落地；i1 / h4 后改对端 `index.ts` 出口）；③ 增补节点 `seq` 缺省 = 同阶段 max + 10；④ 首阶段回退返回 409 `STAGE_STATE_INVALID`（非 400）；⑤ 蓝图写权限按角色码 `admin`（h6 矩阵落地后换功能权限 `admin.blueprint.manage`）；⑥ 增补节点的模板池限定「项目导入版本」，不随蓝图新版本（升级随 M6）；⑦ h2 之前建的项目 `blueprintVersion=0`（无 stages / nodes，本批不做回填脚本；本地均为测试数据）；⑧ 门禁拒绝留痕写 outbox（`audit_logs` 随 h7）；⑨ 推进 / 回退整体一次事务、失败不部分推进；⑩ `GET /blueprint?projectType=` 未建档返回 404（default 不返回给编辑器，只在项目侧解析兜底）；3）验收：h3 卡验收（蓝图 round-trip 无损 + 缺必交成果文件时完成被拒）达成，卡片可置 done。 |
| 备注 | 推送 `wmj`（`a587946`）；未直接推送 `main`；本轮 PR [#61](https://github.com/256-code/LibiaoLink/pull/61)（squash-only，评审后置）。下一批按任务卡顺序推进。 |

## 2026-09-20 14:46 +08:00 | px | px | Push 82

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（项目详情页顶部导航改版 + 两块钱查看板；同批含业务反馈两处修正：卡片材质改白、阶段一栏改口径为「所属阶段」） |
| 分支 | `px`（并入 [PR #60](https://github.com/256-code/LibiaoLink/pull/60)，未直接推送 `main`） |
| PR / commit | [PR #60](https://github.com/256-code/LibiaoLink/pull/60)（`px` → `main`，squash 合并）—— **已于 2026-09-20 14:49 +08:00 合入 `main`**，合并提交 `f3a74eb`，CI 四项检查（frontend / server / database / shared）全绿；功能提交 `53ca0ad`、卡片改白 `9ba9229`、阶段栏名改「所属阶段」`86dcd5b`（文档随各次提交同步）；推送前先 `git merge origin/main` 同步（`8fb330f`），合并后同步（`f3a74eb` → 本分支）。 |
| 做了什么 | ① **顶部视图导航改版**（业务：标签导航栏只保留「项目总览」，其余两块改成看板）：`frontend/src/ProjectDetail.tsx` 的 `VIEW_TABS` 由「项目总览 + 9 个施工阶段」改成 **项目总览 / 人员任务分配 / 任务进展**，新增 `activeView` 状态切换；列显隐（`ColumnPicker`）与底部横向滑块只在「项目总览」渲染；阶段标签不再各占一格（阶段仍可从总览的阶段分组头点开「任务节点 + 模板」卡片加任务，切视图时该卡片自动关闭）。② **人员任务分配看板**（新增 `frontend/src/components/TaskKanban.tsx`，`mode="owner"`）：列 = 任务负责人（头像 + 姓名 + 「N项」，按任务出现顺序、**「待分配」垫底**）。③ **任务进展看板**（`mode="status"`）：列 = 任务状态，固定顺序 **已延期 → 进行中 → 已完成 → 提前完成 → 待开始**（**空列也保留**）。④ 卡片字段（只出任务里真实存在的字段）：`owner` 看板 = 标题（中 / 英）/ 预计完成日期 / 任务状态色标签 / 项目进度条与百分比 / 是否按时交付；`status` 看板 = 标题 / 任务负责人（头像 + `姓名(拼音)`）/ 开始日期 / 项目进度 / 是否按时交付；两张卡片的末行都是 **「所属阶段」色签**（按阶段固定色，看板里直接建的无阶段任务显示「未分组」）。状态与逾期标注取任务表同一套派生（`taskStatus` / `lateDeliveryLabel`，口径同 Push 67）。⑤ **卡片材质按业务样张代码还原**（外层壳 + `repeating-conic-gradient` 细纹叠加 + 内层板 + 多层投影：外壳 `padding:9px` / `rounded-[35px]`、内层 `rounded-[30px]`），用 Tailwind 任意值实现、**不引入 styled-components**；**配色按业务反馈（同日）由样张原色 `#e8e8e8` / `#e2e0e0` 改成白色** —— 业务原话「色彩太灰了 还是要白色」，白壳 + 近白内板 + 发丝边 + 柔和投影 + 底部内阴影，结构保持样张不变。⑥ **卡片上的阶段一栏改成「所属阶段」**（业务 2026-09-20：「所属阶段还是要写的，但是不要写阶段性里程碑」）：阶段色签保留并按阶段固定色渲染（看板里直接建的任务没有阶段 → 显示「未分组」），但卡片标签由「阶段性里程碑」改为 **「所属阶段」** —— 任务字段里没有「里程碑」这一项（`系统功能书.md` A1-11：里程碑属二期），两个视图的 DOM 全文不再出现「阶段性里程碑」字样。⑦ 看板列底部「+ 添加」直接建空任务（标题占位「新任务」，负责人 / 状态按所在列给、进度按 `progressAfterStatus` 联动、紧急重要度「中」），**阶段留空** → `TaskBoard` 新增「未分组」组兜底（有内容才出现、可折叠 / 展开），保证看板建的任务在「项目总览」里也看得到。⑧ 卡片点开任务详情抽屉，抽屉里「编辑任务」进任务编辑弹窗，与表格共用同一张字段覆盖表。 |
| 新增功能 | 项目详情页两块看板（按人员 / 按状态）+ 三视图切换；看板列内直接建任务。 |
| 修复 / 重构 / 优化 | `frontend/src/components/TaskBoard.tsx` 导出 `STATUS_TAG_CLASS`（看板复用同一套状态色签，不另起一份色表）；新增「未分组」组兜底（原实现只认九个施工阶段，看板建的任务会掉出表格）。 |
| 测试与验证 | 1）`frontend/`：`npm run typecheck`（`tsc -p tsconfig.json` + `tsc -p tsconfig.node.json`）通过；`npm run build` 通过（`dist/assets/index-BwYuW11y.css` 65.90 kB、`dist/assets/index-qFZm5ClT.js` 372.06 kB）；2）视觉复核（本地 dev `localhost:3000` + 后端会话，无头 Chrome + CDP，项目 `inmu-0010`，1500×1000）：切「人员任务分配」/「任务进展」两个标签各渲染 44 张卡片，列头 / 卡片字段 / 状态色签 / 进度条 / 逾期标注（逾期未交付红、逾期已交付琥珀）逐项核对；卡片为白壳 + 近白内板；**两视图 `document.body.innerText` 检索「阶段性里程碑」均为 false**；卡片「所属阶段」色签按阶段固定色渲染；3）「+ 添加」建出来的任务在「项目总览」落在「未分组」组（同批已验证，本轮未改动该逻辑）；4）CI：PR #60 四项检查（frontend / server / database / shared）。 |
| 文档同步 | `前端功能需求.md`（§一 现状句、§6.1 顶部视图导航、§6.3 「未分组」兜底、§6.7 滑块只在总览、§6.10 / §6.11 两块钱看板 + 卡片材质白色口径 + **卡片阶段栏名为「所属阶段」**、§3.8 新增 A15、变更记录 Push 82），与代码**同一次推送**；`系统功能书.md` 文档头新增同一条修订（A1-04 分组视图口径，Push 82）；本条目。`字段对照清单.md` 无字段口径变化（「阶段性里程碑」从来不是任务字段）、无需更新。 |
| 风险与后续 | 1）**A15 待 wmj 决议**：看板「+ 添加」建的任务没有阶段上下文，原型把 `stage` 留空（落「未分组」），而契约 `TaskCreateBody.stageKey` 目前必填 —— 二选一（① 放宽为可空 / ② 看板加阶段小下拉），前端倾向 ②；2）原型未接后端、任务存浏览器内存，正式口径按 `GET /api/v1/projects/{id}/tasks`；3）本批含契约新增对齐项（A15）与前端数据模型口径，属高风险面（CONTRIBUTING §15），需非作者评审；4）后续每轮 squash 合并后仍需一次同步合并（Push 69 / 72 / 76 / 78 同例）。 |
| 备注 | 推送 `px`（未直接推送 `main`）。Push 编号按「先入 main 者为准」顺延为 **Push 82**（wmj 的 Push 69~81 先入 main，本条原记 Push 69，本地全部文档同步改 82；`开发日志.md` / `前端功能需求.md` 里 wmj 的 Push 69 条目（A9 定案）与本条互不影响、不动）。同一 PR 内两次推送（`53ca0ad` 功能、`9ba9229` 卡片改白，本次为阶段字段改名「所属阶段」）合并计一条 Push 82（Push 80-81 同例）。 |

## 2026-09-20 14:16 +08:00 | wmj | wmj | Push 82

| 字段 | 内容 |
|---|---|
| 类型 | `chore(merge)`（同步 main：h2 · S6·project 经 [PR #59](https://github.com/256-code/LibiaoLink/pull/59) squash 合入后的 `6606563`） |
| 分支 | `wmj` |
| PR / commit | 直推常驻分支 `wmj`（同步合并、无待合内容，不另开 PR）；合并提交本身随下一张卡的 PR 进入 `main`。 |
| 做了什么 | 把 `origin/main`（`6606563`，PR #59 squash 落地，含 Push 80 / 81 全部产出）合并回 `wmj`：无冲突（squash 后 main 与本线内容一致，仅历史分叉；合并后分支与 main 对齐，h3 从 clean 出发）。 |
| 新增功能 | 无（同步合并）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | PR #59 CI 四 job 全绿（frontend / database / server / shared，`057c712`）后合并；合并前核对 main 已含 Push 80 / 81 全部产出（`0009` / `0010` 迁移、project 与成员接口、契约与生成物、文档同步）；合并过程干净、工作区无残留；纯文档合并（免 typecheck / build）。 |
| 文档同步 | 本条目。 |
| 风险与后续 | 1）**PR #59 已 squash 合入**（`6606563`）：h2 · S6·project 验收（项目 CRUD 与成员用例通过 + 分类字段口径与需求一致）达成，卡片可置 done；2）合并裁定：团队评审档期不足，经用户确认「直接合、差异后置」——本 PR 高风险差异（写保护先于 version 校验、M2-02 / M2-03 归 h3、名册过滤随 h6、`region` / `projectType` 契约缺省、`role_in_project` 与 `manager_id` 不联动）已在 PR 描述「风险与后续」列明，评审意见到位后按新卡回改；3）下一批：h3 · S6·blueprint/node（M2-02 建项目蓝图快照、M2-03 阶段推进）。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。 |

## 2026-09-20 14:15 +08:00 | wmj | wmj | Push 81

| 字段 | 内容 |
|---|---|
| 类型 | `feat(project)`（h2 · S6·project 收口：M2-05 项目成员名册 + 分类字段口径） |
| 分支 | `wmj`（并入 [PR #59](https://github.com/256-code/LibiaoLink/pull/59)，与 Push 80 同批评审） |
| PR / commit | 随 [PR #59](https://github.com/256-code/LibiaoLink/pull/59) 提交（`edfb7c1`）；未直接推送 `main`。 |
| 做了什么 | ① 迁移 `0010_project_members.sql`：`project_members`（`project_id` / `user_id` / `role_in_project` / `joined_at`；`uq_project_members_project_user` 联合唯一 + `ix_project_members_user` 反查索引；角色两值 CHECK）→ Drizzle `projectMembers` 同步；② 契约与生成物：`ProjectMember` / `ProjectMemberRole` / `ProjectMemberListResponse` / `ProjectMemberCreateBody` + 三条 OpenAPI 路径（openapi.json 198352 bytes、api-types 154236 bytes，drift 0）；③ 名册实现（`project-member.repository.ts` / `project-member.service.ts` + controller 三条路由）：列表（项目经理在前、同角色按工号升序、姓名随行）、添加（**幂等 upsert**：同项目 + 同用户唯一，重复添加 = 覆盖角色且保留 `joinedAt`；目标用户不存在 404，复用 `UserService.getUser`）、移除（返回被移除行；不是成员 / 项目不可见统一 404）；④ ADR-022 ②：名册增删改后 `ProjectRepository.touch` 前移项目 `updatedAt`；⑤ 归档写保护扩展到名册（POST / DELETE → 409 `PROJECT_ARCHIVED`，读仍可用）；⑥ 分类字段口径（A1-12 定档）：`region` / `projectType` 创建缺省「未分类」（契约 `default`），空串仍 400、前端表单仍必填，facets 计数口径一致。 |
| 新增功能 | 项目成员名册维护（M2-05 前半）；「未分类」缺省分类。 |
| 修复 / 重构 / 优化 | `ProjectMemberService` 入参按契约输入类型收（`z.input`），使「缺省角色」在服务层与 HTTP 层同口径；`ProjectModule` 注册名册 provider 并导出 `ProjectMemberService`。 |
| 测试与验证 | `shared`：`tsc` 通过 + `generate` + `check-drift`（漂移 0）+ `tsc -p tsconfig.build.json`（dist 更新，供 server 解析）；`server`：`typecheck` 通过、`vitest` 7 文件 87 例全绿（新增 `test/project-members.test.ts` 7 例）、`nest build` 通过、`check:boundaries` 58 文件 / 181 依赖 / 0 违规。本地 PG 实机：`migrate` 0010 通过 + `check:db-schema`（19 表 / 195 列 / 54 索引 / 48 CHECK）；api :3011 闭环 —— 建项目 201、名册空 200、加成员 200（缺省 `project_member`）、**加成员后项目 `updatedAt` 前移**（ADR-022 触点）、重复添加 = 改角色且 `joinedAt` 不变、用户不存在 404、非法 uuid 400、移除 200 / 再移除 404、归档后 POST·DELETE 名册 409 `PROJECT_ARCHIVED` 且名册读 200、软删项目名册 404、不传分类字段 → `region`/`projectType` = 「未分类」、空串 400、无会话 401、缺 CSRF 403。 |
| 文档同步 | `server/src/modules/project/README.md`（h2 第二批章节 + 分类字段口径 + 边界）；`server/README.md`（成员接口 / 0010 数据层 / 测试计数）；`database/README.md`（0010 登记 + 验证行 + 迁移输出示例）；`shared/README.md`（分类字段（A1-12）与项目成员（M2-05）口径行 + M2-05 切片行标已入）；`技术设计v0.3` §3.1 状态行。 |
| 风险与后续 | 1）名册的**记录级过滤**（列表 / 详情 / facets / 搜索按成员裁剪、非成员 404）随 h6 策略服务，本批只落数据面与维护接口 —— 在此之前「登录即可读全部项目」是已知口径；2）`role_in_project` 由成员接口维护，`projects.manager_id` 与名册**不自动联动**（改动经理不会改角色行），联动随 M2-02 建项目事务（h3）/ 管理界面（u 系列），是待复核差异；3）POST 成员为幂等 upsert（重复添加覆盖角色），与 i5 的 `Idempotency-Key` 语义相互独立（后者仍未接）；4）本批含契约新增（三条路径 + 四个 schema）与迁移，属高风险面（CONTRIBUTING §15），需非作者评审。 |
| 备注 | 推送 `wmj`（`edfb7c1`）；未直接推送 `main`；本批与 Push 80 同属 [PR #59](https://github.com/256-code/LibiaoLink/pull/59)（h2 卡整体一次评审 / 一次合并）。h2 卡验收已达成，PR 合并后可推进 h3（S6·blueprint/node）。 |

## 2026-09-20 13:45 +08:00 | wmj | wmj | Push 80

| 字段 | 内容 |
|---|---|
| 类型 | `feat(project)`（h2 · S6·project 第一批：M2-01 项目 CRUD + M2-04 首页列表 / facets） |
| 分支 | `wmj`（PR #59 待评审；未直接推送 `main`） |
| PR / commit | 随 [PR #59](https://github.com/256-code/LibiaoLink/pull/59) 提交（`9bc4373`）；未直接推送 `main`。 |
| 做了什么 | ① 迁移 `0009_projects_soft_delete.sql`：`projects.deleted_at timestamptz` / `deleted_by uuid`（软删落点 + 操作人）+ 活跃行局部索引 `ix_projects_active_updated (updated_at desc) where deleted_at is null`，Drizzle `db/schema/projects.ts` 同步；② 错误码 `PROJECT_ARCHIVED`（409）入契约 `shared/src/common/errors.ts` 与映射表（生成物 `generate.mjs` 重跑、`check-drift` 通过）；③ project 模块四层落地（`project.query.ts` / `project.repository.ts` / `project.service.ts` / `project.controller.ts` + `project.module.ts` + `index.ts` 唯一出口）：`GET /api/v1/projects`（多维 `filter[...]` 多值 + `q` + `filter[timeFrom|timeTo]` 上海日界 + 排序白名单 + 分页）、`GET /api/v1/projects/facets`（与列表同一 filter 构造器，五组计数 + total）、`POST`（201；`seq_no` 序列分配、编号唯一 409）、`GET /{id}`、`PATCH`（乐观锁 `version`）、`DELETE`（`If-Match` 防误删 + 软删），整组挂 `SessionGuard + CsrfGuard`；④ 归档写保护（ADR-027）：`status=archived` 的 PATCH / DELETE 一律 409 `PROJECT_ARCHIVED`；⑤ identity 侧新增 `@CurrentActorId()`（会话 → 本库 `users.id`），项目软删 `deleted_by` 用它（`@CurrentUser()` 仍是 Casdoor 口径，两者不再混用）；⑥ 修 bug：drizzle 0.45 把驱动错误包成 `DrizzleQueryError`（原始错误在 `cause`），唯一约束判定改为逐层解包后按 `code=23505 + constraint` 匹配 —— 实机首轮撞号曾返回 500，修复后为 409。 |
| 新增功能 | 首页「列表 + 分类计数」真实接口（M2-04）+ 项目 CRUD（M2-01）；前端首页可在 M2-07 联调时下线 mock。 |
| 修复 / 重构 / 优化 | 唯一约束违例解包（`isUniqueViolation` 逐层解包 + 单测）；`ProjectModule` 补 `imports: [IdentityModule]`（守卫依赖需在模块内可解析——e2e 测试暴露）；`project/README.md` 由占位改为实现说明。 |
| 测试与验证 | `typecheck` / `vitest`（6 文件 80 例全绿，新增 `test/project-crud.test.ts` 21 例）/ `nest build` / `check:boundaries`（56 文件 168 依赖 0 违规）；shared `typecheck` + `generate` + `check`（漂移 0）。本地 PG 17.6 实机闭环（api :3011 + 真库）：迁移 0009 + `check:db-schema`（18 表 / 190 列 / 51 索引 / 47 CHECK）；创建 201（`seqNo=1`、`managerName` 随行下发）、同编号再建 409 `PROJECT_CODE_EXISTS`、列表默认 `updatedAt:desc` 与 `sort=seqNo:asc`、筛选 `region+status` 命中 1、非法 `filter[status]` / 非法 `sort` / 区间反向 / 非法 uuid 均 400、PATCH v0 → v1、旧 version 409 `VERSION_CONFLICT`、归档后 PATCH 409 `PROJECT_ARCHIVED`、DELETE 缺 `If-Match` 400 / 归档项目 409、正确 version 200 → 详情 404 + 列表 total 1 + facets total 1（软删一致性）、软删后复用同编号仍 409、无会话 401、缺 CSRF 403。 |
| 文档同步 | `server/src/modules/project/README.md`（实现说明 + 边界）；`server/README.md`（项目接口段 / 数据层 / 测试计数）；`server/src/modules/identity/README.md`（`CurrentActorId`）；`database/README.md`（0009 登记 + 验证行）；`shared/README.md`（M2-01 / M2-04 切片行标已入 + `archivedAt` 待随归档卡）；`技术设计v0.3` §3.1 状态行。 |
| 风险与后续 | 1）`Project.archivedAt` 与 archive 端点（C4-02 / M6）未做：本批归档写保护只认 `status=archived`，故写保护先于 version 校验（归档项目 DELETE 返回 `PROJECT_ARCHIVED` 而非 `VERSION_CONFLICT`）—— 口径待复核；2）读接口当前登录即可读、写接口登录即可写：非成员 404 与「仅项目经理 / 管理员可写」随 M2-05 · h6 策略服务（ADR-011）；3）`blueprintVersion` 入参暂忽略（快照事务随 M2-02 · h3）；4）`deleted_by` 目前只落操作人、审计留痕随 h7；5）写接口 `Idempotency-Key` 未接（随 i5）；6）本批含契约错误码与迁移，属高风险面（CONTRIBUTING §15），需非作者评审。 |
| 备注 | 推送 `wmj`（`9bc4373`）；未直接推送 `main`。下一批候选：M2-02 建项目蓝图快照（依赖 h3 蓝图）或 h2 其余卡片（M2-03 / M2-05 / M2-06）。 |

## 2026-09-20 12:55 +08:00 | wmj | wmj | Push 79

| 字段 | 内容 |
|---|---|
| 类型 | `chore(merge)`（同步 main：PR #58 squash 合入后的 `1f537e9`） |
| 分支 | `wmj` |
| PR / commit | 直推常驻分支 `wmj`（同步合并、无待合内容，不另开 PR）；合并提交本身随下一张卡的 PR 进入 `main`。 |
| 做了什么 | 把 `origin/main`（`1f537e9`，PR #58 squash 落地，含 Push 78 全部产出）合并回 `wmj`：无冲突（squash 后 main 与本线内容一致，仅历史分叉；合并后分支与 main 对齐，h2 从 clean 出发）。 |
| 新增功能 | 无（同步合并）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | PR #58 CI 四 job 全绿（shared / server / database / frontend）后合并；合并前核对 main 已含 Push 78 全部内容（`0008_identity_removed.sql` / 离职回收 API / 目录适配器 / 用户目录 / 文档同步）；合并过程干净、工作区无残留；纯文档合并（免 typecheck / build）。 |
| 文档同步 | 本条目。 |
| 风险与后续 | 1）**PR #58 已 squash 合入**（`1f537e9`）；h1 · S6·identity/org 验收项（getUser / listDepartments 可用 + 角色数据范围用例）全部达成，卡片可置 done；2）高风险面（鉴权 / 会话 / 迁移，CONTRIBUTING §15）本次为作者推送 + 非作者批准合并（用户确认）记录；3）下一批：h2 · S6·project（M2-01 项目 CRUD + 数据面 `0009`）。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。 |

## 2026-09-20 12:40 +08:00 | wmj | wmj | Push 78

| 字段 | 内容 |
|---|---|
| 类型 | `feat(identity)`（h1 · S6·identity/org 收口：离职回收 API + 目录适配器 + 用户目录出口） |
| 分支 | `wmj`（PR 待开；未直接推送 `main`） |
| PR / commit | 随 [PR #58](https://github.com/256-code/LibiaoLink/pull/58) 提交（`a1b1df5`）；未直接推送 `main`。 |
| 做了什么 | ① 迁移 `0008_identity_removed.sql`：`users.removed_at`（delete 软删标记）+ Drizzle `identity.ts` 同步；② 离职回收 API：`POST /internal/users/{disable,enable,delete}`（`internal.controller.ts` / `internal-user.service.ts` / `internal-token.guard.ts`）—— `X-Internal-Token` 常量时间比较（`INTERNAL_SYNC_TOKEN`，生产启动强制非空）、入参 `{name,email}`（name 为准 / email 兜底 / 全缺 400）、幂等 200（目录中不存在也成功）、disable / delete 撤销全部在线会话并返回 `affectedSessions`、delete 软删（removed_at + status=disabled）；③ 目录适配器（`casdoor-directory.source.ts`）：Casdoor 管理 API（`/api/get-users` 分页去重 + `/api/get-groups`）→ `OrgDirectorySnapshot`，`isForbidden` / `isDeleted` → disabled、脏行跳过、非 ok 响应抛 INTERNAL；`POST /internal/org-sync/run` 手动触发一次拉取 + 差异应用（默认 report）；④ 用户目录 `GET /api/v1/users`（`users.controller.ts` + `UserService.listUsers` + repository `listDirectory`）：A2 契约口径（只启用用户 / q 命中工号·姓名·邮箱 / 默认工号升序分页）；⑤ env 新增 `CASDOOR_ORG_NAME` / `INTERNAL_SYNC_TOKEN`（生产必填）。 |
| 新增功能 | h1 卡「通讯录同步 + 离职回收闭环 + 用户目录」全部出口；首个 `/api/v1` 业务路由。 |
| 修复 / 重构 / 优化 | 内部动作按接入标准返回 200（Nest 默认 201 → `@HttpCode(200)`）；3 处测试替身补 `removedAt` 字段。 |
| 测试与验证 | `typecheck` / `vitest`（5 文件 59 例全绿，新增 `identity-internal.test.ts` 13 例）/ `nest build` / `check:boundaries`（50 文件 147 依赖 0 违规）；本地 PG 17.6 演练：`migrate` 0008 通过 + 复跑幂等无漂移、`check:db-schema` 通过（18 表 / 188 列 / 50 索引 / 47 CHECK）；实机演练（api + 桩 Casdoor）：无凭证 401 / 缺参 400 / disable 200（`affectedSessions`）/ 重复 disable 幂等 / email 兜底 enable / delete 软删 / 删除不存在 200 / org-sync run：users created 3 + departments created 2、复跑零变更、库内 wangwu（isForbidden）disabled、`removed_at` 按动作置位。 |
| 文档同步 | `server/src/modules/identity/README.md`（h1 收口小节 + 边界）；`server/README.md`（用户目录与内部作业段 + env）；`server/.env.example`（两个新变量）；`database/README.md`（0008 迁移登记 + 验证行）；`技术设计v0.3` §3.2（状态行 + 接口表 + D1-02 行）。 |
| 风险与后续 | 1）Casdoor 管理 API 字段映射（group.parentId / isTopGroup、user.isForbidden）按公开模型实现，正式环境核对随 g7；2）定时调度（worker 周期拉取）仍随 i5 接 worker（适配器 / 引擎已就绪）；3）审计留痕随 h7；4）本批属高风险面（鉴权 / 会话 / 迁移），需非作者评审；5）重试逻辑未做（自动化侧重试 + 幂等已保证）。 |
| 备注 | 推送 `wmj`；PR 待开；h1 卡片验收项（getUser / listDepartments + 角色数据范围用例）已全部达成。 |

## 2026-09-20 11:58 +08:00 | wmj | wmj | Push 77

| 字段 | 内容 |
|---|---|
| 类型 | `chore(merge)`（同步 main：PR #57 squash 合入后的 `46412da`） |
| 分支 | `wmj` |
| PR / commit | 直推常驻分支 `wmj`（同步合并、无待合内容，不另开 PR）；合并提交本身随下一张卡的 PR 进入 `main`。 |
| 做了什么 | 把 `origin/main`（`46412da`，PR #57 squash 落地）合并回 `wmj`：无冲突（squash 后 main 与本线内容一致，仅历史分叉；合并后分支与 main 对齐，下一张卡的 PR 从 clean 出发）。 |
| 新增功能 | 无（同步合并）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 合并前核对 main 已含本批全部内容（§7.3 #16、seeds README 待确认注、Push 75 / 76 条目）；合并过程干净、工作区无残留；纯文档合并（免 typecheck / build）。 |
| 文档同步 | 本条目。 |
| 风险与后续 | 1）PR #57 于 11:57 squash 合入（`46412da`）；2）`h1` 剩余项（目录适配器 / 权限矩阵 h6 / 人员 ↔ 部门映射 D1-06 / 管理端点 u3·u12）与 M1 其余卡片（h7 字典与审计、i5 outbox）为下一批候选。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。 |

## 2026-09-20 11:52 +08:00 | wmj | wmj | Push 76

| 字段 | 内容 |
|---|---|
| 类型 | `chore(merge)`（同步 main：PR #56 squash 合入后的 `f1f994d`） |
| 分支 | `wmj` |
| PR / commit | 随 [PR #57](https://github.com/256-code/LibiaoLink/pull/57) 提交；未直接推送 `main`。 |
| 做了什么 | 把 `origin/main`（`f1f994d`，PR #56 squash 落地，含 Push 71~74 全部产出）合并回 `wmj`：解三处冲突（`开发日志.md`、`技术设计v0.3-实施与验收.md` §7.3、`database/seeds/README.md`），均取本线版本 —— main 侧是 Push 71~74 的 squash 前身、无独有内容，本线为超集；合并后 PR #57 由 `dirty` 收敛为可合并。 |
| 新增功能 | 无（同步合并，合并后工作区与合并前一致）。 |
| 修复 / 重构 / 优化 | 修复 PR #57 `mergeable_state=dirty`：squash 合并造成的历史分叉由本次合并归位。 |
| 测试与验证 | 合并时逐项核对三处冲突均为「main 无 Push 75 内容」；合并后复查：Push 75 条目在位、v0.3 §7.3 为 4 项且含 #16、seeds README 待确认注在位；纯文档合并（无代码差异，免 typecheck / build）。 |
| 文档同步 | 本条目；其余文件随合并与 main 对齐。 |
| 风险与后续 | 1）**PR #56 已于 11:45 由 suikiiovo squash 合入**（head = Push 74 `ce15668`）：Push 74 的迁移 / 种子属高风险变更（CONTRIBUTING §15），合并记录未见 lan / px 非作者评审；若评审后有意见需另开 PR 修正（本次合并未改动 0007 内容）；2）后续每轮 squash 合并后仍需一次同步合并（Push 69 / 72 / 76 同例）。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。 |

## 2026-09-20 11:45 +08:00 | wmj | wmj | Push 75

| 字段 | 内容 |
|---|---|
| 类型 | `docs(design)`（口径登记：数据范围枚举 ↔ C3-02 措辞差 → §7.3 待确认） |
| 分支 | `wmj` |
| PR / commit | 随 [PR #57](https://github.com/256-code/LibiaoLink/pull/57)（`wmj` → `main`，head `0d3d14b`）提交；未直接推送 `main`。 |
| 做了什么 | 把 Push 74 复核出的口径缝隙落成待确认项：① 《技术设计v0.3》§7.3 业务确认清单新增 #16（数据范围枚举口径 C3-02 ↔ v0.2 §4.1：五值先行口径 / 差异说明 / 影响面），清单由 3 项扩为 4 项；② `database/seeds/README.md` 种子清单后补 #6a / #6b 待确认说明，指向 §7.3 #16。 |
| 新增功能 | 无代码。 |
| 修复 / 重构 / 优化 | 无；纯口径登记（未改 `系统功能书.md`，收敛动作随业务 / h6 决议）。 |
| 测试与验证 | 纯文档改动（按仓库约定免 typecheck / build）；逐行核对 v0.2 §4.1 表与 C3-02 原文，确认差异描述与落点（0007 CHECK、种子 #6a）一致。 |
| 文档同步 | 《技术设计v0.3-实施与验收.md》§7.3、`database/seeds/README.md`、本条目。 |
| 风险与后续 | 1）若业务判定改功能书措辞，走 `系统功能书.md` 变更（提出人 + wmj / px 复核）；若判定补「本部门」等枚举，需新迁移 0008 放宽 `roles.data_scope` CHECK + Drizzle 同步；2）h6 权限矩阵定稿前，实现以 §4.1 五值为准。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。 |

## 2026-09-20 11:40 +08:00 | wmj | wmj | Push 74

| 字段 | 内容 |
|---|---|
| 类型 | `feat(db)`（0007 identity/org 迁移 + 角色种子）+ `feat(identity)`（h1 · S6·identity/org：组织同步与角色出口） |
| 分支 | `wmj` |
| PR / commit | 随 [PR #56](https://github.com/256-code/LibiaoLink/pull/56) 提交；未直接推送 `main`。 |
| 做了什么 | ① 迁移 `0007_identity_org.sql`：`departments`（部门树；`source_id` 唯一 + 非自环 CHECK）、`roles`（`code` 唯一 + `data_scope` 枚举 CHECK）、`role_permissions`（`模块.操作` 键格式 CHECK）、`user_roles`（联合主键 + 反查索引），并同步 `server/src/db/schema/identity.ts`；② 种子器 `database/scripts/seed.mjs`（`--dry-run` 全回滚 / `--only=<name>` / advisory lock 20260919）+ 种子 #6a `database/seeds/roles.mjs`（六角色：admin / project_manager / task_owner / project_member / sales / viewer）；③ identity 出口：`listDepartments`（默认 active、名称升序）、`RoleService.getActorAuthorization`（角色 + 数据范围并集 + 权限位并集）与 `assignRole` / `revokeRole`、`OrgSyncService.applySnapshot`（父先于子 upsert、缺失部门停用、离职禁用 + 撤销全部会话、差异报告、缺失安全阀）；④ 单测 `server/test/identity-org.test.ts`（23 例）。 |
| 新增功能 | h1 验收两项均可用：`getUser`（g6 已有）+ `listDepartments`（本批新增）；角色数据范围用例通过（六角色 + 多角色并集 + 空画像）。组织同步以快照为输入产出差异报告，支持 `missingUserPolicy: "disable"` 的离职回收闭环（阈值 max(3, 在职 × 20%) 安全阀，超阈值只报告不执行）。 |
| 修复 / 重构 / 优化 | 无既有缺陷修复；`test/auth.e2e.test.ts` 补 5 个 provider 空替身，使 h1 新 provider 与 `/auth/*` 用例隔离（测试仍不依赖数据库）。 |
| 测试与验证 | 本地全绿（一次性 PG 17.6 演练库，临时目录，非仓库文件）：① `roles/0001` → 空库迁移 7 个 → 幂等复跑（无漂移）→ `check:db-schema` 通过（18 表 / 187 列 / 50 索引 / 47 CHECK 与 Drizzle 一致）；② 种子 `--dry-run`（回滚）→ 首跑 inserted 6 → 复跑 unchanged 6（幂等）；③ server 门禁：`typecheck` / `test`（39 例，含 identity/org 23 例）/ `build` / `check:boundaries`（45 文件 / 123 依赖 / 0 违规）。CI 的 database job 步骤与本地演练一一对应，预期同绿。 |
| 文档同步 | `server/src/modules/identity/README.md`（h1 章节）、`server/README.md`（数据层 / 测试 / 出口）、`database/README.md`（0007 行、seed 命令与目录表、验证口径）、`database/seeds/README.md`（执行约定 + #6a / #6b 拆分）、`技术设计v0.2` §11.1 状态、`技术设计v0.3` §3.2 表注与状态行、`shared/README.md` M1 切片行状态、本条目。 |
| 风险与后续 | 1）迁移 / 种子属高风险变更（CONTRIBUTING §15），需 lan / px 非作者评审后再合 main；2）目录适配器（Casdoor / 企微 HTTP 拉取）与 worker 调度未接（随 i5）——当前同步引擎输入为快照，可单测但未含真实拉取；3）`role_permissions` 矩阵条目随 h6（PoC-6），当前为空集；权限判定仍只在服务端（ADR-011）；4）人员 ↔ 部门映射（多部门 / 兼职）未落，随 D1-06；5）缺失安全阀阈值（max(3, 20%)）为首版经验值，真实通讯录接入后复核。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。时序图 `h1 · S6·identity/org` 可由「部分完成」推进为「数据面 + 出口完成，适配器 / 矩阵 / 管理端点待接」。 |

## 2026-09-20 11:22 +08:00 | wmj | wmj | Push 73

| 字段 | 内容 |
|---|---|
| 类型 | `docs(contracts)`（M0-02 契约切片表）+ `docs(db)`（M0-03 种子数据规格） |
| 分支 | `wmj` |
| PR / commit | 随 [PR #56](https://github.com/256-code/LibiaoLink/pull/56) 提交；未直接推送 `main`。 |
| 做了什么 | ① **M0-02**：把 `shared/README.md` 的「本批范围与后续切片」升级为**契约切片表** —— 按 M1~M8 逐卡登记契约增量（端点 / 字段 / 错误码 / 参数 / 复核），附 11 条 ADR 的契约影响索引（ADR-019 ~ ADR-029）与维护规则；现状口径 paths=44 / schemas=108（54 个操作）。② **M0-03**：新增 `database/seeds/README.md` —— 种子数据规格（与迁移的分界、执行约定、首批 10 项种子清单、验收口径）；`database/README.md` 目录表与规则补指针。③ 技术设计v0.3 的 §7 状态注与 §3.1 M0 状态行更新为 M0-01 ~ M0-04 产出情况。 |
| 新增功能 | 无代码；新增契约切片表与种子规格两份可执行清单（M0 出口输入）。 |
| 修复 / 重构 / 优化 | 消除「切片只散在里程碑表」的状态：契约增量收敛到 `shared/README.md` 单表；种子与迁移的边界（只追加 vs 可重跑）写成明文规则。 |
| 测试与验证 | 纯文档（未触碰 `shared/src` 与生成物，无需重跑 generate / check）：① 切片表逐行对照 v0.3 §3 接口表与 v0.2 §11.3 端点族，路径命名与现有 44 条不冲突；② 现有 44 paths / 54 operations 核对，切片表新增项均标注「新增」且无重名；③ 种子清单 10 项逐项对应 M0-03 的五类产出与 ADR / 功能书来源；④ README 表格结构逐个渲染检查（无断行 / 无错列）。 |
| 文档同步 | `shared/README.md`、`database/README.md`、`database/seeds/README.md`（新增）、`技术设计v0.3-实施与验收.md`（§7 状态注 + §3.1 M0 状态行）、本条目。 |
| 风险与后续 | 1）切片表内 M4~M7 端点为「预计」，随对应卡片落地时以 `shared/src` 为准并回写；2）种子规格的业务占位值（#5 地区 / 项目类型、#8 任务节点清单、#3 SLA 时限）待业务回执；3）M0 出口标准对照：十五项有结论（✓ Push 71）、R01~R07 逐字入仓（✓ Push 71）、蓝图节点与项目类型枚举定稿（部分 —— 待业务）；4）M0 工程侧卡片（M0-01 ~ M0-04）产出齐备。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。时序图 `g9`（M0·决议会）工程侧可收口（业务回执项另记）。 |

## 2026-09-20 11:17 +08:00 | wmj | wmj | Push 72

| 字段 | 内容 |
|---|---|
| 类型 | `chore(merge)`（同步 main：PR #42 squash 合入后的 `14e51e0`） |
| 分支 | `wmj` |
| PR / commit | 随 [PR #56](https://github.com/256-code/LibiaoLink/pull/56) 提交；未直接推送 `main`。 |
| 做了什么 | 把 `origin/main`（`14e51e0`，PR #42 squash 落地）合并回 `wmj`：解 `开发日志.md` 与 `技术设计v0.3-实施与验收.md` 两处冲突（本线内容为超集，取本线版本；main 侧无独有内容），合并后 PR #56 的 diff 收敛为本批 M0 决议改动。 |
| 新增功能 | 无（同步合并，合并后工作区与合并前一致）。 |
| 修复 / 重构 / 优化 | 修复 PR #56 `mergeable_state=dirty`：squash 合并造成的历史分叉由本次合并归位。 |
| 测试与验证 | 合并前用 `git merge-tree --write-tree origin/main origin/wmj` 预演 —— 仅两文件冲突，且 `main..wmj` 差异为该两文件的纯增补（15 / 39 行，无删除项）；合并后复查无冲突标记，关键内容（Push 71 条目 / v0.3 §7.2）在位。 |
| 文档同步 | 本条目；其余文件随合并与 main 对齐。 |
| 风险与后续 | 1）后续每轮 squash 合并后都需一次同步合并（既有节奏，Push 69 / 72 同例）；2）M0-02 契约切片表与 M0-03 种子清单为下一批产出。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。 |

## 2026-09-20 11:15 +08:00 | wmj | wmj | Push 71

| 字段 | 内容 |
|---|---|
| 类型 | `docs(adr)`（M0-01 口径冻结决议：ADR-019 ~ ADR-029）+ `docs(rules)`（M0-04：R01~R07 文案逐字入仓 + 金标用例骨架） |
| 分支 | `wmj` |
| PR / commit | [PR #56](https://github.com/256-code/LibiaoLink/pull/56)（`wmj` → `main`，head `1826c2d`）；未直接推送 `main`。 |
| 做了什么 | ① **M0-01 决议会**：技术设计v0.3 §7 十五项逐项定案 —— 11 项落 ADR-019 ~ ADR-029（蓝图组织 / 节点权限 / 负责人标识与用户目录 / 项目更新时间语义 / 阶段推进 / 成果文件多选与门禁 / 「进行中」自动置位 / 问题 SLA / 归档快照 / 时区口径 / 搜索分词），每条含背景 / 决策 / 备选与不采用原因 / 影响与后果 / 切换触发条件；② **M0-04**：新增 `docs/rules/`（目录说明 + R01~R07 内置规则文案 —— 标题与内容按需求说明书 V1.0 §6.1 原文逐字保留，附金标用例骨架与 5 条待业务确认项）；③ 文档回写：ADR 索引（至 ADR-029）、ADR-015 修订注记（「蓝图全局一份」→ ADR-019；「一期节点体系不做权限控制」→ ADR-020）、技术设计v0.3（§7 状态注 + §7.2 M0 决议落点 + §7.3 业务确认清单；§3.3 / §3.4 三处改为引用 ADR）、技术设计v0.2（§3.1 蓝图口径 + §3.5 导入步骤改按 ADR-019）、系统功能书（C2-02 文案基准指针 + 文档头修订行）。 |
| 新增功能 | 无代码功能；新增 11 条 ADR 与 `docs/rules/` 金标基准目录（M5 规则引擎验收的逐字比对基准）。 |
| 修复 / 重构 / 优化 | 修掉三处口径悬挂：ADR-015 的两条过期口径（蓝图范围 / 节点权限）；A10 契约描述（缺省 = 项目经理兜底）与原型「留空 = 待分配」的冲突（ADR-021 裁定为可空 + 待分配，契约描述随 M0-02 修正）；v0.2 §3.1 / §3.5 与新蓝图组织不一致。 |
| 测试与验证 | 纯文档批次（无代码 / 契约 / 生成物改动，check-drift 不适用）：① v0.3 §7 十五项与 7.2 / 7.3 表逐行核对一一对应（11 ADR + 1 文案 + 3 业务确认）；② ADR 六要素模板逐条自检；③ 交叉引用核对：ADR 编号、`docs/rules/` 路径、章节号无悬空；④ R01~R07 标题与文案与源文档逐字比对（含标点与变量占位符格式）。 |
| 文档同步 | 新增 `docs/adr/ADR-019` ~ `ADR-029`（11 条）、`docs/rules/README.md`、`docs/rules/R01-R07-内置规则文案.md`；更新 `docs/adr/README.md`、`docs/adr/ADR-015-流程节点与蓝图.md`、`技术设计v0.3-实施与验收.md`、`技术设计v0.2-架构与数据模型.md`、`系统功能书.md`、本条目。 |
| 风险与后续 | 1）ADR 属高风险评审（CONTRIBUTING §15）：需 lan / px 非作者评审后再合并；2）M0 剩余 —— M0-02 契约切片表（含 ADR-021 的 A10 描述修正与 ADR-024 的 deliverableTypes 切片）、M0-03 种子数据清单与格式，下一批产出；3）业务确认 3 项（#8 变更阶段映射 / #10 项目类型取值与色值 / #15 演示字段来源）按建议值先行、可修订，待业务回执；4）ADR-029 标注「可修订」（复审 = M7 开工前 PoC）；5）时序图 `g9`（M0·决议会）待 M0-02 / M0-03 完成后收口。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。对应时序图卡 `g9 · M0·决议会`（阶段 5，主责 wmj、评审 lan / px）；v0.3 §7 出口标准「十五项全部有结论」本批满足（12 项定稿 + 3 项按建议值先行并标注可修订）。 |

## 2026-09-20 10:47 +08:00 | wmj | wmj | Push 70

| 字段 | 内容 |
|---|---|
| 类型 | `feat(contracts)`（w1 / A12~A14：任务写入口径定案 —— 状态可写 + 进度 / 完成日期联动 + 逾期派生） |
| 分支 | `wmj` |
| PR / commit | 随 PR #42（wmj → main）提交；未直接推送 `main`。 |
| 做了什么 | ① 决议（时序图卡 `w1`；对照 px 已落地的前端联动逻辑逐条核对 `frontend/src/data/tasks.ts`）：**A12** `TaskUpdateBody` 开放可选 `status`（基础三态 pending / active / done），服务端同事务回填进度与完成日期（done → progress=1 且 actualEnd 缺省按当天；active → 至少 1 格、满格退 3 格，清 actualEnd；pending → 0 格，清 actualEnd）；「已延期 / 提前完成」是派生展示态、不可写（400）且**派生优先**（未完成已过 plannedEnd 一律「已延期」，不因下拉改写）；**A13** 完成日期填 / 清都走 `/progress` —— `progress<1` 服务端一律清空 `actual_end`（清除的唯一方式）、`progress=1` 缺省按当天（Asia/Shanghai）、进度离散五档（迁移 / 演示数据小数先归一，0.49 → 0.5）；**A14** `onTime` 与展示态服务端读时派生（枚举不变），「逾期未交付 / 逾期已交付」由前端按 `onTime` + `displayStatus` 渲染，派生不出回落迁移存储值。② 契约：`shared/src/modules/tasks.ts`（TaskUpdateBody 增 `status`；TaskProgressUpdateBody / TaskProgress / Task / TaskUpdateBody 描述）、`shared/src/common/dicts.ts`（TaskDisplayStatus 描述）。 |
| 新增功能 | 任务状态可写（服务端统一裁决双向联动）；完成日期可清（进度写回 <1 档）；`onTime` 与逾期标签服务端派生（唯一来源）。 |
| 修复 / 重构 / 优化 | 修掉三处前后端各派生一套的隐患：状态「手动优先」语义（原型 `statusOverride`）、完成日期置空语义、逾期标签本地派生；并纠正 `displayStatus` 描述里「逾期标注落在 actualEnd」的过期口径（Push 67 已改挂「是否按时交付」列）。 |
| 测试与验证 | 1）`shared`：重跑生成物（openapi.json 190,371 B / api-types.d.ts 146,606 B）；`check-drift.mjs` 通过 —— **paths=44、schemas=108**（无新增 / 删除 schema），两份生成物与生成结果一致；typecheck / build exit 0；2）生成物抽查：`TaskUpdateBody.properties.status` = `allOf[$ref TaskBaseStatus]` + 描述、required 仍为 `version`；`Task.onTime` 描述含四条派生分支；`TaskProgressUpdateBody.actualEnd` 描述含「progress<1 清空」；3）前端只读核对（未改 `frontend/`）：`progressAfterStatus` / `statusOverrideAfterProgress` / `lateDeliveryLabel` 与本次口径逐条对照，差异三处登记为前端待改。 |
| 文档同步 | `shared/README.md`（口径表补 A12~A14 三行；对齐清单改 A1~A14）、`技术设计v0.3-实施与验收.md`（§3.4 接口表补 `PATCH /tasks/{taskId}` 与 progress 语义、关键设计补任务写入口径、§7.1 标题改 A1~A14 + 新增 A12~A14 三行 + 决议注）、`前端功能需求.md`（§3.8 A12 / A13 / A14 改「已定案（PR #42，Push 70）」+ 索引行 + 变更记录 Push 70）、`字段对照清单.md`（口径补充 + 任务状态 / 实际完成日期两行）、本条目。 |
| 风险与后续 | 1）契约属高风险变更（CONTRIBUTING §15）：仍请 lan / px 非作者评审后合并；2）**前端三处待改**（k4 接线前完成，已随 PR #42 评论同步 px）：① 状态下拉不再盖掉「已延期」（派生优先，`statusOverride` 可删）；② 点满四格 / 选「已完成」后显示服务端补的当天完成日期；③ 逾期判定改用真实日期（原型按「M月D日」比较，跨年会判错）；3）时序图 `w1` 卡随本条收口；4）若业务日后要求「未逾期也能手动标延期」，需单开字段（如 `delay_flag`），本期不做。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。对应时序图卡 `w1 · A12~A14`（阶段 7 / M1~M3，依赖 g2 / p62 均已完成；主责 wmj、评审 px）。 |

## 2026-09-20 10:30 +08:00 | wmj | wmj | Push 69

| 字段 | 内容 |
|---|---|
| 类型 | `feat(contracts)`（A9 定案：项目列表排序白名单补 `createdAt`）+ `chore(merge)`（同步 `main`、解冲突、重跑生成物） |
| 分支 | `wmj` |
| PR / commit | 随 PR #42（wmj → main）提交；未直接推送 `main`。 |
| 做了什么 | ① 同步 `origin/main`（Push 50~68：lan 的平台数据层与迁移 0005 / 0006、`database` CI job，px 的前端多批 / 模板契约切片 / 分类筛选侧栏材质等）到本分支：解 `shared/src/openapi.ts` 冲突（本线 A1~A8 的 5 条路径 / 6 个操作与 main 侧任务节点库 / 任务模板路径并存、无丢项）、`开发日志.md` 冲突（main Push 67~50 在上，本线 Push 49 按 16:14 落回 px Push 49 之前）与 `前端功能需求.md` 变更记录冲突（两行并存：px Push 68 在上、本条目 Push 69 在下）；② **A9 定案**（PR #42 评审 px 提请、本次答复采纳）：`shared/src/modules/projects.ts` 项目列表排序白名单补 `createdAt`（`sort=createdAt:asc\|desc`），缺省序仍 `updatedAt:desc`（最近活动在前）—— 只改白名单与描述，无新增 / 删除路径与 schema；③ 生成物重跑（`node scripts/generate.mjs`），漂移校验通过。 |
| 新增功能 | 项目列表支持 `sort=createdAt:asc\|desc`（服务端按既有 `projects.created_at` 列排序，无 DDL）；前端把 TIME 升级为「维度（创建时间 / 最近活动时间）× 方向」时无需再动契约。 |
| 修复 / 重构 / 优化 | 修复 PR #42 的 `mergeable_state=dirty`：`shared/src/openapi.ts`、`开发日志.md` 与两份生成物的冲突全部解开，`wmj` 与 `main` 的差异收敛为 A1~A9 契约增量。 |
| 测试与验证 | 1）`shared`：重跑生成物（openapi.json 187,941 B / api-types.d.ts 144,935 B）；`check-drift.mjs` 通过 —— **paths=44、schemas=108**，两份生成物与生成结果一致、无漂移；typecheck exit 0、build（CI 同款 tsconfig.build.json）exit 0；2）契约超集核对（以 `origin/main` 生成物为基准逐条比对）：缺路径 0、缺 schema 0，新增 4 条路径（`/users`、`/users/me/preferences`、`/dicts`、`/dicts/{type}`）；3）`server`（本次合并带入 main 的平台数据层）：`tsc -p tsconfig.json --noEmit` exit 0、`vitest run` 3 文件 / 16 例全过、`nest build` exit 0、`check-boundaries` 通过（39 文件 / 100 条依赖 / 0 违规）；4）生成物抽查：`ProjectListQuery.sort` 描述 = 「一期白名单 updatedAt / createdAt / seqNo；缺省 = updatedAt:desc」。 |
| 文档同步 | `shared/README.md`（时间区间 A1 行补排序白名单；对齐清单改 A1~A9 并注明 A9 决议出处 = PR #42）、`技术设计v0.3-实施与验收.md`（§3.3 列表一致性排序白名单、§7.1 标题改 A1~A9 + 新增 A9 行 + 2026-09-20 决议注）、`前端功能需求.md`（§3.2 首页 `sort` 行补白名单口径、§3.8 A9 状态改「已定案（PR #42，Push 69）」、第六章 Q15 契约侧收口、变更记录 Push 69）、本条目。 |
| 风险与后续 | 1）契约属高风险变更（CONTRIBUTING §15）：A9 为白名单扩项、向后兼容（缺省序不变），仍请 lan / px 非作者评审后合并；2）合并 `main` 后本 PR 体量变大（含 Push 50~68 全部内容），评审以 PR 页面 diff 为准；3）`前端功能需求.md` 主责 px，本次只同步 A9 状态与 Q15 两处口径，如与 px 理解不符请回退；4）A12 / A13 / A14（任务状态可编辑、实际完成日期可编辑与联动、逾期派生）仍在等本线决议，未含在本次改动内。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。Push 编号按「先入 main 者为准」顺延为 **Push 69**（Push 68 = px 的分类筛选侧栏「液态玻璃」材质，先于本条目入 main；本条原记 68，随本次同步顺延）；#43（px 落在本分支的 A1 缺省排序 + A2 随行 `managerName` + A3 `accent` / `accentText`）随本 PR 一并入 main，其契约内容已含在上述校验内。 |

## 2026-09-20 10:24 +08:00 | px | px | Push 68

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（分类筛选侧栏换「液态玻璃」材质，纯外观 / 材质，无功能变更） |
| 分支 | `px` |
| PR / commit | [PR #55](https://github.com/256-code/LibiaoLink/pull/55)（`px` → `main`，squash 合并，合并提交 `ad9e2d6`）；CI 四项检查（frontend / server / database / shared）全绿。 |
| 做了什么 | 业务反馈「项目空间的分类筛选也想用任务表行内编辑那个液态玻璃效果」，把侧栏整体换成玻璃材质（交互 / 筛选逻辑 / 计数 / URL 参数一律未动）：① **侧栏底板**由纯白实底改为**玻璃面板** —— 半透明白渐变（`rgba(255,255,255,0.72) → rgba(255,255,255,0.5)`）+ `border-white/80` 白色发丝边 + 顶缘内高光 + `backdrop-blur-2xl backdrop-saturate-150` 背景虚化 + `0 8px 32px rgba(15,23,42,0.14)` 柔和投影，与页面浮层（任务节点卡片 / 任务编辑弹窗）同一材质口径；② **「地区 / 项目类型 / 项目经理」三组多选胶囊**与**「项目时间」触发框**改用**任务表行内可编辑单元格同一套玻璃小胶囊**（Push 66 / 67 定稿口径）：`border-zinc-200/90 bg-white/75` + 顶部内高光 + 极轻投影 + `backdrop-blur-[3px]`，悬停转白并加深描边 / 投影，选中态保留深灰底 + 白字并补浮起投影；③ `CategoryFilterSidebar.tsx` 抽出 `GLASS_PANEL` / `GLASS_SURFACE` 两个材质常量（口径集中一处，后续调浓淡只改一处）；④ `DateRangePicker.tsx` 新增可选 `triggerClassName`（不传 = 原白底描边，任务编辑表单里的同类控件行为完全不变）；⑤ 侧栏头部 / 底部两道分隔线由 `zinc-100` 调为 `zinc-200/70`，保证在玻璃底上仍有层级。 |
| 新增功能 | 无（纯外观；胶囊仍是多选、计数与叠加口径不变）。 |
| 测试与验证 | 1）`frontend/`：`npm run typecheck`（`tsc -p tsconfig.json` + `tsc -p tsconfig.node.json`）通过；`npm run build` 通过（产物 `index-BcbuYIuZ.css` 63.89 kB、`index-By8qphoC.js` 364.53 kB）；2）视觉复核（本地 dev `localhost:3000` + 桩后端 `/auth`，无头 Chrome）：1500×1000 桌面态 + 3× 局部特写逐项核对 —— 玻璃底板（半透明 / 发丝边 / 顶缘高光 / 投影）、玻璃胶囊静止态、选中态（`filter[region]=中国`，深灰胶囊 + 浮起投影）、「项目时间」玻璃触发框；3）窄屏（<md）遮罩浮层打开态正常（遮罩 + 玻璃底叠加无发灰异常）；4）CI：PR #55 四项检查（frontend / server / database / shared）全绿。 |
| 文档同步 | `前端功能需求.md`（§一.3 「筛选侧边栏」玻璃底板口径 + 「侧边栏内容」胶囊 / 项目时间触发框材质口径 + 变更记录 Push 68），与代码**同一次推送**（commit `6f6672c`）；`字段对照清单.md` 无字段口径变化、无需更新；本条目（开发日志回填 Push 68 的 PR #55）。 |
| 风险与后续 | 1）纯样式改动，未改 DOM 结构与筛选状态，回滚 = revert 本提交；2）接口层唯一变化 = `DateRangePicker` 新增可选 props（向后兼容）；3）玻璃底板上色偏淡：仓库页面底色很浅（`#f5f6f8`），`backdrop-blur` 在均匀底色上不产生可见内容，故底板另加「半透明白渐变 + 白色发丝边 + 顶缘高光 + 投影」以立住材质，观感以业务真机为准；若要更浓 / 更淡，只改 `GLASS_PANEL` 一处；4）截图为本地原型（示例数据），一期落库待后端。 |
| 备注 | 材质口径与 Push 66 / 67 的任务表行内编辑单元格一致（同一套 `border-zinc-200/90 bg-white/75` + 顶部内高光 + 极轻投影），保证「同一材质全站复用」；侧栏底板复用页面浮层的玻璃口径（`linear-gradient` + `backdrop-blur-2xl backdrop-saturate-150`）。 |

## 2026-09-19 23:17 +08:00 | px | px | Push 67

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（任务表列对齐 + 实际完成日期可编辑 + 完成态清空联动 + 行内编辑外观 + 「任务节点 / 模板」卡片落点定稿） |
| 分支 | `px` |
| PR / commit | [PR #54](https://github.com/256-code/LibiaoLink/pull/54)（`px` → `main`，squash 合并，合并提交 `4231514`）。 |
| 做了什么 | ① **列对齐**：表头一律 `text-center`、非任务描述列的单元格内容包一层 `flex items-center justify-center`（内容中心对齐列标题中心），去掉开始日期列原先 `text-right` / `justify-self-end` 的靠右口径；任务描述列保持左对齐（内含四格进度条与悬停铅笔）。② **实际完成日期可在表格行内直接选**（`InlineDateCell` 单日期小日历）：填日期 = 写 `doneDate` + 进度 1（四格全亮、状态交回完成态派生，按工期可能「提前完成」）；清除 = 清空 `doneDate` + 进度 3 格 + 状态「进行中」。③ **非完成态自动清空实际完成日期**：行内把任务状态改成 待开始 / 进行中 / 已延期（`data/tasks.ts` 新增 `isCompleteStatus()` 判定），或把四格进度条点回 1~3 格时，一并清空 `doneDate`。④ **行内可编辑单元格外观**：静止态由「很淡的玻璃底 + 白色发丝边」改为**白底 + 淡灰描边小胶囊**（`border-zinc-200/90 bg-white/75` + 顶部内高光 + 极轻投影，对齐业务样张），悬停 / 展开态加深描边与投影。⑤ **默认列调整**（`DEFAULT_VISIBLE_COLUMNS`）：按业务截图把默认列改成「任务描述 / 项目经理 / 任务负责人 / 任务状态 / 紧急重要度 / 是否按时交付 / 输出成果文件 / 文件 / 项目进展描述 / 开始日期 / 预计所需天数 / 预计完成日期 / 实际完成日期 / 变更关联」—— 「文件」「项目进展描述」由默认隐藏改为默认显示，只保留「预计所需施工人数」默认隐藏。⑥ **点进度条不再摘掉「已延期」**（业务反馈「任务状态是已延期时点四点状态怎么变成进行中了」）：`statusOverrideAfterProgress(progress, pastDue)` 增加过期入参（新增 `isPastDue`，与 `isTaskOverdue` 同源）—— 已过预计完成日期的任务点 0~3 格一律保持「已延期」，点满四格才算完成、交回完成态派生。⑦ **逾期标注移位**（业务定案）：新增 `lateDeliveryLabel(task)` —— 未完成且已过预计完成日期 = 红色「逾期未交付」、已完成但晚于预计完成日期（或未填完成日期但预计完成日期已过）= 琥珀「逾期已交付」，落在「是否按时交付」列与任务详情字段；「实际完成日期」列 / 字段只显示日期或「—」。⑧ **「任务节点 + 模板」卡片落点定稿**（业务反馈「浮在右上角太远 / 跟着点击太跳 / 按余量翻转也太跳」）：卡片**顶部与所点阶段的分组头在同一水平线**（卡顶 = 分组头顶，页面滚到哪都保持这条水平线）、**左边缘对齐「项目经理」列**（`TaskBoard` 量表格算落点、`absolute` 渲染在表格容器内，随表格滚动一起走；横向滚动时跟着该列走并夹在表格内不被裁掉；该列被列显隐关掉时回落到表格右边）；高度取 72vh 与「分组头以下表格余量」的较小值（保底 320px）；分组头找不到（异常）时回落到「表头下沿 +10px」；同批修掉 `StageAddCard` 上残留的 `top-28` 定位类与翻转用的 `bottom` 冲突（曾导致卡片飘到屏幕外）。 |
| 新增功能 | 「实际完成日期」与「任务状态」一样可在任务表行内直接填 / 清（`TaskPatch` 增加 `doneDate`）；接口对齐清单新增 **A13**（见 `前端功能需求.md` §3.8，待 wmj 决议）。 |
| 测试与验证 | 1）`frontend/`：`npm run typecheck` / `npm run build` 通过；2）Playwright（本地 3000 + 本地登录，印度项目 `#/project/inmu-0010`）：列居中（含全 15 列）/ 白底胶囊静止态；实际完成日期填 9月18日 → 「已完成」+ 四格 + 日期回填 ✓，再点开「清除」→「进行中」+ 3 格 + 该格回到红色「逾期」✓；「布局定档」把状态改「进行中」「待开始」→ 实际完成日期自动清空 ✓，再选「已完成」→ 四格 + 日期保持空（不虚构日期）✓；3）默认列核对（1470 宽、印度项目，不做任何勾选）：表头 = 任务描述 / 项目经理 / 任务负责人 / 任务状态 / 紧急重要度 / 是否按时交付 / 输出成果文件 / 文件 / 项目进展描述 / 开始日期 / 预计完成日期 / 实际完成日期 / 变更关联，表格 1420/1420 全部可见；4）逾期口径核对：`运输` 行（预计完成 8月31日）= 已延期 / 逾期未交付 / 实际完成「—」，点第 2、3 格进度条后状态仍「已延期」（2 / 3 格）；给该行填实际完成 9月19日（晚于预计）→ 已完成 + 逾期已交付；`施工安全培训` 填 9月18日（晚于预计 9月3日）→ 已完成 + 逾期已交付；`布局定档`（5月20日完成、预计 5月20日）仍显示「按时交付」；5）回归：行内 7 字段（经理 / 负责人 / 紧急重要度 / 开始日期 / 预计完成日期 / 人数 / 进展描述）全部生效、状态 ↔ 四格进度条 8 用例全通过、点浮层不触发行选中 / `Esc` / 点外部关闭、点行体仍开任务详情抽屉；6）卡片落点（本批二次调整后定稿）：点「设计开发」卡顶 317 = 分组头顶 317、点「硬件实施」488 = 488、点「软件部署」422 = 422（差值恒为 0，换阶段不跳）；卡片左边缘 = 项目经理列左边缘（385），表格横向滚 110 时同步到 275；空项目（波兰）与印度项目各阶段均验证；卡片功能回归：点节点加入（组装发货行 1→2、显示「已添加」）、`Esc` / 再点同一阶段标签 / 点页面空白关闭均正常。 |
| 文档同步 | `前端功能需求.md`（§6.3 列对齐 + 行内编辑字段 + 是否按时交付 / 实际完成日期口径、§6.4 点进度条不摘「已延期」、§6.5 抽屉字段、§二.2 交付标注、§四 onTime、§6.6 / §二.4 默认列、§3.5 字段映射、§6.4 联动补实际完成日期、§6.8 只读区口径、§6.9 字段清单 / 可点区域外观 / 实际完成日期列、§四 任务字段、§3.8 新增 **A13**、变更记录 Push 67）、`字段对照清单.md`（口径补充 + 实际完成日期字段行 + 文件 / 施工人数默认显示口径）、`系统功能书.md`（A1-01 列显隐默认口径 + 逾期标注口径 + 文档头修订 + §是否按时交付 / 是否逾期 / 任务详情 / 看板四处「逾期标注位」措辞同步）、本条目（卡片落点定稿：`前端功能需求.md` §6.3 添加任务条目 + 变更记录 Push 67 ⑧）。 |
| 风险与后续 | 1）契约缺口（A13，待 wmj 决议）：`TaskUpdateBody` 不含实际完成日期，`/progress` 的 `actualEnd` 为 optional、不可显式置空 —— 填 / 清建议统一走 `/progress`（清 = `progress<1` 时由服务端清空 `actual_end`），或按 A12 把「状态 / 进度 / 实际完成日期」收进同一条写入裁决；2）定档口径已同步：`系统功能书.md` A1-01 列显隐默认口径 + 文档头修订（Push 67，变更提出人 = 业务、px 落地；wmj 技术可行性评审可在 PR 评审时一并确认），源文档（V1.0）侧需人工同步同一条口径；3）逾期派生目前在前端本地（A14，待 wmj 决议）：契约 `Task.onTime` 为可空 boolean、展示态枚举无「逾期已交付」取值，建议服务端统一回填 `onTime` + 展示态（与 A12 / A13 的联动裁决一并定）；4）任务编辑弹窗内「实际完成日期」仍为只读（本轮只在表格行内放开），如业务要求弹窗同改再放开；5）改动仍只存浏览器内存（换项目 / 刷新回初始数据），一期落库待后端。 |
| 备注 | 本批把「逾期」标注从「实际完成日期」迁到「是否按时交付」列后，实际完成日期列 / 字段只显示日期或「—」；卡片落点最终采用「与所点阶段同一水平线」版（业务在当前原型上逐轮看效果后定稿）。 |
## 2026-09-19 21:26 +08:00 | px | px | Push 66

| 字段 | 内容 |
|---|---|
| 类型 | `style(frontend)`（行内可编辑字段的「可点区域」外观调整，纯前端样式） |
| 分支 | `px` |
| PR / commit | PR #53（`px` → `main`，squash-only；head `bc72c47`）。 |
| 做了什么 | 行内可编辑单元格原先用负外边距向外扩边（`-mx-1.5` + `px-1.5`），鼠标判定范围比看得见的文字大一圈、肉眼也看不出来，用户不知道「点哪里、点得到的范围有多大」。改为把字段元素放进一个**不明显的「液态玻璃」小框**：静止 = 很淡的玻璃底（`bg-zinc-900/[0.045]`）+ `border-white/70` 发丝边 + 顶部内高光 + `backdrop-blur-[3px]`；悬停 = 转白 + 细描边 + 轻投影；展开中 = 白底 + `ring-1 ring-zinc-900/15` + 浮起投影。框本身 = 鼠标判定范围（去掉负外边距），内容仍按列宽 `truncate`；空值（「—」/「待分配」）一样带框提示可填；日期列小框靠右对齐（`justify-self-end` 透传不变）；任务状态 / 紧急重要度的色标签嵌在玻璃框内。 |
| 新增功能 | 无新增字段 / 功能，只调整行内编辑的可点区域外观（`components/InlineEdit.tsx` 的 `InlineCell` 触发器样式）。 |
| 测试与验证 | 1）`frontend/`：`npm run typecheck` / `npm run build` 通过；2）Playwright（本地 3000 + 本地登录，印度项目）：1x / 2x 截图核对静止、行悬停、展开三态外观（框淡、悬停转实、展开带 `ring` 与投影，日期列右对齐，长文本仍截断）；3）回归：行内改负责人 / 任务状态 / 紧急重要度 / 日期 / 人数 / 进展描述全部生效，状态 ↔ 四格进度条联动 8 个用例全通过，点浮层不触发行选中、`Esc` / 点外部关闭、点行体仍开任务详情抽屉。 |
| 文档同步 | `前端功能需求.md`（§6.9 新增「可点区域（Push 66）」、§6.3 行交互补一句、变更记录 Push 66）、本条目；字段口径无变化，`字段对照清单.md` 未改。 |
| 风险与后续 | 1）玻璃框用了 `backdrop-blur-[3px]`，表格行多时会有多图层模糊 —— 实测 44 行无明显掉帧，行数继续增大（几百行）时建议改为「只在悬停时启用模糊」；2）「已填值都带框」会让整表偏「胶囊感」，如业务觉得太满，可改成「仅空值 + 悬停时显示框」。 |
| 备注 | 视觉口径与仓库既有毛玻璃卡片一致（半透底 + 白描边 + 内高光 + 模糊），未引入新依赖。 |
## 2026-09-19 21:17 +08:00 | px | px | Push 65

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（任务状态可编辑 + 与四格进度条双向联动） |
| 分支 | `px` |
| PR / commit | PR #52（`px` → `main`，squash-only；head `94d80b3`）。 |
| 做了什么 | ① **任务状态列改为色标签 + 行内下拉**（不必开弹窗）：色签 = 已延期（红）/ 进行中（琥珀）/ 已完成（绿）/ 待开始（天蓝）/ 提前完成（紫），下拉选项同样用色签（顺序对齐业务截图：已延期 / 进行中 / 已完成 / 待开始 / 提前完成）；`SelectOption.label` 放宽为 `ReactNode` 以支持标签型选项。② **与四格进度条双向联动**（`data/tasks.ts` 新增 `PROGRESS_STEPS` / `progressStep` / `statusOverrideAfterProgress` / `progressAfterStatus`，`Tracker` 改为复用同一套换算）：点进度条 → 0 格 = 待开始、1~3 格 = 进行中、4 格全亮 = 已完成（填了预计完成日期且实际完成更早 → 提前完成；未填预计完成时间 → 只显示「已完成」）；选状态 → 待开始 = 进度条清零、进行中 = 至少亮 1 格（原四格全亮时退回 3 格）、已完成 / 提前完成 = 四格全亮、已延期 = 保持当前格数（0 格也能是已延期）。③ 新增任务字段 `statusOverride`（手动指定，优先于派生；四格全亮时由联动清空、交回完成态派生），`taskStatus()` 优先级改为「手动 → 完成态 → 延期 → 进度格数」——已填实际完成日期的任务把进度条退回 3 格后状态会跟随变「进行中」，再点满四格回到完成态；`ProjectDetail` 里点进度条同时写状态覆盖、行内改状态时进度走进度覆盖表（避免被旧值盖回）。 |
| 新增功能 | 任务状态可直接在表格里选（五态色签），并与四格进度条双向联动（口径见 `前端功能需求.md` §6.3 / §6.4 / §6.9）。 |
| 测试与验证 | 1）`frontend/`：`npm run typecheck` / `npm run build` 通过；2）Playwright（本地 3000 + 本地登录，印度项目 `#/project/inmu-0010`）：0 格选「进行中」→ 点亮 1 格 ✓ / 点第 3 格 → 进行中 3 格 ✓ / 选「已延期」→ 保持 3 格 ✓ / 选「已完成」→ 四格全亮 ✓ / 选「待开始」→ 清零 ✓ / 清除预计完成日期 + 点满四格 → 只显示「已完成」（不带「提前完成」）✓ / 选「提前完成」→ 四格全亮 + 提前完成 ✓ / 已完成行退回 3 格 → 进行中、再点满 → 已完成 ✓；3）回归：行内改负责人 / 紧急重要度 / 日期 / 人数 / 进展描述、任务详情抽屉、项目空间筛选侧栏、任务编辑弹窗滚动均正常。 |
| 文档同步 | `前端功能需求.md`（§6.3 状态列口径、§6.4 Tracker 联动规则、§6.9 行内编辑字段、§四 `status` 字段说明、§3.8 新增 **A12** 接口需求、变更记录 Push 65）、`字段对照清单.md`（口径补充 + 任务状态字段行）、本条目。 |
| 风险与后续 | 1）契约缺口（A12，待 wmj 决议）：`PATCH /api/v1/projects/{id}/tasks/{taskId}`（`TaskUpdateBody`）现不含状态字段、状态由进度与日期派生；业务要求手动改状态后，建议服务端在任务写入时统一按「手动优先 + 进度联动」口径回填 `status`，避免前后端两套派生漂移；2）手动把任务标为「进行中 / 待开始」时，实际完成日期（数据字段）不会自动清空 —— 是否需要联动清空待业务确认；3）状态改动仍只存浏览器内存（换项目 / 刷新回初始数据）。 |
| 备注 | 「已完成 / 提前完成」的区分仍按工期（实际完成日期早于预计完成日期 = 提前完成）；未填预计完成时间只显示「已完成」。 |
## 2026-09-19 18:58 +08:00 | px | px | Push 63~64

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（任务编辑弹窗 + 任务表行内编辑 + 弹窗底部显示修复） |
| 分支 | `px` |
| PR / commit | PR #51（`px` → `main`，squash-only；head `5be27ad`）。 |
| 做了什么 | **Push 63 任务编辑弹窗**：入口 = 任务行悬停铅笔（任务描述列右侧）+ 任务详情抽屉「编辑任务」按钮；可编辑 = 项目经理（**项目级**，可搜索人员下拉，保存回写项目并刷新 `updatedAt`）/ 任务负责人（可搜索：姓名 / 拼音 / 岗位，留空 = 待分配）/ 开始日期 → 预计完成日期（与项目空间「分类筛选」同一套日期区间选择器）/ 预计所需施工人数（≥ 0 非必填）/ 紧急重要度（普通下拉）/ 项目进展描述；只读区 = 任务状态 / 项目进度 / 实际完成日期 / 输出成果文件（A1-17 锁定 + 系统派生）；同批把人员目录换成 `data/members.ts`（**全部虚构姓名**：项目经理 20 人 + 交付成员 11 人），`data/tasks.ts` 44 条演示负责人同步替换、`data/managers.ts` 复用该目录。**Push 64 表格行内编辑**：任务表里点单元格直接改上述 7 个字段（不必开弹窗）——新增 `components/InlineEdit.tsx`（`InlineCell` 通用外壳 + 人员 / 枚举 / 日期 / 数字 / 文本五种单元格）与 `components/usePopover.ts`（浮层 `fixed` + portal 到 `body`、点外部 / `Esc` / 再点同一格关闭）；浮层点击**不冒泡到行**（原先 portal 事件沿 React 树冒泡会误开任务详情抽屉，已修）；日期改其一同时写回开始 + 预计完成 + 联动天数，状态按新日期重派生；与弹窗共用同一张任务覆盖表（任务行 / 抽屉 / 汇总卡即时同步）；项目经理列行内改动走项目级回写（本项目所有任务行 + 项目卡片同步）。**Push 64 弹窗底部显示修复**：根因 = 弹窗原先只有 `max-h-92vh`（高度 auto），Chrome 下 flex 子项不按封顶高度收缩，滚动区拿到**内容高度**（实测 545px）反而溢出到页脚下面被裁；修复 = 弹窗改 `h-fit` + `max-h-[92vh]`（高度确定），`ScrollArea` 结构改 `flex flex-col` + 内部 `min-h-0 flex-1`（原 `h-full` 在不定高父级下退化为内容高），滚动区因此严格等于「弹窗高 − 头部 − 页脚」、超出才滚动；`ScrollArea` 另加 `thumbAlwaysVisible`（弹窗这类「一眼要看出还有内容」的场景滑块常显）。 |
| 新增功能 | ①「每一条任务可编辑」从弹窗延伸为**表格行内直接编辑**（7 个字段，入口见 `前端功能需求.md` §6.9）；② 任务编辑弹窗（§6.8）；③ 弹窗底部不再被裁、内容可滚动且滑块常显。 |
| 测试与验证 | 1）`frontend/`：`npm run typecheck` / `npm run build` 通过；2）Playwright（本地 3000 + 本地登录 zhangsan，印度项目 `#/project/inmu-0010`，行「布局定档」）：行内改负责人（搜索「方」→ 方沐）✓ / 紧急重要度（高 → 中）✓ / 开始日期 5月20日 → 5月6日（天数 1 → 15 联动、预计完成日期保持）✓ / 预计完成日期 → 5月28日（天数 23、状态「已完成」→「提前完成」）✓ / 施工人数 2 → 6 ✓ / 进展描述 ✓ / 项目经理（前 3 行同步为同一人）✓；点行内浮层与选值全程**不打开抽屉**（抽屉数 0）✓、`Esc` 与点外部关闭浮层 ✓、点行体仍开抽屉 ✓；3）弹窗高度实测：1440×700（滚动区 460 / 内容 545、滑块常显、页脚不再压内容）/ 1440×800 / 1560×960 均在视口内完整可见；4）回归：任务详情抽屉（453 / 661）与项目空间筛选侧栏（496 / 760）滚动正常。 |
| 文档同步 | `前端功能需求.md`（§6.3 行交互补行内编辑、§6.8 弹窗形态与滚动口径、新增 §6.9「表格行内编辑（Push 64）」、变更记录 Push 63 / Push 64）、`字段对照清单.md`（口径补充 + 任务负责人行编辑入口）、本条目。 |
| 风险与后续 | 1）行内编辑与弹窗目前都只写浏览器内存覆盖表（换项目 / 刷新即回到初始数据）——正式版由后端落库，接口 = `PATCH /api/v1/projects/{id}/tasks/{taskId}`（A10，Push 62 已入契约）；2）行内编辑暂未覆盖「文件 / 输出成果文件 / 变更关联 / 任务描述」（按 A1-17 锁定或走文件库），状态 / 进度 / 实际完成日期仍为派生只读；3）`ScrollArea` 结构改动影响任务详情抽屉与项目空间筛选侧栏，本次已回归；后续新用 `ScrollArea` 的容器需保证自身高度确定（`flex-1 min-h-0` 或固定高）。 |
| 备注 | 弹窗样式与行内浮层均沿仓库既有视觉口径（毛玻璃卡片 + 自绘滑块），未引入新依赖。 |
## 2026-09-19 17:29 +08:00 | px | px | Push 62

| 字段 | 内容 |
|---|---|
| 类型 | `feat(shared)`（契约补充：A10 任务创建 / 编辑 + A11 任务节点库 / 任务模板 / 从模板生成任务） |
| 分支 | `px` |
| PR / commit | PR #50（`px` → `main`，squash-only；head `e02c5fc`）。 |
| 做了什么 | ① 新增 `shared/src/modules/templates.ts`：任务节点库（`TaskNode`：阶段 / 中英文名 / 库内排序）与任务模板（`TaskTemplate`：名称 + 阶段 + 节点顺序，节点 = 「引用 `nodeId` + `seq` + 名称摘要」）；列表 / 详情 / 新建 / 编辑（改名 + `nodeIds` 全量替换，`version` 乐观锁）/ 删除（带 `version`）的请求与响应 schema。② `shared/src/modules/tasks.ts` 补三组：创建（`TaskCreateBodySchema`：`stageKey` / `title` / `titleEn?` / `taskNodeId?` 判重键 / `ownerId?`（缺省 = 项目经理兜底）/ 日期 / `estimatedDays?` / `headcount?` / `priority?` / `deliverable?` / `note?`）、编辑（`TaskUpdateBodySchema`：负责人 / 日期 / 人数 / 紧急重要度 / 备注 + `version`；任务描述 / 成果文件按 A1-17 生成后锁定，进度走 `/progress`）、从模板批量生成（`TaskCreateFromTemplateBodySchema` / `TaskCreateFromTemplateResponseSchema`：`templateId` + 可选 `nodeIds`，`skipExisting` 默认 true → `created` + `skipped`）。③ `shared/src/openapi.ts` 注册 8 条路径（`POST/PATCH /api/v1/projects/{id}/tasks…`、`POST /api/v1/projects/{id}/tasks/from-template`、`GET /api/v1/task-nodes`、`GET/POST/PATCH/DELETE /api/v1/task-templates…`）并新增 `templates` tag；`shared/src/index.ts` 导出新模块。④ 生成物重出：`generated/openapi.json`（160231 bytes）/ `generated/api-types.d.ts`（126557 bytes）。 |
| 新增功能 | 契约层：任务写入通道（创建 / 编辑 / 从模板批量生成）与任务模板域（任务节点库 + 模板 CRUD）——A10 / A11 从「待 wmj 决议」转为「已入契约」。 |
| 测试与验证 | 1）`shared/`：`npm run generate` → `npm run typecheck` → `npm run check` 通过（OpenAPI 3.1 结构校验：paths=40 / schemas=95；两份生成物与仓库一致、无漂移）→ `npm run build` 通过；2）`server/`：`npm run typecheck` 通过（新 schema 不影响既有实现；任务 / 模板的服务端实现与 DDL 待 wmj / lan 排期）；3）`frontend/`：`npm run typecheck` / `npm run build` 通过（本轮前端零改动）。 |
| 文档同步 | `前端功能需求.md`（§3.8 A10 / A11 改为「已入契约（Push 62）」+ 接口口径、索引行、§三 登记表 #19 / #21、变更记录 Push 62）、`字段对照清单.md` §四（接口需求 → 契约已入；落库建议表名 `task_nodes` / `task_templates` / `task_template_nodes`）、`shared/README.md`（模块清单 + 任务来源 / 任务模板口径 + 本批范围）、本条目。 |
| 风险与后续 | 1）服务端实现与落库 DDL / 迁移（`server/` / `database/`）待 wmj / lan 排期——契约先行、实现后置；2）「任务描述 / 成果文件生成后锁定」的服务端强校验与管理员例外调整（A1-17）在实现时落地；3）任务模板的权限与审计口径（谁能建 / 改模板）待权限矩阵落地后补充；4）`skipExisting=false` 时重复节点的 409 如需专用错误码（如 `TASK_NODE_ALREADY_ADDED`）由 wmj 在实现时随错误码表增补。 |
| 备注 | 契约改动经 px 授权直接落（用户 2026-09-19 指示）；按 CONTRIBUTING §15 高风险变更惯例，合并前经 wmj / lan 复核（CODEOWNERS 评审）。 |
## 2026-09-19 15:40 +08:00 | px | px | Push 61

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（项目总览：点阶段标签 → 右侧卡片「任务节点 + 模板」，把模板预设节点加进项目） |
| 分支 | `px` |
| PR / commit | PR #49（`px` → `main`，squash-only；与 Push 59 / 60 合并成一个 PR，head `f360866`）。 |
| 做了什么 | ① 新增 `frontend/src/components/StageAddCard.tsx`：右侧中等卡片（`fixed right-6 top-28 z-40`、宽 400px、高 `max-h-[72vh]`、毛玻璃底 0.98→0.94 + `backdrop-blur-2xl`）—— 顶部是阶段名 + 「节点 N 个 · 模板 M 块」，下面是**标签导航**：第一个固定「任务节点」（= 这个阶段的节点池，该阶段全部预设节点按出现顺序去重、与任务模板页左列同口径，硬件实施 26 个），其余每个标签 = 这个阶段的一块模板（硬件实施「模板一 / 模板二」、软件部署一块…）；列表区 `flex-1 min-h-0 overflow-y-auto`（**鼠标滚动看全**，模板标签里带序号、按预设顺序预览）；每条节点一行（中英对照 + 右侧「＋ 添加 / 已添加」），点一条就加进项目；模板标签另有「整套添加（N）」一次补齐没加过的节点；关卡片 = 右上 × / `Esc` / **点卡片外的空白处** / **再点同一个阶段标签**（表内空白、汇总卡、页面其它地方都能关，卡片内点击不关；阶段标签除外，由标签自身开关）。② `frontend/src/components/TaskBoard.tsx`：阶段分组头由整行 `<button>` 改为 `<div role="button">`（整行 / 左侧箭头仍折叠展开），**阶段标签（白底 pill）本身是按钮** —— 点一下把右侧卡片切到该阶段、**再点同一个标签 = 关掉卡片**（标签带 `data-stage-pill`，`aria-expanded` 反映开关态；打开中的标签描品牌黄圈、并加「侧栏卡片」小图标提示）；卡片渲染在表格末尾；新增 `viewStage` prop：切顶部阶段标签时自动关掉卡片；把项目已有任务 id 集合传给卡片判重。③ `frontend/src/ProjectDetail.tsx`：任务数据改为「示例 + 本次添加」（`addedTasks` state 叠在 `tasksForProject()` 上），`handleAddNode` 按节点 id 判重后追加 —— 新任务阶段 = 模板所属阶段、默认 `待开始` / 进度 0 / 紧急重要度「中」，负责人 / 起止日期 / 成果文件 / 人数留空待补；换项目时清空本次添加；两个 `TaskBoard` 都传 `viewStage`。（本 PR 内先做的表头 popover 版 `AddTaskCard.tsx` 已废弃并删除。）④ **阶段骨架常显**：`ProjectDetail.tsx` 去掉 `emptySkeleton` 判断、两个 `TaskBoard` 固定传 `skeletonStages`（总览 = 9 个阶段、单阶段 = 当前阶段），`visibleStageNames` 同步改成全部可见阶段 —— 修掉「空项目里加出任务后，其余阶段的分组头消失」。⑤ 项目经理 / 紧急重要度口径：任务表「项目经理」列与任务详情抽屉由固定常量改为**取项目卡片上的经理**（`ProjectDetail` 用 `managerName(project.managerId)` 解析后传给 `TaskBoard` → `TaskRow` / `TaskDrawer`，`PROJECT_MANAGER` 仅作兜底）；新增任务「紧急重要度」默认「中」（`taskFromPresetNode` 口径不变）。 |
| 新增功能 | 点任务表的阶段标签 → 右侧卡片：标签导航切换「任务节点」与各模板；任务节点页逐条添加、模板页预览（滚动）并支持整套添加；已在项目里的节点判重显示「已添加」；空项目也能借它加出任务，阶段分组与汇总卡随之更新。 |
| 修复 / 重构 / 优化 | 1）修掉卡片长列表下把标签导航挤成 14px 的布局 bug（flex 列缺 `min-h-0`、列表把兄弟节点挤扁 —— 已补 `min-h-0` 与 `shrink-0`）；2）卡片底改为更实的毛玻璃（0.98→0.94）保证叠在表格上文字可读；3）修掉「用卡片加出任务后，其余阶段的分组头消失」：阶段骨架改为常显；4）**文案口径校准**：卡片脚注 / 任务模板页保存提示 / 文首现状 / §一.6 / §一.7.2 / `字段对照清单.md` §四 与 Push 59 / 60 / 61 日志条目里的「一期只存在浏览器内存」类表述统一改为「当前原型未接后端（临时内存态）；一期正式交付由后端落库」，避免误读为「一期不做落库」，代码注释同步。 |
| 测试与验证 | 1）`frontend/` 下 `npm run typecheck` 通过；2）`npm run build` 通过（vite v8.3.0，45 modules transformed；含「点卡片外空白关闭 / 再点标签关闭 / 原型口径校准」的最终产物 `dist/assets/index-CwH6mFau.js` 329.88 kB、CSS 56.82 kB）；3）无头 Edge 实测（印度项目 `inmu-0010`）：初始无卡片；点阶段标签「硬件实施」→ 卡片标签 = 任务节点 26 / 硬件实施模板一 18 / 硬件实施模板二 11（列表可滚动）；任务节点页已添加 18（= 该项目硬件实施已有任务），点「桌面平台搭建、魔毯铺设」→ 已添加 19、硬件实施分组 18 → 19 行；切「硬件实施模板二」标签 → 11 条带序号预览、已添加 4 →「整套添加（7）」→ 已添加 11、硬件实施 26 行、汇总 16/44 → 16/52；点阶段标签「软件部署」→ 卡片切成 任务节点 4 / 软件部署模板 4；`Esc` 关卡片；点整行仍是折叠（验收组 `aria-expanded=false`）；重开卡片后切顶部阶段标签 → 卡片自动关掉；4）空项目 `usca-0002` 回归：初始 9 个空阶段 → 整套添加「硬件实施模板一」18 条 → 仍是 9 个分组头（硬件实施 18 行、其余 0 行）、汇总「当前阶段 硬件实施 · 已完成 0/18」；印度项目 9 组 / 16/44 不变、单阶段「试运行」= 1 组 2 行；5）项目经理 / 优先度实测：印度项目任务表「项目经理」列 = 冯雪（= 项目卡片经理）、新加「桌面平台搭建、魔毯铺设」行 = 冯雪 / 中 / 待开始、任务抽屉「项目经理」= 冯雪；美国项目（空项目加 18 条）= 王芳 / 中；6）截图留档 `skeleton-stays.png` / `manager-priority.png` / `drawer-manager.png` / `stagecard-full.png` / `stagecard-nodes.png` / `stagecard-template.png` / `stagecard-el-template.png` / `close-outside-header.png` / `toggle-close-same-pill.png`；7）关卡片复验（无头 Edge，印度项目）：点阶段标签开卡片 → 点表内表头空白 → 卡片关闭；重开 → 点汇总卡「当前阶段」→ 关闭；重开 → 点卡片内标题 → 保持打开；卡片内点「添加」→ 保持打开且任务确实加上（硬件实施 19 → 20 行）；卡片开着点另一阶段标签「软件部署」→ 卡片切到新阶段（标题 = 软件部署）；`Esc` / 右上 × → 关闭；8）标签开关复验（无头 Edge）：点阶段标签开卡片（`aria-expanded=true`）→ **再点同一个标签 → 卡片关闭**（`false`）→ 点第三下重新打开；卡片开着点另一标签 → 切到新阶段且不闪关（两个标签 `aria-expanded` 正确互换）；再点该标签 → 关闭；点表头空白 / `Esc` / × 仍照常关闭；卡片内点击仍不关；点阶段分组整行仍是折叠展开（切换正常）。 |
| 文档同步 | `前端功能需求.md`（文首现状、§一.6 新增「添加任务」条目、§一.7.2 任务模板条目、§三 需求登记表 #19 / #21、§3.8 新增 A11（模板接口 / 实例化对齐）、变更记录 Push 61）、`字段对照清单.md`（§四草稿字段 + 口径补充：新任务字段默认值、「当前原型内存态 / 一期正式交付落库」口径校准）、本条目；`系统功能书.md` 未改（任务创建口径 Q8 已定，写入接口 = A10 待 wmj）、`shared/` 契约零变更。 |
| 风险与后续 | 1）写入接口 = §3.8 A10（任务创建 / 编辑，契约待 wmj 决议）；2）新任务字段（负责人 / 日期 / 成果文件 / 人数 / 紧急重要度）当前原型没有编辑入口、先留空，后续接任务编辑；3）当前原型添加的任务只存在浏览器内存，刷新 / 换项目即丢 —— **一期正式交付由后端落库**（写入接口 = A10），接后端后该临时状态消失；4）卡片当前不记上次标签；判重语义与任务模板页一致（同一节点在项目里只留一份）。 |
| 备注 | 与 Push 59 / 60 同批推送，合并成一个 PR。 |
## 2026-09-19 15:04 +08:00 | px | px | Push 60

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（任务模板：业务写死的九阶段默认模板预设，硬件实施两套） |
| 分支 | `px` |
| PR / commit | PR #49（`px` → `main`，squash-only；与 Push 59 / 61 同一个 PR）。 |
| 做了什么 | ① 新增 `frontend/src/data/templatePresets.ts`：`STAGE_TEMPLATE_PRESETS`（阶段 → 该阶段默认模板数组，每套 = 名称 + 节点顺序）——**硬件实施两套**：模板一 =「项目总览」硬件实施的 18 条长清单（`t11`~`t19`、`t21`~`t25`、`t28`、`t29`、`t31`、`t32`）；模板二 = 业务给的格口 / 滑槽型 11 条（前 3 条复用 `t11`~`t13`，新增 `hx01`~`hx08`：桌面平台搭建、魔毯铺设 / 站人台、扫描架、称及附属硬件安装 / 格口滑槽（或挂包架）安装 / 传感器、按钮盒安装理线 / 强弱电布线 / 扫码台及机柜理线 / 防护围栏安装 / 验收交付）；**软件部署一套 4 条**（`sw01` 通电测试、参数设定 / `sw02` RCS、WES 软件部署调试，随机跑 / `sw03` 按钮盒注册、传感器调节 / `sw04` 与 WMS 联调）；其余七个阶段各一套（节点顺序沿用「项目总览」对应阶段）。② `frontend/src/PlaceholderPage.tsx`：左列「任务节点」池由「项目总览任务」改为 `presetNodesOf(stage)` —— 取该阶段全部预设节点、按出现顺序去重（硬件实施因此 18 + 8 = 26 个节点）；右侧面板初始由「各阶段一张空白模板」改为按预设预填（`initialTemplatesByStage()`：名称 / 节点顺序 / 初始即「已保存」；九个阶段共 10 块面板 —— 硬件实施 2 块、其余 8 个阶段各 1 块；`nextTemplateSeq` 改从 `INITIAL_TEMPLATE_COUNT`（10）起算，「＋ 新建模板」序号从 11 起、不再撞 id）；`PAGES.templates.note` 同步改为预设口径。③ 左列「任务节点」列头下加**搜索框**（`aria-label="搜索任务节点"`，placeholder「搜索节点（中 / 英）」）：按中 / 英文名过滤、不区分大小写，有过滤时列表头显示「{板块} · N / M 个节点」，没有匹配时列表内显示「没有匹配的节点，换个关键词试试」，框内右侧 × 一键清空；切换板块时搜索词自动清空。 |
| 新增功能 | 任务模板默认预设（业务写死）：九个阶段各一套（硬件实施两套），节点池与面板初始内容都来自预设；左列节点池新增搜索框（中 / 英过滤、切板块清空）；新建 / 改名 / 删除 / 拖拽 / 排序等既有交互不变。 |
| 修复 / 重构 / 优化 | 1）修掉「所有阶段初始都是空白模板」：打开即带业务给的内容，硬件实施模板二与软件部署的节点也进了左列节点池；2）节点池与模板内容改为单一数据源（`templatePresets.ts`），后续换后端只改这一个文件。 |
| 测试与验证 | 1）`frontend/` 下 `npm run typecheck` 通过；2）`npm run build` 通过（vite v8.3.0，44 modules transformed；`dist/assets/index-DquHuRxa.js` 323.50 kB、CSS 55.47 kB）；3）无头 Edge 实测：售前规划 = 4 节点 / 1 块面板 4 条；硬件实施 = 26 节点 / 模板一 18 条 + 模板二 11 条；软件部署 = 4 节点 / 1 块面板 4 条；验收 = 1 节点 / 1 条 —— 各面板初始均「已保存」；4）搜索框实测：「货架」→ 3 / 26（货架组装 / 货架检查 / 货架条码黏贴）、「shelf」→ 同上 3 条（不区分大小写）、「zzz」→ 0 / 26 + 「没有匹配的节点」提示、× 清空后恢复 26 条、切到软件部署再切回 = 搜索词已清空；5）截图留档 `preset-hardware.png` / `preset-software.png` / `search-cn.png` / `search-empty.png`。 |
| 文档同步 | `前端功能需求.md`（§一.7.2 预设口径、§二.2、§三 需求登记表 #19、变更记录 Push 60）、`字段对照清单.md`（文首口径 + §四「模板预设为初始来源」）、本条目；`系统功能书.md` 未改（节点清单与模板命名待业务定稿）、`shared/` 契约零变更。 |
| 风险与后续 | 1）`hx01`~`hx08`、`sw01`~`sw04` 的**英文名是按业务截图可见部分补全的，待业务核对**（代码注释已标「待核对」）；2）硬件实施两套 = 18 条 / 11 条已与业务确认（2026-09-19）；3）软件部署第二套模板业务尚未提供；4）节点与模板仍是前端写死示例，正式来源 / 落库 / 契约待后端（见 `字段对照清单.md` §四 接口需求）。 |
| 备注 | 与 Push 59 同批推送，合并成一个 PR；合并以主干 squash commit 为准。 |

## 2026-09-19 14:27 +08:00 | px | px | Push 59

| 字段 | 内容 |
|---|---|
| 类型 | `fix(frontend)`（项目详情的示例任务数据收口到印度项目，其余项目只出页面骨架） |
| 分支 | `px` |
| PR / commit | PR #49（`px` → `main`，squash-only；与 Push 60 / 61 同一个 PR）。 |
| 做了什么 | ① `frontend/src/data/tasks.ts` 新增 `DEMO_TASKS_PROJECT_ID = "inmu-0010"`（印度包裹分拣）与 `tasksForProject(projectId)`：原型阶段只有该项目返回 44 条示例任务（正式版按项目取数），其余项目返回空列表。② `frontend/src/ProjectDetail.tsx` 改为按项目取数（原写死 `PROJECT_TASKS`，导致 20 个项目打开都是同一份数据）；没有任务的项目传 `skeletonStages`（「项目总览」= 9 个阶段、单阶段标签 = 该阶段），并把阶段名常量抽为 `STAGE_NAMES`。③ `frontend/src/components/TaskBoard.tsx`：新增 `skeletonStages?: readonly string[]` —— 这些阶段即使没有任务也渲染分组头（`0/0 完成`，展开后没有任务行）；`ProjectSummary` 在没有任务时「当前阶段」显示「—」（原来会显示「全部完成」，空项目会误读）；空阶段的分组头只显示阶段名、空项目的汇总卡不显示「0/0 完成」这种没有意义的计数（只留「整体进度 0%」）。 |
| 新增功能 | 无（数据归属与空态修正）。 |
| 修复 / 重构 / 优化 | 1）修掉「所有项目打开都是同一份任务数据」：改为按项目 id 取数，示例数据只归印度项目；2）其余项目页面不再空得没结构：阶段标签导航 + 9 个阶段分组头照出（只显示阶段名）、没有任务行；3）空态口径修正：汇总卡不显示「全部完成」改显示「—」，分组头与汇总卡都不再出现没有意义的「0/0 完成」计数；4）完成数文案统一改为「已完成 N/M」（原「N/M 完成」在 3/6、16/44 这类部分完成的情况下容易被读成「这一阶段 / 项目已完成」）。 |
| 测试与验证 | 1）`frontend/` 下 `npm run typecheck` 通过；2）`npm run build` 通过；3）无头 Edge 实测：印度项目 `#/project/inmu-0010` = 9 个阶段头（已完成 4/4、5/5、3/3、3/6、1/18、0/4、0/2、0/1、0/1）+ 44 行任务、汇总卡「当前阶段 组装发货 · 36% · 已完成 16/44」（数据与改动前一致，只是文案改为「已完成 N/M」）；美国项目 `#/project/usca-0002` = 9 个阶段头只显示阶段名（不显示计数）、0 行任务、汇总卡「当前阶段 — · 整体进度 0%」，点「硬件实施」标签只出该阶段分组头且 0 行；4）截图留档 `proj-india-tasks.png` / `proj-empty-skeleton.png` / `proj-empty-stage.png`。 |
| 文档同步 | `前端功能需求.md`（文首现状、§一.6 新增「数据归属」、§6.2 当前阶段、§6.3 阶段分组任务表、§二.2 演示数据、§三 3.1 #12、§3.5、变更记录 Push 59）、`字段对照清单.md`（文首新增「数据归属」一条）、本条目；`系统功能书.md` 未改（字段与业务口径未动）、`技术设计v0.1-选型分析.md` 未改（§1.5「数据样本：源表 1 个项目」本来就写明只有 1 个项目有样本）、`shared/` 契约零变更。 |
| 风险与后续 | 1）「哪个项目带示例数据」目前是前端常量，接后端后随 `GET /api/v1/projects/{id}/tasks` 自然消失；2）其余项目现在没有任何任务入口（新建任务功能未做），后续做任务创建时注意它们从空开始；3）空项目的阶段骨架（`skeletonStages`）是本期演示口径，若业务希望空项目直接出「暂无任务」空态，改一处即可。 |
| 备注 | 需求口径来自业务：数据模板只保留在印度项目，其它项目只出阶段名称、没有具体任务数据。 |

## 2026-09-19 13:52 +08:00 | px | px | Push 58

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（任务模板页第一版：阶段板块标签栏 + 任务节点卡片区 + 毛玻璃模板面板拖拽编排） |
| 分支 | `px`（已推送） |
| PR / commit | PR #48（`px` → `main`，squash-only）。 |
| 做了什么 | ① 新增 `frontend/src/components/TaskNodeCard.tsx`：按业务参考稿用 Tailwind 复刻任务节点卡片（330×80、旋转波浪、圆形图标、中英主副标题、叉形装饰 —— 默认隐藏、仅悬停叉号 28×28 命中区才显示；未引入 styled-components）。② `frontend/src/PlaceholderPage.tsx`：任务模板页由整页占位改为 —— 九阶段板块标签栏（`TEMPLATE_SECTIONS` = `PROJECT_STAGES` 去掉「项目总览」，样式复用项目详情阶段标签栏，行最右为「＋ 新建模板」）；左侧「任务节点」列（一期取 `data/tasks.ts` 的 `PROJECT_TASKS`，按板块过滤、中英对照，列表头「{板块} · N 个节点」）；右侧若干块**毛玻璃模板面板**（列头可改名、默认「{阶段}模板」，含「保存 / 已保存」「删除」按钮、固定高度提示行、半透明白列体）。③ 布局：左列 370px 固定（宽屏滚动时钉在顶栏下方 `lg:sticky lg:top-[81px]`）、右侧面板一行放不下换行且同行等高（窄屏一列往下排）；面板背后垫一层纯装饰四色色雾（`sticky top-0 h-[70vh]` + `blur-3xl`，`pointer-events-none`）保证玻璃有可透背景。④ 拖拽：左侧卡片拖入面板 = 复制一份（原生 HTML5 拖放、`dataTransfer` 只带任务 id、左侧原节点不变）、**同一面板内按节点 id 去重**（重复拖入面板转琥珀提示态）、右列卡片悬停叉号可移除、面板内拖动排序（插入线绝对定位不占位 + 落点按卡片 `getBoundingClientRect` 几何位置判定）。⑤ **模板按阶段分开存**（`templatesByStage`）：每个阶段自己一套面板（名称 / 节点 / 保存状态），新建 / 改名 / 删除 / 拖拽只作用当前阶段。⑥ 板块进 URL：`#/templates?section=设计开发`（`replaceTemplateSection` 走 `history.replaceState`、不产生历史条目；非法板块名回落第一个板块并纠正地址）。⑦ `frontend/src/useHashRoute.ts` 增加 `section` 解析与 `templateSectionHref` / `replaceTemplateSection`；`App.tsx` 把 `route.section` 传给占位页。 |
| 新增功能 | 任务模板页第一版：阶段板块标签栏（九个阶段）、任务节点卡片区（按板块过滤、中英对照）、多块模板面板（新建 / 改名 / 保存状态 / 删除）、拖拽复制 + 去重 + 移除、面板内拖动排序、板块走 URL（`?section=`）。 |
| 修复 / 重构 / 优化 | 1）「拖到两张卡片之间频闪」：排序落点改为按卡片几何位置判定 + 插入线不占位（`pointer-events-none absolute`）；2）叉号显示口径修正：只有鼠标移到叉号自身命中区才显示（原来整卡悬停就显示）；3）左列「任务节点」宽屏滚动时钉住，长列表上下滚动不再带着左列跑；4）模板状态由「单块共享面板」改为按阶段分组，修掉「切标签模板不跟着换」。 |
| 测试与验证 | 1）`frontend/` 下 `npm run typecheck` 通过（tsc 两个工程无错误输出）；2）`npm run build` 通过（vite v8.3.0，43 modules transformed；`dist/assets/index-C13W-Kih.css` 54.48 kB、`dist/assets/index-nbpe1BvF.js` 319.24 kB）；3）`npm run smoke` 回归通过（本地 Casdoor 沙箱 + 3000 dev server，0~8 共 9 组、31 项断言全 PASS，末行「全部通过：SSO 链路完整可用」）；4）无头 Edge 实测：板块切换 / 深链接 / 非法值纠正 / `history.length` 不变；拖入 + 去重 + 移除；面板内 6 个落点排序无频闪（卡片位置恒定）；多面板新建聚焦 + 保存 / 已保存 + 删除；**按阶段分开**（设计开发改名 + 拖 2 节点 + 新建「机械图纸模板」，切回售前规划仍是默认空白、再切回设计开发改动仍在；售前规划删面板不影响设计开发）；窄屏 1440 / 900 / 700 / 420 均无横向滚动；钉住列滚动 400px 后位置不变；5）截图留档（`stage-diff-presale.png` / `stage-diff-design.png` / `wrap-layout.png` / `reorder-dragging.png` 等）。 |
| 文档同步 | `前端功能需求.md`（文首现状、§一.7.2 全节、§7.3 路由表 `#/templates`（可带 `?section=`）、§三 需求登记表 #19、变更记录 Push 58；并修正 3 处「默认通用模板」旧口径 → 「默认 {阶段}模板」）、`字段对照清单.md`（新增 §四「任务模板草稿字段」+ 文首口径补充，原 §四 / §五顺延为 §五 / §六）、`技术设计v0.1-选型分析.md`（§1.5 现状快照 + 修订记录 v0.1.10）、本条目；`系统功能书.md` 未改（任务模板的业务条目待业务定稿）、`shared/` 契约零变更、`README.md` 未改（`frontend/` 目录说明已覆盖）。 |
| 风险与后续 | 1）模板当前是**原型内存草稿**：刷新 / 重新登录即回到「各阶段一张空白 {阶段}模板」；**一期正式交付由后端落库**（模板接口待 wmj 决议），原型阶段是否先落 localStorage 待业务定；2）面板删除即时生效、无二次确认（待定）；3）跨面板移动节点未做（排序只在同一面板内）；4）模板节点一期直接复用「项目总览」任务数据，正式节点来源与模板落库 / 勾选口径待业务定稿（接口需求已列 `字段对照清单.md` §四，待 wmj 决议契约）；5）色雾底衬仅本页，若后续统一页面背景再抽公共层。 |
| 备注 | 与 Push 57 同为 `px` 分支连续推送；合并以主干 squash commit 为准。 |

## 2026-09-18 23:22 +08:00 | px | px | Push 57

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（登录后入口页 + 路由分层 + 顶栏层级导航） |
| 分支 | `px`（已推送） |
| PR / commit | PR #47（`px` → `main`，squash-only；与 Push 56 同属本 PR）。 |
| 做了什么 | ① 新增登录后入口页（`#/`）：页面正中三个入口按钮「项目空间 / 任务模板 / 文件库」，实现 `frontend/src/Hub.tsx` + `frontend/src/components/HubButton.tsx`（按业务提供的参考稿用 Tailwind 复刻：176×56 胶囊、2px 外扩渐变描边 `135deg #30cfd0→#330867`、浅灰渐变填充、柔和投影，悬停填充同款渐变 + 文字转白并放大到 1.3rem；未引入 styled-components 或新依赖）。② 路由分层（`frontend/src/useHashRoute.ts`）：新增 `hub` / `placeholder` 两类路由与 `HUB_HASH`；列表地址由 `#/` 移到 `#/projects`（筛选参数口径不变）、新增 `#/templates` / `#/files` 占位页（`frontend/src/PlaceholderPage.tsx`）；`projectsHref()` 让「项目空间」按钮沿用本地筛选记忆。③ 顶栏（`frontend/src/components/AppHeader.tsx`）：Logo 改为「返回上一层」（`upLevelHref()` / `goUpLevel()`）——详情 → 列表（保留进入前筛选态、优先浏览器后退）、列表 / 占位页 → 入口页、入口页不动作；面包屑「项目空间」行为不变；`ProjectDetail.tsx` 空态链接由 `#/` 改为 `#/projects`。④ 文档同步见下。 |
| 新增功能 | 登录后入口页；任务模板 / 文件库占位页；三层路由与「返回上一层」导航。 |
| 修复 / 重构 / 优化 | 原「顶栏 Logo 回项目空间」改为按层级返回，补齐入口页 → 项目空间 → 项目详情的层级导航；入口按钮按参考稿复刻（描边外扩、135° 精确渐变、投影）。 |
| 测试与验证 | 1）`frontend/` 下 `npm run typecheck` 通过；2）`npm run build` 通过（vite v8.3.0，42 modules transformed；`dist/assets/index-CCHhEysZ.css` 46.07 kB、`dist/assets/index-swvl_h_j.js` 305.56 kB）；3）`npm run smoke` 回归通过（本地 Casdoor 沙箱 + 3000 dev server，0~8 共 9 组、31 项断言全 PASS，登录链路未受影响）；4）界面实测：无头 Edge 渲染入口页与占位页截图核对（三按钮居中、描边 / 渐变 / 投影与参考稿一致，临时预览入口用后已删）；5）导航实测：临时探针逐条切换真实 hash 读取 `upLevelHref()`——`#/`→`#/`、`#/projects?q=abc`→`#/`、详情（自列表进入）→`#/projects?q=abc`、`#/templates`→`#/`。 |
| 文档同步 | `前端功能需求.md`（文首现状、§一.1 登录落点、§一.2 顶栏、§一.3 URL 口径、新增 §一.7 入口页与路由表、§三 3.1 登记表 #18~#20、变更记录 Push 57）、`README.md`（`frontend/` 目录说明）、`技术设计v0.1-选型分析.md`（§1.5 现状快照 + 修订记录 v0.1.9）、本条目；`系统功能书.md` 未改（入口页是导航结构、没有对应业务功能点；业务若要立条目再补）、`字段对照清单.md` 未改（无字段变化）、`shared/` 契约零变更。 |
| 风险与后续 | 1）`#/` 由列表改为入口页：旧书签 `#/?filter[...]` 不再解析筛选（等价于进入入口页）；2）任务模板 / 文件库为占位页，内容与接口待业务定稿；3）入口页按钮后续还会增删（业务未定），样式集中在 `HubButton.tsx` 便于统一调整；4）入口页不涉及任何业务接口。 |
| 备注 | 已随 PR #47 推送；合并以主干 squash commit 为准。 |

## 2026-09-18 22:27 +08:00 | px | px | Push 56

| 字段 | 内容 |
|---|---|
| 类型 | `style(frontend)`（项目详情汇总卡信息收敛） |
| 分支 | `px`（已推送） |
| PR / commit | PR #47（`px` → `main`，squash-only；与 Push 57 同属本 PR）。 |
| 做了什么 | ① `frontend/src/components/TaskBoard.tsx` 的 `ProjectSummary`：移除「逾期 N 条」计数块（本地派生 `tasks.filter(isTaskOverdue)` 与渲染一并删除，避免 `noUnusedLocals` 报错）；「当前阶段」后的阶段名由 `font-medium` 改 `font-bold`。② 文档同步：`前端功能需求.md` §6.2（逾期条目改为「（一期暂不展示）逾期计数」、当前阶段补「阶段名加粗展示」、补「汇总栏为可扩展区域」说明）、§三 需求登记表 #11（改为「总览汇总卡：当前阶段 / 整体进度（完成 / 总数）；逾期计数一期不展示（契约字段保留）」）、§3.4（`overdue` 一期服务端照常下发、汇总卡不展示）、变更记录追加本行。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 汇总卡信息收敛：一期只展示「当前阶段 + 整体进度」，暂不展示逾期计数；契约 `overdue` 字段保留（`shared` 零变更），后续汇总栏扩展再回填。 |
| 测试与验证 | 1）`frontend/` 下 `npm run typecheck` 通过（tsc 两个工程无错误输出）；2）`npm run build` 通过（vite v8.3.0，39 modules transformed；`dist/assets/index-DKl1oqsQ.css` 44.10 kB、`dist/assets/index-JmLMjafg.js` 302.85 kB）；3）`npm run smoke` 通过（本地 Casdoor 沙箱 + 3000 dev server，0~8 共 9 组、31 项断言全 PASS，无 FAIL，末行「全部通过：SSO 链路完整可用」）。 |
| 文档同步 | `前端功能需求.md`（§6.2 / §三 #11 / §3.4 / 变更记录）+ 本条目；`系统功能书.md`、`字段对照清单.md` 未改（任务级「逾期」口径未动）。 |
| 风险与后续 | 1）`overdue` 一期无前端消费方，接线时注意保留字段、勿当死字段清理；2）汇总栏后续要加的信息待业务定稿（逾期计数为候选之一）；3）详情页行内「逾期」标记与抽屉逾期提示本次未动。 |

## 2026-09-18 17:50 +08:00 | px | px | Push 55

| 字段 | 内容 |
|---|---|
| 类型 | `docs(frontend)`（Q8 定案：施工人数 / 紧急重要度 / 文件来源口径落地） |
| 分支 | `px` |
| PR / commit | 随 PR #46（`px` → `main`）提交；未直接推送 `main`。 |
| 做了什么 | ① `前端功能需求.md` 第六章 Q8 定案：施工人数 / 紧急重要度任务创建 / 编辑均可填、非必填（权限控制暂缓，随权限矩阵落地）；文件类型不限、走站内文件库 + 站内预览（不通过第三方应用打开），任务行「文件」读文件库 `filter[taskId]` 关联（列表随行 `files` 摘要见 A7）；② 任务字段模型（`headcount` / `priority` / `files` 行）与 §四.2 校验表同步口径；③ §3.8 新增 A10：任务创建 / 编辑接口未入契约（任务域现仅 `GET /tasks` + `PATCH .../progress`，`Task` 已有可空 `headcount` / `priority` 但无写入通道）——待 wmj 决议，与 Q6「任务数据来源」联动；④ `字段对照清单.md`（口径补充 + 三行 + 空置列小结）同步；⑤ 变更记录 + 本条目。 |
| 新增功能 | 无（纯文档；为 M2 任务创建 / 编辑接线与文件库 UI 提供口径前提）。 |
| 修复 / 重构 / 优化 | 无（口径定案与文档回写）。 |
| 测试与验证 | 纯文档改动，按仓库约定免 `typecheck` / `build`；`rg` 复核文档 / 字段对照清单口径一致、Q8 已标记定案。 |
| 文档同步 | `前端功能需求.md`（§一.5 字段模型 / §四.2 / §3.8 A10 / 第六章 Q8 / 变更记录）、`字段对照清单.md`、本条目。 |
| 风险与后续 | 1）A10 为契约缺口：任务创建 / 编辑写入接口需 wmj 决议（精确字段集以任务创建 / 编辑 UI 定稿为准，与 Q6 联动）；2）文件站内预览依赖 i3 预览切片（lan）；3）权限控制明确暂缓，随权限矩阵落地。 |
| 备注 | 推送 `px`；未直接推送 `main`。Push 54 已先合入主干（#45 / c2db858），本条为独立 PR #46；本推送另含常驻分支同步（merge `main` c2db858；`前端功能需求.md` / `开发日志.md` 冲突按本线超集解决）。 |

## 2026-09-18 17:42 +08:00 | px | px | Push 54

| 字段 | 内容 |
|---|---|
| 类型 | `docs(frontend)`（详情页顶栏面包屑补「序号」段：回写文档与既有实现对齐） |
| 分支 | `px` |
| PR / commit | 随本 PR（`px` → `main`）提交；未直接推送 `main`。 |
| 做了什么 | ① `前端功能需求.md` §一.2 全局顶栏：详情页面包屑由两段「`项目空间 / 项目编号`」更正为三段「`项目空间 / 序号 NN / 项目编号`」（序号段取 `seqNo` 两位补零、≥100 原样）；② `前端功能需求.md` 变更记录追加本行；③ 本条目。核对实现：`frontend/src/components/AppHeader.tsx` L32 `seqNoText` 两位补零、L64-79 渲染「项目空间 / 序号 {seqNoText} / {project.code}」——与卡片徽标 / 搜索同口径；文档原缺「序号」段，属 Push 37 口径落地时的文档遗漏，本次仅回写文档。 |
| 新增功能 | 无（纯文档，代码零改动）。 |
| 修复 / 重构 / 优化 | 修复文档与实现不一致：详情页顶栏面包屑缺失「序号」段。 |
| 测试与验证 | 纯文档改动，按仓库约定免 `typecheck` / `build`；`rg` + 人工核对 `前端功能需求.md` §一.2 与 `AppHeader.tsx:64-79` 渲染口径一致。 |
| 文档同步 | `前端功能需求.md`（§一.2 + 变更记录）、本条目；`字段对照清单.md` 未改（无字段口径变化）。 |
| 风险与后续 | 无逻辑 / 接口影响；纯文档变更走常规评审路由。 |
| 备注 | 推送 `px`；未直接推送 `main`。本推送另含常驻分支同步（merge `main` 222c345：lan 的 S7·file 数据层与上传契约收口，Push 50~53）；Push 序号顺延至 54（主干已至 53）。 |

## 2026-09-18 17:26 +08:00 | lan | lan | Push 53

| 字段 | 内容 |
|---|---|
| 类型 | `docs(contracts)`（分支同步 + 契约同步收口：`UPLOAD_INCOMPLETE` 补登 技术设计v0.3 §4.8；`upload_parts` 不落表口径补齐 v0.3 §3.5 / v0.2 §11.1） |
| 分支 | `lan` |
| PR / commit | 随 PR #41（`lan` → `main`）提交；合并提交 `4a714b6`（同步 `b68dc73`：px Push 49 时间口径）；未直接推送 `main`。 |
| 做了什么 | ① 常驻分支同步：`git merge main` 纳入 `b68dc73`（px Push 49：展示「创建于」`createdAt`、排序 / 筛选 / 搜索取 `updatedAt`（最近活动）、详情页移除编辑入口、任务表一键收起）；② 契约补登（PR #41 评审结论「错误码契约对齐 v0.3 命名」收口）：`技术设计v0.3` §4.8 增补清单新增 `UPLOAD_INCOMPLETE`(409，上传会话分片未齐需补传)，补齐对齐后仍缺登记的最后一项；`技术设计v0.2` §11.4 引用清单同步；③ 「`upload_parts` 不落表」口径补齐到剩余两处表清单：`技术设计v0.3` §3.5「新增表」行改为不落表注记、`技术设计v0.2` §11.1 模块表清单移除 `upload_parts` 并列明唯一真相为对象存储 ListParts；④ 冲突解决：`开发日志.md`（保留双方全条目，lan 条目顺延 Push 50 / 51 / 52；修正顺延遗留引用 3 处）、`前端功能需求.md`（保留 px Push 49 行与 lan 契约同步行，lan 行顺延 Push 52 并补记 `UPLOAD_INCOMPLETE` 补登）；⑤ 本条目。 |
| 新增功能 | 无（文档 / 契约补登）。 |
| 修复 / 重构 / 优化 | 契约口径收口：`UPLOAD_INCOMPLETE`(409) 此前「契约保留但 v0.3 §4.8 未登记」、`upload_parts` 此前「评审定案不落表但两份设计文档仍列为新增表」；本次对齐后错误码四处清单（`shared` 契约 / v0.2 §7.2 / v0.3 §4.8 / `前端功能需求.md`）与 `upload_parts` 四处表述（`database/README` / `shared/README` / v0.3 §3.5 / v0.2 §11.1）完全一致。 |
| 测试与验证 | 1）`rg` 交叉核对：三码（`UPLOAD_INCOMPLETE` 409 / `UPLOAD_SESSION_EXPIRED` 410 / `FILE_HASH_MISMATCH` 422）在 `shared/src/common/errors.ts`、v0.2 §7.2 / §11.4、v0.3 §4.8、`前端功能需求.md`（§3.6 + 统一错误码表）一致；`upload_parts` 在文档中无残留表定义（全部为「不落表」表述）；2）`shared`：`npm run typecheck` → `npm run generate` → `npm run check`（零漂移，paths=35 / schemas=82）→ `npm run build`；3）`server`：`npm run typecheck` / `npm run test`（3 文件 16 例）/ `npm run check:boundaries`；4）`frontend`：`npm run typecheck` / `npm run build`（本次含 px 前端同步，一并复核）；5）CI：本次推送后 `database` / `frontend` / `shared` / `server` 全绿（见 PR #41 检查项）。 |
| 文档同步 | `技术设计v0.3-实施与验收.md` §4.8 / §3.5（跨线代录，请 wmj 复核）、`技术设计v0.2-架构与数据模型.md` §11.4 / §11.1、`开发日志.md`（本条目 + Push 52 条目风险项更新）、`前端功能需求.md`（冲突解决）；`字段对照清单.md` 未改（无字段口径变化）。 |
| 风险与后续 | 1）`技术设计v0.3` / `v0.2` 属 wmj 主责文档，本次为 lan 代录（评审结论收口），请 wmj 在 PR #41 复核措辞；2）顺带发现：v0.3 §3.5 接口表仍用旧路径命名（`/files/upload-sessions`），与已定档契约（`/files/uploads`、`/files/{id}/uploads/{uploadId}`）不一致，建议 wmj 复核是否对齐（本次未改）；3）PR #41 仍待非作者评审（迁移 + 对外契约属高风险变更，CONTRIBUTING §15）后合并；4）合并入 `main` 时 Push 编号如有重号，按「先入 main 者为准」顺延。 |
| 备注 | 推送 `lan`；未直接推送 `main`。本条收口 PR #41 评审结论两项（① `upload_parts` 不落表；② 错误码契约对齐 v0.3 命名）；PR #41 标题 / 描述同步更新至 Push 50~53。 |

## 2026-09-18 16:48 +08:00 | lan | lan | Push 52

| 字段 | 内容 |
|---|---|
| 类型 | `fix(contracts)`（PR #41 评审决定落地：上传错误码命名对齐 技术设计v0.3 §4.8 + `upload_parts` 定案登记） |
| 分支 | `lan` |
| PR / commit | 随 PR #41（`lan` → `main`，与 Push 50 / 51 同一 PR）提交；未直接推送 `main`。 |
| 做了什么 | ① `shared/src/common/errors.ts`：`UPLOAD_EXPIRED`(409) → `UPLOAD_SESSION_EXPIRED`(**410**)、`CHECKSUM_MISMATCH`(422) → `FILE_HASH_MISMATCH`(422)；`UPLOAD_INCOMPLETE`(409) 保留（v0.3 §4.8 未登记，语义独立：分片未齐需补传）；文件头注明增补清单出处。② `shared/src/openapi.ts`：`commonErrors` 增 410；`POST /files/{id}/uploads/{uploadId}/parts` 的 409 改 410；`/complete` 保留 409（分片未齐）并增 410（会话过期）。③ `shared/src/modules/files.ts`：完成上传描述同步新码名。④ 生成物重生成（openapi.json 133,557 bytes / api-types.d.ts 104,324 bytes；paths 35 / schemas 82 不变）。⑤ `shared/README.md` 上传口径行同步（分片未齐 409 / 会话过期 410 / 哈希不符 422），并把「分片状态以对象存储 ListParts 为唯一真相、**不落 `upload_parts` 表**」写入口径。⑥ `技术设计v0.2` §7.2：原 2 行拆为 3 行（`UPLOAD_INCOMPLETE` / `UPLOAD_SESSION_EXPIRED` / `FILE_HASH_MISMATCH`）并注明与 v0.3 §4.8 对齐。⑦ `database/README.md`：`upload_parts` 表述改为「不落表（评审已定案，2026-09-18）」。⑧ `前端功能需求.md`（跨线同步，px 主责）：§3.6 files 端点错误码与「统一错误码」表更新，变更记录加一行并注明「lan 代记，请 px 复核」。⑨ 本条目；Push 51 条目的「待 wmj 裁定」一栏改为「已定案、落地见 Push 52」。 |
| 新增功能 | 无（错误码更名与状态码对齐；契约能力集合不变）。 |
| 修复 / 重构 / 优化 | 消除 PR #41 评审提出的「契约与 技术设计v0.3 §4.8 命名不一致」：上传会话过期由 409 改为 **410**（与 v0.3 一致，语义更准确：资源已过期需重建）。 |
| 测试与验证 | 1）`shared`：`npm run typecheck` 通过 → `npm run generate` → `npm run check` 通过（paths=35 / schemas=82，两份生成物零漂移）；生成物抽查：`ErrorCode` 枚举 = `UPLOAD_INCOMPLETE` / `UPLOAD_SESSION_EXPIRED` / `FILE_HASH_MISMATCH`，parts 响应键 = 200/404/**410**、complete = 200/404/409/**410**/422；`rg` 复核旧码在 `shared/`（源码 + 生成物）零残留，仅历史日志与变更记录按原样保留旧名。2）`server`：`shared` 构建后 `npm run typecheck` / `npm run test`（3 文件 16 例）/ `npm run check:boundaries` 通过。3）`frontend`：`npm run typecheck` / `npm run build` 通过（本次为文档改动，无前端代码变化）。4）CI：本次推送后 `database` / `frontend` / `shared` / `server` 全绿（见 PR #41 检查项）。 |
| 文档同步 | `shared/README.md`、`技术设计v0.2-架构与数据模型.md` §7.2、`database/README.md`、`前端功能需求.md`（跨线代记）、本条目；`字段对照清单.md` 未改（无字段口径变化）。 |
| 风险与后续 | 1）错误码名称与 HTTP 状态属对外契约（高风险变更，CONTRIBUTING §15），随 PR #41 一并评审；2）`UPLOAD_INCOMPLETE`(409) 已在 v0.3 §4.8 补登（跨线代录，见 Push 53 条目，请 wmj 复核）；3）`前端功能需求.md` 由 lan 代记，请 px 复核措辞；4）实现侧（PR-4 起）按新码落错误响应：410 一律提示「重新发起上传」。 |
| 备注 | 推送 `lan`；未直接推送 `main`。本条落地 PR #41 评审决定：① `upload_parts` 不落表；② 错误码契约对齐 v0.3 命名。 |

## 2026-09-18 16:14 +08:00 | wmj | wmj | Push 49

| 字段 | 内容 |
|---|---|
| 类型 | `feat(contracts)`（M0-05 对齐清单 A1~A8 契约增补；决议见 PR #40 评审记录） |
| 分支 | `wmj` |
| PR / commit | 随本 PR（wmj → main）提交；未直接推送 `main`。 |
| 做了什么 | ① `shared/src/modules/projects.ts`：列表查询补 `filter[timeFrom]` / `filter[timeTo]`（DateOnly 闭区间，Asia/Shanghai 日界；一期映射 `updated_at`，A1）并具名 `ProjectListQuery`；新增 `ProjectDeleteHeadersSchema`（`If-Match` 防误删，A5）。② 新增 `shared/src/modules/users.ts`：`UserSummary` / `UserListQuery` / `UserListResponse`（A2，只返回启用用户、默认工号升序）与 `UserPreferences` / `UserPreferencesUpdateBody`（A4，PATCH 合并语义 + `updatedAt`）。③ 新增 `shared/src/modules/dicts.ts`：`DictType`（一期 region / projectType）/ `DictItem` / `Dict` / `DictListResponse`（A3，`projectType` 必含 `metadata.accent`）。④ `shared/src/modules/tasks.ts`：新增 `TaskFileSummary` / `TaskFileBrief` / `TaskListItem`（Task 去 `changeRef` + 内联 ownerName / changeSummary / fileSummary，A7）/ `TaskDetail`（全字段 + 文件清单），列表响应改 `TaskListItem`；新增 `TaskSortSchema` 字段白名单（A8）。⑤ `shared/src/openapi.ts`：注册 5 条新路径（`GET /projects/{id}/tasks/{taskId}`、`GET /users`、`GET / PATCH /users/me/preferences`、`GET /dicts`、`GET /dicts/{type}`）与 `DELETE /projects/{id}`；新增 `users` / `dicts` tag；进度更新响应改 `TaskListItem`。⑥ `shared/src/index.ts` 导出两个新模块。 |
| 新增功能 | 契约层新增 5 条路径、6 个操作：任务详情（抽屉按需请求）、用户目录、用户偏好读写、全量字典、单类型字典、项目软删；列表与 facets 共用一个查询 schema。生成物重跑：paths 35 → 40、schemas 82 → 95（新增 13 个具名 schema，无删除、无破坏性变更）。 |
| 修复 / 重构 / 优化 | ① 项目列表 `q` 描述补「序号」（与 M2-04 出口标准一致）；② 任务列表响应由全字段 `Task` 收敛为 `TaskListItem`，去掉列表用不上的 `changeRef`，避免前端逐行反查用户与文件（N+1）；③ 进度更新响应与列表项同形，前端可直接替换行。 |
| 测试与验证 | 1）`shared`：`tsc -p tsconfig.json`（typecheck）exit 0；`tsc -p tsconfig.build.json`（build，CI 同款）exit 0；2）`npm run generate` 重跑生成物（openapi.json 158,956 B / api-types.d.ts 121,977 B）；3）`npm run check`（漂移校验）通过：OpenAPI 3.1 结构校验 **paths=40、schemas=95**，两份生成物均与生成结果一致；4）生成物抽查：`DELETE /projects/{id}` 带 `If-Match@header` 且响应 200/400/404/409；`TaskDetail = Task & { ownerName, changeSummary, files }`；`TaskListItem` 25 属性；`UserPreferencesUpdateBody` 含 `additionalProperties`（前向兼容）；tags 含 users / dicts。 |
| 文档同步 | `shared/README.md`（目录树补 `users.ts` / `dicts.ts`；口径表补 A1~A8 七行；本批范围补 A1~A8 切片）、`技术设计v0.3-实施与验收.md`（§3.2 接口表与表清单、§3.3 列表一致性 / 软删、§3.4 任务接口与排序、新增 §7.1 决议记录；并修正 3 处与 0004 实测 DDL 不一致的描述）；并同步 px 在 Push 48 点名的跨线过期状态行：`技术设计v0.3` §4.9、`技术设计v0.1` §1.5 / §4.19 的 CI 现状（`database` job 已入）、`server/README.md` 数据库门禁行；本条目。 |
| 风险与后续 | 1）**A1 语义依赖 §7#4「项目时间」ADR**（`updated_at` 触发事件集），ADR 未定案前接口可先落、语义后对齐；2）**A3 依赖 §7#10**（业务给地区 / 项目类型清单与色值），未给前前端主题色只能继续用本地映射；3）**A2 依赖组织同步（D1-02）或 M0 种子** —— `users` 行目前只在首次 SSO 登录时 upsert，否则「前端移除项目经理硬编码」无数据可依；4）A6 的 UI 侧（侧栏一期不展示「阶段 / 状态」）待 px 拍；5）一期不新增 `tasks.seq`，A8 默认序改用 `created_at, id` 兜底；6）契约属高风险变更（CONTRIBUTING §15），需 lan / px 非作者评审。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。同批完成两条评审记录：#39（k6 会话切换，高风险）非作者评审 = 通过（含 3 条 M2-07 前置建议）；#40（M0-05 联调清单）A1~A8 逐条决议。`前端功能需求.md` §3.8 状态改「已定案」由 px 线更新。**Push 编号修正**：本条原记 Push 48，因 lan 线先推 Push 48（PR #41），本线顺延为 **Push 49**，相关文档已同步。 |

## 2026-09-18 16:13 +08:00 | px | px | Push 49

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（时间口径：展示「创建于」取创建时间、排序 / 搜索 / 「项目时间」筛选取最近活动 `updatedAt`；详情页移除编辑入口；任务表一键收起阶段） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① `Project` 模型补 `createdAt`（展示）并保留 `updatedAt`（排序 / 筛选 / 搜索；`frontend/src/types.ts`）；② 演示数据 20 条补 `updatedAt`（暂与 `createdAt` 同值；`frontend/src/data/projects.ts`）；③ 新建项目写入 `createdAt` + `updatedAt`（同值），编辑保存只刷新 `updatedAt`——`createdAt` 不随编辑变化（`App.tsx`）；④ 展示：卡片右下角时间（`Home.tsx` → `Card`）与详情顶栏由「更新于」改为「创建于 `createdAt`」（`AppHeader.tsx`）；⑤ 排序 / 搜索 / 「项目时间」区间筛选（`Home.tsx`）、侧栏最新日期（`CategoryFilterSidebar.tsx`）、URL `sort=updatedAt:asc\|desc`（`useHashRoute.ts`）用 `updatedAt`（项目时间 / 最近活动，定案见 §3.8 A1 / Q11）；工具条 aria-label / title 改「按项目时间…（最近活动在前 / 最早活动在前）」；侧栏「项目时间」标签加 ⓘ 口径说明（悬停 / 聚焦提示）；⑥ 移除详情页工具条「编辑项目」按钮：`ProjectDetail.tsx` 删除 `onEdit` 入参与按钮节点，`App.tsx` 不再向 `ProjectDetail` 传 `onEdit`，编辑仅保留卡片铅笔入口；⑦ 任务表新增「一键收起 / 全部展开」阶段折叠开关：`ProjectDetail.tsx` 持有 `collapsedStages` 状态，按钮渲染在表格表头「任务描述」列右侧（`TaskBoard.tsx` 表头按列 key 特判），`TaskBoard.tsx` 折叠态改由 props 受控（`collapsed` / `onToggleStage` / `onToggleAllStages`）；全部收起点按钮转为「全部展开」一键复位（按当前标签下可见阶段判定）；⑧ 详情页内容区上留白收窄：`ProjectDetail.tsx` 内容区 `pt-6` → `pt-3`，阶段标签栏整体上移。 |
| 新增功能 | 无（口径调整）。 |
| 修复 / 重构 / 优化 | 修复「编辑项目后卡片右下角 / 详情顶栏时间被刷成当前时刻」的问题：展示固定为创建时间（`createdAt`，编辑不刷新）；编辑保存改为刷新 `updatedAt`（项目时间 / 最近活动），驱动排序与筛选。 |
| 测试与验证 | 1）`frontend`：`npm run typecheck` 通过（client + node 两个 tsconfig）；2）`npm run build` 通过（vite 8.3.0，39 modules，303.07 kB / gzip 91.04 kB）；3）`rg` 复核：卡片 / 详情展示为 `createdAt`，排序 / 搜索 / 筛选 / URL 为 `updatedAt`；4）编辑项目后卡片右下角与详情顶栏时间保持不变（创建时间）；列表默认按项目时间（最近活动）倒序，编辑保存后该项目移到最前；5）详情页工具条不再渲染「编辑项目」按钮（编辑仅保留卡片铅笔入口）；6）任务表「一键收起 / 全部展开」：逻辑走查 + 类型检查 / 构建验证——点击后当前可见阶段全部置为收起态（仅剩阶段胶囊行），按钮随 `allCollapsed` 转为「全部展开」可一键复位；浏览器交互待人工复核。 |
| 文档同步 | `前端功能需求.md`（§一.2 / §一.3 / §一.5 / §一.6 / §二.1 / §三.1~3.4 / §3.8：A1~A8 状态改「已定案（PR #42）」/ §四.1 / §五 / 第六章：Q11 定案 + Q14 改写 / 变更记录）、本条目。 |
| 风险与后续 | 1）A1 / Q11 已定案（PR #42）：`filter[timeFrom]` / `filter[timeTo]` 一期映射 `updatedAt`、Asia/Shanghai 日界；排序走 `updatedAt`（v0.3 §3.3 白名单），无契约新增；2）A2 契约补充（`managerName` 随行下发）后 px 侧 M1 接线开始；3）「最近活动时间」是否上卡面为后续项，见 `前端功能需求.md` 第六章第 14 条；4）排序维度（创建时间 / 最近活动）为后续项，见第 15 条 / §3.8 A9（已提请 wmj 把 `createdAt` 列入排序白名单）；5）接口未接线前仍为内存态，刷新回默认。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-18 16:12 +08:00 | lan | lan | Push 51

| 字段 | 内容 |
|---|---|
| 类型 | `feat(db)`（S7·file 数据层落地：迁移 0005 / 0006 + Drizzle schema 同步） |
| 分支 | `lan` |
| PR / commit | 随本 PR（`lan` → `main`）提交；未直接推送 `main`。 |
| 做了什么 | ① 新增 `database/migrations/0005_file_lifecycle.sql`：`files` 增 `finalized_at` / `finalized_by` / `recycled_at` / `recycled_by` / `recycled_from_status` / `purge_after`（A4-12 回收站到期），加索引 `ix_files_purge` 与 3 条 CHECK（定档成对、回收站三列成对、`recycled_from_status` 不得取 `recycled`）；新建 `upload_sessions`（intent = version / change、status = active / completed / aborted / expired、目标对象键、multipart UploadId、分片大小与数量、size / content_hash / mime、`change_payload`、有效期与完成 / 中止时间，7 条 CHECK + 2 索引）。② 新增 `database/migrations/0006_idempotency_keys.sql`：`idempotency_keys`（`(actor_id, route, key_hash)` 唯一、`request_hash`、`status = in_progress / completed`、`response_status` / `response_body`、`expires_at`，5 条 CHECK + 1 索引）。③ `server/src/db/schema/files.ts` 同步 `files` 6 列 + 3 CHECK + `ix_files_purge` 并新增 `uploadSessions` 定义；`platform.ts` 新增 `idempotencyKeys` 定义。④ 文档：`database/README.md`（目录表、迁移命令期望输出、与 v0.2 的对应、验证段）、`server/README.md`（Drizzle 对齐段落改 0001~0006 + file 数据层口径）、本条目。 |
| 新增功能 | 数据层：文件定档 / 回收站 / 到期清理字段（契约 File 的 finalizedAt / finalizedBy / recycledAt / recycledBy / recycledFromStatus 落库）、分片直传会话（上传管道 PR-4 的存储基础）、写接口幂等表（`Idempotency-Key` 语义的存储基础）。 |
| 修复 / 重构 / 优化 | 无（纯新增：既有列与既有表未改语义）。 |
| 测试与验证 | 1）本地起 PostgreSQL 18.1（官方 Windows 二进制，临时目录）：跑 `roles/0001_roles.sql` 后，以 `libiaolink_migrator` 在空库执行全量迁移 —— 6 个迁移一次通过（0005 6 ms / 0006 4 ms），再次执行输出「数据库已是最新（已执行 6 个迁移，无漂移）」。2）`server` 构建后以应用角色 `libiaolink_api` 跑 `npm run check:db-schema`：通过（**14 张表 / 168 列 / 42 索引·唯一 / 42 CHECK 与 Drizzle schema 一致**）。3）约束探针（psql，14 条）：定档 / 回收站字段单向写入被拒、`recycled_from_status=recycled` 被拒、`intent=change` 缺 `change_payload` 被拒、`total_parts=0` 与 `expires_at<=created_at` 被拒、幂等 key 非 64 位十六进制 / completed 缺 `response_status` / 同 `(actor, route, key)` 重复均被拒；合法写入（含应用角色直接写两张新表）全部成功 —— 顺带验证 `roles/0001` 的 default privileges 对新表自动生效。4）迁移不可变红线：临时在 0005 追加一行注释后 `migrate --dry-run` 退出码 1（「已执行迁移内容被修改」），恢复文件后回到「无漂移」。5）`server` 门禁：`npm run typecheck` 通过 → `npm run test` 3 文件 / 16 例全过 → `npm run build`（nest build）通过 → `npm run check:boundaries` 通过（39 文件 / 100 条依赖 / 0 违规）。6）环境：临时 PostgreSQL 与二进制目录均在系统临时目录，不入库；`database/node_modules` 不入库。 |
| 文档同步 | `database/README.md`、`server/README.md`、本条目。契约未改（本 PR 不触碰 `shared/`）：契约字段已在 PR #37 定档，本次只是把 finalizedAt / finalizedBy / recycledAt / recycledBy / recycledFromStatus 落到列。 |
| 风险与后续 | 1）**迁移属高风险变更（CONTRIBUTING §15）**：需非作者评审（wmj / px）后再合并。2）**`upload_parts` 未落表**：分片状态以对象存储 ListParts 为唯一真相（避免双写漂移）；v0.2 §11.1 登记过该表，如需落表请在评审时定夺，后续以新迁移补。3）**错误码命名与 v0.3 §4.8 不一致**：评审已定案按 v0.3 对齐（`UPLOAD_SESSION_EXPIRED` 410 / `FILE_HASH_MISMATCH` 422，`UPLOAD_INCOMPLETE` 409 保留），落地见同一 PR 的 Push 52 条目。4）**CI 缺口**：`ci.yml` 目前不含迁移 / `check:db-schema`（v0.3 §4.9 建议的 `db` job 未落地），本次以本地 PG 18 验证替代；建议由 px 评估加 `postgres:18` service job（`.github/` 归 px）。5）`purge_after` 的到期清理任务、回收站列表 / 恢复接口、预览产物随后续切片。6）幂等表已落库，但「同一 Key 携带不同请求体」的拒绝码口径待与 wmj 对齐后实现。 |
| 备注 | 推送 `lan`；未直接推送 `main`。对应时序图任务 `i1 · S7·file` 的 PR-2（数据层），上游 PR #37（契约切片，Push 44）已合并；Push 编号按「先入 main 者为准」顺延为 51（主干已至 Push 49）。 |

## 2026-09-18 16:09 +08:00 | px | px | Push 48

| 字段 | 内容 |
|---|---|
| 类型 | `chore(ci)`（CI 扩展：`database` 门禁入 GitHub Actions——空库迁移 + Drizzle 结构漂移） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① `.github/workflows/ci.yml` 新增 `database` job（12 步）：`postgres:18` service（`POSTGRES_HOST_AUTH_METHOD=trust`、`POSTGRES_DB=libiaolink`、5432 端口 + `pg_isready` 健康检查）→ `shared` 安装 / 构建 → `server` 锁定安装 / `npm run build`（产出 `dist/db/schema`）→ `database` 锁定安装 → `apt postgresql-client` → `psql` 跑 `database/roles/0001_roles.sql`（幂等角色脚本）→ 空库迁移（`node scripts/migrate.mjs`，`libiaolink_migrator`）→ 迁移幂等复跑（校验 `verifyNoDrift`）→ `server` 的 `npm run check:db-schema`（以 `libiaolink_api` 连接，顺带校验角色脚本的默认授权）。② `CONTRIBUTING.md` 三处同步：第 5 节推送流程、附录 A 第 2 条平台保护（必需状态检查增补 `database`）、附录 A 第 4 条 CI 门禁 job 清单。③ `.github/CODEOWNERS` 头部注释同步。④ `main` 分支保护更新：必需状态检查 `frontend` / `server` 增补为 `frontend` / `server` / `database`（REST 复核 contexts）。 |
| 新增功能 | 每个 PR / main push 自动执行「空库迁移 → 漂移检查」；含迁移（0005+）的卡片不合规即阻断合并。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）本地按 CI 顺序全链路复跑（新起临时 `postgres:18` 容器 127.0.0.1:5434，trust）：`psql -f database/roles/0001_roles.sql` 全部成功 → 迁移器执行 0001~0004（15/3/3/4 ms）→ 复跑输出「已是最新（4 个迁移，无漂移）」→ `check:db-schema` 通过（12 表 · 134 列 · 35 索引/唯一 · 28 CHECK）；2）反例：临时加列后 `check:db-schema` 报「1 处漂移」并 exit 1，撤销后恢复，证明门槛有效；3）`ci.yml` YAML 由 `yaml` 解析器校验通过（jobs：frontend / shared / server / database；database 12 步；service / env / options 结构核对）；4）新增 `database` job 由本次推送后的 GitHub Actions 实际执行验证（结果见本 PR 检查项）。 |
| 文档同步 | `CONTRIBUTING.md`（第 5 节 / 附录 A 第 2、4 条）、`.github/CODEOWNERS`（头部注释）、本条目。 |
| 风险与后续 | 1）CI / CD 与分支保护属高风险变更（CONTRIBUTING §15）：请 wmj / lan 非作者评审后再合并；2）分支保护中的 `database` 必需检查按 job 名绑定（app_id 15368），job 改名需同步更新保护设置；3）跨线状态行已过期、本次未改，建议 wmj / lan 评审时同步：`技术设计v0.3-实施与验收.md` §4.9（「现有 frontend / shared / server 三 job」「新增 db job：…建议与空库迁移合并设计」）、`技术设计v0.1-选型分析.md` 三处 CI 现状（「集成测试与空库迁移按 7.5 继续扩展」）、`server/README.md` 第 155 行「（可选）数据库漂移 job」；4）集成测试（L2 余下部分）与 L3 夜间任务仍未纳入。 |
| 备注 | 推送 `px`；未直接推送 `main`。`.github/` 归 px + lan（《团队分工》§4）；`database/` 迁移脚本与角色脚本本次未改动。 |

## 2026-09-18 16:03 +08:00 | px | px | Push 47

| 字段 | 内容 |
|---|---|
| 类型 | `docs(frontend)`（M0-05 · 前端联调清单收口：`前端功能需求.md` §三 逐页「接口 / 字段 / 错误码」+ 对齐清单 A1~A8） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① §三 从早期草案重写为「页面 → 接口 → 字段 → 错误码」联调清单：3.1 页面总表（17 行，含契约状态与前端落点）；3.2 首页列表参数表 + facets 五组 ↔ 侧栏四组映射；3.3 新建 / 编辑（字段表 + 错误码）+ 删除需求登记；3.4 详情与汇总；3.5 任务查询参数 + 前端演示字段 ↔ 契约 `Task` 映射表 + 四格进度；3.6 files 14 / changes 2 / flow 10 端点与关键错误码（已入契约未接线）；3.7 统一错误码 20 条的前端处理口径；3.8 对齐清单 A1~A8。② 对齐清单（供 wmj 逐条决议）：A1 `filter[timeFrom]` / `filter[timeTo]`（Q11：闭区间 + Asia/Shanghai 日界截断 + 列表 / facets 同口径）、A2 `GET /users`（替代 `managers.ts` 硬编码 20 人）、A3 `GET /dicts`（accent 去硬编码）、A4 `GET / PATCH /users/me/preferences`、A5 `DELETE /projects/{id}` 软删、A6 facets 组数差异、A7 任务列表字段范围（ownerEn / 变更摘要 / 文件摘要）、A8 任务默认排序与 sort 白名单。③ 第六章 Q2 / Q4 / Q9 / Q10 追加提案索引，Q11 改写为「已提案（见第三章对齐清单 A1）、待 wmj 定案」。 |
| 新增功能 | 无代码改动（纯文档）。 |
| 修复 / 重构 / 优化 | 修正 §三 旧稿命名（`keyword` / `pageSize` / `manager` / `title` → `q` / `limit` / `managerId` / `name`），与 `shared/` 契约逐条对齐。 |
| 测试与验证 | 纯文档改动，按 AGENTS.md 免跑 `typecheck` / `build`；文档内接口清单与 `shared/generated/openapi.json` 逐条核对一致（paths=35 / operations=38；projects 6 / tasks 2 / flow 10 / files+changes 16 / auth 4）；错误码 20 条与 `shared/src/common/errors.ts`（含 HTTP 映射）逐条比对；文档表格列数与代码片段闭合性脚本校验通过。 |
| 文档同步 | `前端功能需求.md`（§三 重写、§六 Q2 / Q4 / Q9 / Q10 / Q11、变更记录 Push 47）、本条目。`字段对照清单.md` 未改：本次未新增字段口径，任务字段映射引用既有清单。 |
| 风险与后续 | A1~A8 待 wmj 评审（A1 是 M2-04 前须定案的契约项；A2 / A3 是 M1 出口「前端移除硬编码」的前提）；§三 中「已入契约」条目仍待 M2-07 接线；本次未动代码与契约。 |
| 备注 | 推送 `px`；未直接推送 `main`。对齐清单在 §三 末尾（3.8），评审结论可在 PR 评论逐条回复。 |

## 2026-09-18 16:01 +08:00 | lan | lan | Push 50

| 字段 | 内容 |
|---|---|
| 类型 | `chore`（常驻分支同步：`lan` 合并最新 `main`） |
| 分支 | `lan` |
| PR / commit | 直接推送常驻分支 `lan`（未直接推送 `main`）；合并提交 `688eba2`（Merge branch 'main' into lan，纳入 `61ecd6a`、`a541a50`、`96e0db3`、`92619f9`）；本日志条目随本次推送提交。 |
| 做了什么 | ① 将常驻分支 `lan` 同步到最新 `main`：`git merge main` 生成合并提交 `688eba2`，纳入 PR #35（技术设计 v0.3 + ADR-018 + v0.2 §11 增补，Push 44）、PR #37（S7·file 契约切片，Push 41；本线 65cb780 的落地版）、PR #38（首页筛选与侧栏开合记忆 / 恢复，Push 45）、PR #39（会话切换 /auth/* 切到后端 identity，Push 46）；② `开发日志.md` 冲突解决：lan 侧无独有新增（历史条目已随 PR #35 / #37 入 main），直接采用 main 版本，合并后 `lan` 与 `main` 内容完全一致；③ 一并同步主干变化：删除 `frontend/server/oidc-plugin.ts`（SSO 中间件切到后端 identity）、新增 `frontend/src/api.ts` / `frontend/src/homePrefs.ts` 等；④ 本条目（Push 50）随本次推送提交。 |
| 新增功能 | 无（分支同步，本线无代码改动）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）本线本次改动为纯文档（本日志条目），按 AGENTS.md 约定未跑 frontend 门禁；2）合并后校验：`git diff origin/main` 为空（`lan` 与 `main` 内容零差异）；3）冲突解决校验：无冲突标记残留（0 处）、全文 LF（仓库统一行尾）、UTF-8 无 BOM；4）推送前核对：`origin/lan`（`157be76`）是本地 `lan` 祖先，可快进推送（本次推送 6 个提交：`61ecd6a`、`a541a50`、`96e0db3`、`92619f9`、合并提交 `688eba2` + 本条日志）。 |
| 文档同步 | `开发日志.md`（本条目 Push 50）。 |
| 风险与后续 | 1）`lan` 已含 `main`（`92619f9`）全部内容，后续开发基于 `688eba2` 开展；2）本条目待入 `main`，下次 PR 时随行（入 main 时如有重号，按「以入 main 时为准」顺延）；3）同步推送不含本线代码改动，无回滚风险。 |
| 备注 | 推送 `lan`；未直接推送 `main`。Push 50 为顺延编号（px 已占 Push 47~49）。 |

## 2026-09-18 15:48 +08:00 | px | px | Push 46

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（k6 · 会话切换：前端 `/auth/*` 由 Vite 中间件切到后端 identity 模块） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① `frontend/vite.config.ts`：移除 `oidcPlugin`，改为 Vite 代理 `/auth`（另含 `/healthz`、`/readyz`）→ `BACKEND_ORIGIN`（默认 `http://127.0.0.1:3001`，后端默认 3000 与前端冲突，本地 `server/.env` 设 `PORT=3001`）；② 删除 `frontend/server/oidc-plugin.ts`（dev-only 中间件，383 行）及 `tsconfig.node.json` 的 `server` 包含；③ 新增 `frontend/src/api.ts`：`apiFetch`（写请求自动回传 `X-CSRF-Token` ← 可读 `ll_csrf`；同源 Cookie）与 `redirectToLogin()`（401 跳 `/auth/login?returnTo=`，含 hash 路由回跳）；④ `App.tsx` 的 `/auth/me` 与 401 跳转改用封装；⑤ `.env.example` 收敛：`CASDOOR_*` 迁至 `server/.env`（均 gitignore），前端新增 `BACKEND_ORIGIN` / `TEST_*`；⑥ `scripts/smoke-test.mjs` 改测后端链路：`/readyz` 预检、`ll_sid`（HttpOnly）/ `ll_csrf`（可读）/ 旧 `ll_at`、`ll_idt` 移除断言、returnTo 白名单（合法 + 非法）两组。 |
| 新增功能 | 前端登录 / 回调 / me / 登出走后端（HttpOnly 会话 + DB 只存哈希）；登录回跳支持 `returnTo`（白名单校验）；写接口 CSRF 封装就位，随首个写接口接入启用。 |
| 修复 / 重构 / 优化 | 移除 dev-only OIDC 中间件（约 383 行）与 `ll_at` / `ll_idt` 令牌 Cookie 方案（令牌不再进用户浏览器 Cookie，仅服务端会话行持有）。 |
| 测试与验证 | 1）沙箱实测（Docker Casdoor 4.4.0 + PostgreSQL 18.6 + 后端 :3001 + Vite :3000）：`npm run smoke` **8 组 31 项全过**（登录 → 静默签发 → 回调建会话 → `/auth/me` JWKS 验签 → 登出跳 Casdoor → 401；含 returnTo 合法 / 非法两例）；2）会话超时与落库（DB 回拨实测 **10 项全过**）：空闲 31 分钟 → 401 且 `revoked_at` 置位；绝对过期（id_token `exp` 已到）→ 401 且撤销；超时后静默重登 → 200；DB 只存 sha256 哈希（Cookie 原始值查无）；3）单点登出链：`/auth/logout` 302 → Casdoor `/api/logout` → 回跳站点；登出后静默签发失败（SSO 会话确实失效）、账号口令重登可用；4）无头 Chrome + CDP **5 项全过**：未登录访问 → 跳 Casdoor 授权页且 `redirect_uri` 正确；注入真实会话后页面内 `/auth/me` 200、渲染「项目空间」、0 console 错误；5）后端门禁复核：`server` 测试 16 例 / 边界 0 违规 / `check:db-schema` 12 表 134 列 35 索引 28 CHECK 一致；6）`frontend`：`npm run typecheck` + `npm run build` 通过（39 modules，301.07 kB / gzip 90.52 kB）。 |
| 文档同步 | `frontend/README.md`（重写：本地启动含后端 / 登录流程 / 目录 / 生产 / 常见问题）、`前端功能需求.md`（§一.1、§三.9、变更记录）、`docs/本地沙箱(LibiaoLink 演练环境).md`（§九）、`README.md`（目录表与快速开始第 4 步）、本条目。 |
| 风险与后续 | 1）本地验证库为临时 Docker `postgres:18`（端口 5433，非本机 5432 服务），仅本机验证用、可随时删除；2）Casdoor 登出后会换发一个新的匿名 `casdoor_session_id`（服务端会话已失效，属 Casdoor 行为）；3）认证类改动属高风险（CONTRIBUTING §15），需 lan / wmj 非作者评审；正式环境 Redirect URL 与 SSO 应用核对（g7）仍待办；4）跨线状态行已过期（本次未改，建议 wmj 评审时同步）：`server/README.md`「前端切换 k6 仍在 px 线」、`server/src/modules/identity/README.md`「前端会话切换（k6，px）」、`shared/src/modules/identity.ts` 注释「前端切换见 k6 卡片」、`docs/adr/ADR-010-认证与组织.md`「Vite 中间件保留到 k6 切换完成」。 |
| 备注 | 推送 `px`；未直接推送 `main`。本地联调：后端 `PORT=3001`（`server/.env`，不入库），前端 3000 经 Vite 代理 `/auth/*`；`BACKEND_ORIGIN` 可覆盖。 |

## 2026-09-18 15:21 +08:00 | px | px | Push 45

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（首页记忆并恢复上次选择——系统功能书 A1-18 / Q12 落地） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① 新增 `frontend/src/homePrefs.ts`：本地记忆读写（存储键 `libiaolink.home.prefs.v1`，结构 `{ v, filters, sidebarOpen }`），字段级校验（日期格式 / 字符串数组 / 布尔值）+ try/catch 静默降级（隐私模式），记忆清空时 `removeItem`；② `useHashRoute.ts`：无参数入口按记忆恢复筛选并 `replaceState` 写回 URL（带参数链接严格按 URL、不经记忆），新增导出 `initialRouteRestored()` 供页面判断入口来源；③ `Home.tsx`：侧边栏开合按入口与记忆决策（带参数链接有筛选强制展开；无参数入口完全按记忆，无开合记忆时回退「有筛选则展开」；冷启动收起且不写记忆）；只在主动操作（改筛选 / 开关 / 关闭侧栏）时写记忆；④ `AppHeader.tsx`：退出登录时清除记忆。 |
| 新增功能 | 首页筛选（地区 / 项目类型 / 项目经理 / 时间区间 / 排序）与侧边栏开合的记忆与恢复：URL 优先、本地记忆兜底；关键字 `q` 不记忆（临时操作）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）无头回归 41 项全过（Node + CDP 自建脚本：冷启动不写记忆 / 主动操作写入 / 书签入口恢复 / URL 优先 / 开合记忆 / 重置与排序 / 搜索不记忆 / 退出清除 / 记忆损坏健壮 / 全程 0 console 错误）；2）`npm run typecheck` + `npm run build` 通过（38 modules，产物 `index-KTygA4Hs.js`）。 |
| 文档同步 | `前端功能需求.md`（§一.3 筛选态 / §五 状态保持 / 第六章第 12 条定案 / 变更记录 Push 45 行）、本条目。 |
| 风险与后续 | 1）记忆仅存本机浏览器、跨设备不同步，如需跨设备可评估服务端偏好接口；2）隐私 / 无痕模式下存储不可写时静默降级（不恢复、不影响功能）；3）多人共用设备由「退出登录清除」兜底，未登录直接关浏览器仍会留下记忆，是否需要过期策略待评估。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |


## 2026-09-18 15:12 +08:00 | lan | lan | Push 44

| 字段 | 内容 |
|---|---|
| 类型 | `feat`+`docs`（S7·file 契约切片：上传 / 版本 / 定档 / 变更 / 回收站；含两次主干同步） |
| 分支 | `lan` |
| PR / commit | PR #37（`lan` → `main`，squash 合入）；分支内 4 笔：① 主干同步 `a57eb15`（纳入 `129a27c` px Push 40 品牌黄配色，`开发日志.md` 冲突保留双方 Push 40 全记录）；② 契约提交 `65cb780`；③ 日志提交 `8f7b97a`；④ 主干二次同步（本次合并提交，纳入 px Push 41 CI 扩展 / Push 42 CODEOWNERS、wmj Push 43 g6 会话后端化）。未直接推送 `main`。 |
| 做了什么 | ① 新建 `shared/src/modules/files.ts`：文件五态 `File` / `FileDetail`（含 `finalizedAt/finalizedBy`、`recycledAt/recycledBy/recycledFromStatus`）、`FileVersion`（版本链）、上传会话（`UploadCreateBody` 按 `intent` 判别联合：`version` 仅草稿替换 / `change` 必带变更字段、`UploadSession`、分片预签名 parts、会话状态含已传 / 缺失分片、complete / abort）、文件操作（finalize 定档、rollback 回溯、recycle / restore / purge、签名下载）、变更（`ChangeRequest` / 详情 / 列表查询）；② `openapi.ts` 注册 **16 条路径**（项目文件库列表、上传 5 条、文件详情与版本 2 条、操作 5 条、变更 2 条、下载 1 条），新增 tags `files` / `changes`；③ `common/errors.ts` 新增错误码 `UPLOAD_INCOMPLETE`(409) / `UPLOAD_EXPIRED`(409) / `CHECKSUM_MISMATCH`(422)；④ 重新生成 `generated/openapi.json` 与 `generated/api-types.d.ts`；⑤ `shared/README.md` 目录树与已定案口径表更新（文件状态 / 上传 / 变更 / 回收站 / 签名地址 5 行）、切片说明更新；⑥ `技术设计v0.2` §7.2 错误模型表补 3 码（2 行）；⑦ **主干二次同步冲突解决**：`shared/src/openapi.ts` / `index.ts` 取 files 与 identity 契约并集（auth 4 条路径、tag 与 import 并入），`shared/README.md` 口径表与目录树取并集，`开发日志.md` 保留双方条目（lan 15:12 / wmj 15:02），生成物按合并后源码重新生成。 |
| 新增功能 | 契约层：文件与变更接口面（A4-01~A4-18 / D2 的契约面）——分片直传与续传、版本与定档、变更即通过、回收站、签名下载、变更记录检索；主干同步后契约全量 = **paths 35 / schemas 82**（files 16 + auth 4 + 原有 15）。 |
| 修复 / 重构 / 优化 | 无（纯新增契约与生成物；未改既有 schema 语义）。 |
| 测试与验证 | 1）`shared`（合并后复跑）：`npm run typecheck` 通过 → `npm run generate`（133,201 / 103,912 bytes）→ `npm run check` 通过（OpenAPI 3.1 结构校验 **paths=35、schemas=82**，两份生成物与源码零漂移）→ `npm run build` 通过；2）`server`（含 g6 代码）：`npm run typecheck` 通过 → `npm run test` **3 个文件 / 16 个用例全过** → `npm run check:boundaries` 通过（**39 文件 / 100 条依赖 / 0 违规**）；3）`frontend`（推送门禁）：`npm run typecheck` 通过 → `npm run build` 通过（vite 8.3.0，37 modules，298.58 kB / gzip 89.63 kB）；4）生成物抽查：`UploadCreateBody` 为 `oneOf`（version 仅基础字段；change 必带 change 对象）、`ErrorCode` 枚举含 3 个新码、`File.required` 18 字段齐全、files 16 条路径逐条核对；5）环境说明：本机 PATH 原先只有 Codex 运行时 node（无 npm），本次下载官方 Node 24.19.0（npm 11.17.0）到临时目录执行（与 CI Node 24 口径一致），临时目录不入库。 |
| 文档同步 | `shared/README.md`（目录树 + 已定案口径 6 行 + 切片说明，取 files / identity 并集）；`技术设计v0.2-架构与数据模型.md` §7.2（+3 错误码）；本条目。`前端功能需求.md` 未改：本次无 `frontend/` 改动，文件 UI（u4）接线时由 px 回写接口需求。 |
| 风险与后续 | 1）**对外接口契约属高风险变更（CONTRIBUTING §15）**：请 wmj（契约线主责）/ px 非作者评审后再合并；2）契约中 `finalizedAt/finalizedBy`、`recycledAt/recycledBy/recycledFromStatus` 需迁移落列，**g6 已占 `0004_identity.sql`，PR-2 迁移编号顺延为 `0005`**；若评审调整命名，契约与生成物在同一 PR 同步；3）完成上传时 `contentHash` 为客户端提供、服务端与 init 值比对（大小以对象存储 HEAD 校验），防伪复核留作后续 worker 任务；4）幂等表（`idempotency_keys`）与审计表（`audit_logs`）尚未落库：写接口的 `Idempotency-Key` 语义与审计事件口径待 PR-2 与 wmj 对齐后实现；5）多文件变更（`change_items`）、问题 / 日报关联、归档动作按计划后置；preview 契约随 i3 补；6）**Push 编号**：主干已至 Push 43，本条按「先入 main 者为准」取 44（lan 分支内 30 / 31 / 40 三条与已入 main 的同号条目重复，合并时不再另占号）。 |
| 备注 | 推送 `lan`；未直接推送 `main`。本条契约切片对应时序图任务 `i1 · S7·file` 的 PR-1（契约先行），下游为 i2（PoC-1）/ i3（preview）/ u4（文件 UI）。 |
## 2026-09-18 15:02 +08:00 | wmj | wmj | Push 43

| 字段 | 内容 |
|---|---|
| 类型 | `feat`（g6 · S5·会话后端化：后端 `/auth/*` + users/sessions 迁移 + identity 契约；替换 Vite 中间件） |
| 分支 | `wmj` |
| PR / commit | 随本 PR（wmj → main）提交；未直接推送 `main`。含主干同步（g4 入 main 后与 Push 40~42 合并，冲突按「主干版本 + 双方条目并存」收口）。 |
| 做了什么 | ① 契约（shared）：新增 `modules/identity.ts`（User / MeResponse / login / callback 查询参数），OpenAPI 注册 `/auth/login`、`/auth/callback`、`/auth/me`、`/auth/logout` 四条路径，错误码新增 `AUTH_CALLBACK_FAILED`(400)；生成物随 PR（paths 15→19，schemas 47→49）。② 数据库：`0004_identity.sql`（users + sessions；会话 Cookie 值只存 sha256 哈希，id_token 仅用于单点登出；新表权限由 default privileges 自动授予应用角色），Drizzle schema `identity.ts` 对齐。③ server：`modules/identity/` 首个真实实现——OIDC 客户端（discovery / JWKS 验签 / iss + aud + exp 校验 / 换令牌，带 8s 超时与 10 分钟缓存）、会话用例（空闲超时 + 绝对上限 + 禁用踢线 + 60s 节流 touch）、用户 upsert（claims 归一化）、控制器（PKCE + state + returnTo 白名单、HttpOnly `ll_sid`、可读 `ll_csrf`、Casdoor 单点登出）、`SessionGuard` + `@CurrentUser()` 与 `CsrfGuard` 出口；env 扩展 CASDOOR_* / SESSION_*（生产缺 client 凭据启动即失败）；app.module 装配。④ 测试：桩 IdP（进程内，discovery / JWKS / 换令牌，RS256 真签名）+ 内存仓库，10 例端到端。 |
| 新增功能 | 后端承载 `/auth/*` 会话链路（原仅 Vite 中间件，dev-only）：登录 / 回调 / me / 登出；HttpOnly 会话（DB 只存哈希）；空闲超时按接入标准（SESSION_IDLE_MINUTES 默认 30 分钟）；登出同清本地会话并跳 Casdoor 单点登出。 |
| 修复 / 重构 / 优化 | 会话不再依赖前端 dev server；CSRF 同步 token 机制就位（写接口后续叠加 CsrfGuard）；users 目录落地，为 h1/h2 的项目经理 / 负责人引用提供挂点。 |
| 测试与验证 | ① server 门禁：typecheck / vitest（3 文件 16 例：原 6 + 新 10）/ build / check:boundaries（39 文件 · 100 依赖 · 0 违规）全绿；② 真 PG 18.4（临时集群 127.0.0.1:15432）：干净库 `libiaolink_g6` 迁移 0001→0004（0004 8ms）全绿、幂等复跑「已是最新（4 个迁移，无漂移）」；`libiaolink_g4` 升级路径只补 0004；check:db-schema 12 表 · 134 列 · 35 索引/唯一 · 28 CHECK 一致；反例：临时加列即报漂移（exit 1），撤销后恢复；③ 应用角色 `libiaolink_api` 对 users / sessions 可增删改查、`create table` 被拒（无 DDL）；④ 运行时冒烟（dist 产物）：/healthz 200、/readyz 200、/auth/me 401 信封、/auth/login 302 + Set-Cookie（PKCE 参数齐全）、/auth/callback 400 信封、/auth/logout 302 + 清 Cookie；⑤ 纯后端 + 文档改动，按 AGENTS.md 未跑 frontend 门禁。 |
| 文档同步 | `技术设计v0.2`（§2.1 / §2.3 / §7.1 / §7.2 + v0.2.5 修订行）、`docs/adr/ADR-010-认证与组织.md`（实现回写）、`database/README.md`（0004 + 期望输出）、`server/README.md`（会话链路 / 目录 / 测试 / 后续卡片）、`server/src/modules/identity/README.md`（占位转实现）、`shared/README.md`（目录 / 口径表 / 切片）、`server/.env.example`、本条目。 |
| 风险与后续 | 1）迁移 + 鉴权会话 + 契约属高风险变更（CONTRIBUTING §15），需 lan / px 批准；2）`sessions.id_token` 用于单点登出（id_token_hint）：ADR-010「不存原始凭据」已收紧为「Cookie 值只存哈希；不存 refresh_token / access_token / client_secret」，请在评审确认；3）前端切换（k6）与正式环境 SSO 核对（g7）为 px 线后续卡：本地需把 `/auth/*` 代理到后端，生产 Redirect URL 仍登记站点域名；4）组织 / 角色 / 通讯录同步与 getUser 扩展在 h1；过期会话物理清理随 worker / jobs 卡片；5）本机无 Docker，真实 Casdoor 沙箱全链路（登录→超时→重登→登出）留待 px 沙箱 / g7 演练补证。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。identity 属后端领域（wmj 主责，lan 评审）；本次未改 `frontend/`、`.github/`、`deploy/`。 |
## 2026-09-18 15:04 +08:00 | px | codex/backend-design-v03 | Push 44

| 字段 | 内容 |
|---|---|
| 类型 | `docs`（后端实施与验收设计：新增 v0.3 + ADR-018 + v0.2 增补） |
| 分支 | `codex/backend-design-v03`（自 `origin/main` eb33ffe 切出；rebase 至 a6d3574（含 g6 / Push 43）并解决 开发日志 / 技术设计v0.2 冲突；经 PR 合入 `main`，未直接推送 `main`） |
| PR / commit | 随本 PR（PR #35，codex/backend-design-v03 → main）提交。 |
| 做了什么 | ① 新增 `技术设计v0.3-实施与验收.md`（623 行）：执行模型（卡片流水线八步、DoR / DoD、卡片模板）、M0~M8 里程碑与依赖、九项 PoC 落点、逐里程碑详细设计（M0 口径冻结、M1 身份与平台底座、M2 项目纵切、M3 任务纵切、M4 文件与预览、M5 自动化与通知、M6 日报 / 问题 / 干系人 / 工作台、M7 搜索 / 仪表盘 / 导出、M8 迁移与上线）、横切机制（契约 / 迁移 / 幂等 / 权限 / 审计 / 时间 / 错误码 / 测试 / 可观测）、协作与联调节奏、验收与风险、M0 十五项待定案口径清单（含建议值）；② 新增 `docs/adr/ADR-018-后端实施里程碑与卡片流水线.md` 并登记索引；③ `技术设计v0.2-架构与数据模型.md` 增补 §11（需新增的表 / 需修订字段 / 需新增契约族 / 错误码登记）与修订记录 v0.2.6（v0.2.5 已由 g6 落地回写占用）；④ `README.md` 文档表新增 v0.3、ADR 更新为 18 条、v0.2 版本号同步 v0.2.6；⑤ v0.3 §3.2 与 v0.2 §11.1 / §11.3 回写 g6 已落地状态（/auth/* 会话与 0004 users / sessions 迁移）。 |
| 新增功能 | 无（纯设计文档）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 纯文档改动，按 AGENTS.md 未跑 frontend 门禁；已核对 v0.3 章节结构（8 个一级章节）、v0.2 §11 与修订表、ADR 索引编号连续（ADR-001~018）、README 文档表引用一致；PR #35 的 CI（frontend / shared / server 三 job）在 Push 43 提交时全绿，rebase 后以复跑为准。 |
| 文档同步 | 新增 `技术设计v0.3-实施与验收.md`、`docs/adr/ADR-018-后端实施里程碑与卡片流水线.md`；更新 `技术设计v0.2-架构与数据模型.md`、`docs/adr/README.md`、`README.md`、本条目。 |
| 风险与后续 | 1）v0.3 §7 十五项口径为建议值，需 M0 决议会逐项关闭（ADR-019 起）；2）ADR-018 为草案，需 wmj / lan 评审后转已采纳；3）本批为契约 / 架构类文档，按 CONTRIBUTING §15 需非作者评审；4）下一步：M0-01 决议会 + M0-04 R01~R07 文案入仓。 |
| 备注 | 推送 `codex/backend-design-v03`；未直接推送 `main`。Push 序号因 wmj 的 g6（Push 43）先入 main，本条目顺延为 44。 |

## 2026-09-18 14:49 +08:00 | px | px | Push 42

| 字段 | 内容 |
|---|---|
| 类型 | `chore(ci)`（治理：`main` 分支保护必需状态检查纳入 `server` + 文档同步） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① `main` 分支保护更新：必需状态检查由 `frontend` 增补为 `frontend` / `server`（GraphQL `updateBranchProtectionRule`，REST 复核 `required_status_checks.contexts = ["frontend","server"]`）；② `CONTRIBUTING.md` 两处同步（第 5 节推送流程、附录 A 第 2 条平台保护）；③ `.github/CODEOWNERS` 头部注释同步。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 治理收口：Push 41 合入的 `server` 门禁自此为 PR 合并硬门禁（`frontend` / `server` 双必需检查）。 |
| 测试与验证 | 1）`gh api` REST 复核：`required_status_checks.contexts` = `["frontend","server"]`；2）`server` job 在 PR（run 35316333487）与 `main` push（run 35316452400）均实测 success；3）纯文档 + 治理配置改动，按 AGENTS.md 未跑 frontend 门禁（PR 的 CI 仍会执行三个 job）。 |
| 文档同步 | `CONTRIBUTING.md`（第 5 节 / 附录 A 第 2 条）、`.github/CODEOWNERS`（注释）、本条目。 |
| 风险与后续 | 1）后续 PR 需 `frontend` + `server` 均绿方可合并；2）`server` 检查按 job 名绑定 GitHub Actions（app_id 15368），job 改名时需同步更新保护设置；3）数据库漂移 / 空库迁移 job 仍待规划（见 Push 41）。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-18 14:44 +08:00 | px | px | Push 41

| 字段 | 内容 |
|---|---|
| 类型 | `chore(ci)`（CI 扩展：`server` 门禁入 GitHub Actions——g5 落地） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① `.github/workflows/ci.yml` 新增 `server` job（Node 24，锁定安装）：先 `shared` 安装 + 构建（server 以 `file:../shared` 消费契约包），再 `server` 锁定安装，依次 `npm run typecheck` / `npm run test`（vitest）/ `npm run build`（nest build）/ `npm run check:boundaries`（依赖方向规则）；较 `server/README.md` 登记的 g5 片段补 `Build` 一步（g4 本地验证五项之一，保证部署产物可构建）。② `server/README.md`：CI 接线段落标记落地、后续卡片衔接同步。③ `技术设计v0.1-选型分析.md`：三处 CI 现状表述补 server 门禁（1.5 现状快照 / 4.19 / 7.5.2）；④ `CONTRIBUTING.md` 附录 A 第 4 条 CI 门禁现状同步（frontend / shared / server 三 job 分工）。 |
| 新增功能 | 无（CI 门禁扩展）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）本地按 CI 顺序复跑：`cd shared && npm ci && npm run build` → `cd server && npm ci` → `npm run typecheck` / `npm run test`（2 文件 6 例全过）/ `npm run build`（nest build）/ `npm run check:boundaries`（24 文件 / 50 条依赖，0 违规）；2）`ci.yml` YAML 结构解析通过（jobs：frontend / shared / server；server 9 步）；3）新增 `server` job 由本次推送后的 GitHub Actions 实际执行验证（结果见本 PR 检查项）。 |
| 文档同步 | `server/README.md`、`技术设计v0.1-选型分析.md`（1.5 现状快照 / 4.19 / 7.5.2）、`CONTRIBUTING.md`（附录 A 第 4 条）、本条目。 |
| 风险与后续 | 1）CI / CD 与分支保护属高风险变更（CONTRIBUTING §15）；合入后建议在 `main` 分支保护中把 `server` 加为必需状态检查（当前仅 `frontend`）；2）数据库漂移 / 空库迁移 job 未随本次纳入（`server/README.md` 可选建议：起 `postgres:18` service → 跑 `database` 迁移 → `npm run check:db-schema`，建议与 database 线合并设计）；3）`server` 门禁自此对所有 PR 生效（含 `shared` 构建），CI 时间略增。 |
| 备注 | 推送 `px`；未直接推送 `main`。`.github/` 归 px+lan（《团队分工》§4）；本条按指示直接落地。 |

## 2026-09-18 14:36 +08:00 | px | px | Push 40

| 字段 | 内容 |
|---|---|
| 类型 | `style`（前端：品牌黄配色统一——分类筛选开关与飞箱卡片） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① `CategorySwitch.tsx` 开启态轨道由紫色 `#8a5cd4` 改为品牌黄 `#feca04`，开启态文字由白转深灰 `#313033`（黄底白字对比度不足），关闭态深灰轨道 + 白字不变；② `Card.tsx` amber（飞箱）配色由 `amber-500` 橙改为品牌黄 `#feca04`——类型徽标底色（文字 `text-amber-950` → `#313033`）、小车图标填充、悬停光晕（`rgba(245,158,11,0.35)` → `rgba(254,202,4,0.35)`）三处同色；T-sort 蓝 / 3D分拣 绿不变；③ 文档同步。 |
| 新增功能 | 无（纯配色调整）。 |
| 修复 / 重构 / 优化 | 品牌色收敛：开关开启态与飞箱徽标 / 小车 / 悬停光晕统一为 `#feca04`，与「新建项目」按钮、Logo 黄色一致（此前开关开启态为紫色 `#8a5cd4`、飞箱为 `amber-500` 橙）。 |
| 测试与验证 | ① 无头 Chrome + CDP 实测（mock `/auth/me`）：开关两态 computed style + 截图像素采样（关闭 `[49,48,51]` / 开启 `[254,202,4]`），飞箱卡片徽标与小车像素采样均 `[254,202,4]`，0 console 错误；② `npm run typecheck` / `npm run build` 通过；③ 截图核对（开关两态特写、飞箱卡片特写）。 |
| 文档同步 | `前端功能需求.md`（§一.3 卡片配色与开关描述、§二.1 accent 字段说明、变更记录 Push 40）；本条目。 |
| 风险与后续 | 1）`accent` 代码值仍为 `amber`（仅渲染色改为品牌黄），待 C9 字典元数据下发后由后端驱动（见 §二.1）；2）纯前端配色改动，未触及契约与数据库，无需按 CONTRIBUTING §15 走高风险评估。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-18 14:31 +08:00 | lan | lan | Push 40

| 字段 | 内容 |
|---|---|
| 类型 | `chore`（常驻分支同步：`lan` 合并最新 `main`，PR #23~#31） |
| 分支 | `lan` |
| PR / commit | 直接推送常驻分支 `lan`（未直接推送 `main`）；合并提交 `419b794`（Merge branch 'main' into lan，纳入 `60d72d8`…`673f00f` 共 9 个提交）；本日志条目随本次推送提交。 |
| 做了什么 | ① 将常驻分支 `lan` 同步到最新 `main`：`git merge main` 生成合并提交 `419b794`，纳入 PR #23~#31（项目编号填写口径、g3 数据库基线、CI 契约漂移检查 + 仓库统一 LF 行尾、首页筛选 URL query、人员字段收敛、0002 迁移、seqNo 全链路、g4 后端工程骨架、项目编辑入口）；② `开发日志.md` 顶部冲突解决：保留双方全记录、按时间倒序排列（39 → … → 32 → 31（lan）→ 31（px）→ 30（px）→ 30（lan）→ 29 → … → 26（lan）→ 25）；③ 重号说明：lan 的 Push 30 / 31 与 px 已入 main 的 30 / 31 重号，按「以入 main 时为准」保留临时编号，待 `lan` 后续 PR 合入 `main` 时顺延并注明；④ 行尾：仓库已启用统一 LF（`.gitattributes` `text=auto eol=lf`），冲突解决按 LF 写出；⑤ 本条目（Push 40）随本次推送提交。 |
| 新增功能 | 无（分支同步，本线无代码改动）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）本线本次改动为纯文档（冲突解决 + 本日志条目），按 AGENTS.md 约定未跑 frontend 门禁；2）合并校验：`git merge-base --is-ancestor origin/main lan` 通过（main 已全部并入），`lan` 与 `origin/main` 差异仅 `开发日志.md`（+45 行，Push 26 / 30 / 31 条目）；3）冲突解决校验：无冲突标记残留（0 处）、全文 LF（无 CRLF）、UTF-8 无 BOM；4）推送前核对：`origin/lan`（`a999232`）是本地 `lan` 祖先，可快进推送（本次推送 11 个提交：`60d72d8`…`673f00f` 9 个 + 合并提交 `419b794` + 本条日志）。 |
| 文档同步 | `开发日志.md`（本条目 Push 40）。 |
| 风险与后续 | 1）`lan` 已含 `main`（`673f00f`）全部内容，后续开发基于 `419b794` 开展；2）Push 26 / 30 / 31 条目待入 `main`，合并时按「以入 main 时为准」顺延编号；3）`server/`、`database/`、`shared/` 已落主干，后端平台模块（file / preview / notify / outbox / search / dashboard / 迁移工具链）按分工由本线承接；4）同步推送不含本线代码改动，无回滚风险。 |
| 备注 | 推送 `lan`；未直接推送 `main`。Push 40 为临时编号（当前 main 最大 39），入 main 时如有重号按约定顺延。 |

## 2026-09-18 14:08 +08:00 | px | px | Push 39

| 字段 | 内容 |
|---|---|
| 类型 | `feat`（前端：卡片与详情页新增项目编辑入口） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① `NewProjectModal.tsx` 泛化为 `ProjectModal.tsx`（`mode: create / edit`、`initial` 预填、标题 / 副文案 / 主按钮随态切换）；② `Card.tsx` 新增可选 `onEdit`：卡片左下角常驻幽灵态铅笔按钮（默认无底色无描边、低透明度灰铅笔，悬停卡片略加深、悬停按钮浮现白底 + 淡阴影；`aria-label` / `title` = 编辑项目），`stopPropagation()` 不触发进详情；③ `Home.tsx` 卡片外壳由 `<button>` 改为 `role=link` + `tabIndex=0` 的 div（避免按钮嵌套，保留 Tab + Enter / 空格打开）；④ `ProjectDetail.tsx` 阶段标签栏右侧新增「编辑项目」按钮；⑤ `App.tsx` 新增 `editingProject` 状态与 `handleUpdateProject`（更新 code / description / projectType / accent / managerId / updatedAt，`id` 与 `seqNo` 不变），列表与详情共用同一弹窗；⑥ 文档：`前端功能需求.md` §一.3 卡片编辑入口、§一.5 改名「新建 / 编辑项目」并补编辑条目与内存态说明、§一.6 详情页按钮、§三.8 改为 `PATCH /api/v1/projects/{id}` 需求登记（契约 `ProjectUpdateBody`，含 `version` 乐观锁、409 `PROJECT_CODE_EXISTS`）、§四.1 code 规则、§五 状态保持、变更记录。 |
| 新增功能 | 项目编辑：卡片左下角铅笔按钮 / 详情页「编辑项目」按钮 → 弹窗预填编号 / 描述 / 项目经理 / 项目类型 → 保存后卡片（编号、描述、项目经理姓名、类型徽标与主题色）与详情顶栏（编号、描述、更新时间）即时刷新；`id` / `seqNo` 不可修改。 |
| 修复 / 重构 / 优化 | 编辑按钮初版放在「项目经理」行右侧，与右上角序号装饰圆重叠：绝对定位的装饰圆按绘制顺序压住按钮（铅笔被切角），且真实鼠标点按钮上沿会命中原装饰圆 → 冒泡进详情。改为左下角布局（与编号 / 描述左对齐、与更新时间同行）并保留 `relative z-10` 防护后彻底消除；按反馈把按钮从白底描边芯片改为幽灵态（默认若隐若现、悬停浮现）。 |
| 测试与验证 | ① 无头 Chrome + CDP 全链路实测（mock `/auth/me`）：36 项断言 ALL PASS——20 张卡片均有编辑按钮、弹窗标题 / 预填 / 按钮文案、保存后卡片与详情顶栏联动、按新项目经理筛选命中 2 个项目、卡片主体点击与键盘 Enter 仍进详情、Esc 取消、全程 0 console 错误；② 命中测试专项：`elementFromPoint` 在按钮四角 / 中心均返回「编辑项目」，`Input.dispatchMouseEvent` 真实鼠标多点连点 5/5 打开弹窗、hash 保持 `#/`、0 跳详情；③ `npm run typecheck` / `npm run build` 通过；④ 截图核对（默认 / 悬停卡片 / 悬停按钮 / 整页）。 |
| 文档同步 | `前端功能需求.md`（§一.3、§一.5、§一.6、§三.8、§四.1、§五、变更记录 Push 39）；本条目。 |
| 风险与后续 | 1）编辑为前端内存态，刷新回默认；接 `PATCH /api/v1/projects/{id}` 后由服务端持久化（接口就绪后替换 `App.tsx` 的 `handleUpdateProject`，并补 409 / 403 / 404 提示）；2）演示数据 `id` 仍为 slug，契约 `Project.id` 为 UUID，接接口时统一；3）纯前端改动，未触及契约与数据库，无需按 CONTRIBUTING §15 走高风险评估；4）`region` 不在编辑字段内（新建仍默认「未分类」）。 |
| 备注 | 推送 `px`；未直接推送 `main`。Push 序号：wmj 的 g4（服务端骨架，PR #30）先入 main 记 Push 38，本条目按「以入 main 时为准」顺延为 Push 39（本分支首个 commit 消息里的 Push 38 为准入前旧号）。 |

## 2026-09-18 13:51 +08:00 | wmj | wmj | Push 38

| 字段 | 内容 |
|---|---|
| 类型 | `feat`（g4 · S5·工程骨架：`server/` 后端工程骨架 + 服务边界规则；含 `shared/` 构建出口最小改动） |
| 分支 | `wmj` |
| PR / commit | 随本 PR（wmj → main）提交；未直接推送 `main`。含两次主干同步（`76e6c1e → b31c50b → ac9db47`，PR #26~#29 已合入，冲突按「主干版本 + 双方条目并存」收口）。 |
| 做了什么 | ① 新建 `server/` 独立 npm 包（NestJS 12.0.3 + TypeScript ~6.0.2 + Node 24，ESM / NodeNext）：api / worker 双入口（`src/entry/api.ts`、`src/entry/worker.ts`，worker 支持 `--health-check` 与 60s 心跳）；统一错误（`AppError` + 全局 `APP_FILTER` → 契约包 `ApiError` 信封 code / message / details / traceId，HTTP 状态取自 `HTTP_STATUS_BY_ERROR_CODE`）+ `ZodValidationPipe`（DTO 一律用契约包 schema）；结构化日志（nestjs-pino / Pino JSON，`X-Request-Id` 透传或生成并回写响应头）；健康检查 `/healthz`（存活）与 `/readyz`（PG 连通 + 核心表可达，失败 503）。② 数据访问：`src/db/`（pg Pool + drizzle-orm 0.45.2）与全量 Drizzle schema（对齐 0001 + 0002 + 0003 后的最终结构，10 表）。③ 模块骨架：13 个模块目录占位（README 标注类型 / 职责 / 主责 / 预留接口）+ 四层结构（controller / service / repository / events）与 index 唯一出口约定。④ 边界规则：`scripts/check-boundaries.mjs`（TypeScript 编译器 API，零新增依赖）落地 v0.2 §1.2 四条规则（跨模块深引 / 平台反依赖业务 / infra 反依赖业务 / 循环依赖），违规退出码 1。⑤ 漂移检查：`scripts/check-db-schema.mjs` 比对 Drizzle schema ↔ 实际库（表 / 列类型 / 可空性 / 索引 / CHECK 名称）。⑥ `shared/` 补构建出口：tsconfig.build（tsc + rewriteRelativeImportExtensions → `dist/` + d.ts）、`exports` / `main` / `types`，错误模块补 `ErrorDetail` / `ApiError` 类型导出——server 以 `file:../shared` 消费契约包。⑦ 测试：vitest + supertest 6 例。⑧ 文档：`server/README.md`（含 g5 的 CI job 片段）、根 README 索引、`技术设计v0.2` v0.2.4 修订记录、ADR-017 阶段 0 实测记录。 |
| 新增功能 | server 工程骨架与边界门禁：本地起服务过健康检查；`check:boundaries` / `check:db-schema` 可拒 CI（违规退出码 1）。 |
| 修复 / 重构 / 优化 | shared 补 `dist` 产物出口（g2 只做了生成与漂移校验，未建发布口）；ADR-017 待复核 #1 实测收敛（后端 TS 6.0.x、NestJS 12 为 ESM 包、Drizzle 最新稳定 0.45.2，1.x 仍为 rc）。 |
| 测试与验证 | 真 PostgreSQL 18.4（临时集群 127.0.0.1:15432，临时资产不入仓库）：1）新建干净库 `libiaolink_g4`，按 README 顺序跑 `roles/0001_roles.sql` → 迁移器执行 `0001_baseline.sql（42 ms）`、`0002_projects_manager.sql（3 ms）`、`0003_projects_seq_no.sql（52 ms，随主干 PR #29 合入后补跑）`；2）按 g5 的 CI 顺序 `shared npm ci + build` → `server npm ci` 后五项门禁全绿：typecheck、vitest（2 文件 6 例）、build、`check:boundaries`（24 文件 / 50 依赖 / 0 违规）、`check:db-schema`（0001+0002 基线：10 表 · 116 列 · 27 索引/唯一 · 25 CHECK；合入 0003 后复验：10 表 · 117 列 · 28 索引/唯一 · 26 CHECK，均一致）；3）运行时：`/healthz` 200、`/readyz` 200（三探针）、未知路由 404 统一信封（`code=NOT_FOUND` + traceId）、`X-Request-Id` 透传回写、worker `--health-check` 通过；4）反例演示：跨模块深引与平台反依赖被 `check:boundaries` 拒绝（exit 1）、临时加列漂移被 `check:db-schema` 拒绝（exit 1），撤销后恢复通过；5）纯后端 + 文档改动，按 AGENTS.md 未跑 frontend 门禁。 |
| 文档同步 | `server/README.md`（新建）、根 `README.md` 索引、`技术设计v0.2-架构与数据模型.md`（修订记录 + 头部版本号）、`docs/adr/ADR-017-版本基线与依赖升级策略.md`（阶段 0 实测记录）、`shared/package.json` / `shared/tsconfig.build.json` / `shared/src/common/errors.ts`、本条目。 |
| 风险与后续 | 1）新依赖引入（NestJS 12 / drizzle-orm / pino / vitest / supertest）与 CI 门禁属高风险变更（CONTRIBUTING §15），需 lan 或 px 批准；2）CI 接线按方案 A：g4 交付 job 片段（`server/README.md`），`.github/` 归 px 线、由 g5 落地；3）px 的 0003（seqNo，PR #29）先入 main：本次合并已同步 `server/src/db/schema/projects.ts`（`seq_no` 默认值 / 唯一约束 / CHECK）并复验 `check:db-schema`；4）`技术设计v0.2` 修订行撞号按约定处理：px 的 v0.2.3（seqNo）保留，g4 条目顺延为 v0.2.4（头部同步）；5）Push 序号确认：px 的 Push 36/37 已入 main，本条目为 Push 38。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。`server/` / `shared/` / `database/` 归 wmj + lan，请 lan / px 评审。g4 验收口径：① 本地起服务并过健康检查——`/healthz`、`/readyz` 实测通过；② 违规依赖被 CI 拒绝——检查器本地实测可拒（exit 1），CI 接线由 g5 落地（片段已随本条目交付）。 |

## 2026-09-18 13:50 +08:00 | px | px | Push 37

| 字段 | 内容 |
|---|---|
| 类型 | `feat`（项目序号 seqNo 全链路：数据库迁移 / 契约 / 前端 / 文档；另含首页工具条排序 TIME 标识） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① 数据库新增 `database/migrations/0003_projects_seq_no.sql`（只追加）：`create sequence projects_seq_no_seq`；`projects.seq_no integer not null unique default nextval('projects_seq_no_seq')` + `ck_projects_seq_no check (seq_no > 0)`；存量行按 `created_at, code` 回填 1..N 并把序列推进到 `max+1`；序列 `owned by projects.seq_no`；② 契约（`shared/src/modules/projects.ts`）新增 `Project.seqNo`（服务端分配、创建请求不传），`generated/openapi.json` / `api-types.d.ts` 重新生成，`shared/README.md` 口径表补一行；③ 前端：`types.ts` `Project.index: string` → `seqNo: number`，`data/projects.ts` 演示数据 1–20，`Card.tsx` 徽标两位补零 + 悬停「项目序号」，`Home.tsx` 传参与搜索（原值 / 两位补零），`App.tsx` 原型按「现有最大值 + 1」分配，`AppHeader.tsx` 详情面包屑改为「序号 05 / CNBJ-…」；④ 工具条排序：分段控件内部左侧加 TIME 标识（10px 半粗、0.18em 字距、竖线分隔），两按钮补 `aria-label` / `title`（降序＝最新在前、升序＝最早在前）；⑤ 文档：技术设计 v0.2.3（§2.3 DDL、§8.2 排序）、系统功能书 A1-12【定档】与修订行、前端功能需求（卡片 / 搜索 / 新建 / 数据模型 / 创建响应 / 字段规则 / Q13 / 变更记录）、字段对照清单新增「系统内部生成字段」章节、database/README 迁移清单与验收口径。 |
| 新增功能 | 项目序号（seqNo）：卡片右上角徽标、列表排序 `sort=seqNo:asc\|desc`、按序号检索、详情面包屑与编号并列展示。 |
| 修复 / 重构 / 优化 | 填上「卡片右上角数字」的口径空白：原 `index` 是前端自增的纯展示序号（新建按列表长度 + 1，删除后会重号、与编号无绑定），现为服务端分配、全库唯一、不可修改、不回收的持久化标识，与项目编号一一对应同一项目。 |
| 测试与验证 | ① 契约：`npm run typecheck` + `npm run generate` + `npm run check` 通过（paths=15、schemas=47，生成物无漂移）；② 前端：`npm run typecheck` / `npm run build` 通过，`frontend/src` 已无旧 `index` 字段；TIME 标识按编译后 CSS 静态渲染截图核对；③ 数据库（一次性 Docker Postgres 18 容器，验证后已删除）：空库 0001→0002→0003 全绿、重复执行「已是最新（3 个迁移，无漂移）」；带数据升级场景——3 条旧数据（created_at 2026-01-01/02/03）升级后回填 1/2/3，新插入第 4 条取到 4；应用角色 `libiaolink_api` 不传 `seq_no` 插入由默认值分配 1、2，手填重复值被 `uq_projects_seq_no` 拦下。 |
| 文档同步 | `技术设计v0.2-架构与数据模型.md`（v0.2.3 修订记录 / §2.3 / §8.2）、`系统功能书.md`（修订行 + A1-12）、`前端功能需求.md`（多处 + Q13 + 变更记录）、`字段对照清单.md`（新增章节）、`shared/README.md`、`database/README.md`、本条目。 |
| 风险与后续 | 1）序号为全局连续序列（不分年度 / 地区），如需分段（如 `CNBJ-2026-0001` 式）需另行定口径；2）迁移与契约属高风险变更（CONTRIBUTING §15），需另一名评审人（`lan` / `wmj`）批准；3）`系统功能书.md` 约定与源文档双处同步，源文档不在仓库，需提出人补同步；4）后端实现 `POST /api/v1/projects` 时不要传 `seq_no`（走默认值），列表 `sort=seqNo` 需在服务端白名单校验；5）前端原型仍本地分配 seqNo，接接口后改为取响应值（`前端功能需求.md` 第六章第 13 条）；6）本次一并将未提交的 Push 36（首页筛选 id 化）内容随同一 PR 送入 `main`，Push 序号以入 main 时为准。 |
| 备注 | 推送 `px`；未直接推送 `main`。跨线：`shared/`（wmj 契约线）、`database/`（lan 迁移工具链 / wmj 项目领域）——请评审后合并。 |


## 2026-09-18 13:12 +08:00 | px | px | Push 36

| 字段 | 内容 |
|---|---|
| 类型 | `fix(frontend)`（筛选维度 id 化：`filter[manager]`（姓名）→ `filter[managerId]`（UUID）；模型按契约改 `code` / `managerId` + 显示名） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① `types.ts`：`Project.title` → `code`（契约 `projects.code`）、`manager` → `managerId`（契约 `projects.manager_id`，UUID），新增演示目录类型 `Manager`；② 新增 `data/managers.ts`：20 名项目经理的固定 UUID ↔ 姓名目录 + `managerName(id)` 查名（未知 id 回退返回 id）；`data/projects.ts` 20 条演示数据改为 `code` + `managerId`（其余字段不动）；③ `useHashRoute.ts`：筛选态字段 `managers` → `managerIds`，URL 参数 `filter[manager]` → `filter[managerId]`（多值英文逗号分隔，与契约同名同格式），解析 / 序列化 / `hasListFilters` 同步；④ `Home.tsx`：脏值兜底与过滤改按 `managerId`，搜索仍按项目经理姓名（经目录查名）；⑤ `CategoryFilterSidebar.tsx`：胶囊计数改按 `managerId` 分组、显示名经目录查得；⑥ `Card.tsx` 展示 props 改名 `code` / `managerName`；`AppHeader.tsx` 详情面包屑取 `project.code`；⑦ `NewProjectModal.tsx`：项目经理由自由文本改为目录下拉（产出 `managerId`、必选），草稿字段 `title` / `manager` → `code` / `managerId`；`App.tsx` 创建逻辑同步。 |
| 新增功能 | —（口径对齐与模型收敛，界面观感不变：卡片 / 面包屑 / 筛选胶囊仍显示姓名与编号文本） |
| 修复 / 重构 / 优化 | 消除前端与契约（Push 34 定案）的最后一处显性口径漂移：筛选 URL 参数与项目字段不再使用姓名 / `title`，改与 `shared` 契约的 `filter[managerId]` / `code` / `managerId` 同口径，为接 `GET /api/v1/projects` 预演。 |
| 测试与验证 | 无头 Chrome（CDP，1440×950，mock `/auth/me` + vite dev）24 项断言全过：冷启动 `共 20 个项目`、卡片显示编号与「项目经理：李伟」；勾选项目经理后 URL 写 `filter[managerId]=2f7c1a94-…`（未再出现 `filter[manager]=`）、计数「找到 1 个项目」、侧栏「已选 1 项」；带参刷新筛选与选中态保持、仅 1 张卡片；`filter[region]` 仍生效；旧 `filter[manager]=李伟` 链接不报错、不误筛（回到 20 条）；搜索「李伟」命中 1 条并写 `q`；新建弹窗项目经理下拉为「请选择 + 20 人」，提交后 `共 21 个项目`、新卡片显示编号与姓名、再按项目经理筛选命中 2 条；详情页面包屑显示 `code`、返回保留进入前筛选态；直接打开详情页返回落回 `#/`；全程无未捕获异常与 console.error。`npm run typecheck`、`npm run build` 通过（门禁在 `frontend/` 下复跑）。 |
| 文档同步 | `前端功能需求.md`（§一.3 参数映射表与 URL 兜底、§一.5 新建项目、§二.1 项目模型、§三.2 创建请求、§四.1 字段规则、§六第 9 条、变更记录）、本条目。 |
| 风险与后续 | 1）未改契约（`shared/`），本次只让前端对齐；2）旧 `filter[manager]`（姓名）链接按「未知参数」处理：不报错也不生效，如需平滑迁移可加姓名 → id 的过渡兼容；3）仍待契约增补：`filter[timeFrom]` / `filter[timeTo]`（见 `前端功能需求.md` 第六章第 11 条）；facets 五组 vs 侧边栏四组的差异仍在；4）演示数据 `id` 仍为 slug、`description` 尚未对齐契约 `name`，接接口时一并处理；5）20 条演示数据 `updatedAt` 仍集中在 2026-07-08，时间区间筛选证据不足（Push 32 遗留）。 |
| 备注 | 推送 `px`；未直接推送 `main`。本次只动前端（未改 `shared/` 契约），口径以 Push 34 契约与《前端功能需求》为准；`frontend/` 归 px（《团队分工》§4）。 |

## 2026-09-18 12:25 +08:00 | px | px | Push 35

| 字段 | 内容 |
|---|---|
| 类型 | `feat(db)` / `docs`（数据库基线跟随 v0.2.2 人员口径：新增 0002 迁移收敛 projects 人员字段；术语回写 v0.1） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① 新增 `database/migrations/0002_projects_manager.sql`（只追加，不改已合入的 0001）：a. `update projects set manager_id = owner_id where manager_id is null`（旧口径回填：0001 的 `owner_id` 旧注释为「负责人」，业务口径即项目经理；已有值不覆盖）；b. `drop index ix_projects_facets` + `alter table projects drop column owner_id`（显式删索引，避免隐式删除）；c. `alter table projects alter column manager_id set not null`；d. `create index ix_projects_facets on projects (region, project_type, manager_id)`。② `database/README.md` 同步：目录表补 0002 行（并标注 0001 人员字段为旧口径）、迁移命令示例输出改为 2 个迁移、幂等输出改「已执行 2 个迁移」、§与 v0.2 §2.3 的对应补「人员字段口径（v0.2.2）」说明、g3 验收行 `schema_migrations` 改 2 行。③ `技术设计v0.1-选型分析.md`：修订记录补 v0.1.8（术语更正：首页分类侧边栏「负责人」维度按业务两角色口径更正为「项目经理」，与 v0.2.2 §8 / 系统功能书 A1-18 对齐；正文历史结论不静默改写）。 |
| 新增功能 | —（结构收敛与文档对齐，无界面变化） |
| 修复 / 重构 / 优化 | 消除 g3 基线（Push 33）与 Push 34 契约（v0.2.2）之间的人员字段漂移：`projects.owner_id`（not null）/ `manager_id`（可空）/ `ix_projects_facets(owner_id)` → 单一必填 `manager_id`；「任务负责人」`tasks.owner_id` 不变。 |
| 测试与验证 | 真 PostgreSQL 18.4 端到端（官方 Windows 二进制包，临时集群 127.0.0.1:15432；临时资产不入仓库）：1）空库全流程——先跑 `roles/0001_roles.sql`（DO / GRANT 全部成功），再以 `libiaolink_migrator` 连库执行迁移器：`已执行 0001_baseline.sql（42 ms）`、`已执行 0002_projects_manager.sql（3 ms）`，`schema_migrations` 2 行、10 张业务表 + 迁移记录表；2）最终结构核对——`projects` 无 `owner_id`、`manager_id` 为 NOT NULL、`ix_projects_facets` 为 `(region, project_type, manager_id)`；3）幂等——重跑输出「数据库已是最新（已执行 2 个迁移，无漂移）」；4）旧数据路径——在只应用 0001 的库造两行旧口径数据（A：`owner_id` 有值 / `manager_id` 为空；B：两者都有且不同），执行 0002 后 A 回填为 `owner_id` 的值、B 保持原 `manager_id` 不被覆盖，`owner_id` 列删除、NOT NULL 生效；5）纯后端目录 + 文档改动，按 AGENTS.md 未跑 frontend 门禁。 |
| 文档同步 | `database/README.md`、`技术设计v0.1-选型分析.md`、本条目。 |
| 风险与后续 | 1）`database/` 归 wmj 主责（lan 协办，CODEOWNERS `/database/`），迁移属高风险变更（CONTRIBUTING §15）——合并前需 wmj 或 lan 批准；2）0002 为 0001 的收敛（contract）步：一期未上线、无生产数据，回填在空库为 no-op，正式部署按 Runbook 走 0001 → 0002 即可；3）g4 落地 Drizzle schema 时需与本基线（含 0002 后的最终结构）对齐，避免两套口径漂移（沿用 Push 33 后续项）。 |
| 备注 | 推送 `px`；未直接推送 `main`。触发背景：wmj 的 g3 基线（Push 33）与 px 的契约收敛（Push 34）之间存在 `projects` 人员字段漂移，本条目按契约侧收口并全库对齐。 |

## 2026-09-18 11:45 +08:00 | px | px | Push 34

| 字段 | 内容 |
|---|---|
| 类型 | `fix(contracts)` / `docs`（项目级人员字段收敛：删 `projects.owner_id`、`manager_id` 改必填；首页分类维度口径对齐为「项目经理」） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① 契约（`shared/src`）：`ProjectSchema` 删除 `ownerId`、`managerId` 改必填；`ProjectCreateBody` 的 `managerId` 改必传；`ProjectUpdateBody` 的 `managerId` 由可空改为不可置空；列表查询 `filter[ownerId]` → `filter[managerId]`（多值英文逗号分隔，与 region / projectType 同格式）；facets 的 `ownerId` 计数组 → `managerId`；② `shared/src/openapi.ts` 修正 POST /api/v1/projects 的 summary（「编号服务端生成」→「编号由创建人填写」，Push 30 遗留文案）；③ `shared/README.md` 新增「项目人员」口径行、camelCase 示例改 `manager_id ↔ managerId`、筛选示例补 managerId；④ `shared/generated/*` 由 `npm run generate` 重出（禁手改）；⑤ 文档对齐：`技术设计v0.2-架构与数据模型.md`（补 v0.2.2 修订行；§2.1 聚合说明、§2.3 DDL 与索引、§3.5 校验、§4.1 角色名、§8.1 维度表、§8.2 示例 URL 与索引、§8.3 迁移映射）、`系统功能书.md`（补修订说明；A1-18 维度「负责人」→「项目经理」并去掉「可切换」、A1-12 / B5-01 去掉「总负责人」）、`前端功能需求.md`（参数映射表、Project 模型、Q9 定案、Q10 facets 组名）、`字段对照清单.md`（项目经理口径：项目级必填、源表空值回填）、`团队分工.md`（project 模块归属描述）、`docs/adr/ADR-016-首页分类.md`（按 ADR 规则 4 记「待复核差异」）。 |
| 新增功能 | —（契约与文档口径收敛，无界面变化） |
| 修复 / 重构 / 优化 | 消除 `projects.ownerId` 与 `tasks.ownerId` 同名不同义：项目级唯一责任人为项目经理（`projects.managerId`，必填），任务级为任务负责人（`tasks.ownerId`）；顺带修掉 POST /projects summary 与 Push 30 口径不一致的文案。 |
| 测试与验证 | `shared`：`npm run generate`（源与生成物同 PR 提交）、`npm run check`（漂移校验）、`npm run typecheck` 全过；核对生成物 `ProjectCreateBody.required` 含 `managerId`、`ProjectUpdateBody` 的 `managerId` 可选且不可为 null、facets 组名为 `managerId`；`frontend`：`npm run typecheck`、`npm run build` 通过（本轮未改前端代码，按推送门禁复跑）。 |
| 文档同步 | 见「做了什么」第 ⑤ 项；本条日志。 |
| 风险与后续 | 1）`shared/` 属跨线改动（契约主责 wmj）：按《团队分工》§4 与 CONTRIBUTING §15（对外契约属高风险变更），合并前**必须取得非作者评审**；2）前端仍为旧口径：URL 写 `filter[manager]`（姓名），待 id 化改造（`filter[managerId]` + UUID + demo 人员表），见 `前端功能需求.md` 第六章第 9 条；3）Q11（项目时间区间）仍待定：筛哪个字段 / 参数命名 / 时区截断 / 是否允许单边区间；4）facets 多选计数语义（是否排除自身维度、0 值是否返回、离职人员是否保留）待定。 |
| 备注 | 推送 `px`；未直接推送 `main`。本条目原记 Push 33，合并期间 wmj 的 g3 数据库基线（Push 33，11:10）先入 main，按《开发日志》约定顺延为 Push 34（以入 main 时为准）。 |

## 2026-09-18 11:10 +08:00 | wmj | wmj | Push 33

| 字段 | 内容 |
|---|---|
| 类型 | `feat`+`docs`（g3 · S5·DB 基线：迁移 / 角色 / 迁移器；含主干同步） |
| 分支 | `wmj` |
| PR / commit | 分两笔：① 主干同步 `chore`（`origin/main` `fe8ba02 → 60d72d8`，PR #22 / PR #23 已合入后 wmj 跟进合并；`开发日志.md` 冲突按主干版本收口，消除「在后面 2」）；② g3 DB 基线提交。均推 `wmj`；未直接推送 `main`。 |
| 做了什么 | ① 新建 `database/` 独立 npm 包（唯一依赖 `pg@8.23`，不引入根 package.json）：`migrations/0001_baseline.sql` 把《技术设计v0.2-架构与数据模型.md》§2.3 的 10 张表（projects / project_stages / project_nodes / node_requirements / tasks / task_events / files / file_versions / change_requests / outbox_events）与全部索引落为可执行 DDL，另补两类（文件头注明差异）：稳定枚举的 CHECK 约束、4 条关系外键（`file_versions.change_request_id`、`files.current_version_id`、`files.task_id`、`tasks.change_ref`）；② `roles/0001_roles.sql`：三档最小权限角色——`libiaolink_migrator`（public 建表，唯一持 DDL）/ `libiaolink_api`（业务表增删改查 + 序列，无 DDL）/ `libiaolink_readonly`（只读），含 `alter default privileges` 默认授权与 `schema_migrations` 权限收回，幂等可重跑，密码不落仓库；③ `scripts/migrate.mjs`（Node 24 + pg）：只追加扫描 + `pg_advisory_lock` 串行化 + 逐文件事务 + `schema_migrations`（filename / checksum / applied_at / execution_ms）+ 漂移校验（改 / 删 / 中间插队一律拒绝，退出码 1），支持 `--dry-run`；④ `database/README.md`：目录、角色与密码注入、命令、不变式、与 §2.3 的对应、验收方式；⑤ 根 `README.md` 文档索引同步。 |
| 新增功能 | 数据库基线与迁移设施：空库一键迁移、已合入迁移不可变（校验和 + 序列前缀）、最小权限角色模型。 |
| 修复 / 重构 / 优化 | 验证中发现并修复迁移器一处缺陷：`migrations/` 目录为空时提前返回，导致「已记录迁移但文件被删」被误判为无事发生；改为始终连库校验漂移。 |
| 测试与验证 | **真 PostgreSQL 18.4 端到端验证**（官方 Windows 二进制包 18.4 解压到临时目录，`initdb` + `pg_ctl` 起本地集群 127.0.0.1:15432，均为临时资产、不入仓库）：1）**空库迁移成功**——按 README 顺序先跑 `roles/0001_roles.sql`（psql 输出 GRANT / ALTER DEFAULT PRIVILEGES / DO 全部成功），再以真实角色 `libiaolink_migrator` 连接执行迁移器：`已执行 0001_baseline.sql（42 ms）`；库内 11 张表（10 业务 + `schema_migrations`）、25 CHECK / 16 FK / 11 PK / 5 UNIQUE、28 索引，且 11 张表 `tableowner` 全为 `libiaolink_migrator`（迁移器校验和 98678b10…4ff8，与其它环境一致）；2）**权限模型（真实角色连接）**——`libiaolink_api` 可 insert / update / select 业务表，访问 `schema_migrations` 被拒、建表被拒（permission denied for schema public）；`libiaolink_readonly` 可 select、insert 被拒；3）**幂等**——重跑输出「数据库已是最新（已执行 1 个迁移，无漂移）」；4）**不可变三态**——改内容报「校验和不一致」、删文件报「文件缺失」、中间插队报「迁移顺序非法」，均退出码 1；5）**末尾追加**——`0002_probe.sql` 正常应用（4 ms）；6）**事务原子性**——`0003_bad.sql`（首条建表、次条必失败）报「迁移失败并已回滚」，`bad_tmp` 未落库、`probe_tmp` 在、记录表仍 2 行；7）**并发串行化（advisory lock）**——两个迁移器进程同时启动、待执行迁移含 `pg_sleep(3)`：B 先持锁执行 `0004_slow.sql（3003 ms）`，A 阻塞后读到最新记录并输出「数据库已是最新（已执行 3 个迁移，无漂移）」，无重复执行、退出码均 0，记录表 3 行无重复；8）纯后端目录 + 文档改动，按 AGENTS.md 未跑 frontend 门禁。 |
| 文档同步 | 新增 `database/`（README + 基线迁移 + 角色脚本 + 迁移器 + package.json / package-lock.json）；根 `README.md` 文档索引；本条目（`开发日志.md`）。 |
| 风险与后续 | 1）验证版本 PG 18.4 与 ADR-004 的目标主版本一致；正式部署仍需在目标环境按 Runbook 复跑空库迁移与角色创建（角色密码注入、备份恢复策略属部署阶段）；2）ADR-004 定的是 Drizzle：g4 工程骨架落地时需让 Drizzle schema 与本基线对齐（或由 Drizzle 生成后续迁移），避免两套口径漂移；3）一期不含种子数据、C9 业务字典表、问题 / 日报 / 干系人表（随后续模块切片）；4）迁移与角色属高风险变更（CONTRIBUTING §15），需 lan 或 px 批准后合并；5）CI 就绪后建议加「空库迁移 + 漂移校验」job（阶段 5 范围）。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。`database/` 在 CODEOWNERS 归 wmj + lan（wmj 主责），请 lan / px 评审；本期 g3 验收口径：空库迁移成功、已合入迁移不可变；Push 序号：原记 Push 31，合并期间 px 的 PR #25（Push 31，11:03）与 PR #26（Push 32，11:17）先入 main，按《开发日志》约定顺延为 Push 33（以入 main 时为准）。 |

## 2026-09-18 11:17 +08:00 | px | px | Push 32

| 字段 | 内容 |
|---|---|
| 类型 | `feat(frontend)`（首页筛选态写入 URL query：可分享、可收藏、刷新不丢） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① `frontend/src/useHashRoute.ts` 扩展为筛选态 store：`Route` 的 list 分支携带 `ListQueryState`（regions / projectTypes / managers / timeFrom / timeTo / q / sortDesc），新增 `parseListQuery` / `buildListHash` / `replaceListQuery` / `hasListFilters` / `listHref` / `goBackToList`，订阅改用 `useSyncExternalStore`（`hashchange` 驱动；`replaceState` 不触发 hashchange，故 store 内自行广播，并对 history 限流做一次静默重试）；② `frontend/src/Home.tsx` 删除筛选 / 搜索 / 排序的 `useState`，改为 URL 单一来源，并新增脏值兜底 effect（丢弃当前数据里不存在的地区 / 项目经理 / 项目类型与格式非法的日期，再把地址栏归一化）；③ `frontend/src/components/AppHeader.tsx`：logo 与「项目空间」按 `listHref()` 指向列表地址，普通左键点击走 `goBackToList()`（从列表进详情则 `history.back()` 保留原历史条目，否则跳到记忆的列表地址；带修饰键仍交给浏览器新开标签）；④ 侧边栏开合仍属界面状态（不写 URL），但带筛选参数的链接打开时默认展开。 |
| 新增功能 | 首页筛选态写入 URL query：地区 / 项目类型 / 项目经理 / 项目时间 + 关键字 `q` + 排序 `sort=updatedAt:asc`（默认降序省略）全部随地址栏；可分享、可收藏、刷新不丢；浏览器前进 / 后退在「列表（含筛选）↔ 详情」之间还原；参数命名与 `GET /api/v1/projects` 及 `/projects/facets` 契约同口径（多值英文逗号分隔、排序 `字段:方向`），为将来接接口预演。 |
| 修复 / 重构 / 优化 | 首页筛选 / 搜索 / 排序由内存态改为 URL 单一来源（消除刷新即丢）；详情页返回列表不再丢筛选态；过期或手改链接里不存在的取值不再造成「0 个项目且看不出原因」的死状态。 |
| 测试与验证 | 无头 Chrome（CDP，1440×950，mock `/auth/me` + vite dev）实测 29 项断言通过（首轮 19 项中 1 项为测试自身期望值写错，复验脚本已修正并确认）。覆盖：冷启动不写 URL（`hash=[]`）；勾选地区 / 类型后地址栏出现 `filter[region]=%E4%B8%AD%E5%9B%BD&filter[projectType]=T-sort`、计数切「找到 1 个项目」；带该地址刷新筛选保持、侧栏自动展开（已选 2 项）；搜索写入 `q=CNBJ`；升序写入 `sort=updatedAt:asc`；进详情（`#/project/cnbj-0001`）后顶栏「项目空间」链接携带全量筛选参数、点击返回后 URL 与筛选完全一致；四维度 + 搜索 + 排序的完整分享链接冷启动全部还原（已选 4 项、区间 2026/07/01 – 2026/07/31、搜索框 CNBJ、升序按下、命中 1 个）；区间外日期范围得 0 条并显示空态；脏链接（不存在的地区 / 类型、非法日期、未知 sort）打开后地址栏归一化为 `#/`、计数回到「共 20 个项目」；勾选 / 反选、清空搜索、重置均与 URL 双向同步（重置只清四个维度、保留排序）；浏览器后退 / 前进在列表与详情间正确还原。`npm run typecheck`、`npm run build` 通过。 |
| 文档同步 | `前端功能需求.md`（§一.3 新增「筛选态与 URL query」小节 + 参数映射表、§一.4 搜索补 `q`、§五 状态保持、§六 第 11 条更新 + 新增第 12 条、变更记录 Push 32）、本条目。 |
| 风险与后续 | 1）两处参数键名待契约对齐：项目经理为 `filter[manager]`（姓名），契约现为 `filter[ownerId]`（负责人 UUID）；时间区间用 `filter[timeFrom]` / `filter[timeTo]`，契约尚无该参数（建议同名增补）——已登记在 `前端功能需求.md` 第六章第 9、11 条，提请 wmj 在契约侧定档。2）筛选态用 `replaceState`，连续筛选只留一条历史记录（浏览器后退是离开列表、不是逐条回退筛选）；若产品上要逐条回退，改 `pushState` 即可。3）个别浏览器对 history 调用有限流（如 Safari），写入失败已静默重试，界面状态不受影响。4）演示数据 20 条的 `updatedAt` 全落在 2026-07-08，时间区间目前只能验证「全中 / 全不中」，日期打散后需复验（本轮未改数据，待确认）。5）不带参数时的「上次筛选记忆」（localStorage 兜底）未做，见 `前端功能需求.md` 第六章第 12 条。 |
| 备注 | 推送 `px`；未直接推送 `main`。`frontend/` 归 px（《团队分工》§4）；契约相关差异（`filter[manager]`、`filter[timeFrom]/[timeTo]`）已入文档，PR 描述一并提请评审。 |
## 2026-09-18 11:04 +08:00 | lan | lan | Push 31

| 字段 | 内容 |
|---|---|
| 类型 | `chore`（常驻分支同步：`lan` 合并最新 `main`，含 Push 重号顺延处理） |
| 分支 | `lan` |
| PR / commit | 直接推送常驻分支 `lan`（未直接推送 `main`）；合并提交 `70703e0`（Merge branch 'main' into lan，纳入 `4128454`、`fe8ba02`）；本日志条目随本次推送提交。 |
| 做了什么 | ① 将常驻分支 `lan` 同步到最新 `main`：`git merge main` 生成合并提交 `70703e0`，纳入 PR #21（g2 契约包：/api/v1 Zod 契约 + OpenAPI 3.1 + 前端类型生成与漂移校验，Push 28）与 PR #22（首页分类筛选侧边栏 + 项目类型徽标与配色 + 项目时间排序；顶栏层级与账号菜单样式；侧栏自绘滚动滑块，Push 29）；② `开发日志.md` 顶部冲突解决：保留全记录、最新在上（Push 31 → 30 → 29 → 28 → 27 → 26 → 25）；③ 重号处理：lan 原 Push 28 与 wmj 的 Push 28（PR #21，先入 main）重号，按 Push 27 条目约定「以先入 main 者为准、后合并方改号」，lan 该条顺延为 Push 30（条目备注中已注明）；④ 本条目（Push 31）随本次推送提交。 |
| 新增功能 | 无（分支同步，无代码改动）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）本分支本次改动为纯文档（合并冲突解决 + 本日志条目），按 AGENTS.md 约定未跑 frontend 门禁；2）合并校验：`git merge-base --is-ancestor origin/main lan` 通过（main 已全部并入），`lan` 与 `origin/main` 差异仅 `开发日志.md`（+30 行，Push 26 / Push 30 条目）；3）冲突解决校验：无冲突标记残留（0 处）、全文 CRLF、UTF-8 无 BOM；4）重号校验：全日志 Push 序号唯一（无重复）；5）推送前核对：`origin/lan`（`69e2a03`）是本地 `lan` 祖先，可快进推送（本次推送 4 个提交：`4128454`、`fe8ba02`、`70703e0`、本条日志）。 |
| 文档同步 | `开发日志.md`（本条目 Push 31）。 |
| 风险与后续 | 1）`lan` 已含 `main`（`fe8ba02`）全部内容，后续开发基于 `70703e0` 开展；2）Push 26 / Push 30 条目目前只在 `lan`，待后续 PR 合入 `main` 时随行；3）同步推送不含本分支新功能改动，无回滚风险。 |
| 备注 | 推送 `lan`；未直接推送 `main`。 |

## 2026-09-18 11:03 +08:00 | px | px | Push 31

| 字段 | 内容 |
|---|---|
| 类型 | `chore(ci)`（CI 扩展：shared 契约漂移检查入 GitHub Actions；仓库统一 LF 行尾修复 Windows 误报） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① `.github/workflows/ci.yml` 新增 `shared` job：Node 24 + `npm ci` + `npm run typecheck` + `npm run check`（对应 `shared/README.md` 登记的「阶段 5 · CI 扩展任务」；frontend job 的 Node 22 不动）；② 新增 `.gitattributes`（`* text=auto eol=lf`）：仓库文本文件统一 LF，修复 Windows（`core.autocrlf=true`）检出 CRLF 导致 `shared` 漂移检查误报的问题（索引内容本就是 LF，归一化后与生成器输出一致）；③ `shared/README.md` 同步「CI 已接入本检查」；④ `技术设计v0.1-选型分析.md` 三处「仓库现状 / CI」表述补 shared 检查（现状快照表、§4.19、§7.5.2）。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 修复 Windows 本地 `npm run check` 因 CRLF 误报契约漂移。 |
| 测试与验证 | 1）本地逐条复跑 CI 命令：`cd shared && npm ci && npm run typecheck && npm run check` 全过（漂移校验通过：paths=15、schemas=47）；2）`cd frontend && npm run typecheck && npm run build` 通过；3）新增 `shared` job 由本次推送后的 GitHub Actions 实际执行验证（结果见本 PR 检查项）。 |
| 文档同步 | `shared/README.md`、`技术设计v0.1-选型分析.md`（现状快照表 / §4.19 / §7.5.2）、本条目（`开发日志.md`）。 |
| 风险与后续 | 1）`.gitattributes` 生效后新检出为 LF；已有工作区的 CRLF 文件在下次重新检出时归正（索引无内容变更、不产生额外 diff）；2）`shared` 为新增 CI 门禁，其后所有 PR 都会跑契约漂移检查，失败按 CONTRIBUTING §14 重新生成并提交生成物；3）后续切片（file / preview / notify 等契约）按同模式纳入检查。 |
| 备注 | 推送 `px`；未直接推送 `main`。`.github/`、`deploy/` 归 px+lan（《团队分工》§4）；本条按指示直接纳入本 PR，lan 评审时可一并查看。 |

## 2026-09-18 11:00 +08:00 | px | px | Push 30

| 字段 | 内容 |
|---|---|
| 类型 | `feat(contracts)`（契约口径修订：项目编号改为创建人填写、建后可修改；新增错误码；契约口径回写前端文档） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① `shared/src/modules/projects.ts`：`ProjectSchema.code` 描述改为「创建人填写（建后可修改）；服务端只校验唯一性，不生成」；`ProjectCreateBodySchema` 新增必填 `code`（1~50 字符，示例 `CNBJ-20260708-0001`，格式仅前端提示、契约不做强校验）；`ProjectUpdateBodySchema` 新增可选 `code`（建后可改，更新时同样校验唯一性）；② `shared/src/common/errors.ts` 新增错误码 `PROJECT_CODE_EXISTS`（409），并同步《技术设计v0.2-架构与数据模型.md》§7.2 错误码表；③ `npm run generate` 重新生成 `shared/generated/openapi.json` 与 `api-types.d.ts`（`ProjectCreateBody.required` 含 `code`；`ProjectUpdateBody.properties.code` 为可选；`ErrorCode` 枚举含新码）；④ `shared/README.md` 定案口径表「项目编号」行改写为「创建人填写、建后可修改、唯一性由服务端保证（409 PROJECT_CODE_EXISTS）」；⑤ 《前端功能需求.md》回写契约口径：第一章创建流程、第二章 `accent` 行与第四章编号字段行改写，第六章 Q1 / Q2 标「已定案」、Q9 标「部分定案」（项目经理维度待对齐）、Q10 标「机制定案、取值待业务」、Q11 标「待契约增补」，变更记录新增一行。 |
| 新增功能 | 无（契约与文档口径修订；前端页面未改）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）`shared`：`npm run generate`、`npm run check`（OpenAPI 3.1 结构校验 paths=15 / schemas=47、两项生成物无漂移）、`npm run typecheck` 全过；2）生成物核验：`ProjectCreateBody.required = ["code","name","region","projectType","ownerId"]`、`ProjectUpdateBody.required = ["version"]` 且 `code` 可选；3）`frontend`：`npm run typecheck`、`npm run build` 通过（未改前端代码，按推送门禁复跑）；4）改动文件统一 LF 行尾（顺带消除 Windows 本地 check 的 CRLF 误报）。 |
| 文档同步 | `前端功能需求.md`、`shared/README.md`、`技术设计v0.2-架构与数据模型.md` §7.2、本条目（`开发日志.md`）。 |
| 风险与后续 | 1）契约线（`shared/`）属跨线改动：按《团队分工》§4，API 契约主责 wmj，请在 PR 中重点评审（对外契约属高风险变更，需非作者批准）；2）编号格式仅输入提示、契约不做强校验，若业务要强制「四大写字母-日期-四位序号」再补正则；3）遗留待对齐：Q9「项目经理」维度契约暂无 `filter[managerId]`（现只有 `ownerId`）、facets 五组 vs 侧边栏四组、Q11「项目时间」区间参数待契约增补；4）前端编辑入口（编号建后修改）为后续项，接入时把 `title` 映射到契约 `code`。 |
| 备注 | 推送 `px`；未直接推送 `main`。契约变更按《团队分工》§5 同一 PR 更新《前端功能需求.md》与生成客户端；本次为 px 侧提出的口径修订（编号由创建人填写、建后可修改），契约实现仍归 wmj 线评审。 |

## 2026-09-18 10:37 +08:00 | lan | lan | Push 30

| 字段 | 内容 |
|---|---|
| 类型 | `chore`（常驻分支同步：`lan` 合并最新 `main`） |
| 分支 | `lan` |
| PR / commit | 直接推送常驻分支 `lan`（未直接推送 `main`）；合并提交 `43ddff0`（Merge branch 'main' into lan，纳入 `db97bd5`）；本日志条目随本次推送提交。 |
| 做了什么 | ① 将常驻分支 `lan` 同步到最新 `main`：`lan` 与 `main` 分叉后（`lan` 独有 Push 26 日志、`main` 独有 PR #20）执行 `git merge main`，生成合并提交 `43ddff0`，纳入 PR #20（ADR-001~017 入 `docs/adr/`、索引与模板规则、`README.md` 文档索引同步，Push 27）；② `开发日志.md` 顶部冲突解决：保留 Push 26（lan）与 Push 27（wmj）全记录、最新在上（Push 27 → Push 26 → Push 25）；③ 本条目（Push 30）随本次推送提交。 |
| 新增功能 | 无（分支同步，无代码改动）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）纯文档改动，按 AGENTS.md 约定未跑 frontend 门禁；2）合并校验：`git merge-base --is-ancestor origin/main lan` 通过（main 已全部并入），`lan` 与 `origin/main` 差异仅 `开发日志.md`（+15 行，Push 26 条目）；3）冲突解决校验：无冲突标记残留（0 处）、全文 CRLF、UTF-8 无 BOM；4）推送前核对：`origin/lan`（`c6259b3`）是本地 `lan` 祖先，可快进推送（本次推送 3 个提交：`db97bd5`、`43ddff0`、本条日志）。 |
| 文档同步 | `开发日志.md`（本条目 Push 30）。 |
| 风险与后续 | 1）`lan` 已含 `main`（`db97bd5`）全部内容，后续开发基于 `43ddff0` 开展；2）Push 26 条目目前只在 `lan`，待后续 PR 合入 `main` 时随行；3）同步推送不含功能改动，无回滚风险。 |
| 备注 | 推送 `lan`；未直接推送 `main`。原编号 Push 28：因与 wmj 的 Push 28（PR #21，先入 main）重号，按 Push 27 条目约定「后合并方改号」，本次同步顺延为 Push 30。 |

## 2026-09-18 10:36 +08:00 | px | px | Push 29

| 字段 | 内容 |
|---|---|
| 类型 | `feat`（前端：首页「分类筛选」开关 + 左侧筛选侧边栏 + 自适应布局；卡片「项目类型」徽标） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | ① 新增 `frontend/src/components/CategoryFilterSidebar.tsx`：左侧「分类筛选」侧边栏（地区 / 项目经理两组多选胶囊 + 计数、底部「已选 N 项」与「重置」、头部关闭按钮；Esc 与窄屏遮罩点击均可关闭）；② `CategorySwitch` 改为受控组件（`checked` / `onChange`），开关即侧边栏开合（开启时紫色轨道 + 滑块右移旋转；未引入 styled-components，用 Tailwind 任意值还原）；③ `Home.tsx` 接入筛选状态与组合过滤（地区 + 项目经理，与关键词搜索叠加生效）；计数在有筛选 / 搜索时显示「找到 N 个项目」，无结果时按情况给出「清空搜索 / 清空筛选」；④ 页面自适应：桌面端（≥md）侧边栏把内容推开 280px（`md:pl-[304px]`，300ms 过渡），卡片网格改为容器查询（`@container` + `@md/@4xl/@6xl`），随内容宽度自动 1 / 2 / 3 / 4 列（侧边栏展开时 4 列自动降为 3 列）；窄屏（<md）侧边栏为遮罩浮层、内容不移位；⑤ 数据模型新增 `region` 字段（`types.ts`、`projects.ts` 20 条演示数据，新建项目默认「未分类」），对齐 `技术设计v0.2-架构与数据模型.md` §8 的 `projects.region`；⑥ 搜索匹配范围新增地区；⑦ 新增项目类型：`types.ts` 定义 `PROJECT_TYPES`（T-sort / 3D分拣 / 飞箱）与 `Project.projectType`，20 条演示数据补齐类型，卡片左上角新增类型徽标（底色复用主题色 `accent`，与小车图标一致；amber 卡用深色字保证可读），新建项目弹窗新增类型下拉（默认 T-sort），`App.tsx` 创建时写入 `projectType`，搜索匹配范围同步新增项目类型；⑧ 左侧筛选侧边栏新增「项目类型」维度（T-sort / 3D分拣 / 飞箱，按 `PROJECT_TYPES` 固定顺序展示计数，与地区 / 项目经理多选叠加生效，「重置」与「已选 N 项」同时覆盖三组）；⑨ 分类筛选开关上移（内容区顶部、距顶栏约 16px）并改为 `sticky top-16` 滚动吸附（页面底色 `#f5f6f8` 打底、层级 z-10 低于侧边栏 z-20），下滑浏览时始终可见可点；⑩ 卡片配色改为按项目类型固定：`types.ts` 新增 `PROJECT_TYPE_ACCENTS`（T-sort=blue / 3D分拣=emerald / 飞箱=amber）并把 `CardAccent` 收窄为 blue / emerald / amber，20 条演示数据与新建项目统一按类型取色（`App.tsx` 去掉轮转配色），`Card.tsx` 中 rose / violet 样式改为注释保留（未来新增类型可直接启用）；⑪ 布局调整：`AppHeader` 新增可选 `title`（首页在 logo 右侧显示「项目空间」，竖线分隔、字号保持 `text-2xl`，<640px 隐藏，项目详情面包屑分支不变），`Home.tsx` 去掉内容区标题块，改为「分类筛选开关 + 项目计数 + 新建项目 + 搜索框」同层工具条，窄屏自动换行且按钮不再被拉通；⑫ 筛选侧边栏三个分组标题（地区 / 项目类型 / 项目经理）由 `text-xs font-medium text-zinc-400` 加重为 `text-sm font-semibold text-zinc-700`，层级更清晰；⑬ 筛选侧边栏新增「更新时间」维度：按 `updatedAt` 的 `YYYY-MM` 分月（倒序）生成胶囊并带计数，与其他维度同为多选叠加，纳入 `hasFilters`、「已选 N 项」与重置；⑭ 按反馈把时间维度换成日期区间选择器：新增 `frontend/src/components/DateRangePicker.tsx`（自研、无新增依赖；触发按钮显示「全部时间」或区间，弹层为固定定位月历，支持翻月、区间高亮、清除 / 确定，Esc 与外部点击关闭，默认打开到最近数据月份），侧边栏分组改名「项目时间」；列表改为按 `updatedAt` 降序排序（最新在前）；⑮ 工具条新增「降序 / 升序」分段按钮（紧随分类筛选开关，当前项深色高亮，切换即时重排）。⑯ 修复账号菜单被遮挡：顶栏层级由 `z-10` 提升为 `z-30`（高于内容区工具条 `z-10` 与筛选侧边栏 / 窄屏遮罩 `z-20`，低于日期区间弹层 `z-40`、任务抽屉与新建项目弹窗 `z-50`），内容区吸附工具条保持 `z-10`；账号菜单展开后不再被工具条或侧边栏遮住。⑰ 按反馈把两处侧边面板的滚动条改成「滚动才浮现」：新增 `frontend/src/components/ScrollArea.tsx`——隐藏原生滚动条并自绘悬浮滑块（`ResizeObserver` + `scroll` 监听同步滑块高度与位置，滚动时置 `opacity-100`、停止 900ms 后淡出，支持拖动滑块，悬停滑块时显现，内容未溢出时不渲染滑块），`app.css` 新增 `.scrollbar-hidden` 隐藏原生滚动条，应用到 `CategoryFilterSidebar` 主体与 `TaskDrawer` 字段区。注：Chrome 的自定义原生滚动条不随悬停 / 样式变化重绘（无头与真实窗口均实测），纯 CSS 方案无法做到「滚动才浮现」，故改为自绘滑块。⑱ 账号菜单观感加强：菜单面板由 `w-32` / `rounded-lg` / 单层阴影改为 `w-36` / `rounded-xl` / `border-zinc-200/80` / 双层柔和阴影（`0_18px_40px_-12px` + `0_4px_12px_-4px`）并加 `p-1`，菜单项改为圆角块（`rounded-lg px-3 py-2`）浅灰高亮；`app.css` 新增 `.menu-pop`（`ll-menu-in` 150ms 下移淡入，`transform-origin: top right`，并在 `prefers-reduced-motion` 下禁用）；头像按钮悬停加 `hover:text-zinc-900` 与浮起阴影、按下时收敛。 |
| 新增功能 | 首页「分类筛选」开关 + 左侧筛选侧边栏（地区 / 项目经理多选、计数、重置；桌面推挤、窄屏浮层、网格自动降列）；卡片「项目类型」徽标（T-sort / 3D分拣 / 飞箱）、新建表单类型下拉，筛选侧边栏新增「项目类型」维度（三组维度可叠加）；`ScrollArea` 组件（自绘悬浮滚动滑块，筛选侧边栏与任务抽屉共用）。 |
| 修复 / 重构 / 优化 | `Project` 模型补 `region`；网格由视口断点改为容器查询，实现侧边栏展开时自动降列。 |
| 测试与验证 | 1）`npm run typecheck` 通过；2）`npm run build` 通过（vite v8.3.0；产物 CSS 已核对含 `md:pl-[304px]`、`transition-property:padding-left`、容器查询规则与开关动画色值）；3）无头浏览器实测（Chrome headless + 本地代理挂演示会话，截图时注入 `transition:none` 保证确定性）：收起态 4 列 + 开关关闭；打开态侧边栏推挤内容且网格自动降为 3 列；在侧边栏选中「中国」后计数变为「找到 1 个项目」、仅剩 1 张卡片、底部显示「已选 1 项」；480px 窄屏为遮罩浮层；4）说明：无头虚拟时间下 CSS 过渡可能不推进，属测试环境现象（真实浏览器不受影响），故验证时禁用过渡；5）项目类型复验：`npm run typecheck`、`npm run build` 通过；无头浏览器截图（1600px 宽 4 列 + 放大视角）确认 20 张卡片左上角均显示类型徽标、底色与小车图标一致（五种主题色），类型分布 T-sort 7 / 3D分拣 7 / 飞箱 6；6）侧边栏复验：无头 Chrome 经 CDP 实测——侧边栏展开后展示地区 / 项目类型 / 项目经理三组（项目类型计数 T-sort 7 / 3D分拣 7 / 飞箱 6），选中「飞箱」后计数变为「找到 6 个项目」、仅剩 6 张卡片、底部显示「已选 1 项」；7）吸附复验：无头 Chrome 经 CDP 把页面滚到 545px 后，开关实测位置 top=80px / bottom=116px（视口内可见），点击仍可正常展开侧边栏；8）配色复验：`npm run typecheck` / `npm run build` 通过；无头浏览器截图确认 20 张卡片只出现蓝（T-sort）/ 绿（3D分拣）/ 橙（飞箱）三种颜色，小车图标与左上角类型徽标同色，无红 / 紫卡片；9）布局复验：1440px 宽实测顶栏「项目空间」与工具条四项同行（开关 + 计数在左、按钮 + 搜索在右），项目详情页顶栏面包屑未受影响；420px 宽实测工具条按行换行、无横向溢出；10）侧边栏标题复验：截图确认三个分组标题字号与字重提升、与胶囊区区分明显；11）时间维度复验：侧边栏滚到底部可见「更新时间」组（演示数据 2026-07 共 20 条），点击该胶囊后底部显示「已选 1 项」、列表按月份条件过滤；12）区间选择器复验：无头 Chrome 经 CDP 打开弹层（默认 2026 年 7 月）→ 点选 1 日与 8 日 → 「确定」后触发按钮显示 `2026/07/01 – 2026/07/08`、底部显示「已选 1 项」，列表按项目时间降序（10 / 20 / 16 / 09 …）。注：演示数据项目时间同为 2026-07-08，区间筛选只能演示到「全部命中」，需打散演示时间才能看出部分命中；13）排序按钮复验：默认「降序」时首位为 10（17:41），点「升序」后首位变为 01（08:15）且按钮高亮切换；14）账号菜单层级复验：无头 Chrome 经 CDP 在 1400×620 与 420×900（筛选侧边栏展开）两种场景点击账号菜单，对「退出登录」条目中心做 `elementFromPoint` 命中测试均返回条目自身（未被遮挡），截图确认菜单完整显示在工具条与侧边栏之上；15）滚动条复验：真实窗口 Chrome（远程调试 + CDP，窗口移出可视区）实测——两处滑块静止时 `opacity: 0`，悬停不动仍为 0（符合「滚动才浮现」口径），滚轮滚动后置 1 且位置随滚动更新（滑块高度按 `clientHeight / scrollHeight` 计算，抽屉 620px 视口下 217px），停止滚动 1.6 秒后回到 0；拖动滑块实测 `scrollTop` 由 160 增至 276（到底）且滑块同步下移；裁剪放大截图确认滑块为 6px 圆角细条；16）账号菜单复验：无头 Chrome 经 CDP 展开菜单（144×46）并截图放大 3 倍确认——面板为圆角 + 双层柔和阴影，菜单项悬停显示圆角浅灰高亮，动画结束后位置稳定；`npm run build`（含 typecheck）通过。 |
| 文档同步 | `前端功能需求.md`（§一.3 首页说明与网格口径与侧边栏三组维度、§一.4 搜索新增项目类型、§一.5 新建项目表单四项、§二.1 数据模型新增 `region` 与 `projectType`、§五 状态保持、§六 新增待确认项 9 / 10、变更记录 Push 29）；`开发日志.md`（本条目）。 |
| 风险与后续 | 1）筛选维度暂为地区 / 项目经理（前端演示数据口径）；后端 facets 与 `filter[...]` 契约（技术设计 v0.2 §8.2）就绪后切换为服务端过滤；2）筛选态尚未写入 URL query（v0.2 §8.2 要求可分享 / 收藏），列入后续；3）`region` 演示值为地区名，字典与「未分类」口径以后端为准；4）项目类型当前为前端枚举（T-sort / 3D分拣 / 飞箱），取值与中英文展示待 C9 字典确认；筛选侧边栏已含「项目类型」维度（地区 / 项目类型 / 项目经理），后端 facets 需按同口径返回三组计数；5）卡片配色与项目类型绑定（蓝 / 绿 / 橙），红 / 紫为预留未启用，新增类型时在 `PROJECT_TYPE_ACCENTS` 与 `ACCENT_STYLES` 放开注释即可。 |
| 备注 | 推送 `px`；未直接推送 `main`。其余滚动区域（详情页「筛选（列显隐）」下拉、首页令牌声明折叠区）按反馈暂保留原生滚动条。 |

## 2026-09-18 10:42 +08:00 | wmj | wmj | Push 28

| 字段 | 内容 |
|---|---|
| 类型 | `feat`+`docs`（g2 · S5·契约包：/api/v1 契约 + 统一错误模型 + 生成链路；含主干同步） |
| 分支 | `wmj` |
| PR / commit | 分两笔：① 主干同步 `chore`（`origin/main` `de49b06 → db97bd5`，PR #20 已 squash 合并后 wmj 跟进合并，消除「在后面 1」）；② g2 契约包提交。均推 `wmj`；未直接推送 `main`。 |
| 做了什么 | ① 新建 `shared/` 契约包（独立 npm 包，不引入根 package.json、不预设 workspace —— 待 v0.1 Q15 定案）：`src/common`（错误码与统一信封、Uuid/时间/版本/幂等键/分页/排序、阶段/成果文件/状态机枚举）+ `src/modules`（projects 含首页 facets、tasks 含四格进度、flow 含蓝图 JSON/节点/完成门禁）；② `src/openapi.ts` 注册 **18 个 /api/v1 操作（15 条路径）**，生成 `generated/openapi.json`（OpenAPI 3.1，47 个 schema）；③ 生成 `generated/api-types.d.ts`（前端类型，openapi-typescript）；④ `scripts/`：generate（生成物）与 check-drift（漂移 + 结构校验），全部 Node 24 原生 TS 运行（无需构建）；⑤ `shared/README.md`：目录、命令、生成链路、已定案口径表、后续切片；⑥ 根 `README.md` 文档索引同步。 |
| 新增功能 | 契约层：项目/任务/流程节点与蓝图三组接口契约；统一错误模型（15 个错误码 + HTTP 状态映射）；常量（Uuid/分页/排序/乐观锁/幂等键/时间）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）`npm run typecheck` 通过（tsc 严格模式）；2）`npm run generate` 输出 openapi.json（65,295 bytes）+ api-types.d.ts（49,511 bytes）；3）`npm run check` 通过——OpenAPI 3.1 结构校验（paths=15、schemas=47）与两项生成物漂移校验均为「一致」；4）生成质量抽查：错误码枚举、`filter[...]` 查询参数、TaskProgress 四格、Project.required 字段均正确；5）**新增依赖（需评审）**：zod 4.6、@asteasolutions/zod-to-openapi 9.1、openapi-typescript 7.13、typescript 5.9（均随 package-lock.json 提交）；6）本包无单测（纯 schema 与生成链路，验证以生成 + 漂移校验为准）。 |
| 文档同步 | 新增 `shared/`（README + 源码 + 生成物 + 锁文件）；根 `README.md` 文档索引；本条目（`开发日志.md`）。 |
| 风险与后续 | 1）契约漂移检查尚未接入 CI（阶段 5 · CI 扩展任务负责；在那之前作者手动跑 `npm run check`）；2）类型版本差异：shared 用 TypeScript 5.9（openapi-typescript peer 要求 ^5.x），前端为 7.0.2 —— 已登记 ADR-017「待复核差异」；3）契约确认的两项前端口径（项目编号服务端生成、主题色随字典）建议由 px 回写《前端功能需求.md》；4）后续切片：文件/预览、通知、搜索/统计、迁移、自动化与日报问题（随对应模块落地）；5）file 模块契约由 lan 主责实现、契约仍归本线维护。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。契约包按《团队分工》§4（wmj 主责、lan/px 评审）；新增依赖按 CONTRIBUTING §15 属高风险变更，请在 PR 中重点评审。 |

## 2026-09-18 10:16 +08:00 | wmj | wmj | Push 27

| 字段 | 内容 |
|---|---|
| 类型 | `docs`（g1 · S5·ADR：技术选型定档 ADR-001~017 + 索引入库） |
| 分支 | `wmj` |
| PR / commit | 随本 PR（wmj → main）提交；未直接推送 `main`。推送前已同步主干：`origin/wmj` = `origin/main` = `de49b06`。 |
| 做了什么 | ① 新建 `docs/adr/`：索引 README（清单 + 状态流转 + 模板与编写规则）；② 按《技术设计v0.1-选型分析》§0.1 / §4 / §5 / §9 固化 16 条选型 ADR（交付形态、总体架构、语言框架、数据库与数据访问、队列与调度、对象存储、在线预览、三维与 CAD、消息通道、认证与组织、权限、前端、部署与容量、测试与 CI、流程节点与蓝图、首页分类）；③ 新增 ADR-017「版本基线与依赖升级策略」，收口 v0.1 §4.20 与 CONTRIBUTING §13/§14（版本基线表 + 升级规则）；④ 根 `README.md` 文档索引同步。 |
| 新增功能 | 无（纯文档）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）纯文档改动，按 AGENTS.md 约定未跑 frontend 门禁；2）逐条核对：ADR 的决策、备选与切换触发条件对照 v0.1 §0.1 / §4.x / §5 决策汇总表；3）发现并在 ADR-017「待复核差异」显式记录两处口径差异——TypeScript 版本（v0.1 §4.20 写 5.9.x vs 仓库现状 7.0.2）、包管理器（§4.20 写 pnpm vs 仓库现状 npm，Q15 未定案）——未静默选择，待评审定案；4）编码与行尾：新增 18 个文件均为 UTF-8 + LF，与仓库现状一致。 |
| 文档同步 | 新增 `docs/adr/`（18 个文件）；`README.md` 文档索引；本条目（`开发日志.md`）。 |
| 风险与后续 | 1）ADR 状态为「草案」：需 lan / px 评审，合并 main 后转「已采纳」；2）g2 契约包（Zod / OpenAPI / 生成客户端）以 ADR-003 / 004 / 017 为输入，下一步启动；3）Push 序号说明：远端当前最高为 26（`origin/lan`，未入 main），本次顺延取 27；若与其他分支重号，以先入 main 者为准、后合并方改号。 |
| 备注 | 推送 `wmj`；未直接推送 `main`。ADR 按《团队分工》§4（wmj 主责、全员评审）；`docs/` 目录主理人为 px，本次改动请 px 一并评审。 |

## 2026-09-18 09:36 +08:00 | lan | lan | Push 26

| 字段 | 内容 |
|---|---|
| 类型 | `chore`（常驻分支同步：`lan` 快进到最新 `main`） |
| 分支 | `lan` |
| PR / commit | 直接推送常驻分支 `lan`（未直接推送 `main`）；快进 `896ce2d → de49b06`；本日志条目随本次推送提交。 |
| 做了什么 | ① 将常驻分支 `lan` 更新到最新 `main`：`git merge --ff-only main` 快进 `896ce2d → de49b06`（`lan` 为 `main` 祖先、无独有提交、无冲突），带入 4 个已合并提交——PR #16（系统功能书 V1.1 + 任务状态五态）、#17（两篇技术设计入仓）、#18（团队分工 v1.0）、#19（分工路由落地：AGENTS.md 分工章节 + CODEOWNERS 按模块细化）；② 本条目（Push 26）随本次推送提交。 |
| 新增功能 | 无（分支同步，无代码改动）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）纯文档改动（新增本日志条目），按 AGENTS.md 约定未跑 frontend 门禁；2）快进前置校验：`git merge-base --is-ancestor lan main` 通过、`main..lan` 独有提交为空，确认无分叉、无冲突；3）推送前核对：本地 `lan` 提交与 `origin/main` 一致（`de49b06`）；4）行尾 / 编码核对：本文件保持 CRLF、UTF-8 无 BOM。 |
| 文档同步 | `开发日志.md`（本条目 Push 26）。 |
| 风险与后续 | 1）本分支后续开发基于 `de49b06` 开展，后端平台模块（file / preview / notify / outbox / search / dashboard / 迁移工具链）按 `AGENTS.md`「分工与路由」执行；2）同步推送不含功能改动，无回滚风险。 |
| 备注 | 推送 `lan`；未直接推送 `main`。 |

## 2026-09-17 17:42 +08:00 | px | px | Push 25

| 字段 | 内容 |
|---|---|
| 类型 | `docs`（分工路由落地：AGENTS.md 分工章节 + CODEOWNERS 按模块细化） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。Push 24（PR #18）已 squash 合并，本分支已同步主干。 |
| 做了什么 | ① `AGENTS.md` 新增「分工与路由（谁负责哪块）」章节：团队角色一行（px = 前端 + 运维 / 交付负责人；wmj = 后端 A 领域负责人兼技术负责人；lan = 后端 B 平台负责人）+ 范围→主责→评审表（`frontend/`、`assets/`、`deploy/`、`.github/`、`docs/` 与根目录文档 → px；后端领域 identity/org、project、blueprint/node、task、report/issue、stakeholder、automation、admin → wmj；后端平台 file、preview、notify、outbox、search、dashboard、迁移工具链 → lan；API 契约（Zod / OpenAPI / 生成客户端）、ADR、`技术设计*.md` → wmj；`系统功能书.md` → 变更提出人），并写明代理动手前按表判断归属、跨线改动先对齐、后端目录未落地前先落文档与契约、高风险变更需他人评审；② `.github/CODEOWNERS` 由全员 `*` 兜底细化为模块规则：`frontend/`、`assets/`、`docs/` 与根目录 6 个文档 → px(+wmj)，`deploy/`、`.github/` → px+lan，`技术设计v0.1` → wmj(+px)、`技术设计v0.2` → wmj+lan(+px)，`AGENTS.md`、`CONTRIBUTING.md` → 全员，`server/`、`database/`、`shared/` → wmj+lan（目录落地后自动生效）；兜底 `*` 保持全员，每条具体路径 ≥2 名所有者；③ `团队分工.md` 升 v1.1：§5.1 由「建议细化」改为「已细化（随 Push 25 落地）」并列路由表与说明；④ `CONTRIBUTING.md` 附录 A 第 2 条补注「CODEOWNERS（评审自动路由；已按模块细化，路由表见 `团队分工.md` §5.1）」。 |
| 新增功能 | 无（文档与仓库配置）。 |
| 修复 / 重构 / 优化 | 无代码改动。 |
| 测试与验证 | 1）纯文档 / 仓库配置改动，按 AGENTS.md 约定未跑 frontend 门禁，PR 的 CI `frontend` 仍会执行；2）行尾 / 编码核对：`AGENTS.md`、`.github/CODEOWNERS`、`CONTRIBUTING.md`、`团队分工.md`、`开发日志.md` 均 CRLF、无松散 LF、UTF-8 无 BOM；3）内容核对：路由表与 `团队分工.md` §5.1 和第 3~4 节模块归属逐条一致；owner 账号与 `.github/CODEOWNERS` 现有成员（px=@256-code、wmj=@suikiiovo、lan=@XSZR341）一致；4）CODEOWNERS 语法：按「后匹配生效」排序，具体路径写在 `*` 之后。 |
| 文档同步 | `AGENTS.md`「分工与路由」；`.github/CODEOWNERS` 模块规则；`团队分工.md` v1.1（§5.1 + 变更记录）；`CONTRIBUTING.md` 附录 A 第 2 条；`开发日志.md`（本条目 Push 25）。 |
| 风险与后续 | 1）按模块路由后 PR 只自动请求对应模块所有者，兜底 `*` 仍请求全员；2）`server/`、`database/`、`shared/` 规则先行登记，后端落地首个 PR 时验证触发；3）`团队分工.md` §5.1 与 `AGENTS.md` 同口径，后续调整需两处同步。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 17:34 +08:00 | px | px | Push 24

| 字段 | 内容 |
|---|---|
| 类型 | `docs`（新增《团队分工》并入仓库根） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。注意：Push 23 随 PR #17 提交。 |
| 做了什么 | 新增 `团队分工.md`（v1.0，136 行）到仓库根：① 角色到人——px（前端 + 运维，交付负责人）、wmj（后端 A：领域负责人兼技术负责人）、lan（后端 B：平台负责人），含 GitHub 账号与常驻分支；② 后端 14 个模块的主责 / 评审（按 `技术设计v0.2-架构与数据模型.md` §1.2：wmj 负责 identity/org、project、blueprint/node、task、report/issue、stakeholder、automation、admin，lan 负责 file、preview、notify、outbox 队列调度、search/dashboard、迁移工具链）；③ 功能书 A1~D5 各模块的前端 / 服务端交付人；④ 契约与文档主责（API 契约 = wmj；前端功能需求 / 字段对照清单 = px；系统功能书 = 变更提出人；技术设计与 ADR = wmj；部署 Runbook = px）；⑤ 协作流程（分支 / 日志 / 评审 / 合并窗口 / 联调顺序 / 提测 / CI 分层 L1~L3 / 工期预期）与 CODEOWNERS 细化建议；⑥ 阶段 0 九项 PoC 的主责、协办与验收证据；⑦ 运维值班、单点风险与消减、修订流程。同步更新 `README.md` 文档表与 `CONTRIBUTING.md` 附录 A 第 3 条文档清单。 |
| 新增功能 | 无（文档）。 |
| 修复 / 重构 / 优化 | 无代码改动。 |
| 测试与验证 | 1）纯文档改动，按 AGENTS.md 约定（纯文档改动除外）未跑 frontend 门禁，PR 的 CI `frontend` 仍会执行；2）文件校验：UTF-8、CRLF 一致（无松散 LF）、无遗留占位符；3）内容核对：模块划分逐条对齐 `技术设计v0.2-架构与数据模型.md` §1.2；PoC 清单对齐 `技术设计v0.1-选型分析.md` §7.3；分支 / 评审 / 发布规则对齐 `CONTRIBUTING.md` 第 5 / 11 / 15 / 16 / 17 节；GitHub 账号取自 `.github/CODEOWNERS`。 |
| 文档同步 | 新增 `团队分工.md`；`README.md` 文档表 +1 行；`CONTRIBUTING.md` 附录 A 第 3 条补入；`开发日志.md`（本条目 Push 24）。 |
| 风险与后续 | 1）角色到人的绑定（后端 A = wmj、后端 B = lan）如与实际分工不符，按文档第 9 节修订并同步日志；2）CODEOWNERS 按模块细化（frontend → px；server/database → wmj、lan；deploy/.github → px、lan）待后端目录落地后作为独立 PR 执行；3）本文件只细化「谁负责什么」，与 `CONTRIBUTING.md` 冲突时以更严格者为准。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 17:25 +08:00 | px | px | Push 23

| 字段 | 内容 |
|---|---|
| 类型 | `docs`（两篇技术设计入仓仓库根） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。Push 22（含 Push 20 / 21 内容）已经 PR #16 squash 合并进 main（9627ab2，2026-09-17 17:24）；本条目为新的 PR。 |
| 做了什么 | 1）把《技术设计 v0.1（选型分析篇）》《技术设计 v0.2（架构与数据模型篇）》从微信文件缓存目录入仓到仓库根，文件名去掉下载副本后缀：`技术设计v0.1-选型分析.md`（入仓后版本 v0.1.7）、`技术设计v0.2-架构与数据模型.md`（入仓后版本 v0.2.1）（入仓复制时与源文件逐字节一致）；2）文档内按入仓现状更新 8 处（含修订记录 v0.1.7 / v0.2.1）：v0.1 头部补「入仓」行、1.5 现状快照补「技术设计」行、7.1 文档分层说明改写、8.2 Q16 标记已办、第 9 节「文档入仓」标记已办；v0.2 头部补「入仓」行；3）仓库文档索引同步：`README.md` 文档表新增两行，`CONTRIBUTING.md` §17 Definition of Done 的文档清单与附录 A 第 3 条补入《系统功能书》与两篇技术设计。 |
| 新增功能 | 无（文档入仓）。 |
| 修复 / 重构 / 优化 | 无代码改动；文档分层口径统一为「仓库根：系统功能书 / 前端功能需求 / 字段对照清单 / 开发日志 / 技术设计 v0.1、v0.2；docs/：SSO 等专题」。 |
| 测试与验证 | 1）入仓校验：复制后逐字节比对 `equals=true`（v0.1 85,641 bytes / v0.2 33,384 bytes）；2）编辑校验：8 处替换逐一断言「匹配次数=1」后才写入，其余内容零改动；3）门禁复跑（本 PR 含 Push 22 代码改动）：`npm run typecheck` 通过、`npm run build` 通过（vite v8.3.0，31 modules transformed，`index-c5yzkE9A.css` 37.65 kB / `index-D-vQS2vc.js` 274.00 kB，899ms——与 Push 22 产物哈希一致，无漂移）；CI `frontend` 在 PR #16 通过（Push 22）；本 PR 的 CI 结果以 PR 页面为准。 |
| 文档同步 | `README.md`、`CONTRIBUTING.md`、`技术设计v0.1-选型分析.md`、`技术设计v0.2-架构与数据模型.md`、`开发日志.md`（本条目 Push 23）。 |
| 风险与后续 | 1）微信缓存目录的源文件与仓库副本自此分叉，后续以仓库根版本为唯一口径（再分发从仓库导出）；2）两篇仍为讨论稿（v0.1.7 / v0.2.1），冻结 v1.0 基线时按 8.2 待确认项（如 Q28）更新；3）技术设计与代码的一致性自本次起纳入 §17 Definition of Done 维护范围。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 17:21 +08:00 | px | px | Push 22

| 字段 | 内容 |
|---|---|
| 类型 | `feat`（任务状态五态）+ `docs`（四份文档口径对齐） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 1）任务状态由 3 态扩为 5 态派生展示：`frontend/src/data/tasks.ts` 新增 `TaskStatus`（待开始 / 进行中 / 已完成 / 提前完成 / 已延期）、`isTaskDoneEarly()`、`taskStatus()`；`TaskBoard.tsx` 状态列改用 `taskStatus()` 并按态着色（已完成 / 提前完成 = 绿、进行中 = 蓝、已延期 = 红、待开始 = 灰）；`TaskDrawer.tsx` 状态胶囊同步 5 态，并移除与状态重复的头部「已逾期」胶囊。2）口径：逾期不进入任务状态枚举——红色「逾期」仍标注在实际完成日期（表格列 + 抽屉字段），抽屉进度区保留「已超过预计完成日期…」红色提示；「已延期」＝未完成且已过预计完成日期（派生展示态，不写回）；「提前完成」＝完成且实际完成日期早于预计完成日期；逾期补完按「已完成」（是否按时交付 = 否，不回退「提前完成」）。3）文档对齐：`前端功能需求.md`（§6.3 状态列、§6.5 抽屉、§二 ProjectTask.status 与状态派生规则、§四 status 规则、变更记录 Push 22）；`字段对照清单.md`（任务状态行 + 数据质量第 2 条）；`系统功能书.md` 与源文档同口径 6 处（A1-01、A1-06、A2 状态机图与口径说明、A2-09、B2-01）；技术设计 v0.2（§2.3 补 tasks / task_events DDL 与 projects.manager_id、§2.2 关系、§2.4 任务状态规则）；技术设计 v0.1（星级进度→四格进度、1.5 现状快照补任务状态口径、4 处文档路径与仓库结构按现状修订）。 |
| 新增功能 | 任务状态新增「提前完成」「已延期」两态（由日期字段派生展示，接口仍可只回传基础态 待开始 / 进行中 / 已完成）。 |
| 修复 / 重构 / 优化 | 修复状态与日期矛盾行的展示：t11「人员进场、场地检查、施工对接」源表状态=待开始但已完成（8月11日，早于预计完成日期 9月2日），现按「提前完成」显示，与《字段对照清单》数据质量记录一致。 |
| 测试与验证 | 1）`frontend/` 下 `npm run typecheck` 通过（tsc 两个工程无错误输出）；2）`npm run build` 通过（vite v8.3.0，31 modules transformed，`dist/assets/index-c5yzkE9A.css` 37.65 kB、`dist/assets/index-D-vQS2vc.js` 274.00 kB，909ms）；3）口径验证：直接用 node 加载 `src/data/tasks.ts`（`--experimental-strip-types`）对 44 条 mock 数据派生——已完成 15 / 提前完成 1 / 待开始 23 / 已延期 5（合计 44）；`isTaskDone` 16 条、`isTaskOverdue` 5 条，与改造前一致（仅改变状态展示口径，完成率与逾期计数不变）；4）产物核对：新配色类 `bg-red-500` / `text-red-600` / `text-emerald-700` 已进构建 CSS；5）文档核对：`系统功能书.md` 与源文档逐字节一致（53,500 bytes），源文档改动前已备份。 |
| 文档同步 | `前端功能需求.md`、`字段对照清单.md`、`系统功能书.md`（与源文档同步）、`开发日志.md`（本条目 Push 22）；两份技术设计文档在仓库外（WXWork 缓存目录），已按同一口径更新并留存 .bak 备份。 |
| 风险与后续 | 1）接口契约：后端建议回传基础态 `status`（待开始 / 进行中 / 已完成）＋ 三个日期字段，展示态由前端派生（当前实现），或由服务端直接回传 5 态——二选一需在接口对齐时定案；2）「提前完成」在 mock 中仅 1 条（t11，源表状态与日期矛盾行），真实数据接入后需复核派生结果；3）源文档（功能定档目录）与仓库 `系统功能书.md` 需双处同步维护，本次已同步。 |
| 备注 | 推送 `px`；未直接推送 `main`。备份：源功能点清单 → `市场项目管理系统-功能点清单-V1.1.修改前备份-20260917-任务状态口径.md`；技术设计 v0.1 / v0.2 → 同目录 .bak-任务状态口径。 |

## 2026-09-17 17:06 +08:00 | px | px | Push 21

| 字段 | 内容 |
|---|---|
| 类型 | `docs`（系统功能书入库 + 项目总览字段口径对齐） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 1）把《市场项目管理系统 · 功能点清单（定档版 V1.1）》以 `系统功能书.md` 入库到仓库根（与 `前端功能需求.md`、`字段对照清单.md` 同级），内容与源文档逐字符一致，作为系统功能书的仓库版本；2）按当前系统实现口径对齐「项目总览」字段并清理失效引用，共 27 处：① A1-01 字段范围改为当前 15 列（任务描述（中英文）、项目经理、任务负责人（中英文）、任务状态、紧急重要度、是否按时交付、输出成果文件、文件、项目进展描述、开始日期、预计所需天数、预计完成日期、预计所需施工人数、实际完成日期、变更关联）＋三项非独立列（阶段性里程＝阶段分组、项目进度＝四格进度、是否逾期），去掉 16 个已不存在字段、新增「预计所需施工人数」；② A1-06 自动计算字段改为四格档位 / 是否按时交付 / 是否逾期 / 阶段与整体完成率；③ A1-13 由「星级进度展示」改为「进度展示（四格进度）」；④ A1-11 里程碑与依赖、A2-03 部门维度移至二期；⑤ A1-17 模板映射与 C9-06 模板节点字段同步（去掉责任部门 / 协同部门、前置任务、是否里程碑、完成检查项，默认工期写入「预计所需天数」）；⑥ 清理其余引用已删字段的位置：A1-04、A1-12、A1-15、A1-16、A2-07、A2-09、B1-01、B2-03、B2-04、B3-01、B3-06、B3-09、B4-04、B4-08、B5-01、C9-07、D5-04（星级口径统一为四格）；⑦ 文档抬头补「修订」说明行。 |
| 新增功能 | 无（文档）。 |
| 修复 / 重构 / 优化 | 文档口径修正：去掉文档中当前系统已不存在的任务字段及其引用（星级进度、实际开始日期、责任部门 / 协同部门、前置 / 后置任务、剩余天数、任务健康度、完成检查项、任务类型、是否里程碑、关注人、关联日报数 / 问题数 / 变更次数、归档状态、审计字段等），避免与前端实现（项目总览 15 列 + 任务详情抽屉）不一致。 |
| 测试与验证 | 1）`frontend/` 下 `npm run typecheck` 通过（tsc 两个工程无错误输出）；2）`npm run build` 通过（vite v8.3.0，31 modules transformed，`index-CtSdJudy.css` 37.56 kB / `index-C2shZ_tC.js` 273.45 kB，与 Push 20 记录一致）——本次推送含 Push 20 的前端样式改动，按门禁要求执行；3）文档核对：改动前原件已备份，逐行比对（`git diff --no-index` 49 insertions / 44 deletions）全部落在预期的 27 处，其余内容零改动。 |
| 文档同步 | 新增 `系统功能书.md`；`README.md` 文档目录表新增该文件一行；`开发日志.md`（本条目 Push 21）。 |
| 风险与后续 | 1）字段口径以当前前端实现为准，后端建模请以 `系统功能书.md` A1-01 字段范围为准；2）A1-11、A2-03 已标为二期，B2 / B3 / B4 / B5、C9 中相关引用已按同口径改写，后续恢复字段需同步回改；3）星级进度统一为四格进度，客户如仍要求星级展示需重新定档；4）源文档（项目资料目录内的功能点清单）与仓库 `系统功能书.md` 需双处同步维护。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 15:52 +08:00 | px | px | Push 20

| 字段 | 内容 |
|---|---|
| 类型 | `style`（界面布局微调） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 首页「项目空间」标题区控件布局调整：`新建项目`按钮从搜索框上方（竖向堆叠）移到搜索框左侧；宽屏（≥640px）两者同一行、垂直居中，窄屏保持上下堆叠（按钮在上、搜索框通栏）。仅调整 `frontend/src/Home.tsx` 控件容器的一处响应式类名（`sm:flex-row sm:items-center`），交互、文案与数据均未变。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 界面优化：标题区右侧控件由两行收拢为一行，减少头部高度，按钮与搜索框的关联更直观。 |
| 测试与验证 | 在 `frontend/` 下执行：1）`npm run typecheck` 通过（tsc 两个工程均无错误输出）；2）`npm run build` 通过（vite v8.3.0，31 modules transformed，产物 `index-CtSdJudy.css` 37.56 kB / `index-C2shZ_tC.js` 273.45 kB，131ms）；3）产物 CSS 核对：`sm:flex-row` / `sm:items-center` 已生成（位于 `@media (width>=40rem)` 块）。未运行 `npm run smoke`：该冒烟依赖本地 Casdoor 沙箱与 3000 端口 dev server，本次改动不涉及认证链路与数据逻辑。 |
| 文档同步 | `前端功能需求.md`（§3 首页布局说明 + 变更记录 Push 20 行）；`开发日志.md`（本条目 Push 20）。 |
| 风险与后续 | 纯样式调整、无逻辑风险；如需回退只还原 `frontend/src/Home.tsx` 这一处类名即可。 |
| 备注 | 推送 `px`；未直接推送 `main`。曾试改阶段分组胶囊「液体」配色（emerald-500）与文字深绿，产物核对通过后按反馈于推送前整体回退（`frontend/src/app.css`、`frontend/src/components/TaskBoard.tsx` 已与 `main` 现有内容一致、diff 为空），最终交付仅含按钮位置调整。 |

## 2026-09-17 14:10 +08:00 | px | px | Push 19

| 字段 | 内容 |
|---|---|
| 类型 | `chore`（分支归属修正 + 规则补充） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 1）删除由 `px` 代建的 `wmj`、`lan` 两条常驻分支（删除前临时把规则集 `resident-branches-no-deletion` 条件收窄为仅 `px`，删除后已恢复为 `px` / `wmj` / `lan` 三分支覆盖）——改由本人创建，避免分支归属显示在他人账号下；2）`CONTRIBUTING.md` 第 5 节与附录 A 第 1 条补充「常驻分支由本人创建并维护」。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 修正 Push 11 中「由 `px` 代建 `wmj` / `lan` 分支」的归属问题（Push 11 条目如实保留，本条为后续修正记录）。 |
| 测试与验证 | `gh api` 实测：规则集条件收窄后 DELETE `wmj` / `lan` 均成功，远端复查两条分支已不存在；规则集 PUT 已恢复为三分支覆盖并确认 active（`deletion` 规则）；`git ls-remote` 复查远端当前仅剩 `main` / `px`（Dependabot 临时分支已随 #6/#7 合并、#8 关闭清理），`wmj` / `lan` 创建后将按名称自动受防删除保护。 |
| 文档同步 | `CONTRIBUTING.md`（第 5 节、附录 A 第 1 条）；`开发日志.md`（本条目 Push 19）。 |
| 风险与后续 | 1）待 `wmj` / `lan` 本人创建各自常驻分支（GitHub 网页 Branches → New branch，或本地 `git push origin main:wmj`），创建即受规则集保护；2）本 PR 无代码差异，CI（`frontend`）照常触发。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 14:02 +08:00 | px | px | Push 18

| 字段 | 内容 |
|---|---|
| 类型 | `docs`（文档结构 / 仓库布局） |
| 分支 | `codex/move-agent-docs-to-root` |
| PR / commit | 本条目随本 PR 合入 `main`；未直接推送 `main`。 |
| 做了什么 | 把 AI 代理每次工作都要读 / 写的三份文档从 `docs/` 上移到仓库根目录：`前端功能需求.md`、`开发日志.md`、`字段对照清单.md`（对应 `AGENTS.md`「文档同步」三条约定）。同步更新全部现行引用：`AGENTS.md`、`CONTRIBUTING.md`（§11 / §15 / §17 / 附录 A）、`README.md` 文档目录表、`.github/pull_request_template.md`、`前端功能需求.md` 内的字段对照指引。 |
| 新增功能 | 无（无代码改动）。 |
| 修复 / 重构 / 优化 | 仓库布局调整：高频读写文档从 `docs/` 子目录移到根目录，打开仓库即可定位，降低代理找错 / 写错路径的概率；文档内容本身不变。 |
| 测试与验证 | 纯文档改动，按 `AGENTS.md` 免跑 `npm run typecheck` / `npm run build`（PR 上 CI `frontend` 照常触发，结果见 PR 检查）。实际执行：`git grep -n -E "docs/(前端功能需求|开发日志|字段对照清单)\.md"` 全库核验，除 `开发日志.md` 历史条目（按 §11 保留）外无旧路径残留；`git status` / `git diff --stat -M` 核对改动仅涉及上述文档与模板、且三份文档按重命名识别。 |
| 文档同步 | 本次改动自身即文档同步：三份文档已位于仓库根；`AGENTS.md` / `CONTRIBUTING.md` / `README.md` / `.github/pull_request_template.md` / `前端功能需求.md` 引用已同步；`开发日志.md`（本条目 Push 18）。 |
| 风险与后续 | 1）`docs/前端功能需求.md` 等旧路径不再存在：仓库内已无站外 URL 引用（已全库扫描），历史 PR / Issue / 书签中的旧路径需按新路径访问；2）其他成员本地工作区 `git pull` 后按新路径使用；3）后续新增「AI 常读」文档沿用根目录口径，并在 `AGENTS.md` 登记。 |
| 备注 | 历史日志条目中的旧路径按 CONTRIBUTING §11「已合入记录不再修改」原样保留；`docs/` 继续承载 SSO / WeCom / 沙箱 / 实测 / 接入标准等按需查阅文档。 |

## 2026-09-17 13:58 +08:00 | px | px | Push 17

| 字段 | 内容 |
|---|---|
| 类型 | `chore`（依赖 / 平台） |
| 分支 | `codex/push17-devlog` |
| PR / commit | 本条目随本 PR 合入 `main`；依赖升级随 PR #6 / #7 已 squash 合并（`39d6dfd`、`b630635`）；未直接推送 `main`。 |
| 做了什么 | 处理 Dependabot 存量 PR（Push 13 暂停版本更新前自动开出的三条）：合并 `#6`（`actions/checkout` v4 → v7）与 `#7`（`actions/setup-node` v4 → v7），两条都只改 `.github/workflows/ci.yml` 的版本号、检查全绿；关闭 `#8`（`@types/node` 22.20.3 → 26.5.1），原因已写进关闭评论：CI 失败定位到 `frontend/server/oidc-plugin.ts:236` 使用 `node:crypto` 的 `JsonWebKey`，`@types/node` v26 不再导出该成员（error TS2694），需先改代码；且本仓库 CI / 本地运行在 Node 22，类型包不宜跳到 v26。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 无代码改动（仅工作流版本号）。 |
| 测试与验证 | 1）`#6` / `#7` 各自 PR 检查 `frontend` 均为 SUCCESS；2）合并后 `main` 上实测：run 35187791986（`39d6dfd`）success、run 35187810114（`b630635`）success（Install / Typecheck / Build 全绿，日志已核对）；3）`#8` 失败原因取自 run 35186214065 的失败日志；4）确认 `.github/workflows/ci.yml` 现为 `actions/checkout@v7` + `actions/setup-node@v7`（`node-version: 22` 未变）。 |
| 文档同步 | `docs/开发日志.md`（本条目 Push 17）；`.github/workflows/ci.yml` 由依赖 PR 自带变更记录。 |
| 风险与后续 | 1）checkout / setup-node 大版本升级仅在 CI 生效，不影响业务系统运行时；若后续 CI 异常，可把两个 action 回退到 v4；2）`@types/node` 待人工升级（需先处理 `JsonWebKey` 引用），升级前本地 / CI 保持 Node 22 口径；3）暂停版本更新期间不再自动新增依赖 PR。 |
| 备注 | `#6` / `#7` 合并后 Dependabot 源分支已由 GitHub 自动清理；`#8` 分支自动清理，本地无残留。 |

## 2026-09-17 13:52 +08:00 | px | px | Push 16

| 字段 | 内容 |
|---|---|
| 类型 | `fix`（安全 / 文档） |
| 分支 | `codex/fix-deploy-readme-secret` |
| PR / commit | 随本 PR 合入 `main`；未直接推送 `main`。 |
| 做了什么 | 补漏：Push 14 的凭据去明文遗漏两处 Casdoor 默认管理员口令 —— `deploy/casdoor/README.md` 第 24 行、`docs/公司统一登录(SSO)实施方案.md` 第 85 行（两份文件都不在上一轮扫描清单里）。现均改为指向本地 `deploy/casdoor/.env.local` 的 `CASDOOR_ADMIN_PASSWORD`，并说明本地联调需自备该文件；本轮扫描扩大到全部 `*.md`（含 `deploy/`）与 `docs/` 全量。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 修复：仓库内最后一处沙箱明文口令。 |
| 测试与验证 | 1）全仓 `rg`（排除 `node_modules` / `dist` / `.git` / `*.svg`）复查：管理员账号口令组合、文档中的「密码」字样、常见弱口令模式均 0 命中（除本条日志的描述性文字）；2）GitHub 上 `main` 的根 `README.md` 经 `raw.githubusercontent.com` 拉取复核为新内容（此前用户截图所见为旧缓存 / 旧提交）；3）`git log -S` 确认本次改动文件的历史无新增明文。 |
| 文档同步 | `deploy/casdoor/README.md`；`docs/公司统一登录(SSO)实施方案.md`；`docs/开发日志.md`（本条目 Push 16）。 |
| 风险与后续 | 1）`deploy/casdoor/.env.local` 为本机文件（不入库），其他人本地联调首次登录需向操作者获取管理员凭据；2）git 历史里的旧口令仍在（公开仓库不可撤回），建议轮换本地沙箱口令；3）本地联调文档已统一为「口令见 `.env.local`」口径。 |
| 备注 | 推送 `codex/fix-deploy-readme-secret`（临时分支，合并后删除）；未直接推送 `main`。 |

## 2026-09-17 13:47 +08:00 | px | px | Push 15

| 字段 | 内容 |
|---|---|
| 类型 | `docs` |
| 分支 | `px` |
| PR / commit | 本条目为 PR #9（px → main）合并后的补充记录，随后续 PR 合入 `main`；未直接推送 `main`。 |
| 做了什么 | 记录 Push 10–14 的合并落地：PR #9 以 squash 合并进 `main`，合并提交 `e1cd245`（操作方 `px`）；源分支 `px` 按常驻分支模型保留（未删除）。补充记录而不改写已合并条目（CONTRIBUTING §11「日志条目随 PR 提交、合并后视为历史」）。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 1）PR #9 必需检查 `frontend` 通过（job 105091339960）；2）合并后复查 `origin/main:frontend/scripts/smoke-test.mjs` 无明文口令（0 命中）；3）合并后 `git ls-remote --heads origin` 确认 `main` / `px` / `wmj` / `lan` 四条常驻分支均在（`delete_branch_on_merge` 保持 false）；4）本次合并未引入代码差异（Push 10–14 的验证结论见各条目，未重复执行 smoke）。 |
| 文档同步 | `docs/开发日志.md`（本条目 Push 15）；其余文档随 PR #9 已同步并入 `main`。 |
| 风险与后续 | 1）`main` 上仍有 Dependabot 历史 PR #6 / #7 / #8 待人工处理；2）git 历史中的旧口令仍在（公开仓库不可撤回），建议轮换本地沙箱口令；3）常驻分支 `wmj` / `lan` 仍指向 `51aa33d`，后续开发从各自分支开始即可。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 13:44 +08:00 | px | px | Push 14

| 字段 | 内容 |
|---|---|
| 类型 | `chore`（安全加固 / 文档） |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 本地沙箱凭据去明文：1）此前写在仓库内的 `localhost:8000` 沙箱凭据——测试账号口令、管理员口令、DB / Redis 口令——全部从版本控制移出，只存本机 gitignore 的 `deploy/casdoor/.env.local`（新增；含 `TEST_USERNAME` / `TEST_PASSWORD` / `CASDOOR_ADMIN_USERNAME` / `CASDOOR_ADMIN_PASSWORD`）与 `frontend/.env.local`（含 smoke 用 `TEST_USERNAME` / `TEST_PASSWORD`）；2）`frontend/scripts/smoke-test.mjs` 去掉内置默认口令，改为读取上述 `.env.local`（已存在的环境变量优先），缺口令时明确报错退出；3）`README.md`、`frontend/README.md`、`docs/本地沙箱(LibiaoLink 演练环境).md`、`docs/开发者接入注意事项(SSO接入标准).md` 中的明文口令改为指向本地 `.env.local` 的引用。 |
| 新增功能 | 无（本地凭据管理方式变化）。 |
| 修复 / 重构 / 优化 | 修复：公开仓库内不应出现的沙箱口令明文；smoke 脚本不再内置口令。 |
| 测试与验证 | 1）密钥体检：`rg` 全仓（排除 `node_modules` / `dist` / `.git`）确认测试口令、管理员口令、DB / Redis 口令零命中；`git log -S` 复查历史，`frontend/.env.local`、`deploy/casdoor/.env.local` 从未入库，client secret（`b3ff1cc8…`）从未出现在任何提交；2）`git check-ignore -v` 确认两个 `.env.local` 均被忽略；3）`npm run smoke`（7 组断言，账号口令由本地 `.env.local` 提供）：全绿；4）`npm run typecheck`、`npm run build`：通过。 |
| 文档同步 | `README.md`；`frontend/README.md`；`docs/本地沙箱(LibiaoLink 演练环境).md`（新增「未入库说明」）；`docs/开发者接入注意事项(SSO接入标准).md`；`docs/开发日志.md`（本条目 Push 14）。`docs/前端功能需求.md` 不涉及（本次无页面 / 交互 / 数据模型 / 接口需求变化）。 |
| 风险与后续 | 1）本地开发 / CI 若要跑 smoke，需自备 `.env.local`（含 `TEST_USERNAME` / `TEST_PASSWORD`），否则脚本按缺凭据报错退出；2）git 历史里仍保留旧口令，按 CONTRIBUTING §12 视同已泄露，建议轮换本地沙箱三条口令（本地演示库，风险低）；3）公司环境（正式 / 测试）凭据不在本仓库，未受影响。 |
| 备注 | 推送 `px`；未直接推送 `main`；未改写 git 历史（仓库已公开，改写无法撤回已发生的曝光）。 |

## 2026-09-17 13:58 +08:00 | px | px | Push 13

| 字段 | 内容 |
|---|---|
| 类型 | `chore` |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 通知治理（团队反馈被 Dependabot 邮件刷屏）：`.github/dependabot.yml` 两个生态（npm `/frontend`、github-actions `/`）均设置 `open-pull-requests-limit: 0`，按官方文档写法暂停 Dependabot 版本更新 PR（不再每周批量开 PR）；Dependabot 漏洞提醒与安全更新保持不变（安全更新 PR 不受该上限约束，仓库安全设置未动）。 |
| 新增功能 | 无（平台配置）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 官方文档核对：`open-pull-requests-limit` 设为 0 即暂停该生态的版本更新（安全更新 PR 不受此限制、不计入该上限）；配置合入 `main` 后生效，若之后仍自动开出「版本更新」类 PR 视为未达标。推送前门禁：`frontend/` 下 `npm run typecheck`、`npm run build` 均通过（exit 0；本次无前端代码差异）。 |
| 文档同步 | `.github/dependabot.yml`（注释写明暂停原因与恢复方式）；`docs/开发日志.md`（本条目 Push 13）。 |
| 风险与后续 | 1）版本更新暂停后依赖升级改由人工发起；恢复自动升级 = 把两个 0 改回 1-5（建议同时改 monthly + groups 以降低打扰）；2）Dependabot 安全更新 PR 仍会零星开出，且仍会按 `CODEOWNERS` 自动请求三位评审——个人邮件彻底静音需三位开发者各自调整 GitHub 通知设置（`Settings → Notifications`），仓库侧无法代设；3）已开的 Dependabot PR #6 / #7 / #8 保持原样，待人工确认合并或关闭。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 13:53 +08:00 | px | px | Push 12

| 字段 | 内容 |
|---|---|
| 类型 | `docs` |
| 分支 | `px` |
| PR / commit | 随 PR #9（px → main）追加提交；未直接推送 `main`。 |
| 做了什么 | 规则补充：1）`CONTRIBUTING.md` 新增「5. Git 工作流（落地版）」——开发者先推自己的常驻分支（`px` / `wmj` / `lan`），再经 PR 合入 `main`，禁止直接推送 `main`；含常驻分支保留、squash-only、防删除规则集、日志与 PR 模板要求；2）抬头说明同步为「已落地：第 5 节、第 11–17 节」；3）`AGENTS.md` 新增「分支与推送」一节；4）附录 A 第 1 条补充指向第 5 节。 |
| 新增功能 | 无（规则文档）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 纯文档改动，无代码差异；规则与平台现状一致性核对：`main` 保护（require PR + 必需检查 `frontend`）、squash-only、常驻分支防删除规则集均为 Push 11 实测结论，未重复执行。 |
| 文档同步 | `CONTRIBUTING.md`（第 5 节 + 抬头 + 附录 A 第 1 条）；`AGENTS.md`；`docs/开发日志.md`（本条目 Push 12）。 |
| 风险与后续 | 1）第 1–4、6–10 节仍待补充；2）新增开发者时同步：建常驻分支 → 加入规则集与 `CODEOWNERS`。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 13:46 +08:00 | px | px | Push 11

| 字段 | 内容 |
|---|---|
| 类型 | `chore` |
| 分支 | `px` |
| PR / commit | 随 PR #9（px → main）追加提交；未直接推送 `main`。 |
| 做了什么 | 常驻分支模型落地：1）关闭「合并后自动删除源分支」（`delete_branch_on_merge=false`）——开发者分支合并后保留；2）新建 `wmj`、`lan` 常驻分支（基于 `main`），与 `px` 一起构成三条开发者分支；3）新增仓库规则集 `resident-branches-no-deletion`（id 23580071）：禁止删除 `refs/heads/px` / `wmj` / `lan`；4）`CONTRIBUTING.md` 附录 A 第 1 / 2 / 6 条与日志说明行同步更新（Push 序号不再写死）。 |
| 新增功能 | 无（分支与平台配置）。 |
| 修复 / 重构 / 优化 | 修正分支模型偏差：此前「合并后自动删除源分支」会在 PR 合并时删掉个人常驻分支（PR #5 合并时 `px` 曾被自动删除）。 |
| 测试与验证 | `gh api` 实测：`delete_branch_on_merge=false` 已生效；`git ls-remote` 确认 `main` / `px` / `wmj` / `lan` 四条常驻分支存在（`wmj` / `lan` 指向 `51aa33d`）；规则集创建返回 active（id 23580071，condition=三个 refs，rule=deletion），实测删除 `wmj` 被拒绝（422，分支保留）；CI 随本 PR 提交触发，结果见 PR 检查。 |
| 文档同步 | `CONTRIBUTING.md` 附录 A 第 1 / 2 / 6 条及日志说明行；`docs/开发日志.md`（本条目 Push 11）。 |
| 风险与后续 | 1）临时分支不再自动清理（如 Dependabot 合并后的分支），需要时手动删除；2）新增开发者时需同步：建常驻分支 → 加入规则集与 `CODEOWNERS`；3）`main` 保护 required approvals 仍为 0（待定是否调为 1）。 |
| 备注 | 推送 `px`；未直接推送 `main`。 |

## 2026-09-17 13:37 +08:00 | px | px | Push 10

| 字段 | 内容 |
|---|---|
| 类型 | `chore` |
| 分支 | `px`（PR #5 合并后源分支被自动删除，本次按流程重建） |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 1）`.github/CODEOWNERS` 补齐团队账号：px=`@256-code`、wmj=`@suikiiovo`、lan=`@XSZR341`，移除「待补充」TODO 与已过时的套餐限制说明；2）按 CONTRIBUTING 第 11 节补本条目（Push 9 已随 PR #5 合入 `main`，按「合并后不再修改」原则不改写历史）。 |
| 新增功能 | 无（配置与文档）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 账号核对：`gh api repos/256-code/LibiaoLink/collaborators` 确认三账号均为协作者（`@256-code` admin；`@suikiiovo` / `@XSZR341` write）；本 PR 无代码差异，CI（`frontend`：锁定安装 → typecheck → build）随 PR 触发，结果见 PR 检查。 |
| 文档同步 | `.github/CODEOWNERS`；`docs/开发日志.md`（本条目 Push 10）。 |
| 风险与后续 | 1）`CODEOWNERS` 合入 `main` 后才对后续 PR 生效（自动请求评审）；2）`main` 分支保护 required approvals 仍为 0（作者可自合并），如需「非作者评审」硬门禁可调为 1，成员账号已就绪。 |
| 备注 | 推送 `px`（重建分支）；未直接推送 `main`。 |

## 2026-09-17 13:23 +08:00 | px | px | Push 9

| 字段 | 内容 |
|---|---|
| 类型 | `chore` |
| 分支 | `px` |
| PR / commit | 随本 PR（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 工程化与平台配置：1）仓库合并策略改为 squash-only、合并后自动删除源分支（`gh api` 已生效）；2）新增 `.github/workflows/ci.yml`（PR / `main` push 触发：锁定安装 → typecheck → build）；3）新增 PR 模板、`CODEOWNERS`、`dependabot.yml`（npm + github-actions 每周，人工确认，无自动合并）；4）开启 Dependabot 漏洞提醒与安全更新；5）仓库转公开后启用 `main` 分支保护（require PR、必需状态检查 `frontend`、conversation resolution、linear history、禁 force push / 删除、enforce admins、无绕过者）与 GitHub Secret scanning + push protection；6）按 `CONTRIBUTING.md` §12 清理示例环境文件：`frontend/.env.example`、`deploy/casdoor/.env.example` 移除占位 / 示例密码值，只保留变量名与说明注释；7）`CONTRIBUTING.md` 附录 A 按实测结果更新。 |
| 新增功能 | 无（工程化配置）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 平台项实测（`gh api`）：合并设置确认 `squash=true / merge=false / rebase=false / auto-delete=true`；Dependabot 漏洞提醒返回 204（已启用）；仓库转公开后复测：`main` 分支保护生效（require PR、必需检查 `frontend`、`enforce_admins=true`、linear history、禁 force push / 删除、conversation resolution、无 bypass）、Secret scanning 与 push protection 均 enabled、Dependabot 安全更新 enabled；受限项如实记录：Secret scanning 的 non-provider patterns / validity checks 仍为 disabled（响应确认需付费 SKU）；CI 工作流复跑成功（run 35185708338，job `frontend` success）；历史核查：`git log --all` 未发现真实 `.env` / 凭据 / Token 进入历史，两个 `.env.example` 已按 §12 清理；`npm run smoke` 依赖本地 Casdoor 沙箱，未纳入 CI。 |
| 文档同步 | `CONTRIBUTING.md` 附录 A（平台保护 / CI 状态实测更新）；`frontend/.env.example`、`deploy/casdoor/.env.example`（按 §12 清理占位值）；`docs/开发日志.md`（本条目 Push 9）。 |
| 风险与后续 | 1）CI 已通过并设为 `main` 必需检查，后续 PR 须保持全绿；2）Secret scanning 的 non-provider patterns / validity checks 需付费 SKU，暂不可用；3）`main` 分支保护当前 required approvals = 0（作者可自行合并），如需「非作者评审」硬门禁应调为 1，待 `wmj` / `lan` 的 GitHub 账号确认后执行；4）`CODEOWNERS` 待确认成员账号后补齐 / 调整；5）smoke 纳入 CI 需先解决 Casdoor 沙箱编排。 |
| 备注 | 本提交随本 PR 进入 `main`；未直接推送。 |

## 2026-09-17 13:18 +08:00 | px | px | Push 8

| 字段 | 内容 |
|---|---|
| 类型 | `docs` |
| 分支 | `px` |
| PR / commit | 随 PR #4（px → main）提交；未直接推送 `main`。 |
| 做了什么 | 新增仓库根 `CONTRIBUTING.md`：《通用开发规则》第 11–17 节结合 LibiaoLink 现状的落地版（开发日志、Secrets 与通用安全、依赖与版本管理、生成物与不可变历史、高风险变更清单、发布与标签、Definition of Done、仓库落地清单）；`AGENTS.md` 增加「完整规则」章节指向 CONTRIBUTING 并声明冲突时从严。 |
| 新增功能 | 无（规则与文档）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 纯文档改动，无代码差异，未运行构建；落地前核对仓库现状：根目录结构、`frontend/package.json` 脚本（typecheck / build / smoke）、`frontend/.gitignore` 与根 `.gitignore` 实际忽略项（`git check-ignore` 验证 `.env` / `*.local` / `dist` / `files/*`（brand 除外）/ `logs` / `backup`）、确认无 `.github` 与 CI。 |
| 文档同步 | 新增 `CONTRIBUTING.md`；更新 `AGENTS.md`；`docs/开发日志.md`（本条目 Push 8）。 |
| 风险与后续 | 1）平台保护、CODEOWNERS、PR 模板、CI 门禁、ADR 目录尚未配置，已在 `CONTRIBUTING.md` 附录 A 标记待办；2）《通用开发规则》第 1–10 节尚未落地（章节编号已预留）；3）本 PR（#4）需非作者评审确认后合并。 |
| 备注 | 本提交进入 PR #4；`main` 未直接推送。 |

## 2026-09-17 12:26 +08:00 | px | px | Push 7

| 字段 | 内容 |
|---|---|
| 类型 | `docs` |
| 分支 | `px` |
| PR / commit | 直接推送 `px`（未创建 PR，未进 `main`）；本次为文档同步提交，不动前端代码。 |
| 做了什么 | 把 `docs/前端功能需求.md` 从 Push 5 状态补齐到 Push 6 实现：新增「项目详情页」全部功能描述（阶段标签导航、总览汇总卡、阶段分组任务表、四格进度条、任务详情抽屉、字段筛选、底部横向滑块）、任务数据模型与任务相关接口需求；并建立维护约定 —— 前端每次改动必须同步更新该需求文档（写入仓库根 `AGENTS.md` 与文档头部）。 |
| 新增功能 | 无（文档与约定）。 |
| 修复 / 重构 / 优化 | 修正需求文档滞后问题：此前文档停留在 Push 5（首页 + 搜索 + 新建项目），与含项目详情页的当前前端不一致。 |
| 测试与验证 | 文档类改动，无代码差异；内容按当前实现逐段核对（`TaskBoard` / `Tracker` / `TaskDrawer` / `ColumnPicker` / `TableScrollbar` / `ProjectDetail` / `AppHeader` / `data/tasks.ts`）。 |
| 文档同步 | `docs/前端功能需求.md`（全面更新 + 变更记录）；新增 `AGENTS.md`（文档同步约定）；`docs/开发日志.md`（本条目 Push 7）。 |
| 风险与后续 | 1）后续前端改动若未同步本文档，按 `AGENTS.md` 约定执行，避免再次滞后；2）任务进度 / 列显隐等内存态行为的接口建议已列入需求文档，待后端就绪后回填真实契约；3）`main` 未变更。 |
| 备注 | 推送 `px` 分支；本次仅文档与约定。 |

## 2026-09-17 12:20 +08:00 | px | px | Push 6

| 字段 | 内容 |
|---|---|
| 类型 | `feat` |
| 分支 | `px` |
| PR / commit | 直接推送 `px`（未创建 PR，未进 `main`）；本次含上一笔回退点快照 `9fc7e1f` 与本次功能提交。 |
| 做了什么 | 把项目详情页从占位骨架做成可用原型：源表（任务清单）落成可交互表格 —— 顶部阶段标签导航 + 「项目总览」汇总卡 + 阶段分组任务表（液体进度分组头、四格进度条、行点击抽屉、字段筛选、横向滑块），首页与详情页放开为全宽布局，认证门加载态换成无框 spinner。 |
| 新增功能 | 1）`data/tasks.ts`：44 条任务、16 个字段的数据模型与阶段派生逻辑（施工人数 / 紧急重要度 / 文件为演示值，源表对应列为空）；2）`components/TaskBoard.tsx`：阶段分组表格，列定义带最小宽 `min`、表宽 = 各列 `min` 之和，任务描述列锁定常显，阶段头内嵌淡绿色流动液体表示该阶段完成度；3）`components/Tracker.tsx`：四格进度条（灰=未开始、1 格绿=刚开工、2 格=完成一半、3 格=快完成、4 格=完成，悬停给文字提示、点击推进且不可回退）；4）`components/TaskDrawer.tsx`：行点击右侧抽屉展示全部字段；5）`components/ColumnPicker.tsx`：「筛选」按钮（列显隐 + 重置），默认显示项目经理 / 任务负责人 / 任务状态 / 紧急重要度 / 是否按时交付 / 输出成果文件 / 开始日期 / 预计完成日期 / 实际完成日期 / 变更关联，文件 / 项目进展描述 / 预计所需施工人数默认隐藏；6）`components/TableScrollbar.tsx` 与详情页底部 sticky 条：表格横向溢出时出现、不溢出自动隐藏，支持拖动滑块、点轨道跳转、键盘左右键；7）`components/Loader.tsx`：8 圆点脉冲加载动画，替换原先的加载卡片。 |
| 修复 / 重构 / 优化 | 1）主容器去掉 `max-w-[1440px]` 改全宽（Home / ProjectDetail）；2）顶栏面包屑副行去掉「项目经理：」；3）表头标签加 `truncate` + 悬浮全名，杜绝标签首尾相连；4）列宽体系由估算公式改为「每列最小宽之和」，`变更关联` 收窄（0.65fr/60px → 0.54fr/54px），任务描述列 460 → 340px（四格进度条位置随之左移且固定）；5）文件列文件名截到 4 个字加省略号，完整名保留在悬浮提示与抽屉；6）隐藏表格容器原生横向滚动条，避免与底部滑块重复；7）认证门拆开加载态与未登录态文案，避免未登录前的误提示。 |
| 测试与验证 | `npm run typecheck`（tsc 严格模式）与 `npm run build`（Vite 8 生产构建）通过；无头 Chrome 注入真实 SSO 令牌实测：默认列在 1324 与 1920 视口下 `scrollWidth = clientWidth`（滑块隐藏、15 个表头无一截断）；全列打开时「预计所需施工人数」与「实际完成日期」标签间距由 5px 提升到 46px；四格进度条 `barsX` 固定（437 → 377 → 317）且不随列筛选变动；文件列渲染为「技术协议…+1」；底部滑块拖动后表格 `scrollLeft` 0 → 173（到底）；2560 视口表格不溢出时滑块节点为空。 |
| 文档同步 | `docs/开发日志.md`（本条目 Push 6）；新增 `docs/字段对照清单.md`（源表字段与页面字段对照、默认显示列、待补字段）。 |
| 风险与后续 | 1）数据仍为前端内存态：进度点击、列显隐、新建项目刷新即回默认，待后端接口接管；2）源表数据瑕疵仍按原样呈现（人员进场 / 场地检查 / 施工对接状态为「待开始」但实际已完成；项目经理 4 行为空，页面用常量「贾海洋」）；3）字段筛选状态未持久化（刷新回默认列）；4）行内进度未写回后端，无操作审计；5）`main` 未变更，需评审后另建 PR。 |
| 备注 | 推送 `px` 分支；本次一并推送 `9fc7e1f` 快照（项目详情页骨架 + 顶栏头像菜单）。 |
## 2026-09-16 15:29 +08:00 | px | px | Push 5

| 字段 | 内容 |
|---|---|
| 类型 | `feat` |
| 分支 | `px` |
| PR / commit | 直接推送 `px`（未创建 PR，未进 `main`）；功能 commit `4446f33`，开发日志与需求文档随同一次推送提交。 |
| 做了什么 | 开发 LibiaoLink 首页「项目空间」：项目卡片列表、关键字搜索、新建项目弹窗的完整前端原型（`px` 分支），前端接入 Tailwind 4 原子化样式，统一卡片与控件视觉。 |
| 新增功能 | 1）`frontend/src/Home.tsx`：项目卡片网格（示例 20 个项目，4 列自适应降列），标题区含项目计数（共 N 个 / 搜索时显示找到 N 个）、新建项目按钮与搜索框；2）`frontend/src/components/Card.tsx`：序号徽标（浅灰底）+ 小车图标（CSS mask 上色、五色轮换）+ 项目编号 + 描述 + 项目经理 + 右下角更新时间，悬停缩放与主题色光晕；3）`frontend/src/components/SearchInput.tsx`：实时匹配项目编号 / 描述 / 更新时间 / 项目经理，支持一键清空与空态提示；4）`frontend/src/components/NewProjectModal.tsx`：居中弹窗表单（编号 / 描述 / 项目经理三项必填），取消、Esc、点遮罩均可关闭，创建后追加到列表（序号自增、时间取当前、颜色自动轮换）；5）小车图标素材入库（`assets/` 与 `frontend/public/`）。 |
| 修复 / 重构 / 优化 | 1）`app.css` 全局 `button` 样式收窄为 `.card button`（只作用于旧骨架页），修复 Tailwind 组件被全局按钮样式污染的问题；2）卡片宽度由固定 `w-64` 改为 `w-full`，随网格自适应；3）`App.tsx` 收敛为认证门（加载 / 未登录跳转 / 错误重试），登录后渲染首页。 |
| 测试与验证 | `npm run typecheck`（tsc 严格模式）与 `npm run build`（Vite 8 生产构建）通过；无头 Chrome 走完整 SSO 流程、注入真实令牌渲染首页实测：4 列 × 20 卡布局；搜索「日本」「08:15」「李伟」各命中 1 张，无匹配关键词出空态；新建弹窗打开 → 填 3 项 → 创建后列表变 21 张、计数同步、新卡片带当前时间。 |
| 文档同步 | `docs/开发日志.md`（本条目 Push 5）；新增 `docs/前端功能需求.md`（前端功能清单与后端接口需求，供后端开发参考）。 |
| 风险与后续 | 1）项目数据为前端内存态（刷新回到 20 张示例），待后端按需求文档提供接口后替换；2）项目经理与时间为示例数据；3）新建项目暂无权限控制与持久化；4）生产部署（构建产物托管、反向代理）尚未配置。 |
| 备注 | 本次推送至 `px` 分支；`main` 未变更。 |

## 2026-09-16 11:35 +08:00 | px | main | Push 4

| 字段 | 内容 |
|---|---|
| 类型 | `feat` |
| 分支 | `main` |
| PR / commit | 直接推送 `main`（未创建 PR）；commit `ad61736` |
| 做了什么 | 新增 LibiaoLink 前端接入参考（`frontend/`，React 19 + Vite 8 + TypeScript 严格模式），把「Casdoor 应用页点卡片 → 进入业务前端 → 自动单点登录 → 展示用户字段」整条链路在本地沙箱端到端跑通。 |
| 新增功能 | 1) 前端项目：`npm run dev` 起在 3000 端口，未登录自动跳 SSO，登录后展示 5 个标准字段（Name / DisplayName / Email / Id / Owner）与令牌声明；2) 开发用 OIDC 后端 `server/oidc-plugin.ts`：`/auth/login`（state + PKCE S256）、`/auth/callback`（code + verifier + secret 换令牌、HttpOnly 会话）、`/auth/me`（JWKS RS256 验签后返回用户字段）、`/auth/logout`（清理会话并跳 Casdoor 统一登出）；3) 冒烟测试 `scripts/smoke-test.mjs`（无浏览器，7 组断言）；4) `frontend/README.md`（接入说明、生产移植清单、常见问题）。 |
| 修复 / 重构 / 优化 | 本地 Casdoor 应用 `libiaolink` 补齐三项运行配置：Homepage URL = `http://localhost:3000`、Redirect URLs 增加 `http://localhost:3000/auth/callback`、开启 `enableAutoSignin`（已有会话免二次点击，对应接入标准第 4 条）。 |
| 测试与验证 | `npm run smoke` 全部通过（首页 / 登录入口 PKCE / 口令登录建会话 / 已有会话自动签发 / 回调换令牌 / 取用户字段 / 登出共 7 组）；`npm run typecheck`（tsc 严格模式）与 `npm run build`（Vite 8 生产构建）通过。 |
| 文档同步 | `README.md`（目录表与快速开始第 4 步）；`docs/本地沙箱(LibiaoLink 演练环境).md`（新增第九节「前端联调」，更新登录入口、配置快照、常见问题、与公司环境对照）；`docs/实测报告(SSO接入验证).md`（新增第六节：前端端到端验证结果 + 两条工程发现）；`docs/开发日志.md`（本文件，Push 1 起的历史见下方条目）。 |
| 风险与后续 | 1) 令牌交换目前放在 Vite 中间件（仅本地开发用），生产环境需把 4 个路由搬到后端（Java 可用 Spring Security oauth2Login），Cookie 加 `Secure`；2) 公司测试 / 正式环境的 `LibiaoLink` 应用仍待按接入标准调整（JWT-Custom、5 个 tokenFields、只留 `authorization_code`、替换 Logo）；3) 前端目前只是接入参考与起点，业务功能待开发。 |
| 备注 | 两个易踩点已记入实测报告：`/api/login` 的 `type=code` 不落地 SSO 会话（`type=login` 才建立）；「免二次点击」由登录页 JS 自动提交实现，服务端不会直接 302。 |

## 2026-09-16 11:11 +08:00 | px | main | Push 3

| 字段 | 内容 |
|---|---|
| 类型 | `docs` |
| 分支 | `main` |
| PR / commit | 直接推送 `main`；commit `26bfb56` |
| 做了什么 | 把公司测试环境的最新现状同步进文档：LibiaoLink 应用已由演练期的 `libiaolink`（clientId `75a4ed…`）更换为 `LibiaoLink`（clientId `41e162…`），本地沙箱联邦同步对齐；补充 `/callback` 报错说明。 |
| 新增功能 | 无（文档与运行环境同步）。 |
| 修复 / 重构 / 优化 | 定位并修复本地联邦登录的 `Invalid client_id`：根因是测试环境旧演练应用已被删除；本地认证源 `provider_authtest` 的运行配置改为指向新应用（clientId / secret），测试环境 `LibiaoLink` 应用的 Redirect URLs 补 `http://localhost:8000/callback`（联邦回跳，必须保留）。 |
| 测试与验证 | 管理员 API 复测联邦链路：测试环境出授权码 → 本地换令牌 `status: ok`；`panxing` 账号核对正常（displayName / email / owner / isAdmin）。 |
| 文档同步 | `docs/实测报告(SSO接入验证).md`（测试环境现状快照：13 个应用、18 个用户、待按接入标准调整的 4 项）；`docs/本地沙箱(LibiaoLink 演练环境).md`（联邦章节 clientId 更新、应用名修正、新增 `/callback` 常见问题）。 |
| 风险与后续 | 测试环境 `LibiaoLink` 应用仍未按接入标准配置：`tokenFormat=JWT`（应 JWT-Custom）、`tokenFields=[]`（应 5 字段）、Grant Types 开了 6 种（应只留 `authorization_code`）、Logo 为 Casdoor 默认图 —— 待调整。 |
| 备注 | 本地沙箱与测试环境的运行侧改动（不入 git）：认证源凭据、Redirect URLs。直接打开 `/callback` 报 `Unknown authentication type` 属正常现象（联邦回跳落地页，仅带 `code` / `state` 时有效）。 |

## 2026-09-15 14:53 +08:00 | px | main | Push 2

| 字段 | 内容 |
|---|---|
| 类型 | `feat` |
| 分支 | `main` |
| PR / commit | 直接推送 `main`；commit `9a43acb` |
| 做了什么 | LibiaoLink 统一登录（SSO）接入初版：基于开源 Casdoor 的方案设计、本地沙箱部署模板、应用图标与配套规范文档。 |
| 新增功能 | 1) `docs/公司统一登录(SSO)实施方案.md`：选型决策、部署、控制台配置、各系统接入方式、接口速查、上线清单、常见坑；2) `deploy/casdoor/`：本地联调部署模板（Docker Compose：Casdoor 4.4.0 + MySQL 8.0 + Redis 7，配置全部用环境变量注入，便于审计与回滚）；3) `assets/`：LibiaoLink 应用图标 SVG（浅色 / 深色两版，viewBox 收紧去掉四周空白，可直接填 Casdoor 应用 Logo 字段）；4) 文档：开发者接入标准（只用标准 OIDC、仅 authorization_code、JWT-Custom + Name / Owner / Id / DisplayName / Email 五个字段）、企业微信对接指南（登录通道 + 通讯录同步离职回收）、本地沙箱说明、实测报告。 |
| 修复 / 重构 / 优化 | 固化 MySQL DSN 拼接坑：`dataSourceName` 必须停在 `/` 结束（带上 `?charset=` 会与 dbName 拼出非法串、容器启动即 panic），字符集改由 MySQL 服务端参数保证 —— 已写进模板注释。 |
| 测试与验证 | 本地沙箱实测三轮：① 授权码 + PKCE 全链路（故意传错 `code_verifier` 被拒、`state` 防伪生效、单点登出可用、JWKS 本地验签 id_token 通过、refresh_token 换新令牌成功）；② 公司接入标准落地（JWT-Custom 五字段令牌、只留 authorization_code）；③ 合规改造。另做公司测试环境端到端演练（授权页 → 授权码 → 换令牌 → userinfo，令牌字段与登录名口径符合标准）与正式环境只读核对（企微 Provider 配置）。 |
| 文档同步 | README（目录表、公司环境对照、快速开始）、五份 docs 与 `deploy/casdoor/README.md` 一并上线。 |
| 风险与后续 | 默认令牌有效期偏长（内置应用 168 小时），生产需按应用改短并配合 refresh_token；角色 / 权限默认为空，做授权前需先建 Roles / Permissions；正式环境 `libiaolink` 应用尚不存在，需 SSO 管理员创建。 |
| 备注 | 本地登录入口：带应用 Logo 的 Casdoor 登录页（应用 `libiaolink`，clientId `libiaolink-a195b721bb30a7d4`），不依赖额外进程。 |

## 2026-09-15 10:47 +08:00 | px | main | Push 1

| 字段 | 内容 |
|---|---|
| 类型 | `chore` |
| 分支 | `main` |
| PR / commit | 直接推送 `main`；commit `f0eec6b` |
| 做了什么 | 创建 LibiaoLink 仓库（README 初版：仓库名与一句话定位）。 |
| 新增功能 | 无（仓库初始化）。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 无。 |
| 文档同步 | README 初版。 |
| 风险与后续 | 待补充 SSO 接入方案与本地沙箱（见 Push 2）。 |
| 备注 | 仓库创建。 |
