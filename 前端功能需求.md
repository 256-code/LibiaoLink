# LibiaoLink 前端功能需求（后端开发参考）

> 面向后端 / 接口开发同学。现状：前端原型已完成三级页面 —— 登录后「入口页」（项目空间 / 任务模板 / 文件库 三个入口）、「项目空间」卡片列表、「项目详情」顶部四视图 —— 项目总览 / 人员任务分配 / 任务进展 / 日报及问题（中间两块是看板：按人头 / 按状态，Push 82，卡片材质按业务样张；第四个视图「日报及问题」Push 128 新增，见 §一.6.12）—— 其中「项目总览」是阶段分组任务表（点任务表的阶段标签 = 右侧打开「任务节点 + 模板」卡片、从模板预设选节点加进项目，Push 61）；「任务模板」已按业务口径分出九个阶段板块标签栏，并把「项目总览」的任务节点以卡片汇总到左侧「任务节点」列、右侧为同尺寸的模板草稿列（列头是可改名的模板名称，默认「{阶段}模板」）（两列都是**毛玻璃面板**：半透明白 + 背景模糊 + 柔和投影，用于区分列；入口页与顶栏层级见 §一.7，Push 57；任务模板见 §一.7.2，Push 58；列表 / 详情为 Push 6 起的既有实现）。业务数据均为浏览器内存示例（前端原型未接后端，**一期正式交付由后端落库**；示例任务数据 44 条当前只挂在印度项目 `inmu-0010`，其余项目打开只出页面骨架 —— 阶段标签导航 + 阶段分组头、没有任务行，见 §一.6，Push 59），认证走公司统一登录（Casdoor，标准 OIDC）。本文档描述**已实现的页面与功能**及**所需后端接口**，用于后端开发与联调对接。
>
> **维护约定：前端每次改动（页面 / 交互 / 数据模型 / 接口需求）必须同步更新本文档，并与改动同一次推送提交（见仓库根 `AGENTS.md`）。**

## 一、页面与交互（前端已实现）

### 1. 登录与会话

- 前端不承接登录表单：未认证自动跳转公司统一登录（Casdoor，标准 OIDC，PKCE + state）。
- 登录回跳后通过 `/auth/me` 获取用户；认证中转场为无框 8 圆点加载动画，失败展示「加载失败 + 重试」。
- 账号口径遵循公司接入标准：`Name = 工号`（如 `zhangsan`）、`DisplayName = 张三`、`Email = zhangsan@libiaorobot.com`。
- 会话由后端承载（k6，Push 46）：四个 `/auth/*` 路由走后端 identity 模块（本地由 Vite 代理到后端）；HttpOnly `ll_sid` 会话 + 可读 `ll_csrf`；401 时自动跳登录并带 `returnTo`，登录后回跳当前页面（含 hash 路由）；空闲超时 30 分钟（`SESSION_IDLE_MINUTES`，超时后静默续期）。
- 登录后默认落到入口页（`#/`，见 §一.7）；带 `returnTo` 的链接回跳原地址（含 hash 路由），回跳后按该地址所属层级渲染。

### 2. 全局顶栏

- 左侧：LibiaoLink Logo 与副标「立镖全链路信息平台」；点击返回**上一层**——项目详情 → 项目空间（保留进入前的筛选态、优先走浏览器后退）、项目空间 / 任务模板 / 文件库 → 入口页、入口页 → 不动作（已在顶层）。层级与路由总表见 §一.7。
- 列表页与两个占位页时，Logo 右侧显示页面标题（「项目空间」/「任务模板」/「文件库」，竖线分隔，`text-2xl` 加粗，与原页面标题字号一致；<640px 隐藏），标题不再出现在内容区；入口页不显示标题。
- 进入项目详情后，Logo 右侧显示面包屑：`项目空间 / 序号 NN / 项目编号`（序号段取 `seqNo` 两位补零展示、≥100 原样，如「序号 10」，与卡片徽标 / 搜索同口径），副行「项目描述 · 创建于 时间」（视口较窄时隐藏）。
- 右侧账号区：圆形头像（取 DisplayName 首字符，浅灰底），悬停提示「姓名 · 邮箱」；点击展开菜单，当前仅「退出登录」一项；点击外部或 Esc 关闭。头像悬停时底色加深并浮起柔和阴影；菜单为圆角卡片（`rounded-xl` + 双层柔和阴影），打开时 150ms 轻微下移淡入（`.menu-pop`），菜单项为圆角浅灰高亮（`hover:bg-zinc-100`）。
- 层级：顶栏 `sticky top-0 z-30`——高于首页内容区工具条（`z-10`）与筛选侧边栏及其窄屏遮罩（`z-20`），低于日期区间弹层（`z-40`）、任务抽屉与新建项目弹窗（`z-50`）；账号菜单展开后完整可点，不会被工具条或侧边栏遮住。

### 3. 首页「项目空间」

- 项目卡片网格：每行 4 列，随可用内容宽度自适应降列（1 / 2 / 3 / 4 列）——侧边栏展开使内容变窄时同样自动降列；页面为全宽布局，网格基于容器查询（`@container` + `@md/@4xl/@6xl`）。
- 卡片内容（自上而下）：左上角项目类型徽标、右上角浅灰项目序号徽标（`seqNo`，两位补零、≥100 原样；悬停提示「项目序号」）、小车图标（彩色）+「项目经理：xxx」、项目编号（等宽字体，如 `CNBJ-20260708-0001`）、项目描述、右下角创建时间（`createdAt`，`YYYY-MM-DD HH:mm`）。
- 卡片编辑入口：每张卡片左下角常驻铅笔按钮（与编号 / 描述左对齐、与右下角创建时间同一行；32×32 幽灵态：默认无底色无描边、低透明度灰铅笔（`text-zinc-400/70`），悬停卡片时铅笔轻微加深、悬停按钮时浮出白底 + 淡阴影；`aria-label` 与 `title` 均为「编辑项目」），点击打开编辑弹窗并阻止冒泡——不会同时进入项目详情；卡片主体仍按整卡点击进入详情（卡片外壳为 `role=link` + `tabIndex=0` 的 div，支持 Tab 聚焦 + Enter / 空格打开，为容纳编辑按钮由 button 改为 div，避免按钮嵌套）。
- 卡片配色跟项目类型走（同一类型同一颜色）：T-sort 蓝 / 3D分拣 绿 / 飞箱 品牌黄（#feca04，与「新建项目」按钮、Logo 同色；徽标文字深灰 #313033）；小车图标、类型徽标、悬停光晕三处同色，当前仅启用这三种颜色；红色与紫色已按预留注释保留在代码里（`Card.tsx` / `types.ts`），未来新增项目类型时可直接启用。
- 悬停：卡片轻微放大 + 主题色光晕。
- 排序：按项目时间（`updatedAt`，项目最近活动时间——主数据变更 / 阶段推进 / 任务变更刷新，上传文件、写日报不刷新）排序，默认降序（最近活动在前）；工具条内「降序 / 升序」分段按钮可即时切换（当前项深色高亮），分段按钮组内左侧刻有浅灰 TIME 标识（10px 半粗 + 0.18em 字距，与按钮同容器、以竖线分隔），两个按钮均带悬停提示（降序 = 最近活动在前，升序 = 最早活动在前），整体以 role=group + aria-label「按项目时间排序」标注，明确排序依据是项目时间。
- 内容区顶部工具条：分类筛选开关、TIME 排序切换（容器内 TIME 标识 + 降序 / 升序）、项目计数（`共 N 个项目`，搜索 / 筛选时切换为「找到 N 个项目」）、`新建项目`按钮、搜索框同处一行、同一层级；宽屏时左侧为开关 + 排序 + 计数、右侧为按钮 + 搜索框（按钮在搜索框左侧）；窄屏自动换行（开关 + 排序 + 计数一行、按钮与搜索框各一行，按钮保持自适应宽度）。
- 分类筛选开关：深灰轨道 + 白色滑块 + 播放图标，开启时轨道转品牌黄 #feca04（文字同步转深灰 #313033）、滑块右移并旋转一周（400ms），标签「分类筛选」，实现在 `frontend/src/components/CategorySwitch.tsx`（用 Tailwind 任意值还原，未引入 styled-components）；开关位于工具条最左，控制左侧筛选侧边栏开合。工具条整体吸附固定在顶栏下方（`sticky top-16` + 页面底色打底，层级低于顶栏与左侧栏），下滑浏览卡片时开关始终可见可点。
- 筛选侧边栏：开关打开后从左侧滑入（宽 280px，`frontend/src/components/CategoryFilterSidebar.tsx`）—— 侧栏底板为「液态玻璃」玻璃面板：半透明白渐变 + 白色发丝边 + 顶缘内高光 + `backdrop-blur-2xl backdrop-saturate-150` 背景虚化 + 柔和投影，与页面浮层（任务节点卡片 / 任务编辑弹窗）同一材质口径；桌面端（≥md）内容区同步右移 280px（`md:pl-[304px]`，300ms 过渡）实现自适应，窄屏（<md）改为半透明遮罩浮层、内容不移位。
- 侧边栏内容：头部「分类筛选」标题 + 副标题「按常看组合、地区、项目类型、项目经理、项目时间筛选项目」 + 关闭按钮；主体自上而下为**「常用筛选」**（Push 138 新增，见下一条）与「地区」「项目类型」「项目经理」「项目时间」四组多选胶囊（带计数，可叠加；**胶囊与「项目时间」触发框采用与任务表行内可编辑单元格同一套「液态玻璃」材质** —— `border-zinc-200/90 bg-white/75` + 顶部内高光 + 极轻投影 + `backdrop-blur-[3px]`，悬停转白并加深描边 / 投影，选中态为深灰底 + 白字 + 浮起投影（`frontend/src/components/CategoryFilterSidebar.tsx` 的 `GLASS_SURFACE` / `GLASS_PANEL`；`DateRangePicker` 通过可选 `triggerClassName` 接入，不传保持默认白底描边，任务编辑表单里的同类控件不受影响）；项目类型取 T-sort / 3D分拣 / 飞箱，按固定顺序展示；项目时间为内置日期区间选择器——触发按钮显示「全部时间」或 `YYYY/MM/DD – YYYY/MM/DD`，点开为月历弹层（可翻月、选中区间高亮、底部「清除 / 确定」，Esc 或点击外部关闭），无新增依赖，实现在 `frontend/src/components/DateRangePicker.tsx`；弹层默认打开到最近数据所在月份；标签右侧带 ⓘ 口径说明（悬停 / 聚焦提示「项目最近活动时间：修改项目信息、推进阶段或变更任务时更新；上传文件、写日报不更新」），分组标题加重（`text-sm font-semibold text-zinc-700`）以突出层级；底部显示「已选 N 项」与「重置」；主体区域使用组件（`frontend/src/components/ScrollArea.tsx`）自绘的悬浮滑块——原生滚动条隐藏（`.scrollbar-hidden`），滑块默认不可见，滚动时浮现、停止滚动约 1 秒后淡出，可直接拖动。
- 「常用筛选」（Push 138 新增；frontend/src/savedFilters.ts + components/CategoryFilterSidebar.tsx）：给「不同员工关注的项目不一样」提供可命名的组合 —— ① **添加**：分组标题右侧「+ 添加」（满 20 组置灰，悬停提示「最多保存 20 组常用筛选」）→ 就地展开琥珀色编辑卡片「新建常用筛选」（含说明、名称输入框、「已选 N 项」、「取消 / 保存」）；② **勾选多项**：编辑态下点下方任一分类胶囊 = 勾进**草稿**（可跨「地区 / 项目类型 / 项目经理 / 项目时间」任意组合，不影响当前列表），再点取消；③ **命名保存**：名称自定义（上限 20 字；名称为空或一项未选时「保存」置灰；Enter 保存 / Esc 先退编辑态）；保存 = 写入本地记忆并**立即生效**（列表套用该组合、地址栏同步 filter[...]）；④ **胶囊**：保存后进入「常用筛选」分组（名称 + 命中项目数，过长省略），点名称 = 应用；应用中（条件与当前筛选等价）为深灰底白字高亮；⑤ **编辑 / 删除**：悬停或键盘聚焦时胶囊**就地展开**（200ms 过渡）浮出铅笔与 ×（aria-label「编辑常用筛选 {名称}」/「删除常用筛选 {名称}」）—— 铅笔 = 载入重编辑（可改名 / 改勾选）、× = 直接删除；**未悬停时胶囊与普通分类胶囊等宽**、不预留动作位（Push 139 修正：短名组合不再右侧拖出一截空白，实测「亚洲 1」61px = 「中国 1」61px，展开态 103px）；⑥ **记忆**：浏览器本地（localStorage 键 libiaolink.home.savedFilters.v1，损坏数据静默丢弃，退出登录随 clearHomePrefs() 清除）；跨设备记忆待后端定契约（§3.8 A24）。
- 关闭方式：关闭按钮 / 窄屏遮罩点击 / Esc；关闭时开关同步关闭，已选条件保留并继续生效。
- 筛选与搜索叠加生效：有筛选或搜索时计数显示「找到 N 个项目」；无结果时按情况提供「清空搜索 / 清空筛选」按钮。
- 筛选态写入 URL query（列表页地址 `#/projects`，可分享 / 可收藏 / 刷新不丢；对应技术设计 v0.2 §8.2、v0.1 §7.2）：参数命名与 `GET /api/v1/projects` 同口径，默认值不落 URL；地址栏用 `history.replaceState` 更新（不新增历史条目，连续筛选只留一条记录）；带筛选参数的链接打开时侧边栏自动展开，直接看得到命中的条件；不带参数进入列表时（含入口页点「项目空间」）按本地记忆（localStorage）恢复上次的筛选与侧边栏开合（URL 优先、本地次之；恢复结果同步写回地址栏），见第六章第 12 条与 §一.7。
- 参数与接口映射（多值统一英文逗号分隔，与契约 `filter[...]` 口径一致）：

| URL 参数 | 含义 | 前端取值 | 未来接口映射 |
|---|---|---|---|
| `filter[region]` | 地区（多值） | `Project.region`，如 `中国,美国` | 契约同名参数 |
| `filter[projectType]` | 项目类型（多值） | `T-sort` / `3D分拣` / `飞箱` | 契约同名参数 |
| `filter[managerId]` | 项目经理（多值） | `Project.managerIds`（UUID 数组，至少一位）；界面按项目经理目录显示姓名（与 `managerNames` 同下标） | 契约同名参数（Push 34 定案）；前端已 id 化（Push 36）；**Push 136 起多位、任一位命中即命中（A22）**，见第六章第 9 条 |
| `filter[timeFrom]` / `filter[timeTo]` | 项目时间区间（闭区间） | `YYYY-MM-DD`，按 `Project.updatedAt` 的日期部分比较 | 已定案（PR #42，Push 49）：同 A1，见第六章第 11 条 |
| `q` | 关键字 | 与卡片字段做包含匹配 | 契约同名参数 |
| `sort` | 排序 | `updatedAt:desc`（默认，省略不写）/ `updatedAt:asc` | 契约 `SortQuerySchema`（`字段:方向`）；项目列表白名单 `updatedAt` / `createdAt` / `seqNo`（A9 定案，Push 69；前端本期仍只产生 `updatedAt`） |

- URL 兜底：当前数据里不存在的地区 / 项目经理（managerId）、不在三选一里的项目类型、格式非法的日期会被丢弃，并把地址栏归一化为丢弃后的结果（分享过期或手改的链接不会停在「0 个项目且看不出原因」的状态）；时间区间只写一端时不生效。旧口径 `filter[manager]`（姓名）链接不再解析，等价于未筛选（不报错、不误筛）。
- 顶栏 Logo 与详情页面包屑「项目空间」：Logo 为「返回上一层」（口径见 §一.2 / §一.7）；面包屑「项目空间」——列表页点击不改变筛选态（原地保留、不新增历史条目），详情页点击回到进入前的列表地址（含筛选态）；直接打开详情页链接时回落到干净的列表地址。
- 侧边栏开合属于界面状态，不写入 URL；但会随筛选态一起写入本地记忆（localStorage），无参数进入首页时按上次开合恢复（无开合记忆时回退「有筛选则展开」）。

### 4. 搜索

- 输入即过滤（实时），匹配范围：项目编号 / 项目序号（原值或两位补零，如 `1` / `01`）/ 描述 / 地区 / 项目类型 / id / 创建时间 / 项目经理（不区分大小写）。
- 一键清空；无结果时显示空态与「清空搜索」按钮。
- 关键字写入 URL 的 `q` 参数：输入时同步更新地址栏（`replaceState`，不新增历史条目），刷新 / 分享 / 收藏后保留。

### 5. 新建 / 编辑项目

- 点标题区「新建项目」按钮打开居中弹窗。
- 表单四项：项目编号、项目描述、项目经理（下拉取项目经理目录 `frontend/src/data/managers.ts`，产出 `managerId`；演示态，接接口后改为用户选择）、项目类型（下拉：T-sort / 3D分拣 / 飞箱，默认 T-sort）；前三项必填（全部填写后「创建项目」才可点击）。
- 关闭方式：取消按钮 / Esc / 点击遮罩。
- 创建成功后：新项目追加到列表末尾（默认按项目时间降序，新建项目显示在首位）；创建时间（`createdAt`）与项目时间（`updatedAt`，初始同值）由前端临时分配——创建时间此后再不改变，项目时间在编辑保存时刷新，项目序号（`seqNo`）在原型阶段按「现有最大值 + 1」临时分配（接入后端后改由服务端分配、以下发的 `seqNo` 为准，见第六章第 13 条）；主题色按项目类型映射（演示阶段临时实现；契约定案为随 C9 字典元数据下发、前端不硬编码，见第六章第 2 条）；地区默认「未分类」（表单是否加地区字段待定）。
- 编辑入口：卡片铅笔按钮（详情页工具条入口已移除，Push 49），弹窗组件 `frontend/src/components/ProjectModal.tsx`（由原 `NewProjectModal` 泛化，mode=create/edit）。
- 编辑弹窗预填当前四项（编号 / 描述 / 项目经理 / 项目类型）；标题「编辑项目」、副文案「修改项目信息，保存后立即生效。」、主按钮「保存修改」；`id` / `seqNo` / `createdAt` 不可修改（创建时间不随编辑变化）；保存会刷新项目时间 `updatedAt`（最近活动）。
- 保存后即时生效：卡片编号 / 描述 / 项目经理姓名 / 类型徽标与主题色、详情页顶栏编号与描述同步刷新（创建时间不变）；仍为内存态，刷新回默认（接入 `PATCH /api/v1/projects/{id}` 后由服务端持久化，见第三章第 8 条）。
### 6. 项目详情页（项目总览 / 甘特图 / 人员任务分配 / 任务进展 / 日报及问题）

进入方式：项目空间列表点击项目卡片，路由切到 `#/project/{项目id}`；可直接输入 URL 直达，项目不存在时显示「未找到该项目」。

- 数据归属（Push 59）+ 阶段骨架（Push 61 起常显）：原型阶段示例任务数据（源表 44 条）只挂在印度项目 `inmu-0010`。其余项目（含新建项目）没有任务 —— **阶段骨架常显**：「项目总览」的 9 个阶段分组头始终照出（Push 82 起顶部导航固定为 项目总览 / 人员任务分配 / 任务进展 三视图（Push 128 起再加第四个视图「日报及问题」，见 §一.6.12），阶段标签不再单独占位），没有任务的阶段只显示阶段名（不显示 `已完成 N/M` 计数）、组内没有任务行；用「点阶段标签 → 右侧卡片」加出任务后，其余阶段的分组头也不会消失。项目完全没有任务时，汇总卡显示「当前阶段 —」与「整体进度 0%」（同样不显示 `0/0 完成`）；单阶段标签下只出该阶段的分组头。正式口径本来就是按项目取数（`GET /api/v1/projects/{id}/tasks`）。
- 添加任务（Push 61）：**点任务表里的阶段标签**（分组头那个白底 pill：「售前规划」…「验收」）→ 右侧出现一张**中等卡片**（宽 400px、`max-h-72vh`、毛玻璃；**落点在表格里**（Push 67 调整：原来浮在页面右上角，会压住顶部标签栏与汇总卡）—— **位置规则 = 与所点阶段同一水平线**（Push 67 业务定稿：试过「浮在页面右上角」「固定开在表头下方」「跟着点的阶段跑」「按余量上下翻转」几版都太跳或太远，最终定为对齐版）—— 卡片**顶部与所点阶段的分组头在同一水平线**（卡顶 = 分组头顶，卡片正好贴在这一阶段左侧，页面滚到哪都保持这条水平线，不会忽上忽下）；**左边缘对齐「项目经理」列**（该列被列显隐关掉时回落到表格右边；卡片始终夹在表格内、不出表格边界）；量表格定位（`absolute` 渲染在表格容器内），随表格 / 页面滚动一起走；横向滚动表格时卡片跟着「项目经理」列走（该列滚出屏幕则贴住表格左边），不会被裁掉；高度取「72vh」与「分组头以下的表格余量」的较小值（保底 320px）；分组头找不到（异常）时回落到「表头下沿 +10px」；右上 × / `Esc` / **点卡片外的空白处** / **再点同一个阶段标签**关闭（表内空白、汇总卡、页面其它地方都能关，卡片内部点击不关）；切顶部阶段标签时自动关掉）。卡片顶部是**标签导航**：① 第一个固定是**「任务节点」** = 这个阶段的节点池（该阶段全部预设节点按出现顺序去重，与任务模板页左列同口径 —— 硬件实施 26 个），点一条就把该节点加成项目任务；② 其余每个标签 = 这个阶段的一块模板（如硬件实施有「硬件实施模板一 / 模板二」；模板标签里带序号、按预设顺序预览，**列表可鼠标滚动看全**），模板标签上有「整套添加（N）」一次补齐没加过的节点。已经在项目里的节点按**节点 id 判重**，显示「已添加」、点不动。加进来的任务：阶段 = 模板所属阶段；字段默认值 = 任务状态「待开始」、进度 0、紧急重要度「中」，任务负责人 / 开始日期 / 预计完成日期 / 输出成果文件等留空（后续在任务详情里补）；当前原型未接后端、暂存浏览器内存（**换项目或刷新即回到初始数据**）；**一期正式交付由后端落库** —— 任务创建接口 = §3.8 A10（待 wmj 决议）。阶段分组头的整行 / 左侧箭头仍是折叠展开（点标签才开卡片）。

**6.1 顶部视图导航（Push 82 改版）**

- 5 个视图标签：**项目总览**（阶段分组任务表，§6.3）、**甘特图**（阶段分组甘特图：左侧任务列表 + 右侧时间轴 + 里程碑菱形，Push 145 新增，§6.14）、**人员任务分配**（按任务负责人的看板 —— Push 136 起一个任务多位负责人时，这张卡会出现在每一位负责人的列里，§6.10）、**任务进展**（按任务状态的看板，§6.11）、**日报及问题**（日报与问题四态看板，Push 128 新增，§6.12）。原来各占一格的 9 个施工阶段标签（售前规划 … 验收）**不再出现在导航栏**：阶段仍然存在，只是回到「项目总览」的阶段分组（§6.3），任务卡片上以**「所属阶段」色签**呈现（§6.10 / §6.11）。
- 默认选中「项目总览」；点击其它视图切到对应看板；标签栏可横向滚动。标签栏为内容区首行，上留白 12px（`pt-3`），紧贴顶栏下沿。
- 标签栏右侧为表格控制区（**只在「项目总览」出现**）：列显隐控件（`ColumnPicker`）；两块钱查看板不带列显隐。详情页「编辑项目」入口已移除（Push 49），编辑仅保留卡片铅笔入口。
- 底部那条常驻横向滑块（§6.7）同样只在「项目总览」出现。
- 「甘特图」视图自带一条工具条（图例 + 粒度 / 缩放 / 全部折叠 / 回到今天 + 甘特内筛选，见 §6.14），渲染在甘特图卡片上方，不占标签栏右侧工具区；标签栏右侧工具区（列显隐 / 醒目模式）与底部横向滑块仍只在「项目总览」出现。

**6.2 项目总览汇总卡**

- 当前阶段：按阶段顺序找到第一个「存在未完成任务」的阶段；全部完成时显示「全部完成」；没有任务的项目显示「—」（原型阶段除印度外的项目）；阶段名加粗展示。空项目右侧只显示「整体进度 0%」，不显示「0/0 完成」这种没有意义的计数。
- （一期暂不展示）逾期计数：未完成且预计完成日期早于今天的任务数（红色，0 条时为正常色）；口径与 §3.4 `overdue` 一致，后续汇总栏扩展再回填。
- 整体进度：进度条 + 百分比 + 「已完成 N/M」（完成数 / 总任务数）；文案把「完成」放在数字前，避免「3/6 完成」被读成「这一阶段已完成」。
- 汇总栏为可扩展区域：后续要加哪些信息待业务定稿后回填（逾期计数为候选之一）。

**6.3 阶段分组任务表**

- 按 9 个阶段分组；组头可点击折叠 / 展开（默认展开）。**阶段骨架常显（Push 61）**：没有任务的阶段也保留分组头（只有阶段名 —— 空阶段不显示 `已完成 N/M` 计数）、组内没有任务行（`TaskBoard` 的 `skeletonStages` = 9 个阶段；单阶段标签下只出该阶段的分组头）；因此用「添加任务」加出任务后，其余阶段的分组头不会消失。
- **「未分组」兜底（Push 82）**：任务的阶段不在九个施工阶段里时（目前只有看板「+ 添加」建出来的任务阶段留空），表格在最后一个阶段之后追加一个「未分组」组（**有内容才出现**，同样可折叠 / 展开），保证看板建的任务在总览里也看得到。
- 表头「任务描述」列右侧有「一键收起」按钮：一次性收起当前可见的全部阶段（收起后仅剩阶段胶囊行）；全部处于收起态时按钮转为「全部展开」，一键复位；切换阶段标签后按该标签下的可见阶段重新判定。
- 组头：折叠箭头 + 胶囊；胶囊内嵌淡绿色「液体」背景，液面高度 = 该阶段完成率（完成数 / 该阶段任务数）；右侧「已完成 N/M」（如「已完成 3/6」＝ 6 条里完成 3 条，不是说这一阶段已完成）。
- 表头 15 列（按显示顺序）：任务描述（常显锁定）、项目经理、任务负责人、任务状态、紧急重要度、是否按时交付、输出成果文件、文件、项目进展描述、开始日期、预计所需天数、预计完成日期、预计所需施工人数、实际完成日期、变更关联。
- 列宽策略：任务描述列固定 340px；其余列按「最小宽度」配比弹性伸缩；表格总宽 = 各显示列最小宽之和，视口不够时出现横向滚动（任务描述列不被挤压）。
- 列对齐（Push 67）：表头文字与单元格内容一律按列居中（内容中心对齐列标题中心），空值占位「—」同样居中；**任务描述列保持左对齐**（内含四格进度条与悬停删除按钮，左对齐更符合阅读顺序）。
- 列头固定（Push 141，业务反馈「这个标题栏要固定 鼠标移动可以依旧显示」）：任务表列头（含「任务描述」列的「一键收起」按钮）在页面纵向滚动时吸顶 —— 固定在应用顶栏（64px）下方始终可见；表格横向滚动（视口不够或拖底部滑块）时列头同步平移（`translateX(-scrollLeft)`），与各列始终对齐。
- 行内元素：
  - 任务描述：中文标题 + 英文标题（灰色小字，截断悬浮看全）；**中文标题自 Push 103 起加粗加深**（`font-bold` + `text-zinc-900`，与看板卡片标题同一层级，业务反馈「这边也同样需要」）；行内右侧固定 4 格进度条（位置不随列筛选移动）；进度条右侧 = 行悬停浮现的删除按钮（Push 141，静止为幽灵态浅灰小图标、悬停展开成小号红底「删除」胶囊，见「行交互」）。
  - 任务状态（五态，Push 65 起表格列 = **色标签 + 行内下拉可改**）：色签口径 = 已延期（红）、进行中（琥珀）、已完成（绿）、待开始（天蓝）、提前完成（紫）；单元格点开即选状态（见 §6.9），选中值与四格进度条双向联动（见 §6.4）；未手动改过时按进度与日期派生（四格全亮 → 已完成，填了预计完成日期且实际完成更早 → 提前完成）；「逾期」不是状态取值，逾期标注只落在「是否按时交付」列（见下）。
  - 紧急重要度：徽标（高 = 红、中 = 琥珀、低 = 灰）。
  - 是否按时交付：**逾期标注位（Push 67 业务定案：从「实际完成日期」列移到这里）** —— 已过预计完成日期且未完成显示红色「逾期未交付」；已完成但晚于预计完成日期（或没填完成日期、但预计完成日期已过）显示琥珀色「逾期已交付」；其余照源表值显示浅绿「按时交付」徽标，空值「—」。输出成果文件：浅色徽标；空值显示「—」。
  - 文件：文件名截 4 字 + `+N`，悬浮显示全部文件名。
  - 预计所需天数：渲染为开始日期与预计完成日期之间的短横线，横线上方仅显示数字。
  - 实际完成日期：已完成显示日期，未完成显示「—」（Push 67 起「逾期」不再标在这一列，逾期标注移到「是否按时交付」列；任务状态列同样不出「逾期」）；这一格可直接点选日期（填 = 完成、清 = 退回进行中，口径见 §6.9）。
  - 变更关联：有变更日期时显示琥珀色「变更」徽标，悬浮显示日期。
- 行交互：整行可点击（键盘 Enter / 空格同效），点击打开任务详情抽屉；选中行显示琥珀色左侧条与浅底；行悬停时任务描述列右侧（四格进度条之后）浮现**删除按钮**（Push 141，原「编辑任务」铅笔已下线）：静止 = 24px 幽灵态圆形按钮（无底色、浅灰垃圾桶图标，不抢视线）、悬停 / 键盘聚焦展开成 48px 小号红底「删除」胶囊（`aria-label` / `title` = 「删除任务」），点击不触发行选中、不打开抽屉；展开只吃按钮槽位（任务描述列右侧固定 48px、槽位右侧另留 16px 留白），不挤动四格进度条与描述文字。（尺寸与色彩口径经业务 2026-09-22 三轮反馈收定：「这个状态下有点太大了 要再小一点」「鼠标触碰列表时候黑的太突兀了 还是要比较看不出来吧」「当项目经理大于两位时 就是会触碰到」—— 展开胶囊右缘原与「项目经理」列齐平，留白后不再贴列。）点击 = 从项目任务列表移除该任务（原型内存态，刷新 / 换项目复位；不弹二次确认，「删除」字样即操作提示）。编辑任务仍走整行点击 / 看板卡片 → 任务详情抽屉（Push 91 起弹窗删除，字段在抽屉里直接改，见 §6.5 / §6.8）。
- 行内编辑（Push 64；Push 65 起含任务状态；Push 67 起含实际完成日期）：在行内直接改单元格（项目经理 / 任务负责人（**Push 136 起两列都是多选**）/ 任务状态 / 紧急重要度 / 开始日期 / 预计完成日期 / 实际完成日期 / 预计所需施工人数 / 项目进展描述，见 §6.9）；改日期同时联动「预计所需天数」并按新日期重新派生任务状态；可编辑的单元格套一个白底小胶囊标出可点区域（框 = 判定范围，Push 66 起、Push 67 调整外观）；点行内浮层不触发行选中 / 抽屉。

**6.4 行内四格进度条（Tracker）**

- 4 格状态：全灰 = 未开始；1 格绿 = 刚开工；2 格 = 完成一半；3 格 = 快完成了；4 格 = **已完成**（Push 98：第 4 档档位名由「完成了」改为「已完成」，业务口径 —— 中文档位一律用这个名字，任务表 / 抽屉 / 看板卡片三处一致）。
- 悬停：进度条左侧浮现文字状态并高亮到悬停格；悬浮提示「项目进度：X」。
- 点击第 N 格：进度设为 N/4（前 N 格点亮）；点击当前已点亮的最后一格回退一格（误点修正）。
- 进度条固定在任务描述列内，不随列筛选 / 滚动移动。
- 抽屉的进度区（Push 98）是**进度条 + 条上四颗点**：四颗点**平均分布在进度条上**（1/4、2/4、3/4、末尾各一颗，末尾那颗贴右端），档位文字常显在右上角；**进度条长度不变**，只有填充随档位（悬停时随预览档位）走；点哪颗写哪档、鼠标悬停（或键盘聚焦）即预览到那一档，换算与任务表共用 `trackerNextProgress`（含「点当前已点亮那格回退一格」的误点修正）。任务表仍是「档位文字 + 四格点」（四格点排在文字右边），两处只是摆放不同、口径完全一致。**进度条与四颗点一律用绿色**（与任务表四格点同一个绿，Push 101）—— 进度条只表达「做了多少」，状态色由状态签 / 色点单独表达（原先 已完成 / 提前完成 绿、进行中 蓝、其余 灰，业务反馈「为什么是灰的 应该也要绿的」）。
- 与任务状态联动（Push 65，双向）：
  - 点进度条：进度条 0 格 = 待开始、1~3 格 = 进行中、4 格全亮 = 已完成（填了预计完成日期且实际完成更早 → 提前完成；未填预计完成时间 → 只显示「已完成」）；
  - 选状态：待开始 = 进度条清零；进行中 = 至少点亮 1 格（若原本四格全亮则退回 3 格）；已完成 / 提前完成 = 四格全亮；已延期 = 保持当前格数（0 格也可以是已延期）；
  - 手动选过的状态优先于系统派生（例如已填实际完成日期的任务，把进度条退回 3 格后状态跟随变「进行中」），直到进度条再次被点。
  - 实际完成日期与状态 / 进度同一套口径（Push 67）：填上实际完成日期 = 完成（四格全亮、按工期派生 已完成 / 提前完成）；清除 = 退回进行中（进度 3 格）；反向 —— 把状态手动改成非完成态（待开始 / 进行中 / 已延期）或把进度条点回 1~3 格时，**实际完成日期一并清空**（业务定案 2026-09-19），避免出现「状态还是进行中、却留着实际完成日期」这种自相矛盾的数据。
  - 点进度条不摘掉「已延期」（Push 67 修正）：任务只要已过预计完成日期且未完成，点 0~3 格都保持「已延期」（不再被改成 待开始 / 进行中）；点满四格才算完成、交回完成态派生。

**6.5 任务详情抽屉**

- 触发：点击任务行；从右侧滑入（宽 460px，小屏 94vw）；点遮罩或 Esc 关闭。
- 头部：阶段胶囊、状态胶囊（五态；未完成且逾期时为红色「已延期」）、关闭按钮；中文标题 + 英文标题。**没有「编辑任务」按钮** —— 要改直接在抽屉里改（Push 91：任务编辑弹窗已删除）。
- 进度区：「项目进度」+ 文字档位（右上角）+ **带四颗点的进度条**（Push 98 起可点：四颗点**平均分布在进度条上** —— 1/4 / 2/4 / 3/4 / 末尾各一颗，分别代表 刚开工 / 完成一半 / 快完成了 / 已完成；点哪颗 = 写到哪一档，悬停 / 键盘聚焦即预览到该档，点完即存、右下角闪「已保存」，联动口径与任务表完全一致，见 §6.4）+（逾期时）进度条下方红色提示「已超过预计完成日期（M月D日），当前仍未完成」；进度条与四颗点一律是**绿色**（Push 101，与任务表四格点同色）。
- 字段区（不受表格列筛选影响）：项目经理、任务负责人、任务状态、紧急重要度、是否按时交付（Push 67 起逾期标注落在这一项：逾期未交付 / 逾期已交付）、输出成果文件、文件（全部文件名）、项目进展描述、开始 / 预计完成（Push 91 起合成一行，用日期区间控件）、预计所需天数、预计所需施工人数、实际完成日期（Push 101 起：小日历可直接点选，口径同 §6.9）、变更关联。
- **要改直接在抽屉里改**（Push 91；原「编辑任务」弹窗删除 —— 业务反馈「这个不需要了 要修改直接在抽屉里面修改即可」）：**可编辑** = 项目经理（项目级字段、可多位：Push 136 起为**多选**、至少一位，改后全项目同步）/ 任务负责人（可搜索人员**多选**下拉：Push 136 起可多位、全不选 = 待分配）/ 紧急重要度（高 / 中 / 低）/ 项目进展描述（多行文本）/ 开始 → 预计完成日期（日期区间选择器，与分类筛选同一套）/ 预计所需施工人数（数字，≥ 0 非必填）/ **项目进度（进度条上的四颗点，Push 98；口径同 §6.4）** / **任务状态（五态色标签下拉，Push 101；联动口径同 §6.4 / §6.9 —— 选完成态 = 四格全亮、进行中 = 至少一格、待开始 = 清零、已延期 = 保持格数，改成非完成态时实际完成日期一并清空）** / **实际完成日期（单日期小日历，Push 101；填 = 完成、清 = 退回进行中，口径同 §6.9）**。**保存 = 改完即存**（选择类控件选完即存、日期区间确定即存、人数与描述失焦即存；值没变不写，避免无谓刷新项目时间），存完右下角闪一下「已保存」（约 1.6 秒）。
- **可改字段（Push 101 收口；Push 136 起「项目经理」「任务负责人」两项为多选）** = 项目经理 / 任务负责人 / 任务状态 / 紧急重要度 / 项目进展描述 / 开始 → 预计完成日期 / 预计所需施工人数 / 项目进度（Push 98）/ 实际完成日期（Push 101）—— 与任务表行内（§6.9）、看板卡片（§6.10 / §6.11）共用同一张任务覆盖表与同一套联动口径（§6.4）。
- **只读字段**（派生 / 已锁定，不在抽屉里改）：是否按时交付（读时派生，§6.9 / A14）、输出成果文件（A1-17 锁定）、文件（走站内文件库）、变更关联；页脚常驻「改动即时保存（原型暂存浏览器内存）· 点空白处或按 Esc 关闭」。
- 字段区滚动条：与首页筛选侧边栏同一套自绘悬浮滑块（`ScrollArea`；默认隐藏，滚动时浮现、停止约 1 秒后淡出，可拖动）。

**6.6 字段筛选（列显隐）**

- 表格右上「筛选」按钮，下拉面板列出全部 15 列，可勾选显示 / 隐藏。
- 常显列（任务描述）锁定不可取消，标注「常显」；面板内提供「重置」恢复默认列。
- 默认隐藏：预计所需施工人数；其余默认显示（Push 67 起「文件」「项目进展描述」改为默认显示）。（定档口径已同步：`系统功能书.md` A1-01 与文档头修订，Push 67。）
- 「预计所需天数」依赖开始日期与预计完成日期两列，二者任一隐藏时该列自动隐藏。

**6.7 底部横向滑块**

- **只在「项目总览」出现**（Push 82；两块钱查看板不渲染滑块条）：表格横向溢出时，页面底部（sticky）出现自定义滑块条；不溢出时自动隐藏。
- 支持拖动滑块、点击轨道跳转、键盘左右键（步进 160px）。
- 表格自身原生横向滚动条隐藏，避免与滑块重复。

**6.8 任务编辑（Push 63 弹窗 → Push 91 起在任务详情抽屉里直接改）**

- **口径变化（2026-09-20 业务反馈「这个不需要了 要修改直接在抽屉里面修改即可」）**：任务编辑弹窗（原 `frontend/src/components/TaskEditModal.tsx`）与抽屉头部「编辑任务」按钮**已删除** —— 可编辑字段、控件、保存口径（覆盖表 / 接口）全部不变，只是**入口与形态改为任务详情抽屉内联**（§6.5）。
- 入口：① 任务表任意行（整行点击；任务描述列右侧的入口已随 Push 141 由「编辑任务」铅笔改为「删除任务」按钮，编辑仍走整行点击）；② 两块钱看板的卡片（§6.10 / §6.11）。
- 可编辑字段（口径 = 契约 `TaskUpdateBody` + Q8 定案，全部非必填）：
  - 项目经理：**项目级字段**（Push 136 起 `project.managerIds`，可多位、至少一位）—— 可搜索人员**多选**下拉（人员目录里 role=项目经理 的 20 人；已选以胶囊列出、可单个 ×移除、取消最后一位不生效）；保存后**本项目所有任务行、项目卡片同步**（详情顶栏同步），并刷新项目时间 `updatedAt`；
  - 任务负责人：可搜索人员**多选**下拉（人员目录 31 人；搜索支持姓名 / 拼音 / 岗位；**Push 136 起可多位，全不选 = 待分配**），保存 = 该任务的 `owners` / `ownersEn`（Push 136 起为两组同下标数组）；
  - 开始日期 → 预计完成日期：**日期区间选择器**（与项目空间「分类筛选」侧栏同一套组件与交互口径；「清除」= 两个日期一起清空）；
  - 预计所需天数：随日期区间联动的**只读**展示（含首尾天数，与表格「预计所需天数」列同一算法）；
  - 预计所需施工人数：数字输入（≥ 0、非必填，Q8 定案）；
  - 紧急重要度：普通下拉（高 / 中 / 低；新建任务默认「中」）；
  - 项目进展描述：多行文本。
- 可在抽屉里改的字段 = 项目经理 / 任务负责人 / **任务状态（Push 101）** / 紧急重要度 / 项目进展描述 / 开始与预计完成日期 / 预计所需施工人数 / **项目进度（Push 98）** / **实际完成日期（Push 101）** —— 任务表行内（§6.9）、看板卡片（§6.10 / §6.11）与抽屉共用同一张任务覆盖表、同一套联动口径（§6.4）。
- 只读字段（按字段口径不在抽屉里改）：是否按时交付（读时派生，A14）、输出成果文件（A1-17 生成后锁定）、文件（走站内文件库）、变更关联；提示文案：任务描述 / 输出成果文件按 A1-17 生成后锁定；进度走四格 Tracker；文件走站内文件库。
- 保存：**改完即存**（Push 91；值没变不写），写入任务覆盖表（当前原型未接后端、暂存浏览器内存；换项目 / 刷新即回到初始数据），任务行 / 抽屉 / 汇总卡即时刷新；状态与逾期按新日期重新派生（如预计完成日期推后 → 由「已完成」变「提前完成」，源表口径的工期推延即时可见）；存完抽屉右下角闪一下「已保存」。
- 接口：契约 = `PATCH /api/v1/projects/{id}/tasks/{taskId}`（`TaskUpdateBody`，Push 62 已入契约，`version` 乐观锁）；项目经理回写走项目更新接口（`PATCH /api/v1/projects/{id}`）；任务描述 / 成果文件不在本接口（A1-17 锁定），进度仍走 `/progress`。
- 人员目录（`frontend/src/data/members.ts`）：**全部为虚构姓名**（项目经理 20 人 + 交付成员 11 人：实施 / 机械 / 软件 / 质量 / 采购 / 商务），不对应真实员工；演示任务负责人（`data/tasks.ts`）同步改用该目录人员；接 identity 用户表后由接口数据替换（界面统一按 id 取姓名 + 拼音名展示）。

**6.9 表格行内编辑（Push 64）**

- 入口：任务表里直接点单元格即可改（不必开抽屉）—— 可改字段与抽屉「可编辑字段」完全一致（Push 101 起**任务状态**与**实际完成日期**在抽屉里也能改，此前这两项只在行内可改；**Push 136 起「项目经理」「任务负责人」两列为多选**）：项目经理 / 任务负责人 / 任务状态 / 紧急重要度 / 开始日期 / 预计完成日期 / 实际完成日期 / 预计所需施工人数 / 项目进展描述；单元格悬停浅底、展开中描边。
- 可点区域（Push 66；Push 67 调整外观）：行内可编辑的字段元素都套一个**不显眼的白底小胶囊** —— 静止态 = 白底（`bg-white/75`）+ 淡灰描边（`border-zinc-200/90`）+ 顶部内高光 + 极轻投影（对齐业务样张：鼠标不触碰时就是这个样子），悬停 = 描边加深 + 纯白底 + 稍强投影，展开中 = 纯白底 + 描边 + 浮起投影；框本身就等于鼠标判定范围（不再用负外边距向外扩），用户一眼能看出「点哪里、点得到的范围有多大」；空值（「—」/「待分配」）也带框，提示这里可以填。
- 浮层：点单元格在该格下方弹出小浮层（`fixed` + portal 到 `body`，不被表格横向滚动裁切）；点浮层外、按 `Esc`、或再点同一单元格关闭；浮层里的点击**不冒泡到行**（不触发行选中 / 任务详情抽屉）。
- 人员列（项目经理 / 任务负责人）：**Push 136 起两列都是多选**（可搜索人员下拉；已选以胶囊列出、可单个 ×移除、点「继续添加」接着勾，浮层不随勾选关闭；多位按「、」连接展示、过长悬浮标题看全）；项目经理是**项目级字段**（改后本项目所有任务行 + 项目卡片同步，并刷新项目时间 `updatedAt`；**至少留一位，取消最后一位不生效**）；任务负责人**可多位、全不选 = 待分配**。
- 任务状态：下拉选 已延期 / 进行中 / 已完成 / 待开始 / 提前完成（选项为色标签，顺序同业务截图），与四格进度条双向联动（口径见 §6.4）；手动改成非完成态（待开始 / 进行中 / 已延期）时，实际完成日期一并清空（Push 67 业务定案）；
- 紧急重要度：普通下拉（高 / 中 / 低）。
- 日期列（开始日期 / 预计完成日期）：单日期小日历（上 / 下月、清除、今天）；改任一日期会同时写回开始 / 预计完成日期与**联动天数**（含首尾），任务状态（待开始 / 进行中 / 提前完成 / 已延期）按新日期即时重新派生（手动选过状态时以手动为准）。
- 实际完成日期列（Push 67）：同样是单日期小日历（上 / 下月、清除、今天）—— **填日期 = 完成**（四格全亮、按工期派生 已完成 / 提前完成）；**清除 = 退回进行中**（进度写回 3 格、状态「进行中」）；填 / 清都即时同步任务行 / 任务详情抽屉 / 汇总卡。
- 预计所需施工人数：数字输入（≥ 0、非必填，留空 = 0），Enter 保存、`Esc` 取消。
- 项目进展描述：多行文本（Enter 换行），保存 / 取消按钮。
- 与抽屉的关系：共用同一张任务覆盖表 —— 行内改完，任务行 / 任务详情抽屉 / 汇总卡即时同步；其它列仍只读（任务描述 / 输出成果文件按 A1-17 锁定；进度仍走任务描述列里的四格 Tracker；文件走站内文件库）。
- 接口：与抽屉同一条（`PATCH /api/v1/projects/{id}/tasks/{taskId}`，`TaskUpdateBody`，只提交被改字段）；项目经理回写走 `PATCH /api/v1/projects/{id}`；当前原型未接后端、暂存浏览器内存。

**6.10 人员任务分配看板（Push 82）**

- 列 = **任务负责人**（人头维度）：列头 = 头像（人员目录按 id 稳定取色 + 姓名首字；不在演示目录里的用灰底首字兜底）+ 姓名 + 「N项」；列顺序按任务在该项目里的出现顺序，**「待分配」（负责人为空）固定垫底**。**Push 136 起任务负责人可多位**：一张任务卡会出现在**每一位负责人的列里**（列计数 = 该人手上的任务数；同一任务可在多列重复出现），一位都没挂才落「待分配」。卡片本身在这块看板不单列「任务负责人」（列头就是负责人；要挂多头请到任务表行内或抽屉里改），完整名单在任务进展看板的卡片（§6.11）/ 任务表 / 任务详情抽屉里看。
- 卡片（点开任务详情抽屉 —— 字段在抽屉里直接改，与表格共用同一张字段覆盖表）：任务标题（中 / 英；Push 102 起**标题加粗加深** —— `font-bold` + `text-zinc-900`，与卡片里浅灰字段名、深灰取值拉开层级，业务反馈「任务的标题加粗加深」）+ 预计完成日期 + 任务状态色标签 + **实际完成日期（Push 98：卡片上直接点选小日历就能改，填 = 完成、清 = 退回进行中，口径同 §6.9）** + 项目进度条与**中文档位**（Push 98：原来写百分比，改成 未开始 / 刚开工 / 完成一半 / 快完成了 / 已完成，与任务表 Tracker 同一套标签）+ 是否按时交付（逾期标注优先，口径同 §6.9 / Push 67）+ **「所属阶段」色签**（按阶段固定色；看板里直接建的任务没有阶段，显示「未分组」）。卡片只出任务里真实存在的字段 —— 任务字段里没有「里程碑」这一项（`系统功能书.md` A1-11：里程碑属二期），**卡片不写「阶段性里程碑」字样**（2026-09-20 业务口径：阶段要写，但栏目名就叫「所属阶段」）。
- **卡片材质按业务样张代码还原**（外层壳 + 细纹叠加 + 三层投影）：外壳内边距 9px / 圆角 35px / 白色壳 + 发丝边 + 柔和投影 + 底部内阴影，内层板（圆角 30px / 近白面板 + 发丝内边）**已按业务口径整块去掉** —— Push 106 业务反馈「只保留外框 内框不要了」，内容直接落在外壳上、只留原来的内边距（**配色为白色** —— 样张原色 `#e8e8e8` / `#e2e0e0` 偏灰，2026-09-20 业务反馈「太灰，还是要白色」后改白），实现在 `frontend/src/components/TaskKanban.tsx` 的 `CARD_SHELL` / `CARD_NOISE` / `CARD_BODY`（原 `CARD_INNER` 已去掉），用 Tailwind 任意值还原（**不引入 styled-components**，与仓库既有口径一致）。
- **列底「添加」固定在列底、不随卡片滚动**（Push 86；按钮在滚动视口之外，滚到哪都看得见、位置不跳）。点开是两个入口：
  - **临时任务**（自己填内容）：弹出小表单 —— 任务名称（必填）+ 英文名（可留空），点「创建」直接挂到该列（负责人 = 该列的人（**Push 136 起 = 只挂这一位**）、状态「待开始」、进度 0、紧急重要度「中」），**阶段留空** → 到「项目总览」落在「未分组」组（§6.3）；后续可在任务详情抽屉里补字段。
  - **阶段任务**（从模板里选）：先选阶段（9 个施工阶段、顺序同「项目总览」），再打开该阶段的**「任务节点 + 模板」卡片**（与总览页同一张：任务节点池 + 各块模板，点一条即加进项目、已加过的按节点 id 判重显示「已添加」、模板标签可「整套添加」）；卡片贴在该列右侧、与列顶齐平（`Esc` / 点卡片外空白处关闭，可连点加多条）。加进来的任务**自带阶段**，并带上所在列的负责人 / 状态（**Push 136 起 = 挂这一位负责人**；要挂多头请到任务表行内或任务详情抽屉里改）。**Push 113 / 114 起**点「＋ 添加」的落位分两种模式：卡片「默认顺序添加」勾选（默认）= 按顺序直接加，取消勾选才弹**位置浮层**选插入位（详见「添加任务时的插入位」条）。
- **列内滚动（Push 85 / 86 / 87）**：列高按「铺满视口」算（`h-[calc(100vh-12.75rem)]`，上限 52rem、下限 22rem；Push 87 从 `100vh-15.5rem` 调高，列底不再在页面下部留一大块空白），**列头（头像 + 姓名 + 「N项」）固定不动**、**列底「添加」固定在列底**，中间卡片列表在列内纵向滚动 —— 卡片再多也不会把整列 / 页面一直往下拉长，页面高度保持稳定。
- **滚动条是隐式的**（Push 86 / 87）：**列内纵向**与**看板横向**都隐藏原生滚动条，改由 `ScrollArea` 自绘悬浮滑块（与分类筛选侧栏 / 任务抽屉同一套）—— 默认透明，滚动 / 悬停时才浮现，不占列宽、不打断卡片视觉。横向滑块浮在列底预留的 12px 留白里（Windows 下那条带左右箭头、带灰色凹槽的原生横杠不再出现）。
- 列数超出屏宽时看板横向滚动（横向滚动条同样隐式，见上一条）；项目没有任务时显示空态提示（引导到「项目总览」点阶段标签、从任务节点 / 模板添加）。
- **卡片可拖动换列（Push 104）**：按住卡片拖到别的列放开，等价于「编辑」—— 人员任务分配看板 = **改任务负责人**（**Push 136**：拖到某位负责人的列 = 该任务的负责人**整体换成这一位**（多负责人任务会收敛成单人；要挂多头请在任务表行内 / 抽屉里改）；拖到「待分配」列 = **清空全部负责人**、回到待分配），与任务表行内编辑（§6.9）、抽屉（§6.5）**共用同一个 `onPatchTask`（同一张覆盖表，§6.4）**。拖动中的卡片悬浮提示「按住拖到别的列 = 移到那个组（负责人 / 状态跟着改）；点一下看任务详情」；只读场景（未传 `onPatchTask`）卡片不可拖。
- **拖动落点 = 列内插入位（Push 105）**：业务反馈「要有合适的交互显示拖动后的位置」—— ① **拖动中的卡片样式保持原样**：不淡出、也不做「拿起来」的实体态（业务口径「不做成实体化了」，拖影用浏览器默认），只在拖动时把光标换成抓手；② **落点 = 列内插入位**：鼠标拖到哪两张卡片之间，那里就浮出一块**绿色虚线槽位**（换列 = 「放开：移到「X」」、同列 = 「放开：放到这里」），放开就插到那个位置 —— **同一列内也能拖着换顺序**；③ 槽位按鼠标与卡片中线算（某张卡片中线以上 = 插到这张前面、都在上面 = 插到列尾）；**目标列本来就没有别的卡片时只换列、不动顺序**。列内顺序存「看板顺序表」（原型内存态，与任务覆盖表同一层；项目总览任务表同阶段内顺序跟着走），后端落库口径见 §3.8 A19；目标列整圈描边高亮保留。
- **拖动 = 指针拖动 + 滚轮自己控制（Push 108）**：业务反馈「我不想要自动滚动，想要鼠标控制」—— ① 拖动**不再用浏览器原生 HTML5 拖拽**（原生拖拽期间浏览器会把 `wheel` 吞掉，滚轮根本到不了页面），改成**指针拖动**：按住卡片、位移超过 4px 才算拖动（没过阈值 = 「点一下」，照旧打开任务详情抽屉；卡片里的小日历等可点元素按下不算拖动）；
  ② 拖起来之后**滚轮直接可用**：指针在列上滚 = 翻这一列的卡片（原生滚动；鼠标不动、列在滚时，插入槽位按帧重算，不会停在旧位置），**Shift + 滚轮 / 横向滚轮 / 触控板左右滑 = 翻列**（横向看板跟着滚）；
  ③ 放开时落在哪一列的第几格就插到那里（换列 = 改负责人 / 改状态，口径同 §6.9 与 §6.4；同列 = 只排顺序），Esc / 指针取消 = 原地取消；④ 拖动中卡片样式照旧（不淡出、不抬起），落点仍是**列内绿色虚线槽位 + 整列描边高亮**（插到哪两格之间就浮在哪里）。
  ⑤ **拖动中有一块跟着鼠标走的「拖动卡片」**（Push 109）：内容就是这张卡本身（同一套外壳材质 —— 发丝边 + 三层投影 + 细纹，底色口径见 ⑥），按抓取点对齐鼠标、只跟着鼠标走，`pointer-events-none`（不挡落点判定、不进无障碍树）；原地那张卡片照旧不动、不淡出、不抬起。
  ⑥ **拖动卡片是半透明的**（Push 110）：业务反馈「还是要透明的吧」—— 拖动卡片的外壳从实心白换成**半透明白（不透明度 60%）+ 轻磨砂**（`backdrop-blur(5px)` + `saturate(1.5)`，与本应用浮层同一套「液态玻璃」手感），压住底下的卡片 / 绿色虚线槽位时能透出去、看得出落点在哪儿；投影保持卡片本体同一条（**不因透明而减淡**，业务此前提过「卡片阴影也没了」），原地那张卡片照旧不动、不淡出。拖动卡片不进无障碍树，也不占 Tab 焦点（`tabIndex = -1`，`aria-hidden` 里不放可聚焦元素）。
  Push 107 的「拖到边缘自动滚动」与 Push 104 的原生拖拽**均已撤回** —— `ScrollArea` 回到最朴素的「原生滚动 + 自绘滑块」。
- **列内顺序 = 项目总览的顺序（Push 111）**：业务口径「我想了一下不对 应该按照项目总览的顺序排 —— 某个工作人员在项目总览下的顺序，他的从上到下的任务排」。前端口径：展示顺序 **阶段为主键**（项目总览的分组顺序：售前规划 → 设计开发 → 加工采购 → 组装发货 → 硬件实施 → 软件部署 → 试运行 → 生产阶段 → 验收 → 未分组垫底）、**组内按看板顺序表**（拖动 / 添加时定的先后）—— 两块看板与项目总览任务表拿到的是同一份顺序，所以「人员在项目总览里从上到下读到的顺序」与他在人员任务分配看板里那一列的顺序一致。
- **添加任务时的插入位（Push 111 / 113 / 114 / 115 / 117）**（业务口径「人员要指定位置放入」）：卡片提示行右侧有一枚复选框「**默认顺序添加**」（**Push 114**，业务口径「影响正常情况下添加任务了 所以默认是顺序添加 要改的话手动改」）——**默认勾选**：点「**＋ 添加**」/「整套添加」**直接按顺序加**（不动顺序表、落在它那个阶段那一段的末尾）；**取消勾选**之后，点「＋ 添加」**先贴这一行浮出位置浮层**、选完才真的加进项目（**Push 113**，业务口径「我要点击这个添加后选择位置」）——浮层里前两档固定（`该阶段最后（默认）` / `该阶段最前`），下面按**当前阶段的顺序**列出该阶段任务（固定高度 176px、隐式滚动条、可滑动；鼠标放上去高亮并浮出「插到它后面」，**点一条 = 插到这张任务之后**）；浮层里点任意一档 / 按 `Esc` 都会关掉浮层，**不选位置就不会加进项目**（`Esc` 第一次只关浮层、再按一次才关卡片）；点浮层不算「点卡片外面」，卡片不关。**每次打开卡片，复选框都回到默认勾选**；复选框的勾是**黑色**、画法与业务样例逐字对齐（勾上时方框在**右上角开一个缺口**、勾线从缺口穿出去，未勾选是一个**干净的圆角方框**、没有多余的点；样例里写死的两个 `stroke-dashoffset`（方框 73 / 勾线 1）是这套动画的关键 —— 少了它们未勾选会多出一个小圆点、勾上时缺口也不会落在右上角；键盘焦点圈已去掉，**Push 117**），提示行只留「已添加 N」（模板页 =「模板预览 · 已添加 N」）—— 不再写「点一条节点就加进项目」「（鼠标滚动看全）」这类说明（**Push 115**）。列底「添加 → 阶段任务」与**项目总览阶段标签卡片**（§6.3）是同一个入口，两边都是这一套（走同一张顺序表）。新加的任务还没有开始日期，所以默认就落在**它那个阶段那一段的末尾**；同一阶段段内拖动照旧跟手，跨阶段段拖动会按阶段主键归位（回到它自己阶段段里最近的位置）。「整套添加」一次加多个时**按列表顺序整段插在锚点位置**（不会倒序）。「添加 → 临时任务」的阶段留空（未分组垫底），插入位置固定为末尾。
- **列底「添加」浮层同一时刻只开一个（Push 118）**（业务口径「这有bug吧 不能同时打开 点击别的应该关闭另一个吧」，截图 = 人员任务分配看板六列的「添加任务」菜单同时开着）：原实现里浮层的打开态是**每列一份**（`KanbanColumn` 的列内 state），六列互不感知、点哪列开哪列，所以可以同时挂着好几个菜单；现在把这份状态提到 **`TaskKanban`（整块看板一份、单值 `AddOverlay`）**——浮层记「开在哪一列 + 是哪一种」（`menu` 添加菜单 / `temp` 临时任务表单 / `stage` 阶段选择 / `template` 阶段任务的模板卡片），每列只渲染 `key` 等于自己的那一份。**点别的列的「添加」= 那一列直接接管**（上一列的菜单 / 表单 / 阶段选择 / 模板卡片立刻关掉，`aria-expanded` 同步）；同一个按钮仍是**开关**（再点一次关掉）；点列头 / 卡片等浮层外的任何地方、按 `Esc` 照旧关掉；模板卡片自己关的时候只关 `template` 那一种（关晚一步也不会误伤刚打开的菜单）。临时任务表单里没提交的内容在浮层关掉后清空（下次打开是空表单）。

**6.11 任务进展看板（Push 82）**

- 列 = **任务状态**，固定五列、顺序为 **已延期 → 进行中 → 已完成 → 提前完成 → 待开始**（业务定稿；**空列也保留**，最少只有「+ 添加」）。
- 列头 = 状态色标签（五色签口径同任务表 §6.3 / §6.9）+ 「N项」；任务状态取派生结果（`taskStatus`，与任务表同一个函数：手动状态优先、完成态按工期派生「已完成 / 提前完成」、过期未完成 = 已延期）。
- 卡片：任务标题（中 / 英；标题加粗加深同 §6.10，Push 102）+ 任务负责人（头像 + `姓名(拼音)`）+ 开始日期 + **实际完成日期（Push 98：可直接点选改，口径同 §6.10）** + 项目进度条与**中文档位**（Push 98，标签同 §6.4）+ 是否按时交付 + 「所属阶段」色签（口径同 §6.10）；卡片材质与交互同 §6.10，同样不写「阶段性里程碑」字样。
- **列底「添加」固定在列底**（Push 86），两个入口同 §6.10：**临时任务**（自己填名称 / 英文名）建的任务挂到该列 —— 状态 = 该列的状态、进度按状态联动口径给（待开始 0 格 / 进行中 1 格 / 已完成与提前完成四格全亮 / 已延期保持 0 格，见 `progressAfterStatus`），负责人留空（列在「待分配」）、阶段留空 →「未分组」；**阶段任务**选阶段后从「任务节点 + 模板」卡片里挑节点加进项目（自带阶段 + 带上该列状态；**Push 114**：卡片上的「默认顺序添加」默认勾选 = 点「＋ 添加」直接按顺序加，取消勾选才弹位置浮层选插入位，口径同 §6.10）。
- **列内滚动（Push 85 / 86 / 87）**：口径同 §6.10 —— 列高按铺满视口算、列头（状态色签 + 「N项」）固定、「添加」固定在列底、卡片列表在列内纵向滚动，纵向 / 横向滚动条都是**隐式**的（滚动 / 悬停才浮出自绘滑块），页面高度不再随卡片数量延伸、列底也不留大块空白。
- **卡片可拖动换列（Push 104）**：口径同 §6.10，拖到别的状态列放开 = **改任务状态** —— 与任务表行内（§6.9）/ 抽屉（§6.5）同一套联动：同时写四格进度（`progressAfterStatus`：待开始 0 格 / 进行中至少 1 格 / 已完成与提前完成四格全亮 / 已延期保持原格数），并**在改成非完成态时清空实际完成日期**（Push 67 定案，§6.4 / §6.9）；卡片上的进度档位、实际完成日期与「是否按时交付」随之同步。两块看板互通（写的是同一张覆盖表，拖完切另一块看板 / 项目总览都看得到）。
- **拖动落点 = 列内插入位（Push 105）**：口径同 §6.10 —— 拖动中卡片样式保持原样（不淡出、也不做实体态，只换抓手光标）、落点是**列内插入位**（绿色虚线槽位：「放开：移到「X」」/ 同列「放开：放到这里」），同一列内也能拖排序；空列只换列、不动顺序。顺序同样是「一套全局顺序、两块看板共用」（见 §6.10 与 A19）。
- **拖动 = 指针拖动 + 滚轮自己控制（Push 108）**：口径同 §6.10 —— 位移超过 4px 才算拖动、拖动中滚轮翻卡片（Shift + 滚轮 / 横向滚轮翻列）、放开落在哪一格就改状态并联动进度 / 实际完成日期；原生 HTML5 拖拽与 Push 107 的「拖到边缘自动滚动」均已撤回。拖动中另有一块**跟着鼠标走的拖动卡片**（Push 109；Push 110 起改成**半透明 + 轻磨砂** —— 材质与投影同卡片本体、投影不减淡，压住底下的落点槽位时能透出来）。列内顺序同样按 §6.10 的 Push 111 口径（**阶段为主键** + 组内看板顺序表），「添加 → 阶段任务」同样可指定**插入位置**（该阶段最后 / 最前 / 在《某张同阶段任务》之后；展开样式同 §6.10 的 Push 112 口径：前两档固定 + 当前阶段任务顺序列表，固定高度可滑动，点一条 = 插到它后面）。

**6.12 日报及问题（Push 128）**（业务口径：①「标签导航栏旁边新加『日报及问题』导航」—— 项目详情页顶部标签栏的第四个视图；②「把这个部分做成页内导航栏，参考样式如图二，第一个日报填写、第二个日报记录、第三个问题追踪（用表格记录日报填写的问题）、第四个问题看板」—— 视图内部再分四块子视图，顶部统计条改成**页内导航栏**；③「导航栏换成这个四个按钮吧，放在标签栏下方，大小不用太大」—— 页内导航栏改为**四个独立的玻璃胶囊按钮**，紧贴顶部标签栏正下方一排、尺寸收紧）：
项目详情页顶部标签栏在三视图之后**新增第四个视图「日报及问题」**，视图内部是**页内导航栏 + 四块子视图**，口径承 `系统功能书.md` A3「日报与问题跟踪」（日报字段 A3-01、草稿与补填 A3-02、提交校验 A3-04、自动生成问题 A3-09、问题四态 A3-10；处理时限 SLA 见 ADR-026）。
① **顶部标签栏**：项目总览 / 人员任务分配 / 任务进展 / **日报及问题**（新标签排在「任务进展」之后，样式与前三块完全一致：选中 = 深色下划线 + 深色文字、未选 = 灰、悬停加深）；列显隐（`ColumnPicker`）与底部横向滑块仍**只在「项目总览」**出现。
② **页内导航栏**（业务样张三轮：iOS 风毛玻璃胶囊 → 玻璃胶囊按钮 → **键帽按钮**，给的都是 styled-components 代码，按 ADR-012「React 19 + Tailwind 4 + 自研组件」的前端栈用 **Tailwind 任意值等价还原**同一套视觉口径，不新增样式库依赖、**不引入 styled-components**）：**四个独立键帽按钮**（不再是占满整行的大胶囊）—— 从左到右 **日报填写 / 日报记录 / 问题追踪 / 问题看板**，每项 = 16px 线性图标 + 单行 13px 文字；键帽 = **白面**（未选中与当前项**同面**；业务口径「灰色的不太好看，我想不点击也是问题看板这个时候的颜色」）+ **0.5em 圆角** + 样张那串**实心堆叠投影**（内高光 `inset 0 .0625em 0 #fff` + 7 层侧壁 `0 .0625em #f2f2f2` → `0 .425em #cacaca` + 末层柔和落影 `0 .425em .5em #cecece`；全部 em 口径，字号一改整套厚度自动跟缩）+ `text-shadow: 0 .0625em 0 #fff`，`transition: .15s ease`；悬停 = 面微压暗（`#fff` → `#fafafa`）+ 字色转深，**按下 = `translate: 0 .225em` + 堆叠压扁**（样张 `button:active`）；**当前项与未选项靠文字区分**：当前项 = 黑字（字重 700）、未选项 = 中灰字（字重 600，悬停转黑）；**位置 = 顶部标签栏正下方一排、与标签栏左对齐**（间距 24px），尺寸收紧（单个约 100×26px，另加拨起的侧壁）；默认停在第一项「日报填写」，点哪项切哪块（子视图切换不进 URL、与顶部标签栏一致）。
③ **日报填写**（A3-01 字段，A3-04 校验）：项目名称（自动带入、只读）/ **时间**（日期，必填，默认今天；**用站内日期选择器** = `DateRangePicker` 的**单选模式**（本轮新增 `mode="single"`），与分类筛选 / 任务编辑同一套弹层口径 —— 月份箭头 + 「N年N月」+ 一~日 表头 + 清除 / 确定，**不用浏览器原生 `input[type=date]`**） 提交人（当前登录用户，只读）/ 今日施工人数 / **关联任务**（项目现有任务多选，可滚动；「当日完成工作」关联任务后回写任务「项目进展描述」）/ **当日完成工作**（多行，必填）/ 明日计划 / **现场发现问题**（多行；**填了就自动生成问题记录**）/ **问题归类**（C9 字典十项；「现场发现问题」非空时**必填**，为空时置灰）/ 当前问题附图 / 解决方案或建议 / 现场工作附图（原型只记文件名，正式版走站内文件库）；底部 = 「提交日报」（必填未齐时置灰 + 提示「还差：…」）+「暂存草稿」。**提交**后：写进「日报记录」并自动切到该块 + 顶部提示（含「已自动生成问题记录（未分组）」），表单复位；**暂存草稿**只写「日报记录」（状态 = 草稿），不生成问题。
④ **日报记录**：**列表 / 表格**（业务口径「日报记录还是做成列表 不要卡片」；一行一篇、按日期倒序 新 → 旧，窄屏横向滚动）—— 列 = 时间（+ 状态签 草稿 / 已提交 / 补填 + 小字提交时间）/ 填写者（头像 + 姓名）/ 今日施工人数 / 关联任务 / 当日完成工作 / 明日计划 / 现场发现问题（归类签 + 描述 + 「已生成问题记录 · 《该问题当前态》」）/ 解决方案或建议 / 现场工作附图；表壳与「问题追踪」同一套（白底 + 圆角边框 + 行悬停）。
⑤ **问题追踪**：**表格**（列 = 问题描述 / 问题归类 / 状态 / 提出人 / 提出日期 / 处理时限 / 责任 / 所属任务 / 解决方案 / 回复；横向可滚、行悬停高亮），按提出日期倒序；「责任」空 = 「待分派」「解决方案 / 回复」空 = 「—」。
⑥ **问题看板**：四态看板（**未分组 → 未解决 → 处理中 → 已完成**，空列保留，列头 = 状态色签 + 「N 项」），卡片 = 问题描述 + 问题归类 + 责任 + 提出日期 + 处理时限 + 所属任务 + 解决方案 / 回复（有才显示）。
⑦ **原型边界**：日报 / 问题**存浏览器内存**（与任务覆盖表同一层，换项目 / 刷新即重置）；演示数据（6 篇日报 + 5 条问题）只挂在示例项目印度 `inmu-0010`，其余项目从空白开始（四块子视图各自空态）；问题**状态流转 / 拖动改状态、回复解决方案、日报编辑 / 补填**待后续迭代。
⑧ **接口需求（提案，见 §3.8 A21）**：`GET /api/v1/projects/{id}/reports`、`GET /api/v1/projects/{id}/issues`（筛选 + 分页）；填报 / 暂存走 `POST/PATCH /api/v1/reports…`、问题状态流转与回复走 `PATCH /api/v1/issues/{id}`。
⑨ **列宽（业务口径「页面太宽大了，可以窄一点」→「我只要日报填写页面居中然后尺寸舒适一点、像一个表单，其它的不变还是全屏」）**：视图整体**全宽**（不封顶、不居中）—— 「日报记录」「问题追踪」「问题看板」照旧铺满内容区；**只有「日报填写」**收成**居中窄栏**：内容区里 `mx-auto w-full max-w-3xl`（768px，像一张表单），表单首行四个字段由 `xl:grid-cols-4` 改成**固定两列**（`md:grid-cols-2`，窄栏里 4 列会挤）；实测 2300px 下表单宽 768、居中（左沿 759 = (2285 − 768) ÷ 2）、页内导航栏仍贴顶部标签栏左沿（24px）、问题看板与表格 / 日报记录仍铺满 2252px 且无横向滚动。

**6.13 醒目模式（Push 134）**（业务口径：①「增加一个按钮 下面是参考代码 图二图三为参考样式 我要旁边的文字改成 醒目模式 然后这个颜色改成图一中各个状态的颜色的平均占比」；②「按钮加在它的旁边」= 放在项目总览右上角「筛选」按钮的左边；③「当启用醒目模式时 项目总览的卡片…都会变成 图一状态的颜色 没有使用 则保持现状 默认不启用」，随后更正为「是整个这一行都变色」；④「正常模式下 这个颜色填满胶囊试试」= 状态列的色签底色铺满整颗胶囊、⑤「紧急程度也要」= 紧急重要度列同样收口）：

- **位置与样式**：只在「项目总览」出现（与列显隐同一排），位于右侧工具区、「筛选」（`ColumnPicker`）按钮**左边**（间距 16px）。外形对齐业务给的参考代码 + 两张样图 —— 未勾选 = **白底 + 2px 灰描边圆角方框**（**20×20**、圆角 5px、常驻柔影，鼠标悬停微放大 1.05；Push 134 收口：业务反馈「这个按钮小一点」，方框由 32 收到 20）；勾选 = 方框整体换成**五个状态色的平均占比色带**（已延期 / 进行中 / 已完成 / 待开始 / 提前完成**各占 1/5**，取五色 500 档实色 `#f43f5e` / `#f59e0b` / `#10b981` / `#0ea5e9` / `#d946ef`）；右侧文字固定「醒目模式」（14px 中灰，不随悬停变色）；按样图**不带勾线**。
- **默认不启用**：进项目 / 刷新都是关；打开状态存浏览器内存（换视图再回来仍开着、换项目保留），正式版是否落本地记忆待业务定。
- **打开后**：项目总览任务表**每一行整行铺该任务状态的底色** —— 底色 = 图一那五个状态色系**压到 6% 不透明度**（马卡龙级极淡，只隐约看得出色彩区分、不抢眼；行悬停抬到 12% 留住「这一行可点」的手感）。**状态列同时收口**（业务口径「整行已经有状态颜色了，内部的『已完成』『已延期』等 Tag 取消白底」）：撤掉「液态玻璃」白底小框与色签底色，只留该状态的深色字（`text-rose-700` / `text-amber-800` / `text-emerald-700` / `text-sky-700` / `text-fuchsia-700` + 加粗，**透明底**；点开仍是状态下拉，悬停只给一点淡淡的可点提示，不再出现白底）。选中行的琥珀色竖标线（`inset 3px`）与「点整行开任务详情」交互照旧，键盘聚焦走 inset 焦点圈（不换底色）；**关掉 = 行保持现状**（白底行 + 行悬停灰），状态列则按下面的「填满胶囊」口径收口。阶段分组头 / 表头不参与上色；其它列（负责人 / 紧急重要度 / 日期…）的液态玻璃小框照旧。
- **正常模式的状态列 / 紧急重要度列 = 色签填满整颗胶囊（Push 134 收口，业务口径④「正常模式下 这个颜色填满胶囊试试」+ ⑤「紧急程度也要」）**：状态列 / 紧急重要度列原来是「白底玻璃小框 + 内层色签」两层，现合并成**一颗** —— 图一该状态的色签底色**直接铺满整颗胶囊**（没有白底、没有外层描边、没有内层留白），文字仍是该状态的深色档（已完成 `text-emerald-700` / 进行中 `text-amber-800` / 已延期 `text-rose-700` / 待开始 `text-sky-700` / 提前完成 `text-fuchsia-700`），悬停时胶囊加深一档（如已完成 `bg-emerald-100` → `hover:bg-emerald-200/70`）当作「这里可点」的提示；点开仍是同一套状态下拉，字段口径与联动（四格进度 / 实际完成日期）完全不变。**紧急重要度列同口径**：高 `bg-rose-100` + `text-rose-600` / 中 `bg-amber-100` + `text-amber-700` / 低 `bg-zinc-100` + `text-zinc-500`，底色由原标签的 50 档抬到 100 档（铺满整颗胶囊后 50 档几乎看不出颜色），字色沿用原标签、悬停同样加深一档；关 / 开两个模式都按「填满胶囊」渲染（紧急重要度与整行底色无关，不随醒目模式再变）。其它列（项目经理 / 负责人 / 日期…）的白底小框照旧。
- 状态口径取 `taskStatus`（与状态列、两块看板同一个派生函数）：手动状态优先、完成态按工期派生 已完成 / 提前完成、过期未完成 = 已延期 —— 所以行内下拉 / 抽屉 / 看板拖动改状态后，整行颜色立刻跟着变。
- 实现：新增 `frontend/src/components/FocusModeToggle.tsx`（受控开关组件）；`ProjectDetail.tsx` 新增 `focusMode` 状态、渲染在 `ColumnPicker` 左边并透传给 `TaskBoard`；`TaskBoard.tsx` 新增 `STATUS_ROW_CLASS`（状态 → 整行底色）、`STATUS_CAPSULE_CLASS`（状态列：色签填满整颗胶囊）与 `PRIORITY_CAPSULE_CLASS`（紧急重要度列同口径）与 `STATUS_TAG_TEXT_CLASS`（醒目模式下状态列的字色）与 `focusMode` 开关，`TaskRow` 按它切换行底色与状态列样式；`InlineEdit.tsx` 的 `InlineCell` / `InlineOptionCell` 补 `bare`（裸框模式：撤掉白底小框，不传 = 现状逐字不变）与 `triggerClassName`。不改任务字段、不改数据模型、无新接口需求。
- 实测（无头 Chrome 截图，1560 宽，临时预览页 = 顶栏工具区 + `TaskBoard`，数据 `inmu-0010`）：关 = 行白底（行本身与现状逐像素一致），状态列与紧急重要度列 = 整颗胶囊铺该色签色（无白底、无外层描边、无内层色签；取色实测胶囊 已完成 57×29 = `bg-emerald-100`、高 35×29 = `bg-rose-100`、中 = `bg-amber-100`）；开 = 取色实测已完成行 `rgb(240,251,247)`、已延期行 `rgb(255,242,245)`（= 6% 档马卡龙浅色，白底上只隐约区分）；状态列只剩深色字（`已完成` emerald-700 加粗 / `已延期` rose-700 加粗）、**白底小框与色签底色都不再出现**，点开仍是同一套状态下拉；开关勾选态的色带 = 五色各 1/5；「筛选」按钮位置不变。

**6.14 甘特图（Push 145）**（业务口径：①「我想做甘特图」；②「在标签导航栏加一个甘特图页面」；③「我的甘特图参考图一的开源网页 想要复刻图一的甘特图」；④「维持手绘尽量还原官方的功能」—— 仍**不引第三方图表库**，把官方甘特图的可交互能力逐项补齐，参考 https://visactor.io/vtable/demo/gantt/gantt-basic?version=1.26.6）

- **入口与位置**：项目详情页顶部标签栏的**第二个**标签「甘特图」（排在「项目总览」之后）；点开即渲染，标签只是页内视图（不是路由），刷新 / 切走再回来仍回到「项目总览」。
- **形态（对照参考样例）**：**左侧固定任务列表**（行号 / 任务 / 开始日期 / 预计完成日期 / 进展）+ **右侧时间轴** —— 左表横向不滚（`sticky left-0`）、表头纵向吸顶（`sticky top-0`），两边共用一个滚动容器、逐行靠同一组行高常量对齐（不靠 JS 同步滚动）；时间轴表头两级：上行 = 月份（如「2026年9月」，按天数合并列），下行 = 日期（日粒度逐日、周粒度逐周、月粒度逐月 —— 粒度可切，见下条；日粒度下周末灰字 + 浅灰底纹，每周一与每月一号各一条略重的分隔线）。行高 36px、表头 2×30px、左表 404px（行号 48 + 任务 284 + 进展 72），每天宽度随缩放 18 / 26 / 36px。**左表宽度可拖 = 移动「遮挡边界」**（官方 `frame.verticalSplitLineMoveable`）：拖左表右边线在 188~572px 之间（默认 572px = 各列全露、双击复位）—— **列宽固定不变**（行号 48 / 任务 284 / 开始日期 72 / 预计完成日期 96 / 进展 72，合计 572），拖窄了是被右侧时间轴**盖住 / 裁掉**、不是把列挤窄（业务口径「右侧看板是遮挡 不是压缩左侧」：整行按自然宽渲染、`clip-path: inset(0)` 只裁不重排，列宽一直不变，也不新建滚动容器、不影响 sticky）；拖动时那条分隔线亮蓝（对应官方 `verticalSplitLineHighlight`：2px 蓝线压在分隔线本身上）。分隔线把手与高亮线画在裁切层**外面**（裁切层只包住表头 + 数据行）—— 否则把手外侧那 8px 抓手与悬停高亮会被一起裁掉；拖动期间另在 window 上挂一层 `pointermove` / `pointerup` 兜底（指针捕获失败、指针被拖出窗口时照样跟手）。
- **时间轴粒度 = 日 / 周 / 月**（官方 `timelineHeader.scales`，业务口径「先做这两个」）：工具条「粒度」三选一 —— **日**（下排逐日、周末底纹、逐日竖线）/ **周**（下排逐周，标签 = 该周首日「5/18」，列宽 = 7 × 每天宽度，竖线改周线，去掉周末底纹）/ **月**（下排逐月「5月」，上排改年份「2026年」，列宽按当月真实天数 28~31 天展开，月末另加一条略重的月线 + 每年一号加年线）。**换粒度只换表头分段与网格密度，不换条的位置口径**：几何基准始终是「每天多少 px」，任务条 / 里程碑 / 实际完成刻度 / 今天线一律按天精确落点（周粒度实测「规划设计」5月15日 → 5月20日 正好落在「5/13」列内、右端落在「5/18」列内）。三档缩放对每档粒度都成立（列宽 = 一列跨几天 × 每天宽度：标准档 日 26px / 周 91px / 月约 138px）。
- **甘特内筛选**（B3-06 余下，业务口径「先做这两个」）：工具条第二行 = **负责人**搜索下拉（业务口径「下拉框组件设计 保持一致 搜索下拉框」：与人员下拉 / 任务表行内成员单元格**共用同一套搜索列表** —— 搜索框「搜索成员」自动聚焦 + 头像 + 姓名 + 拼音 + 角色 + 选中绿勾 + 页脚「共 N 位负责人 · 按姓名 / 拼音搜索 · 演示数据」；↑↓ / Enter 选、Esc 关，触发器（白底描边胶囊）显示当前值；选项 = 全部 / 按任务出现顺序的负责人名单 / 待分配，**不再用原生 `select`**）+ **行筛选**（全部 / 只看已延期 / 只看未排期）+ 命中计数（「筛选后 24 / 共 44 条任务」）+ 「清空筛选」。筛的是**任务集合**：时间轴范围、阶段汇总条、左表出的行与行号全部跟着筛完剩下的任务走；**空阶段不再占行**（不筛选时空阶段照旧出骨架行）；筛完一条不剩 → 空态「当前筛选下没有任务：N 条任务全被筛掉了。」，筛完都没排期 → 空态「当前筛选出的 N 条任务都还没填「开始日期 / 预计完成日期」…」，两个空态都带「清空筛选」按钮。口径同左表日期列（未排期 = 没填齐「开始 + 预计完成」），负责人多选任务在每一位负责人的筛选里都出（同看板口径）。
- **左表「开始日期 / 预计完成日期」两列**（对齐官方样例左表的 start / end 两列，业务口径「我这个移动也要有这些信息 现在没做」）：任务行 = 这条任务的开始 / 预计完成日期、阶段行 = 组内任务工期并集（与阶段汇总条同源）、没排期 =「—」；**拖动条（改期）/ 拖两端（改工期）/ 拖圆点（改进度）时，这两列跟着预览值实时更新**（与条、浮标同一套 `liveOf` 预览口径，单元格加一档强调），松手才写回、Esc 回位。列宽固定（行号 48 / 任务 284 / 开始日期 72 / 预计完成日期 96 / 进展 72），左表自然宽 = 572；分隔线只决定「露多少」，超出的列由右侧时间轴盖住（见下条）。
- **数据口径**：与「项目总览」**同一份项目任务数据**（`ProjectDetail` 合并后的内存态结果），新增 / 编辑 / 删除任务或改进度后回到本页即时生效；**无新增接口**（复用 `GET /api/v1/projects/{id}/tasks`，见 §3.1 #24），也不新增任何任务字段。 **取数口径（业务 2026-09-22 确认）**：按现状量级（单项目几十～数百条）一次取全，列表分页上限 200 足够；**不针对「单项目 1 万行」另做全量 / 视窗取数设计**。
- **分组 = 阶段**（同 §6.3 口径）：九个施工阶段按项目总览顺序各出一行（**空阶段照样出骨架行**，右列显示「暂无任务」、不画汇总条），阶段行**行号也占一个号**（与参考样例一致）；非九阶段的任务落**「未分组」**（有内容才出现，固定垫底）。阶段行点阶段名或左侧三角**折叠 / 展开**，工具条另有「全部折叠 / 全部展开」。
- **阶段行 = 汇总条**：工期 = 组内任务 [开始, 预计完成] 的**并集**，条比任务条细（10px）；完成度 = 已完成 / 组内任务数（与 §6.3 阶段头的「已完成 N/M」、汇总卡整体进度同一套 `isTaskDone` 口径），左表「进展」列同步显示该百分比。
- **任务条**（开始日期 → 预计完成日期，含首尾）：**青色 = 已完成进度**（`task.progress` 四档 → 0 / 25 / 50 / 75 / 100%；**这个百分比就是任务数据里的人工值，不是按日期 / 工期算出来的** —— 与四格进度条同源；阶段行的百分比才是按「已完成任务数 / 组内任务数」派生，见上一条）、**橙色 = 剩余工期**（对齐参考样例的双色条）；**条内标签 =「任务名 + 进度」**（官方 `labelText: {title} {progress}%` 同口径，实测「规划设计 100%」；条宽 ≥ 84px 写全、40~84px 只写百分比、< 40px 不写）。**指针停在条上盖一层半透明遮罩**（官方 `hoverBarStyle`）、**点条 = 选中**（蓝色描边，官方 `taskBar.selectable`；点时间轴空白或左表空白、按 Esc 取消选中）；悬停 / 选中 / 拖动时，条两端浮出 6px 改工期热区、条上浮出进度圆点。**深色竖线 = 实际完成日期刻度**（源功能书 B3-01「计划条 + 实际完成标注」的前半：填了实际完成日期才画，位置取该日在条内的中点并贴住条边界）。**已延期 = 条头一段红内嵌色条**（3px、`red-500`；B3-05：「已延期」状态优先，含行内下拉 / 抽屉指定的手动状态与按日期派生的逾期 —— 色条是**独立子层并抬到进度层之上**，否则会被上面的青色进度层盖住，实测直接给条挂 `inset` 阴影时看不见）。**条本身不画描边**（业务口径「这个 ui 有点突兀 不太相容啊 细节和美感处理的不好」「就是在末端 或者说两种颜色交接的地方 还有橙色的边框」：原 `ring-1 ring-inset ring-amber-600/40` 只压在橙色段上 —— 青色进度是子层、把它盖住了 —— 看着就像给橙色块单独镶了个边；去掉后条是平的，**青色进度与橙色剩余工期之间是一条硬直缝**、末端圆角内也没有镶边，与参考样例一致；业务口径「之前的颜色可以的 不用改啊 我只是让你改细节」→ 青 / 橙 / 选中蓝 / 里程碑绿 / 实际完成深色刻度这些颜色一律不动）。
- **拖动交互**（业务口径 ④ 的核心：官方 `taskBar.moveable` / `taskBar.resizable` + 进度圆点）：**拖条 = 整条平移改期**（工期不变）、**拖条两端 = 改工期**（只动被拖的那一端；单日任务拖左端仍是单日）、**拖条上圆点 = 改进度**（按四档吸附 0 / 25 / 50 / 75 / 100%，与四格进度条同口径）。拖动中是**预览态**：条跟着指针按天走、上方浮一条「开始 → 预计完成 · 共 N 天」（拖圆点时是「进度 N%」）的浮标（**深色底白字**：带 `select-none`，且拖动开始即清掉页面残留选区 —— 免得被浏览器选区高亮成蓝底白字），**松手才写回**；**Esc 或指针取消 = 不写回**、日期位置原样回位。
- **拖动写回口径**（与表格行内编辑完全一致）：只写 `startDate` / `dueDate` / `days`（含首尾）/ `progress` 这批**既有字段，不新增字段**，落进 `ProjectDetail` 的内存态覆盖表（`taskEdits` / `progressOverrides`，与表格行内编辑 / 四格进度条同一张表，刷新复位；正式版走既有 `PATCH` 任务写路径，无新接口）。**日期域 = 2026 年**：数据里的日期是「M月D日」不带年份（`isoFromCnDate` 固定按 `TASK_DATE_YEAR` 解释），所以拖动 / 改工期都不许跨出这一年 —— 顶到年初 / 年末按工期回夹，不把工期拖变形。进度走 `onSetProgress`（沿用四格进度条那套状态联动：0 格 = 待开始、1~3 格 = 进行中、4 格 = 交回完成态派生、已过预计完成日期保持「已延期」）。
- **定位任务条**（官方 `gantt-locate-taskbar`）：点左表任务行 = 选中这条 + 把它的条滚到时间轴可视区中间（条已经完整可见就不动 —— 免得点一下就跳一下）；左表任务行悬停提示写明「点一下 = 选中这条，并把条定位到时间轴可视区」。
- **里程碑 = 绿色菱形**：**单日任务（开始 = 预计完成）**只画菱形、不画条（对齐参考样例的 Milestone 行）。一期没有「是否里程碑」字段（源功能书 A1-11 / B3-02 在二期），这里按单日派生 —— 字段口径已登记 §3.8 A28。
- **未排期**：只填了一个日期（或两个都没填）的任务在左表「进展」列显示「未排期」、时间轴该行写「未排期」灰字、不画条 —— 任务仍在列表里，不会凭空消失。 **口径（业务 2026-09-22 确认）**：开始日期或预计完成日期**任一未设定**即算未排期（两个都填才算已排期）；甘特内「只看未排期」按此口径筛 —— 服务端筛选参数（如 `filter[unscheduled]`）待 wmj 定，落地前仍在内存态算。
- **今天**：今天的日期格高亮（蓝底蓝字）+ 一条蓝色虚线纵贯时间轴；**今天不在时间轴范围内时不画线**（「回到今天」按钮同时置灰）。
- **时间轴范围**：最早开始日期 − 2 天 → 最晚预计完成日期 + 2 天（两端各留 2 天余量）；打开视图**停在最早那头**（不自动滚到今天，同参考样例的默认位置），要看今天用工具条「回到今天」（把今天滚到可视区左 1/3 处）。
- **工具条**（渲染在甘特图卡片上方，不占标签栏右侧工具区）：左 = 图例（已完成进度 / 剩余工期 / 已延期 / 里程碑（单日任务）/ 实际完成日期 / 今天）；右 = **粒度**（日 / 周 / 月）、**缩放**（紧凑 / 标准 / 宽松 —— 只改每天宽度，网格粒度由「粒度」决定）、**全部折叠 / 全部展开**、「回到今天」（**今天在时间轴范围内 = 把今天所在列滚到可视区左 1/3；今天不在范围内**（筛掉今天附近的任务后范围跟着剩下的任务走）**= 先把今天并进时间轴范围、画出今天虚线，再居中** —— 业务口径「为什么筛选后不能选择回到今天」；只有「画不出时间轴」（没有排期齐的任务）时才置灰，禁用态与可用态各带一句 `title` 说明）；**第二行 = 甘特内筛选**（负责人搜索下拉 / 全部 · 只看已延期 · 只看未排期 / 命中计数 / 清空筛选）；末行 = 操作提示。**工具条控件统一「液态玻璃」材质**（业务口径「改成同款液体玻璃」：`border-zinc-200/90` + `bg-white/75` + 顶部内高光 + 极轻投影 + `backdrop-blur-[3px]`，与首页分类筛选侧栏 / 任务表行内编辑单元格同一套 —— 负责人搜索下拉的触发器与「全部折叠 / 回到今天 / 清空筛选」用整套胶囊材质（含 hover 转白），粒度 / 缩放 / 行筛选三段控件的**轨道**用同一材质但不带 hover 反馈、悬停反馈留给里面的分段按钮；只换材质，圆角 8px / 内边距 / 字号与配色口径不动）。**粒度 / 筛选都不在工具条里挂说明文案**（业务口径「这个不要展示」：原先那行「粒度 = 日 / 周 / 月…· 筛选 = 负责人…」与筛选行尾「（筛掉的阶段不再占行；时间轴范围跟着筛完剩下的任务走）」两句已删，口径仍按上面两条 bullet 执行）。
- **排序**：完全沿用「项目总览」的展示顺序（阶段为主键、组内按看板顺序表）；本视图**不改顺序**（拖动只改日期 / 进度，不换行位置；表内行序仍跟项目总览）。
- **空态**：① 项目一个任务都没有 → 「这个项目还没有任务：到「项目总览」点阶段标签，从任务节点 / 模板里挑节点加进来。」；② 筛选后一条不剩 → 「当前筛选下没有任务：N 条任务全被筛掉了。」+「清空筛选」按钮；③ 有任务但（筛选后）一个都没排期 → 「这个项目有 N 个任务，但都还没填「开始日期 / 预计完成日期」，暂时画不出时间轴。」（筛选态下改写成「当前筛选出的 N 条任务都还没填…」），同样带「清空筛选」。
- **本轮补齐**（业务口径 ④ + 「先做这两个」）：时间轴粒度（日 / 周 / 月，官方 `timelineHeader.scales`）、甘特内筛选（负责人 / 只看已延期 / 只看未排期，B3-06 余下）、拖动条形改期（B3-08 前半）、拖条两端改工期（B3-08 后半，**不含依赖冲突校验** —— 依赖字段在二期 A1-11）、进度圆点拖拽改进度、条选中 / 悬停遮罩、条内「任务名 + 进度」标签、点左表任务行定位、拖左表右边线 = 移动分隔线（左表被时间轴盖住多少）。
- **本期未做**（源功能书 B3 其余条目，属二期 / 后续迭代）：前置 / 后置依赖箭头（B3-03）、关键路径（B3-04）、导出图片 / PDF（B3-07）（B3-06 的粒度切换与筛选已于本轮补齐，见上面两条）；B3-08 的**依赖冲突自动校验**要等依赖字段（A1-11）落地。**业务 2026-09-22 确认**：依赖三条（B3-03 / B3-04 与 B3-08 的冲突校验）**暂不做**；里程碑仍按「单日任务」派生，「是否里程碑」字段随 A1-11 二期。
- **实现**：新增 `frontend/src/components/GanttChart.tsx`（Tailwind + 绝对定位手绘，**不引入第三方图表库** —— 本仓库前端目前零图表依赖，且 VTable 的甘特图要真实 `Date` 记录与自有表格实例，与现有内存态数据模型对不上；结构、条色、菱形、虚线按参考样例复刻，文字与配色沿用本仓库既有口径）；日期用 `data/tasks.ts` 的 `isoFromCnDate`（`TASK_DATE_YEAR` = 2026 年）换算成 UTC 零点毫秒做几何计算，状态 / 完成度 / 负责人文案复用 `taskStatus` / `isTaskDone` / `ownersLabel`；`ProjectDetail.tsx` 的 `VIEW_TABS` 加「甘特图」并按 `activeView` 渲染；`TaskBoard.tsx` 的 `STATUS_DOT_CLASS` 由模块内改为导出（甘特图左表的状态圆点与任务表同色）。拖动 / 选中 / 列宽都用 React 指针事件（`pointerdown` + `setPointerCapture` + `pointermove` / `pointerup` / `pointercancel`）在组件内闭环（分隔线那一条另在 window 上挂一层 `pointermove` / `pointerup` 兜底 —— 指针捕获在部分环境会失效）：拖动会话只存**预览值**（`GanttDrag`，每次 move 都从按下时的 base 重算、不逐帧累加），松手才调 `onPatchTask`（日期，`TaskPatch`，与表格行内编辑同一个 prop）或 `onSetProgress`（进度，与四格进度条同一个 prop）—— `GanttChart` 因此新增这两个**可选** prop，`ProjectDetail.tsx` 传 `handlePatchTask` / `handleSetProgress`（原型内存态）；条两端热区与进度圆点只在悬停 / 选中 / 拖动时渲染，进度圆点左右各让开 6px（免得压住两端热区）；`Esc` 用 window 级 keydown 统一取消（拖动 + 选中）。负责人筛选的控件走 `frontend/src/components/MemberSelect.tsx` 新增的通用单选搜索下拉 `SearchSelect`（内部 `SearchList` = 与 `MemberSearchList` / `MemberMultiSelect` 共用的同一份搜索列表：搜索框 + 选项行 + 空态 + 页脚；选项模型 `SearchSelectItem` 支持成员行（头像 + 姓名 + 拼音 + 角色）与普通行（「全部」「待分配」）混排）：触发器 + `usePopover` 浮层（`createPortal`、280px、`rounded-xl` + `shadow-[0_16px_40px_rgba(15,23,42,0.18)]`，与人员下拉 / `SelectMenu` 同一套材质），选中后收起；选中值仍走任务里的姓名（与 `visibleTasks` 的过滤口径同一套字符串空间），查不到目录的负责人退化成普通行、不丢筛选项。
- **实测**（无头 Chrome + CDP，1440×900，`http://localhost:3000/#/project/inmu-0010` 点「甘特图」，数据 = 演示 44 条）：左表 53 行（9 阶段 + 44 任务）= 32 条任务条 + 12 枚里程碑菱形 + 9 条阶段汇总条；条内标签实测「规划设计 100%」；悬停实测 1 层遮罩 + 2 个改工期热区 + 1 枚进度圆点；**拖条 +78px（= 3 天）**实测「开始 5月15日 预计完成 5月20日 → 开始 5月18日 预计完成 5月23日」且条整体右移 78px（工期不变），拖动中浮标实测「5月20日 → 5月25日 · 共 6 天」；**拖右端 +52px（= 2 天）**实测条宽 +52px、预计完成 5月23日 → 5月25日；**拖进度圆点 −¼ 条宽**实测左表进展与条内标签同时 100% → 75%；**里程碑**（布局定档，5月20日单日）拖动实测 5月20日 → 5月22日、仍是单日（菱形不变成条）；**Esc 中途取消**实测日期与位置逐像素不变；**拖左表右边线 +80px** 实测左表 403 → 483px；**点左表「飞箱电控箱及AP安装」行**实测时间轴 `scrollLeft` 0 → 3135、该条从屏幕外（left 4045）滚进可视区（left 910）并保持选中；控制台零报错。**浮标取色**（业务口径「为什么是蓝色的字样」）：拖动中浮标实测深底白字 `#2F2F31`；把选区程序化套在浮标文字上、以及 Ctrl+A 全选（选区 1684 字）后复测仍为 `#2F2F31`（修复前同一手法实测 `#093DA7`，与业务截图 `#093DA8` 同色 = 浏览器选区高亮）；松手后条照常写回（+130px → left 611、不弹回），浮标随松手消失。**细节修订实测**（业务口径「这个 ui 有点突兀……就是在末端，或者说两种颜色交接的地方，还有橙色的边框」+「之前的颜色可以的 不用改啊 我只是让你改细节」）：`运输`（49% / 已延期）条实测 `left 25 / width 1508 / height 20`，条 class 已无 `ring-1 ring-inset ring-amber-600/40`、也无 `inset` 阴影（此前那圈只压在橙色段上），条色实测仍是 `rgb(127, 209, 203)` / `rgb(247, 166, 60)`；6× 放大截图确认两色交接处是硬直缝、末端圆角内无镶边、条头 3px 红条可见（红条不抬到 `z-10` 时实测被青色进度盖住、看不见）；点条选中实测 `selectedCount() = 1`（`ring-2 ring-blue-600`）；演示数据里 4 条已延期（运输 / 清关 / 送仓 / 物料转运、清点分类）；控制台零报错。**第七轮实测**（业务口径「先做这两个」：时间轴粒度 + 甘特内筛选，同上环境 1600×1000、mock 鉴权）：**粒度** —— 日粒度上排实测「2026年5月 … 2026年11月」（宽度 = 天数 × 26px）、下排 182 个日格；切「周」后上排月段宽度整体减半（247 / 390 / 403 …）、下排 27 个周列（首个不满周「5/13」宽 65px、其后 91px = 7 × 13px），「规划设计」条实测 left 623 / right 701 正好落在「5/13」列（597–662）与「5/18」列（662–753）之内；切「月」后上排只剩「2026年」、下排 7 个月列（5月 87.39px = 19 天 × 4.6px、6月 138px = 30 天、7月 142.59px = 31 天…），条落在「5月」列内、今天所在的「9月」列整列蓝底；切回「日」后行数与条位置逐像素复原（barLeft 649、时间轴总宽 4732px）。**筛选** —— 负责人下拉实测「全部 / 彭砚 / 石昀 / 岑宁 / 苏珩 / 卢青 / 方沐 / 程屿 / 蒋恬 / 谢遥 / 夏珂」（按任务出现顺序；演示数据没有「待分配」任务所以不出现该项）；选「彭砚」实测行 44 → 24、时间轴总宽 4732 → 4602px、工具条计数「筛选后 24 / 共 44 条任务」、空阶段不再占行；「只看已延期」（不限负责人）实测 5 行 / 4 条（运输 / 清关 / 送仓 / 物料转运、清点分类）；**「只看未排期」用看板「人员任务分配 → 列底添加 → 临时任务」造了一条「未排期验证任务」**（演示数据里本来没有未排期的任务）：不筛选时行数 44 → 45、末行 = 新任务（落「未分组」、进展「未排期」、不画条），切「只看未排期」实测命中 1 条 → 空态「当前筛选出的 1 条任务都还没填「开始日期 / 预计完成日期」，画不出时间轴。」+「清空筛选」；点「清空筛选」实测回到 45 行 / 9 条阶段汇总条、时间轴总宽 4732px；控制台零报错。

### 7. 入口页（登录后主页）与占位页

**7.1 入口页 `#/`**

- 登录后默认落到本页；顶栏只有 Logo 与账号菜单，不显示页面标题。
- 页面正中一排入口按钮（`frontend/src/Hub.tsx`）：项目空间 / 任务模板 / 文件库，居中排列、窄屏自动换行；一期固定三个入口，后续增删只改 `Hub.tsx` 的入口数组。
- 按钮视觉（`frontend/src/components/HubButton.tsx`；用 Tailwind 复刻参考稿，未引入 styled-components）：176×56px 胶囊（圆角 3rem），描边为 2px 渐变（`linear-gradient(135deg, #30cfd0 0%, #330867 100%)`，画在按钮尺寸之外）、填充为浅灰渐变（`to top, #cfd9df → #e2ebf0`），加深紫柔和投影；悬停时填充换成同款渐变、文字转白并由 1.2rem 放大到 1.3rem（300ms 过渡）。
- 「项目空间」按钮落点：有本地筛选记忆时带记忆地址（与不带参数进入列表的恢复口径一致，见第六章第 12 条），否则 `#/projects`；其余两个入口分别落 `#/templates` / `#/files`。

**7.2 任务模板 `#/templates`（阶段板块标签栏 + 任务节点卡片区）+ 文件库 `#/files`（占位页）**

- 任务模板（`frontend/src/PlaceholderPage.tsx`）：顶栏显示页名；顶部是阶段板块标签栏；下方是一排**同一尺寸的毛玻璃面板**（半透明白 + 背景模糊 + 柔和投影，用于区分列 —— 一列一类内容）：最左是「任务节点」（当前板块的节点卡片列表，列头下方带搜索框），右侧是若干块模板面板（列头显示可改名的模板名称，**模板按阶段分开存** —— 每个阶段一套、互不影响，各阶段初始就是**业务写死的模板预设**（面板已按该阶段的节点顺序预填好、状态是「已保存」，硬件实施初始两套）；一行放不下就换到下一行，最左那列「任务节点」始终不动、宽屏滚动时钉在顶栏下方）。
- 板块标签栏：按业务给定的九个阶段分板块 —— 售前规划 / 设计开发 / 加工采购 / 组装发货 / 硬件实施 / 软件部署 / 试运行 / 生产阶段 / 验收（口径与项目详情一致：取 `data/projects.ts` 的 `PROJECT_STAGES` 去掉「项目总览」；样式复用项目详情的阶段标签栏 —— 选中项深色文字 + 2px 下划线，未选中灰色、悬停加深，窄屏横向滚动）。点板块 = 换地址 + 换右侧那一套模板（**板块走 URL query**，与列表页筛选态同一口径）：`#/templates?section=设计开发`，URL 是当前板块的唯一来源 —— 刷新 / 收藏 / 分享都停在同一板块，地址栏与页面显示始终一致（不认识的板块名会把地址纠正回第一个板块）；切换用 `history.replaceState`，不产生历史条目（浏览器后退不会逐个板块退）。板块列表增删只改 `PlaceholderPage.tsx` 的 `TEMPLATE_SECTIONS` 常量。**这一行最右侧是「＋ 新建模板」按钮**：点一下在「任务节点」右侧插入一块空白的模板面板、原来的模板面板往右挪（新面板的名字自动聚焦并全选，直接敲名字即可）。
- 左侧「任务节点」区：**由上方标签栏驱动** —— 只列出当前选中板块的任务节点（点板块切换，列表头显示「{板块} · N 个节点」），不再混排所有板块；列表头下方是**搜索框**（`aria-label="搜索任务节点"`，placeholder「搜索节点（中 / 英）」）：按中文名 / 英文名过滤、不区分大小写，有过滤时列表头显示「{板块} · N / M 个节点」（N = 命中、M = 总数），没有匹配时列表内显示「没有匹配的节点，换个关键词试试」、框内右侧 × 一键清空；切换板块时搜索词自动清空。一期内容**写死在 `data/templatePresets.ts`**（`STAGE_TEMPLATE_PRESETS`）：取该板块全部预设模板里的节点、按出现顺序去重 —— 多数板块 = 项目总览对应阶段的任务顺序（九个板块合计 44 个节点），硬件实施 = 模板一 18 条 + 模板二新增 8 条 = 26 个节点，软件部署 = 业务给的 4 条（通电测试、参数设定 / RCS、WES 软件部署调试，随机跑 / 按钮盒注册、传感器调节 / 与 WMS 联调）。一个任务节点一张卡片、中英对照（未填英文名的节点只显示中文）。卡片（`frontend/src/components/TaskNodeCard.tsx`）按业务给的参考稿用 Tailwind 复刻：330×80、圆角 8px、白底 + 柔和投影（`0 8px 24px rgba(149,157,165,.2)`），左侧为旋转波浪与圆形图标（成功绿系 `#04e4003a` / `#04e40048`、图标与主标题 `#269b24`），主标题 17px 粗体为任务中文名、副标题 14px `#555` 为英文名，右侧叉形图标是参考稿的装饰元素（一期无点击行为）：**默认隐藏，只有鼠标移到叉号自己那块小区域（28×28 命中区，不是整张卡片）时才显示**（隐藏时保留占位，卡片右侧不抖动）；长文案单行截断，悬停显示完整中英文；卡片可拖拽（拖到任意一块模板面板里 = 复制一份，见下条）。
- 面板材质（毛玻璃，所有面板一致）：`rounded-2xl border border-white/80` + 竖向白色渐层 `bg-[linear-gradient(to_bottom,rgba(255,255,255,.62),rgba(255,255,255,.32))]` + 顶部内高光 `inset 0 1px 0 rgba(255,255,255,.75)` + 外投影 `0 8px 32px rgba(15,23,42,.14)` + `backdrop-blur-2xl backdrop-saturate-150`；内边距 `p-5`、宽 370px（窄屏整宽；模板面板放进右侧可换行的区域 —— 一行放不下就到下一行、同一行等高，间距 32px，窄屏整体一列往下排）。
- 玻璃底衬（纯装饰，仅本页）：面板后面垫一层**淡色雾** —— `sticky top-0 h-[70vh]` 跟随视口（长列表滚动时玻璃始终有色可透），四团不同色径向渐变（黄 `#feca04` 0.75 / 粉 `#f472b6` 0.45 / 青 `#30cfd0` 0.60 / 靛 `#6366f1` 0.50）+ `blur-3xl`，`pointer-events-none` 不接收鼠标事件，外层 `overflow-x-clip` 防横向溢出；仅本页、不影响其他页面。
- 模板面板（右侧每一块 = 一份模板草稿，原来这里只有一块「已选节点」）：与「任务节点」同尺寸 —— 宽 370px（窄屏整宽），**列头显示模板名称**（当前原型是本地草稿字段、未接后端：默认取预设名（「{阶段}模板」；硬件实施是「硬件实施模板一 / 模板二」），点一下就能改成「英国订单」这类具体名字；未接模板列表 / 落库），名称右侧显示这块模板的节点数量（`aria-label="已选节点数"`），名称下方留一行**固定高度的提示行**（平时 `invisible`，重复拖入时显示「该节点已在右侧，不会重复添加」；固定高度是为了拖拽时不顶动下方卡片），列体为半透明白容器（`rounded-xl border border-white/60 bg-white/25`），高度按内容、同一行的面板拉成等高（窄屏按内容高）。列头右侧有两个按钮：**「保存」/「已保存」**（当前原型未接后端，保存＝把当前名字 + 节点顺序记进浏览器内存；正式版由后端落库 —— 之后再改动按钮就变回「保存」，算是「有未保存改动」的提示）和**「删除」**（删掉这块面板；删光后面板区显示虚线提示，可再用「＋ 新建模板」建回来）。**模板按阶段分开**：每个阶段记自己的一套面板（名字 / 节点 / 保存状态），切标签就切到该阶段那一套 —— 在 A 阶段新建 / 改名 / 删除 / 拖拽都不影响 B 阶段（同一阶段内的多块面板之间同样互不影响）；新建入口在下方「板块标签栏」那条（见上），新建 / 保存 / 删除都只作用于当前阶段。
- 拖拽复制（去重）：左侧卡片可拖动（**指针拖动**，光标 `cursor-grab`，拖动中 `cursor-grabbing`），拖到某一块模板面板里就在那块面板**追加一份副本** —— **Push 116 起改成指针事件（`pointerdown` / `pointermove` / `pointerup`，位移阈值 `DRAG_THRESHOLD = 4`，与看板同一套），不再是原生 HTML5 拖放**：原生拖拽期间浏览器会把 `wheel` 吞掉，「拖着卡片的同时滚轮翻列表」根本不成立（业务口径「这里拖动卡片也要可以滑动鼠标」）。拖动中鼠标底下浮出一块**跟着鼠标走的半透明拖动卡片**（`data-drag-ghost`：`bg-white/[0.6]` + `backdrop-blur-[5px]` + `pointer-events-none`，能透出底下的插入线、不挡落点判定）；位移没过阈值（点一下 / 抖一下）不算拖动、什么都不改，`Esc` / 指针取消 = 原地取消。**左侧原节点不变**；**同一个任务节点在同一块面板里只保留一份**（重复拖入不生效），不同模板面板里可以各有同一个节点（本来就是不同的模板）。拖动进入某块面板时那块面板高亮（`ring-2 ring-zinc-900/10` + 列体边框加深），面板空态显示提示「把左侧「任务节点」拖到这里」；若拖的节点这块面板里已经有了，整块面板换成琥珀色提示态（`border-amber-400/70 ring-2 ring-amber-300/40`、列体转琥珀底，并在列头名称下方显示「该节点已在右侧，不会重复添加」（提示行固定高度，不顶动下方卡片列表），对应卡片描琥珀色 `ring-2 ring-amber-400/80`），松手也不会新增。
- 右列卡片可移除：悬停时叉号变成可点按钮（`aria-label="移除 {任务名}"`），点击即从模板里删掉该条；切换板块标签不影响各面板已选（各板块拖入的节点累计在同一块面板里）。
- 面板内可拖动调顺序：模板面板里的卡片本身也能拖（同样是指针拖动、同一套阈值与取消口径，Push 116）—— 落点在目标卡片**上半 = 插到它前面、下半 = 插到它后面**（卡片之间的空白处、所有卡片之下都落到末尾），拖动中鼠标底下浮出半透明拖动卡片、落点上显示品牌黄 `#feca04` 的插入指示线、被拖的那张降到 40% 透明度留位；调整的是这块模板里的节点顺序，不影响左侧任务节点，数量也不变（排序只在同一块面板内生效：拖到别的模板面板上不处理）。**拖动中滚轮照常可用**，拖动卡片与落点由 `requestAnimationFrame` 每帧重算 —— 鼠标可以不动、页面在滚，插入线也跟着走到新的那一格。指示线**绝对定位、不占位**（`pointer-events-none absolute`，画在卡片之间的 12px 缝里），落点按**卡片的几何位置**判定（`elementFromPoint` 认 `[data-template-panel]` 面板 + 逐个量卡片的 `getBoundingClientRect` 决定插到哪，而不是看鼠标底下是卡片还是缝）—— 两者缺一都会让「拖到两张卡片之间」时来回频闪：占位的指示线一出现就把后面的卡片顶开，鼠标底下的元素跟着换、落点又变，形成抖动循环。
- 文件库（`frontend/src/PlaceholderPage.tsx`）：仍为整页占位 —— 顶栏显示页名，内容区为虚线卡片（「文件库还没开工：先把入口与路由占好，后续按需求填充」+「返回入口页」链接）。

**7.3 路由与层级（顶栏 Logo = 返回上一层）**

| 地址 | 页面 | 上一层 |
|---|---|---|
| `#/` | 入口页 | —（已在顶层，不动作） |
| `#/projects`（可带 `filter[...]` / `q` / `sort`） | 项目空间列表 | 入口页 `#/` |
| `#/project/{项目id}` | 项目详情 | 项目空间（保留进入前的筛选态；优先走浏览器后退） |
| `#/templates`（可带 `?section=<板块>`，缺省为第一个板块「售前规划」） | 任务模板（阶段板块标签栏 + 任务节点卡片区 + 若干模板面板） | 入口页 `#/` |
| `#/files` | 文件库（占位） | 入口页 `#/` |

- 路由实现在 `frontend/src/useHashRoute.ts`（`HUB_HASH` / `LIST_BASE_HASH` / `upLevelHref()` / `goUpLevel()`）；列表页 URL 参数口径不变（见 §一.3 与 §三）。旧书签里的 `#/?filter[...]`（`#/` 让位给入口页之前的列表地址）不再解析筛选，等价于进入入口页。

## 二、前端数据模型（当前内存态）

### 1. 项目 Project

| 字段 | 类型 | 说明 | 示例 |
|---|---|---|---|
| id | string | 唯一标识（原型为演示 slug；接契约后为 `projects.id` UUID） | cnbj-0001 |
| seqNo | number | 项目序号：服务端创建时分配（全库唯一正整数、不可修改、不回收，契约 `projects.seq_no` ↔ `seqNo`）；卡片右上角两位补零展示；演示数据 1–20 | 1 |
| code | string | 项目编号（契约 `projects.code`；创建人填写、建后可修改） | CNBJ-20260708-0001 |
| description | string | 项目描述 | 中国包裹分拣 |
| region | string | 项目地区（演示数据为地区名；后端按 `projects.region` 字典下发，缺失置「未分类」，见技术设计 v0.2 §8） | 中国 |
| projectType | 枚举 | 项目类型：T-sort / 3D分拣 / 飞箱（新建表单下拉选择，默认 T-sort；对应「系统功能书」A1-12 / A1-18 的「项目类型」字典，取值以后端字典为准） | T-sort |
| managerIds | string[]（UUID 数组） | 项目经理（**多位**，Push 136）：至少一位、可多位，数组顺序 = 展示顺序；对齐契约（提案）`projects.manager_ids`（uuid[]、非空）；演示数据取自项目经理目录 `frontend/src/data/managers.ts`（id ↔ 姓名），界面按 id 取姓名、按「、」连接展示；接 identity 接口后由用户表替换 | [ 2f7c1a94-…-2b7001, 3a8d2b05-…-6b3c8002 ] |
| createdAt | string | 创建时间（展示用：卡片右下角、详情顶栏「创建于」；创建时生成、编辑不修改） | 2026-07-08 08:15 |
| updatedAt | string | 项目时间（最近活动）：排序 / 搜索 / 「项目时间」区间筛选使用；主数据变更 / 阶段推进 / 任务变更刷新（原型演示：编辑保存刷新） | 2026-07-08 08:15 |
| accent | 枚举 | 主题色。当前实现由项目类型映射：T-sort=blue（蓝）/ 3D分拣=emerald（绿）/ 飞箱=amber（品牌黄 #feca04）（rose / violet 为预留色，未启用）；契约定案为随 C9 项目类型字典元数据下发、前端不硬编码，接入后端后本映射移除 | blue |

### 2. 任务 ProjectTask（本次新增）

| 字段 | 类型 | 说明 | 示例 |
|---|---|---|---|
| id | string | 任务唯一标识 | t01 |
| stage | string | 所属阶段（9 个施工阶段之一） | 售前规划 |
| title | string | 任务描述（中文） | 布局定档 |
| titleEn | string | 任务描述（英文，可空） | Layout scheduling |
| owners | string[] | 任务负责人（**多位**，Push 136）：数组顺序 = 展示顺序；**空数组 = 「待分配」**（合法中间状态，沿用 A18）；与 `ownersEn` 同下标一一对应 | [ 苏珩, 彭砚 ] |
| ownersEn | string[] | 负责人拼音名数组（与 `owners` 同下标一一对应，元素可为空串） | [ suheng, pengyan ] |
| status | 枚举 | 任务状态：已完成 / 提前完成 / 进行中 / 待开始 / 已延期（Push 65 起业务要求表格可手动改：接口需支持「手动优先」语义 —— 手动选过就按手动，直到进度条再次被点；接口可只回传基础态 已完成 / 进行中 / 待开始，展示态由派生规则计算；联动口径见第七章 §3.8 A12） | 已完成 |
| progress | number | 进度：0 / 0.25 / 0.5 / 0.75 / 1 | 1 |
| startDate | string | 开始日期（原型为中文短日期） | 5月15日 |
| dueDate | string | 预计完成日期 | 5月20日 |
| doneDate | string | 实际完成日期（空 = 未完成） | 5月20日 |
| days | number | 预计所需天数 | 6 |
| deliverable | string | 输出成果文件（可空） | 验收单 |
| change | string | 变更关联日期（可空） | 2026/8/11 |
| onTime | string | 是否按时交付（可空） | 按时交付 |
| note | string | 项目进展描述（可空） | 物料清单 |
| headcount | number | 预计所需施工人数（可空、非必填；任务创建 / 编辑均可填，来源定案见第六章第 8 条） | 10 |
| priority | 枚举 | 紧急重要度：高 / 中 / 低（可空、非必填；任务创建 / 编辑均可填——项目经理 / 任务负责人均可，见第六章第 8 条） | 高 |
| files | string[] | 附件文件名列表（来源定案：站内文件库挂到该任务的文件，类型不限；不通过第三方应用打开、走站内预览，见第六章第 8 条） | 部署手册.pdf |

- 项目经理是项目级字段：**任务表「项目经理」列与任务详情抽屉都取项目卡片上的名单**（`project.managerIds` → 项目经理目录 `data/managers.ts` 的姓名、多位按「、」连接，Push 61 起；如印度项目 = 冯雪、李伟，美国项目 = 王芳、陈晨），不再按固定常量展示（`PROJECT_MANAGER` 只作兜底）；Push 63 起任务编辑里也能改项目经理（Push 91 起改为在任务详情抽屉里直接改；Push 136 起抽屉与任务表行的该字段都是**多选**），改完即回写项目（全项目同步、刷新项目时间）。
- **一个项目可以有多个项目经理（Push 136，业务口径「一个项目可以有多个项目经理和任务负责人」）**：至少一位、可多位（**取消最后一位不生效**，对齐契约非空要求）。改动落在：新建 / 编辑项目弹窗（多选：已选成员以胶囊列出、可单个移除、点「添加 / 继续添加」开搜索列表接着勾）、首页卡片（「项目经理：冯雪、李伟」，过长省略号 + 悬停看全）、首页侧栏筛选（多位经理的计数**每位各计一次**；筛选命中 = 项目挂的任意一位经理命中即命中）、搜索（任一经理姓名命中即命中）、项目详情顶栏 / 任务表「项目经理」列（行内多选：勾一位加一位、取消一位减一位）、任务详情抽屉（多选，项目级字段改后全项目同步）。
- **一个任务可以有多个任务负责人（Push 136）**：可多位、**空数组 = 「待分配」**（沿用 A18 合法中间状态）。改动落在：任务表「任务负责人」列（行内多选；多位以「、」连接、过长标题提示看全）、任务详情抽屉（多选；全部取消 = 待分配）、人员任务分配看板（**这张卡出现在每一位负责人的列里**，列计数 = 该人手上的任务数；拖动换列 = 负责人**整体换成**拖入列那一位、拖进「待分配」= 清空 —— 要挂多头请用抽屉 / 任务表）、日报及问题面板的关联任务（多位以「、」连接）。
- 人员目录（Push 63）：`data/managers.ts` 与任务负责人下拉共用 `data/members.ts`（**全部虚构姓名**：项目经理 20 人 + 交付成员 11 人），演示任务负责人已同步改用虚构成员（避免真实员工姓名进入原型界面）；接 identity 用户表后由接口数据替换。
- 完成判定：`doneDate` 非空或 `progress >= 1`；逾期判定：未完成且 `dueDate` 早于今天（按 月 / 日 比较）。
- 状态派生（展示态）：完成且 `doneDate` 早于 `dueDate` → 提前完成；完成且未早于 → 已完成（逾期补完按已完成，不回退「提前完成」）；未完成且已过 `dueDate` → 已延期；其余按接口 `status`（进行中 / 待开始）。
- 交付标注（Push 67，落在「是否按时交付」列 / 字段）：未完成且已过 `dueDate` → 「逾期未交付」（红）；已完成但 `doneDate` 晚于 `dueDate`（或未填 `doneDate`、但 `dueDate` 已过）→ 「逾期已交付」（琥珀）；其余照源表 `onTime` 值。逾期标注不再写在实际完成日期列。
- 演示数据 44 条任务，**只挂在印度项目 `inmu-0010`**（`data/tasks.ts` 的 `DEMO_TASKS_PROJECT_ID` / `tasksForProject()`；其它项目取到空列表 → 只出阶段骨架，骨架常显、见 §6.3）；施工人数 / 紧急重要度 / 文件为原型派生演示值（源表对应列为空）；维护口径已定案（2026-09-18，见第六章第 8 条）：施工人数 / 紧急重要度任务创建 / 编辑均可填、非必填，文件走站内文件库（多类型、不通过第三方应用打开）。

### 3. 阶段列表

「项目总览」（汇总视图）+ 9 个施工阶段：售前规划、设计开发、加工采购、组装发货、硬件实施、软件部署、试运行、生产阶段、验收。

### 4. 表格默认显示列

- 默认显示（Push 67 起 = 业务截图那套；Push 136 起「项目经理」「任务负责人」两列为**行内多选**、展示多位以「、」连接）：任务描述（常显）、项目经理、任务负责人、任务状态、紧急重要度、是否按时交付、输出成果文件、文件、项目进展描述、开始日期、预计完成日期、实际完成日期、变更关联（预计所需天数随两日期列显示）。
- 默认隐藏：预计所需施工人数。
- 详细字段对照见 `字段对照清单.md`。

### 5. 日报与问题（本次新增，Push 128）

- `DailyReport`（`data/reports.ts`）：`id` / `date`（源表口径「M月D日」，与任务日期列同一套写法）/ `author`（提交人）/ `submittedAt`（提交时间）/ `state`（草稿 / 已提交 / 补填，口径 A3-02）/ `headcount`（今日施工人数）/ `doneWork`（当日完成工作）/ `plan`（明日计划）/ `foundIssue`（现场发现问题；非空 = 已自动生成问题记录）/ `issueCategory`（问题归类；口径 A3-04：`foundIssue` 非空时必填）/ `suggestion`（解决方案或建议）/ `tasks`（关联任务，回写任务「项目进展描述」并留痕）/ `photos`（现场工作附图，原型存文件名）。
- `Issue`（同文件）：`id` / `title`（问题描述，取自来源日报）/ `state`（四态：未分组 / 未解决 / 处理中 / 已完成，口径 A3-10）/ `category`（问题归类）/ `reporter`（提出人 = 来源日报提交人）/ `owner`（责任部门 · 责任人，按归类自动分派；空 = 待分派）/ `task`（所属任务）/ `raisedAt`（提出日期）/ `dueAt`（处理时限）/ `solution`（解决方案 / 回复）/ `reportId`（来源日报 id，保证同一条日报只生成一次问题）。
- 演示数据：**6 篇日报**（9月17日 ~ 9月21日，覆盖 已提交 / 补填 / 草稿 三种状态）+ **5 条问题**（未分组 1 / 未解决 1 / 处理中 2 / 已完成 1），只挂在示例项目 `inmu-0010`（`reportsForProject()` / `issuesForProject()`，其余项目返回空列表）。
- `ReportDraft`（`ReportIssuePanel.tsx` 的「日报填写」表单草稿）：`dateIso` / `headcount` / `doneWork` / `plan` / `foundIssue` / `issueCategory` / `suggestion` / `tasks`（关联任务描述数组）/ `photos` / `issuePhotos`（附图名数组）；**切子视图不丢**，提交 / 暂存后复位（日期仍默认今天）。
- 提交口径（A3-04 / A3-09）：必填 = 时间 + 当日完成工作；「现场发现问题」非空时问题归类必填 —— 未齐时「提交日报」置灰并提示还差哪几项；提交 = 写一条 `DailyReport`（`state = 已提交`）并把「现场发现问题」自动落一条 `Issue`（`state = 未分组`、`reportId` 指回该日报、`dueAt` = 填报日 + 2 天（原型演示值），幂等口径由后端落地）；暂存 = 只写 `DailyReport`（`state = 草稿`）。
- 字段映射（前端演示模型 → 契约，接线时按此替换）待 wmj 定契约后补 —— 提案见 §3.8 A21，届时同步 `字段对照清单.md`。

## 三、后端接口需求与前端联调清单

> 口径说明（2026-09-18，Push 47 · M0-05 收口）：本章按「页面 → 接口 → 字段 → 错误码」逐页补全，供 M2-07 前端联调直接对照。契约唯一来源为 `shared/src/`（Zod → OpenAPI → TS 生成物，当前 paths=35 / operations=38）；早期草案的 `keyword` / `pageSize` / `manager` / `title` 命名不再使用，对应契约名为 `q` / `limit` / `managerId` / `name`。

- 记号：**已入契约** = `shared/` 已定义、前端待接线；**待增补** = 前端已按某口径落地或已提需求、契约尚未定义（提案见第 3.8 节对齐清单）；**已接入** = 前后端已联通。
- 统一前缀 `/api/v1`（`/auth/*` 除外，根路径直连）；JSON；认证复用 OIDC 会话（HttpOnly `ll_sid` + 可读 `ll_csrf`，写请求回传 `X-CSRF-Token`）。
- 错误码唯一来源 `shared/src/common/errors.ts`；统一信封 `{ code, message, details[], traceId }`；前端处理口径见第 3.7 节。

### 3.1 页面 → 接口总表

| # | 页面 / 交互 | 接口 | 状态 | 前端落点（接入前） |
|---|---|---|---|---|
| 1 | 登录会话：未认证跳转、回跳、账号区、退出 | `GET /auth/login`、`/auth/callback`、`/auth/me`、`/auth/logout` | **已接入**（k6 / Push 46） | `src/api.ts`、`App.tsx` |
| 2 | 首页列表：卡片网格、分页、排序 | `GET /api/v1/projects` | 已入契约 | `Home.tsx`（mock 过滤） |
| 3 | 首页筛选侧栏计数 | `GET /api/v1/projects/facets` | 已入契约 | `CategoryFilterSidebar.tsx`（本地计数） |
| 4 | 首页「项目时间」区间 | `GET /api/v1/projects?filter[timeFrom]=…&filter[timeTo]=…` | 已定案（PR #42；Q11 → A1） | `DateRangePicker.tsx` |
| 5 | 项目经理目录：筛选 / 卡片 / 新建 / 编辑下拉（Push 136 起为**多选**） | `GET /api/v1/users` | 已定案（PR #42；A2，`managerName` 随行下发补充中）；**多位口径见 A22** | `src/data/managers.ts`（硬编码 20 人） |
| 6 | 字典：项目类型 + accent、阶段名、成果文件类型、紧急重要度 | `GET /api/v1/dicts`（`/dicts/{type}`） | 已定案（PR #42；A3，一期 region / projectType） | `src/types.ts`（`PROJECT_TYPES` / `PROJECT_TYPE_ACCENTS` 硬编码） |
| 7 | 新建项目 | `POST /api/v1/projects` | 已入契约 | `ProjectModal.tsx` → `handleCreateProject`（内存态） |
| 8 | 编辑项目 | `PATCH /api/v1/projects/{id}` | 已入契约 | 同上 → `handleUpdateProject` |
| 9 | 删除项目（入口待定） | `DELETE /api/v1/projects/{id}` | 已定案（PR #42；A5） | 暂无入口 |
| 10 | 项目详情顶栏：编号 / 描述 / 类型 / 项目经理（可多位）/ 创建时间 | `GET /api/v1/projects/{id}` | 已入契约（经理字段多位口径见 A22） | `ProjectDetail.tsx` |
| 11 | 总览汇总卡：当前阶段 / 整体进度（完成 / 总数）；逾期计数一期不展示（契约字段保留） | `GET /api/v1/projects/{id}/summary` | 已入契约 | `ProjectDetail.tsx`（本地派生） |
| 12 | 任务表：15 列、阶段分组、四格进度 | `GET /api/v1/projects/{id}/tasks` | 已入契约 | `TaskBoard.tsx`（演示 44 条，只挂在印度项目 `inmu-0010`；其余项目出阶段骨架（常显，见 §6.3）） |
| 13 | 任务详情抽屉 | 复用 #12（契约按全字段下发） | 已入契约 | `TaskDrawer.tsx`（Push 98 起进度条带四颗点，可直接改进度） |
| 14 | 四格进度点击（含回退；任务表 + 任务详情抽屉共用） | `PATCH /api/v1/projects/{id}/tasks/{taskId}/progress` | 已入契约 | `Tracker.tsx` / `TaskDrawer.tsx`（内存态） |
| 15 | 列显隐（跨设备记忆） | `GET / PATCH /api/v1/users/me/preferences` | 已定案（PR #42；A4） | `ColumnPicker.tsx`（内存态） |
| 16 | 文件 / 变更（未接线，接口面已备） | files 14 条 + changes 2 条（见第 3.6 节） | 已入契约 | 未接入 |
| 17 | 蓝图与流程节点（管理端，未接线） | flow 10 条（见第 3.6 节） | 已入契约 | 未接入 |
| 18 | 入口页：项目空间 / 任务模板 / 文件库 三个入口按钮 | 无（纯前端导航） | 本地实现（Push 57） | `Hub.tsx` + `components/HubButton.tsx` |
| 19 | 任务模板：九个阶段板块标签栏（售前规划 / 设计开发 / 加工采购 / 组装发货 / 硬件实施 / 软件部署 / 试运行 / 生产阶段 / 验收）+ 左侧「任务节点」卡片区（随板块标签栏过滤；节点池当前原型取业务写死的模板预设、按出现顺序去重、中英对照，Push 60 起；正式来源待后端）+ 右侧若干块模板面板（每块 = 一份草稿：列头可改名、默认「{阶段}模板」，带「保存 / 已保存」与「删除」按钮；板块标签栏行最右侧「＋ 新建模板」在「任务节点」右侧插入新面板、原面板右移；面板一行放不下就换到下一行、同一行等高，最左「任务节点」列固定不动；与左列同尺寸的毛玻璃面板，拖拽落点：拖入即复制一份、可移除；面板内可拖动排序） | 已入契约（Push 62，§3.8 A11）：`GET /api/v1/task-nodes?stage=`、`GET/POST/PATCH/DELETE /api/v1/task-templates…`（模板「保存」= `PATCH` 节点全量替换、`version` 乐观锁）；勾选等交互口径待业务定稿 | 标签栏、节点卡片、毛玻璃面板、拖拽复制（按面板去重 + 移除）、面板内排序、模板名 / 多面板新建 / 换行布局 / 面板保存与删除 / 板块进 URL（`?section=`）与**模板按阶段分开**已实现（Push 58）；**各阶段默认模板改为业务写死的预设**（硬件实施两套：长清单 18 条 + 格口 / 滑槽型 11 条；软件部署一套 4 条；节点池含硬件实施模板二新增的 8 个节点，共 26 个）已实现（Push 60，含左列节点池搜索框：中 / 英过滤、`N / M 个节点`、切板块清空）；模板真正落库（含刷新后保留）、模板列表与跨面板移动未接 | `PlaceholderPage.tsx` + `components/TaskNodeCard.tsx` |
| 20 | 文件库（占位页） | 待定（files 契约见 §3.6） | 占位（Push 57） | `PlaceholderPage.tsx` |
| 21 | 项目总览「添加任务」：点阶段标签 → 右侧卡片（标签导航：任务节点 + 各模板；逐条 / 整套添加，按节点 id 判重） | `POST /api/v1/projects/{id}/tasks` + `POST /api/v1/projects/{id}/tasks/from-template`（§3.8 A10 / A11，已入契约，Push 62） | 本地实现（Push 61，内存态） | `components/StageAddCard.tsx` + `components/TaskBoard.tsx` + `ProjectDetail.tsx` |
| 22 | 项目详情「日报及问题」视图：页内导航栏（日报填写 / 日报记录 / 问题追踪 / 问题看板）+ 四块子视图（填写表单 A3-01 / 日报记录列表（表格） / 问题表格 / 问题四态看板 A3-10） | 待增补（§3.8 A21 提案）：`GET /api/v1/projects/{id}/reports`、`GET /api/v1/projects/{id}/issues`；填报 / 暂存 `POST/PATCH /api/v1/reports…`、问题状态流转与回复 `PATCH /api/v1/issues/{id}` | 待增补（提案） | `components/ReportIssuePanel.tsx` + `data/reports.ts`（演示 6 篇日报 / 5 条问题，只挂在 `inmu-0010`；填写与提交为**内存态原型**） |
| 23 | 首页「常用筛选」组合集（跨设备记忆，与 #15 同源） | `GET / PATCH /api/v1/users/me/preferences`（新增 `homeSavedFilters` 键） | 待增补（§3.8 A24，待 wmj 定契约）；当前 = 浏览器 `localStorage` | `src/savedFilters.ts`（本地记忆，Push 138） |
| 24 | 干系人台账页 / 项目详情「干系人」面板（前端未建，接口已备） | `GET /api/v1/stakeholders`（`filter[projectId]` 反查 / `filter[companyType]` 分类）、`GET/POST/PATCH/DELETE /api/v1/stakeholders…`、`POST/DELETE /api/v1/stakeholders/{id}/projects…` | 已入契约（Push 144） | 未接入（源表 6 人对照见 `字段对照清单.md` §二.1） |
| 25 | 任务批量操作（任务表 / 两块看板多选后批量指派 / 改状态 / 改期 / 重要度 / 备注；前端未建，接口已备） | `PATCH /api/v1/projects/{id}/tasks/batch`（§3.8 A29） | 已入契约并 HTTP 落地（Push 149） | 未接入（随 M3-07 联调；失败清单 `failures[]` 逐行标红） |

| 24 | 项目详情「甘特图」视图：阶段分组甘特图（左固定任务列表（行号 / 任务 / 开始日期 / 预计完成日期 / 进展）+ 右时间轴、阶段折叠、三档缩放、粒度（日 / 周 / 月）、筛选（负责人 = 与人员下拉同一套**搜索下拉** / 只看已延期 / 只看未排期）、回到今天（今天不在筛选后的时间轴范围内时，先把今天并进范围再居中）；工具条控件 = 仓库既有「液态玻璃」材质；拖条改期 / 拖两端改工期 / 圆点改进度 / 选中 / 悬停 / 定位 / 拖左表右边线 = 移动遮挡边界（列宽固定、超出的列被右侧时间轴盖住，不是压缩列宽） | 复用 #12（`GET /api/v1/projects/{id}/tasks`）+ 既有任务写路径（`PATCH .../tasks/{taskId}` / `.../progress`），**无新增接口** | 本地派生（Push 145；与任务表同一份数据，仅「里程碑」字段待补，见 §3.8 A28） | `components/GanttChart.tsx`（演示 44 条，同 `inmu-0010`） |

### 3.2 首页「项目空间」（列表 + 筛选 + facets）

**列表 `GET /api/v1/projects`（已入契约）**

| 参数 | 口径 | 前端来源 |
|---|---|---|
| `filter[region]` | 多值英文逗号分隔 | 侧栏「地区」 |
| `filter[projectType]` | 多值英文逗号分隔 | 侧栏「项目类型」 |
| `filter[managerId]` | 多值英文逗号分隔（UUID） | 侧栏「项目经理」；命中口径（Push 136）= **项目挂的任意一位经理命中即命中** |
| `filter[stageKey]` | 多值英文逗号分隔（`StageKey`） | 契约已有，侧栏一期不展示（A6） |
| `filter[status]` | 多值英文逗号分隔（`active/paused/done/archived`） | 同上（A6） |
| `filter[timeFrom]` / `filter[timeTo]` | `YYYY-MM-DD`，闭区间 | 侧栏「项目时间」→ **待增补（A1）** |
| `q` | 关键字：编号 / 名称 / 客户 / 序号 | 顶栏搜索框 |
| `page` / `limit` | 默认 1 / 50，`limit` 上限 200 | 列表分页（原型全量，接线后按需分页） |
| `sort` | `field:asc,field2:desc`；前端默认 `updatedAt:desc`、可切 `updatedAt:asc` | 工具条降 / 升序 |

- 响应 `{ items: Project[], page, limit, total }`；`Project` 字段：`id / code / seqNo / name / customer / region / projectType / managerIds / managerNames / stageKey / status / description / version / createdAt / updatedAt`。（Push 136 起前端按多位展示 / 筛选、下发数组；经理字段多位口径已定案并落地 —— 契约 `Project.managerIds`（至少一位）+ `managerNames`、库 `projects.manager_ids`，见 A22 与迁移 `0017_multi_manager_and_owner.sql`。）
- 前端展示取 `createdAt`（创建时间，「创建于」）；排序 / 搜索 / 「项目时间」区间筛选取 `updatedAt`（项目最近活动时间；定案见 §3.8 A1 / 第六章 Q11）；「最近活动时间」暂不上卡面（见第六章第 14 条）。
- 前端「项目描述」= 契约 `name`（唯一展示名）；`customer` 为独立字段，新建 / 编辑表单暂未采集，接线时补。
- 卡片主题色取自 `projectType` 字典元数据 `metadata.accent`（A3 落地前用本地映射，见第二章第 1 节）。

**计数 `GET /api/v1/projects/facets`（已入契约）**

- 请求与列表**同一套筛选参数**（`filter[...]` + `q`；不传 `page` / `limit` / `sort`），保证「计数与列表同口径」。
- 响应：`total` + 五组计数 `region` / `projectType` / `managerId` / `stageKey` / `status`（每组为 `{ 取值: 计数 }`）。

侧栏四组 ↔ facets 五组映射（差异登记 A6）：

| 侧栏维度 | facets 组 | 本期处理 |
|---|---|---|
| 地区 | `region` | 直接取计数 |
| 项目类型 | `projectType` | 直接取计数 |
| 项目经理 | `managerId`（键为 UUID） | 用 `GET /users` 解析姓名展示；Push 136 起一个项目可挂多位 → **每位各计一次**（多经理项目同时进多位经理的计数） |
| 项目时间 | 无对应组 | 不参与计数；由 `filter[timeFrom]` / `[timeTo]` 过滤（A1） |
| （侧栏未展示）阶段 / 状态 | `stageKey` / `status` | 契约已返回，一期不展示，是否补两维待产品确认（A6） |

**常用筛选（Push 138；前端本地记忆，跨设备待定契约 → §3.8 A24）**

- 一组 = 自定义名称 + 同一套筛选条件（regions / projectTypes / managerIds / timeFrom / timeTo）；**应用 = 把这套条件写回既有筛选参数**（`filter[region]` / `filter[projectType]` / `filter[managerId]` / `filter[timeFrom]` / `filter[timeTo]`）—— 服务端**不需要新参数**，列表 / 计数 / facets 口径完全不变。
- 胶囊计数 = 当前项目集里命中该组合的项目数（与列表同一判定：多值任一命中、时间闭区间按 `updatedAt` 的日期部分比较，实现见 `frontend/src/savedFilters.ts` 的 `matchesCriteria`）。
- 生效判定：当前筛选条件与某一组**逐项等价**（与勾选顺序无关）即高亮该组，不记录「点过哪一组」（`sameCriteria`）。
- 持久化：`localStorage`（`libiaolink.home.savedFilters.v1`，上限 20 组 / 名称上限 20 字 / 退出登录清除）；跨设备建议复用 #15 用户偏好（`homeSavedFilters` 键，`GET / PATCH /api/v1/users/me/preferences`，值 = `SavedFilter[]`）。
- 错误码与前端处理：400 `VALIDATION_FAILED`（`limit` 超限、UUID / 日期格式非法）→ 丢弃非法筛选值并归一化地址栏（Push 32 已实现）；401 → `redirectToLogin()`；5xx / 网络失败 → 保留上次结果 + 顶部提示重试。
- 前端落点：`Home.tsx`（mock 过滤 → 服务端查询）、`CategoryFilterSidebar.tsx`（本地计数 → facets）、`DateRangePicker.tsx`（区间 → A1 参数）。

### 3.3 新建 / 编辑 / 删除项目

**新建 `POST /api/v1/projects`（已入契约）**

| 字段 | 必填 | 前端表单 |
|---|---|---|
| `code` | 是 | 项目编号（提示四大写字母-日期-四位序号；服务端只校验唯一） |
| `name` | 是 | 项目描述（前端标签「项目描述」；契约名 `name`） |
| `region` | 是 | 项目地区（字典下拉，A3） |
| `projectType` | 是 | 项目类型（字典下拉 + accent，A3） |
| `managerId` | 是 | 项目经理（`GET /users` 下拉，A2）；Push 136 起改为 `managerIds` 数组（**至少一位**、可多位，顺序 = 展示顺序） |
| `customer` | 否 | 客户（表单待补） |
| `description` | 否 | 备注长文本（≠ 前端「项目描述」） |
| `stageKey` | 否 | 缺省由服务端定 |
| `blueprintVersion` | 否 | 缺省 = 当前已发布蓝图（导入即快照） |

- 响应 201 + 完整 `Project`（含服务端分配的 `seqNo`）；前端把返回值插入列表（默认降序下即首位）。
- 错误码：400 `VALIDATION_FAILED`（字段级 `details[].path`）；409 `PROJECT_CODE_EXISTS`（编号重复 → `code` 字段级提示）；401；500。写请求可选带 `Idempotency-Key`，防重复提交。

**编辑 `PATCH /api/v1/projects/{id}`（已入契约）**

- 请求：`ProjectUpdateBody`（`code` / `name` / `customer` / `region` / `projectType` / `managerId` / `stageKey` / `status` / `description` 均可选）+ **必须回传 `version`**（乐观锁）；`seqNo` 不在更新字段内、不可修改；`createdAt` / `updatedAt` 同样不接受更新。
- 响应 200 + 最新 `Project`；前端以返回值覆盖本地行（`createdAt` 不受更新影响；`updatedAt` 由服务端刷新为最近活动时间）。
- 错误码：400；404（不存在 / 非成员，统一 404 语义）；409 `VERSION_CONFLICT`（他人已改 → 提示「数据已被他人更新」并刷新，不自动重试）；409 `PROJECT_CODE_EXISTS`（编号重复）；403（权限矩阵落地后）。
- 前端现状：卡片铅笔入口使用 `ProjectModal`（Push 39；详情页入口已于 Push 49 移除），保存为内存态；接口就绪后替换 `handleUpdateProject`。

**删除 `DELETE /api/v1/projects/{id}`（待增补 → A5）**

- 前端需求：软删（列表 / 详情 / facets / 搜索统一不可见）；`seqNo` 不回收；需 `version` 防误删；非成员 / 不存在统一 404。
- 前端交互：卡片 / 详情页删除入口 + 二次确认（建议输入编号确认），待接口定案后一并提 PR。

### 3.4 项目详情与总览汇总

**`GET /api/v1/projects/{id}`（已入契约）**：响应 = `Project`（字段同第 3.2 节）；详情页顶栏展示 `code` / `name` / `projectType`（accent）/ `managerIds`（显示名，多位按「、」连接，见 §二.1 / A22）/ `createdAt`（「创建于」）；404 → 「项目不存在或无权访问」空态 + 返回列表。

**`GET /api/v1/projects/{id}/summary`（已入契约）**：响应 `{ projectId, currentStage, overdue, done, total }`。

- `currentStage`：按阶段顺序第一个存在未完成任务的阶段；全部完成时前端按 `done === total && total > 0` 显示「全部完成」，不依赖额外取值。
- `overdue` = 未完成且 `plannedEnd` 早于今天（Asia/Shanghai）；一期服务端照常下发、汇总卡不展示（后续汇总栏扩展再回填）；`done` / `total` = 已完成数 / 总任务数。汇总卡展示项与任务表同源，接线后前端删除本地派生。
- 错误码：404（非成员同口径）。

### 3.5 任务表与任务详情抽屉

**`GET /api/v1/projects/{id}/tasks`（已入契约）**

| 参数 | 口径 |
|---|---|
| `stage` | 单值 `StageKey` |
| `filter[ownerId]` | UUID（任务负责人）；**Push 136 起负责人可多位**：命中口径 = 该任务任一位负责人命中即命中（见 A23） |
| `filter[status]` | 展示态多值：`pending/active/done/overdue/early_done` |
| `q` | 关键字（中英文任务描述） |
| `page` / `limit` | 默认 1 / 50，上限 200 |
| `sort` | 字段白名单待明确（A8） |

- 响应 `{ items: Task[], page, limit, total }`，每条为全字段（表格与抽屉直接渲染）。
- 展示顺序：前端按 `STAGE_KEYS` 九阶段分组；建议服务端默认返回顺序 = 阶段顺序 + 组内稳定顺序（A8）。
- 错误码：404（项目不存在 / 非成员）；400。
- 原型演示（Push 59 / 61）：任务只挂在印度项目 `inmu-0010`（前端 `tasksForProject()`），其余项目取到空列表 → 任务表按阶段骨架渲染（**常显**：只显示阶段名、没有任务行 —— 加出任务后其余阶段不消失）、汇总卡 `currentStage` 显示「—」；接后端后按 `{id}` 取数，行为与契约一致。

字段映射（前端演示模型 → 契约 `Task`，接线时按此替换）：

| 前端 `ProjectTask` | 契约 `Task` | 说明 |
|---|---|---|
| `id`（`t01`） | `id`（UUID） | |
| `stage`（中文名） | `stageKey`（英文 key） | 展示名用契约字典 `STAGE_NAMES` |
| `title` / `titleEn` | `title` / `titleEn` | 一致 |
| `owners` / `ownersEn`（姓名数组，Push 136） | `ownerIds`（UUID 数组，多位契约已落地：`Task.ownerIds` + `ownerNames`，见 A23） | 姓名由 `GET /users` 解析（A2）；`ownerEn` 契约无（A7） |
| `status`（五态中文） | `status`（基础态）+ `displayStatus`（派生展示态） | 前端改用 `displayStatus`，删除本地派生逻辑 |
| `progress` | `progress` | 0 / 0.25 / 0.5 / 0.75 / 1 |
| `startDate` / `dueDate` / `doneDate` | `plannedStart` / `plannedEnd` / `actualEnd` | `YYYY-MM-DD`（DateOnly） |
| `days` | `estimatedDays` | |
| `deliverable`（中文名） | `deliverable`（`DocType` 枚举） | |
| `change`（展示日期） | `changeRef`（UUID） | 变更摘要来源待定（A7） |
| `onTime`（「按时交付」） | `onTime`（boolean） | Push 67 起演示态还带派生标注「逾期未交付 / 逾期已交付」（由 `plannedEnd` / `actualEnd` + 完成情况派生，口径见 §二.2 与 §3.8 A14） |
| `note` / `headcount` / `priority` | 同名 | `priority` 为四象限枚举 |
| `files`（文件名数组） | 走文件接口 `filter[taskId]` | 列表是否随行下发摘要待定（A7） |

**`PATCH /api/v1/projects/{id}/tasks/{taskId}/progress`（已入契约）**

- 请求 `{ progress, actualEnd?, note?, version }`；`progress=1` 且缺 `actualEnd` 时服务端按当天写入。点击第 N 格 / 回退均调此接口，前端只做乐观更新 + 失败回滚。
- 响应 200 + 最新 `Task`（覆盖行内状态与汇总卡）。
- 错误码：400；404；409 `VERSION_CONFLICT`（刷新行数据 + 提示）；403（权限矩阵 M2-05 落地后由服务端裁决，前端置灰并提示）。
- 前端落点：`TaskBoard.tsx` / `TaskDrawer.tsx` / `Tracker.tsx`（当前内存态；接线后 `TaskDrawer` 复用列表数据，不做额外请求）。

### 3.6 文件 / 变更与蓝图流程（已入契约，本期未接线）

**文件与变更（files 14 条 + changes 2 条）**

| 接口 | 用途 | 关键错误码 |
|---|---|---|
| `GET /api/v1/projects/{id}/files` | 文件库列表（`filter[nodeId]/[taskId]/[status]/[docType]/[uploadedBy]`、`q`、分页、排序） | 404 |
| `POST /api/v1/files/uploads` | 发起分片上传（`intent=version` 草稿替换 / `change` 定档后变更；201） | 400 / 409 / 422 |
| `GET /api/v1/files/{id}` | 文件详情（状态、当前版本） | 404 |
| `GET /api/v1/files/{id}/versions` | 版本链（只追加） | 404 |
| `GET /api/v1/files/{id}/versions/{versionId}/download-url` | 短时签名下载地址（写审计） | 404 |
| `POST /api/v1/files/{id}/uploads/{uploadId}/parts` | 上传分片 | 410 `UPLOAD_SESSION_EXPIRED` |
| `GET /api/v1/files/{id}/uploads/{uploadId}` | 上传会话进度（续传依据） | 404 |
| `POST /api/v1/files/{id}/uploads/{uploadId}/complete` | 完成上传：登记版本（`change` 同事务写变更） | 409 `UPLOAD_INCOMPLETE` / 410 `UPLOAD_SESSION_EXPIRED` / 422 `FILE_HASH_MISMATCH` |
| `POST /api/v1/files/{id}/uploads/{uploadId}/abort` | 取消上传 | 409 |
| `POST /api/v1/files/{id}/finalize` | 定档锁版（此后修改必须走变更） | 409 `FILE_STATE_INVALID` |
| `POST /api/v1/files/{id}/rollback` | 版本回滚 | 409 |
| `POST /api/v1/files/{id}/recycle` / `restore` / `purge` | 回收 / 恢复 / 彻底删除（`purge` 仅管理员） | 409 / 403 |
| `GET /api/v1/projects/{id}/change-requests` | 变更记录列表 | 404 |
| `GET /api/v1/change-requests/{id}` | 变更详情 | 404 |

预览契约已入 `shared/`（`GET /api/v1/files/{id}/preview`，可选查询参数 `versionId`（A4-06 历史版本预览，缺省 = 当前版本）；响应 `FilePreviewResponse` = `status`（ready / not_ready / failed）+ `target`（渲染通道 pdf / image / structured）+ `url` / `expiresAt` / `pipelineVersion` / `generatedAt` / `reason`）—— 状态口径（PR #103 评审定案，D2-04~D2-07）：`ready` 内嵌查看器；`not_ready` 占位 + 轮询（服务端幂等补投生成任务、按三元组去重，不引入请求约定）；`failed` 失败态 + 降级「暂不支持在线预览，请下载查看」（D2-05，`reason` 随提示副行）；`target` 收到未知值（枚举追加属兼容变更，如 M4-06 CAD 通道定案后）一律降级「请下载」。文件 UI（u4）接线时在本章与第一章 / 第二章同步补页面口径。

**蓝图与流程节点（flow 10 条）**

| 接口 | 用途 | 关键错误码 |
|---|---|---|
| `GET` / `PUT /api/v1/blueprint` | 当前蓝图 / 保存草稿（schema + 引用校验） | 422 `BLUEPRINT_SCHEMA_INVALID` / `BLUEPRINT_REF_UNKNOWN` |
| `POST /api/v1/blueprint/publish` | 发布（递增版本，不影响已生成项目） | 422 |
| `GET /api/v1/blueprint/export` / `POST /api/v1/blueprint/import` | 导出 / 导入（round-trip 无损、导入幂等） | 422 |
| `GET /api/v1/projects/{id}/flow` | 项目流程（阶段 + 节点 + 约束 + 状态） | 404 |
| `POST /api/v1/projects/{id}/nodes` / `DELETE /api/v1/projects/{id}/nodes/{nodeId}` | 节点增 / 删（软删留痕） | 404 / 409 |
| `POST /api/v1/nodes/{id}/complete` | 完成节点（事务内过门禁；缺件明细 `missing`） | 422 `NODE_REQUIRED_DOC_MISSING` / 409 `NODE_ALREADY_DONE` / `NODE_DELETED` |
| `GET /api/v1/nodes/{id}/can-complete` | 完成预检（UI 置灰依据，不替代服务端校验） | 404 |

属管理端 / 后置能力，前端暂不接线。

### 3.7 统一错误码与前端处理口径

| 错误码 | HTTP | 触发场景（前端可见） | 前端处理 |
|---|---|---|---|
| `VALIDATION_FAILED` | 400 | 参数 / 请求体校验失败（`details[]` 字段级） | 表单字段级提示；列表请求保留上次结果并提示 |
| `AUTH_REQUIRED` | 401 | 会话缺失 / 过期 / 已撤销 | `apiFetch` 统一拦截 → `redirectToLogin()`（带 `returnTo`），业务代码不重复判断 |
| `AUTH_CALLBACK_FAILED` | 400 | 登录回调 state / PKCE / 换令牌失败 | 后端错误页；提供「重新登录」 |
| `FORBIDDEN` | 403 | 权限不足 | 提示无权限；按钮置灰（随权限矩阵落地） |
| `NOT_FOUND` | 404 | 不存在或无权访问（统一语义） | 详情空态 / 返回列表；不区分「是否存在」（防 IDOR） |
| `VERSION_CONFLICT` | 409 | 乐观锁冲突（项目 / 任务被他人先改） | 提示「数据已被他人更新」+ 刷新最新数据，不自动重试 |
| `PROJECT_CODE_EXISTS` | 409 | 新建 / 编辑项目编号重复 | `code` 字段级错误提示 |
| `NODE_REQUIRED_DOC_MISSING` | 422 | 节点 / 任务完成门禁缺件（`missing` 明细） | 弹窗列出缺件清单（M3-07） |
| `NODE_ALREADY_DONE` | 409 | 节点已完成 | 刷新流程状态 |
| `NODE_DELETED` | 409 | 节点已删除 | 刷新流程状态 |
| `BLUEPRINT_SCHEMA_INVALID` | 422 | 蓝图 schema 校验失败 | 蓝图编辑器逐项标错（管理端） |
| `BLUEPRINT_REF_UNKNOWN` | 422 | 蓝图引用未知节点 / 字典 | 同上 |
| `FILE_STATE_INVALID` | 409 | 文件状态不允许该操作 | 提示并刷新文件状态 |
| `UPLOAD_INCOMPLETE` | 409 | 分片未齐 | 续传 / 重传 |
| `UPLOAD_SESSION_EXPIRED` | 410 | 上传会话过期 | 重新发起上传 |
| `FILE_HASH_MISMATCH` | 422 | 内容哈希不符 | 重新上传 |
| `IDEMPOTENT_REPLAY` | 200 | 幂等重放（非失败） | 按成功处理（使用首次结果） |
| `PREVIEW_NOT_READY` | 200 | 预览未就绪 | 占位 + 轮询至 ready / failed（契约已入，待 u4 接线） |
| `PREVIEW_FAILED` | 200 | 预览失败 | 失败态 + 降级「暂不支持在线预览，请下载查看」（D2-05，`reason` 随提示副行） |
| `INTERNAL` | 500 | 服务端异常 | 通用错误提示（附 `traceId`），支持重试 |

- 非 2xx 一律按 `ApiError` 信封解析：`code` 决定分支、`message` 用于提示、`details[].path` 用于字段定位、`traceId` 附在提示副行 / console（排障）。
- 网络错误 / 非 JSON 响应：降级为「网络异常，请重试」，保留当前界面数据。
- 写操作（POST / PATCH / DELETE）带 `Idempotency-Key`（8–128 字符，同一表单提交复用同一键）；重复提交返回首次结果（`IDEMPOTENT_REPLAY`）。
- 预览失败不提供「重试」入口：用户触发重试一期不做（PR #103 评审定案）—— `failed` 一律降级「请下载查看」；如需保留，另行登记为后续卡片（显式写接口，如 `POST /files/{id}/preview/regenerate`），不在本期前端口径内。

### 3.8 对齐清单（px → wmj，M0-05 产出）

> 使用方式：wmj 在评审 / M0-01 决议会逐条给出「采纳 / 修改 / 拒绝 + 理由」；采纳项由 wmj 在 `shared/` 增补契约（PR 内附生成物），px 随后接线。A1 / A2 / A3 为 M1 / M2 出口前提（前端移除硬编码、时间区间直传）。

| 编号 | 事项 | 前端口径 / 依据 | 建议契约 | 归属 | 状态 |
|---|---|---|---|---|---|
| A1 | 首页「项目时间」区间（Q11） | 前端 URL 已按 `filter[timeFrom]` / `filter[timeTo]`（`YYYY-MM-DD`，闭区间）落地（Push 32 / 45）；技术设计 v0.3 §3.3 已点名「待契约增补」 | `ProjectListQuerySchema` 增 `filter[timeFrom]` / `filter[timeTo]`（DateOnly）：按 `updatedAt`（「项目时间」= 项目最近活动时间，v0.3 §7 第 4 项口径——主数据变更 / 阶段推进 / 任务变更刷新，文件与日报不刷新）在 **Asia/Shanghai 日界**截断——下界 `timeFrom 00:00:00 +08:00`（含）、上界 `timeTo 次日 00:00:00 +08:00`（不含）；列表与 facets 同一 QueryBuilder、同口径；只传一端合法，`timeFrom > timeTo` / 格式非法 → 400（前端丢弃非法值 + 归一化地址栏）；侧栏标签带 ⓘ 口径说明 | wmj | 已定案（PR #42，Push 49） |
| A2 | 用户目录 | `src/data/managers.ts` 硬编码 20 人；项目经理下拉、卡片姓名、任务负责人（`ownerId`）解析均缺接口 | `GET /api/v1/users`：`q?` / `page` / `limit`，只返回启用用户（PR #42 已落）；**卡片 / 列表 / 详情 / 创建与编辑返回随行下发 `managerName`（选 b，PR #42 补充中）**；供 `filter[managerId]`、新建 / 编辑项目、任务指派候选 | wmj | 已定案（PR #42，Push 49） |
| A3 | 字典下发 | `PROJECT_TYPES` 与 `PROJECT_TYPE_ACCENTS` 硬编码（`src/types.ts`）；Q2 已定案「accent 随 C9 字典元数据」，接口未定义 | `GET /api/v1/dicts`（全量）与 `GET /api/v1/dicts/{type}`（单类型，PR #42 已落）：一期 `region` / `projectType`（阶段 / 成果文件类型 / 紧急重要度走契约枚举）；项 `{ code, name, sort, enabled, metadata }`，项目类型必含 `metadata.accent`（建议 hex + `accentText`）；前端据此渲染、删除本地映射 | wmj | 已定案（PR #42，Push 49；色值待业务确认） |
| A4 | 用户偏好 | `ColumnPicker.tsx` 列显隐为内存态，刷新回默认；需求为跨设备记住勾选列 | `GET / PATCH /api/v1/users/me/preferences`：`PATCH` 合并语义（只传变更键），body 建议 `{ taskTableHiddenColumns?: string[] }`（值为列 key），响应回全量偏好；单用户单写者，可不带 `version`（PR #42 已落，含 `updatedAt`） | wmj | 已定案（PR #42，Push 49） |
| A5 | 项目软删 | 契约有更新无删除；`DELETE /projects/{id}` 已在技术设计 v0.3 §3.3 列出 | `DELETE /api/v1/projects/{id}`：软删（列表 / 详情 / facets / 搜索均不可见）；`seqNo` 不回收、`code` 不复用（唯一性保留）；带 `If-Match` 防误删（PR #42 落点，不用 body 传 version）；响应 200（400 / 404 / 409 见契约）；非成员 / 不存在统一 404；权限限项目经理 / 管理员并写审计 | wmj | 已定案（PR #42，Push 49） |
| A6 | facets 五组 vs 侧栏四组 | 契约返回 `region` / `projectType` / `managerId` / `stageKey` / `status` 五组；侧栏一期为地区 / 项目类型 / 项目经理 / 项目时间（时间维度不占 facets 组，见 A1） | facets 维持五组固定返回、不裁剪（PR #42 定案）；一期侧栏不展示「阶段 / 状态」（px 确认）；时间维度两端按 A1 同口径 | wmj + px | 已定案（PR #42，Push 49） |
| A7 | 任务列表字段范围 | 前端演示模型含 `ownerEn`（英文名）、`change`（变更日期）、`files`（文件名数组），契约 `Task` 无对应字段（`changeRef` 仅 UUID，文件走 files 接口） | 已定案（PR #42）：`TaskListItem` 去 `changeRef`、随行内联 `ownerName` / `changeSummary` / `files` 摘要；`ownerEn` 裁掉；新增 `GET /api/v1/projects/{id}/tasks/{taskId}` 供抽屉 | wmj | 已定案（PR #42，Push 49） |
| A8 | 任务默认排序 | 前端按九阶段（`STAGE_KEYS`）顺序分组、组内按源表顺序 | 已定案（PR #42）：任务列表默认返回顺序 = 阶段顺序 + 组内稳定顺序（一期 `created_at, id` 兜底）；`sort` 走 `TaskSortSchema` 白名单 | wmj | 已定案（PR #42，Push 49） |
| A9 | 列表排序维度（后续） | 一期 TIME 仅切换方向（项目时间 / 最近活动 `updatedAt`，默认降序省略）；后续支持「维度 × 方向」——按创建时间或最近活动时间排序 | `ProjectListQuery` 排序白名单补 `createdAt`（`sort=createdAt:asc\|desc`）；默认仍 `updatedAt:desc` | wmj | 已定案（PR #42，Push 69）：白名单补 `createdAt`（`sort=createdAt:asc\|desc`），默认仍 `updatedAt:desc`；前端「维度 × 方向」升级仍为后续项（Q15） |
| A10 | 任务创建 / 编辑接口（Q8 派生） | Q8 定案（2026-09-18）：施工人数 / 紧急重要度任务创建 / 编辑均可填、非必填；契约 `Task` 已有可空 `headcount` / `priority`，但任务域现仅 `GET /tasks` + `PATCH .../progress`，无写入通道 | `POST /api/v1/projects/{id}/tasks` 与 `PATCH /api/v1/projects/{id}/tasks/{taskId}` 已入契约（Push 62）：创建 body = `stageKey` / `title` / `titleEn?` / `taskNodeId?`（按项目判重键）/ `ownerId?`（缺省 = 项目经理兜底）/ `plannedStart?` / `plannedEnd?` / `estimatedDays?` / `headcount?` / `priority?` / `deliverable?` / `note?`；编辑 body = 负责人 / 日期 / 人数 / 紧急重要度 / 备注 + `version`（任务描述 / 成果文件按 A1-17 生成后锁定，进度走 `/progress`）；批量与去重语义见 A11（已入契约） | wmj | 已入契约（Push 62） |

| A11 | 任务模板接口（模板查询 / 增删改 / 实例化；Push 60 / 61 派生） | 任务模板页（Push 58~60）与项目总览「添加任务」卡片（Push 61）已按此口径落地：模板 = 名称 + 节点顺序、按阶段分开；节点池 = 该阶段全部预设节点按出现顺序去重（硬件实施 26 个）；添加任务按节点 id 判重（同一节点在项目里只留一份），支持逐条添加与「整套添加」（模板里未加过的节点批量创建）；草稿字段口径见 `字段对照清单.md` §四 | 已入契约（Push 62）：`GET /api/v1/task-nodes?stage=`（任务节点库）；`GET /api/v1/task-templates?stage=` / `GET /api/v1/task-templates/{id}`；`POST /api/v1/task-templates`、`PATCH /api/v1/task-templates/{id}`（改名 / `nodeIds` 全量替换 + `version` 乐观锁）、`DELETE /api/v1/task-templates/{id}`（带 `version`）；实例化 = `POST /api/v1/projects/{id}/tasks/from-template`（`templateId` + 可选 `nodeIds`；`skipExisting` 默认 true → `created` + `skipped`）；落库建议 `task_nodes` / `task_templates` / `task_template_nodes`（见 `字段对照清单.md` §四） | wmj | 已入契约（Push 62） |
| A12 | 任务状态可编辑 + 与进度条联动（Push 65 派生） | 业务口径（2026-09-19）：任务表「任务状态」列要能直接选（色标签下拉），并与四格进度条双向联动 —— 点进度条 0 格 = 待开始 / 1~3 格 = 进行中 / 4 格 = 已完成（按工期可能「提前完成」）；选状态反向驱动进度条（进行中至少 1 格、已完成 / 提前完成四格全亮、已延期保持当前格数）；手动状态优先于派生，直到进度条再次被点 | 契约 `Task` 只有 `status` 枚举、`PATCH /api/v1/projects/{id}/tasks/{taskId}`（`TaskUpdateBody`）**不含状态字段**（现口径 = 状态由进度与日期派生、客户端只改进度 `/progress`）；需要 wmj 决议：① 编辑接口是否放开 `status`（含「手动优先」语义）② 进度与状态的联动由前端计算还是服务端统一裁决（避免两端口径漂移）—— 建议服务端在任务写入时统一按本口径回填 `status`，客户端只提交状态或进度其一 | wmj | 已定案（PR #42，Push 70）：`TaskUpdateBody` 开放基础三态 `status`（服务端同事务联动进度 / 完成日期）；「已延期 / 提前完成」派生不可写、且派生优先（下拉不再盖掉「已延期」）；前端 `statusOverride` 可删 |

| A13 | 实际完成日期可编辑 + 与状态 / 进度联动（Push 67 派生） | 业务口径（2026-09-19）：任务表「实际完成日期」列要能直接点选日期 —— 填日期 = 完成（四格全亮、按工期派生「已完成 / 提前完成」）；清除 = 退回进行中（进度 3 格）；反之把状态手动改成非完成态（待开始 / 进行中 / 已延期）或把进度条退回 1~3 格时，实际完成日期自动清空（与 A12 同一套联动口径） | 契约现状：`PATCH /api/v1/projects/{id}/tasks/{taskId}`（`TaskUpdateBody`）不含实际完成日期，但 `PATCH /api/v1/projects/{id}/tasks/{taskId}/progress`（`TaskProgressUpdateBody`）已有可空 `actualEnd`（`progress=1` 且缺省时服务端按当天写入）—— 建议：填 = 走 `/progress` 写 `progress=1` + `actualEnd`；清除 = 走 `/progress` 写 `progress=0.75`，由服务端在 `progress < 1` 时清空 `actual_end`（现 `actualEnd` 为 optional、不可显式置空，清除语义需服务端定）；或按 A12 决议把「状态 / 进度 / 实际完成日期」收进同一条写入裁决（服务端统一回填，避免三者在前后端各派生一套） | wmj | 已定案（PR #42，Push 70）：填 / 清都走 `/progress` —— 清 = 进度写回 <1 档（服务端清 `actual_end`，唯一清除方式）；点满四格 / 选「已完成」由服务端补当天（原型现留「—」） |


| A14 | 「是否按时交付」的逾期派生（Push 67 派生） | 业务定稿（2026-09-19）：逾期标注从「实际完成日期」列移到「是否按时交付」列 —— 未完成且已过 `dueDate` 显示「逾期未交付」；已完成但晚于 `dueDate`（或未填完成日期、但 `dueDate` 已过）显示「逾期已交付」；其余照源表 `onTime` 值。另：任务只要已过 `dueDate` 且未完成，点四格进度条不得把状态从「已延期」改成「待开始 / 进行中」 | 契约现状：`Task.onTime` 为可空 boolean、展示态枚举已有 `overdue`（未完成超期）/ `early_done`，没有「逾期已交付」的取值 —— 建议服务端在任务写入 / 派生时统一回填 `onTime`（是否按时）与展示态，前端按本口径渲染（`onTime=false` + 已完成 = 逾期已交付、`onTime=false` + 未完成且已过 `plannedEnd` = 逾期未交付）；与 A12 / A13 的联动裁决一并定 | wmj | 已定案（PR #42，Push 70）：服务端读时派生 `onTime` + 展示态（枚举不变）；「逾期未交付 / 逾期已交付」按 `onTime` + `displayStatus` 渲染，前端删本地派生；k4 接线时逾期判定改用真实日期 |
| A15 | 看板「+ 添加」建的任务阶段留空（Push 82 派生） | 两块钱查看板（人员任务分配 / 任务进展）列底部「+ 添加」直接建任务：列只承载「负责人」或「状态」，**没有阶段上下文**，所以原型把 `stage` 留空，到「项目总览」落在「未分组」组（§6.3）。契约 `TaskCreateBody`（A10）的 `stageKey` 目前是必填 | 二选一：① `stageKey` 放宽为可空（看板上建出来的任务允许先无阶段，后续编辑再补）；② 看板「+ 添加」改为带阶段选择（前端做一个小下拉：9 阶段 + 未分组），契约不变。倾向 ②（数据的阶段性口径更稳），请 wmj 决议 | wmj + px | **已定案（2026-09-21 业务确认：1 选 A —— 允许「临时任务」不带阶段）** —— `tasks.stage_key` 由 not null 放宽为可空（或补一个「未分组」合法值，机制由 wmj 定）；契约 `TaskCreateBody.stageKey` 由必填改可选；前端**不加**阶段下拉（保持现状：卡片「所属阶段」显示「未分组」、到项目总览落最后一组）。 |

| A16 | 进度档位名 / 抽屉内可改进度 / 卡片可改实际完成日期（Push 98 派生） | 业务口径（2026-09-20）：① 四格进度第 4 档中文档位名 = **已完成**（原「完成了」），任务表 / 任务详情抽屉 / 看板卡片三处一致；② **任务详情抽屉的项目进度可直接改** —— 进度条长度不变，**四颗点平均分布在条上**（1/4 / 2/4 / 3/4 / 末尾各一颗 = 刚开工 / 完成一半 / 快完成了 / 已完成），点哪颗写哪档，联动口径与任务表一致（A12）；③ **看板卡片**的进度显示中文档位（不再写百分比），并新增「实际完成日期」一栏 —— 卡片上直接点选小日历改（填 = 完成、清 = 退回进行中，与 A13 同一套口径）。 | 契约无新增字段：进度仍走 `PATCH /api/v1/projects/{id}/tasks/{taskId}/progress`（#14），实际完成日期走任务更新接口 `PATCH /api/v1/projects/{id}/tasks/{taskId}`（`TaskUpdateBody`，A13 已提）—— 前端只需把这条路径也接到卡片上。 | 待 wmj 决议（前端已按此实现，原型内存态） | **已定案（2026-09-21 业务确认，无需 wmj 复核）** —— 前端三条保留：第 4 档档位名「已完成」、抽屉内可改进度（四颗点平均分布）、看板卡片可改实际完成日期；后端**无契约缺口** —— 进度走 `PATCH /api/v1/projects/{id}/tasks/{taskId}/progress`，实际完成日期同走该接口（填 = `progress=1`、清 = 写回 <1 档，A13 口径），接后端时直接接线即可。 |
| A17 | 抽屉内可改任务状态 / 实际完成日期 + 抽屉进度条与四颗点统一绿色（Push 101 派生） | 业务反馈（2026-09-20）：① **任务详情抽屉的「任务状态」改为可改** —— 五态色标签下拉（已延期 / 进行中 / 已完成 / 待开始 / 提前完成），联动与任务表行内完全一致（选完成态 = 四格全亮、进行中 = 至少一格、待开始 = 清零、已延期 = 保持格数；改成非完成态时实际完成日期一并清空，见 A12 / A13）；② **抽屉的「实际完成日期」改为可改** —— 单日期小日历（上 / 下月、清除、今天），填 = 完成（四格全亮、按工期派生 已完成 / 提前完成）、清 = 退回进行中（进度 3 格），与任务表行内 / 看板卡片同一套口径（A13）；③ **抽屉的进度条与条上四颗点统一用绿色**（与任务表四格点同一个绿）—— 原先按状态取色（已完成 / 提前完成 绿、进行中 蓝、其余 灰），业务反馈「为什么是灰的 应该也要绿的」；进度条只表达「做了多少」，状态色仍由状态签 / 色点表达。 | 契约无新增字段：任务状态走任务更新接口 `PATCH /api/v1/projects/{id}/tasks/{taskId}`（`TaskUpdateBody`，状态字段的契约缺口见 A12）、实际完成日期走同一接口（缺口见 A13）、进度走 `PATCH /api/v1/projects/{id}/tasks/{taskId}/progress`（#14）—— 前端只是把这两条路径也接到抽屉上。 | 待 wmj 决议（前端已按此实现，原型内存态） | **已定案（2026-09-21 业务确认，无需 wmj 复核）** —— 抽屉内改任务状态走 `PATCH /api/v1/projects/{id}/tasks/{taskId}`（`status` 基础三态，A12 · Push 70 已入契约）、改实际完成日期走 `/progress`（A13）、抽屉进度条与四颗点统一绿色为纯前端样式；后端**无契约缺口**。 |
| A20 | 列内顺序以项目总览为准（阶段为主键）+ 添加时可指定插入位置（Push 111 派生） | 业务口径（2026-09-20）：「我想了一下不对 应该按照项目总览的顺序排 —— 某个工作人员在项目总览下的顺序，他的从上到下的任务排」；「人员要指定位置放入」。前端口径：① 项目总览任务表 / 人员任务分配 / 任务进展三块视图共用一套顺序 —— **阶段为主键**（项目总览分组顺序，未分组垫底）、组内按看板顺序表；② 「添加 → 阶段任务」的模板卡片里可选**插入位置**（该阶段最后（默认）/ 该阶段最前 / 在《某张同阶段任务》之后），整套添加时按列表顺序整段插入。 | 契约**无新增字段**：顺序仍走 A19 的顺序表（`Task` 没有顺序 / 位次字段，落库口径同 A19 —— `sort_index` 列或批量排序接口二选一，请 wmj 决议）；「插入位置」只是前端写顺序表时选的初始位次，不新增接口。 | 待 wmj 决议（前端已按此实现，原型内存态） | **已定案（2026-09-21 业务确认：1 选 A —— 顺序要持久化）** —— 同 A19：列内顺序与「插入位置」选出来的位次都要存下来（机制同上，由 wmj 定）。 |
| A19 | 看板列内顺序（Push 105 派生） | 业务反馈（2026-09-20）：「要有合适的交互显示拖动后的位置」—— 两块看板的列内顺序由拖动落点决定（插到哪两格之间就落在哪），前端把顺序存在 `ProjectDetail` 的看板顺序表（`taskOrder`，任务 id 数组，原型内存态）。这是**一套全局顺序**：两块看板的「列」只是它的子序列，`项目总览` 任务表同阶段内顺序也按它走；目标列没有别的卡片时只换列、不动顺序。 | 契约**无字段可存**：`Task` 现有字段里没有顺序 / 位次（`sort` 只在列表查询上、`TaskSortSchema` 只是排序白名单），服务端要落库需二选一 —— ① `tasks` 加 `sort_index`（或 `position`）列，并让任务更新接口 `PATCH /api/v1/projects/{id}/tasks/{taskId}`（`TaskUpdateBody`）放行该字段；② 单开一个批量排序接口（如 `PATCH /api/v1/projects/{id}/tasks/order`，body = 有序任务 id 数组，一次落一批）。倾向 ②（顺序是整列语义，批量写更贴合拖动）。请 wmj 决议 | 待 wmj 决议（前端已按此实现，原型内存态） | **已定案（2026-09-21 业务确认：1 选 A —— 顺序要持久化）** —— 拖动后的顺序刷新 / 换设备 / 换人打开都要保持；落库机制二选一（`tasks` 加 `sort_index` 列 or 批量排序接口）由 wmj 定。 |
| A18 | 看板卡片拖动换列 = 改负责人 / 改状态（Push 104 派生） | 业务反馈（2026-09-20）：「卡片要支持拖动 移动别的组 也相当于编辑 移动后相关的信息要对应」（截图 = 人员任务分配 / 任务进展两块看板）。前端口径：**把卡片拖到别的列放开 = 在该列上改一次字段** —— 人员任务分配看板 = 改任务负责人（拖到「待分配」列 = 清空负责人，回待分配；**Push 136 起负责人可多位** —— 多负责人任务拖进某位负责人的列 = 负责人整体换成这一位、拖进「待分配」= 清空，字段与口径见 A23）；任务进展看板 = 改任务状态，并沿用既有联动（同写四格进度 `progressAfterStatus`、改成非完成态时清空实际完成日期 `isCompleteStatus`，见 A12 / A13 / A16）。两条路写的都是任务表行内编辑（§6.9）与任务详情抽屉（§6.5）同一张任务覆盖表，拖动后卡片上的进度档位 / 实际完成日期 / 是否按时交付随之同步。 | 契约**无新增字段**：负责人走任务更新接口 `PATCH /api/v1/projects/{id}/tasks/{taskId}`（`TaskUpdateBody`，清空 = 待分配；多位字段、命中口径与看板列计数见 A23）、任务状态走同一接口（状态字段的契约缺口见 A12）、进度走 `PATCH /api/v1/projects/{id}/tasks/{taskId}/progress`（#14）—— 前端只是把「拖动换列」这一个手势映射到这三条已有路径上，未新增接口。另：**「拖动换列 = 编辑」是否登记为定档交互，请 wmj 一并复核**（`系统功能书.md` 目前未登记该手势）。 | 待 wmj 决议（前端已按此实现，原型内存态） | **已定案（2026-09-21 业务确认：3 选 A —— 保留「待分配」列）** —— 负责人为空是合法中间状态；`tasks.owner_id` 由 not null 放宽为可空，契约 `Task.ownerId` 允许 null、`TaskUpdateBody.ownerId` 支持**显式置空**（现在只有「不传 = 不改」）——**Push 136 起该口径升级为多位**（`owner_ids` 数组或关联表，空集 = 待分配；见 A23）；拖进「待分配」列 = 清空负责人。「拖动换列 = 编辑」**已登记为定档交互**（2026-09-21 业务确认，无需 wmj 复核）：上游功能书本就有 B2-01「卡片拖拽换人」与 B2-02「拖拽变更状态」，项目详情页的「人员任务分配 / 任务进展」两块看板沿用同一手势（对应改负责人 / 改状态），本次只在 `系统功能书.md` 的 A1-04 三视图口径里点名同一套手势。 |
| A21 | 日报与问题（Push 128 新提） | 业务口径（2026-09-21）：「标签导航栏旁边新加『日报及问题』导航」—— 项目详情页第四块视图，内容 = 该项目日报（A3-01 字段，新 → 旧）+ 问题四态看板（A3-10）。前端口径：① 日报按项目 + 日期区间取（默认近一个月、分页）；② 问题按项目 + 状态取，四态固定顺序 未分组 → 未解决 → 处理中 → 已完成、空列保留；③ 日报「现场发现问题」非空 → 服务端自动生成问题记录（**幂等**：同一条日报只生成一次，A3-09），并按归类自动分派责任部门 / 责任人（A3-10 四态流转 + ADR-026 时限落 `due_at`）；④ 日报填报 / 编辑（草稿、提交、补填 A3-02）与问题状态流转（允许回退且留痕）、解决方案回复为后续迭代，接口先按下栏建议契约留位。 | 建议契约：`GET /api/v1/projects/{id}/reports?filter[dateFrom]&filter[dateTo]&page&limit`（`DailyReport` 全字段：`date` / `authorId` + `authorName` / `submittedAt` / `state` / `headcount` / `doneWork` / `plan` / `foundIssue` / `issueCategory` / `suggestion` / `taskIds` / `files` 摘要）、`GET /api/v1/projects/{id}/issues?filter[state]&filter[category]&page&limit`（`Issue` 全字段：`title` / `state` / `category` / `reporterId` + `reporterName` / `ownerDept` + `ownerId` / `taskId` / `raisedAt` / `dueAt` / `solution` / `reportId`）；写路径 `POST /api/v1/reports`（草稿 / 提交 / 补填）、`PATCH /api/v1/reports/{id}`、`PATCH /api/v1/issues/{id}`（状态流转 + 解决方案回复）。落点 = `server/src/modules/report-issue`（wmj 线，当前 g4 骨架占位），与字典（归类取值 C9）同批落地。 | wmj | 提案（待 wmj 定契约；前端已按演示模型实现：四块子视图 + 日报填写 / 暂存 / 提交（提交时按 A3-09 自动生成「未分组」问题）+ 表格与看板两处呈现，均**内存态原型**） |
| A22 | 多项目经理（Push 136 新提） | 业务口径（2026-09-22）：「一个项目可以有多个项目经理和任务负责人」。前端口径（已实现，原型内存态）：`Project.managerIds` 数组（至少一位、顺序 = 展示顺序）；卡片 / 详情顶栏 / 任务表「项目经理」列按「、」连接展示；新建 / 编辑弹窗、任务表行内、任务详情抽屉三处都是**多选**（取消最后一位不生效）；首页侧栏计数每位各计一次、筛选「任一位命中即命中」、搜索任一姓名命中即命中。 | **已落契约与落库（Push 136 同批，wmj 线代为落地）**：主数据取**数组列**方案 —— `projects.manager_ids uuid[] not null`（迁移 `0017_multi_manager_and_owner.sql`，ck 非空 / 无空元素、`ix_projects_manager_ids` GIN），删旧列 `manager_id`；契约 `Project.managerIds` + `managerNames`（同下标数组）、`ProjectCreateBody.managerIds`（至少一位）/ `ProjectUpdateBody.managerIds`（不传 = 不改、空数组 400）；`filter[managerId]` 命中 = `manager_ids && 筛选集`（任一位命中即命中）；facets 的 `managerId` 维度 = 数组展开按人头计数（每位各计一次）；记录级可见性（`listOwnedProjectIds`）与节点增删 / 阶段推进（`assertProjectManager`）均按「名单内任一位」放行。 | wmj | **已落 0017 + 契约**（§3.8 A22 关闭；系统功能书修订待 wmj 复核） |
| A23 | 多任务负责人（Push 136 新提） | 业务口径（2026-09-22）：「一个项目可以有多个项目经理和任务负责人」。前端口径（已实现，原型内存态）：`ProjectTask.owners` / `ownersEn` 两组同下标数组（可多位、**空数组 = 待分配**，沿用 A18）；任务表「任务负责人」列与任务详情抽屉都是多选；人员任务分配看板按每位负责人各出一列（同一张卡会在多列出现）、拖动换列 = 负责人整体换成拖入列那一位、拖进「待分配」= 清空；日报及问题面板的关联任务多位以「、」连接展示。 | **已落契约与落库（Push 136 同批，wmj 线代为落地）**：同样取**数组列**方案 —— `tasks.owner_ids uuid[] not null default array[]::uuid[]`（迁移 `0017`，ck 无空元素、`ix_tasks_owner_ids` GIN 取代 `ix_tasks_owner_due`），删旧列 `owner_id`（回填：null → 空数组、非空 → `array[owner_id]`）；契约 `Task.ownerIds`（空数组 = 待分配）、`TaskListItem` / `TaskDetail.ownerNames`（同下标数组）、`TaskCreateBody.ownerIds`（缺省 = 项目全部项目经理兜底、显式 `[]` = 待分配）/ `TaskUpdateBody.ownerIds`（不传 = 不改、`[]` = 置空、数组 = 整体替换）；`filter[ownerId]` 命中 = `owner_ids @> [值]`（任一位命中即命中）。「我参与的任务」判定随负责人名单（列表按项目名册口径不变）；看板的「每位负责人一列」是前端展示口径，服务端只存数组。 | wmj | **已落 0017 + 契约**（§3.8 A23 关闭；系统功能书修订待 wmj 复核） |
| A24 | 常用筛选（Push 138 新提） | 业务口径（2026-09-22）：「这个分类筛选 增加一个常用筛选 一组 就是不同的员工可能注意的项目不一样 所以需要分类 点击添加后选择多项 别的普通分类 然后保存 即出现在常用筛选 筛选的名字可以自定义」。前端口径（已实现，本地记忆）：侧栏第一组「常用筛选」= 可命名的筛选组合 —— 名称自定义（上限 20 字）、组合 = 地区 / 项目类型 / 项目经理 / 项目时间任意多项、最多 20 组；点「+ 添加」→ 在下方的分类里勾选多项 → 命名保存 → 立即出现在「常用筛选」并生效；胶囊带命中计数、悬停可编辑（改名 / 改勾选）或删除；应用中按条件等价高亮。 | 待 wmj 定契约：**跨设备记忆**建议复用 #15 用户偏好（A4）—— `GET / PATCH /api/v1/users/me/preferences` 新增 `homeSavedFilters` 键，值 = `SavedFilter[]`（`{ id, name, criteria: { regions: string[], projectTypes: string[], managerIds: string[], timeFrom: string \| null, timeTo: string \| null } }`，上限 20 组 / 名称 20 字）；A4 的 PATCH 为**合并语义 + catchall**，新增偏好键不破坏既有契约；**应用侧不需要新接口**（写回既有 `filter[...]`，列表 / facets 口径不变）。 | wmj | 待定契约（前端已按本地记忆实现，接口就绪后改存偏好；同步见 `技术设计v0.2` §8.2） |
| A25 | 任务删除（Push 141 新提） | 业务口径（2026-09-22）：「不要这个笔作为编辑了 这个基本上不用 改成删除按钮吧」—— 任务表行悬停的入口由「编辑任务」铅笔改为「删除任务」按钮（原型内存态：点击即从项目任务列表移除，刷新 / 换项目复位；不弹二次确认，悬停展开的「删除」胶囊即操作提示）。前端口径：删掉后该任务在三块视图（项目总览 / 人员任务分配 / 任务进展）一并消失，阶段完成度与整体进度同步重算，「添加 → 阶段任务」里该节点回到可添加（`existingTaskIds` 随之释放）；编辑任务入口仍为整行点击 / 看板卡片 → 任务详情抽屉。 | 契约**无删除通道**：任务域现有列表 / 详情 / 创建 / 编辑 / 进度（`POST /projects/{id}/tasks`、`PATCH .../tasks/{taskId}`、`PATCH .../tasks/{taskId}/progress`），`server/README.md`「后续卡片衔接」已登记「任务软删（DELETE，需 `tasks.deleted_at` 迁移）」。建议：`DELETE /api/v1/projects/{id}/tasks/{taskId}`（软删：列表 / 看板 / 门禁均不可见、留痕；对已删任务再次操作建议 409 `TASK_DELETED`（错误码待新增）或统一 404，由 wmj 定）；软删与「同一节点不可重复添加」约束的兼容口径一并定。 | wmj | **业务已定案（2026-09-22）：要做** —— 口径按本条建议（软删 + 列表 / 看板 / 门禁不可见 + 留痕）；契约与实现归 wmj 线，前端行内入口已就绪（原型内存态），接口落地即接线。 |
| A26 | 完成门禁接线（Push 143 后端已落地） | 业务口径（2026-09-22，wmj 线代记请 px 复核）：M3-03 完成门禁 + 「输出成果文件」多选已随 Push 143 在后端落地（契约 `Task.deliverableTypes: DocType[]`；`GET …/tasks/{taskId}/can-complete` 预检；`POST …/complete` 提交；缺件 422 `TASK_REQUIRED_DOC_MISSING` 返回清单；已上传未定档放行但 `warnings` 提醒定档 R02；重复完成 409 `TASK_ALREADY_DONE`）。前端口径（随 M3-07 联调）：① 表格 / 看板 / 抽屉三个「完成」入口统一改调 `POST /api/v1/projects/{id}/tasks/{taskId}/complete`（body：`version` + 可选 `actualEnd` / `note`），缺件按 `details[].code=required_doc` 展示清单并提供直接上传入口（A4-20）；② 完成前置灰依据 `GET …/can-complete`（`canComplete=false` 展示 `missing`）；③ 完成响应 `warnings[]`（`draft_doc_present`）非空 = 立即提示定档；④ 「输出成果文件」列表 / 抽屉列改多值展示，`frontend/src/data/tasks.ts` 的 `deliverable: string` 原型字段改数组（`deliverableTypes`）；⑤ `PATCH …/{taskId}`（`status=done`）与 `/progress`（`progress=1`）服务端走同一门禁，表格行内 / 看板拖动遇 422 的缺件提示口径一致。 | wmj（契约 / 后端，已落 · Push 143）｜px（前端接线） | 后端已落（Push 143）；前端随 M3-07 联调 |
| A27 | 干系人台账接线（Push 144 后端已落地） | 业务口径（2026-09-22，wmj 线代记请 px 复核）：j6 干系人台账已落地（迁移 0019；契约 `shared/src/modules/stakeholders.ts`）。前端口径（随 u 系列 / M6-06 联调）：① 台账页 / 项目详情「干系人」面板列表调 `GET /api/v1/stakeholders`（`q` 关键词（姓名 / 公司 / 职务）、`filter[companyType]` 四类多值、`filter[projectId]` 看某项目联系人、排序白名单 `updatedAt` / `createdAt` / `name`，缺省最近更新在前）；新建 / 编辑走 `POST` / `PATCH`（部分更新：`null` = 清空该字段、缺键 = 不改），删除为**软删**（`DELETE` 回 `{ id, deleted: true }`，前端本地移除即可）；② **字段级脱敏表现为「键不存在」而不是 null / 空串**：`phone` / `wechat` / `email`（需 `stakeholder.contact.view`）、`company` / `title`（`stakeholder.view`）、`remark`（`stakeholder.manage`）无权时响应里没有该键 —— 前端按「键是否存在」判断渲染「—」占位，**不要**把缺键当空值回写（否则会把无权字段清空）；③ 公司分类下拉四值：`libiao` 立镖机器人 / `supplier` 供应商 / `general_contractor` 总包单位 / `customer` 客户（契约 `STAKEHOLDER_COMPANY_TYPES`）；④ 项目关联：新建可带 `projectIds` 一并关联，详情与关联反查用 `projects[]`（`{ id, code, name }`）；⑤ 权限：读 `stakeholder.view`、写 `stakeholder.manage`（缺位 403；记录级不可见 / 不存在 / 已软删统一 404，前端按「不存在」提示）。 | wmj（契约 / 后端，已落 · Push 144）｜px（前端接线） | 后端已落（Push 144）；前端随 u 系列 / M6-06 联调 |

| A28 | 甘特图（Push 145 新提） | 业务口径（2026-09-22）：「我想做甘特图」「在标签导航栏加一个甘特图页面」「我的甘特图参考图一的开源网页 想要复刻图一的甘特图」（参考 VTable 甘特图基本用法）。前端口径（已实现，原型内存态）：项目详情页新增第二个视图「甘特图」（§6.14）—— 阶段分组、任务条 = 开始 → 预计完成（青 = 已完成进度 / 橙 = 剩余工期）、单日任务 = 里程碑菱形、深色竖线 = 实际完成日期、已延期 = 条头一段红内嵌色条（条本身不画描边）、今天 = 蓝底日期格 + 蓝虚线、三档缩放 + 阶段折叠 + 回到今天；**第二轮（业务口径 ④「维持手绘尽量还原官方的功能」）补齐官方交互** —— 拖条整体改期 / 拖条两端改工期 / 拖进度圆点改四档进度（拖动中给预览值 + 日期浮标，松手才写回，Esc 取消）、条选中（蓝描边）与悬停遮罩、条内「任务名 + 进度」标签、点左表任务行定位到条、拖左表右边线 = 移动分隔线（左表列宽固定，拖的是「右侧时间轴盖住左表多少」的边界；可拖 188~572px）；拖动只写既有字段（`startDate` / `dueDate` / `days` / `progress`，日期域夹在 2026 年内），进 `ProjectDetail` 内存态覆盖表、刷新复位；**与任务列表共用同一份数据，无新增接口**；左表状态圆点与任务表同色（`STATUS_DOT_CLASS` 改为导出）；**第三轮（业务口径「这个 ui 有点突兀 不太相容啊」「就是在末端 或者说两种颜色交接的地方 还有橙色的边框」「之前的颜色可以的 不用改啊 我只是让你改细节」）只修细节、配色一律不动** —— 任务条不再画描边（去掉 `ring-1 ring-inset ring-amber-600/40`，青色进度与橙色剩余工期之间为硬直缝、末端无镶边）、已延期改为条头 3px 红内嵌色条（独立子层抬到进度层之上）、图例补「已延期」一项；**第四轮补左表「开始日期 / 预计完成日期」两列**（对齐官方左表的 start / end，业务口径「我这个移动也要有这些信息 现在没做」）—— 任务行 = 该任务日期、阶段行 = 组内工期并集、没排期 =「—」，拖动条 / 拖两端 / 拖圆点时这两列跟着预览值实时更新（松手才写回、Esc 回位）、左表列宽固定（合计 572、默认全露）、分隔线可拖 188~572（拖窄 = 右侧时间轴盖住超出的列，不是把列挤窄）。**第七轮补 B3-06 余下的两件**（业务口径「先做这两个」）：① 时间轴**粒度**日 / 周 / 月（官方 `timelineHeader.scales`：表头两排随粒度换、列宽 = 一列跨几天 × 每天宽度，几何基准仍是每天多少 px，所以条 / 里程碑 / 实际完成刻度 / 今天线一律按天精确落点）；② **甘特内筛选** —— 负责人（含「待分配」）/ 只看已延期 / 只看未排期 + 命中计数 + 清空筛选，筛的是任务集合（时间轴范围、阶段汇总、左表行与行号跟着走，空阶段不再占行），筛空 / 筛得都没排期各有一个带「清空筛选」的空态。两件都**无新增接口、无新增字段**；工具条里不挂功能说明文案（业务口径「这个不要展示」—— 粒度 / 筛选的操作说明与筛选行尾那句「筛掉的阶段不再占行…」都已删）。**第八轮（业务口径「下拉框组件设计 保持一致 搜索下拉框」，截图 = 现状的原生 `select` 系统蓝高亮 + 参考的人员搜索下拉）—— 甘特图负责人筛选改走仓库统一口径的搜索下拉**：与人员下拉 / 任务表行内成员单元格共用同一份搜索列表（搜索框 + 头像 + 姓名 + 拼音 + 角色 + 选中绿勾 + 页脚；↑↓ / Enter 选、Esc 关），选项 = 全部 / 各负责人 / 待分配，选中值仍是任务里的姓名，无新增接口、无新增字段。 | 无接口缺口（复用 `GET /api/v1/projects/{id}/tasks`）。**唯一字段缺口 = 里程碑**：一期任务字段里没有「是否里程碑 / 任务类型」（源功能书 A1-11「里程碑与依赖」、B3-02「阶段里程碑菱形标注」都在二期，一期字段清单已删「是否里程碑 / 任务类型」），前端暂按「单日任务（开始 = 预计完成）」派生菱形。建议：二期补 `tasks.is_milestone`（或任务类型字典 / 模板节点上的里程碑标记）后，甘特图菱形改读该字段；另 B3-01 的「双条对比」本期只做了「计划条 + 实际完成刻度」（未画第二条），若要真双条请一并定字段。 | wmj | **业务已确认（2026-09-22）**：未排期口径（开始或预计完成任一未设定）、取数口径（不做万行专门设计）、里程碑按单日派生、依赖三条暂不做 —— 均已定案；仅「是否里程碑」字段（二期 · A1-11）与「只看未排期」服务端筛选参数待 wmj 定。 |

| A29 | 任务批量操作接线（Push 149 后端已落地） | 业务口径（2026-09-22，wmj 线代记请 px 复核）：M3-04 批量操作已随 Push 149 在后端落地（契约 `TaskBatchBody` / `TaskBatchChanges` / `TaskBatchResponse`；`PATCH /api/v1/projects/{id}/tasks/batch`）。前端口径（随 M3-07 联调）：① 任务表 / 两块看板支持多选后批量操作 —— 批量指派负责人（`ownerIds`，显式 [] = 待分配）、改状态（基础三态；`done` = 批量完成，服务端走同一完成门禁）、改期（`plannedStart` / `plannedEnd`）、人数 / 重要度 / 备注；② 请求 = `{ ids: string[]（1~100）, changes: {...} }`，**整体 200**：成功项在 `succeeded[]`（更新后的任务全量视图，按行替换）、失败项在 `failures[]`（`{ id, code, message, missing? }`）—— 前端按 `failures` 逐行标红并展示原因，不要因部分失败整表报错；③ `code=gate_not_passed` 的 `missing[]` 与完成门禁同形（docType / required / present），提示口径与 A26 一致；`already_done` = 该任务已完成（跳过）、`not_found` = 任务不存在 / 不属于本项目、`archived` = 项目已归档、`version_conflict` = 并发写入冲突（提示刷新）、`invalid_state` = 其它业务校验失败；④ 空 `changes` 前端拦截（服务端 400 `VALIDATION_FAILED`）；批量白名单不含任务描述 / 成果文件（A1-17 生成后锁定）与列内顺序（顺序走拖拽逐条）。 | wmj（契约 / 后端，已落 · Push 149）｜px（前端接线） | 后端已落（Push 149）；前端随 M3-07 联调 |

以上 A1~A29 与第六章 Q8 / Q11 / Q15 互为索引：Q11 = A1、Q15 = A9、Q8 = A10；Q9 / Q10 的 facets 差异 = A6；Q2 = A3；Q4 = A2；A11 为 Push 60 / 61 新提（模板接口与实例化）；A12 为 Push 65 新提（任务状态可编辑 + 与进度条联动，待 wmj 决议）；A13 为 Push 67 新提（实际完成日期可编辑 + 与状态 / 进度联动，待 wmj 决议）；A15 为 Push 82 新提（看板建任务的阶段口径，待 wmj 决议）；A16 为 Push 98 新提（进度档位名 / 抽屉内可改进度 / 卡片可改实际完成日期，待 wmj 决议）；A17 为 Push 101 新提（抽屉内可改任务状态 / 实际完成日期 + 抽屉进度条与四颗点统一绿色，待 wmj 决议）；A18 为 Push 104 新提（看板卡片拖动换列 = 改负责人 / 改状态，手势是否定档请 wmj 复核，待 wmj 决议）；A20 为 Push 111 新提（列内顺序以项目总览为准 + 添加时可指定插入位置，待 wmj 决议）；A19 为 Push 105 新提（看板列内顺序的落库口径，待 wmj 决议）；A10 / A11 已于 Push 62 入契约。**2026-09-21 业务定案（px 侧）**：A15 → 允许「临时任务」不带阶段（`stage_key` 放宽可空，选方案 ①）；A18 → 保留「待分配」列（`owner_id` 放宽可空，允许显式清空负责人）；A19 / A20 → 列内顺序**要持久化**（机制 —— `sort_index` 列或批量排序接口 —— 由 wmj 定）；A16 / A17 同日**已定案**（2026-09-21 业务确认，无需 wmj 复核：前端已实现，后端走既有 `PATCH .../tasks/{taskId}` 与 `/progress` 两条路径、无契约缺口）；A18 的「拖动换列 = 编辑」也已登记为定档交互（源功能书 B2-01 / B2-02 已有，项目详情页两块看板沿用）。三条定案的迁移与契约增补归 wmj，前端行为不变（已按此实现，仍为原型内存态）；同步落在 `字段对照清单.md` §七。A21 为 Push 121 新提（项目详情第四块视图「日报及问题」的日报 / 问题接口与写路径 —— 含页内四块子视图与「日报填写」提交时自动生成问题的幂等口径，待 wmj 定契约；前端已按演示模型实现内存态原型，契约落地后补字段映射并同步 `字段对照清单.md`）。A24 为 Push 138 新提（首页「常用筛选」组合的跨设备记忆键 —— 应用侧复用既有 `filter[...]`、无新接口，待 wmj 定契约）；A25 为 Push 141 新提（任务删除 —— 行悬停入口由编辑铅笔改为删除按钮，契约无删除通道，建议软删 `DELETE /projects/{id}/tasks/{taskId}`，待 wmj 决议）。A26 为 Push 143 新提（完成门禁 + 成果文件多选后端落地后的前端接线：完成入口统一改调 `POST …/complete`、缺件清单与 `canComplete` 置灰、`warnings` 定档提醒、409 已完成的提示、`deliverableTypes` 多值展示，随 M3-07 联调）。A27 为 Push 144 新提（干系人台账接线：列表 / 详情 / 新建 / 编辑 / 软删 / 项目关联，**字段级脱敏按「键不存在」处理**，随 u 系列 / M6-06 联调）。A28 为 Push 145 新提（甘特图 —— 视图与任务列表同数据源、无新增接口；第二轮补齐官方交互：拖条改期 / 拖两端改工期 / 进度圆点改进度 / 选中 / 悬停 / 定位 / 拖左表右边线 = 移动遮挡边界（左表列宽固定、超出的列被时间轴盖住，不是把列挤窄），同样只写既有字段；缺口是「是否里程碑」字段：一期没有，前端暂按单日任务（开始 = 预计完成）派生菱形，建议随 A1-11 / B3-02 二期字段一并补，待 wmj 决议）。A29 为 Push 149 新提（任务批量操作接线：任务表 / 看板多选后批量指派 / 改状态 / 改期 / 人数 / 重要度 / 备注；整体 200 + `failures[]` 逐行标红；批量完成缺件按 `missing[]` 提示，随 M3-07 联调）。
## 四、字段校验与规则

### 1. 项目字段

| 字段 | 必填 | 规则 |
|---|---|---|
| code | 是 | 项目编号，对齐契约 `projects.code`；由创建人填写（新建表单必填）；格式建议四大写字母-日期-四位序号（如 `CNBJ-20260708-0001`），仅作输入提示、不做强校验；唯一性由服务端保证（重复返回 409 `PROJECT_CODE_EXISTS`）；创建后可在编辑时修改（更新接口支持 code，同样校验唯一性；前端编辑入口已上线：卡片铅笔按钮（Push 39；详情页入口已于 Push 49 移除）） |
| seqNo | 只读 | 项目序号：服务端创建时分配（全库唯一正整数、不可修改、不回收；契约 `projects.seq_no` ↔ `seqNo`）；卡片右上角两位补零展示，列表可排序 / 可检索 |
| description | 是 | 中文短语，建议不超过 30 字（契约侧对应 `name`；字段名对齐为后续项） |
| managerIds | 是 | 项目经理（UUID 数组，**至少一位**、可多位；Push 136）；演示取自项目经理目录，界面按「、」连接显示姓名 |
| createdAt | 只读 | 服务端生成（创建时写入；编辑不改；卡片右下角 / 详情顶栏「创建于」展示） |
| updatedAt | 只读 | 服务端生成（最近活动时间：主数据变更 / 阶段推进 / 任务变更刷新；列表排序 / 搜索 / 「项目时间」区间筛选使用） |
| accent | 只读 | 服务端下发或前端分配（待确认，见第六节） |

### 2. 任务字段

> Push 136 起「任务负责人」可多位（A23）：字段名由 `owner` / `ownerEn` 改为 `owners` / `ownersEn`（两组同下标数组，空数组 = 待分配）。

| 字段 | 必填 | 规则 |
|---|---|---|
| stage | 是 | 必须属于 9 个施工阶段枚举 |
| title | 是 | 中文任务描述 |
| owners / ownersEn | 否（可空） | 任务负责人（数组，Push 136 起可多位；空数组 = 「待分配」，沿用 A18）；元素为「姓名」与「拼音名」两组同下标数组，界面按「姓名(拼音)」以「、」连接显示 |
| status | 是 | 枚举：待开始 / 进行中 / 已完成 / 已延期 / 提前完成（后二者为派生展示态，见第二节「状态派生」），与 progress 联动 |
| progress | 是 | 0 / 0.25 / 0.5 / 0.75 / 1；等于 1 视为完成 |
| startDate / dueDate / doneDate | 否 | 原型为「M月D日」中文短日期；建议后端用 ISO 8601，前端本地化展示；`doneDate`（实际完成日期）Push 67 起支持在表格行内填 / 清（填 = 完成、四格全亮；清 = 退回进行中、进度 3 格），与状态 / 进度互为反向联动（状态改成 待开始 / 进行中 / 已延期 或进度条退回 1~3 格时自动清空） |
| days | 否 | 整数；建议由开始 / 预计完成日期推导，避免与日期矛盾 |
| headcount | 否 | 正整数（人）；任务创建 / 编辑均可填、非必填（Q8 定案）；源表为空时当前由前端演示值兜底 |
| priority | 否 | 枚举：高 / 中 / 低；任务创建 / 编辑均可填（项目经理 / 任务负责人均可）、非必填（Q8 定案）；源表为空时当前由前端演示值兜底 |
| onTime | 否 | 源表「是否按时交付」：值 = 「按时交付」或空；空 = 未交付。Push 67 起展示口径由日期派生：未完成且已过预计完成日期 → 「逾期未交付」（红）；完成但晚于预计完成日期 → 「逾期已交付」（琥珀）；其余照源表值（绿）。接口口径见 §3.8 A14 |
| files | 否 | 文件名数组；建议限制类型与大小，前端只读展示 |

## 五、非功能需求

- 排序：项目默认按项目时间（`updatedAt`）倒序（最近活动在前）；卡片展示仍为创建时间（`createdAt`，「创建于」）；任务按阶段顺序 + 表内原始顺序。
- 分页：项目与任务数量增长后必须分页（原型为全量渲染：项目 20+、单项目任务 44）。
- 搜索：建议服务端过滤（前端当前为本地过滤，仅适用小数据量）。
- 错误处理：接口失败时前端需要提示（原型未实现，接口就绪后补）。
- 时区：统一 Asia/Shanghai；时间字段建议 ISO 8601 带时区，前端本地化展示。
- 权限：新建 / 编辑 / 删除项目、更新任务进度的权限模型待定（建议接入 Casdoor Roles / Permissions）。
- 状态保持：首页搜索与筛选条件已按技术设计 v0.2 §8.2 写入 URL query（可分享 / 可收藏 / 刷新不丢，浏览器后退可回到上一次的列表地址）；任务进度、阶段折叠 / 列显隐、新建 / 编辑项目仍为内存态，刷新回默认，接入后端后由服务端持久化；不带参数时的「上次筛选记忆恢复」已落地（Push 45，localStorage 兜底、URL 优先），见第六章第 12 条。

## 六、待确认问题（需与 SSO 管理员 / 后端对齐）

1. 项目编号由前端输入还是后端生成？唯一性由谁保证？→ **已定案**：由创建人填写；唯一性由服务端校验 + 数据库唯一约束保证（重复返回 409 `PROJECT_CODE_EXISTS`）；格式（四大写字母-日期-四位序号）仅作输入提示、不做强校验；创建后可通过更新接口修改（同样校验唯一性）。
2. 主题色 `accent` 由服务端下发还是前端分配？→ **已定案**：随项目类型字典（C9）元数据下发、前端不硬编码颜色；当前 `PROJECT_TYPE_ACCENTS` 映射为演示阶段临时实现，待字典接口就绪后移除；字典接口（`GET /dicts`）与用户目录（`GET /users`）提案见第三章对齐清单 A3 / A2（Push 47）。
3. 新建 / 删除 / 更新任务进度的权限：按组织、部门还是角色控制？是否需要操作审计？
4. 项目与任务数据是否与 Casdoor 用户体系关联（项目经理、任务负责人是否取自用户表 / 工号）？→ **接口待增补**：提案见第三章对齐清单 A2（`GET /users`，只返回启用用户；项目经理与任务负责人均按 UUID 关联、显示名由用户目录解析）。
5. 项目数量级与分页策略；单项目任务量（原型 44 条）与多项目后的规模。
6. 任务数据来源：是否由源表格（Excel）导入？导入频率与覆盖策略（全量重导 / 增量更新）？
7. 任务进度是否允许回退（当前原型支持回退到点击格）？回退是否需要写回与审计？
8. 源表为空的三列（预计所需施工人数 / 紧急重要度 / 文件）由谁维护、如何取值？→ **已定案（2026-09-18）**：① 预计所需施工人数——任务创建 / 编辑均可填写、**非必填**（可空；权限控制暂缓，随权限矩阵落地）；② 紧急重要度——任务创建 / 编辑均可填写（项目经理 / 任务负责人均可），枚举 高 / 中 / 低、非必填（契约 `PrioritySchema` 已含可空字段）；③ 文件——类型不限（多类型），公司要求**不通过第三方应用打开**、统一走站内**文件库** + 站内预览；任务行「文件」读文件库挂到该任务的文件（`GET /projects/{id}/files?filter[taskId]` 已入契约；列表随行 `files` 摘要见 A7），预览随 i3 切片补契约。派生后续：任务创建 / 编辑接口未入契约（任务域现仅列表 + 进度更新）——写入通道见 §3.8 A10，请 wmj 决议。
9. 首页筛选的地区 / 项目经理维度与技术设计 v0.2 §8 的 `filter[region]` / facets 接口如何对齐？`region` 字典（含「未分类」）由谁维护？→ **已定案（Push 34）**：地区走 `filter[region]`（多值逗号分隔），字典（缺省「未分类」）由 C9 维护；项目经理走 `filter[managerId]`（多值逗号分隔，UUID）——项目级唯一责任人为「项目经理」（`projects.manager_id`，必填），原 `projects.owner_id` 已删除；任务级为「任务负责人」（`tasks.owner_id`），两者不同粒度不可混用；facets 对应组名为 `managerId`。前端已按本口径 id 化（Push 36）：URL 参数 `filter[managerId]`、模型 `managerId` + 项目经理目录显示名；旧 `filter[manager]`（姓名）链接不再解析。剩余差异已收口：facets 五组 vs 侧边栏四组——时间维度定案见 A1 / Q11，阶段 / 状态一期不做（A6）。（对齐清单 A1 / A6）
10. 项目类型取值（T-sort / 3D分拣 / 飞箱）是否即 C9 字典「项目类型」的定版取值？卡片徽标是否需要英文名或图标？侧边栏已含「项目类型」维度（地区 / 项目类型 / 项目经理 / 项目时间），后端 facets 是否按同口径返回四组计数？→ **机制定案、取值待业务**：项目类型走字典 `projectType`、主题色随字典元数据；取值是否为 C9 定版待业务确认（技术设计 v0.2 §10 Q28）。facets 已入契约，返回 region / projectType / managerId / stageKey / status 五组；与侧边栏四组的差异已定案：阶段 / 状态一期不做（A6）、时间维度见 A1 / Q11。卡片徽标英文名 / 图标待定。facets 差异与时间区间见第三章对齐清单 A6 / A1。
11. 「项目时间」区间筛选后端如何对齐？日期按哪一时区截断？→ **已定案（PR #42，Push 49）**：契约落点同 A1——`filter[timeFrom]` / `filter[timeTo]`（`YYYY-MM-DD`，闭区间）按 **`updatedAt`（项目最近活动时间）** 在 **Asia/Shanghai 日界**截断（下界当日 00:00:00 +08:00 含、上界次日 00:00:00 +08:00 不含）；列表与 facets 同一 QueryBuilder、同口径；`timeFrom > timeTo` / 格式非法 → 400，前端按「丢弃非法值 + 归一化地址栏」处理；侧栏「项目时间」标签带 ⓘ 口径说明（文案见 §3.8 A1）。
12. 不带 URL 参数进入首页时是否需要「记忆并恢复用户上次选择」（系统功能书 A1-18）：用 localStorage 兜底（URL 优先、本地次之）？侧边栏开合状态是否也一起记忆？→ **已定案（Push 45）**：采用「URL 优先、本地记忆兜底」——带参数的链接严格按 URL 展示（不经记忆改写，有筛选时侧边栏仍强制展开）；无参数入口（书签 / 直接输域名）按本地记忆恢复，恢复结果用 replaceState 同步写回地址栏。记忆范围：地区 / 项目类型 / 项目经理 / 时间区间 / 排序 + 侧边栏开合，共用一个本地存储键 `libiaolink.home.prefs.v1`；关键字 `q` 属临时操作、不写入记忆。侧边栏开合：无参数入口完全按记忆恢复，无开合记忆时才回退「有筛选则展开」；冷启动（无记忆）收起且不写记忆。记忆只在用户主动操作（改筛选 / 开关 / 关闭侧栏）时写入，被动恢复不改写。退出登录（`/auth/logout`）时清除全部记忆，避免多人共用设备看到他人的上次选择。记忆损坏 / 不可写（隐私模式）时静默降级为默认视图。
13. 项目序号（`seqNo`）与项目编号（`code`）是什么关系？→ **已定案（2026-09-18）**：两者一一对应同一个项目、互不替代——序号由系统在创建时分配（`projects.seq_no`，全库唯一正整数，序列 `projects_seq_no_seq` 分配，不可修改、不回收：删除项目不重排 / 不重发），编号由创建人填写（`projects.code`，建后可改）；序号用于卡片徽标、列表排序（`sort=seqNo:asc|desc`）与快速检索；接后端后前端不再本地分配。
14. 最近活动时间（`updatedAt`）要不要上卡面展示？→ **后续项（本次不做）**：`updatedAt` 已用于排序 / 搜索 / 「项目时间」区间筛选（口径 = 项目最近活动时间——主数据变更 / 阶段推进 / 任务变更刷新，见 §3.8 A1 / 第六章 Q11）；卡面展示仍为创建时间（卡片右下角、详情顶栏「创建于」，编辑不修改）；「最近活动时间」是否也上卡面（或两者并列）待业务反馈后定。

16. 一个项目多位项目经理 / 一个任务多位负责人怎么落库？（2026-09-22 业务口径「一个项目可以有多个项目经理和任务负责人」）→ **已定案并落地（Push 136 同批）**：取**数组列**方案 —— `projects.manager_ids uuid[] not null` / `tasks.owner_ids uuid[] not null default 空数组`（迁移 `0017_multi_manager_and_owner.sql`）；`filter[managerId]` / `filter[ownerId]` 命中口径 = 与筛选值有交集（任一位命中即命中）；facets 的经理维度按「人头 × 项目」计数；记录级可见性与项目经理判定按「名单内任一位」放行。四处口径见 §3.8 A22 / A23 与 `技术设计v0.2-架构与数据模型.md` §2.3 / §8。

15. 列表排序维度是否支持「按创建时间」与「按最近活动时间」两种？→ **后续项（本次不做）**：一期排序维度 = 项目时间（最近活动 `updatedAt`）单维、默认降序；后续把 TIME 标识升级为「维度（创建时间 / 最近活动时间）× 方向（降序 / 升序）」，URL 支持 `sort=createdAt:asc|desc` 与 `sort=updatedAt:asc|desc`（默认仍 `updatedAt:desc`），排序记忆（`homePrefs`）沿用；契约侧已补（A9 · Push 69）：白名单 `updatedAt` / `createdAt` / `seqNo`，默认仍 `updatedAt:desc`。

## 变更记录

| 日期 | 对应推送 | 变更摘要 |
|---|---|---|
| 2026-09-16 | Push 5（commit `4446f33`） | 初版：登录与会话、首页项目空间、搜索、新建项目、项目接口需求 |
| 2026-09-17 | Push 6（commit `ef4d3f6`） | 新增项目详情页「项目总览」：阶段标签、汇总卡、分组任务表、四格进度、详情抽屉、字段筛选、底部滑块；补任务数据模型与任务接口需求（Push 6 落地，随 Push 7 补记本文档） |
| 2026-09-17 | Push 7 | 建立维护约定：前端每次改动必须同步更新本文档 |
| 2026-09-17 | Push 20 | 首页标题区控件布局微调：`新建项目`按钮从搜索框上方移至搜索框左侧（宽屏同行、窄屏堆叠） |
| 2026-09-17 | Push 22 | 任务状态口径对齐：状态改为五态（待开始 / 进行中 / 已完成 / 提前完成 / 已延期）派生展示；「逾期」红色标注只落在实际完成日期（列 / 字段），不进入状态枚举；抽屉头部去掉与状态重复的「已逾期」胶囊 |
| 2026-09-18 | Push 29 | 首页新增「分类筛选」：开关（深灰/紫色轨道 + 滑块动画）控制左侧侧边栏；侧边栏含地区 / 项目经理多选筛选与计数、重置；桌面端推挤内容、网格容器查询自动降列，窄屏遮罩浮层；数据模型新增 `region`（新建项目默认「未分类」），搜索匹配新增地区；用 Tailwind 实现、无新增依赖；卡片左上角新增「项目类型」徽标（T-sort / 3D分拣 / 飞箱，底色与小车图标一致），数据模型与新建表单同步新增 `projectType`（默认 T-sort），搜索匹配新增项目类型；左侧筛选侧边栏新增「项目类型」维度（T-sort / 3D分拣 / 飞箱，与地区、项目经理可叠加）；分类筛选开关上移（距顶栏约 16px）并改为滚动吸附固定，下滑页面仍可点击；卡片配色改为按项目类型固定（T-sort 蓝 / 3D分拣 绿 / 飞箱 橙，红 / 紫注释保留备用），新建项目按类型取色；页面标题「项目空间」移至顶栏 logo 右侧（字号不变），内容区改为「分类筛选开关 + 计数 + 新建项目 + 搜索框」同一层级工具条；筛选侧边栏三个分组标题加重；筛选侧边栏新增「项目时间」维度（日期区间选择器，可与其他维度叠加、参与「已选 N 项」与重置）；列表改为按项目时间降序排列；工具条新增「降序 / 升序」切换按钮；顶栏层级提升为 `z-30`（高于内容区工具条 `z-10` 与筛选侧边栏 / 窄屏遮罩 `z-20`），账号菜单「退出登录」不再被遮挡；首页筛选侧边栏与任务详情抽屉改用新增的 `ScrollArea` 组件自绘悬浮滑块（隐藏原生滚动条；默认不可见，滚动时浮现、停止约 1 秒后淡出，可拖动）；账号菜单样式加强（双层柔和阴影 + 150ms 下移淡入动画，头像悬停浮起、菜单项圆角高亮） |
| 2026-09-18 | Push 30 | 契约口径回写：项目编号改为创建人填写（契约创建请求新增 `code`、新增错误码 `PROJECT_CODE_EXISTS`）；主题色 `accent` 定案为随 C9 字典元数据下发、前端不硬编码；Q9~Q11 补契约落点与待对齐项 |
| 2026-09-18 | Push 32 | 首页筛选态写入 URL query（技术设计 v0.2 §8.2「可分享、可收藏」）：地区 / 项目类型 / 项目经理 / 时间区间 + 关键字 + 排序全部随地址栏走，参数与 `GET /api/v1/projects` + facets 契约同口径（多值英文逗号分隔、排序 `字段:方向`）；默认值省略、`replaceState` 更新不刷历史；非法 / 过期取值兜底丢弃并归一化地址栏；带筛选的链接打开时侧边栏自动展开；顶栏 logo 与详情页「项目空间」返回保留筛选态（新增 Q12） |
| 2026-09-18 | Push 34 | 契约与口径对齐：项目级人员字段收敛——删 `projects.owner_id`、`manager_id` 改必填（项目经理）；首页筛选定为 `filter[managerId]`（多值逗号分隔）、facets 组名同步；任务级为任务负责人（`tasks.owner_id`）；Q9 定案、Q10 更新；技术设计 v0.2 / 系统功能书 / 字段对照清单 / 团队分工 / ADR-016 同步 |
| 2026-09-18 | Push 36 | 筛选维度 id 化：URL 参数 `filter[manager]`（姓名）→ `filter[managerId]`（UUID，与契约同名）；`Project` 模型 `title`→`code`、`manager`→`managerId` + 项目经理目录（`frontend/src/data/managers.ts`，id ↔ 姓名）；`Card` / `AppHeader` / `CategoryFilterSidebar` / 新建项目弹窗（项目经理改目录下拉）同步；搜索仍按姓名匹配；旧 `filter[manager]` 链接不再解析（不报错、不误筛） |
| 2026-09-18 | Push 37 | 项目序号口径落地：`index`（前端自增的两位展示序号）→ `seqNo`（契约 `projects.seq_no`，服务端分配、全库唯一、不可修改 / 不回收）；卡片右上角徽标改由 `seqNo` 两位补零展示并带悬停提示，顶栏面包屑与搜索同步；`shared` 契约新增 `Project.seqNo`（生成物同步）、`database/migrations/0003_projects_seq_no.sql` 新增序号列与序列；技术设计 v0.2.3、系统功能书 A1-12、字段对照清单同步 |
| 2026-09-18 | Push 39 | 卡片与详情页新增编辑入口：卡片左下角常驻幽灵态铅笔按钮（与编号 / 描述左对齐、与更新时间同行；默认无底色、悬停浮现）、详情页工具条「编辑项目」按钮，共用由 `NewProjectModal` 泛化出的 `ProjectModal`（mode=create/edit）；弹窗预填编号 / 描述 / 项目经理 / 项目类型，`id` / `seqNo` 不可改，保存后卡片与详情顶栏即时更新（含类型主题色与更新时间），仍为内存态；卡片外壳由 button 改为 `role=link` div（避免按钮嵌套，保留 Tab + Enter / 空格打开）；第三章补 `PATCH /api/v1/projects/{id}` 需求登记（契约 `ProjectUpdateBody`） |
| 2026-09-18 | Push 40 | 品牌黄配色统一：分类筛选开关开启态轨道由紫（`#8a5cd4`）改品牌黄 `#feca04`（开启态文字转深灰 `#313033`）；卡片飞箱类型徽标底色、小车图标与悬停光晕由橙（`amber-500`）改品牌黄 `#feca04`（徽标文字转深灰）；T-sort 蓝 / 3D分拣 绿不变 |
| 2026-09-18 | Push 45 | 首页记忆并恢复上次选择（A1-18 / Q12 定案）：无 URL 参数进入首页时按 localStorage 记忆恢复筛选（地区 / 项目类型 / 项目经理 / 时间区间 / 排序）与侧边栏开合；URL 优先、本地次之，恢复结果用 replaceState 写回地址栏；关键字 `q` 不写入记忆（临时操作）；冷启动（无记忆）收起且不写记忆；退出登录清除记忆（多人共用设备隔离）；记忆损坏 / 隐私模式静默降级为默认视图；新增 `frontend/src/homePrefs.ts`（存储键 `libiaolink.home.prefs.v1`） |
| 2026-09-18 | Push 46 | 会话切换（k6）：登录 / 回调 / me / 登出由后端承载（移除前端 Vite OIDC 中间件与 `ll_at` / `ll_idt` Cookie）；前端新增 `src/api.ts`（写请求 CSRF 头 `X-CSRF-Token`、401 跳 SSO 带 `returnTo`）；冒烟脚本覆盖 `ll_sid` / `ll_csrf` / returnTo 白名单 |
| 2026-09-18 | Push 47 | M0-05 前端联调清单收口：§三 按页面补全「接口 / 字段 / 错误码」——首页列表与 facets（五组 ↔ 侧栏四组映射）、新建 / 编辑 / 删除项目、项目详情与汇总、任务表 / 抽屉 / 四格进度（前端演示字段 ↔ 契约 `Task` 映射表）、文件 / 变更 / 蓝图（已入契约未接线）、统一错误码前端处理 20 条；新增 §3.8 对齐清单 A1~A8（供 wmj 逐条决议）；第六章 Q2 / Q4 / Q9 / Q10 / Q11 补提案引用 |
| 2026-09-18 | Push 49 | 时间口径调整（展示取创建、排序 / 筛选取最近活动）：卡片右下角 / 详情顶栏由「更新于 `updatedAt`」改为「创建于 `createdAt`」（编辑不刷新）；列表排序 / 搜索 / 「项目时间」区间筛选 / 侧栏最新日期 / URL `sort=updatedAt:asc\|desc` 按项目时间（最近活动 `updatedAt`）口径；侧栏「项目时间」标签加 ⓘ 口径说明；§3.8 A1~A8 状态改「已定案（PR #42）」；第六章 Q11 定案（`updatedAt` + Asia/Shanghai 日界）+ Q14 改写 + Q15 / A9 新增（排序维度后续项：按创建时间 / 最近活动时间）；移除详情页工具条「编辑项目」入口（编辑仅保留卡片铅笔按钮）；任务表新增「一键收起 / 全部展开」阶段折叠开关（表头「任务描述」列右侧）；详情页内容区上留白收窄（`pt-6` → `pt-3`，阶段标签栏整体上移） |
| 2026-09-18 | Push 52（lan） | 上传错误码命名对齐技术设计v0.3 §4.8（评审定案）：`UPLOAD_EXPIRED`→`UPLOAD_SESSION_EXPIRED`（HTTP 409→410）、`CHECKSUM_MISMATCH`→`FILE_HASH_MISMATCH`（422）；`UPLOAD_INCOMPLETE`（409）保留并补登 v0.3 §4.8 增补清单。§3.6 files 端点错误码与「统一错误码」表同步更新（本行为 lan 代记的契约同步，请 px 复核） |
| 2026-09-18 | Push 54 | 详情页顶栏面包屑补「序号」段（回写既有实现）：由「`项目空间 / 项目编号`」更正为「`项目空间 / 序号 NN / 项目编号`」——序号取 `seqNo` 两位补零、≥100 原样，如「序号 10」，与卡片徽标 / 搜索同口径（Push 37 已实现，属文档遗漏；本次代码零改动） |
| 2026-09-18 | Push 55 | Q8 定案（施工人数 / 紧急重要度 / 文件来源）：两项元数据任务创建 / 编辑均可填、非必填（权限暂缓）；文件走站内文件库（多类型、不通过第三方应用打开、站内预览，任务字段随行 `files` 摘要见 A7）；§3.8 新增 A10（任务创建 / 编辑写入接口，待 wmj 决议），`字段对照清单.md` 同步 |
| 2026-09-18 | Push 56 | 汇总卡信息收敛：一期暂不展示「逾期 N 条」（口径与契约 `overdue` 保留，后续汇总栏扩展再回填）；「当前阶段」阶段名加粗 |
| 2026-09-18 | Push 57 | 登录后新增「入口页」（`#/`：项目空间 / 任务模板 / 文件库 三个渐变按钮，Tailwind 复刻参考稿、未引入 styled-components）；路由分层：列表移到 `#/projects`、新增 `#/templates` / `#/files` 占位页；顶栏 Logo 改为「返回上一层」（详情 → 列表且保留筛选态、列表 / 占位页 → 入口页）；按钮悬停填充渐变 + 文字转白放大 |
| 2026-09-19 | Push 58 | 任务模板页第一版：① 九个阶段的板块标签栏（售前规划 / 设计开发 / 加工采购 / 组装发货 / 硬件实施 / 软件部署 / 试运行 / 生产阶段 / 验收），复用项目详情的阶段标签栏样式与 `PROJECT_STAGES` 口径（去掉「项目总览」），板块用 `TEMPLATE_SECTIONS` 常量集中增删；② 左侧「任务节点」区 —— 一期取「项目总览」任务数据（`PROJECT_TASKS`，九个板块合计 44 个节点），**随板块标签栏过滤**（选中板块只显示该板块节点，列表头显示「{板块} · N 个节点」），每节点一张中英对照卡片，卡片按参考稿用 Tailwind 复刻（330×80 / 旋转波浪 / 圆形图标 / 主副标题 / 叉形装饰且悬停叉号区域才显示，未引入 styled-components）；③ 布局改为左右两列**同一尺寸的毛玻璃面板**（半透明白 + 背景模糊 + 柔和投影，用于区分列，一列一类内容）：左列「任务节点」（370px 宽 / 圆角 16px / 半透白渐层 + `border-white/80` 白描边 + 顶部内高光 + `backdrop-blur-2xl backdrop-saturate-150` / 投影 `0 8px 32px rgba(15,23,42,.14)`，列头显示「{板块} · N 个节点」）、右侧 = 若干块同宽同材质的**模板面板**（每块一份草稿，**按阶段分开存**：每个阶段一套、初始都是一张空白的「{阶段}模板」，切标签就切到该阶段那一套）：**列头显示模板名称** —— 一期是本地草稿字段、默认「{阶段}模板」且可直接改名（如「英国订单」），名称右侧显示「N 个」与「保存 / 已保存」「删除」两个按钮（一期保存只写浏览器内存：保存后再改动会变回「保存」＝有未保存改动；删除即时生效，删光后面板区显示虚线空提示），名称下方一行固定高度提示行（平时不可见），列体一期为半透明白空容器、高度按内容、同一行等高；**板块进 URL（`?section=<板块>`，点标签 = replace 换地址，刷新 / 收藏停在原板块，非法值回落并纠正地址）**；**新建入口**在板块标签栏这一行的最右侧（「＋ 新建模板」）：点一下在「任务节点」右侧插入一块新的空白面板、原来的模板面板右移，新面板名称自动聚焦 + 全选，面板一行放不下就换到下一行（间距 32px、同一行等高，窄屏一列往下排），最左「任务节点」列固定不动（宽屏滚动时钉在顶栏下方），各面板名字 / 节点互不影响、阶段之间也不互相影响；面板背后垫一层纯装饰**淡色雾**（`sticky top-0 h-[70vh]` 跟随视口，黄 / 粉 / 青 / 靛四团径向渐变 + `blur-3xl`，`pointer-events-none`）让玻璃始终有可透的背景；④ 拖拽复制 + 去重：左侧卡片可拖到任意一块模板面板里（原生 HTML5 拖放，`dataTransfer` 只带任务 id，落点按 id 找回内容）—— 落点**追加一份副本**、左侧原节点不变；**同一节点在同一块面板里只留一份**（重复拖入不生效；不同模板面板可以各有同一个节点），拖到该面板已有的节点时整块面板转琥珀色提示态并在列头名称下方显示「该节点已在右侧，不会重复添加」、对应卡片描琥珀色；拖动进入某块面板时那块高亮、空态提示「把左侧「任务节点」拖到这里」，右列卡片悬停叉号可点击移除（`aria-label="移除 {任务名}"`），列头数量实时更新，切换板块不影响各面板已选；⑤ 面板内调顺序：模板面板里的卡片可拖动重排（落点在目标卡片上半 / 下半分别插到前面 / 后面，卡片之间空白处视为末尾；拖动中显示品牌黄插入指示线、被拖的卡片半透明留位；指示线绝对定位不占位 + 落点按卡片几何位置判定，修掉「拖到两张卡片之间时卡片来回频闪」），改的是这块模板里节点的顺序、数量与左侧不变（只在同一块面板内生效，拖到别的面板不处理）；模板保存 / 勾选等交互待业务定稿，接口一并待定 |
| 2026-09-19 | Push 59 | 项目详情的示例任务数据收口到**印度项目**：`data/tasks.ts` 新增 `DEMO_TASKS_PROJECT_ID`（`inmu-0010`）与 `tasksForProject()`，`ProjectDetail` 改为按项目取数；其余项目（含新建项目）打开只出页面骨架 —— 阶段标签导航 + 9 个阶段分组头（只有阶段名 —— 空阶段不显示 `已完成 N/M` 计数）、没有任务行，汇总卡「当前阶段 — / 整体进度 0%」（也不显示 `0/0 完成`），单阶段标签下只出该阶段分组头（`TaskBoard` 新增 `skeletonStages`）；印度项目的 44 条任务与各阶段完成度不变；**Push 61 起阶段骨架改为常显**（加出任务后其余阶段分组头不消失） |
| 2026-09-19 | Push 60 | 任务模板预设写死（业务口径，同批含 Push 59）：新增 `data/templatePresets.ts` —— `STAGE_TEMPLATE_PRESETS` 九个阶段各一套默认模板（名称 + 节点顺序），**硬件实施两套**（模板一 = 项目总览硬件实施 18 条长清单；模板二 = 格口 / 滑槽型 11 条，前 3 条复用 `t11~t13`、新增 `hx01~hx08`）、**软件部署一套 4 条**（`sw01` 通电测试、参数设定 / `sw02` RCS、WES 软件部署调试，随机跑 / `sw03` 按钮盒注册、传感器调节 / `sw04` 与 WMS 联调）、其余七个阶段各一套（顺序沿用项目总览对应阶段）；左列节点池改为「该阶段全部预设节点按出现顺序去重」（硬件实施 18 + 8 = 26 个节点），右侧面板初始按预设预填、状态「已保存」（九个阶段共 10 块面板，新建模板序号从 11 起）；左列「任务节点」加搜索框（中 / 英文名过滤、不区分大小写，有过滤时列表头显示 `N / M 个节点`，无结果显示「没有匹配的节点」，× 一键清空，切板块自动清空）；新增节点英文名按业务截图补全、待业务核对；软件部署第二套模板待业务提供；节点与模板仍为前端写死示例，正式来源与契约待后端 |
| 2026-09-19 | Push 61 | 项目总览从任务模板添加任务：**点任务表的阶段标签** → 右侧中等卡片（`fixed`、毛玻璃、右上 × / Esc / 点卡片外空白处 / 再点同一标签关闭、切顶部阶段标签自动关）内用**标签导航**切换 —— 第一个「任务节点」= 该阶段节点池（点一条加进项目），其余每个标签 = 该阶段一块模板（带序号预览、可鼠标滚动、支持「整套添加（N）」）；已在项目里的节点按 id 判重显示「已添加」；加进来的任务阶段 = 模板所属阶段、默认 待开始 / 进度 0 / 紧急重要度「中」、负责人与日期等留空；计入阶段分组与汇总卡（印度实测 16/44 → 16/52），空项目也能加出任务；当前原型未接后端、暂存浏览器内存（换项目 / 刷新即重置）；一期正式交付由后端落库（任务创建接口 = §3.8 A10，待 wmj 决议）；阶段骨架改为**常显**（加出任务后其余阶段分组头不消失）；任务表 / 任务详情的「项目经理」改为取项目卡片上的经理（`managerId` → 姓名），新增任务「紧急重要度」默认「中」；同批把「浏览器内存」类表述校准为「当前原型未接后端 / 一期正式交付由后端落库」；§3.8 新增 A11（模板接口与实例化对齐，待 wmj 决议） |
| 2026-09-19 | Push 62 | 契约补充（A10 / A11 入契约）：任务写入通道 = `POST /api/v1/projects/{id}/tasks`（创建，`headcount` / `priority` 可空，`taskNodeId` 判重）与 `PATCH …/tasks/{taskId}`（编辑，`version` 乐观锁；任务描述 / 成果文件按 A1-17 锁定）；「整套添加」= `POST …/tasks/from-template`（`skipExisting` 默认 true → `created` + `skipped`）；新增「任务节点库与任务模板」契约模块（`GET /api/v1/task-nodes`、`GET/POST/PATCH/DELETE /api/v1/task-templates…`，节点顺序 = `nodeIds` 数组、同模板按 id 去重）；`shared/` 生成物同步（paths 40 / schemas 95）；落库表建议 `task_nodes` / `task_templates` / `task_template_nodes`（server / database 实现待 wmj、lan 排期） |
| 2026-09-19 | Push 63 | 任务编辑弹窗（每行可编辑）：入口 = 任务行悬停铅笔（任务描述列右侧）+ 任务详情抽屉「编辑任务」；可编辑字段 = 项目经理（**项目级**，可搜索人员下拉，保存回写项目并刷新项目时间 `updatedAt`）/ 任务负责人（可搜索人员下拉，支持姓名 / 拼音 / 岗位搜索，留空 = 待分配）/ 开始日期 → 预计完成日期（**与分类筛选同一套日期区间选择器**）/ 预计所需天数（随日期区间联动、只读）/ 预计所需施工人数（≥0 非必填）/ 紧急重要度（普通下拉，新建默认「中」）/ 项目进展描述；只读区 = 任务状态 / 项目进度 / 实际完成日期 / 输出成果文件（任务描述与成果文件按 A1-17 锁定、状态与进度由系统派生）；保存后任务行 / 抽屉 / 汇总卡即时刷新、状态按新日期重新派生；新增人员目录 `frontend/src/data/members.ts`（**全部虚构姓名**：项目经理 20 人 + 交付成员 11 人），演示任务负责人同步改为虚构成员（`data/tasks.ts`）；接口对齐 `PATCH /api/v1/projects/{id}/tasks/{taskId}`（A10，Push 62 已入契约），当前原型未接后端、暂存浏览器内存 |
| 2026-09-19 | Push 64 | 任务行内编辑 + 弹窗显示修复：① **行内编辑**（点单元格直接改，入口见 §6.9）—— 项目经理（可搜索人员下拉，**项目级**：改后本项目所有任务行 + 项目卡片同步、刷新 `updatedAt`）/ 任务负责人（可搜索人员下拉，姓名 / 拼音 / 岗位，留空 = 待分配）/ 紧急重要度（普通下拉）/ 开始日期与预计完成日期（单日期小日历：上 / 下月、清除、今天；改任一日期同时写回两个日期 + **联动天数**，状态按新日期即时重派生）/ 预计所需施工人数（数字输入，留空 = 0）/ 项目进展描述（多行文本）；浮层 = `fixed` + portal 到 `body`（不被表格横向滚动裁切）、点外部 / `Esc` / 再点同一格关闭、点击**不冒泡到行**（不误开任务详情抽屉），与弹窗共用同一张覆盖表（任务行 / 抽屉 / 汇总卡即时同步）；② **任务编辑弹窗底部显示修复**：`max-h-88vh` → `max-h-92vh`、内容区上下留白与行距收紧、只读区改单行紧凑布局、提示文案缩短、进度描述文本框 3 行 → 2 行，**根因修复**：弹窗原先只有 `max-h-92vh`（高度 auto），Chrome 下 flex 子项不按封顶高度收缩 —— 滚动区拿到内容高度（545px）反而溢出到页脚下面被裁；改为 `h-fit` + `max-h-92vh` 让高度确定，`ScrollArea` 结构改为 `flex flex-col` + 内部 `min-h-0 flex-1`（原先靠 `h-full`，百分比在不定高父级下退化成内容高），滚动区因此严格等于「弹窗高 − 头部 − 页脚」，超出才滚动；`ScrollArea` 另加「有溢出时滑块常显」（原先滑块默认透明、看不出下方还有内容）；实测 1440×700（滚动区 460 / 内容 545，滑块常显、页脚不再压内容）/ 1440×800 / 1560×960 下弹窗与「保存修改」按钮均在视口内完整可见，任务详情抽屉、项目空间筛选侧栏滚动不受影响；③ 行内编辑与弹窗同接口：`PATCH /api/v1/projects/{id}/tasks/{taskId}`（只提交被改字段），项目经理回写走 `PATCH /api/v1/projects/{id}`；当前原型未接后端、暂存浏览器内存 |
| 2026-09-19 | Push 65 | 任务状态可编辑 + 与四格进度条双向联动：① **状态列**改为色标签（已延期红 / 进行中琥珀 / 已完成绿 / 待开始天蓝 / 提前完成紫）并支持行内下拉直接改（选项也是色标签，顺序同业务截图：已延期 / 进行中 / 已完成 / 待开始 / 提前完成）；② **联动口径**（`data/tasks.ts` 新增 `progressStep` / `statusOverrideAfterProgress` / `progressAfterStatus`，`PROGRESS_STEPS` 与 `Tracker` 共用一套换算）：点进度条 → 0 格 = 待开始、1~3 格 = 进行中、4 格全亮 = 已完成（填了预计完成日期且实际完成更早 → 提前完成；未填预计完成时间 → 只显示「已完成」）；选状态 → 待开始 = 进度条清零、进行中 = 至少亮 1 格（原四格全亮时退回 3 格）、已完成 / 提前完成 = 四格全亮、已延期 = 保持当前格数（0 格也能是已延期）；③ 新增任务字段 `statusOverride`（手动指定，优先级高于派生；四格全亮时由联动清空、交回完成态派生），`taskStatus()` 改为「手动 → 完成态 → 延期 → 进度格数」优先级，已填实际完成日期的任务把进度条退回 3 格后状态会跟随变「进行中」；④ `SelectOption.label` 放宽为 `ReactNode`（支持标签型选项）；任务状态已写入 `前端功能需求.md` §3.8 A12（接口需求：编辑接口是否放开 `status`、联动由前端还是服务端裁决，待 wmj 决议）；当前原型仍未接后端、存浏览器内存 |
| 2026-09-19 | Push 66 | 行内可编辑字段的「可点区域」外观调整（纯交互 / 外观）：原先单元格按钮用负外边距向外扩边（`-mx-1.5`），鼠标判定范围比看得见的文字大一圈且看不出来，用户不知道能点哪里 —— 改为**把字段元素放进一个不明显的「液态玻璃」小框**：默认 = 很淡的玻璃底（`bg-zinc-900/[0.045]`）+ `border-white/70` 发丝边 + 顶部内高光 + `backdrop-blur-[3px]`，悬停 = 转白 + 细描边 + 轻投影，展开中 = 白底 + `ring-1 ring-zinc-900/15` + 浮起投影；框本身 = 鼠标判定范围（去掉负外边距），内容仍按列宽 `truncate`；空值（「—」/「待分配」）一样带框提示可填；日期列的小框靠右对齐（`justify-self-end` 透传不变）；状态 / 紧急重要度的色标签就嵌在玻璃框里 |
| 2026-09-19 | Push 67 | 任务表可编辑性收口（列对齐 + 完成类字段联动 + 外观）：① **列对齐**改为「表头与列内容一律按列居中」—— 表头加 `text-center`、非任务描述列的单元格内容包一层 `flex items-center justify-center`（内容中心对齐列标题中心），去掉开始日期列原先 `text-right` / `justify-self-end` 的靠右口径；**任务描述列保持左对齐**（内含四格进度条与悬停铅笔）；② **实际完成日期可在表格行内直接选**（单日期小日历）—— 填日期 = 完成（四格全亮、按工期派生 已完成 / 提前完成），清除 = 退回进行中（进度写回 3 格、状态「进行中」）；③ **非完成态自动清空实际完成日期**（业务定案 2026-09-19）：把任务状态手动改成 待开始 / 进行中 / 已延期，或把四格进度条点回 1~3 格时，实际完成日期一并清空（`data/tasks.ts` 新增 `isCompleteStatus`），避免「状态进行中、却留着完成日期」；④ **行内可编辑单元格外观**：静止态由「很淡的玻璃底 + 白色发丝边」改为**白底 + 淡灰描边小胶囊**（`border-zinc-200/90 bg-white/75` + 顶部内高光 + 极轻投影，对齐业务样张），悬停 / 展开态描边与投影加深；⑤ **默认列调整**（`DEFAULT_VISIBLE_COLUMNS`）：按业务截图把默认列改成「任务描述 / 项目经理 / 任务负责人 / 任务状态 / 紧急重要度 / 是否按时交付 / 输出成果文件 / 文件 / 项目进展描述 / 开始日期 / 预计所需天数 / 预计完成日期 / 实际完成日期 / 变更关联」—— 只默认隐藏「预计所需施工人数」，「文件」「项目进展描述」由默认隐藏改为默认显示（定档口径 `系统功能书.md` A1-01 + 文档头修订同步）；⑥ **点进度条不再摘掉「已延期」**（`statusOverrideAfterProgress(progress, pastDue)`，新增 `isPastDue`）：已过预计完成日期的任务点 0~3 格都保持「已延期」，不再被改成「待开始 / 进行中」，点满四格才算完成；⑦ **逾期标注从「实际完成日期」列移到「是否按时交付」列**（新增 `lateDeliveryLabel`）：未完成超期 = 红色「逾期未交付」、完成但晚于预计完成日期 = 琥珀「逾期已交付」，实际完成日期列（表格与抽屉）只显示日期 /「—」；接口侧新增对齐项 A13（实际完成日期建议走 `/progress` 的 `actualEnd`）/ **A14**（是否按时交付的逾期派生，待 wmj 决议）；⑧ **「任务节点 + 模板」卡片落点调整**：原来浮在页面右上角（`fixed right-6 top-28`，会压住顶部阶段标签栏与汇总卡），现在**开在表格里** —— **顶部与所点阶段的分组头同一水平线**（卡顶 = 分组头顶；试过「跟随点击阶段」「按余量上下翻转」「固定开在表头下方」几版后业务定稿此版）、**左边缘对齐「项目经理」列**（`TaskBoard` 量表格算落点、`absolute` 定位，随表格滚动一起走；横向滚动表格时卡片跟着该列走、并夹在表格内不被裁掉；该列被列显隐关掉时回落到表格右边；卡高取 72vh 与分组头以下表格余量的较小值、保底 320px；分组头找不到时回落到表头下沿 +10px）；当前原型仍未接后端、存浏览器内存 |
| 2026-09-20 | Push 68 | 分类筛选侧栏换「液态玻璃」材质（纯外观，筛选逻辑与交互未变）：① **侧栏底板**由纯白实底改为玻璃面板 —— 半透明白渐变（`rgba(255,255,255,0.72) → rgba(255,255,255,0.5)`）+ `border-white/80` 白色发丝边 + 顶缘内高光 + `backdrop-blur-2xl backdrop-saturate-150` 背景虚化 + 柔和投影（`0 8px 32px rgba(15,23,42,0.14)`），与页面浮层（任务节点卡片 / 任务编辑弹窗）同一材质口径；② **「地区 / 项目类型 / 项目经理」三组多选胶囊**与**「项目时间」触发框**改用任务表行内可编辑单元格同一套玻璃小胶囊（`border-zinc-200/90 bg-white/75` + 顶部内高光 + 极轻投影 + `backdrop-blur-[3px]`，悬停转白 + 描边 / 投影加深），选中态补浮起投影；`frontend/src/components/CategoryFilterSidebar.tsx` 抽出 `GLASS_PANEL` / `GLASS_SURFACE` 两个材质常量；③ `frontend/src/components/DateRangePicker.tsx` 新增可选 `triggerClassName`（不传 = 原白底描边，任务编辑表单不受影响）；④ 侧栏头部 / 底部两道分隔线由 `zinc-100` 调为 `zinc-200/70`，保证在玻璃底上仍有层级 |
| 2026-09-20 | Push 69（wmj） | §3.8 A9 定案（PR #42 评审）：项目列表排序白名单补 `createdAt`（`sort=createdAt:asc\|desc`），默认仍 `updatedAt:desc`；§3.2 首页 `sort` 行与 Q15 契约侧同步收口（前端 TIME「维度 × 方向」升级仍为后续项） |
| 2026-09-20 | Push 70（wmj） | §3.8 A12 / A13 / A14 定案（任务写入口径，时序图 w1 卡）：`TaskUpdateBody` 开放基础三态 `status`（服务端同事务联动进度 / 完成日期，「已延期 / 提前完成」派生不可写且派生优先）；`progress<1` 清 `actual_end`（清除完成日期的唯一方式）、`progress=1` 缺省补当天、进度离散五档；`onTime` 与逾期标签服务端读时派生（前端删本地派生）。前端待改三处：下拉不再盖掉「已延期」、完成动作后显示服务端补的当天日期、逾期判定改用真实日期 |
| 2026-09-20 | Push 82（px） | 项目详情页顶部导航改版 + 两块钱查看板：① **顶部视图导航**由「项目总览 + 9 个施工阶段」改成 **3 个视图** —— **项目总览**（原阶段分组任务表，阶段分组与「点阶段标签开任务节点 / 模板卡片」全部保留）/ **人员任务分配**（按任务负责人的看板）/ **任务进展**（按任务状态的看板）；阶段标签不再各占一格，列显隐（`ColumnPicker`）与底部横向滑块只在「项目总览」出现；② **人员任务分配看板**（`frontend/src/components/TaskKanban.tsx`，`mode="owner"`）：列 = 任务负责人（头像 + 姓名 + 「N项」，按任务出现顺序、「待分配」垫底），卡片 = 标题（中 / 英）/ 预计完成日期 / 任务状态色标签 / 项目进度条与百分比 / 是否按时交付 / 「所属阶段」色签；③ **任务进展看板**（`mode="status"`）：列 = 任务状态，**固定顺序 已延期 → 进行中 → 已完成 → 提前完成 → 待开始**（空列也保留），卡片 = 标题 / 任务负责人（头像 + `姓名(拼音)`）/ 开始日期 / 项目进度 / 是否按时交付 / 「所属阶段」色签；两块钱看板的状态都取 `taskStatus` 派生（与任务表同一函数）、逾期标注口径同 Push 67；④ **卡片材质按业务样张代码还原**（外层壳 + `repeating-conic-gradient` 细纹叠加 + 内层板 + 三层投影：外壳 `p-9px / rounded-35px`、内层 `rounded-30px`）；配色**按业务反馈（同日）改成白色** —— 样张原色 `#e8e8e8` / `#e2e0e0` 渲染出来偏灰（业务原话「色彩太灰了 还是要白色」），改成白壳 + 近白内板 + 发丝边 + 柔和投影 + 底部内阴影，结构（外壳 / 细纹 / 内板 / 投影）保持样张不变；用 Tailwind 任意值实现、**不引入 styled-components**；⑤ 看板列底部 **「+ 添加」**直接建空任务（标题占位「新任务」，负责人 / 状态按所在列给、进度按 `progressAfterStatus` 联动、紧急重要度「中」），**阶段留空** → `TaskBoard` 新增 **「未分组」组**兜底（有内容才出现，可折叠 / 展开），保证看板建的任务在「项目总览」里也看得到；⑥ 卡片点开任务详情抽屉（抽屉里「编辑任务」进任务编辑弹窗，与表格共用同一张字段覆盖表）；⑦ 接口侧新增对齐项 **A15**（看板建任务的阶段口径：`stageKey` 放宽为可空 / 或看板加阶段选择，待 wmj 决议）；原型未接后端、暂存浏览器内存 |
| 2026-09-20 | Push 85 | 两块钱看板**列内滚动**（业务反馈「项目卡片不能一直延伸下去」）：① 列高随视口封顶（`h-[calc(100vh-15.5rem)]`，上限 52rem / 下限 22rem），**列头固定**（人员看板 = 头像 + 姓名 + 「N项」；进展看板 = 状态色签 + 「N项」），卡片列表在列内纵向滚动 —— 卡片再多也不再把整列 / 页面一直往下拉长，页面高度保持稳定（实测 1500×1000：页面无纵向滚动，列内 `scrollHeight` 8847 / `clientHeight` 716）；②「+ 添加」跟在最后一张卡片之后（列内滚到底可见）；③ 滚动条为细滚动条：`frontend/src/app.css` 新增 `.kanban-scroll`（WebKit 宽 8px、圆角灰拇指、悬停加深；Firefox `scrollbar-width: thin`）；④ 列宽 280px、卡片材质 / 字段（含「所属阶段」）/ 交互（点开抽屉、+ 添加、横向滚动）一律未动。 |
| 2026-09-20 | Push 86 | 两块钱看板的「添加」与滚动收口（业务反馈「添加按钮不是在最下方的 / 添加按钮是固定的 不随滚消失出现 / 滑动栏做成隐式的 / 添加有两种任务 临时任务 和 阶段任务」）：① **「添加」固定在列底**（`TaskKanban.tsx` 的 `COLUMN_FOOTER`：按钮移到滚动视口之外，卡片再长也不会把按钮顶走 / 滚没）；② **滚动条改隐式**：列内滚动改用仓库既有 `ScrollArea`（原生滚动条隐藏，滚动 / 悬停才浮出自绘滑块，与分类筛选侧栏 / 任务抽屉同一套），删除上一版自建的 `.kanban-scroll` 细滚动条（`app.css` 同步清理）；③ **两种添加**：点「添加」弹出菜单 —— **临时任务**（自己填内容：任务名称必填 + 英文名可空，创建后挂到该列，阶段留空 → 「未分组」）/ **阶段任务**（先选 9 个施工阶段之一 → 打开该阶段的「任务节点 + 模板」卡片（`StageAddCard` 复用，按节点 id 判重、支持「整套添加」）→ 选中节点加进项目，任务自带阶段并带上所在列的负责人 / 状态）；卡片贴该列右侧、与列顶齐平（`Esc` / 点外部关闭，可连点加多条）；④ 列宽 / 卡片材质 / 卡片字段（含「所属阶段」）/ 点开抽屉等一律未动。 |
| 2026-09-20 | Push 87 | 两块钱看板底部滚动条与留白收口（业务反馈「这个底部滚动条以及留白太丑了」）：① **看板横向滚动条改隐式** —— 列排布交给 `ScrollArea axis="horizontal"`（原生滚动条隐藏，Windows 下那条带左右箭头的灰横杠不再出现；滑块只在滚动 / 悬停时浮在列底留白里，与列内纵向滑块 / 分类筛选侧栏同一套）；② **列高改按铺满视口算**（`h-[calc(100vh-12.75rem)]`，上限 52rem / 下限 22rem）—— 列底与页面底之间不再留一大块空白（1500×1000 下由约 100px 收到 56px，且页面不产生纵向滚动）；③ `ScrollArea` 加 `axis` 参数（默认纵向，四处既有用法不变），横向用法在 `TaskKanban` 的看板容器。 |
| 2026-09-20 | Push 91 | 任务编辑入口收口（业务反馈「这个不需要了 要修改直接在抽屉里面修改即可」）：① **删除任务编辑弹窗**（`frontend/src/components/TaskEditModal.tsx` 整文件删除，保存值类型 `TaskEditSubmit` 随实现挪到 `TaskDrawer.tsx`）与抽屉头部「编辑任务」按钮；② **任务详情抽屉改为可直接编辑** —— 项目经理 / 任务负责人 / 紧急重要度（下拉，选完即存）、开始 → 预计完成日期（日期区间控件，确定即存；天数联动只读展示）、预计所需施工人数（数字，失焦即存）、项目进展描述（多行文本，失焦即存），值没变不写（避免无谓刷新项目时间）；存完抽屉右下角闪一下「已保存」（约 1.6 秒），页脚常驻「改动即时保存（原型暂存浏览器内存）」；③ **只读口径不变**：任务状态 / 项目进度 / 是否按时交付 / 实际完成日期（派生；状态与实际完成日期在任务表行内可改）、输出成果文件 / 文件 / 变更关联；④ **入口合并**：任务表整行点击与行内铅笔（`aria-label` = 「编辑任务」）都打开抽屉，两块钱看板卡片同理；⑤ 保存仍走同一张任务覆盖表（`handleSubmitTaskEdit`），任务行 / 抽屉 / 汇总卡即时同步，状态与逾期按新日期重新派生（如 5/20 → 5/10–5/15 后由「按时交付」变「逾期已交付」）。 |
| 2026-09-20 | Push 98 | 进度口径与卡片可编辑性（业务反馈「改成已完成 / 抽屉里面这个进度也要可以修改 长度不变加四个点 代表刚开工 完成一半 快完成了 已完成 / 同理卡片里面显示的也是中文表述 / 还有卡片里面也要可以修改实际完成日期」）：① **四格进度第 4 档档位名由「完成了」改为「已完成」**（`Tracker.tsx` 的 `TRACKER_LABELS`；任务表 / 任务详情抽屉 / 看板卡片三处一致）；② **任务详情抽屉的「项目进度」改为可改** —— 进度条长度不变，四颗点平均分布在进度条上（1/4 / 2/4 / 3/4 / 末尾 = 刚开工 / 完成一半 / 快完成了 / 已完成），点哪颗写哪档、悬停 / 聚焦即预览到该档，换算与任务表共用抽出的 `TrackerDots` + `trackerNextProgress`（`Tracker.tsx` / `TaskDrawer.tsx`，抽屉新增 `onProgress` 入参）；③ **两块钱看板卡片**：进度一栏由百分比改成中文档位，并新增「实际完成日期」一栏 —— 卡片上直接点选小日历改（填 = 完成、清 = 退回进行中，口径同 §6.9；`TaskKanban.tsx` 新增 `onPatchTask` / `onSetProgress`，`ProjectDetail.tsx` 接同一张覆盖表）；④ **抽屉里的任务改为按 id 取当前值**（`drawerTask`，`TaskBoard.tsx` / `TaskKanban.tsx`）—— 抽屉里点进度 / 卡片上改日期后，抽屉本身也立刻跟着变（原来存的是点击那一刻的任务快照，父级刷新后抽屉仍显示旧进度）；⑤ 新增附录口径条目 **A16**。 |
| 2026-09-20 | Push 101 | 抽屉可编辑性补齐 + 进度条统一绿色（业务反馈「抽屉里仍不能改任务状态 / 实际完成日期 这个也要可以修改 / 还有图片里面为什么是灰的 应该也要绿的」）：① **任务详情抽屉的「任务状态」改为可改**（五态色标签下拉，`SelectMenu`）—— 与任务表行内同一套联动：选完成态 = 四格全亮、进行中 = 至少一格、待开始 = 清零、已延期 = 保持格数，改成非完成态时实际完成日期一并清空（`progressAfterStatus` / `isCompleteStatus`，§6.4 / §6.9）；② **抽屉的「实际完成日期」改为可改**（复用任务表行内的 `InlineDateCell` 小日历：上 / 下月、清除、今天）—— 填 = 完成（四格全亮、按工期派生 已完成 / 提前完成）、清 = 退回进行中（进度 3 格），与任务表行内 / 看板卡片同一套口径（§6.9 / §6.10）；③ **抽屉的进度条与条上四颗点统一改成绿色**（与任务表四格点 `bg-emerald-500` 同一个绿）—— 原先按状态取色（已完成 / 提前完成 绿、进行中 蓝、其余 灰），业务反馈灰的看不懂；进度条只表达「做了多少」，状态色仍由状态签 / 色点表达；④ 抽屉新增 `onPatch` 入参（`TaskBoard.tsx` / `TaskKanban.tsx` 传 `onPatchTask`，与行内 / 卡片共用 `handlePatchTask` 同一张覆盖表）；⑤ 新增附录口径条目 **A17**；§6.5 / §6.8 的只读口径同步收窄为 是否按时交付 / 输出成果文件 / 文件 / 变更关联。 |
| 2026-09-20 | Push 102 | 看板卡片标题加粗加深（业务反馈「任务的标题加粗加深」，截图 = 人员任务分配 / 任务进展两块看板）：`frontend/src/components/TaskKanban.tsx` 卡片标题由 `font-medium text-zinc-800` 改为 **`font-bold text-zinc-900`**（字号 / 行高不变，仍是 `text-sm / leading-20`）—— 与卡片里浅灰字段名（`text-zinc-400` / `text-zinc-500`）和深灰取值（`text-zinc-800`）拉开层级，两块钱看板（人员任务分配 §6.10 / 任务进展 §6.11）同一次生效；实测计算样式 `font-weight: 700`、`color: oklch(0.21 0.006 285.885)`（= zinc-900），未动其它元素。 |
| 2026-09-20 | Push 103 | 任务表标题加粗加深（业务反馈「这边也同样需要」，截图 = 项目总览任务表的「售前规划 / 设计开发」阶段分组与任务描述列）：`frontend/src/components/TaskBoard.tsx` 任务描述列的中文标题由 `text-sm text-zinc-800` 改为 **`text-sm font-bold text-zinc-900`** —— 与 Push 102 的看板卡片标题同一层级（字号 / 行高 / 列宽 / 列对齐都不变，只改字重与颜色，英文副标题仍是 `text-[11px] text-zinc-400`）。 |
| 2026-09-20 | Push 104 | 看板卡片可拖动换列（业务反馈「卡片要支持拖动 移动别的组 也相当于编辑 移动后相关的信息要对应」，截图 = 人员任务分配 / 任务进展两块看板）：`frontend/src/components/TaskKanban.tsx`（本轮唯一改动文件）—— ① **`KanbanCard` 支持拖动**（`draggable` + `dataTransfer.setData("text/plain", task.id)` + `effectAllowed = "move"`；拖动中 `opacity-50` + `cursor-grabbing`；悬浮 `title` = 「按住拖到别的列 = 移到那个组（负责人 / 状态跟着改）；点一下看任务详情」）；② **`KanbanColumn` 作落点**（`onDragOver` 必须 `preventDefault` + `dropEffect = "move"` → 整列 `ring-2 ring-emerald-400/70` 高亮 + 列头下方虚线提示条「放开：移到「X」」；`onDragLeave` 用 `relatedTarget` 判真离开；`onDrop` 读 `text/plain` 里的任务 id）；③ 主组件新增 `draggingId` state 与 `moveTaskToGroup`：人员任务分配看板 = 写 `owner` / `ownerEn`（拖到「待分配」列 = 清空 = 待分配，其余列取人员目录 `memberByName(列名).handle`，兜底用列的 `ownerEn`）、任务进展看板 = 写 `statusOverride` + `progress`（`progressAfterStatus`）+ 非完成态清空 `doneDate`（`isCompleteStatus`，口径同 §6.4 / §6.9 / Push 67）—— 两条路都走任务表行内编辑同一个 `onPatchTask`（同一张覆盖表），抽屉与另外两块视图同步生效；同值直接 return（不写无谓数据）；④ 被拖卡片就在本列时**不高亮、不提示**（放回原列 = 没变化）；⑤ 只读场景（未传 `onPatchTask`）卡片不可拖。实测（无头 Chrome，1500×1000，项目 `inmu-0010`，`DataTransfer` + 合成 `DragEvent`）：拖「布局定档」到石昀列 → 彭砚 23项→22项、石昀 2项→3项、卡片出现在石昀列；拖「运输」到「进行中」列 → 已延期 5项→4项、进行中 0项→1项，被移卡片实测 = 任务负责人 苏珩(suheng) / 实际完成日期 — / 项目进度 完成一半 / 是否按时交付 逾期未交付 / 所属阶段 组装发货（进度档位与逾期标注同步跟着走）；切回人员任务分配结果保持（同一张覆盖表）。新增附录口径条目 **A18**（「拖动换列 = 编辑」是否算定档交互，请 wmj 复核）。 |
| 2026-09-20 | Push 105 | 拖动落点 = 列内插入位（业务反馈「要有合适的交互显示拖动后的位置」；「拖动要实体化 / 不要虚化」一条同日由业务口径修正为**不做成实体化**）：`frontend/src/components/TaskKanban.tsx` + `frontend/src/ProjectDetail.tsx` —— ① **拖动中的卡片样式保持原样**：先按「拖动要实体化 不要虚化」去掉了 `opacity-50`、试过抬起的实体态与自定义拖影（`setDragImage`），随后按业务口径「不做成实体化了」**全部撤回** —— 最终不淡出、不做抬起（无 `scale` / 描边）、不换拖影，`CARD_DRAGGING` 只把光标换成抓手；② **落点 = 列内插入位**：`KanbanColumn` 新增 `dropIndex`，`onDragOver` 按鼠标与卡片中线算插入位（`indexAt`：某张卡片中线以上 = 插到这张前面、都在上面 = 插到列尾），在卡片之间浮出一块绿色虚线槽位「放开：移到「X」」（同列 = 「放开：放到这里」），`onDrop` 带上 `index`；③ 列头下方原来的提示条去掉（槽位顶替）、整列描边高亮保留；④ **同一列内也能拖着排序**（原来本列不接受放下）—— 同列拖动只排顺序、不写负责人 / 状态；⑤ 顺序存看板顺序表（`ProjectDetail` 新增 `taskOrder` + `handleReorderTask`，与任务覆盖表同一层、换项目即重置）：落点用 `beforeTaskId` / `afterTaskId` 两个锚（落在列尾锚在这一列最后一张上，**目标列没有别的卡片时只换列、不动顺序** —— 免得卡片莫名跳到项目任务顺序末尾），任务表同阶段内顺序跟着走。实测（无头 Chrome，1500×1000，项目 `inmu-0010`）：拖动中卡片计算样式 = `opacity: 1`、`scale: none`（不做抬起）、`cursor: grabbing`（类里没有 `opacity-50`）；拖「布局定档」hover 石昀列第 2 格 → 槽位实测 `["技术协议定档","SLOT","规划设计"]`、文案「放开：移到「石昀」」，放开后 石昀 = 技术协议定档 | **布局定档** | 规划设计（彭砚 23项→22项）；同列把「布局定档」拖到列首 → 槽位 `["SLOT",…]`、放开后 石昀 = **布局定档** | 技术协议定档 | 规划设计；进展看板拖「运输」进空列「进行中」→ 已延期 5项→4项、进行中 0项→1项，卡片实测 = 苏珩(suheng) / 完成一半 / 逾期未交付 / 组装发货，且苏珩列顺序保持（项目启动 | 运输 | 清关 | 送仓 | 物料清单完整版 —— 空列不重排）。新增附录口径条目 **A19**（看板列内顺序的落库口径）。 |
| 2026-09-20 | Push 106 | 看板卡片去掉内层板（业务反馈「只保留外框 内框不要了」，截图 = 「人员任务分配」看板卡片）：`frontend/src/components/TaskKanban.tsx` —— 样张里的**内层板**（`CARD_INNER` = `rounded-[30px]` + 近白面板 `#fcfcfd` + `ring-1 ring-zinc-900/[0.04]` + `overflow-hidden`）整块撤掉，改成只带内边距的内容区 `CARD_BODY`（`px-4 py-3.5`），内容直接落在**外层壳**上；外壳口径（`p-[9px]` / `rounded-[35px]` / 白色壳 + 发丝边 + 三层投影）与壳上的细纹叠加（`CARD_NOISE`）保持不变，留白节奏也不变（9px + 16/14px 照旧）。实测（无头 Chrome，项目 `inmu-0010`）：卡片子节点 = `SPAN`（细纹，`bg: rgba(0,0,0,0)` / 无投影）+ `DIV`（内容，`bg: rgba(0,0,0,0)` / 无投影 / 圆角 0 / 内边距 16px·14px），外壳 = `rgb(255,255,255)` / 圆角 35px / 内边距 9px —— 卡片现在就是「一个框 + 内容」。 |
| 2026-09-20 | Push 107 | 拖动到边缘自动滚动（业务反馈「拖动时支持鼠标滑动」）：`frontend/src/components/ScrollArea.tsx` + `frontend/src/components/TaskKanban.tsx` —— ① `ScrollArea` 的滚动视口新增 `onDragOver`：鼠标进到**视口边缘 56px 内**就按「离边多深」算每帧滚动量（贴着边 18px/帧、退到边缘区外沿 2px/帧）并用 `requestAnimationFrame` 循环滚（横向看板滚 `scrollLeft`、列内卡片列表滚 `scrollTop`，两块看板 + 抽屉通用）；该方向滚不动的容器不算（内层容器不会白滚）；② **收尾即停**：拖出容器 `dragleave` 停、`onDrop` 停、`document` 上的 `dragend` / `drop` 兜底停（`dragend` 只派给起始元素、别的列收不到），放开后实测不再继续滚；③ `KanbanColumn` 的插入槽位改成**拖动落在本列期间按帧重算**（Chrome 鼠标不动时 `dragover` 约 350ms 才来一次，自动滚动时槽位会慢半拍），并新增「拖动结束清空本列 `over` / `dropIndex`」兜底（Esc 取消这类收尾下 `dragleave` 不一定派到每一列）。实测（无头 Chrome + CDP，1500×1000，项目 `inmu-0010`，`DataTransfer` + 合成 `DragEvent`）：拖「布局定档」贴看板右边 → `scrollLeft` 0 → 675（0.7s）→ 1320（1.4s，容器 max 1492），改贴左边 0.6s 回滚到 885、`dragend` 后 0.7s 仍为 885（不再滚）；列内贴列底 → `scrollTop` 0 → 975（1.05s）→ 夹在 max 1070，`dragend` 后停在 1070；**鼠标不动、列内在滚**时槽位实测从 `["通电测试","RCS部署及联动随机跑","SLOT","工作站软件部署",…]` 走到列尾 `[…,"WES软件部署及与WMS联调;设备运行测试","SLOT"]`；端到端：贴右边滚到 `scrollLeft` = max 1492，把卡片放进**原来在视口外**的「程屿」列，落列后 程屿 1项 → **硬件研发 \| 布局定档**。 |
| 2026-09-20 | Push 108 | 拖动改成指针拖动 + 滚轮自己控制（业务反馈「我不想要自动滚动，想要鼠标控制」）：`frontend/src/components/TaskKanban.tsx` —— ① **原生 HTML5 拖拽撤掉**（`draggable` / `dragstart` / `dragover` / `drop` / `dataTransfer` 全删）：原生拖拽期间浏览器不派发 `wheel`，滚轮到不了页面，「自己用鼠标滚」就不成立；② 改成**指针拖动**：卡片 `onPointerDown` 先记「可能拖动」，`pointermove` 位移超过 `DRAG_THRESHOLD = 4px` 才算真拖动（没过阈值 = 「点一下」→ 打开任务详情抽屉；`suppressClickRef` 把拖动收尾那一下的 `click` 吃掉），真拖动时把指针捕获在卡片上（鼠标滑出窗口再放开也收得到 `pointerup`），`window` 上收 `pointermove` / `pointerup` / `pointercancel` / `Esc`；③ **落点按几何算**：`document.elementFromPoint` → `closest('[data-kanban-column]')` 认列（列上新增 `data-kanban-column` 标记）、卡片中线认格（口径同 Push 105），拖动中按帧重算（滚轮在滚、鼠标不动时槽位也跟着走）；④ 放开把落点交给原来的 `moveTaskToGroup`（换列 = 改负责人 / 改状态，同列 = 只排顺序），Esc / 指针取消 = 原地取消。`frontend/src/components/ScrollArea.tsx`：Push 107 的拖动自动滚动（`onDragOver` + rAF + `DRAG_EDGE` / `DRAG_MAX_STEP` / `dragScrollStep`）整块撤回，只留 `data-scroll-area` 标记给自动化走查。实测（无头 Chrome + CDP，1500×1000，`inmu-0010`，**真实鼠标事件** `Input.dispatchMouseEvent` 而非合成事件）：按下「布局定档」只挪 3px → 不算拖动、抽屉不弹；再挪到 14px → 卡片进入拖动态（`cursor-grabbing`）；移到「苏珩」列 → 该列高亮 + 槽位「放开：移到「苏珩」」；**拖动中滚轮**连滚 3 下 → 该列 `scrollTop` 0 → 640（max 1498）、槽位在鼠标不动的情况下跟着重算（第 1 格 → 第 2 格）；**拖动中横向滚轮** → 看板 `scrollLeft` 0 → 780（max 1492，原来在视口外的「谢遥」列滚进视口）；放开 → 卡片落进「谢遥」列首格（谢遥 3项 → 4项、彭砚 23项 → 22项），松开后无残留高亮 / 槽位、抽屉未被误触；回归：点一下卡片仍正常打开抽屉（`aside[role=dialog]` = open）、Esc 关闭。 |
| 2026-09-20 | Push 109 | 拖动中补回「跟着鼠标走的拖动卡片」（业务反馈「卡片阴影也没了 这个不符合原本的效果」）：`frontend/src/components/TaskKanban.tsx` —— Push 108 把卡片拖动从原生 HTML5 拖拽换成指针拖动后，浏览器默认的半透明拖影（带卡片投影）一起没了。改法：拖动期间渲染一块 `data-drag-ghost` 的**拖动卡片** —— 内容直接用同一个 `KanbanCard`（外壳材质完全一致：白壳 + 发丝边 + 三层投影 + 细纹），宽度取被拖卡片的实测宽度，按**抓取偏移**对齐鼠标（按下时鼠标在卡片内的位置），位置在既有的每帧循环里用 DOM `transform` 更新（不走 state，免得每帧 re-render），`pointer-events-none` + `aria-hidden`（不参与 `elementFromPoint` 的落点判定、也不进无障碍树）；原地那张卡片照旧不动、不淡出、不抬起（业务口径不变）。实测（无头 Chrome + CDP，真实鼠标事件，1500×1000，`inmu-0010`）：拖动卡片实测 `bg = rgb(255,255,255)` / `radius = 35px` / 投影 = `rgba(15,23,42,0.18) 0 18px 40px -20px, rgba(15,23,42,0.06) 0 4px 14px -8px, rgba(15,23,42,0.05) 0 -2px 6px`（与卡片静止时完全一致）；鼠标移到 `(84,222)` → 拖动卡片实测 `left/top = 41/194`（抓取偏移 43/28 = 按下点 + 阈值内位移），移到苏珩列 → `left/top = 1009/420`（跟着鼠标走）；拖动卡片压着列时落点照样算得出来（槽位 `项目启动 / SLOT / 运输 / …`、列高亮 on）；拖动中滚轮仍可用（`scrollTop` 0 → 400）；放开 → 拖动卡片消失、抽屉未被误触，卡片落在槽位那一格（苏珩 = 项目启动 \| 运输 \| **布局定档** \| 清关 \| 送仓 \| 物料清单完整版）。 |
| 2026-09-20 | Push 110 | 拖动卡片改成半透明（业务反馈「还是要透明的吧」）：`frontend/src/components/TaskKanban.tsx` —— ① 新增 `CARD_SHELL_GHOST` 壳（`KanbanCard` 多一个 `ghost` 开关，只有跟着鼠标走的那一块走这套壳）：实心白 `bg-white` → **半透明白 `bg-white/[0.6]`**，并叠一层**轻磨砂 `backdrop-blur-[5px]` + `backdrop-saturate-150`**（与本应用浮层的液态玻璃同一套手感）；② 三层投影沿用卡片本体同一条（`0 18px 40px -20px rgba(15,23,42,0.18)` + `0 4px 14px -8px rgba(15,23,42,0.06)` + `inset 0 -2px 6px`），**不因透明而减淡**（业务前一轮提过「卡片阴影也没了」）；③ ghost 壳不带 `transition` / 悬停位移（拖动卡片由指针循环逐帧用 `transform` 摆位，`pointer-events-none` 也拿不到悬停）；④ 顺手修无障碍：拖动卡片 `tabIndex = -1`（原来是 0）—— `aria-hidden` 里不再有可聚焦元素。实测（无头 Chrome + CDP，真实鼠标事件，1500×1000，`inmu-0010`，「人员任务分配」看板）：拖动卡片计算样式 = `background-color: rgba(255,255,255,0.6)`（Chrome 归一化成 `oklab(… / 0.6)`）、`backdrop-filter: blur(5px) saturate(1.5)`、`border-radius: 35px`、`box-shadow` 与卡片本体逐字一致；跟随鼠标（鼠标 `(84,222)` → 拖动卡片 `left/top = 41/194`；移到「苏珩」列 → `1009/420`）；2 倍放大截图确认**下层卡片的绿色进度条 / 「已完成」色签 / 文字以模糊色块透出**，拖动卡片自己的标题 / 日期仍清晰；拖动中滚轮仍可用（`scrollTop` 0 → 400）；放开 → 拖动卡片消失、抽屉未被误触、卡片落进槽位那一格（苏珩 = 项目启动 \| 运输 \| **布局定档** \| 清关 \| 送仓 \| 物料清单完整版）。 |
| 2026-09-20 | Push 111 | 列内顺序改为「以项目总览为准」（阶段为主键）+ 添加阶段任务可指定插入位置（业务反馈「我想了一下不对 应该按照项目总览的顺序排 即某个工作人员在项目总览下的顺序 他的从上到下的任务排」；「人员要指定位置放入」）：`frontend/src/ProjectDetail.tsx` —— ① 新增 `stageRankOf`（阶段序号：售前规划 → … → 验收，未分组垫底，口径同项目总览的分组顺序）与 `stagedTasks`（在 `orderedTasks` 之后按「阶段序号 + 组内位次」稳定排序）—— 三块视图共用这一份顺序，人员任务分配看板列内因此与项目总览自上而下一致（项目总览本来就是按阶段分组渲染，组内顺序不受影响）；② 新增 `insertNewTasksIntoOrder(taskIds, placement)`：把新加的任务写进看板顺序表 —— `last`（默认）不动顺序表（新任务本来就排最后 = 落在它那个阶段段的末尾），`before` / `after` 以某张同阶段任务为锚插进去，一次多个按传入顺序**整段**插入；③ `handleKanbanAddNode` 改签名 —— `(context, stage, nodes[], placement)`，支持一次多个（整套添加）+ 按 id 判重。`frontend/src/components/TaskKanban.tsx` —— ① `KanbanColumn` 新增 `stagePlacement` 状态（打开阶段任务卡片时重置为 `last`）与 `stageTasksOf`（该阶段现有任务，顺序 = 项目总览里的先后）；② `StageAddCard` 透传 `placement` + `onAddNodes`。`frontend/src/components/StageAddCard.tsx` —— 新增导出的 `StagePlacement` 类型（`last` / `before` / `after`）、「**插入位置**」一行（`SelectMenu` 单选：该阶段最后（默认）/ 该阶段最前 / 在《某张任务》之后；整个阶段还没任务时只显示一句提示）、`onAddNodes` 批量入口；点下拉不算「点卡片外面」（新增 `data-select-popover` 标记，卡片不关）。`frontend/src/components/SelectMenu.tsx` —— 浮层加 `data-select-popover` 标记（给「点外面就关」的宿主用）。实测（无头 Chrome + CDP 真实鼠标事件，1500×1000，`inmu-0010`）：① 阶段为主键：人员任务分配看板苏珩列由「项目启动 \| 运输 \| 清关 \| 送仓 \| 物料清单完整版」变成「项目启动 \| **物料清单完整版** \| 运输 \| 清关 \| 送仓」（物料清单完整版 = 设计开发，排到组装发货那三张前面，与项目总览分组顺序一致）；② 插入位置下拉实测选项 = `该阶段最后（默认） / 该阶段最前 / 在《通电测试》之后 / 在《RCS部署及联动随机跑》之后 / 在《工作站软件部署》之后 / 在《WES软件部署及与WMS联调;设备运行测试》之后`；③ 在苏珩列「添加 → 阶段任务 → 软件部署」选「在《RCS部署及联动随机跑》之后」加「通电测试、参数设定」→ 项目总览·软件部署组变成 `通电测试 \| RCS部署及联动随机跑 \| **通电测试、参数设定** \| 工作站软件部署 \| WES…`；再选「该阶段最前」加「RCS、WES软件部署调试，随机跑」→ 该组首格变成 `**RCS、WES软件部署调试，随机跑** \| 通电测试 \| …`；④ 两张新卡实测 = 任务状态「待开始」/ 项目进度「未开始」/ 所属阶段「软件部署」，落在苏珩列尾（软件部署段）；⑤ 拖动回归：同阶段段内把「送仓」拖到「清关」之前 → 列内顺序变成 `… \| 运输 \| 送仓 \| 清关`（跟手）；跨阶段段拖到设计开发那一段上方 → 槽位浮在「物料清单完整版」前，放开后停在**组装发货段**内（阶段为主键归位），拖动中滚轮仍可用。 |
| 2026-09-20 | Push 112 | 「插入位置」下拉改成「固定两档 + 当前阶段顺序列表」（业务反馈「前两项可以 但是下面的太乱了 只要显示当前阶段的顺序即可 然后不要全部展示 要可以滑动 固定尺寸 鼠标放上去 点击将插入此任务之后」，截图 = 选项列表平铺一长串「在《…》之后」）：`frontend/src/components/StageAddCard.tsx` —— ① 新增 `PlacementPicker`（触发器 + `createPortal` 浮层，沿用 `usePopover` 定位）：浮层里前两档固定（`该阶段最后（默认）` / `该阶段最前`，带勾选态），中间一行小字说明「该阶段任务顺序 / 点一条 = 插到它后面」；② 下面是**该阶段任务顺序列表**（序号 + 标题，顺序 = 项目总览里该阶段任务的先后）—— **固定尺寸**（浮层 330×278、列表视口固定 176px，内容超出时用 `ScrollArea` 的隐式滚动条滑动，不再整屏越长越长）；③ **鼠标放上去**该行高亮并在右侧浮出「插到它后面」，**点一条 = 插到这张任务之后**（选中行显示勾选态）；④ 触发器上的文案仍显示当前选择（该阶段最后（默认）/ 该阶段最前 / 在《某张任务》之后）；⑤ Esc 分层：浮层开着时第一次 Esc 只关浮层、第二次才关阶段任务卡片；点浮层不算「点卡片外面」（`data-select-popover` 标记，卡片不关）。实测（无头 Chrome + CDP 真实鼠标事件，1500×1000，`inmu-0010`，苏珩列「添加 → 阶段任务 → 硬件实施」）：浮层实测 `330×278`、列表视口 `clientHeight = 176` / 内容 `scrollHeight = 508`（18 条）→ 可滑动，滚轮两下 `scrollTop` 0 → 332、滚回顶部回到 0；悬停第 2 行 → 该行高亮 + 右侧「插到它后面」实测 `opacity = 1`；点「施工安全培训」→ 触发器变成「在《施工安全培训》之后」、浮层关闭、卡片保持打开（`card-open`）；第一次 Esc → 浮层关、卡片仍开，第二次 Esc → 卡片关；另：该阶段一条任务都没有时仍只显示「该阶段还没有别的任务 —— 只能排在该阶段最后」。三张截图（浮层展开 / 悬停提示 / 滑动到第 13~18 条）。 |
| 2026-09-20 | Push 113 | 阶段任务「点添加 → 先选位置」（业务反馈「我要点击这个添加后选择位置」，截图 = 卡片里每一行右侧的「＋ 添加」）：`frontend/src/components/StageAddCard.tsx` —— ① 删掉卡片顶部那一行「插入位置」下拉（Push 111 / 112 的触发器形态），新增 **`PlacementPopover`**：**没有触发器**，由 `anchor`（点的那一行 / 「整套添加」按钮）决定浮出位置 —— 沿用 `usePopover` 定位（宽度 = 该行宽度、最小 320px；默认贴该行下面 6px，视口不够时翻到上面），仍然是 `createPortal` 挂 body；② 点「＋ 添加」/「整套添加」时把这一次要加的节点记进 `armed = { nodes, anchor }`，**只有选完位置（`onPick` → `commitAdd`）才真的加进项目**；点浮层外面 / 按 `Esc` 关掉浮层且**不插任务**（`armed` 清空，再点同一行能重新弹）；③ 浮层内容与前两档口径照旧：标题 = 这条节点名（整套添加时 = 「整套添加 N 条」）+「插到哪一格？」、`该阶段最后（默认）` / `该阶段最前` 两档固定、下面是该阶段任务顺序列表（序号 + 标题，固定 176px 视口 + 隐式滚动条 + 悬停「插到它后面」+ 点一条 = 插到它之后）；④ props 收敛成 `placement?: { tasks }`（不再有 `value` / `onChange`）+ `onAddNodes?: (stage, nodes, placement)`；⑤ 修一个自锁 bug：浮层挂载那一下 `open` 还是 false，原来的 `useEffect` 会立刻回调 `onClose()` 把 `armed` 清掉（浮层根本显示不出来）—— 改成 `openedRef`「真的开过之后再变 false」才算关掉。`frontend/src/components/TaskKanban.tsx` —— 删掉 `stagePlacement` 状态（打开卡片时不再重置）+ 删掉 `onAddNode` 那条单条兜底，`StageAddCard` 只传 `placement={{ tasks: stageTasksOf(templateStage) }}` + `onAddNodes`（位置由浮层选出来的那次调用带上来）。`frontend/src/components/TaskBoard.tsx` + `frontend/src/ProjectDetail.tsx` —— **项目总览的阶段标签卡片接上同一套**（业务口径「这个指定位置主要看项目总览的」）：TaskBoard 新增 `onAddNodes` 入参 + 按 `tasks`（已经是展示顺序）现算该阶段任务顺序表，ProjectDetail 新增 `handleAddNodes`（按预设节点自带的负责人 / 状态加进项目 + 走同一张 `insertNewTasksIntoOrder` 落位置），卡片渲染条件放宽成「`onAddNode` 或 `onAddNodes` 有一个就开」。实测（无头 Chrome + CDP 真实鼠标事件，1500×1000，`inmu-0010`，20/20 项通过）：① 人员任务分配 · 苏珩列「添加 → 阶段任务 → 硬件实施」，点「桌面平台搭建、魔毯铺设」那一行的「＋ 添加」→ 浮层贴该行弹出（行底 567 / 浮层顶 573 = 行底 + 6，左边缘与行对齐 1101，宽 347 = 行宽），标题 = 这条节点名，两档固定都是可点态，下面 18 条该阶段任务顺序（视口 176px / 内容 332px）；② 悬停第一条 → 「插到它后面」`opacity = 1`；滚轮两下 `scrollTop` 0 → 332；③ 第一次 `Esc` → 浮层关、卡片仍开、**没有插任务**（那一条还是「＋ 添加」）；④ 再点同一条 → 选「该阶段最后（默认）」→ 浮层关、卡片仍开、这条变「已添加」；重开浮层可见它排在该阶段**末尾**；⑤ 换一条点列表第 1 条 → 重开浮层可见它**插在「人员进场、场地检查、施工对接」之后**（前三条 = 人员进场… / 站人台… / 施工安全培训）；⑥ 项目总览点「硬件实施」阶段标签开同一张卡片 → 点「＋ 添加」也弹浮层，**浮层里的顺序与项目总览该阶段顺序逐条一致**（18 条全等）→ 选「该阶段最前」→ 项目总览该组首格变成「格口滑槽（或挂包架）安装」，阶段标签计数 1/18 → 1/19。截图：卡片 + 贴该行弹出的浮层 / 滑动后的列表 / 项目总览选位置前后。 |
| 2026-09-20 | Push 114 | 阶段任务卡片加「默认顺序添加」复选框（业务反馈「影响正常情况下添加任务了 所以默认是顺序添加 要改的话手动改」，并给了复选框的参考实现）：改动全在 `frontend/src/components/StageAddCard.tsx` —— ① 新增 `SequentialToggle`：照业务给的样例视觉做成 **SVG 方框 + 勾线**（勾上时 `stroke-dasharray` 由 `88 0 → 80 18` 让方框开一个缺口、勾线由 `0 30 → 30 30` 画出来，配 `transition-[stroke-dasharray]`；勾选态 `text-emerald-600`、未勾选 `text-zinc-400`）—— 沿用 Tailwind 任意值 + `peer` / `sr-only`，**不引 styled-components**（仓库约定：前端样式一律 Tailwind）；② 这枚复选框挂在提示行右侧（节点页在提示文字右边、模板页在「整套添加」按钮左边），文案固定「默认顺序添加」，`title` 里写明两种模式的区别；③ 新增 `sequential` 状态 **默认 `true`**（每次打开卡片都回到默认勾选）；`startAdd` 改口径：勾着（或卡片没配插入位置）→ `commitAdd(picked, { kind: "last" })` **直接按顺序加**（`last` 不动顺序表，按「阶段为主键」的展示顺序落在该阶段段的末尾），**取消勾选**才走 Push 113 的 `setArmed` → 弹位置浮层 → 选完才加；④ 提示行文案跟着模式走：勾着 = 「点一条节点就加进项目」，取消勾选 = 「点一条 → 先选插入位置」；⑤ `commitAdd` 提到 `startAdd` 之前（同一个渲染内不再有先后依赖），并把「默认顺序添加」的触发条件补进浮层与 `startAdd` 的注释。实测（无头 Chrome + CDP 真实鼠标事件，1500×1000，`inmu-0010`，脚本 `ll114_toggle.mjs`，**15/15 项通过**）：① 卡片里复选框默认 `checked = true`、文案 =「默认顺序添加」、勾选态实测 `stroke-dasharray` = `80 18` / `30 30`；② 勾着时点「＋ 添加」（硬件实施「桌面平台搭建、魔毯铺设」）→ **不弹浮层**、这条直接变「已添加」，重开浮层（先取消勾选）可见它排在该阶段**末尾**；③ 取消勾选 → 实测 `88 0` / `0 30`（方框回到闭合态）、提示行变成「点一条 → 先选插入位置 · 已添加 19」；④ 取消勾选后点「＋ 添加」（「站人台、扫描架、称及附属硬件安装」）→ 浮层照常贴该行弹出，选「该阶段最前」→ 该节点排到该阶段**第一位**（前三条 = 站人台… / 人员进场… / 施工安全培训）；⑤ 项目总览的阶段标签卡片里同样有这枚复选框、同样默认勾选。截图：勾选态卡片 / 取消勾选后的卡片 / 取消勾选后点「＋ 添加」弹出位置浮层。 |
| 2026-09-20 | Push 115 | 卡片提示行收口 + 勾改黑（业务反馈「这个勾改成黑色的 然后（鼠标滚动看全） 这个删除 点一条节点就加进项目 这个删除」）：改动全在 `frontend/src/components/StageAddCard.tsx` —— ① `SequentialToggle` 勾选态由 `text-emerald-600` 改成 **`text-zinc-900`（黑色，与「整套添加」按钮同一档）**，未勾选仍是 `text-zinc-400`；② 提示行文案收口：节点页由「点一条节点就加进项目 · 已添加 N」/「点一条 → 先选插入位置 · 已添加 N」（Push 114 加的模式提示）改成只留 **「已添加 N」**，模板页由「模板预览（鼠标滚动看全）· 已添加 N」改成 **「模板预览 · 已添加 N」**。实测（无头 Chrome + CDP 真实鼠标事件，1500×1000，`inmu-0010`，脚本 `ll115_copy.mjs`，**7/7 项通过**）：① 节点页提示行 =「已添加 18」，勾选态实测 `color = oklch(0.21 0.006 285.885)`（zinc-900，黑）；② 取消勾选 → 勾变 `oklch(0.705 0.015 286.067)`（zinc-400，灰）、提示行仍是「已添加 18」（不再写「先选插入位置」）；再勾回来又变黑；③ 勾着点「＋ 添加」仍然直接加（回归）；④ 模板页提示行 =「模板预览 · 已添加 18」；截图：节点页 / 模板页各一张。 |
| 2026-09-20 | Push 116 | 任务模板页拖动改指针拖动（业务反馈「这里拖动卡片也要可以滑动鼠标」，截图 = 任务模板页三列节点卡片）：原来模板页用的是原生 HTML5 拖放（`draggable` + `onDragStart/onDragOver/onDrop`），原生拖拽期间浏览器会把 `wheel` 吞掉 —— 「拖着卡片的同时滚轮翻列表」根本不成立。改动：① `frontend/src/components/TaskNodeCard.tsx` —— props 由 `draggable` / `onDragStart` / `onDragEnd` 换成 **`grab?: boolean`（只负责抓手光标）+ `onPointerDown?: (event) => void` + `ghost?: boolean`**（`ghost` = 拖动卡片外壳：半透明 `bg-white/[0.6]` + 轻磨砂 `backdrop-blur-[5px] backdrop-saturate-150` + 深投影），`<article>` 上只留 `onPointerDown`；② `frontend/src/PlaceholderPage.tsx` —— 照搬看板 Push 108 的指针拖动：新增 `DRAG_THRESHOLD = 4`、`DragInfo` / `PendingDrag` / `DropSpot` 三份类型，state 换成 `drag` / `dropTarget` + `pendingRef` / `pointerRef` / `grabRef` / `ghostRef` / `dropTargetAtRef` / `commitDropRef`（旧的 `draggingNodeId` / `dragSource` / `dragFromTemplateId` / `dragOverTemplateId` 全删）；`beginDrag` 按下只记「可能拖动」（`event.button !== 0` 与 `closest("button")` 直接 return，所以点卡片上的叉号「移除」不会起拖）→ `pointermove` 位移超过阈值才算真拖动（`setPointerCapture` 保证滑出窗口再放开也收得到 `pointerup`）→ `pointerup` 交给 `commitDrop`（左列 → 复制一份并按 id 去重；面板内 → 同面板调顺序，原位置在后面的下标减 1）；③ 拖动中 **`wheel` 照常可用**（整段拖动没有原生拖拽参与），且 `requestAnimationFrame` 每帧按 `pointerRef` 重算拖动卡片位置与落点（`elementFromPoint` 认 `[data-template-panel]` 面板 + 卡片中线认格）—— 鼠标不动、页面在滚，落点也跟着走到新的那一格；④ 拖动卡片挂在 `</main>` 之后（`data-drag-ghost="true"`、`pointer-events-none fixed left-0 top-0 z-50`、宽度 = 被拖卡片宽度），不挡落点判定；⑤ 面板 `<section>` 加 `data-template-panel={template.id}`、删掉原来的 `onDragOver/onDragLeave/onDrop`，`hovered` 改由 `dropTarget` 推导（面板高亮与琥珀色重复提示口径不变）。实测（无头 Chrome + CDP 真实鼠标事件，1500×1000，`#/templates?section=硬件实施`，脚本 `ll116_run.mjs`，**21/21 项通过**）：① 鼠标只挪 1px（合计 2px 没过 4px 阈值）不出现拖动卡片；拖起来出现拖动卡片（实测 `backgroundColor = oklab(0.999994 0.0000455677 0.0000200868 / 0.6)`、`backdropFilter = blur(5px) saturate(1.5)`）；鼠标 +100/+50 → `transform` 由 `translate(45px, 519px)` → `translate(145px, 569px)`（跟手）；② **拖动中滚轮**连滚 3 下 → `scrollY` 0 → 600 且拖动状态保持（原来原生拖拽会把滚轮吞掉）、反向滚回 600 → 0 同样保持；落点跟着滚动按帧重算（面板视口 top 148 → -452）；③ 悬停到模板二第 2 格 → 出现插入线（视口 y = 317），放开 → 「货架组装」插到第 2 格、11 → 12 张，拖动卡片消失；④ 拖已在面板里的节点 → 提示「该节点已在右侧，不会重复添加」且 12 → 12 张（去重）；⑤ 面板内把第 1 张拖到第 4 格 → 顺序由「人员进场… / 施工安全培训 / 物料转运…」变成「货架组装 / 施工安全培训 / 人员进场…」；⑥ `Esc` 一按拖动卡片立刻消失、什么都没插入（12 → 12）；⑦ 点一下不改任何东西（数量与顺序不变）。截图：拖动中（已滚到 600，半透明拖动卡片）/ 插入线 + 半透明拖动卡片 / 放开后的面板。 |
| 2026-09-21 | Push 117 | 「默认顺序添加」复选框对齐业务样例（业务反馈「你做的效果不对啊」，截图 = 勾选态 / 未勾选态）：改动全在 `frontend/src/components/StageAddCard.tsx` 的 `SequentialToggle` —— ① **补上样例里写死的两个 `stroke-dashoffset`**（方框 `[stroke-dashoffset:73]`、勾线 `[stroke-dashoffset:1]`）：方框周长约 75.4，虚线图案整体前移 73 个单位之后，勾上时 `80 18` 里那 18 个单位的缺口才会正好落在**右上角**（勾线尾巴穿出去的位置）；少了它缺口会被推到路径末尾（等于不画），方框看起来就是闭合的。勾线同理：未勾选时 `0 30` 里的 0 长虚线 + 圆头会在路径起点画出一个**小圆点**（业务截图里未勾选方框中的那个点就是它），`dashoffset 1` 把这个点推到路径起点之前 = 不画。② **去掉键盘焦点圈**（原 `peer-focus-visible:ring-2 peer-focus-visible:ring-[#feca04]/70`）：样例里没有它，而它盖在 16px 的勾选框上就是业务截图里那圈扎眼的黄框（实测该 ring 只在**键盘焦点态**出现、鼠标点击不会出现；去掉后两种态都不再有黄框，键盘仍可 Tab 聚焦 + 空格切换）。实测（无头 Chrome + CDP 真实鼠标事件，1500×1000，`inmu-0010`，脚本 `ll117_reg.mjs` / `ll117_verify.mjs`，**12/12 项通过**）：① 勾选态实测 `stroke-dasharray = 80px, 18px` / `30px, 30px`、`stroke-dashoffset = 73px` / `1px`、勾色 `oklch(0.21 …)`（zinc-900 黑）、`box-shadow: none`；② 真实鼠标点一下 → 未勾选 `88px, 0px` / `0px, 30px` + `dashoffset 73px` / `1px`、颜色掉到 `oklch(0.705 …)`（zinc-400 灰）、方框闭合**且没有多余的点**；③ 键盘焦点态（Tab + focus）实测 `box-shadow: none`（黄框没了）；④ 功能回归：勾着点「＋ 添加」仍然直接加（该行变「已添加」、不弹浮层）、取消勾选后点「＋ 添加」仍然弹位置浮层、再勾回来回到默认顺序添加。截图：8 倍放大（勾选态 / 未勾选态）+ 卡片整体。 |
| 2026-09-21 | Push 118 | 「添加」浮层同一时刻只开一个（业务反馈「这有bug吧 不能同时打开 点击别的应该关闭另一个吧」，截图 = 人员任务分配看板六列的「添加任务」菜单同时开着）：`frontend/src/components/TaskKanban.tsx`（本轮唯一改动文件）—— ① **根因**：「添加」浮层的打开态（`panel: "none" \| "menu" \| "temp" \| "stage"`）原来是 `KanbanColumn` 的**列内局部 state**，六列各存各的、互不感知；列内那套「点浮层外就关」的 `mousedown` 监听又用 `closest("[data-add-root]")` 判「点的是不是自己这列」—— 点别的列的「添加」按钮同样落在那列自己的 `data-add-root` 里，于是两边都不关 → 六个菜单能同时开着；② **改法**：打开态提到父组件 `TaskKanban`，整块看板共用**单值** `AddOverlay = { key: 哪一列, kind: menu \| temp \| stage \| template, stage }`，`KanbanColumn` 只渲染 `key` 等于本列（`group.key`）的那一份 —— 点别的列 = 直接覆盖这一份状态，上一列的浮层应声关掉；`kind` 里带上阶段任务的模板卡片（原来 `templateStage` 也是列内 state，同一条链路一起收口）；③ **关闭路径保持**：`Esc` / 点浮层外（列头、卡片、页面空白）/ 同一个「添加」按钮再点一次（开关）；模板卡片的 `onClose` 改成**只关 `template` 这一种**（`setOverlay((prev) => prev !== null && prev.kind === "template" && prev.key === 本列 ? null : prev)`），避免卡片关得比点击晚时误伤刚打开的菜单；④ 浮层关掉后清空临时任务表单里没提交的标题 / 英文名（下次打开是空表单，与原来 `closePanels` 口径一致）；⑤ 拖动 / 抽屉 / 卡片点按不受影响（点卡片仍先关浮层、再开任务详情抽屉）。实测（无头 Chrome + CDP 真实鼠标事件，1500×1000，`inmu-0010` 的「人员任务分配」看板 10 列，脚本 `ll118_addmenu.mjs`，**26/26 项通过**）：① 点列 A「添加」→ 1 个菜单；② **再点列 B「添加」→ A 的菜单关掉、只剩 B 这一个**（`aria-expanded` A = false / B = true）；③ 同一个按钮再点一次 → 0 个菜单；④ 对着业务截图场景**连着点 5 列**（彭砚 / 石昀 / 岑宁 / 苏珩 / 卢青）→ 每一步都只有 1 个菜单、不叠加（截图 `ll118_sweep.png`）；⑤ 菜单开着时点列头 / 按 `Esc` → 关掉；⑥「临时任务」表单开着时点别的列「添加」→ 表单关掉、只剩那一列的菜单；⑦「阶段任务」→ 1 个阶段选择浮层（9 个阶段）→ 选阶段后开出 1 张模板卡片，此时点别的列「添加」→ 卡片关掉、只剩那一列的菜单；⑧ **回归**：点卡片仍开任务详情抽屉（菜单先关）、`Esc` 关抽屉、按住卡片位移合计 2px 不算拖动 / 超过 4px 阈值出现跟随鼠标的拖动卡片 / `Esc` 取消拖动、临时任务表单填标题后点「创建」仍能建卡（彭砚列 23 → 24 张）。 |
| 2026-09-21 | Push 119 | **任务落库口径三条定案**（业务确认 1A / 2B / 3A；**纯文档，无代码改动**）：① **看板列内顺序要持久化**（A19 / A20）—— 拖动排序与「插入位置」选出来的位次要能留住（刷新 / 换设备 / 换人打开都保持），落库机制二选一（`tasks` 加 `sort_index` 列 or 批量排序接口）由 wmj 定；② **任务阶段允许为空**（A15）—— `tasks.stage_key` 由 not null 放宽为可空（或补一个「未分组」合法值），契约 `TaskCreateBody.stageKey` 由必填改可选，列底「添加 → 临时任务」保持不带阶段（卡片显示「未分组」、项目总览落最后一组）；③ **任务负责人允许为空**（A18）—— `tasks.owner_id` 由 not null 放宽为可空，契约 `Task.ownerId` 允许 null、`TaskUpdateBody.ownerId` 支持显式置空，人员任务分配看板的「待分配」列保留、拖进去 = 清空负责人。前端三条均已按定案实现（原型内存态），本轮只动文档：`前端功能需求.md`（A15 / A18 / A19 / A20 状态列 + §3.8 结论段）与 `字段对照清单.md`（新增 §七）。 |
| 2026-09-21 | Push 120 | **A16 / A17 定案 + 「拖动换列 = 编辑」登记为定档交互**（业务确认「你直接改即可 不需要 wmj 复核」；**纯文档，无代码改动**）：① **A16 定案** —— 四格进度第 4 档档位名「已完成」、任务详情抽屉内可改进度（进度条长度不变、四颗点平均分布）、看板卡片可改实际完成日期，三条前端均已实现且保留；后端**无契约缺口**（进度与完成日期都走 `PATCH .../tasks/{taskId}/progress`，A13 口径）。② **A17 定案** —— 抽屉内可改任务状态（走 `PATCH .../tasks/{taskId}` 的 `status` 基础三态，A12 · Push 70 已入契约）、可改实际完成日期（走 `/progress`）、抽屉进度条与四颗点统一绿色（纯前端样式）；后端同样无契约缺口。③ **「拖动换列 = 编辑」登记为定档交互** —— 上游 `系统功能书.md` 本就登记了 B2-01「卡片拖拽换人」与 B2-02「拖拽变更状态」，项目详情页的「人员任务分配 / 任务进展」两块看板沿用同一套手势（拖到别的列放开 = 改负责人 / 改状态），本次在 `系统功能书.md` 的 A1-04 三视图口径里点名，不再等 wmj 复核。 |
| 2026-09-21 | Push 128 | 项目详情页标签栏新增第四块视图「日报及问题」（业务口径「标签导航栏旁边新加『日报及问题』导航」）：`frontend/src/ProjectDetail.tsx` —— `VIEW_TABS` 由三视图扩为**四视图**（项目总览 / 人员任务分配 / 任务进展 / **日报及问题**），第四块走独立分支渲染 `ReportIssuePanel`（列显隐与底部横向滑块仍只在「项目总览」）；新增 `frontend/src/components/ReportIssuePanel.tsx`（摘要条 + 「日报」新 → 旧日报列表 + 「问题」四态看板 未分组 → 未解决 → 处理中 → 已完成，卡片材质与两块钱看板同一套外壳 / 细纹 / 投影）与 `frontend/src/data/reports.ts`（`DailyReport` / `Issue` 演示模型 + 6 篇日报 / 5 条问题，只挂在示例项目 `inmu-0010`，其余项目空态；口径承 `系统功能书.md` A3-01 / A3-02 / A3-09 / A3-10 与 ADR-026）。本视图内部随后又按业务口径改成**页内导航栏 + 四块子视图**（同一未推送批次）：① 顶部统计条换成**页内导航栏**（业务样张三轮：先 iOS 风毛玻璃胶囊、再玻璃胶囊按钮、最后**键帽按钮**，均用 Tailwind 任意值等价还原样张 styled-components 口径、不引入新依赖）—— **四个独立键帽按钮** = **日报填写 / 日报记录 / 问题追踪 / 问题看板**（16px 图标 + 单行 13px 文字，单个约 100×26px；**白面**（未选中与当前项同面 —— 业务口径「灰色的不太好看，我想不点击也是问题看板这个时候的颜色」，不再用样张的浅灰 `#f0f0f0`）+ 0.5em 圆角 + `text-shadow: 0 .0625em 0 #fff`；立体 = 白键帽的实心堆叠投影（内高光 `inset 0 .0625em 0 #fff` + 7 层侧壁 `0 .0625em #f2f2f2` → `0 .425em #cacaca` + 末层柔和落影 `0 .425em .5em #cecece`）；悬停 = 面微压暗 `#fafafa` + 字色转深；**按下 = `translate: 0 .225em` + 堆叠压扁**；当前项与未选项同面（白）、靠文字区分（当前项黑字 700 / 未选项灰字 600）；**紧贴顶部标签栏正下方一排、与标签栏左对齐**），默认停在「日报填写」；② **日报填写** = A3-01 字段表单（项目名称 / 提交人自动带入且只读、时间默认今天且改用**站内日期选择器**（`DateRangePicker` 新增单选模式 `mode="single"`，弹层与分类筛选 / 任务编辑同一套；此前是浏览器原生 `input[type=date]`，弹层样式与站内其它页面不一致）、关联任务多选 44 条、问题归类按 A3-04 条件必填），提交 / 暂存草稿都会写进「日报记录」，提交时「现场发现问题」按 A3-09 自动落一条「未分组」问题；③ **列宽**（业务口径「页面太宽大了，可以窄一点」→ 随后「我只要日报填写页面居中然后尺寸舒适一点、像一个表单，其它的不变还是全屏」）：整块视图**全宽、不封顶**，「日报记录」/「问题追踪」/「问题看板」照旧铺满；**只有「日报填写」**收成居中窄栏 `mx-auto w-full max-w-3xl`（768px）+ 首行字段固定两列（`md:grid-cols-2`）；④ **日报记录** = **列表 / 表格**（一行一篇、按日期倒序；业务口径「日报记录还是做成列表 不要卡片」—— 原卡片网格已撤，列 = 时间 / 填写者 / 今日施工人数 / 关联任务 / 当日完成工作 / 明日计划 / 现场发现问题 / 解决方案或建议 / 现场工作附图，表壳与「问题追踪」同一套）；⑤ **问题追踪** = 9 列表格（问题描述 / 归类 / 状态 / 提出人 / 提出日期 / 处理时限 / 责任 / 所属任务 / 解决方案 · 回复）；⑥ **问题看板** = 原四态看板；⑦ 表单草稿切子视图不丢、提交后复位，日报 / 问题存浏览器内存（换项目 / 刷新即重置）。实测（无头 Chrome + CDP 真实鼠标事件，1500×1000，脚本 `ll121_daily.mjs`，**20/20 项通过**（摘要条一条按「去掉全部空白」的口径比对 —— JSX 相邻行内元素之间本来就没有空白字符，视觉间距由 `ml-1.5` / `ml-1` / `ml-2` 给））：① 标签栏 4 个标签、顺序正确、默认仍是「项目总览」；② 点「日报及问题」→ 该标签高亮、列显隐（筛选按钮）与底部滑块**不出现**；③ 摘要条 = 日报 6 篇 · 最近填报 9月21日 / 问题 5 个 · 待处理 4（未分组 1 · 未解决 1 · 处理中 2 · 已完成 1）；④ 日报 6 行按日期倒序（9月21日 ×2 → 9月17日），状态签 = 已提交 / 草稿 / 已提交 / 补填 / 已提交 / 已提交，有问题的日报带「已生成问题记录 · 《当前态》」；⑤ 问题看板 4 列、计数 1 / 1 / 2 / 1，未分组那条「待分派」、处理中与已完成带「解决方案 / 回复」；⑥ 回归 —— 切回「项目总览」列显隐回来、「人员任务分配」10 列看板不受影响、再切回本视图内容仍在；⑦ 无演示数据的项目（`cnbj-0001`）四块子视图各自空态、表单仍可用。**页内导航栏与四块子视图另跑一轮 e2e**（无头 Chrome + CDP 真实鼠标 / 键盘事件 + React 受控控件赋值，1500×1000，脚本 `ll122_nav.mjs` 26/26 → 导航栏换材质 + 时间字段换站内选择器后同一套流程用 `ll123_nav.mjs` 重跑，**38/38 项通过**）：① 导航栏四项顺序与默认项正确、四个按钮 = 键帽（圆角 0.5em ≈ 6.5px、100×26px、高 ≤ 34px）、面 = 一律白 `rgb(255,255,255)`（未选中与当前项同面）、立体 = 实心堆叠投影（内高光 `rgb(255,255,255)` + 7 层侧壁 `rgb(202,202,202)` + 末层柔和落影 `rgb(206,206,206)`、0.425em ≈ 5.525px / blur 6.5px）、字 = 600 / 700 + 白色下高光 `text-shadow`、**导航栏紧贴顶部标签栏正下方一排**（间距 25px、与标签栏左对齐 24px）、悬停 = 面微压暗 `rgb(250,250,250)` + 字色转深、**按下 = `translate: 0px 2.925px`（0.225em）+ 侧壁压扁、松手回弹**；② **时间字段 = 站内日期选择器**（`[data-field=date]` 里没有 `input[type=date]`、只有一个按钮，值显示 `2026/09/21`）：点开弹层 = `fixed` + 264px 宽 + 白底 + 「2026年9月」+「一 二 三 四 五 六 日」+ 清除 / 确定（**与分类筛选 / 任务编辑同一套**）、单选模式只高亮一个格子（21 号）、点 15 号 → 确定 → 触发器变 `2026/09/15` 且弹层关闭、**提交后日报日期 = 9月15日**（证明值真写进表单）；② 表单字段齐全、项目名称 / 提交人自动带入、关联任务 44 条、未填「当日完成工作」时提交置灰并提示「还差：当日完成工作」；③ 填了「现场发现问题」但没选归类 → 提示「还差：问题归类」、选完归类才可提交；④ 点「提交日报」→ 自动切到「日报记录」+ 提示「已提交 9月21日 的日报。「现场发现问题」已自动生成问题记录（未分组）」、日报 6 → 7 篇且新篇在最上（提交人 = 登录用户张三、状态 = 已提交）；⑤「问题追踪」5 → 6 行、新问题落在首行（归类 / 未分组 / 张三 / 待分派 / 时限 9月23日）；⑥「问题看板」未分组列 1 → 2 项、其余列不变；⑦ 「暂存草稿」→ 日报 8 篇（状态 = 草稿）且**不**生成问题；⑧ 空项目（`cnbj-0001`）导航栏仍在、表单可用、日报记录与问题追踪为空态。**列宽另跑一轮专项**（同一套 CDP 脚本 `ll127_width.mjs`，**5/5 项通过**）：2300px 下「日报填写」= 居中窄栏 768px（`formW=768`、`formLeft=759`、`pageW=2285`）、面板整体全宽（`rootW=2237`）且页内导航栏贴顶部标签栏左沿（`navLeft=24 = tabLeft`）、问题看板 4 列铺满（`need=2252 / have=2252`，不出横向滚动）、「问题追踪」/「日报记录」内容宽 = 面板宽（`lastW = rootW = 2252`）、1500px 下本视图无横向滚动。接口需求以 **A21** 提案给 wmj（`GET /projects/{id}/reports`、`GET /projects/{id}/issues` + 写路径），`字段对照清单.md` 待契约落地后同步；`系统功能书.md` A1-04 同步登记「四视图」口径。 |
| 2026-09-22 | Push 134 | 新增「醒目模式」开关（`frontend/src/components/FocusModeToggle.tsx`，放在「项目总览」右上角「筛选」按钮左边；未勾选 = 白底灰描边方框（20×20），勾选 = 五个状态色各占 1/5 的色带，默认不启用）：打开后项目总览**每张任务卡片整行铺该任务状态的底色** —— 底色 = 图一五个状态色系压到 **6% 不透明度**（悬停 12%，马卡龙级极淡；`TaskBoard.STATUS_ROW_CLASS`），**状态列同步收口**：撤掉白底小框与色签底色、只留深色字（`STATUS_TAG_TEXT_CLASS`；`InlineEdit.tsx` 的 `InlineCell` / `InlineOptionCell` 新增 `bare` 裸框模式，不传 = 现状逐字不变）。关掉 = 行保持现状（白底行 + 行悬停灰），正常模式的状态列则按业务「这个颜色填满胶囊」收口：图一状态色**铺满整颗胶囊**（`TaskBoard.STATUS_CAPSULE_CLASS` —— 白底小框 + 内层色签合并成一颗，悬停加深一档），随后又按「紧急程度也要」把**紧急重要度列同口径收口**（`TaskBoard.PRIORITY_CAPSULE_CLASS`：高 `bg-rose-100` / 中 `bg-amber-100` / 低 `bg-zinc-100`，底色由原标签的 50 档抬到 100 档、字色沿用原标签），其它列的液态玻璃小框照旧。`ProjectDetail.tsx` 新增 `focusMode` 状态并透传 `TaskBoard`；状态口径仍取 `taskStatus`（与状态列 / 两块看板同一派生函数），改状态后整行颜色即时联动。实测（无头 Chrome 截图，1560 宽，临时预览页 + `inmu-0010`）：开 = 已完成行 `rgb(240,251,247)` / 已延期行 `rgb(255,242,245)`（6% 档），状态列只剩深色字、无白底无小框；正常模式（关）另测：状态胶囊 `bg-emerald-100`（57×29）与紧急重要度胶囊 `bg-rose-100` / `bg-amber-100`（各 35×29）都铺满整颗胶囊、无白底 / 无外层描边 / 无内层色签。详见 §6.13。 |
| 2026-09-22 | Push 136 | **一个项目可以有多个项目经理、一个任务可以有多个任务负责人**（业务口径「一个项目可以有多个项目经理和任务负责人」）：① **项目**：`Project.managerId`（单个）改为 `managerIds`（uuid 数组，至少一位、顺序 = 展示顺序）—— 新建 / 编辑项目弹窗改多选（`MemberMultiSelect`：已选以胶囊列出、可单个移除、点「添加 / 继续添加」开搜索列表接着勾）、卡片「项目经理：冯雪、李伟」（过长省略 + 悬停看全）、首页侧栏计数每位各计一次、筛选「任一位命中即命中」、搜索任一姓名命中即命中、项目详情 / 任务表「项目经理」列 / 任务详情抽屉（三处行内多选；**取消最后一位不生效**）；② **任务**：`owner` / `ownerEn`（单个字符串）改为 `owners` / `ownersEn`（两组同下标数组，可多位、空数组 = 待分配）—— 任务表「任务负责人」列与抽屉改多选（多位「、」连接）、人员任务分配看板按每位负责人各出一列（同一任务在多列出现，列计数 = 该人手上的任务数；拖动换列 = 负责人整体换成拖入列那一位、拖入「待分配」= 清空）、日报及问题面板关联任务多位展示；③ 新增共用组件 `MemberMultiSelect`（`MemberSelect.tsx`）与行内 `InlineMemberMultiCell`（`InlineEdit.tsx`），随之下线已无调用方的单选 `MemberSelect` / `InlineMemberCell`；④ 演示数据：印度项目（冯雪 + 李伟）、美国项目（王芳 + 陈晨）为双经理样例，任务 t09 包装（谢遥 + 石昀）、t10 运输（苏珩 + 彭砚）为双负责人样例；⑤ 接口 / 落库多位口径同批定案并落地（代 wmj 线，见 §3.8 A22 / A23、第六章 Q16）：契约 `Project.managerIds` / `Task.ownerIds`（+ `managerNames` / `ownerNames`）与迁移 `0017_multi_manager_and_owner.sql`（`manager_ids` / `owner_ids` uuid[]）；⑥ 数据层清理：`ProjectModal` 多选下拉改用 `PROJECT_MANAGERS`（`data/members.ts`）后，删除 `data/managers.ts` 里已无调用方的 `MANAGERS` 目录常量与 `types.ts` 的 `Manager` 类型（`managerName` / `managerNames` 保留）；⑦ **文档修复（同批）**：恢复 Push 113（提交 `f90d29b`）误删的「一、页面与交互（前端已实现）」§1–§6.10 共 203 行（含 H1 标题与导语），并按后续推送把该章重新对齐 —— §6.10 合并 Push 113 / 114 / 115 / 117（「默认顺序添加」复选框 + 位置浮层）与 Push 118（「添加」浮层单值）口径、§6.1 与章标题/导语改为**四视图**（Push 128 增「日报及问题」）、§6.5 / §6.8 / §6.9 / §6.10 补 Push 136 多项目经理 / 多任务负责人口径；章节恢复为 §6.1 → §6.13 数字顺序（原先 6.12 / 6.13 置于 6.11 之前）；本批还代 wmj 线落了契约与落库（`shared/` / `server/` / `database/`，见 A22 / A23 与迁移 0017），前端其余部分仍是原型内存态。 |
| 2026-09-22 | Push 138 | 首页分类筛选侧栏新增**「常用筛选」**一组（业务口径「不同的员工可能注意的项目不一样 所以需要分类 点击添加后选择多项 别的普通分类 然后保存 即出现在常用筛选 筛选的名字可以自定义」）：① **新增** frontend/src/savedFilters.ts（FilterCriteria / SavedFilter / 名称与组数上限 / 判定 `matchesCriteria` 与等价比较 `sameCriteria` / localStorage 读写，损坏丢弃）；② CategoryFilterSidebar.tsx 加入「常用筛选」为侧栏第一组 —— 标题右侧「+ 添加」、琥珀色编辑卡片（名称输入 + 已选 N 项 + 取消 / 保存）、编辑态下四组分类胶囊改为写草稿（可跨组多选、不影响当前列表）、空态与上限提示、底部栏「常用筛选编辑中：已勾选 N 项」；③ 保存后的胶囊 = 名称 + 命中计数，点名称应用、应用中深灰高亮（按条件等价），悬停 / 聚焦浮出铅笔与 ×（编辑改名 / 直接删除）；④ Home.tsx 接线：应用 = 写回既有 `filter[...]`（服务端无新参数）、保存即应用、保存前丢弃已不存在的取值、高亮由条件比较派生；⑤ homePrefs.ts 退出登录时随首页偏好一并清除；头部副标题改为「按常看组合、地区、项目类型、项目经理、项目时间筛选项目」。上限 20 组 / 名称 20 字；跨设备记忆待 wmj 定契约（§3.8 A24，同步技术设计 v0.2 §8.2）。 |
| 2026-09-22 | Push 139 | 「常用筛选」胶囊收窄（业务反馈「这个不美观 太大了吧」，截图 = 只有「亚洲 3」却右侧拖出一截空白的黑色胶囊）：① 根因 —— 胶囊内的「编辑 / 删除」动作区（铅笔 + ×）原来是**常驻但透明**（`opacity-0` 只隐去不占位，仍占 ~42px 宽度），短名组合因此显得又大又空；② 改法 —— 动作区改为**收起**（`max-w-0` + `opacity-0` + `overflow-hidden`），悬停 / 键盘聚焦（`group-hover/chip` / `group-focus-within/chip`）时 `transition-all` 200ms 展开到 44px；未悬停时胶囊与同排普通分类胶囊**等宽**（实测「亚洲 1」61px =「中国 1」61px，此前 103px；展开态 103px），行内其余胶囊顺滑右移、不换行；③ 顺带修掉深色（应用中）胶囊上铅笔 / × 悬停转 `text-zinc-900` 与底色同色看不见的问题（应用中改 `hover:text-white`）。纯样式改动，交互流程与数据口径不变。 |
| 2026-09-22 | Push 140 | **预览口径按 PR #103 评审定案对齐**（纯文档，无代码改动；wmj 建议 + lan 转达）：① §3.7 `PREVIEW_FAILED` 行「失败态 + 重试」→「失败态 + 降级『暂不支持在线预览，请下载查看』（D2-05）」—— 用户触发重试一期不做（契约无该接口，`not_ready` 由服务端幂等补投、前端只轮询至 ready / failed），如需保留另行登记后续卡片（显式写接口如 `POST /files/{id}/preview/regenerate`）；② §3.6 预览段收口 —— i3 契约（`GET /api/v1/files/{id}/preview` / `FilePreviewResponse` / `versionId`）已入 `shared/`（paths = 60），并补「`target` 收到未知值一律降级『请下载』」（枚举追加属兼容变更，M4-06 CAD 通道待 PoC）；③ §3.7 `PREVIEW_NOT_READY` 行「（i3 后接入）」→「（契约已入，待 u4 接线）」。 |
| 2026-09-22 | Push 141 | 任务表行悬停入口由「编辑任务」铅笔改为**「删除任务」按钮**（业务口径「不要这个笔作为编辑了 这个基本上不用 改成删除按钮吧」；形态参考业务给的代码，并按同日三轮反馈收定：「这个状态下有点太大了 要再小一点」「鼠标触碰列表时候黑的太突兀了 还是要比较看不出来吧」「当项目经理大于两位时 就是会触碰到」）：① **新增** `frontend/src/components/RowDeleteButton.tsx` —— 静止 24px 幽灵态圆（无底色、浅灰垃圾桶图标）、悬停 / 键盘聚焦展开成 48px 小号红胶囊（红底白字「删除」，11px）、图标放大下滑离场 + 「删除」同步浮出；② **槽位**：任务描述列右侧预留固定 48px + 右侧 16px 留白（第三轮反馈「当项目经理大于两位时 就是会触碰到」——原展开胶囊右缘与「项目经理」列齐平、gap = 0，加留白后 gap = 16px），展开只吃槽位，四格进度条与描述文字**零位移**（实测进度条 left 269 不变、按钮 left 321 不变、宽 24 → 48、高 24 不变；静止底色透明、展开底色 = red-500）；③ **接线**：`TaskBoard` 新增 `onDeleteTask`（点击 `stopPropagation` 不触发行选中；删除的是当前打开抽屉的任务时先关抽屉），`ProjectDetail` 用 `deletedTaskIds` 覆盖表移除（原型内存态，换项目 / 刷新复位）—— 三块视图同步消失、阶段完成度与整体进度重算（实测行数 44 → 43、整体进度 16/44 → 15/43、抽屉未误开）；④ 原「编辑任务」铅笔下线（编辑入口仍为整行点击 / 看板卡片 → 任务详情抽屉）；⑤ **列头固定**（业务反馈「这个标题栏要固定 鼠标移动可以依旧显示」）：表头移出横向滚动容器、`sticky top-16`（应用顶栏 64px 之下）+ 实底浅灰底，页面纵向滚动时始终可见；横向滚动时列头用 `translateX(-scrollLeft)` 同步平移，与各列保持对齐（实测滚动 1400px 后列头仍贴顶可见、横向 260px 后表头「项目经理」列中心与行内单元格中心同为 156.59px）。契约无删除通道，登记 §3.8 A25（软删 + `TASK_DELETED` / 404 口径由 wmj 定）；`系统功能书.md` 同步 A1-01 修订。 |
| 2026-09-22 | Push 145 | **新增「甘特图」视图**（业务口径「我想做甘特图」「在标签导航栏加一个甘特图页面」，形态参考 VTable 甘特图基本用法 https://visactor.io/vtable/demo/gantt/gantt-basic?version=1.26.6）：项目详情页顶部标签栏在「项目总览」之后新增第二个标签「**甘特图**」——**左侧固定任务列表**（行号 / 任务 / 开始日期 / 预计完成日期 / 进展；任务名前状态圆点与任务表同色）+ **右侧时间轴**（月份 + 逐日两级表头、周末底纹、每周一 / 每月一号分隔线、今天蓝底日期格 + 蓝色虚线）；按**施工阶段分组**（空阶段照出骨架行、非九阶段落「未分组」垫底、阶段行可折叠 + 工具条「全部折叠 / 展开」），阶段行画组内任务工期并集的**汇总条**（细条，完成度 = 已完成 / 组内任务数）；**任务条** = 开始 → 预计完成（含首尾，**青色 = 已完成进度、橙色 = 剩余工期**，条内标签 =「任务名 + 进度」）；**单日任务 = 里程碑绿色菱形**（一期无里程碑字段，按单日派生，登记 §3.8 A28）、**深色竖线 = 实际完成日期刻度**（源功能书 B3-01 前半）、**已延期 = 红色描边**（B3-05）、**未排期任务**左表 / 时间轴均标「未排期」且不画条；工具条另含**三档缩放**（每天 18 / 26 / 36px）与「回到今天」；时间轴范围 = 最早开始 − 2 天 → 最晚完成 + 2 天，打开停在最早那头。数据与「项目总览」**同一份**（含内存态新增 / 编辑 / 删除 / 改进度），**无新增接口、无新增字段**；新增 `frontend/src/components/GanttChart.tsx`（Tailwind + 绝对定位手绘，**不引入第三方图表库**）、`ProjectDetail.tsx`（`VIEW_TABS` 加「甘特图」+ 渲染分支）、`TaskBoard.tsx`（`STATUS_DOT_CLASS` 改为导出）。实测（无头 Chrome + CDP，1440×900，mock 鉴权）：五标签就位、53 行（9 阶段 + 44 任务）左右逐行对齐（表头 60px = 60px；首行 / 第 7 行 / 末行 top 完全一致）、44 条任务条 + 9 条阶段汇总条 + 12 个里程碑 + 52 个周末底纹、今天线在范围内（点「回到今天」后落在可视区左 1/3）、点「全部折叠」行数 53 → 9、（折叠后纵向不再滚动）、三档缩放与「全部展开」正常、条内进度填充与条色实测 `rgb(127, 209, 203)` / `rgb(247, 166, 60)`、普通条 `ring` = amber 40%、已延期条 `ring` = red 70%、控制台**零报错**；空态 ①（无任务项目）与 ②（有任务但都没排期）、以及看板「添加 → 临时任务」建出的**未分组 + 未排期**行均实测通过。**第二轮（业务口径「维持手绘尽量还原官方的功能」，仍不引第三方图表库）把官方那几个开关逐项补齐**：拖条 = 整体改期、拖条两端 = 改工期、拖条上圆点 = 改进度（四档吸附）、点条 = 选中（蓝描边）、悬停遮罩、条内「任务名 + 进度」标签（官方 `labelText`）、点左表任务行 = 定位到条（官方 locate taskbar）、拖左表右边线 = 移动分隔线（左表列宽固定、整行按自然宽渲染，右侧时间轴盖住超出的列 —— 业务口径「这个层级关系不一样 右侧看板是遮挡 不是压缩左侧」；可拖 188~572px、双击复位到全露 572px，官方 `frame.verticalSplitLineMoveable`）；拖动中给预览值 + 日期（进度）浮标，松手才写回、Esc 取消不写回；只写 `startDate` / `dueDate` / `days` / `progress` 这批既有字段（日期域夹在 2026 年内，**不新增字段**），进 `ProjectDetail` 内存态覆盖表（`taskEdits` / `progressOverrides`），`GanttChart` 新增两个可选 prop `onPatchTask` / `onSetProgress`。第二轮实测（同上环境）：拖条 +78px（3 天）→ 日期 +3 天且条右移 78px、拖动中浮标实测「5月20日 → 5月25日 · 共 6 天」；拖右端 +52px（2 天）→ 条宽 +52px；拖圆点 −¼ 条宽 → 100% → 75%（左表进展与条内标签同步）；里程碑拖动 5月20日 → 5月22日仍是单日；Esc 取消逐像素不变；拖左表右边线 +80px → 左表 403 → 483px；点左表任务行 → 时间轴 `scrollLeft` 0 → 3135、条从屏幕外（left 4045）滚进可视区（left 910）；控制台零报错（业务反馈「为什么是蓝色的字样」：拖动浮标本身是**深色底白字**，被浏览器**选区高亮**时变蓝底白字 —— 修复前实测 `#093DA7` ≈ 业务截图 `#093DA8`；浮标补 `select-none` + 拖动开始 `window.getSelection().removeAllRanges()` 清残留选区，修复后「拖动中 / 程序化套选区 / Ctrl+A 全选」三种情况实测均为 `#2F2F31`）。**第三轮（业务口径「这个 ui 有点突兀 不太相容啊 细节和美感处理的不好」「就是在末端 或者说两种颜色交接的地方 还有橙色的边框」「之前的颜色可以的 不用改啊 我只是让你改细节」）：配色整体回退到初版**（青 `#7fd1cb` / 橙 `#f7a63c` / 选中蓝描边 / 里程碑绿菱形 / 实际完成深色刻度全部不动），**只修细节** —— ① **条不再画描边**：去掉 `ring-1 ring-inset ring-amber-600/40`（它只压在橙色段上，青色进度是子层、把它盖住了，看着像给橙色块单独镶边），末端与两色交接处不再出现橙色边框，交接处是一条硬直缝（与参考样例一致）；② **已延期改为条头 3px 红内嵌色条**（`bg-red-500`，独立子层 `z-10` 压在进度层之上 —— 直接挂 `inset` 阴影会被青色进度盖住、实测看不见），B3-05「已延期变红」仍成立；③ 图例补「已延期」一项、实际完成刻度补 `title`（顺手修掉判空写成空格的问题）。第三轮实测（无头 Chrome + CDP，1600×1000，mock 鉴权，`http://localhost:3000/#/project/inmu-0010`）：`运输`（49% / 已延期）条 class 实测无 `ring`、无 `inset` 阴影，条内青 / 橙色值仍是 `rgb(127, 209, 203)` / `rgb(247, 166, 60)`；6× 放大截图确认两色交接处硬直缝、末端圆角无镶边、条头红条可见；点条选中实测 `selectedCount() = 1`（`ring-blue-600`）；演示数据里 4 条已延期（运输 / 清关 / 送仓 / 物料转运、清点分类）；控制台零报错。**第四轮（业务口径「我这个移动也要有这些信息 现在没做」）——左表补「开始日期 / 预计完成日期」两列**（对齐官方样例左表的 `start` / `end`，左表 = 行号 / 任务 / 开始日期 / 预计完成日期 / 进展）：任务行 = 该任务的开始 / 预计完成日期、阶段行 = 组内任务工期并集（与阶段汇总条同源）、没排期 / 空阶段 =「—」；**拖动条（改期）/ 拖两端（改工期）/ 拖圆点（改进度）时这两列跟着预览值实时更新**（与条、浮标同一套 `liveOf` 预览口径，单元格加一档强调），松手才写回、Esc 回位。列宽固定（行号 48 / 任务 284 / 开始日期 72 / 预计完成日期 96 / 进展 72 = 572），左表默认全露、分隔线可拖 188~572。第四轮实测（同上环境，无头 Chrome + CDP）：`规划设计`（左表实测 5月15日 / 5月20日）拖条 +78px（= 3 天）时该行实测 `5月18日 / 5月23日`、条与浮标同步「5月18日 → 5月23日 · 共 6 天」，Esc 后实测回位 `5月15日 / 5月20日`；阶段行实测并集 `5月15日 / 5月20日`（不跟组内任务预览走 —— 与阶段汇总条同口径）；左表 572px 下左表 / 时间轴仍逐行对齐、控制台零报错。**第五轮（业务口径「这个层级关系不一样 右侧看板是遮挡 不是压缩左侧」）—— 分隔线由「调列宽」改为「移动遮挡边界」**：左表五列列宽固定（48 / 284 / 72 / 96 / 72 = 572），整行按自然宽渲染、`clip-path: inset(0)` 裁到可见宽度，拖分隔线只改「露多少」（188~572px、双击复位到全露），拖窄了列是被右侧时间轴盖住 / 裁掉、不是被挤窄（对齐官方样例：列被裁掉一截，不是变窄）。第五轮实测（同上环境）：默认可见 572px、五列实测 `[48, 284, 72, 96, 72]`；分隔线左拖 240px 后可见 332px、列宽实测**仍是** `[48, 284, 72, 96, 72]`（开始日期 / 预计完成日期 / 进展 三列整体被时间轴盖住）；在分隔线上双击实测回位 572px；第四轮那套「拖动时日期列跟着预览值走」不受影响；控制台零报错。**第六轮收尾**：分隔线把手与高亮线移出裁切层（裁切层只包表头 + 数据行）—— 修前 `clip-path` 连把手一起裁，外侧 8px 抓手点不到、悬停高亮也看不见；高亮线改压在分隔线本身上（2px 蓝）；分隔线拖动补 window 级 `pointermove` / `pointerup` 兜底（无头调试下指针捕获实测不生效：补兜底前拖 260px 实测 0px，补后同手法实测 572 → 312px）。收尾实测（同上环境）：默认可见 572px、五列实测 `[48, 284, 72, 96, 72]`；把手命中实测 `elementFromPoint(边界 −6px / +6px)` 两侧都命中（修前 +6px 侧是死区）；悬停高亮实测 2px 蓝线、左端正好压在边界（实测 597 = 分隔线中心）；从把手外侧 8px 起拖 −260px → 可见 572 → 312px、五列列宽实测**不变**；双击实测回位 572px；横向滚 360px 后左表区域 `elementFromPoint` 命中仍是左表（时间轴从底下滚过、被盖住）；控制台零报错。**第七轮（业务口径「先做这两个」）补齐 B3-06 余下的两件**：① **时间轴粒度日 / 周 / 月**（官方 `timelineHeader.scales`）—— 表头两排随粒度换（月粒度上排 = 年份、下排 = 「5月」；周粒度下排 = 逐周「M/D」、竖线改周线并去掉周末底纹），列宽 = 一列跨几天 × 每天宽度（标准档 日 26px / 周 91px / 月约 138px），几何基准仍是「每天多少 px」，所以条 / 里程碑 / 实际完成刻度 / 今天线一律按天精确落点；② **甘特内筛选** —— 负责人（含「待分配」）/ 只看已延期 / 只看未排期 + 命中计数 + 清空筛选，筛的是任务集合（时间轴范围与阶段汇总跟着走、空阶段不再占行），筛空 / 筛得都没排期各有空态并带「清空筛选」。实测：日 → 周 → 月切换的表头分段 / 列宽 / 条落点逐项核对（周粒度下条正好落在对应周列内），负责人「彭砚」44 → 24 行、时间轴 4732 → 4602px，只看已延期 5 行 / 4 条，另在看板造了一条未排期任务验证「只看未排期」→ 命中 1 条 + 空态③ + 清空筛选回位；控制台零报错。无新增接口与字段。**第八轮（业务口径「下拉框组件设计 保持一致 搜索下拉框」）把负责人筛选的原生 `select` 换成统一口径的搜索下拉**实测（同上环境）：原生 `select` 个数 0；触发器「全部」88×26px；浮层 280px / 圆角 12px / 白底 / 阴影含 `0.18`；搜索框 placeholder「搜索成员」自动聚焦；11 项（全部 + 10 位负责人）、只有「全部」带绿勾；真实键盘输入「石」→ 1 项、Enter 选中 → 浮层关、行 44 → 3、时间轴 4732 → 1430px、计数「筛选后 3 / 共 44 条任务」；重开 → 「石昀」带勾、「全部」不带；鼠标点「卢青」→ 触发器「卢青」、计数「筛选后 1 / 共 44 条任务」；输入「zzz」→ 空态「没有匹配的成员」；清空搜索点「全部」→ 行 44、时间轴 4732px、计数文案消失；共用列表回归（项目总览任务表行内「修改任务负责人」）= 31 项 / 1 项已选 / 页脚「共 31 人 · 按姓名 / 拼音搜索 · 当前为虚构演示成员」；控制台零报错。**第九轮（业务口径「改成同款液体玻璃」）工具条改材质**实测（同上环境）：负责人触发器 / 「全部折叠」/「回到今天」/「清空筛选」的实算样式 = `background-color: rgba(255,255,255,0.75)` + `backdrop-filter: blur(3px)` + 圆角 8px + 描边 `zinc-200/90`；三段控件轨道宽实测 106 / 142 / 214px（粒度 / 缩放 / 行筛选），激活段仍是纯白胶囊；切「只看已延期」后筛选行整体同材质、计数「筛选后 5 / 共 44 条任务」；控制台零报错。**第十轮（业务口径「为什么筛选后不能选择回到今天」）——「回到今天」不再因今天落在筛选后的范围外而置灰**实测（同上环境）：未筛选时按钮可用（`disabled=false`）、点一下 scrollLeft 0 → 3126（今天列落在可视区左 1/3）、时间轴仍 4732px；筛选「卢青」后时间轴收到 260px、今天线不画（今天在范围外）但**按钮仍可用**且提示换成「今天不在当前时间轴范围内（范围跟着筛完剩下的任务走）：点一下把时间轴延到今天并居中」；点一下 → 时间轴 260 → 3510px、今天虚线出现（面板内偏移 3445px、落在可视区）且 scrollLeft = 2554（= 今天列 − 可视宽度 1/3）；再点一次时间轴不再变宽、scrollLeft 不动；「清空筛选」后范围收回 4732px、今天线仍在；控制台零报错。**同轮收口**：按业务口径「这个不要展示」删掉工具条里两处说明文案（粒度 / 筛选那行功能说明、筛选行尾「（筛掉的阶段不再占行；时间轴范围跟着筛完剩下的任务走）」），只留末行「条上操作」提示；实测不筛选时筛选行只剩「筛选 / 负责人 / 全部 · 只看已延期 · 只看未排期」四件控件、末行只剩一条提示。 **同轮追加**（同一 PR 内收口）：**甘特图负责人筛选改用仓库统一口径的搜索下拉**（业务口径「下拉框组件设计 保持一致 搜索下拉框」+ 两张截图：现状 = 原生 `select` 的系统蓝高亮、参考 = 人员搜索下拉）：① 新增通用单选搜索下拉 `SearchSelect`（`frontend/src/components/MemberSelect.tsx`）—— 触发器（白底描边胶囊 + 右侧箭头，显示当前值）+ `usePopover` 浮层（`createPortal`、280px、`rounded-xl` + `shadow-[0_16px_40px_rgba(15,23,42,0.18)]`）+ 搜索列表，选中后收起；② 把「搜索框 + 选项行 + 空态 + 页脚」抽成内部 `SearchList`，供 `MemberSearchList`（人员下拉 / 任务表行内成员单元格）与新组件**共用同一份版式与搜索口径**（姓名 / 拼音 / 角色，大小写不敏感，无匹配 = 「没有匹配的成员」）；选项模型 `SearchSelectItem` 支持成员行（头像 + 姓名 + 拼音 + 角色 + 选中绿勾）与普通行（「全部」「待分配」，首字圆底 + 右侧说明）混排，`value` / `onChange` 走调用方自己的字符串空间；③ 甘特图侧：负责人选项 = 全部 → 各负责人（按任务出现顺序；能对上 `data/members.ts` 的按成员行渲染，目录外姓名退化成普通行、不丢筛选项）→ 待分配（有「没负责人」的任务才出），选中值仍是任务里的姓名，`visibleTasks` 过滤口径与「清空筛选」不动；④ **实测**（无头 Chrome + CDP，`http://localhost:3000/#/project/inmu-0010`，数据 = 演示 44 条）：原生 `select` 个数 = 0；触发器「全部」88×26px；浮层 280px / 圆角 12px / 白底 / 阴影含 `0.18` = true；搜索框 placeholder「搜索成员」自动聚焦 = true；页脚「共 10 位负责人 · 按姓名 / 拼音搜索 · 演示数据」；11 项（全部 + 10 位负责人）、选中行只有「全部」带绿勾；真实键盘输入「石」→ 1 项（石昀）→ Enter 选中 → 浮层关闭、行 44 → 3、时间轴 4732 → 1430px、计数「筛选后 3 / 共 44 条任务」；重开 → 「石昀」行带勾、「全部」行不带勾；鼠标点「卢青」→ 触发器「卢青」、计数「筛选后 1 / 共 44 条任务」；输入「zzz」→ 空态「没有匹配的成员」；清空搜索后点「全部」→ 行 44、时间轴 4732px、计数文案消失；**共用列表回归**：项目总览任务表行内「修改任务负责人」单元格浮层 31 项、1 项已选、搜索框在、页脚「共 31 人 · 按姓名 / 拼音搜索 · 当前为虚构演示成员」；控制台零报错；⑤ 无新增接口、无新增字段（负责人筛选项来自任务数据，头像 / 拼音 / 角色取自 `frontend/src/data/members.ts`）；⑥ **同轮追加**（业务口径「改成同款液体玻璃」）：甘特图工具条控件材质统一到仓库既有「液态玻璃」—— 负责人搜索下拉触发器 / 全部折叠 / 回到今天 / 清空筛选 = 整套胶囊材质（`border-zinc-200/90` + `bg-white/75` + 顶部内高光 + 极轻投影 + `backdrop-blur-[3px]` + hover 转白），粒度 / 缩放 / 行筛选三段控件的轨道用同一材质（不带 hover 反馈，悬停反馈留给分段按钮），只换材质、圆角 / 内边距 / 字号与配色口径一律不动；实测实算样式 `rgba(255,255,255,0.75)` + `blur(3px)`、三段轨道 106 / 142 / 214px、筛选行同材质，控制台零报错；⑦ **同轮修**（业务口径「为什么筛选后不能选择回到今天」）：「回到今天」原来 `disabled={todayIndex === null}` —— 筛掉今天附近的任务后时间轴范围跟着剩下的任务走，今天落到范围外就被置灰。改为：今天在范围内 = 直接居中；不在范围内 = 先把今天并进时间轴范围（`includeToday`，多一个 `buildModel` 入参）、画出今天虚线，等范围延出来后自动居中一次（`pendingToday`），「清空筛选」收回延出来的范围；只有「画不出时间轴」（没有排期齐的任务）时才置灰，可用 / 不可用两态各带一句 `title` 说明。实测：未筛选点一下 scrollLeft 0 → 3126、时间轴仍 4732px；筛选「卢青」后时间轴 260px、今天线不画但按钮可用，点一下 → 260 → 3510px、今天线落在可视区、scrollLeft 2554，再点一次不再变宽，清空筛选回到 4732px；控制台零报错。 |
| 2026-09-22 | Push 148 | 甘特图 / 任务域**五条口径定案**（业务 2026-09-22 逐条确认）：① 「未排期」= 开始日期或预计完成日期**任一未设定**（两个都填才算已排期）；② 取数按现状量级（几十～数百条）一次取全、分页上限 200 足够，**不做「单项目 1 万行」专门设计**；③ 里程碑维持「单日任务派生」（「是否里程碑」字段随 A1-11 二期）；④ 依赖三条（B3-03 / B3-04 / B3-08 冲突校验）**暂不做**；⑤ **任务删除要做** —— §3.8 A25 由「待 wmj 决议」改为「业务已定案：要做」，契约 `DELETE /api/v1/projects/{id}/tasks/{taskId}`（软删）与实现归 wmj 线，前端行内入口已就绪、接口落地即接线。 |
