# LibiaoLink 协作与开发规则（CONTRIBUTING）

> 本文是《通用开发规则》第 11–17 节结合 LibiaoLink 现状的落地版，章节编号保持原文档编号；第 1–10 节（总原则、开工流程、Git 工作流、提交与 PR、评审与合并、测试与验证等）待补充。
> 适用范围：本仓库全体成员（`px`、`wmj`、`lan`）与在本仓库工作的自动化代理；代理补充约定见 `AGENTS.md`，两者冲突时以更严格者为准。

## 11. 开发日志

- 位置与顺序：`docs/开发日志.md`，最新记录在最上面。
- 触发时机：每次向远端推送功能分支、或创建 / 更新 PR 之前，必须更新开发日志；一个 PR 至少一条记录，同一分支多次推送用「推送序号」区分（本仓库记录见 `docs/开发日志.md`）。
- 记录格式：标题行 `日期 时间 | 作者 | 分支 | Push 序号`；作者 = 当次推送人（`px` / `wmj` / `lan`）。
- 每条记录至少包含：日期时间、作者、分支、推送序号、PR / commit、变更类型（feat / fix / docs / refactor 等）、做了什么、新增功能、修复 / 重构 / 优化、测试与验证（实际执行的命令与结果；失败、跳过、未运行必须如实写明）、文档同步、风险与后续事项。
- 日志条目随产生它的 PR 一起提交；PR 合并后视为历史记录，不再修改；补充内容新增记录，不重写已合入的事实。
- 日志不得包含 Secret、Token、密码、生产数据、备份内容或本地敏感路径（如本机绝对路径）。
- 日志不能替代 PR 描述、提交说明、测试报告或 ADR；没有可记录内容时不应推送。

## 12. Secrets 与通用安全

- 不提交、不打印、不记录、不复制：`.env` 文件（本仓库：`deploy/casdoor/.env`、`frontend/.env.local`）、凭据、Token、私钥、生产数据、数据库备份与真实 Secret。
- 示例环境文件（`deploy/casdoor/.env.example`、`frontend/.env.example`）只放非敏感变量名，不放任何 Secret 示例值或占位值。
- 生产 Secret 只从受控来源（密钥挂载、Secrets 管理器等）读取；缺失、空值、路径越界或权限不合规必须 fail closed，不允许敏感环境变量回退。
- `.gitignore` 不是安全边界（当前已忽略 `.env*`、`*.local`、`frontend/dist/`、`deploy/casdoor/files/*`（`brand/` 除外）、`logs/`、`backup/`）。发现 Secret 进入提交后：先停止传播 → 通知维护者撤销 / 轮换 → 再评估历史清理；只在后续提交中删除不算完成处置。
- 服务端是唯一可信校验点；客户端校验只用于体验优化，不能替代服务端校验；鉴权不得信任客户端提交的身份或归属信息，必须由服务端依据真实归属与实时权限关系判定（前端展示的姓名 / 邮箱仅作展示，不构成授权依据）。
- 资源不存在与无权访问对外表现一致，避免泄露资源存在性；内部异常、堆栈与存储细节不得原样返回客户端；错误响应使用统一结构与请求追踪标识。
- 管理员与高风险操作（Casdoor 组织 / 应用配置、用户与凭据管理、令牌签发等）必须有不可变审计留痕，且不得被业务侧删除。
- 按项目能力启用依赖漏洞扫描与 Secret 扫描；平台支持时启用 GitHub secret scanning 与 push protection。
## 13. 依赖与版本管理

- 本仓库依赖清单与锁文件：`frontend/package.json` 与 `frontend/package-lock.json`（当前唯一 Node 依赖；仓库无根 `package.json`）。
- 依赖清单、锁文件、CI 与生产构建必须锁定经过批准的实际版本；生产依赖禁止使用 `latest`、`next`、`beta`、`rc` 等浮动版本。
- 修改依赖清单必须同步提交锁文件；不得手工编辑锁文件。
- 依赖升级只通过独立 PR 完成，必须人工确认；不得自动合并依赖更新，不得把依赖变更夹带在功能 PR 中。
- 引入新的核心依赖、升级主版本或更换架构 / 基础设施属于高风险变更：需要明确授权，必要时先立 ADR（见第 15 节）。

## 14. 生成物与不可变历史

- 当前仓库尚无机器生成物；如引入（例如后端 OpenAPI 生成客户端 / 文档），生成物必须提交进仓库，只能由生成工具更新，禁止手工修改；源定义与生成结果必须同一 PR 落地，并通过「无漂移」检查（生成物与源定义不一致即失败）。
- 已合并的历史变更（数据库迁移、已合入主干的文档与快照等）不得修改、删除或重排；新变更采用 expand / contract（先扩展、后收敛）思路，必须支持从上一正式版本升级验证与回滚兼容窗口。
- 结构变更由独立、受限的迁移任务执行；应用进程不得在启动时自动修改结构（后端落地后适用）。
- 构建产物（`frontend/dist/`）、覆盖率、测试报告、本地环境（`frontend/.env.local`）、数据库备份（`deploy/casdoor/backup/`）与临时文件不提交。

## 15. 高风险变更清单（必须人工评审）

- 数据库迁移、约束、角色与权限；
- 鉴权、授权、CSRF、会话等安全机制与业务不变量（本仓库：Casdoor 对接、OIDC 登录 / 回调 / 登出、令牌与会话处理）；
- Secrets 与敏感数据处理；
- 对外接口契约（含不兼容变更；本仓库现有 `/auth/*` 与 `docs/前端功能需求.md` 所列后端接口需求）；
- 容器 / 部署配置（`deploy/casdoor/`）、CI / CD 与分支保护设置；
- 依赖引入与升级；
- 测试基座 / 测试基础设施的新增与修改（`frontend/scripts/smoke-test.mjs` 及后续测试设施）。

单人维护时，以上变更必须取得另一名合格人工评审者批准才能合并（现团队 `px` / `wmj` / `lan` 互为评审人）。

## 16. 发布与标签

- 软件版本（建议 Semantic Versioning 2.0.0）与设计文档版本分开管理；文档版本号 ≠ 已发布软件版本。
- 发布标签只能指向主干上通过全部门禁的 commit，使用 annotated tag；签名流程就绪后优先使用签名标签。
- 已发布标签不可移动、复用或删除；平台支持时为发布标签配置保护。
- 每次发布记录：版本号、commit SHA、产物 digest、SBOM（如适用）、升级 / 回滚要求与构建时间。
- 当前仓库尚未发布版本；首次发布前先在 GitHub 配置标签保护与发布流程。

## 17. Definition of Done

只有同时满足以下条件才能声明「完成」：

- 用户目标和验收条件已满足，且没有夹带无关重构；
- 新行为、失败路径与回归路径均有适当测试（前端现状：`npm run typecheck`、`npm run build`、`npm run smoke`；界面改动以无头浏览器或手动实测为准并记入开发日志）；
- 所有适用验证已通过；未执行或失败的检查已被明确报告；
- 派生产物无漂移（当前无生成物；锁文件与依赖清单保持一致）；
- ADR、设计文档、需求文档与运行手册已按行为同步（本仓库：`docs/前端功能需求.md`、`docs/字段对照清单.md`）；
- diff 已经人工检查，不包含 Secret、调试输出、临时文件或未批准的依赖；
- 开发日志已按本次 PR 的实际推送、验证与交付内容更新；
- 交付说明列出：修改内容、验证证据、剩余风险与后续事项。
## 附录 A：仓库落地清单（LibiaoLink 现状）

1. 仓库初始化：主干 `main`；开发者常驻分支 `px` / `wmj` / `lan`（各人默认分支，合并后保留、不删除，各自承接本人后续开发）；共享区域主理人待明确。
2. 平台保护：已配置（2026-09-17 仓库转公开后全部生效）—— squash-only 合并、合并后保留源分支（`delete_branch_on_merge=false`）、常驻分支删除保护（规则集 `resident-branches-no-deletion`：`px` / `wmj` / `lan`）、`main` 分支保护（require PR、必需状态检查 `frontend`、conversation resolution、linear history、禁止 force push / 删除、enforce admins、无绕过者）、GitHub Secret scanning + push protection、Dependabot 漏洞提醒与安全更新、`CODEOWNERS`（评审自动路由）。暂不可用：Secret scanning 的 non-provider patterns 与 validity checks（需付费 SKU）。
3. 文档骨架：`README.md`、`AGENTS.md`、本文件、`docs/开发日志.md`、`docs/前端功能需求.md`、`docs/字段对照清单.md`、PR 模板（`.github/pull_request_template.md`）已有；待补 ADR 目录与唯一索引。
4. CI 门禁：`.github/workflows/ci.yml` 已配置（PR 与 `main` push 触发：锁定安装 `npm ci` → typecheck → build）；仓库转公开后复跑成功（run 35185708338，job `frontend` success），并已设为 `main` 的必需状态检查；单测与 smoke 尚未纳入（smoke 依赖本地 Casdoor 沙箱 + 3000 端口 dev server）；依赖漏洞通过 Dependabot 提醒与安全更新。
5. 接口契约：前端需求已在 `docs/前端功能需求.md` 第三节列出；后端落地时先定义唯一来源与生成流程，再写第一个纵切片。
6. 流程验证：分支 → PR → 评审 → 合并（源分支保留） → 更新开发日志 已走通（PR #1 / #2 / #3）；规则随 PR #4 落地，平台保护与 CI 门禁随 PR #5 落地。
