# 字段对照清单：源表 → 项目总览页

- 对照时间：2026-09-17
- 源表：《[XXXX-XXXX] XXX项目名称.xlsx》（4 个 sheet：项目名称 / 日报及问题汇总 / 干系人 / 变更管理）
- 对照对象：项目总览页表格（15 列，支持「字段」按钮勾选显隐，任务描述常显；预计所需天数随开始/预计完成日期联动）+ 任务详情抽屉（始终显示全部字段）+ 首页项目卡片（项目序号徽标）
- 口径补充：2026-09-18 新增项目序号（`seqNo`）定案；同日 Q8 定案——施工人数 / 紧急重要度任务创建 / 编辑均可填（非必填），文件走站内文件库（多类型、不通过第三方应用打开）；2026-09-19 新增「任务模板草稿字段」口径（Push 58，见 §四；无源表对应、当前原型前端本地草稿、正式版落库）；2026-09-19 Push 60 起 §四 的初始内容由业务写死的模板预设生成（`frontend/src/data/templatePresets.ts` 的 `STAGE_TEMPLATE_PRESETS`，九个阶段各一套、硬件实施两套 18 / 11 条）；2026-09-19 Push 61 起项目总览可「从任务模板添加任务」（入口 = 点任务表的阶段标签 → 右侧卡片：标签导航「任务节点 + 各模板」，逐条 / 整套添加）：阶段 = 模板所属阶段，项目经理 = 项目卡片上的经理（项目级字段，不是任务字段），新任务字段默认值 = `待开始` / 进度 0 / 紧急重要度「中」，负责人 / 日期 / 成果文件 / 人数留空待补（接口需求见 `前端功能需求.md` §3.8 A10）
- 数据归属（2026-09-19，Push 59）：源表那一个项目的 44 条任务**只挂在印度项目 `inmu-0010`**（前端 `tasksForProject()`），其余项目打开只出阶段骨架（阶段分组头只显示阶段名、没有任务行；Push 61 起骨架常显，加出任务后其余阶段分组头不消失）；字段口径本身不变，接后端后按 `GET /api/v1/projects/{id}/tasks` 的 `{id}` 取数。

## 一、sheet「项目名称」：44 条任务 / 18 列

| 源表列 | 源表数据量 | 表格 | 抽屉 | 说明 |
| --- | --- | --- | --- | --- |
| 任务描述 | 44 | 有（第 1 列） | 有（标题） | 拆成中英文两行显示 |
| 项目经理 | 40（均为贾海洋） | 有（取项目级值） | 有 | 项目级必填字段（projects.manager_id）；源表 4 行空取同项目非空值回填（小批量实物测试 / 客户培训、上线 / 产能爬坡 / 验收交付） |
| 任务负责人 | 44 | 有 | 有 | 中英文合并显示 |
| 任务状态 | 44（已完成 15 / 进行中 1 / 待开始 28） | 有 | 有 | 展示态按日期派生 5 态（已完成 / 提前完成 / 进行中 / 待开始 / 已延期）；「逾期」不进入状态枚举，红色标注在实际完成日期的表格列与抽屉字段 |
| 项目进度 | 22 行有值（1×16、0.49×1、0×5），22 行空 | 无（列已删） | 有（档位文字） | 表格改为四格 Tracker；阶段标题改为液体水位容器 |
| 是否按时交付 | 16 | 有 | 有 | |
| 输出成果文件 | 10 | 有 | 有 | |
| 项目进展描述 | 2 | 有 | 有 | |
| 开始日期 | 44 | 有 | 有 | |
| 预计完成日期 | 44 | 有 | 有 | |
| 预计所需施工人数 | 0（全空） | 有（可选列） | 有 | 页面为演示值，源表为空；2026-09-18 定案：任务创建 / 编辑可填、非必填（Q8） |
| 预计所需天数 | 44 | 无（列已删） | 有 | 表格改为两个日期之间横线上方的数字 |
| 实际完成日期 | 16 | 有 | 有 | |
| 阶段性里程 | 44（9 个阶段） | 无（列已删） | 有（徽标） | 用作分组标题，未单独成列 |
| 紧急重要度 | 0（全空） | 有（可选列） | 有（徽标） | 页面为演示值（高/中/低），源表为空；2026-09-18 定案：任务创建 / 编辑可填（项目经理 / 任务负责人均可）、非必填（Q8） |
| 结束时间 | 0（全空） | 无 | 无 | 未接入 |
| 文件 | 0（全空） | 有（可选列） | 有（文件标签） | 页面为演示值，源表为空；2026-09-18 定案：走站内文件库任务关联（多类型、不通过第三方应用打开、站内预览）（Q8） |
| 变更关联 | 2（均为 2026/8/11） | 有（标签） | 有 | 涉及任务：包装 Packing、物料清单完整版 |

## 二、完全未接入的 3 个 sheet

### 1. 干系人（6 条记录，7 列）— 页面无入口

- 字段：干系人姓名/name、电话或whatsapp或微信号、邮箱Email、职务Position/责任板块、所属公司company、填写者、提交时间
- 示例：Christian Winkler（项目经理 / 客户customer）等 6 人

### 2. 变更管理（3 条记录，13 列）— 表格和抽屉都没有

- 字段：变更时间、变更阶段、变更文件、变更内容描述、变更前、变更后、变更原因、变更申请人、填写者、变更后文件版本、变更后文件、进度、关联
- 数据：3 条均为 2026/8/11，关联任务：包装 Packing、物料清单完整版
- 附件：附件下载_[XXXX-XXXX] XXX项目名称/变更管理/变更后文件/（3 个 xlsx）
- 现状：页面只有任务行上的「变更 2026/8/11」小标签

### 3. 日报及问题汇总（11 列）— 未接入（源表无数据）

- 字段：时间time、填写者、当日完成工作、明日计划、现场工作附图、现场发现问题、问题归类、解决方案或建议、问题附图、问题是否处理、今日施工人数

## 三、系统内部生成字段（无源表对应）

| 字段 | 位置 | 说明 |
| --- | --- | --- |
| 项目序号 seqNo | 首页项目卡片右上角徽标（`projects.seq_no`） | 源表无对应列：创建项目时由系统分配（全库唯一正整数、不可修改、不回收；与项目编号一一对应同一项目）；卡片两位补零展示（≥100 原样），列表可按 seqNo 排序、可按序号检索。2026-09-18 定案 |

## 四、任务模板草稿字段（无源表对应，Push 58 新增；Push 60 起初始内容取业务预设，当前原型前端本地、正式版落库）

> 任务模板页（`frontend/src/PlaceholderPage.tsx`）右侧每一块面板 = 一份模板草稿（最左的「任务节点」列是节点来源，不是模板）。当前原型全部存在浏览器内存里（正式版由后端持久化）：**面板初始内容与左列节点池都由业务写死的模板预设生成**（`frontend/src/data/templatePresets.ts` 的 `STAGE_TEMPLATE_PRESETS` —— 九个阶段各一套，硬件实施两套：18 条 / 11 条；软件部署一套 4 条），刷新即回到这份预设；下表先按本地草稿定义字段口径，落库时与 wmj 对齐 `templates` / `template_nodes`。

| 字段 | 位置 | 说明 |
| --- | --- | --- |
| 模板 id `id` | 面板列表 key / 拖拽定位（`TemplateDraft.id`） | 当前原型前端自增序号 `tpl-N`（跨阶段不重复），刷新即重排；落库建议 `templates.id`（全库唯一，UUID / 雪花） |
| 模板名称 `name` | 面板列头（可直接改名） | 当前原型默认取预设名（多数阶段「{阶段}模板」如「设计开发模板」，硬件实施「硬件实施模板一 / 模板二」），可改成「英国订单」这类业务名；本地草稿字段、未接模板列表；落库建议 `templates.name`（必填；长度与「同阶段内唯一」是否约束待业务定） |
| 所属阶段 `stage` | 板块标签栏选中态（`templatesByStage` 的 key） | 九个阶段（售前规划 / 设计开发 / 加工采购 / 组装发货 / 硬件实施 / 软件部署 / 试运行 / 生产阶段 / 验收）＝ `PROJECT_STAGES` 去掉「项目总览」；当前原型按阶段分开存储（每个阶段一套草稿、互不影响；正式版落库）；落库建议 `templates.stage`（枚举同上，或阶段表外键） |
| 节点列表 `nodes` | 面板内卡片列表，顺序即模板内顺序 | 元素引用任务节点（当前原型写死在 `frontend/src/data/templatePresets.ts`：多数阶段沿用「项目总览」对应阶段的任务顺序；硬件实施模板二 `hx01`~`hx08`、软件部署 `sw01`~`sw04` 是业务给的新节点；左列节点池 = 该阶段全部预设节点按出现顺序去重，硬件实施 26 个）；拖拽复制、叉号移除、拖动排序都改这份列表；同一模板内按节点 id 去重（同一节点只留一份），不同模板之间可重复引用同一节点；落库建议 `template_nodes(template_id, task_id, position)` 关联表（`position` 连续、顺序敏感） |

**预设来源（Push 60）**：面板初始名称与节点顺序 = `STAGE_TEMPLATE_PRESETS`（九个阶段各一套，硬件实施两套：长清单 18 条 / 格口-滑槽型 11 条）；新增节点 `hx01`~`hx08` / `sw01`~`sw04` 的英文名按业务截图补全、**待业务核对**；软件部署第二套模板待业务提供。
| 保存快照 `saved` | 列头「保存 / 已保存」按钮的判据 | 当前原型是前端内存里的「上次保存时的样子」（名称 + 节点 id 顺序的 JSON 序列化）：与当前值不一致 → 按钮显示「保存」（＝有未保存改动）；落库后由服务端 `updated_at` / 版本号取代，前端只做对比展示 |
| 「保存」动作 | 面板列头 | 当前原型只写浏览器内存（不落库、刷新即回到该阶段默认空白模板）；正式版落库，建议 `PUT /templates/{id}`（名称 + 节点顺序），返回更新后的模板 |
| 板块导航 `section` | 地址 `#/templates?section=设计开发` | 板块名进 URL query，非法值回落到第一个板块并纠正地址；纯前端导航态、不入库 |

**接口需求（前端原型暂未接入，正式版落库；契约待 wmj 决议）**：① 按阶段查模板列表（返回 id / 名称 / 阶段 / 节点顺序 / 更新时间，节点与任务表联查）；② 模板增删改（新建 / 改名 / 删除 / 节点编排 + 去重 + 排序）；③ 模板实例化到项目的口径（模板应用到一个项目时怎么生成 / 复用任务）。细节待业务定稿后补进 `前端功能需求.md` §3.6 与 A1-16 / A1-17；对齐清单见 `前端功能需求.md` §3.8 A11。

## 五、数据质量提醒

1. 项目经理：项目级必填字段（`projects.manager_id`），源表 4 行空取同项目非空值回填，不留空值；**页面已按项目取经理姓名展示（Push 61：项目卡片 / 任务表 / 任务详情都取 `managerId` → 姓名，如印度项目 = 冯雪、美国项目 = 王芳）**。
2. 状态与进度不一致（1 条）：任务「人员进场、场地检查、施工对接」源表状态=待开始，但实际完成日期=8月11日、进度=1；页面按日期派生为「提前完成」（8月11日早于预计完成日期 9月2日）。
3. 项目进度：22 行空（页面按 0 处理）——建议后端统一为数值或档位枚举。
4. 四列空置：预计所需施工人数、紧急重要度、结束时间、文件（源表全空）。2026-09-18 Q8 定案：施工人数 / 紧急重要度任务创建 / 编辑均可填、非必填；文件走站内文件库（多类型、站内预览，不通过第三方应用打开）；结束时间仍未接入。

## 六、后续补充建议（优先级）

1. 高：项目经理取项目级字段（源表非空值回填），任务表该列展示所属项目的项目经理，替换当前全局常量。
2. 高：变更管理明细进任务抽屉（变更前/后、原因、附件），替换目前只有日期的标签。
3. 中：项目级「干系人」面板（6 人：姓名/职务/公司/联系方式）。
4. 低：日报及问题汇总——源表暂无数据，等有数据再设计。
