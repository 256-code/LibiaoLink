# 市场项目管理系统 · 技术设计 v0.3（实施与验收篇）

- 版本：v0.3 讨论稿，2026-09-18
- 入仓：仓库根 `技术设计v0.3-实施与验收.md`（新建）；数据模型与契约增补见《技术设计 v0.2》§11
- 输入基线：《技术设计v0.2-架构与数据模型.md》v0.2.6、《系统功能书.md》V1.1（定档版，含 2026-09-18 修订）、《前端功能需求.md》（Push 40 现状）、《团队分工.md》v1.1、《CONTRIBUTING.md》第 5、11、14~17 节
- 本文范围：后端落地的执行流程（卡片流水线）、里程碑与依赖、逐里程碑详细设计、横切机制、协作与联调节奏、验收与风险、M0 待定案口径清单
- 不在本文范围：业务功能点定义（系统功能书）、页面与交互（前端功能需求）、技术选型理由（技术设计 v0.1）、表结构最终形态（以 `database/migrations/` 为准）

**修订记录**

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.3 | 2026-09-18 | 首稿：卡片流水线与 DoR/DoD、M0~M8 里程碑与依赖、九项 PoC 落点、逐里程碑详细设计、横切机制、协作与联调、待定案口径清单（含建议值）、验收与风险 |

---

## 1. 执行模型

### 1.1 交付单位与三条轨道

- 交付单位 = 卡片（card）：一个可独立验收的纵切片，端到端穿透「契约 → 迁移 → schema → 模块 → 测试 → 文档 → 联调」；不做「先把所有表建完、再统一写接口」的分层式交付。
- 三条轨道并行，契约先于实现：
  - 契约与数据轨道（wmj 主责 / lan 评审）：`shared/` 契约、`database/` 迁移、ADR 与口径决议；
  - 领域轨道（wmj）：identity、project、blueprint/node、task、report/issue、stakeholder、automation、admin（字典 / 审计）；
  - 平台轨道（lan）：file、preview、notify、outbox / 队列 / 调度、search/dashboard、迁移工具链。
- 前端（px）在契约冻结后接入联调；契约未冻结期间继续使用内存模型开发，不手写与契约重复的第三份类型。

### 1.2 卡片流水线（八步，缺一步不算完成）

| 步 | 动作 | 产出物 | 负责 | 门禁 |
|---|---|---|---|---|
| 1 | 口径确认 | 决议记录（ADR / 技术设计增补 / 开发日志条目） | 提出人 + wmj | 影响契约、字段口径、权限的必须有结论 |
| 2 | 契约 | `shared/src/modules/*.ts` + openapi 注册 + 生成物 | wmj | `npm run typecheck` + `npm run check` 无漂移 |
| 3 | 迁移 | `database/migrations/000N_*.sql`（只追加） | 卡主责 | `migrate.mjs --dry-run` + 空库跑通 + 对端评审 |
| 4 | Schema | `server/src/db/schema/*.ts` | 卡主责 | `npm run check:db-schema` 无漂移 |
| 5 | 模块实现 | controller / service / repository / events + `index.ts` | 卡主责 | `typecheck` + `check:boundaries` |
| 6 | 测试 | 单测 + 集成 + 权限允许 / 拒绝 + 金标 | 卡作者 | `npm run test` 全绿；权限类卡片必须有拒绝用例 |
| 7 | 联调 | 前端替换 mock + 实测证据 | px + 卡主责 | 验收清单逐条勾选 |
| 8 | 文档与推送 | 需求 / 字段 / 设计 / 日志同步 + PR | 卡作者 | `CONTRIBUTING.md` §17 DoD + 非作者评审 |

### 1.3 DoR 与 DoD

- DoR（可开工）：口径已决；依赖卡片已合并；契约草案已评审；测试数据或样本已就绪（蓝图节点、成果文件样本、企微测试企业等）。
- DoD（在 `CONTRIBUTING.md` §17 之上，后端增补两条）：
  1. 空库迁移跑通：`node database/scripts/migrate.mjs` 在全空库可一次跑通，且 `npm run check:db-schema` 无漂移；
  2. 权限矩阵：涉及数据的接口至少各有一条允许与一条拒绝用例，且拒绝路径为 404 语义（防 IDOR）。

### 1.4 卡片模板（写进 PR 描述）

- 背景与目标 / 范围与非目标 / 口径决议引用 / 契约变更（增量或破坏性）/ 迁移与前滚方案 / 接口清单 / 事务与并发 / 幂等键 / 权限与字段策略 / 错误码 / 测试点 / 联调步骤 / 验收证据 / 文档同步项。

### 1.5 编号与既有编号映射

- 本文里程碑用 M0~M8 编号，与既有编号的映射：
  - 定档任务卡片 `g1`~`g6`（ADR、契约包、数据库基线、server 骨架、CI、auth 会话后端化）落在 M0 / M1；
  - `shared/README.md` 的「阶段 6 纵切 h1~h4」= M2 / M3；
  - 「阶段 7（文件与预览、自动化、日报与问题）」= M4 / M5 / M6；
  - 「阶段 8（通知、搜索、统计）」= M5 / M7；
  - 「阶段 9（迁移工具链）」= M8。

---

## 2. 里程碑与依赖

### 2.1 总览

| 里程碑 | 内容 | 主要卡片 | 依赖 | 预计 | 出口标准 |
|---|---|---|---|---|---|
| M0 | 口径冻结与种子数据 | 决议、契约切片表、种子清单、规则文案入仓 | — | 1 周 | 待定案清单（§7）全部落 ADR / 文档 |
| M1 | 身份与平台底座 | auth 会话、用户 / 组织 / 角色、字典、审计、幂等、outbox / jobs、权限策略 | M0 | 3 周 | 前端真实登录；字典接口可用；杀 worker 不丢消息 |
| M2 | 项目纵切（h1 / h2） | 项目 CRUD、建项目快照、阶段推进、facets、成员、视图、关注 | M1 | 3 周 | 首页全部走真实接口；facets 与列表一致 |
| M3 | 任务纵切（h3 / h4） | 任务列表 / 详情、进度与状态、门禁、批量、性能 | M1、M2 | 3 周 | 门禁拒绝可复现；1 万行 p95 达标（压测脚本 + CI 真机执行 · Push 157） |
| M4 | 文件与预览（阶段 7） | 上传会话、版本 / 定档 / 回收站、关联、变更、预览管道、CAD PoC | M1、M3 | 5 周 | 200MB 续传；转换成功率 ≥95% |
| M5 | 自动化与通知（阶段 7~8） | 规则引擎、调度、企微、站内信 + SSE、R01~R07 | M1、M3、M4 | 5 周 | 金标回放全绿；限速实测 |
| M6 | 日报 / 问题 / 干系人 / 工作台 | 日报闭环、问题四态 + SLA、干系人、工作台 | M1~M3 | 4 周 | 填报 → 问题 → 关闭全链路可演示 |
| M7 | 搜索 / 仪表盘 / 导出 | 搜索投影、B4 / B5 统计、导出作业、归档 | M2~M6 | 4 周 | 搜索与权限一致；导出审计可查 |
| M8 | 迁移与上线（阶段 9） | 迁移工具链、全量演练、部署备份、验收 | M1~M7 | 4 周 | 真实数据导入 + 对账报告 + 恢复演练 |

### 2.2 依赖与关键路径

~~~text
M0 ─▶ M1 ─▶ M2 ─▶ M3 ─┬─▶ M4 ─▶ M5 ─┐
                       │                ├─▶ M8
                       └─▶ M6 ──────────┘
                                M4 / M6 ─▶ M7 ─┘
~~~

- 关键路径：M0 → M1 → M2 → M3 → M4 → M5 → M8；任何一项延期都直接推迟上线。
- M6 在 M3 之后即可开工（wmj 领域线），与 M4 / M5 并行；M7 在 M4 / M6 数据就绪后并行（lan 平台线）。
- M5 的规则框架可在 M3 后先行（不依赖文件），仅「文件事件类规则」需等待 M4 的 outbox 主题就位。

### 2.3 并行策略与瓶颈

- 并行瓶颈：文件管道与自动化（`技术设计v0.1-选型分析.md` §7.5.1 已指出）。对策：PoC 1 / 4 / 5 提前到 M1~M2 期间完成，M4 期间由 wmj 承接 M6。
- 平台线与领域线按模块边界解耦：跨模块只经 `index.ts`，副作用走同事务 Outbox，避免互相等待。
- 前端不等待全部后端：每个里程碑结束后即可接入对应页面（M2 首页、M3 任务表与抽屉、M4 文件库、M5 消息中心、M6 日报与问题、M7 搜索与仪表盘）。

### 2.4 九项 PoC 落点（`团队分工.md` §6）

| PoC | 内容 | 落点 |
|---|---|---|
| 1 | 文件直传 + 断点续传 + 转换预览 | M4 |
| 2 | 企微打通（应用消息 + 群机器人 + 限速） | M5 |
| 3 | 规则时间语义（R03~R07 金标回放） | M5 |
| 4 | Outbox 投递语义（杀 worker 不丢、重复不重发） | M1 |
| 5 | PG 队列并发（50 并发领取无重复无死锁） | M1 |
| 6 | 权限矩阵与脱敏（五类出口自动化用例） | M1 |
| 7 | 历史数据迁移（真实数据 + 附件全量） | M8 |
| 8 | 二维 CAD 本地转换与渲染（条件性） | M4 |
| 9 | 流程节点门禁与蓝图导入（round-trip + 拒绝留痕） | M2 / M3 |

---

## 3. 逐里程碑详细设计

### 3.1 M0 口径冻结与种子数据（1 周）

**卡片**

| 卡片 | 内容 | 主责 | 产出 |
|---|---|---|---|
| M0-01 | 待定案口径决议会（§7 十五项，逐项定案） | wmj | ADR-019~（按项拆分或合并）+ 技术设计增补 |
| M0-02 | 契约切片表：每张卡新增哪些端点 / 字段 / 错误码 | wmj | `shared/README.md` 切片段更新 |
| M0-03 | 种子数据清单与格式（九阶段、十类成果文件、问题归类、角色权限矩阵、蓝图节点） | wmj | 种子脚本规格（可重跑，与迁移分离） |
| M0-04 | R01~R07 文案入仓 + 金标用例骨架 | wmj | `docs/rules/` 或系统功能书附录 + 金标测试骨架 |
| M0-05 | 前端联调清单：每个页面需要哪些接口 / 字段 / 错误码 | px | 前端功能需求 §三 回写 |

**出口标准**

- §7 十五项全部有结论（未决项按建议值先行，并在 ADR 标注「可修订」与复审时间）；
- 蓝图节点清单与项目类型枚举定稿，能支撑 M2 建项目快照；
- R01~R07 文案逐字入仓，规则引擎可通过金标比对验收。
- 【状态 2026-09-20 · Push 71 / 73】M0-01 决议已落 ADR-019 ~ ADR-029（Push 71）；M0-04 文案与金标骨架已入仓 `docs/rules/`（Push 71）；M0-02 契约切片表已更新 `shared/README.md`、M0-03 种子数据规格已落 `database/seeds/README.md`（Push 73）。

### 3.2 M1 身份与平台底座（3 周）

**已落地（g6 · Push 43，2026-09-18）**：`/auth/*` 四条路径的契约与实现（identity 模块）、`0004_identity.sql`（users / sessions）、CSRF 同步 token 与会话守卫；本里程碑剩余部分为组织 / 角色 / 字典 / 审计 / 幂等 / outbox / 权限策略。

**新增表（`database/migrations/0004+`）**

| 表 | 关键字段 | 说明 |
|---|---|---|
| users（已随 g6 落地 0004） | casdoor_id（唯一）、username（工号，唯一）、display_name、email、owner、status | 以 SSO 为准；首次登录按 claims 归一化 upsert；一期不落 wecom_userid（需要时再加列） |
| departments | name、parent_id、source_id、status | 企微 / Casdoor 同步（**已落 0007** · h1 / Push 74） |
| roles / role_permissions / user_roles | 角色、权限位、用户绑定 | 功能权限 + 数据范围（**已落 0007** · h1 / Push 74；矩阵条目随 h6） |
| sessions（已随 g6 落地 0004） | user_id、token_hash（sha256，唯一）、id_token（仅登出 id_token_hint）、expires_at、last_seen_at、revoked_at | 会话落库（无 Redis）；Cookie 值只存哈希，不存 refresh / access token |
| idempotency_keys | key + user + endpoint、request_hash、response_snapshot、status | 写接口幂等 |
| audit_logs | actor、action、object、before / after / diff、entry、trace_id、result | 追加写，保留 ≥6 个月（**已落 0013** · h7 / Push 97；api 角色无 UPDATE / DELETE） |
| dict_types / dict_items | code、name、sort、enabled、metadata（含 accent） | C9 字典唯一口径；接口形态见 A3（Push 49）（**已落 0013** · h7 / Push 97；种子 #5 下发 region / projectType） |
| user_preferences | user_id（主键）、prefs（jsonb）、updated_at | 用户级 UI 偏好（A4 / A24）；与项目视图 project_views 分离 —— **已落地（Push 169 · 迁移 `0029_user_preferences.sql`）** |
| jobs | kind、run_at、cron、last_run_at、status、locked_by | 定时 / 单例任务 |
| calendar_days | date（主键）、day_type（holiday / makeup_workday）、name、note、updated_by | 工作日历例外（D5-01）：只存例外，未登记日期按默认规则（**已落 0014** · h8 / Push 99） |
| calendar_settings | id（单行，恒 true）、reminder_shift_enabled、shift_direction、updated_by | 顺延规则配置（D5-02，单行；迁移即建默认行）（**已落 0014** · h8 / Push 99） |

**接口**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /auth/login | 生成 state + PKCE，302 到 Casdoor（已随 g6 落地） |
| GET | /auth/callback | 校验 state、换令牌、建会话（已随 g6 落地） |
| GET | /auth/me | 验签 id_token / 会话校验，返回用户字段（已随 g6 落地） |
| GET | /auth/logout | 清会话，302 到 Casdoor 登出（已随 g6 落地） |
| GET | /users | 用户目录（项目经理下拉 / 任务负责人候选；只返回启用用户）——**已实现 `GET /api/v1/users`（Push 78）**；契约（A2，Push 49） |
| GET | /dicts / GET /dicts/{type} | 字典下发（一期 region / projectType，含 metadata.accent；阶段 / 成果文件类型 / 紧急重要度走契约枚举）——**已入契约（A3，Push 49）**，**已实现（h7 · Push 97）** |
| POST / PATCH / DELETE | /dicts/{type}/items（+ /{code}） | 字典条目维护（**Push 168 修订：POST 新增 `region` = 任何登录用户**（全站共享的公共标签）、其余类型与 PATCH 改名 / 排序 / DELETE 删除 = `dict.manage`；**Push 173 修订：删除 = 物理删行**（删除前快照写审计 `action=delete`，删除无记忆 —— 同码可重新新增、按全新条目处理），**Push 174 修订：删除加引用守卫** —— 条目被未删除项目引用（`DictItem.usageCount > 0`）时 `DELETE` 返回 409 `DICT_ITEM_IN_USE`（不删行、不写审计）；下发条目带 `usageCount`。`enabled` 降级为兼容字段；同码 409；变更写审计）——**已实现（h7 · Push 97；权限口径修订 Push 168；删除口径修订 Push 173）** |
| GET | /audit-logs | 审计检索（按对象 / 操作人 / 动作 / 结果 / 项目 / 时间区间；仅 `audit.view`）——**已实现（h7 · Push 97）** |
| GET / PUT / DELETE | /calendar/days（含 /{date}）、/calendar/day、/calendar/settings、/calendar/shift、/calendar/offset | 工作日历（D5）：某年日历 / 单日判定 / 顺延配置 / 顺延与 T-N·T+N 求值（读登录即可）；维护与配置写仅 `calendar.manage` 且写审计——**已实现（h8 · Push 99）** |
| GET / PATCH | /users/me/preferences | 用户偏好（PATCH 合并语义；键 `taskTableHiddenColumns`（A4 列显隐，Push 170 起为白名单枚举数组）、`homeSavedFilters`（A24 常用筛选，≤ 20 组）与 `focusMode`（A4 醒目模式 · §6.13，Push 171 增补，布尔））——**已入契约（A4 · Push 49；A24 键随 Push 169 增补、白名单随 Push 170 增补）**、**已实现（Push 169 / 170 / 171：迁移 0028 + `server/src/modules/identity/user-preference.*`；只读写会话 actor 自己那一行，PATCH 挂 CSRF；前端 `src/preferencesApi.ts` + `ProjectDetail.tsx` / `App.tsx` 接线；醒目模式随 Push 171 接入 —— 读侧非布尔收敛 false）** |
| POST | /auth/mfa/verify | 本地账号 TOTP（D1-05，高风险操作重认证） |

**关键设计**

- `/auth/*` 为生产实现（当前仅 Vite 中间件，见 `frontend/README.md`）：state 双提交 Cookie + PKCE S256 + nonce；JWKS 缓存与 kid 轮换；时钟偏移容忍；id_token 验签后才采信。
- 会话 Cookie：`HttpOnly + Secure（生产）+ SameSite=Lax + Path=/`；30 分钟闲置超时（滑动续期）+ 12 小时绝对上限（对齐《开发者接入注意事项》第三章）；**会话校验比对 `users.status`：非 active 即撤销该用户全部会话**（覆盖离职踢线，g6 现状）——一期不设 `auth_version`，将来若需「改密 / 越权修复后强制全端下线」再补迁移。
- 组织同步（D1-02）：worker 定时拉取，产出差异报告；停用 / 离职触发会话撤销 + 审计；调岗触发数据范围重算（D1-07）。【Push 78】同步引擎（`OrgSyncService`）与 Casdoor 适配器（`CasdoorDirectorySource`）已落地，手动入口 `POST /internal/org-sync/run`；定时调度随 i5，审计留痕随 h7。
- 权限策略服务（ADR-011 落地形态）：`can(actor, action, resource)` / `buildScopeWhere(actor)` / `fieldPolicy(entity)` / `exportPolicy`；角色与字段策略表驱动、内存缓存 + 版本号失效；Guard + 查询注入统一出口，保证「列表 / 详情 / 导出 / 搜索 / 通知」五出口一致（v0.1 §4.12 不变量）。
- 审计：api 角色仅授 SELECT / INSERT（无 UPDATE / DELETE）+ 按月清理任务（保留 ≥6 个月）；越权尝试单独标记（`result=denied`）——h7 已落库级禁删与检索，告警推送随 M5 通知。
- 幂等：`Idempotency-Key` 表 + 请求指纹；同键并发串行化并返回首次结果。
- Outbox / jobs：`FOR UPDATE SKIP LOCKED` 领取、指数退避、最大次数、死信告警；`jobs` + advisory lock 单例调度；ClockService 注入（规则代码禁取系统时间）。

**出口标准**

- 前端登录 → 会话建立 → `/auth/me` 返回用户；未登录 401 触发前端重走 SSO；
- 字典接口驱动项目类型主题色（前端移除硬编码映射，见 `前端功能需求.md` §二.1）；
- 权限矩阵 CI 用例（每条涉及数据的路由允许 + 拒绝）；
- PoC 4 / 5 / 6 证据入开发日志。
- 【状态 2026-09-20 · Push 74（h1 · S6·identity/org）】组织 / 角色数据面与出口已落地：`0007_identity_org.sql`（departments / roles / role_permissions / user_roles + Drizzle 对齐）+ 角色种子（`database/seeds/roles.mjs`，六角色）+ identity 出口（`listDepartments`、`RoleService.getActorAuthorization` 授权画像、`OrgSyncService.applySnapshot` 差异报告 / 缺失停用 / 离职踢线 / 安全阀）与单测（23 例）。剩余：目录适配器（Casdoor / 企微 拉取）随调度卡片 i5 接 worker；`role_permissions` 矩阵随 h6；组织 / 角色管理端点随 u3 / u12；人员 ↔ 部门映射随 D1-06。M1 其余卡片（字典 / 审计 = h7、幂等 / outbox = i5、偏好 = 随 M2-06）未开始。
- 【状态 2026-09-20 · Push 78（h1 · S6·identity/org 收口）】h1 卡收口：① 离职回收 API `POST /internal/users/{disable,enable,delete}`（`X-Internal-Token` 常量时间比较 + 幂等 200 + disable / delete 撤销全部在线会话；delete 为软删 `users.removed_at`，迁移 `0008_identity_removed.sql`）—— 对应接入标准第五部分「权限回收闭环」；② 目录适配器 `CasdoorDirectorySource`（get-users 分页 + get-groups → `OrgDirectorySnapshot`）与 `POST /internal/org-sync/run` 手动同步入口（定时调度仍随 i5 接 worker）；③ 用户目录 `GET /api/v1/users`（A2 契约：只返回启用用户 / q / 工号升序）。新增单测 13 例（`test/identity-internal.test.ts`）；本地实机演练全绿（桩 Casdoor：created 用户 3 + 部门 2、幂等复跑零变更；disable 200 + 会话撤销、无凭证 401、缺参 400）。h1 验收（getUser / listDepartments 可用 + 角色数据范围用例）此前已达成，卡片可置 done。
- 【状态 2026-09-20 · Push 80（h2 · S6·project 第一批）】M2-01 + M2-04 落地：迁移 `0009_projects_soft_delete.sql`（`projects.deleted_at` / `deleted_by` + 活跃行局部索引 `ix_projects_active_updated`）+ project 模块（controller / service / repository / query + index 出口）：`GET /api/v1/projects`（多维筛选 + `q` + 上海时区日界 + 排序白名单）、`GET /api/v1/projects/facets`（与列表同 filter 构造器）、`POST`（`seq_no` 分配 / 编号唯一 409）、`GET /{id}`、`PATCH`（乐观锁 `version`，409 `VERSION_CONFLICT`）、`DELETE`（`If-Match` + 软删）；归档写保护 409 `PROJECT_ARCHIVED`（ADR-027；错误码入契约与映射表）。新增单测 21 例（`test/project-crud.test.ts`，不连库）+ 本地 PG 实机闭环（创建 / 撞号 409 / 乐观锁 / 归档写保护 / 软删可见性与 facets 一致性 / 401·403·400）；`check:db-schema` 18 表 / 190 列 / 51 索引 / 47 CHECK。未做（按卡片边界）：建项目蓝图快照（M2-02 · h3）、阶段推进（M2-03）、成员与记录级权限（M2-05）、视图 / 关注 / 偏好（M2-06）、`Project.archivedAt` 与 archive 端点（随 C4-02 / M6）、幂等键（i5）、审计（h7）。
- 【状态 2026-09-20 · Push 81（h2 · S6·project 收口：M2-05 名册 + 分类字段口径）】① 迁移 `0010_project_members.sql`：`project_members`（`project_id` / `user_id` / `role_in_project` / `joined_at`；同项目同用户唯一 + `user_id` 反查索引，角色两值 `project_manager` / `project_member`）+ Drizzle 同步；② 名册接口 `GET / POST / DELETE /api/v1/projects/{id}/members`（POST 幂等 upsert：重复添加 = 覆盖角色并保留 `joinedAt`；目标用户不存在 404；不是成员 / 项目不可见统一 404）——名册是记录级权限（非成员 404）与「我参与的项目」的来源，过滤消费随 h6；③ ADR-022 ② 触点：成员增删 / 改角色后 touch 项目 `updated_at`；④ 归档写保护同样覆盖名册（409 `PROJECT_ARCHIVED`，名册读仍可用）；⑤ 分类字段口径（A1-12）：`region` / `projectType` 创建缺省「未分类」（契约 default；空串 400，前端表单仍必填），facets 计数口径一致。新增单测 7 例（`test/project-members.test.ts`）+ 本地 PG 实机闭环（建项目 / 加成员（缺省角色）/ 改角色 / touch 后 `updatedAt` 前移 / 同步验证 / 用户 404 / 非法 uuid 400 / 移除与重复移除 404 / 归档后名册 409 且只读 / 项目 404 / 401·403）；`check:db-schema` 19 表 / 195 列 / 54 索引 / 48 CHECK。h2 卡验收（项目 CRUD 与成员用例通过 + 分类字段口径与需求一致）达成；M2-02 建项目快照 / M2-03 阶段推进归 h3（S6·blueprint/node），M2-06 视图 / 关注 / 偏好不在本卡。
- 【状态 2026-09-20 · Push 83（h3 · S6·blueprint/node：M2-02 建项目快照 + M2-03 阶段推进 / 回退 + 蓝图版本化）】流程节点卡落地：① 数据面 `0011_blueprints.sql`（`blueprints` 按 `project_type` 唯一 + 草稿 / 发布版本 / 乐观锁；`blueprint_versions` 版本快照 + 校验 issues ——「导入即快照」的版本来源）与 `0012_project_stages_tracking.sql`（`project_stages` 增 `advanced_at/by`、`rolled_back_at/by`、`rollback_reason`）+ 种子 #7（default 模板 9 阶段 19 节点，幂等可重跑）；② 蓝图出口 `GET / PUT /api/v1/blueprint`、`POST /publish`、`GET /export`、`POST /import`（校验：schema / key 唯一 / seq 递增 / docType 引用 → 422 `BLUEPRINT_SCHEMA_INVALID` / `BLUEPRINT_REF_UNKNOWN`；发布幂等 —— 无变更不递增版本，比较前做键序归一化；round-trip 无损；写接口仅管理员，非管理员 403；读 / 写按 `projectType` 精确取、default 只在项目侧解析时兜底）；③ 建项目单事务导入快照（M2-02：stages / nodes / requirements + `projects.stage_key` 指向蓝图首阶段；蓝图不可用 → 422 `BLUEPRINT_NOT_PUBLISHED`；`GET /projects/{id}/flow` 返回快照，`blueprintVersion=0` = h3 之前建的项目）；④ 阶段推进 / 回退（M2-03 · ADR-023：`GET /projects/{id}/stages`、`POST …/advance`（门禁 = 节点全 done + 任务全 done + 必交成果文件齐备；失败 422 `STAGE_GATE_NOT_PASSED` + `details[].code = node_not_done / task_not_done / doc_missing`，**整体一次事务、不部分推进**）、`POST …/rollback`（相邻上一阶段、原因必填、不做门禁；首阶段 409 `STAGE_STATE_INVALID`））；⑤ 节点增删（ADR-020：仅项目经理 —— `admin` 角色 / `manager_id` / 名册 `project_manager`；`nodeKey` 必须命中项目导入版本的模板节点池 → 422 `BLUEPRINT_REF_UNKNOWN`；`node_key` 项目内唯一：已有未删节点 409 `NODE_ALREADY_EXISTS`，软删后再增补 = 还原同一行；有成果文件 409 `NODE_HAS_FILES`）与完成门禁（`POST /api/v1/nodes/{id}/complete`、`GET …/can-complete`；缺件 422 `NODE_REQUIRED_DOC_MISSING` + `missing[]`）；错误码新增 5 个（`STAGE_GATE_NOT_PASSED` / `BLUEPRINT_NOT_PUBLISHED` / `NODE_HAS_FILES` / `STAGE_STATE_INVALID` / `NODE_ALREADY_EXISTS`），契约 paths 49 / schemas 117（drift 0）。新增单测 13 例（`test/blueprint-validation.test.ts` 7 + `test/flow-gate.test.ts` 6；全量 100 例 / 9 文件）+ 本地实机闭环（真 PG + 真 api :3011）：蓝图导出 → 导入 → 再导出无损、发布幂等（无变更不递增 / 有变更 +1）、建项目 201 → 快照 9 阶段 19 节点、完成门禁缺件 422 + missing 明细、阶段门禁 422（node_not_done ×3）且不部分推进、推进 200（presale=done / design=active / `stage_key=design`）、回退 200（design=pending / presale=active / 原因留痕）、首阶段回退 409、stale version 409、增补 / 删除节点（409 `NODE_ALREADY_EXISTS` / 409 `NODE_HAS_FILES` / 软删后还原同一行）、归档项目 409 `PROJECT_ARCHIVED`、非项目经理（lisi）403、h2 老项目 `blueprintVersion=0` 无快照；`check:db-schema` 21 表 / 217 列 / 59 索引 / 51 CHECK。**h3 验收（蓝图 round-trip 无损 + 缺必交成果文件时完成被拒）达成**。收口口径（随卡登记）：记录级 404 语义与权限矩阵随 h6；`GateService` 直读 `files` / `tasks` 表为过渡口径（file / task 未落地，i1 / h4 后改对端 `index.ts` 出口）；项目升级到新蓝图版本随 M6；写接口 `Idempotency-Key` 随 i5、审计留痕随 h7。
- 【状态 2026-09-20 · Push 89（h4 · S6·task：M3-01 任务列表 / 详情 + M3-02 进度与状态 + 项目总览四格）】任务卡落地：① 规则纯函数 `task.rules.ts`（展示五态读时派生不写回存储、`applyStatusWrite`（A12：done 满格 + 缺省当天完成日期 / active 至少 1 格并清完成日期 / pending 清零）、`applyProgressWrite`（A13：0 / 0.25 / 0.5 / 0.75 / 1，`progress<1` 清完成日期 = 唯一清法）、`deriveOnTime`（A14）、`shanghaiToday`（ADR-028 日界以参数进 SQL））；② 接口挂 `api/v1/projects`：`GET /{id}/summary` 四格（当前阶段 / 逾期 / 已完成 / 总数）、`GET /{id}/tasks`（阶段 / 负责人 / 展示态多值（条件下推 SQL）/ `q` + 排序白名单（plannedStart / plannedEnd / actualEnd / progress / title / createdAt，默认序 = 阶段序 + 预计开始 + 创建时间 + id））、`GET /{id}/tasks/{taskId}` 详情（文件清单 + 免 N+1 行摘要）、`POST /{id}/tasks`（任务节点生成：节点须属本项目、`stageKey` 与节点阶段不一致 400、同节点已有未删任务 409 `TASK_ALREADY_EXISTS`；手工创建仅管理员（A1-13）否则 403）、`PATCH /{id}/tasks/{taskId}`（乐观锁 409 `VERSION_CONFLICT` + 三态联动 + 字段级留痕）、`PATCH …/progress`（响应与列表行同形 `TaskListItem`，`note` 写 note_change 事件）；③ 归档项目写保护 409 `PROJECT_ARCHIVED`（ADR-027）、任务变更 touch 项目 `updated_at`（ADR-022 ④）；④ 留痕 / 队列：`task_events` 四类型（status_change / progress_change / date_change / note_change，before / after 为 JSON）+ outbox `task.created` / `task.updated` / `task.progress_changed`（dedupeKey 带版本）；⑤ h3 过渡口径收口：`node/gate.repository` 不再直读 `tasks` 表，阶段推进门禁的「任务全 done」与阶段任务计数改经 `TaskStatsService` 出口（`StageProgressRow` 去任务字段），门禁语义不变（`files` 直读随 i1）；⑥ 错误码新增 `TASK_ALREADY_EXISTS`，契约 paths 49 / schemas 117（drift 0）。新增单测 30 例（`test/task-rules.test.ts` 17 + `test/task-service.test.ts` 13；全量 130 例 / 11 文件）+ 本地实机闭环（真 PG + 真 api :3011）：建任务 201（节点属本项目、预计完成已过 → `overdue` + `onTime=false`、自动带出负责人名与文件摘要 0/0/0）、判重 409、非管理员手工创建 403、进度 0.25 / 0.5 / 1 联动（1 → done + 当天完成日期 + `early_done` / `onTime=true`；写回 0.75 → active 并清完成日期）、改 status=done 满格 + 缺省当天 + `onTime=false`、展示态筛选（overdue 命中 / done,active 正确排除逾期项）与排序生效、summary `{currentStage: design, overdue: 1, done: 1, total: 4}`、阶段任务计数与门禁 422 `STAGE_GATE_NOT_PASSED`（`task_not_done 1/3`）、STALE 409、归档项目 409；psql 核对 `task_events` 8 条（三类 before → after 正确）/ outbox 9 条（created 3 / progress_changed 4 / updated 2）/ 项目 `updated_at` 已 touch。**h4 验收（① 状态口径与系统功能书 A1-06 一致；② 进度 0 / 25 / 50 / 75 / 100 可更新）达成**。差异登记（待复核）：记录级 404 与成员权限矩阵随 h6（当前读登录即可、写成员平权）；手工创建仅管理员的口径随 A1-13 复核；列表快捷筛选参数、任务软删 / 批量操作 / `deliverableTypes` 校验、门禁增强 M3-03 ~ M3-06 属后续卡；列表默认序暂无复合索引（随 M3-06 压测一起评）；`GateService` 仍直读 `files` 表随 i1。
- 【状态 2026-09-20 · Push 93（h5 · S6·PoC-9：蓝图 round-trip 与门禁拒绝的证据入库）】PoC-9 证据落地：① 新增真机回放脚本 `server/scripts/poc9-replay.mjs`（连真 PG + 真 api；自铸 / 撤销管理员会话、建 `POC9-xxx` 回放项目跑完软删、节点上补定档成果文件跑完硬删；`--keep` / `--json` / `--out` 可选）：**B1~B4** 蓝图导出 → 导入 → 再导出逐字相等（键序归一化）+ 发布幂等不递增版本；**P1 / P2** 建项目导入即快照（blueprintVersion=4 / 9 阶段 19 节点 / 10 个必交成果文件节点）；**G1** `can-complete` 预检缺件 false（UI 置灰依据）→ **G2** 完成请求 422 `NODE_REQUIRED_DOC_MISSING` + `details[].code=required_doc`（服务端强校验）→ **G3** outbox `node.gate_rejected` 留痕（含 missing 明细与操作人）→ **G4** 节点未部分生效 → 补齐两张定档成果文件后 **G5** 预检放行 + **G6** 完成 200（done + doneAt）+ **G7** `node.completed` —— 真机 14 项断言全过；② 证据入库 `docs/PoC-9-回放证据(蓝图round-trip与门禁拒绝).md`（环境 / 代码版本 / 逐条断言 / 验收对照 / 复跑命令）；③ CI 回归（不连库）`server/test/flow-gate-rejection.test.ts` 5 例（拒绝 422 + 留痕 + 不部分生效 + 预检置灰 + 门禁通过对照；全量 135 例 / 12 文件）；④ 回放中发现并修正契约漂移：`POST /api/v1/nodes/{id}/complete` 原先返回裸节点视图、与契约 `NodeCompleteResponse = { node }` 不一致 —— 已按契约包成 `{ node }`。**h5 验收（① 金标回放证据入库；② 蓝图 round-trip 无损 + 缺必交成果文件时完成被拒（UI 置灰 + 服务端强校验 + 留痕））达成**。差异与后续：任务侧完成门禁（M3-03）仍属后续卡；前端置灰接线随 px 联调（消费 `can-complete`）；回放脚本依赖本地沙箱 PG + api、不入 CI（CI 走不连库的 5 例回归）。
- 【状态 2026-09-20 · Push 95（h6 · S6·PoC-6：权限矩阵与脱敏五出口）】权限策略层落地（ADR-011 五层 + 5 条不变量）：① 契约新增 `shared/src/modules/permissions.ts`（`PERMISSION_KEYS` 24 键 / `PermissionKey` / `DataScopeSchema` 五值 / `ActorPermissions` / `PermissionMeResponse`）+ `GET /api/v1/permissions/me`（paths 50 / schemas 121，drift 0）；② 新模块 `server/src/modules/permission/` —— 纯函数策略层 `permission.rules.ts`：记录级 `projectScopeSpec` / `visibleProjectIds` / `isProjectVisible`（列表与详情同一谓词）、功能权限 `can` = 全局位 ∪ 项目内项目经理 / 成员隐含位（ADR-011 §4.3 平权例外 + ADR-020 / ADR-023）、字段级表驱动 `FIELD_POLICIES` + `canSeeField` / `visibleFields` / `hiddenFields` / `projectFields`（**无权字段不返回**，不落 null 占位 · C3-08）、五出口 `planExit`（page / export / search / notify 同源，导出额外要求 `project.export`）与 `exitsConsistent`；只读数据面 `permission.repository.ts`（名册 / 项目主数据 / 节点→项目）；服务 `permission.service.ts`（画像缓存 TTL 10s + 显式 `invalidate`、`projectScope`（all / ids，空集短路）、`resolveProjectAccess` / `assertProjectVisible`（不可见 / 不存在 / 已软删统一 404）、`assertCan`（403））；入口 `project-access.guard.ts`（`@ProjectAccess("node"|"project")` / `@RequirePermission(key)` / `@ProjectScope()`）；③ 接线：project / task / flow / nodes 路由挂守卫与键位（`project.create` / `project.update` / `project.delete` / `member.manage` / `task.create` / `task.update` / `task.progress` / `node.advance` / `node.rollback` / `node.create` / `node.delete`），列表与 facets 取 `@ProjectScope()`（按 id 集合过滤），blueprint 的 `assertAdmin` 改 `blueprint.manage`；④ 矩阵种子 #6b `database/seeds/role-permissions.mjs`（六角色 71 条；键唯一来源 = 契约枚举；**以文件为准、移除键会 DELETE** —— 权限吊销必须生效）+ 自检脚本 `check:permission-matrix`（种子 ↔ 契约枚举 ↔ 角色集三方对齐，admin 必须全量）；⑤ 用例：`test/permission-matrix.test.ts` **34 例**（记录级 9 / 功能权限 7 / 字段级 6 / 五出口 5 / 策略服务 7），全量 **169 例 / 13 文件**；`check:boundaries` 90 文件 / 323 依赖 / 0 违规；⑥ 真机回放 `server/scripts/poc6-replay.mjs`（真 PG + 真 api，管理员 + 受限账号双会话、回放项目跑完硬删）：**24 项断言全过**（记录级列表 / 详情 / 子资源统一 404、名册即刻可见、可见但无权限位 403、成员平权 200、授权画像键位数据面、收尾零残留）+ 证据入库 `docs/PoC-6-回放证据(权限矩阵与脱敏五出口).md`。**h6 验收（字段级 / 记录级 / 导出 / 搜索 / 通知五类出口自动化用例全绿）达成**。差异与后续（待复核）：`own_stakeholders`（随 j6 干系人）与 `granted`（随 C3-06 临时授权）未落地、仅名册兜底；字段级真实出口随 j6（策略表与投影入口已就绪，接入时直接用）；位形打码（masked）/ 只读档留待 j6 / C3-09；列表可见集按 id 集合过滤（量级上来后随 M2-06 / 压测评）；导出 / 搜索 / 通知模块本身（i 系列 / M7）未落地；权限自检报告（C3-09 · u12）与越权留痕告警（h7）未做；员工邮箱一期不做字段级（策略表登记为全员可见）。
- 【状态 2026-09-20 · Push 97（h7 · S6·admin：字典 C9 与审计留痕 C7）】admin 模块落地：① 数据面 `0013_admin_dict_audit.sql` —— `dict_types`（类型注册表）/ `dict_items`（条目：`uq_dict_items_type_code` 同类型内码唯一 + 排序索引 + metadata jsonb + `updated_by`）/ `audit_logs`（追加写：谁 / 何时 / 对什么 / 从什么改成什么 + 结果（succeeded / denied / failed）+ 入口 + trace）；按对象 / 操作人 / 项目 / 时间索引；防篡改（C7-05）：迁移与 `roles/0001_roles.sql` 双处收回 api 角色的 `UPDATE` / `DELETE`（角色脚本可重复执行、每次重放收回）。 ② 契约：`shared/src/modules/dicts.ts` 补读取参数 / 维护请求；新增 `shared/src/modules/audits.ts`（动作 / 对象类型 / 结果 / 入口枚举 + 列表查询与响应）；`PERMISSION_KEYS` 补 `dict.manage` / `audit.view`（26 键）；错误码补 `DICT_ITEM_EXISTS`（409）；OpenAPI 新增字典条目 POST / PATCH 与 `GET /api/v1/audit-logs`，paths 53 / schemas 130（drift 0）。 ③ 模块 `server/src/modules/admin/`：字典读（默认只回启用项；`includeDisabled` 需 dict.manage（403）；未知类型 404 —— 回放中发现路径参数枚举硬拦导致的 400 漂移并修正）/ 写（新增 201、停用替代删除 200（无物理删除）；同码 409；响应即更新后的整个字典（前端直接替换缓存））；审计写（`record()` 与业务**同一事务**；已接线：项目创建 / 修改 / 归档、名册增删、任务创建 / 修改 / 进度、节点增删 / 完成、阶段推进 / 回退；门禁拒绝补写 `result=failed`）、越权留痕（全局异常过滤器经 `AUDIT_SINK` 令牌：403 全记 + 项目域 404，异步补写不阻塞响应；路径 → 对象解析为纯函数，字典对象 id 与写入侧同形 `type[:code]`）、检索（`GET /api/v1/audit-logs` 仅 audit.view：按对象 / 操作人 / 动作 / 结果 / 项目 / 时间区间，occurredAt 降序）。 ④ 种子 #5 `database/seeds/dicts.mjs`（region 8 项 / projectType 3 项 + 主题色 metadata；幂等、不覆盖库内已修订值、不删除）与 #6b 补 `dict.manage` / `audit.view`（admin = 契约 26 键全量；六角色 73 条）。 ⑤ 用例：新增 `test/admin-audit.test.ts` 15 例（diff 纯函数 / 越权判定 / 字典与审计服务），全量 **184 例 / 14 文件**（含既有用例对审计注入的适配）；`check:db-schema` 24 表 / 246 列 / 69 索引 / 64 CHECK、`check:permission-matrix` 6 角色 / 73 条 / 26 键、`check:boundaries` 102 文件 / 377 依赖 / 0 违规。 ⑥ 真机回放 `server/scripts/poc7-replay.mjs`（真 PG + 真 api，管理员 + 受限账号双会话、回放字典条目跑完硬删 —— 审计行用 migrator 连接清理）：**23 项断言全过**（默认只下发启用项、管理口径 403、新增 / 停用 200 且同码 409、未知类型 404 无噪声行、审计按对象 / 按人检索命中、越权 403 落 denied 行、审计接口仅 audit.view、收尾零残留）+ 证据入库 `docs/PoC-7-回放证据(字典C9与审计留痕C7).md`。**h7 验收（字典唯一口径 + 审计留痕按对象与操作人可检索）达成**。 差异与后续（待复核）：前端仍硬编码项目类型 / 主题色（改读字典随 u12 · px 线）；审计页面与导出、高风险导出重认证随 u12；越权告警推送（企微）随 M5 通知；蓝图写路径字段级留痕未接线（其 403 由全局过滤器记 denied）；保留 ≥6 个月的按月清理未自动化（库级已禁删，由运维 / 迁移器按月执行）。

- 【状态 2026-09-20 · Push 99（h8 · S6·工作日历：D5 日历维护 / 顺延规则 / T-1·T+1）】calendar 模块落地：① 数据面 `0014_work_calendar.sql` —— `calendar_days`（只存例外：`holiday` 放假 / `makeup_workday` 调休上班；date 主键 + 名称 / 说明 + `updated_by` + 4 CHECK + (day_type, date) 索引；未登记日期按默认规则：周一至周五工作日、周六周日非工作日）与 `calendar_settings`（单行顺延配置：`reminder_shift_enabled` / `shift_direction`；迁移即建默认行、幂等）。 ② 契约：新增 `shared/src/modules/calendar.ts`（例外类型 / 日期判定 / 顺延方向 / 求值开关 + 某年日历 / 单日判定 / 维护请求 / 顺延与 T-N·T+N 查询与响应）；`PERMISSION_KEYS` 补 `calendar.manage`（27 键）；审计对象类型补 `calendar_day` / `calendar_settings`；OpenAPI 新增 6 条路径 8 个操作（paths 59 / schemas 142，drift 0）。 ③ 模块 `server/src/modules/calendar/`（controller / service / repository / `calendar.rules.ts` 纯函数 + index 出口 + README）：维护（`PUT /calendar/days/{date}` 幂等 upsert、`DELETE` 回落默认规则、删不存在 404）、顺延（`/calendar/shift`：forward / backward，跳过的非工作日逐条返回）、T-N / T+N（`/calendar/offset`：自然日偏移 + 顺延 + 提醒时刻 08:00 → UTC 时间戳，实时求值不缓存）、配置（`/calendar/settings`）；写 = 仅管理员（`calendar.manage`）+ 审计留痕（与业务同事务，changes 字段级）；读 = 登录即可。 ④ 新增 `server/src/common/clock/clock.service.ts`（ClockService，v0.3 §4.7：规则 / 调度禁止直接取系统时间；工作日历为首个接入模块，求值基准缺省 = 今天（Asia/Shanghai）），既有模块随各自改动迁移。 ⑤ 越权路径解析扩展 calendar（`/calendar/days/{date}` → `calendar_day` + 业务日期、`/calendar/settings` → `calendar_settings` + default，与写入侧同形）；`check-boundaries` 增横切模块登记与自检（`CROSSCUT_MODULES`：不得同时登记为领域 / 平台）。 ⑥ 种子 #6b 补 `calendar.manage`（admin = 契约 27 键全量；六角色 74 条）。 ⑦ 用例：新增 `test/calendar-rules.test.ts` 17 例 + `test/calendar-service.test.ts` 12 例（金标：T-1 命中、节假日顺延开 / 关两态、跨年顺延、窗口用尽、改期后重算、08:00 时刻换算），全量 **213 例 / 16 文件**；`check:db-schema` 26 表 / 258 列 / 72 索引 / 70 CHECK、`check:permission-matrix` 6 角色 / 74 条 / 27 键、`check:boundaries` 110 文件 / 405 依赖 / 0 违规。 ⑧ 真机回放 `server/scripts/poc8-replay.mjs`（真 PG + 真 api，**api 角色直连**以验证 0014 表级授权；管理员 + 受限账号双会话，合成日历落 2099 年、跑完硬删 + 配置恢复）：**30 项断言全过**（读 = 登录即可、写 = 仅管理员 403 + denied 留痕、PUT 幂等 / DELETE 404 与回落默认规则、顺延两向、顺延开关两态（配置驱动 + `shift=on` 覆盖）、T-1 / T+1 / 08:00 时刻、审计按对象检索、收尾零残留）+ 证据入库 `docs/PoC-8-回放证据(工作日历D5).md`。**h8 验收（顺延与 T-1/T+1 金标用例）达成**。 差异与后续（待复核）：节假日 / 调休**业务数据未回执**（种子 #10）—— 先交付表结构 + 管理入口 + 顺延 / T±N 计算，实际年历数据待业务提供后经管理端（u12 · px 线）录入；规则引擎执行与跨天补跑（worker 重启按应执行清单补发且不重复）随 i8 / i9（依赖 lan 线 i5 outbox）；日历视图（D5-04）属二期；`calendar.manage` 改变矩阵计数（26 → 27 键）已同批同步。
- 【状态 2026-09-21 · Push 124（w2 · 任务落库口径：A15 阶段可空 / A18 负责人可空 / A19·A20 顺序持久化）】落库口径落地：① 迁移 `0015_task_order_and_nullable_scope.sql` —— `tasks` 加 `sort_index`（integer + default 0 + `ck_tasks_sort_index` + `ix_tasks_project_stage_order (project_id, stage_key, sort_index)`；存量按迁移前默认读序 `planned_start asc nulls last, created_at, id` 按 (project_id, stage_key) 分区回填 → 迁移前后读序一致）、`stage_key` / `owner_id` drop not null；Drizzle schema 与契约 `shared/src/modules/tasks.ts` 三处同步（`check:db-schema`：26 表 / 259 列 / 73 索引 / 71 CHECK）。② 口径：一组 = 同一项目 + 同一阶段（`stage_key` 为空 = 「未分组」，自成一组、默认序落最后）；创建 `sortIndex` = 插入位次（A20「插入位置」）、编辑 = 移到组内第 N 位（越界 = 组尾），同组顺延只写位次列、并发靠组行锁；默认读序改为 阶段序 + `sort_index` + id（A8 修订）；阶段完成度 / 阶段门禁只统计带阶段任务（未分组不计入）；`ownerId` 显式 null = 「待分配」、不传 = 不改，创建缺省仍兜底项目经理。③ 测试：新增 `test/task-order.test.ts`（4 例纯函数）+ `test/task-service.test.ts` 追加 5 例（全量 222 例 / 17 文件），`nest build` / `check:boundaries`（111 文件 / 407 依赖 / 0 违规）通过。④ 真机回放：`server/scripts/w2-replay.mjs` 连真 PG + 真 api :3011，33 项断言全过（未分组落库 / 待分配可空 / 组内位次持久化（插入 · 拖动 · 越界 · 乐观锁 409 · 组隔离）/ 节点来源口径（缺省取节点阶段、不一致 400、重复 409）/ 阶段完成度剔除未分组 / 字段级留痕 from→to），证据入 `docs/w2-回放证据(任务落库口径A15A18A19).md`。差异与后续：前端把 `ProjectDetail` 的内存态 `taskOrder` 换成读接口顺序（px 线，无需改交互）；批量排序接口未做（口径选 ① 位次列）。
- 【状态 2026-09-22 · Push 143（h4 · S6·task 续卡：M3-03 完成门禁 + ADR-024 成果文件多选落地）】完成门禁卡落地：① 契约 —— `Task.deliverable` → `deliverableTypes: DocType[]`（数组、服务端去重、空数组 = 不要求）；新增 `GET /projects/{id}/tasks/{taskId}/can-complete`（`canComplete` + `missing[]` + `warnings[]`）与 `POST …/complete`（`TaskCompleteBody` = `version` + 可选 `actualEnd` / `note`；响应 `{ task, warnings }`）；错误码新增 422 `TASK_REQUIRED_DOC_MISSING` 与 409 `TASK_ALREADY_DONE`（生成物 paths = 62 / schemas = 150，零漂移）。② 数据面 —— 迁移 `0018_task_deliverable_types.sql`（`deliverable` text → `deliverable_types` text[] 非空默认空数组 + `ck_tasks_deliverable_types` / `ck_tasks_deliverable_types_no_null` + GIN `ix_tasks_deliverable_types`；存量单值转单元素数组、空值转空数组），Drizzle schema 同步。③ 门禁口径（ADR-024 / A4-20）—— 有节点任务按所属节点 `node_requirements` 逐类统计、无节点任务按自身 `deliverable_types` 兜底（每类 ≥ 1 份）；定档口径 = `status ∈ (final, changed)` 且 `current_version_id` 非空；**三入口同一判定**（完成提交与 `PATCH status=done` / `progress=1` 事务内共用断言，缺件不部分生效）；缺件 422 + `missing[]`（`details[].code=required_doc`），拒绝写 outbox `task.gate_rejected` + 审计 failed；draft 存在 → 放行 + `warnings[]`（`draft_doc_present`）+ outbox `task.draft_doc_reminded`（R02 触发点）；成功写 outbox `task.completed` + 审计 `complete`；重复提交 409。④ 创建带出 —— 从节点生成任务缺省带出该节点 `required_doc` 类型（body 显式给出优先）；`TaskUpdateBody` 不含该字段 = 生成后锁定（A1-17；例外调整随 M3-05）。⑤ 测试 —— 新增 `test/task-gate.test.ts` 8 例（全量 318 例 / 20 文件），server typecheck / build / `check:boundaries`（0 违规）/ `check:permission-matrix` 全绿；CI 走空库迁移（0018）+ db-schema 漂移门禁。差异与后续：R01 变更关联多值命中（v0.2 §5.3 读面派生）随 M4-04 切片（#111）并入后同批改；真机回放随 M3-06 压测 / 联调卡。
- 【状态 2026-09-22 · Push 144（j6 · S8·stakeholder：A5-01 ~ A5-04 / A5-07）】干系人台账卡落地：① 数据面 `0019_stakeholders.sql` —— `stakeholders`（全库台账、不挂项目：八个业务字段 + created_by 录入人 + 软删 deleted_at / deleted_by；8 CHECK + 4 条局部索引，默认读序 = updated_at 降序且排除软删）与 `project_stakeholders`（多对多：联合唯一 `uq_project_stakeholders` + 两侧反查索引）；② 契约 `shared/src/modules/stakeholders.ts`（tags=stakeholders；六个受保护字段 `nullable().optional()` —— 无权 = 键不存在、有权但空 = null）+ `AUDIT_OBJECT_TYPES` 增 `stakeholder`（生成物重出：paths 62 → 66、schemas 150 → 158，零漂移）；③ 服务端 `server/src/modules/stakeholder/`（controller / service / repository / rules + index 出口；7 条路由：台账 CRUD + `/{id}/projects` 关联与幂等解除；读 `stakeholder.view` / 写 `stakeholder.manage`；记录级可见集 = 「我录入的」∪「关联项目在可见项目内」，不可见 / 不存在 / 已软删统一 404；字段级直接复用 `projectFields`，无权键不存在；建 / 改 / 删 / 关联 / 解绑同事务写审计，重复关联不重复留痕）；④ 用例 `test/stakeholder-rules.test.ts` 12 例 + `test/stakeholder-service.test.ts` 15 例（字段级脱敏为核心验收），全量 **345 例 / 22 文件**；`check:boundaries` 132 文件 / 504 依赖 / 0 违规、`check:permission-matrix` 通过（种子 #6b 无需改动）；⑤ 差异：导入（A5-05 · M8-01 · lan）、去重合并（A5-06 · 二期）、提醒（A5-08 · M5）、导出（A5-09 · M7-03 · lan）、「干系人角色」列（口径未定）、前端台账页与项目「干系人」面板（u 系列 · px 线）。
- 【状态 2026-09-22 · Push 150（h4 · S6·task 续卡：M3-04 任务批量操作 · A1-08）】批量操作卡落地：① 契约 —— 新增 `PATCH /api/v1/projects/{id}/tasks/batch`（`TaskBatchBody` = `ids` 1~100 去重 + `changes`；`TaskBatchChanges` 白名单 = ownerIds / status / plannedStart / plannedEnd / estimatedDays / headcount / priority / note（null = 清空、缺键 = 不改；不含任务描述 / 成果文件（A1-17）与 sortIndex）；`TaskBatchResponse` = total / succeededCount / failedCount / succeeded / failures，`TaskBatchFailureCode` 六值），无新错误码，生成物 paths 67 / schemas 163 零漂移；② 实现 —— task 模块抽出单条写入内核 `applyUpdate`（编辑 / 批量共用：锁行 → 版本校验 → 位次 → 状态联动 → 完成门禁 → 落库 + 事件 + outbox + touch + 审计），`batch()` 逐条独立事务 + 条目级错误降级（not_found / archived / gate_not_passed（带 `missing[]`）/ already_done / version_conflict / invalid_state），非 AppError 照旧抛出；批量完成与单条编辑 / `complete` 同一门禁，拒绝留痕沿用 outbox `task.gate_rejected` + 审计 `result=failed`（metadata 标注 batchId）；批次审计一条（`project` 域，记 batchId / 计数 / 失败清单）；③ 路由 —— `TaskController` 的 `PATCH :id/tasks/batch` 注册在 `:taskId` 之前（否则 batch 会被当作任务 id 命中），权限键 `task.update`（项目内成员平权）；④ 测试 —— 新增 `test/task-batch.test.ts` 9 例（server 全量 354 例 / 23 文件；typecheck / build / `check:boundaries` 0 违规 / `check:permission-matrix` 全绿）。差异与后续：前端多选交互随 M3-07 联调；`ids` 上限 100（超出由契约 400 拦截）。
- 【状态 2026-09-22 · Push 152（h4 · S6·task 续卡：M3-05 任务软删 · A25）】任务删除卡落地（业务 Push 148 定案「任务删除要做」）：① 契约 —— 新增 `DELETE /api/v1/projects/{id}/tasks/{taskId}`（tags=tasks；响应 `TaskDeleteResponse = { id, deleted: true }`）+ 错误码 409 `TASK_HAS_REFERENCES`（生成物 **paths = 67 不变**（同路径增方法）、**schemas = 165**（+`TaskDeleteResponse`），`npm run check` 零漂移）；② 数据面 —— 迁移 `0022_task_soft_delete.sql`（`tasks.deleted_at` / `deleted_by` + 部分索引 `ix_tasks_active_group (project_id, stage_key, sort_index) where deleted_at is null`），Drizzle schema 同步；③ 口径 —— **软删不物理删行**（历史与留痕保留）；读面（列表 / 详情 / summary 四格 / 阶段计数 / 看板与甘特图同源数据 / 完成门禁预检与提交 / 节点判重 / 组内顺序读 / 锁行）一律过滤 `deleted_at is null`；**重复删除与已删任务上的任何写操作 = 统一 404**（记录级 404 语义，不新增错误码）；**引用守卫**（系统功能书 A2-01「已产生日报 / 问题 / 变更的任务不允许删除」）= `change_refs` 非空 409 `TASK_HAS_REFERENCES`（`details[].code = change_ref` + `changeRequestId`；日报 / 问题两表随 M5 落表后在守卫处一并加判定），拒绝时不软删 / 不压缩位次 / 不写留痕；组内位次同事务压缩（0 起、密集，A19 / A20 不变式）；来源节点约束随软删释放（同节点可重建，A10 / A11 口径不变 —— `tasks` 本无 `(project_id, node_id)` 唯一约束，无需改约束）；权限键沿用 `task.update`（与编辑同一权限位）；④ 留痕 —— 审计 `action=delete`（summary「删除任务：<标题>」，changes = 删除前快照）+ outbox `task.deleted`（dedupeKey 带版本）+ touch 项目 `updated_at`；**不写 `task_events`**（其类型为四值闭集，删除不属于字段级变更）；⑤ 测试 —— 新增 `test/task-remove.test.ts` 10 例（server 全量 379 例 / 25 文件；typecheck / build / `check:boundaries` 136 文件 523 依赖 0 违规 / `check:permission-matrix` 6 角色 74 条目 27 键全绿）；CI 走空库迁移（0022）+ db-schema 漂移门禁。差异与后续：真机回放（真 PG + 真 api）随 M3-06 压测 / 联调卡；前端行内删除入口（原型内存态）随 M3-07 接线；日报 / 问题引用守卫随 M5。
- 【状态 2026-09-22 · Push 155（M6-01 ~ M6-03 · S6·report-issue：日报填报与回写 / 问题闭环 · A3-01 / A3-02 / A3-04 / A3-08 ~ A3-13）】日报与问题第一刀落地：① 数据面 `0023_daily_reports_issues.sql` —— `daily_reports`（一人一项目一天一条 `uq_daily_reports_author_date`；state 三值 draft / submitted / supplement（补填由服务端按日期推导）；`task_ids` GIN + 9 CHECK，含「发现问题 ⇔ 归类」成对）、`issues`（`uq_issues_source_report` 兜底 A3-09 幂等；`ck_issues_closed_pairs`（done ⇔ closed_at 非空）；4 索引 + 6 CHECK）、`issue_events`（四类事件留痕 + `(issue_id, created_at)` 索引）；Drizzle `src/db/schema/reports.ts` 同步（**32 表 / 328 列 / 103 索引·唯一 / 103 CHECK**，CI database job 复核）。② 契约 —— 新增 `shared/src/modules/reports.ts` / `issues.ts` 与 **项目嵌套七端点**（`GET|POST …/reports`、`GET|PATCH …/reports/{reportId}`、`GET …/issues`、`GET|PATCH …/issues/{issueId}`；A21 提案的扁平 `/reports/{id}` 在 `ProjectAccessGuard` 下拿不到项目上下文，差异登记）；`PERMISSION_KEYS` 27 → **31**（report.view / report.fill / issue.view / issue.manage；admin 全量、成员平权）、`AUDIT_OBJECT_TYPES` + `daily_report` / `issue`、错误码 + `REPORT_ALREADY_EXISTS`(409)；生成物 **paths = 72 / schemas = 180**。③ 服务端 —— 新模块 `src/modules/report-issue/`（双控制器 + service / repository / rules + 出口 + README）：提交触发两件**幂等**副作用（A3-09 生成「未分组」问题 + A3-12 按归类落 `owner_department`（原因类不分派）；A3-08 回写任务「项目进展描述」+ `task_events(note_change)`，幂等标记 `【日报 <日期>】`，只动 note、不动乐观锁 —— 与 R01 同口径）；问题四态**允许回退且留痕**（不设流转白名单，**不做 `ISSUE_TRANSITION_INVALID`**，差异登记）、关闭 / 回退维护 `closed_at` / `closed_by`、空更新 400、归档 409、跨项目 404；同批补齐 A2-01 删除引用守卫（`daily_reports.task_ids` / `issues.task_id` → 409 `TASK_HAS_REFERENCES` + details `report_ref` / `issue_ref`，不软删不压缩位次）。④ 测试与门禁 —— `test/report-issue.test.ts` **27 例**（日报 18 + 问题 9）、`test/task-remove.test.ts` 10 → **12 例**；**全量 416 例（27 文件）**、`check:boundaries` 145 文件 / 579 依赖 / 0 违规、`check:permission-matrix` 6 角色 / 78 条目 / 31 键、`check:drift`（shared）零漂移。⑤ 差异登记（5 条）：扁平路径改项目嵌套；归类十项为契约固定枚举（C9 可维护随后）；`dueAt` 一期仅落库（T+1 / T+3 随 M5）；一期不做日报版本历史（补填以 state + submittedAt 表达）；自动分派一期只覆盖部门名归类（「提示项目部」随 M5）。
- 【状态 2026-09-22 · Push 157（M3-06 · S6·task：1 万行数据集压测与索引调优评估 + M6 真机回放接线）】性能卡落地：① 脚本 —— `server/scripts/m3-06-stress.mjs`（自建 1 万行合成项目：九阶段 + 未分组、8 位负责人、200 条软删；9 个读取形状 p50 / p95 阈值断言 + `EXPLAIN (ANALYZE, BUFFERS)` 计划 + 索引对照：同库 DROP `ix_tasks_project_stage_order` 前后逐形状对照、p50 倍率 <= 1.5x，收尾默认建回）；② M6 回放脚本 —— `server/scripts/m6-replay.mjs`（A3-01 ~ A3-13 + A2-01 引用守卫 + 归档写保护，空库自动补合成管理员）；③ CI 接线 —— `database` job 在迁移 / 漂移检查后追加「种子 → 后台起 api → M3-06 → M6」真机执行（证据逐行落 CI 日志）；④ 索引结论（真机证据达标 · 已定案）：迁移 `0024` 下线旧索引（Push 158；Drizzle schema 同步），读面由部分索引 `ix_tasks_active_group` 兜底。
- 【状态 2026-09-22 · Push 158（M3-06 索引下线定案：迁移 0024 + schema 同步 + 脚本双态兼容）】索引调优评估的结论落地：真机对照达标（最差 p50 倍率 1.02x / 阈值 1.5x；下线后默认读序 p50 4.1 ms / 阈值 800 ms）→ ① 新增迁移 `0024_drop_tasks_legacy_order_index.sql`（`drop index if exists ix_tasks_project_stage_order;`）；② `server/src/db/schema/tasks.ts` 同步移除该索引（与库结构一致，避免双索引写放大）；③ `server/scripts/m3-06-stress.mjs` 兼容双态：旧索引在 = 前后对照（A12 ~ A15 原口径），已下线 = 直接按下线态逐形状压预算 + 记录命中计划；证据 `docs/m3-06-压测证据(CI).md`。

- 【状态 2026-09-22 · Push 153（h4 · S6·task 续卡：M3-05 锁定字段例外调整 · A1-17 / C9-07）】锁定字段例外调整落地：① 契约 —— 新增 `PATCH /api/v1/projects/{id}/tasks/{taskId}/locked-fields`（tags=tasks；body `TaskLockedFieldsAdjustBody` = `version` + `reason`（1~500 必填）+ 可选 `title` / `titleEn`（null = 清空）/ `deliverableTypes`（字典内去重保序）；200 返回 `Task`；错误码零新增），生成物重出（**paths = 68**（+1）、**schemas = 166**（+`TaskLockedFieldsAdjustBody`），`npm run check` 零漂移）；② 口径 —— 任务描述 / 英文描述 / 输出成果文件按流程节点模板生成后锁定，常规编辑（`PATCH …/{taskId}`）与批量（`PATCH …/batch`）均不含该三项；确需修正时**仅系统管理员**（控制器 `task.update` 只作项目上下文门禁，服务端 `assertAdmin` 复核，非管理员 403）；`reason` 必填并留痕；至少一个实际变化（空调整 400 `VALIDATION_FAILED`）；乐观锁 409 `VERSION_CONFLICT`；归档项目 409 `PROJECT_ARCHIVED`；不存在 / 跨项目 / 已软删统一 404；③ 留痕 —— 审计 `action=update`（changes = 锁定字段 before → after，metadata = { reason, kind: locked_field_exception, adminOnly: true, fields }）+ outbox `task.locked_fields_adjusted`（dedupeKey 带版本，payload 带 fields / reason / actorId）+ touch 项目 `updated_at`；**不写 `task_events`**（四值闭集）；④ 门禁联动 —— `deliverableTypes` 修正后即刻成为无节点任务的完成门禁依据（有节点任务仍以 `node_requirements` 为准，ADR-024）；⑤ 测试 —— 新增 `test/task-locked-fields.test.ts` 8 例（server 全量 387 例 / 26 文件；typecheck / build / `check:boundaries` 136 文件 523 依赖 0 违规 / `check:permission-matrix` 6 角色 74 条目 27 键全绿）。差异与后续：「阶段性里程」一期 `tasks` 表无列，本期不开放（A1-17 映射修订）；模板实例化与快筛仍为 M3-05 剩余切片；真机回放随 M3-06。

- 【状态 2026-09-23 · Push 173（C9-02 字典删除口径修订：物理删行；后端 + 契约 + 迁移 + 前端同批 · 业务定调「删除了就没有记忆了」）】① 契约 —— `shared/src/modules/dicts.ts` 新增 `DELETE /api/v1/dicts/{type}/items/{code}`（仅 `dict.manage`；未知类型 / 未知条目 404；响应 = 删除后的整个字典）；`DictItemSchema.enabled` 与 `DictReadQuerySchema.includeDisabled` 标注为**兼容字段 / 兼容参数**（二期「临时下架」），`DictItemUpdateBodySchema.enabled` 同步标注；`shared/src/openapi.ts` 注册 delete path（paths 72 / schemas 182，`npm run check` 无漂移，生成物 `generated/openapi.json` + `api-types.d.ts` 同批重生成）。② 后端 —— `dict.repository.ts` 增 `deleteItem`（`delete … returning`，返回 null = 不存在）、`dict.service.ts` 增 `deleteItem`（事务内先 `findItem` 快照 → 删行 → `touchType` → 审计 `action=delete`（`objectId=type:code`，changes = `code/name/sort/enabled/metadata → null`）→ 返回更新后的字典）、`dicts.controller.ts` 增 `@Delete(":type/items/:code") + ProjectAccessGuard + @RequirePermission("dict.manage")`。③ 迁移 `0030_dict_item_hard_delete.sql`（只改注释：`dict_items` / `dict_types` 表注释与 `dict_items.enabled` 列注释改为「删除 = 物理删行 / enabled 为兼容字段」；不改结构、不改权限 —— `libiaolink_api` 在 0013 已有 `delete`）。④ 用例 —— `server/test/admin-audit.test.ts` 增 3 例（物理删行 + 审计字段级 from→null + 触碰版本；删除后同码可重新新增（不 409、按本次参数落库）；未知类型 / 未知条目 404 且不写审计），原「停用替代删除」例改名标注为兼容路径；全量 **476 例 / 32 文件**。⑤ 前端 —— 删除按钮改调 `deleteDictItem`（DELETE），去掉停用视图 / 写回 / 「恢复」整条链路（`dicts.ts` / `dictTools.ts` / `DictSelect.tsx` / `ProjectModal.tsx` / `Home.tsx` / `App.tsx`）；`npm run typecheck` + `npm run build` 通过。
- 【状态 2026-09-23 · Push 174（C9-02 引用守卫：有项目在用的字典条目不给删；契约 + 后端 + 前端同批 · 业务定调「当存在项目的类型或者地区在卡片里面的时候 不允许在这里删除」）】① 契约 —— `DictItemSchema` 新增 `usageCount`（引用该码的**未删除项目**数；region → `projects.region`、projectType → `projects.project_type`，其余维度恒 0），`shared/src/common/errors.ts` 新增 `DICT_ITEM_IN_USE`（409，`HTTP_STATUS_BY_ERROR_CODE` 同步），`shared/src/openapi.ts` 的 delete path 补 409 响应、GET 摘要点明 `usageCount`；生成物重出（paths 72 / schemas 182，`npm run check` 零漂移）。② 后端 —— `dict.repository.ts` 增 `usageCounts(type, client?)`（一次 `group by` 出该维度全部计数：只数 `deleted_at is null` 的项目；维度外的字典类型返回空表）、`dict.service.ts` 的 `toDict()` 逐条带 `usageCount`、`deleteItem()` 在事务内先判引用（> 0 → 抛 `DICT_ITEM_IN_USE`，**不删行、不 touchType、不写审计**）。③ 前端 —— `SelectOption` 增 `deleteDisabledReason`（`SelectMenu.OptionList` 渲染置灰、不可点、悬停出原因的删除位；`deletable:false` 仍是「不渲染」）、`DictSelect.tsx` 的 `deleteBlockReason()` 按 `usageCount > 0` 给文案、`App.errorMessageOf` 对 409 原样透出服务端文案；`npm run typecheck` + `npm run build` 通过。④ 用例与回放 —— `server/test/admin-audit.test.ts` 增 3 例（引用中 409 且不删行 / 不写审计 / 不 touchType；计数为 0 照常删；下发带 `usageCount`），全量 **484 例 / 32 文件**；`poc7-replay.mjs` 增 D13 / D14，**29 项断言全过**（`docs/PoC-7-回放证据(字典C9与审计留痕C7).md` 已重出）；`check:boundaries` 155 文件 / 634 依赖 / 0 违规、`check:permission-matrix`、`check:db-schema` 全绿。跨线改动（`shared/` + `server/`）请 wmj 评审。

### 3.3 M2 项目纵切（3 周，h1 / h2）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M2-01 | 项目 CRUD：编号唯一（409 PROJECT_CODE_EXISTS）、seq_no 分配、乐观锁、软删 / 归档写保护 | wmj |
| M2-02 | 建项目事务：按项目类型蓝图快照生成 stages + nodes + requirements（PoC 9 前半） | wmj |
| M2-03 | 阶段推进：门禁校验 + 推进 / 回退 + 留痕 | wmj |
| M2-04 | 首页列表 / facets / 时间区间 / 排序（与前端 URL 参数全对齐） | wmj |
| M2-05 | 项目成员与记录级权限（非成员 404 语义） | wmj |
| M2-06 | 视图（个人 / 公共）、关注订阅、用户偏好 | wmj |
| M2-07 | 前端联调：首页 mock 下线、错误态、字典驱动主题色 | px |

**新增表**

| 表 | 关键字段 |
|---|---|
| project_members | project_id、user_id、role_in_project、joined_at |
| project_views | owner_id / scope（个人 / 公共）、filters、columns、sort、group、is_default |
| follows | user_id、object_type（project / task）、object_id、created_at |

**接口**

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /projects | 已入契约；补「导入蓝图版本」与成员初始化 |
| GET | /projects/{id} | 详情（含 stageKey / status / description / updatedAt） |
| PATCH | /projects/{id} | 已入契约；乐观锁 version |
| DELETE | /projects/{id} | 软删（**已入契约，A5 · Push 49**）：`If-Match` 回传 version 防误删；seq_no 不回收、code 唯一性保留（同编号再建仍 409） |
| GET | /projects/{id}/stages | 九阶段状态与完成度 |
| POST | /projects/{id}/stages/{key}/advance | 阶段推进（422 返回缺项明细） |
| GET / POST / DELETE | /projects/{id}/members | 成员维护 |
| GET | /projects/facets | 与列表同口径计数（权限过滤后） |
| GET / POST / PATCH / DELETE | /views | 视图 CRUD |
| GET / POST / DELETE | /follows | 关注 / 取关 |

**关键设计**

- 建项目事务（单事务）：校验 code 唯一 → `nextval` 分配 seq_no → 取该项目类型「已发布蓝图版本」→ 生成 stages / nodes / requirements（node_key 唯一防重，重复导入幂等）→ 写审计 → 同事务写 outbox（启动通知）。
- 阶段推进算法（**ADR-023**：项目经理手动触发 + 门禁校验 + 缺项明细，不做自动推进）：锁 stage → 校验该阶段全部任务完成且必要成果文件满足（复用 GateService）→ `project_stages.status = done`、下一阶段 active、`projects.stage_key` 前移 → 审计 + outbox；失败返回缺项清单（422），不部分推进。
- 列表与 facets 一致性：列表与计数共用一个 QueryBuilder（同 filter 构造器 + 同权限 JOIN），禁止两套 SQL；`filter[timeFrom] / filter[timeTo]` 按 Asia/Shanghai 日界截断（**已入契约，A1 · Push 49**；**Push 175 修订：维度 = `projects.created_at`（项目创建时间），原「最近活动 `updated_at`」口径作废**）；排序支持 `updatedAt` / `createdAt` / `seqNo`（**A9 · Push 69**：白名单补 `createdAt`；**Push 175 修订：缺省序 = `createdAt:desc`（最近创建的在前），原 `updatedAt:desc` 作废；前端 TIME = 维度 × 方向，A9 落地**）；`q` 覆盖编号 / 名称 / 客户 / 序号。
- 项目「时间」语义按 **ADR-022** 执行（`updated_at` 触发事件集：主数据 / 成员 / 阶段 / 任务；文件日报与系统调度不触发），所有列表排序 / 时间筛选依赖该口径。
- 记录级权限：非成员项目在列表 / 详情 / facets / 搜索统一按 404 语义处理。

**出口标准**

- 首页筛选 URL 参数（region / projectType / managerId / timeFrom / timeTo / q / sort）直传后端，facets 与列表计数一致；
- 建项目 → 节点生成 → 阶段推进 → 门禁拒绝可复现；
- 前端移除项目经理 / 主题色硬编码假数据。

### 3.4 M3 任务纵切（3 周，h3 / h4）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M3-01 | 任务列表 / 详情：分页、筛选、排序、快筛、字段裁剪 | wmj |
| M3-02 | 进度与状态：联动、事件留痕、系统置位调度、逾期派生 | wmj |
| M3-03 | 完成门禁：任务侧三入口统一校验（PoC 9 后半）——**已落 Push 143** | wmj |
| M3-04 | 批量操作：批量指派 / 改状态 / 改日期 / 批量完成，部分失败清单 ——**已落 Push 150** | wmj |
| M3-05 | **任务软删已落（Push 152）**：`DELETE /projects/{id}/tasks/{taskId}`（A25，软删 + 统一 404 + 引用守卫 409 `TASK_HAS_REFERENCES` + 组内位次压缩 + 节点释放）；**锁定字段例外调整已落（Push 153）**：`PATCH …/tasks/{taskId}/locked-fields`（A1-17 / C9-07：仅系统管理员 + `reason` 必填并留痕 + 空调整 400；「阶段性里程」一期无列，差异登记）；余「模板实例化与快筛」 | wmj |
| M3-06 | 性能：1 万行数据集压测与索引调优（**脚本与 CI 接线已落 · Push 157**：`scripts/m3-06-stress.mjs` 1 万行数据集 + 9 形状 p50 / p95 阈值 + EXPLAIN 计划 + 旧索引前后对照；**索引下线已定案：0024 下线旧索引 · Push 158**） | wmj |
| M3-07 | 前端联调：表格 15 列、抽屉、进度点击、409 冲突提示 | px |

**接口**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /projects/{id}/tasks | 已入契约；返回 `TaskListItem`（内联 ownerName / changeSummary / fileSummary，**A7 · Push 49**）；补快筛参数（我的 / 今天到期 / 本周 / 逾期 / 未完成 / 未上传成果文件） |
| GET | /projects/{id}/tasks/{taskId} | 任务详情（抽屉全字段 + 文件清单）——**已入契约（A7，Push 49）** |
| PATCH | /projects/{id}/tasks/{taskId}/progress | 已入契约；**A13 · Push 70**：`progress<1` 清空 `actual_end`（清除完成日期的唯一方式）、`progress=1` 缺省按当天；进度离散五档 |
| PATCH | /projects/{id}/tasks/{taskId} | 任务编辑（A10）；**A12 · Push 70** 开放可选 `status`（基础三态，服务端同事务联动进度与完成日期；「已延期 / 提前完成」派生不可写） |
| PATCH | /projects/{id}/tasks/batch | **已落（Push 150）**：批量更新（`TaskBatchBody` / `TaskBatchChanges` / `TaskBatchResponse`；字段白名单 + 逐条独立事务 + 部分失败清单 `failures[]`；批量完成走同一完成门禁；无新错误码） |
| GET | /projects/{id}/tasks/{taskId}/can-complete | **已落（Push 143）**：完成预检（`canComplete` + `missing` + `warnings`；UI 置灰依据） |
| POST | /projects/{id}/tasks/{taskId}/complete | **已落（Push 143）**：完成提交（含门禁；未通过 422 + missing；draft 放行 + warnings） |
| DELETE | /projects/{id}/tasks/{taskId} | **已落（Push 152）**：软删（`tasks.deleted_at` / `deleted_by`）—— 列表 / 看板 / 甘特图 / 完成门禁不可见 + 写留痕；重复删除 / 已删任务上的写操作 = 统一 404；已有引用 409 `TASK_HAS_REFERENCES`：变更记录（`tasks.change_refs`）+ **日报（`daily_reports.task_ids`）与问题（`issues.task_id`）已随 M6-01 ~ M6-03 落表并在守卫处加判定（Push 155）**，`details[].code = change_ref` / `report_ref` / `issue_ref`）|
| PATCH | /projects/{id}/tasks/{taskId}/locked-fields | **已落（Push 153）**：锁定字段例外调整（任务描述 / 英文描述 / 输出成果文件；仅系统管理员 + `reason` 必填并留痕；空调整 400；不改表结构） |

**关键设计**

- 状态与派生：存储基础态三值 + 查询期派生展示五态（不写回，见 v0.2 §2.4）；「进行中」自动置位由每日调度任务写入（**ADR-025**：每日 00:05，进度至少 1 格，不触发项目时间与提醒），`task_events` 标注来源 system（A1-10）；人工修改与回退留痕。**任务写入口径（A12~A14 · Push 70）**：状态只收基础三态（pending / active / done），服务端同事务联动进度与完成日期；进度离散五档、`progress<1` 清空 `actual_end`、`progress=1` 缺省按当天；`onTime` 与展示态服务端读时派生，「逾期未交付 / 逾期已交付」由前端按 `onTime` + `displayStatus` 渲染（派生优先：未完成已过 `plannedEnd` 一律「已延期」）。
- 门禁：`checkTaskCompletion(taskId)` 返回 `{ ok, missing[], warnings[] }`；按 `node_requirements` 逐 doc_type 统计 `files.status ∈ (final, changed)`；存在未定档（draft）时放行但返回 warning 并触发 R02；拒绝写审计（A4-20）。表格 / 看板 / 详情三个入口调用同一服务，服务端为准。**（已落 Push 143）** 实现口径：有节点任务按所属节点 `node_requirements` 统计、无节点任务按自身 `deliverable_types` 兜底（每类 ≥ 1 份，ADR-024）；完成提交与 `PATCH status=done` / `progress=1` 在事务内走同一断言（三入口一致）；拒绝 422 `TASK_REQUIRED_DOC_MISSING` + 缺件明细并写 `task.gate_rejected` / 审计 failed，draft 放行写 `task.draft_doc_reminded`（R02）。
- 批量：预校验 → 分批执行 → 汇总失败清单，避免长事务；逐条校验权限与模板锁定。**（已落 Push 150）** 实现口径：单请求 = 同一组变更应用到 1~100 个任务（`ids` 去重），逐条**独立事务**并复用单条写入内核（锁行 / 版本校验 / 状态联动 / 完成门禁 / 事件 / outbox / 审计），条目级可预期错误降级为 `failures[]`（not_found / archived / gate_not_passed + missing / already_done / version_conflict / invalid_state）而成功项照常生效；**审计双层**：批次一条（`project` 域，summary「批量操作任务：N 条（成功 X，失败 Y）」+ `metadata` = batchId / taskIds / changedFields / 计数 / failures[]，满足 A1-08「批量操作整体写审计日志」）＋逐条字段级一条（`metadata.entry = batch` + 同批 `batchId`）；批量完成（`changes.status=done`）与单条编辑 / `POST …/complete` 共用同一完成门禁；变更白名单不含任务描述 / 成果文件（A1-17）与 `sortIndex`。
- 性能：索引 `ix_tasks_project_stage (project_id, stage_key)`（0001 基线现有）+ `ix_tasks_project_stage_order (project_id, stage_key, sort_index)`（0015 · Push 124，默认读序；**已于 0024 下线 · Push 158**，读面由部分索引 `ix_tasks_active_group` 兜底）+ 稳定排序键；**一期不新增 `tasks.seq`**；默认序 = 阶段序（`STAGE_KEYS` 序，`stage_key` 为空 = 「未分组」落最后）+ 组内位次 `sort_index` + `id`（**A8 · Push 49；A15 / A19 / A20 · Push 124 修订** —— 位次由 `tasks.sort_index` 落库、组内 0 起密集，与看板列内顺序同口径；id 兜底保证分页不跳行）；files 用随行 fileSummary 聚合、避免 N+1；limit 上限；1 万行首屏 50 行 p95 < 500ms 基线，压测脚本入仓。**M3-06 · Push 157 补：压测脚本与索引对照**（`ix_tasks_project_stage_order` vs 部分索引 `ix_tasks_active_group`，逐形状 p50 倍率 <= 1.5x 且默认读序阈值不破；真机对照达标后已由迁移 `0024` 下线旧索引（Push 158））。
- 成果文件多选按 **ADR-024** 执行：任务侧 `deliverable_types`（数组），门禁唯一来源 = `node_requirements`（无节点任务按自身类型兜底）。**（已落 Push 143）**：迁移 0018 + 契约 `deliverableTypes` + 完成 / 预检端点；锁定字段例外调整 **已落 Push 153**（`PATCH …/locked-fields`：仅系统管理员 + `reason` 必填并留痕 + 空调整 400；A1-17 / C9-07）。

**出口标准**

- 门禁拒绝可复现且留痕；三入口判定一致；
- 批量部分失败返回清单且不影响成功项**（已落 Push 150：`test/task-batch.test.ts` 9 例）**；
- 1 万行压测报告入开发日志。

### 3.5 M4 文件与预览（5 周，阶段 7，lan 主线）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M4-01 | 上传会话：分片、断点续传、秒传、过期清理（PoC 1） | lan |
| M4-02 | 版本 / 定档 / 回溯 / 回收站 + 到期清理任务 | lan |
| M4-03 | 多态关联与文件库查询（项目 / 任务 / 日报 / 问题 / 变更双向跳转） | lan |
| M4-04 | 变更申请即通过：事务、R01 回写接口、变更记录与统计 | lan |
| M4-05 | 预览管道：converter 沙箱、队列、缓存、失败降级、短时签名 | lan |
| M4-06 | 二维 CAD 本地转换 PoC（条件性交付，PoC 8） | lan |
| M4-07 | 前端联调：文件库、预览入口、变更申请与记录 | px |

**新增表**

| 表 | 关键字段 |
|---|---|
| upload_sessions | project_id、node_id / task_id、doc_type、name、size、hash、mime、status、expires_at、created_by |
| ~~upload_parts~~ | **不落表**（评审定案，2026-09-18）：分片状态以对象存储 ListParts 为唯一真相，避免双写漂移 |
| file_links | file_id、object_type（project / task / node / report / issue / change）、object_id |
| preview_artifacts | file_id、version_id、pipeline_version、target、status、object_key、error、generated_at |
| ~~change_items~~ | **一期不落表**（变更明细由 `file_versions.change_request_id` + `files.doc_type` 表达：一次变更 = 一个文件 / 一个版本；支持「一次变更多文件」时再落表 —— 同 `upload_parts` 口径，PR-7 评审定案） |

**接口**

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /files/uploads | 发起上传：声明元数据 + 权限校验 + 秒传判定，返回分片签名（`fileId` 省略 = 新建文件；`intent=version` 给出 = 既有 draft 替换 / 追加版本；`intent=change` 必填 = 定档后变更） |
| GET | /files/{id}/uploads/{uploadId} | 断点续传状态（已收分片） |
| POST | /files/{id}/uploads/{uploadId}/complete | 校验分片、合并、哈希比对、建版本与关联（`intent=change` 完成时同事务生效） |
| GET | /projects/{id}/files / /files/{id}/versions | 文件库查询与版本链（文件创建 = 上传入口） |
| POST | /files/{id}/finalize | 定档锁版 |
| POST | /files/{id}/rollback | 回溯生成新版本 |
| DELETE / POST | /files/{id}（回收站）/ /files/{id}/restore | 软删与恢复（30 天） |
| GET | /files/{id}/preview | 预览状态 + 短时签名 URL |
| POST / GET | /changes | 变更申请（申请即通过）与记录检索 —— 契约实况：变更经上传入口 `intent=change` 生效，读面 `GET /projects/{id}/change-requests` / `GET /change-requests/{id}`；任务变更关联（R01）读面派生、按 change id 去重 |
| GET | /files/{id}/download | 下载（鉴权 + 审计） |

**关键设计**

- 上传：api 只做签名与元数据，分片直传对象存储（S3 兼容；沙箱 MinIO pinned，生产实现待选 —— 见 ADR-006 阶段 0 修订）；complete 时校验分片完整性、合并、`contentHash` 一致性（与 init 声明不一致返回 422 `FILE_HASH_MISMATCH`），再由服务端把暂存对象（`…/staging/{sessionId}`）复制（CopyObject）到契约键（`…/v{seq}/{contentHash}.{ext}`）后写 `file_versions` + `file_links` + 审计 + outbox（预览任务）。断点续传 = 查询已收分片；过期会话与孤儿分片由存储侧兜底清理 + 应用侧定时任务，**不依赖存储 lifecycle 规则**。上传入口按 `intent` 分派（`UploadCreateBody` · Push 130 定案）：`fileId` 省略 = 新建文件；`intent=version` 给出 = 对既有 draft 文件替换 / 追加版本；`intent=change` 必填 = 定档后变更（目标须 final / changed）——状态不符 409 `FILE_STATE_INVALID`、不存在 / 无权 404、与 `projectId` 不一致 400。
- 版本与状态机：`draft → final → changed → archived / recycled`；定档锁 `current_version`；回溯生成新版本不删历史；回收站到期清理任务（默认 30 天，可配置）。
- 变更（A4-13 / A4-14）：单事务完成 `change_requests` + 新版本 + `files.status = changed` + 任务 `change_ref` 回写（R01）+ 审计 + outbox（通知）；缺「变更后文件」直接 422（CHANGE_FILE_REQUIRED）；幂等键 = 申请表单指纹；**变更关联（R01）**：写面 `tasks.change_ref` 落「最近一次变更」（单列覆盖式、不递增任务乐观锁 `version`），「追加 + 去重」由变更读面按「同项目 + 变更文件 `doc_type` 命中任务成果类型」派生；`change_items` 一期不落表。
- 预览：worker 领 outbox `preview.job` → 调 converter（无 DB、无外网、只读根文件系统、超时与资源限额）→ 产物回 MinIO → 更新 `preview_artifacts`；缓存键 = 内容哈希 + `pipelineVersion` + `target`（三元组，与 v0.2 §5.1 对象键 / 契约 `PreviewTarget` 对齐；`target` 三值 pdf / image / structured，口径见 ADR-007）；失败记录 reason 并降级「请下载」（D2-05）；定档时预生成（P1）；预览地址短时签名 + 审计 + 禁匿名（D2-04）。
- **converter 运行形态与落点（2026-09-23 px 线交付件登记，待 wmj 复核）**：converter = 独立容器 `deploy/preview/`（沙箱基线：`internal` 网络真无外网、只读根文件系统、非 root、`cap_drop: ALL` + no-new-privileges、独立 tmpfs 工作目录、2C2G / 并发 2 / 队列 8 / 硬超时 60s 全可配，生产限额按 ADR-013 复核）；镜像 `libiaolink/preview-converter:<X.Y.Z>` 与 `PREVIEW_PIPELINE_VERSION` **必须同值、任何镜像内容变化一并递增**（该值进缓存键，换镜像不递增 = 读到旧产物）；中文字体 = `fonts-noto-cjk` + fontconfig 宋体 / 黑体映射（构建期 `fc-match` 门禁，映射失效即构建失败）；内部接口 = **字节流进 / 字节流出**（不触对象存储、不携 S3 凭证）、`POST /convert`（`target` = pdf / image / structured，`structured` 一期 501、xlsx 走 pdf）+ `GET /healthz`（soffice 与字体自报，容器健康检查与 worker 自检共用），错误码 400 / 413 / 415 / 422 / 501 / 503 / 504；worker 侧 ENV = `PREVIEW_CONVERTER_URL` / `PREVIEW_PIPELINE_VERSION` / `PREVIEW_CONVERT_TIMEOUT_MS` / `PREVIEW_CONVERT_MAX_ATTEMPTS`（客户端超时建议 90s，留出容器内 60s 硬超时的余量）；dev 形态由哑中继发布 `127.0.0.1:9900`，生产形态删中继、worker 与 converter 同处 internal 网络；完整契约与镜像台账见 `deploy/preview/README.md`，实测证据见 `docs/m4-05b-回放证据(预览转换器沙箱deploy-preview).md`。
- 预览 / 下载 / 定档 / 变更全部写审计（C7）。 **（Push 160 定案）** 审计动作扩 `download`（A4-10「离线下载受权限控制并记日志」）—— 与 `preview`（D2-07）同口径：契约 `AUDIT_ACTIONS` 十值、库侧 `ck_audit_logs_action` 随 M4-05 迁移一次扩值（preview + download，只追加不回改 0013）；预览权限口径 = 「项目可见即可」（同文件详情 / 版本链），下载受 `file.download` 权限点约束；outbox 领取器切片（PR-10）只落「领取 + 消费 + 重试 + dead」，不含规则 / 通知编排（避免与 i5 重复实现）。

**出口标准**

- 200MB 续传演示、断网恢复、秒传命中、并发定档 409、回收站恢复；
- 转换成功率 ≥95% 且中文不乱码（PoC 1）；CAD 达标上线、不达标降级不阻塞（PoC 8）。

### 3.6 M5 自动化与通知（5 周，阶段 7~8，lan + wmj）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M5-01 | 规则模型 + 条件求值 + 执行器 + 回放器（PoC 3）——**内核切片已落 Push 161**（条件求值 / 触发窗口 / 幂等键 / 回放器 + R02 ~ R07 金标；余：A01 / A02 / A03 / A14 文案与调度接线） | wmj |
| M5-02 | 调度 / 补发 / 幂等执行键 / 状态回退再生窗口 | lan |
| M5-03 | 企微通道：应用消息、群机器人、token 管理、限速（PoC 2） | lan |
| M5-04 | 站内信 + SSE + 稍后提醒 + 合并免打扰 + 每日上限 | lan |
| M5-05 | R01~R07 + A01 / A02 / A03 / A14 落地 + 金标用例 ——**R02 ~ R07 金标回放已落 Push 161**（R01 随 M4-04 落地；余：A01 / A02 / A03 / A14） | wmj |
| M5-06 | 规则管理接口、发送记录、重发 | lan |
| M5-07 | 前端联调：消息中心、规则管理页 | px |

**新增表**

| 表 | 关键字段 |
|---|---|
| automation_rules | code、enabled、trigger、conditions、actions、version |
| automation_runs | rule_id、entity、dedupe_key、命中 / 求值 / 动作结果、channel、时间 |
| notifications | user_id、type、title、body、object_ref、status（未读 / 已读 / 已处理）、remind_at |
| notify_prefs | user_id、免打扰时段、每日上限、合并窗口 |

**关键设计**

- 规则引擎：JSON 规则模型（触发器 / 条件 / 动作）+ 白名单操作符求值器 + ClockService 注入 + 幂等执行键（规则 ID + 实体 ID + 触发窗口）+ 回放器（给定数据集与时间 → 应发送清单）+ 金标文案逐字比对（C2-02）。
- 触发：事件型（业务事务写 outbox → worker 消费）、调度型（cron + 窗口 + `last_run_at` 补发）、手动补跑；状态回退再生新窗口（避免重复轰炸）。
- 投递：ChannelAdapter（企微应用消息 / 群机器人 / 站内信 / 邮件预留）；access_token 缓存刷新；群机器人队列限速（20 条 / 分钟，实测确认）；失败降级站内信（C5-04）；合并窗口 + 免打扰次日补发 + 每人每日上限（C2-09）。
- 站内信：收件箱 / 分类 / 未读计数 / 已读 / 已处理 / 稍后提醒（定时生成新消息）/ 全量留档（含企微成功发送）。
- SSE：通知与导出进度共用；断线重连与幂等游标。

**出口标准**

- 杀 worker 不丢消息、重复领取不重发、限速实测不超限；
- R03~R07 金标回放全绿（跨天、节假日、改期、补发）。

**进展**

- 【状态 2026-09-24 · Push 161（wmj 线）】M5-01 内核切片落地（纯函数：不连库、不取系统时间）：① **契约** `shared/src/modules/automation.ts` —— 规则模型（code / name / enabled / trigger / conditions / actions / version）与枚举（`AUTOMATION_RULE_CODES` = R01 ~ R07 + A01 / A02 / A03 / A14、`RULE_TRIGGER_KINDS`、`RULE_EVENT_TOPICS` 7 个 outbox 主题、`RULE_SCHEDULE_WINDOWS` = T_MINUS_1 / SAME_DAY / T_PLUS_1 / WEEKLY、`RULE_CONDITION_OPERATORS` 13 个白名单操作符、`RULE_ACTION_KINDS`、`NOTIFY_CHANNELS`、`RULE_RECIPIENTS`、`AUTOMATION_RUN_STATUSES`；**本切片尚无端点，paths / operations / schemas 计数不变**，生成物零漂移）；② **服务端** `server/src/modules/automation/` —— `automation.rules.ts`（条件求值 / 模板逐字渲染（缺变量显性抛错）/ 触发窗口求值（顺延走 calendar `evaluateOffset`）/ 幂等执行键 `dedupeKey(规则码, 实体, 窗口)`）、`builtin-rules.ts`（R02 ~ R07 六条内置规则 + 8 种逐字模板）、`automation.replay.ts`（回放器：业务日 + 任务数据集 + 日历 → 应发送清单，逐条给命中 / 跳过原因；R07 同负责人合并到收件人粒度）；③ **用例** `server/test/automation-rules.test.ts` 16 例 + `test/automation-replay.test.ts` 15 例（金标文案逐字、节假日顺延两态、跨周幂等、同规则多渠道路由各自成条），全量 **532 例 / 35 文件**；`check:boundaries` 160 文件 / 651 依赖 / 0 违规、`check:permission-matrix`、契约漂移检查全绿。**口径登记**：`docs/rules/R01-R07-内置规则文案.md` 增 R07 合并模板 `R07_APP_MERGED` 与待确认项 6。**余**：事件型规则在回放中以「任务处完成态 = 完成事件已发生」近似（真实事件由 outbox 承载 · M5-02）、A01 / A02 / A03 / A14 文案与调度接线未接。
- 【状态 2026-09-24 · Push 162（wmj 线 · M6-01 收口）】日报「当日汇总（A7-01）」与「应填未填清单（A7-05）」两条读接口落地（前端日报中心、A01 提醒与 A02 每日 19:00 群推送的**共用数据面**）：① **契约** `shared/src/modules/reports.ts` 增 `DailyReportDayQuery`（内联 `date` 查询：缺省 = 今天（Asia/Shanghai）、未来日期 400）/ `DailyReportRosterEntry`（名册成员当日状态）/ `DailyReportMissing` / `DailyReportSummary`（二者复用 `DailyReport`、`CalendarDayKind` 与 `ProjectMemberRole`），`openapi.ts` 注册 `GET /api/v1/projects/{id}/reports/summary` 与 `GET /api/v1/projects/{id}/reports/missing`（tags = reports，`report.view`）—— **paths 72 → 74、operations 93 → 95、schemas 182 → 185**，生成物重出、零漂移。② **服务端** `src/modules/report-issue/report-summary.service.ts`（`ReportSummaryService`）：一次 `ReportRepository.listByDate` 取当日全量行 —— 汇总**只算已提交**（submitted / supplement；草稿只计数不进正文与人数）、`headcountTotal` 未填按 0 计、`issueCount` 计「现场发现问题」非空（空白串不算）、`entries` 提交时刻升序（同刻按作者 id 兜底）；应填未填以**项目名册为应填范围**（新增依赖 project 出口 `ProjectMemberService` 与 calendar 横切出口 `CalendarService` —— 工作日判定 D5-03 同源），草稿未提交仍计未填、**非工作日整列为空（不催报）**；静态段 `summary` / `missing` 路由注册在 `:reportId` **之前**（Nest 按声明顺序匹配）。③ **测试与门禁** `test/report-summary.test.ts` **14 例**（仓储 / 日历 / 名册全替身，不连库）→ 全量 **546 例 / 36 文件**；`check:boundaries` 161 文件 / 663 依赖 / 0 违规、`check:permission-matrix`（6 角色 / 78 条目 / 31 键）、契约漂移检查（paths = 74 / schemas = 185）全绿。④ **真机** `scripts/m6-replay.mjs` 证据五（S0 ~ S6）：先落 1 名项目成员（M2-05）→ 当日汇总（entryCount / headcountTotal / issueCount / entries）/ 缺省日期 = 今天 / 未来日期 400 / 已提交不进漏填名单 / 工作日无日报 → 名册全员进名单 / 非工作日 → 整列为空（工作日与休息日由日历接口自证，不硬编码星期）—— 本地无 PG，真机由 CI `database` job 执行。⑤ **余**：提醒与每日 / 每周发送接线（A01 / A02）随 M5 通知（lan）；漏填名单的「已提醒」标记随 M5-06 发送记录（lan）。

### 3.7 M6 日报 / 问题 / 干系人 / 工作台（4 周，wmj 线，可与 M4 / M5 并行）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M6-01 | 日报：草稿 / 提交 / 补填、模板、19:00 汇总、漏填名单（**填报 / 草稿 / 提交 / 补填已落 · Push 155**：项目嵌套四端点 + 一人一天一条 + 未来日期 400；模板 A3-05 随 M5；**「19:00 汇总」（A7-01）与「漏填名单」（A7-05）的读数据面已落 · Push 162**（`GET …/reports/summary` + `GET …/reports/missing`，名册 × 日历 × 当日状态；发送 / 提醒接线随 M5 通知）） | wmj |
| M6-02 | 日报回写任务进展与问题自动生成（幂等）（**已落 · Push 155**：A3-08 回写 `tasks.note` + `task_events(note_change)`、A3-09 `uq_issues_source_report` 兜底；两件副作用均幂等） | wmj |
| M6-03 | 问题闭环：四态、分派、SLA、升级、关闭、看板、统计（**四态 / 分派 / 关闭 / 留痕已落 · Push 155**（问题追踪表与看板同源，前端看板随 M6-06）；SLA `dueAt` 一期仅落库 —— T+1 提醒 / T+3 升级（A3-14）随 M5、关闭确认（A3-15）与统计（A3-17）随后续切片） | wmj |
| M6-04 | 干系人：台账、项目关联、导入、隐私字段权限、导出（**台账 / 项目关联 / 隐私字段权限已落 · Push 144（j6）**；导入 A5-05 随 M8-01、导出 A5-09 随 M7-03） | wmj |
| M6-05 | 工作台：我的任务 / 待办 / 关注动态 / 快捷入口 | wmj |
| M6-06 | 前端联调：日报、问题看板、干系人、工作台 | px |

**新增表**

| 表 | 关键字段 |
|---|---|
| daily_reports | project_id、author_id、report_date、done_work、plan、headcount、state（草稿 / 已提交 / 补填）、submitted_at、found_issue / issue_category / suggestion、task_ids（**已落 0023 · Push 155**：列名以迁移为准 + `uq_daily_reports_author_date` 一人一天一条） |
| daily_report_files | report_id、file_id、kind（现场图 / 问题图） |
| issues | project_id、task_id、category、state（四态）、due_at、owner_id / owner_department、reporter_id、raised_at、solution、source_report_id（唯一）、closed_by / closed_at（**已落 0023 · Push 155**） |
| issue_events | issue_id、event_type（created / state_change / solution / assignment）、from_state / to_state、actor_id、note、created_at（**已落 0023 · Push 155**） |
| issue_dispatch_rules | category → department / assignee（一类对多） |
| stakeholders / project_stakeholders | 台账字段 + 多对多关联（**已落 0019 · Push 144**：公司分类四值 + 联系方式 + 备注；判重走应用层提示） |
| todos | user_id、title、object_ref、remind_at、repeat_rule、status |

**关键设计**

- 防重提交：同人 + 同项目 + 同日期唯一约束；补填保留原始记录并标注。**（已落 Push 155）** 实现口径：`uq_daily_reports_author_date`（同项目 + 同人 + 同日期）；重复填报 409 `REPORT_ALREADY_EXISTS`（提示改用编辑）；补填 = 对过去日期首次提交（`state = supplement`，服务端按日期推导），一期不做版本历史 —— 「补填保留原始记录」以状态 + `submittedAt` 表达（差异登记）。
- 应填未填：项目成员 × 工作日历 × 当日未提交清单；提醒记录可查（A7-05）。
- 回写与生成幂等：日报 → 任务进展描述（来源标注 + 幂等）；日报 → 问题自动生成（`unique(source_report_id)` 兜底），重编辑不重复生成（A3-09）。**（已落 Push 155）** 实现口径：① A3-08 追加「当日完成工作」到关联任务的「项目进展描述」（`tasks.note`）+ `task_events(note_change)`，**幂等标记 `【日报 <日期>】`**（同任务同日期只追加一次），只动 note、不动任务乐观锁（与 R01 同口径）；② A3-09 生成 `state = unassigned` 的问题（标题超 500 字截短，原文留在日报行），并写 `created` 事件 + 审计 + outbox `issue.created`；两件副作用在提交事务内完成、重复提交不重复生效。
- SLA：`due_at` 按归类配置计算（§7 第 11 项）；T+1 提醒、T+3 升级（走规则引擎 + 工作日历）；超时未确认自动关闭并单独标注（A3-15）。
- 干系人隐私字段：列表 / 导出 / 搜索 / 消息内容同一策略，无权字段不返回（A5-07、C3-08）。**页面出口已随 j6 落地（Push 144）**：台账列表 / 详情 / 关联反查即真实出口（permission `projectFields`，无权**键不存在**）；导出 / 搜索 / 消息出口（M7 / M5 · lan）复用同一策略表与投影入口。

**出口标准**

- 填报 → 问题生成 → 分派 → 处理 → 关闭全链路演示（**一期已具备 · Push 155**：项目嵌套四端点 + 幂等副作用 + 四态留痕）；
- 漏填提醒与升级记录可查；跨节假日用例通过。

### 3.8 M7 搜索 / 仪表盘 / 导出（4 周）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M7-01 | 搜索投影 + 分词 PoC + 回填脚本 + 高亮 + 权限过滤 | lan |
| M7-02 | 仪表盘 B4 与专项统计 B5（含风险提示阈值） | lan |
| M7-03 | 导出作业框架 + 五处导出接入（C4-05、B5-05、A7-04、A5-09、A4-15） | lan |
| M7-04 | 归档：门禁、清单、只读保护、检索 | wmj |
| M7-05 | 前端联调：搜索、仪表盘、导出入口 | px |

**关键设计**

- 搜索：`search_docs`（doc_type、project_id、title、body、meta、权限标签、tsv）+ GIN 索引；业务写入同事务维护投影（C6-04）；回填 / 重建脚本；分词方案按 PoC 定案（§7 第 14 项）；`ts_headline` 高亮；结果与字段按权限裁剪。
- 仪表盘：B4 每张卡片定义 SQL 口径（按时交付率分母、平均解决时长、TOP N、7 日到期、四格分布、下钻明细）；B5 风险提示阈值可配置；统计与列表同权限过滤。
- 导出：统一异步作业（jobs kind = export → worker 生成 → MinIO 临时对象 → 短时下载 URL → 过期清理）；进度 SSE；字段级过滤 + 审计 + （二期）水印。
- 归档：归档动作 = 门禁校验 → 生成归档清单 / 目录 → 项目状态 archived → 全写接口只读拦截；归档检索；归档包二期（C4-04）。

**出口标准**

- 搜索、统计、导出三处与页面权限口径一致（同一策略服务）；
- 导出审计可查、临时对象按期清理。

### 3.9 M8 迁移与上线（4 周，阶段 9）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M8-01 | 迁移工具链：导入模板、校验、干跑报告、分批入库、幂等重导、附件包 | lan |
| M8-02 | 全量演练与对账报告（PoC 7） | lan |
| M8-03 | 部署 / 备份 / 恢复演练 + 监控告警 + 值班 | px |
| M8-04 | 上线检查单、灰度与回滚预案 | px |
| M8-05 | 前端 E2E 全链路与验收 | px |

**关键设计**

- 导入：Excel / CSV 模板（任务、干系人、历史项目）→ 校验（必填、字典匹配、节点匹配模板）→ 干跑报告（脏数据处理建议）→ 分批入库 → 幂等重导 → 附件包上传与关联 → 对账报告（条数 / 文件数 / 金额）。
- 上线：空库迁移演练、全量演练、前滚修复预案、恢复演练（两人会操、季度一次）、告警接线（死信、转换失败、企微失败、outbox 积压）。

**出口标准**

- PoC 7 证据（真实数据 + 附件全量导入 + 报告可读）；
- 恢复演练与上线检查单完成签认。

---

## 4. 横切机制

### 4.1 契约变更三类处理

- 增量（新增可选字段 / 新端点）：单 PR 完成，源与生成物同批（`CONTRIBUTING.md` §14）。
- 破坏性（改必填 / 改名 / 改语义）：必须 ADR + 前后端同批改造 + 兼容窗口，禁止静默变更。
- 生成流程：改 `shared/src` → `npm run generate` → 提交 `generated/`；CI 的 `shared` job 漂移检查必须绿。

### 4.2 迁移与 schema 同步

- 迁移编号顺延 `0004+`，只追加、合并后不可修改（`CONTRIBUTING.md` §14）；采用 expand / contract（先扩展后收敛）。
- 种子数据（九阶段、成果文件十类、问题归类、角色权限、蓝图节点）用独立可重跑脚本，不进迁移。
- 应用角色 `libiaolink_api` 无 DDL 权限（`database/README.md`）；迁移由独立任务执行，应用启动不改结构。
- 每张含迁移的卡片必须跑：`node database/scripts/migrate.mjs --dry-run`（演练库）→ 空库全量 → `npm run check:db-schema`。

### 4.3 模块实现骨架与事务边界

- 四层：controller（HTTP / Guard / Zod 管道）→ service（用例与事务边界）→ repository（Drizzle）→ events（同事务写 outbox）；对外只经 `index.ts`（`npm run check:boundaries`）。
- 一个业务用例 = 一个事务：业务表 + outbox + 搜索投影（同库）同事务；外部副作用只能由 worker 执行（v0.2 §1.3）。

### 4.4 幂等、乐观锁与并发

- 写接口支持 `Idempotency-Key`：落表 + 请求指纹 + 响应快照；同键并发串行化并返回首次结果（契约错误码 IDEMPOTENT_REPLAY）。
- 更新携带 `version`：冲突 409（VERSION_CONFLICT）；节点/阶段/任务/项目统一。
- 队列并发：`FOR UPDATE SKIP LOCKED`；单例调度：advisory lock；批量为「预校验 + 分批执行 + 失败清单」，不打包长事务。

### 4.5 权限策略服务（ADR-011 落地形态）

- 四个出口：`can(actor, action, resource)`、`buildScopeWhere(actor)`、`fieldPolicy(entity, actor)`、`exportPolicy(actor, dataset)`。
- 策略表驱动 + 内存缓存 + 版本号失效；Guard 声明式接入；查询层统一注入数据范围，禁止各模块自写权限 SQL。
- 五出口一致：列表 / 详情 / 导出 / 搜索 / 通知内容；无权字段不返回；资源不存在与无权统一 404。
- 越权尝试单独留痕并告警（C7-03）；临时授权到期自动收回（C3-06）。
- **一期落地形态（Push 178 · 业务口径 2026-09-23「我们当前这个系统就不要考虑权限」）**：环境开关 `PERMISSION_ENFORCED`（默认 `false`）在**唯一注入点** `RoleService.getActorAuthorization()` 短路 —— 授权画像一律等效管理员（`admin` 角色码 + 数据范围 `all` + 契约全量权限位），上述四个出口与角色内硬检查（task / flow / file 三处读 `roleCodes`）随之整体放开；置 `true`（二期）回到本节的 ADR-011 判定，无代码改动。只影响**裁定**、不动数据（用户偏好仍按账号各存一行）；两态真机对照见 `docs/权限开关回放证据(PERMISSION_ENFORCED).md`。

### 4.6 审计

- 统一 `audit_logs`：actor、IP、UA、action、object、before / after / diff、entry（页面 / 批量 / 系统 / API）、trace_id、result。
- 追加写：api 角色仅 INSERT；保留 ≥6 个月 + 清理任务；高风险导出重认证（C7-04）。
- 字段级修改历史（C7-02）由 service 层 diff 生成，任务详情可直接查询。

### 4.7 时间与时钟

- 存 UTC（timestamptz），展示与业务计算按 Asia/Shanghai；业务日期用 date。
- ClockService 注入：规则、调度、逾期判定禁止直接取系统时间，保证可回放与金标测试。**已落地（h8 / Push 99）**：`server/src/common/clock/clock.service.ts`（`now()` / `today()`，测试可 `setSource` 注入固定时刻）；工作日历（求值基准缺省 = 今天）为首个接入模块，既有模块（task 等）随各自改动迁移。
- 工作日历（D5）提供节假日 / 调休与 T-1 / T+1 计算，提醒顺延按配置规则执行。**已落地（h8 / Push 99）**：`calendar_days`（只存例外）+ `calendar_settings`（单行顺延配置）+ `GET /api/v1/calendar/*`（单日判定 / 顺延 / T-N·T+N 实时求值）；规则引擎侧接线随 i8 / i9。

### 4.8 错误码增补清单（在 v0.2 §7.2 之上）

| code | HTTP | 场景 |
|---|---|---|
| STAGE_GATE_NOT_PASSED | 422 | 阶段推进门禁未通过（含缺项明细） |
| TASK_REQUIRED_DOC_MISSING | 422 | 任务完成缺必交成果文件（含 missing） |
| TASK_HAS_REFERENCES | 409 | 任务已产生变更记录（`tasks.change_refs` 非空）不允许删除（系统功能书 A2-01；M3-05 · Push 152）|
| CHANGE_FILE_REQUIRED | 422 | 变更缺「变更后文件」 |
| FILE_STATE_INVALID | 409 | 文件状态不允许该操作（已有；补回收站 / 回溯路径 + 上传目标文件状态：`version` 须 draft、`change` 须 final / changed） |
| UPLOAD_INCOMPLETE | 409 | 上传会话分片未齐（需补传后重试） |
| UPLOAD_SESSION_EXPIRED | 410 | 上传会话过期，需重建 |
| FILE_HASH_MISMATCH | 422 | 合并后哈希与声明不一致 |
| REPORT_ALREADY_EXISTS | 409 | 同一人同一项目同一天已有一条日报（A3-02 一人一天一条；重复填报提示改用编辑）—— M6-01 · Push 155 |
| DICT_ITEM_IN_USE | 409 | 字典条目正被项目引用，不允许删除（A3 引用守卫 · Push 174：`DELETE /dicts/{type}/items/{code}` 事务内判 `usageCount > 0` —— 不删行、不写审计；前端同款判定把删除位置灰） |
| DICT_VALUE_UNKNOWN | 422 | 字典取值不合法，或该取值对应的字典条目已删除（Push 173 起删除 = 物理删行；存量值仍按原码渲染，仅新写入被拒） |
| DUPLICATE_SUBMISSION | 409 | 同人同场景重复提交（日报等） |
| ISSUE_TRANSITION_INVALID | 409 | 问题状态流转不允许（**一期不做 · Push 155 差异登记**：问题四态允许回退、不设流转白名单，每次变更写一条 `issue_events` 即留痕 —— 保留本错误码给二期可能的流转约束） |
| RATE_LIMITED | 429 | 触发限流（登录 / 导出 / 搜索 / 预览签名） |
| MFA_REQUIRED | 403 | 高风险操作需重认证 |

### 4.9 测试与 CI 分层

- 现有：`frontend` / `shared` / `server` / `database` 四 job（`.github/workflows/ci.yml`）；`database` 已并入「空库迁移 + Drizzle 漂移检查」红线（Push 48）。
- 待补（L2 余下）：文件管道与 outbox 的集成测试（真实约束与并发）。
- 权限矩阵作为必跑用例（每条数据路由允许 + 拒绝），失败即阻断合并。
- L3 夜间：E2E 关键路径、恢复演练、依赖漏洞扫描、性能基线（1 万行任务表、facets 聚合）。
- 规则引擎以「回放 + 金标文案」为准入测试，不依赖真实企微。

### 4.10 可观测性

- 指标：outbox 积压 / 死信数、转换队列深度与失败率、企微发送失败率与限速触发、慢查询 TOP、导出失败、会话与 401 突增。
- 日志：Pino JSON + traceId 透传（已有）；审计与业务日志分离。
- 告警路由按 `团队分工.md` §7（px 服务 / 部署、lan 数据与投递、wmj 领域逻辑）。

---

## 5. 协作节奏与联调

### 5.1 周节奏

| 时间 | 事项 |
|---|---|
| 周一上午 | 计划与口径会：集中处理待定案口径（默认 1 小时），确认本周卡片与依赖 |
| 周一~周四 | 开发；每日 11:00 / 17:00 两个合并窗口（`团队分工.md` §5） |
| 周三 | 联调日：前端接真、错误态回归 |
| 周五 | 验收演示 + 文档同步检查 + 非紧急合并冻结（17:00 后） |

### 5.2 联调流程

- 契约合并 → 前端接真（保留 mock 开关便于离线开发）→ 卡主责提供演示数据（种子 / 演示项目）→ px 按验收清单执行（含 404 / 409 / 422 / 429 错误态）→ 证据（截图 / 无头实测）进开发日志。
- 前端接入顺序：M2 首页 → M3 任务表与抽屉 → M4 文件库 / 预览 → M5 消息中心 → M6 日报 / 问题 → M7 搜索 / 仪表盘。

### 5.3 文档同步矩阵

| 变更类型 | 必须同步 | 责任 |
|---|---|---|
| 契约 / 字段 | `前端功能需求.md` §三 + `字段对照清单.md` + 生成物 | wmj + px |
| 业务口径 | `系统功能书.md`（与源文档双处同步） | 变更提出人 |
| 架构 / 数据模型 | `技术设计v0.2` / `技术设计v0.3` + ADR | wmj |
| 每次推送 | `开发日志.md` 顶部条目（日期 时间 / 作者 / 分支 / Push 序号） | 推送人 |

### 5.4 评审与高风险

- 高风险（迁移、鉴权、契约、CI / 部署、Secrets）按 `CONTRIBUTING.md` §15 必须非作者批准；`CODEOWNERS` 自动路由。
- ADR 状态随 PR 合并转「已采纳」；被取代时新增 ADR 并回写旧条目。

---

## 6. 验收与风险

### 6.1 里程碑出口标准汇总

| 里程碑 | 一句话验收 |
|---|---|
| M0 | 十五项口径全部有结论，蓝图 / 字典 / 规则文案定稿 |
| M1 | 真实登录可用；权限矩阵、幂等、outbox 三项证据齐 |
| M2 | 首页全真实数据；facets 与列表一致；建项目 + 阶段推进可演示 |
| M3 | 三入口门禁一致；批量失败清单可用；1 万行性能达标（**压测脚本 + CI 真机执行 · Push 157 ~ 158**：阈值与索引断言随每次 PR 常跑、旧索引由 0024 下线；证据 `docs/m3-06-压测证据(CI).md`） |
| M4 | 200MB 续传 + 预览成功率 + 变更即通过全链路 |
| M5 | 金标回放全绿（**R02 ~ R07 先行口径已随 Push 161 回放全绿**；余 A01 / A02 / A03 / A14）；限速与降级实测（随 M5-03 企微 · lan） |
| M6 | 日报 → 问题 → 关闭闭环（**一期主链路已随 Push 155 落地**：填报 → A3-09 自动生成 → 分派 / 流转 → 关闭留痕）；SLA 升级可查（T+1 / T+3 随 M5 规则引擎） |
| M7 | 搜索 / 统计 / 导出与页面权限一致；导出审计可查 |
| M8 | 真实数据导入 + 对账 + 恢复演练 + 上线检查单 |

### 6.2 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| M0 口径拖延 | 全部卡片阻塞 | 十五项带建议值开会，未决项按建议值先行并在 ADR 标注可修订 |
| R01~R07 文案未入仓 | 规则引擎无法验收 | M0-04 强制入仓 + 金标骨架先行 |
| 蓝图按项目类型 vs 全局 | 建项目事务返工 | 先定案再改契约与 DDL，再写 M2-02 |
| 文件管道人力单点（lan） | 关键路径 | PoC 1 / 4 / 5 提前；M4 期间 wmj 承接 M6 |
| M4 / M5 串行风险 | 上线推迟 | M5 规则框架在 M3 后先开工，文件事件类规则后接 |
| 迁移数据质量 | 上线延期 | M7 末尾先做一次干跑，M8 全量演练 |
| 前端 mock 与契约漂移 | 联调返工 | 契约冻结后前端只消费生成类型，禁止手写模型 |
| 企微限速 / 失败 | 消息丢失 | 队列限速 + 站内信兜底 + 死信告警（M1 已具备 outbox） |

### 6.3 上线检查单（最小集）

- 空库迁移、`check:db-schema`、权限矩阵、金标回放、恢复演练五项全绿；
- 备份（PG + MinIO）与恢复演练记录、告警接线、值班表；
- Secret 全部在环境变量或密钥管理（`CONTRIBUTING.md` §12），仓库无 Secret；
- 上线演练的对账报告与遗留问题清单签认。

---

## 7. 待定案口径清单（M0 输入，含建议值）

> **状态（2026-09-20 · Push 71，M0-01 第一批）**：十五项已逐项定案 —— 11 项落 ADR-019 ~ ADR-029（见 7.2），#9 文案入仓 `docs/rules/`（M0-04），#8 / #10 / #15 为业务确认项（见 7.3，按建议值先行、可修订）；M0-02 契约切片表（`shared/README.md`）与 M0-03 种子数据规格（`database/seeds/README.md`）已出（Push 73）。

| # | 事项 | 建议口径 | 决议形式 |
|---|---|---|---|
| 1 | 蓝图组织 | 按项目类型各一份蓝图（`blueprints` 增 `project_type` 维度 + 版本化），默认模板 + 类型覆盖 | ADR |
| 2 | 节点权限 | 模板仅管理员；项目内节点增删限项目经理；完成 / 回退平权 | ADR |
| 3 | 负责人 id 化 | 任务 `owner_id`；新增用户目录接口；前端 `owner / ownerEn` 改 id + 显示名 | 契约 + 前端改造 |
| 4 | 项目「时间」语义 | 主数据变更 + 阶段推进 + 任务变更触发 `updated_at`；文件 / 日报不触发 | ADR |
| 5 | 阶段推进 | 项目经理手动触发 + 门禁校验 + 缺项明细；不做自动推进 | ADR |
| 6 | 成果文件多选 | 任务侧 `deliverable_types`；门禁以 `node_requirements` 为准 | 契约 + 迁移 |
| 7 | 「进行中」置位 | 每日 00:05 调度批量置位，写 `task_events`（来源 system） | ADR |
| 8 | 变更阶段映射 | 采用 v0.2 §2.5 建议（采购备货 → 加工采购等） | 业务确认 |
| 9 | R01~R07 文案 | 需求说明书附录 C.1 七条原文入仓（`docs/rules/`） | M0-04 |
| 10 | 项目类型取值 + 主题色 | 业务给清单与色值；字典元数据下发 | 业务确认 |
| 11 | 问题 SLA | `issues.due_at` + 按归类配置处理时限（字典） | ADR + 迁移 |
| 12 | 归档快照 | 归档时点清单 + 冻结状态位 + 可选导出包，不做全库快照表 | ADR |
| 13 | 时区口径 | 统一 Asia/Shanghai 计算「今天 / 本周 / 逾期」，ClockService 注入 | ADR |
| 14 | 搜索分词 | PoC 二选一：zhparser vs pg_trgm 双字切分（看部署成本与质量） | PoC 结论 |
| 15 | 演示字段来源 | `headcount / priority / files` 由模板默认值 + 用户填写，明确优先级 | 业务确认 |

### 7.1 决议记录（对齐清单 A1~A14，Push 49 / 69 / 70）

| 编号 | 事项 | 决议 | 落点 |
|---|---|---|---|
| A1 | 首页「项目时间」区间 | 采纳：`filter[timeFrom]` / `filter[timeTo]`（DateOnly 闭区间，Asia/Shanghai 日界；**Push 175 起映射 `created_at`（项目创建时间），原 `updated_at` 口径作废**） | 契约已入（Push 49）；**维度 Push 175 修订为 `created_at`**；列表 `sort` 缺省 = `createdAt:desc`（最近创建的在前） |
| A2 | 用户目录 | 采纳：`GET /users`（启用用户、`q` + 分页、默认工号升序）；选 b：`Project.managerName` 随行下发（列表 / 详情 / 创建与编辑返回；取不到 `null`，前端显示「—」） | 契约已入（Push 49）；数据依赖组织同步（D1-02）或 M0 种子 |
| A3 | 字典下发 | 采纳（收窄）：接口只下发 `region` / `projectType`；阶段 / 成果文件类型 / 紧急重要度留契约枚举 | 契约已入（Push 49）；取值与色值依赖 §7#10；`metadata.accent` 定为 CSS 颜色字符串（hex）、补 `metadata.accentText`（徽标文字色，缺省 `#fff`） |
| A4 | 用户偏好 | 采纳：`GET / PATCH /users/me/preferences`（合并语义；表 `user_preferences`） | 契约已入（Push 49）；**表 + 端点已落地（Push 169 · 迁移 0028）**，前端常用筛选（A24）同批接入；**列显隐白名单落地（Push 170）**：`taskTableHiddenColumns` = 枚举数组（`TaskTableColumnKey`，14 键 = 前端 `TABLE_COLUMNS` 非锁定列；未知 key 400；读侧同白名单收敛），前端「筛选 → 表格显示字段」已接线（同账号跨设备） |；**醒目模式（§6.13）接入（Push 171）**：`focusMode` 布尔键打通（GET 必回 / PATCH 可选，非布尔 400；读侧非布尔一律收敛 false），前端 `App.tsx` 持有 + `preferencesApi.saveFocusMode` 单键 PATCH（乐观更新 + 失败回滚）、`ProjectDetail.tsx` 开关改受控（null = 偏好未到 → 默认关），**无迁移**（键存既有 `user_preferences.prefs` jsonb）
| A5 | 项目软删 | 采纳：`DELETE /projects/{id}` + `If-Match` 防误删；`code` 唯一性保留 | 契约已入（Push 49） |
| A6 | facets 五组 vs 侧栏四组 | 后端维持五组返回；侧栏一期不展示「阶段 / 状态」（px 已确认） | 契约不变 |
| A7 | 任务列表字段范围 | 采纳：`TaskListItem`（内联摘要）+ 任务详情接口；裁掉 `ownerEn` | 契约已入（Push 49） |
| A8 | 任务默认排序 | 采纳：阶段序（`STAGE_KEYS` 序，未分组落最后）+ 组内位次 `sort_index` + `id`（**w2 · Push 124 修订**：原判据 `plannedStart ASC NULLS LAST, created_at ASC, id ASC` 随 A19 / A20 顺序持久化落地改为 `tasks.sort_index`，与看板列内顺序同口径）；`sort` 白名单 6 项 | 契约已入（Push 49），排序口径 2026-09-21 修订（Push 124）；一期不新增 `tasks.seq` |
| A9 | 项目列表排序白名单补 `createdAt`（PR #42 评审提请） | 采纳：`ProjectListQuery` 白名单 = `updatedAt` / `createdAt` / `seqNo`（`sort=createdAt:asc\|desc`）；**Push 175 起缺省 = `createdAt:desc`（最近创建的在前），原 `updatedAt:desc` 作废**；**前端 TIME「维度 × 方向」已落地（Push 175，`前端功能需求.md` §3.8 A9 / 第六章 Q15）** | 契约已入（Push 69，随 PR #42）；**缺省口径与前端落地 Push 175** |
| A12 | 任务状态可编辑 + 与进度条联动 | 采纳：`TaskUpdateBody` 开放 `status`（基础三态）；服务端同事务回填进度与完成日期；「已延期 / 提前完成」派生不可写、派生优先（与 `系统功能书.md` §52 一致） | 契约已入（Push 70）；前端需改：下拉不再盖掉「已延期」（原型 `statusOverride` 可删） |
| A13 | 实际完成日期可编辑 | 采纳：填 / 清都走 `/progress` —— 填 = `progress=1` + `actualEnd`（缺省当天）；清 = 进度写回 <1 档（服务端清空 `actual_end`，唯一清除方式）；进度归一为离散五档 | 契约已入（Push 70）；前端需改：点满四格 / 选「已完成」后显示服务端补的当天日期（原型现留「—」） |
| A14 | 「是否按时交付」的逾期派生 | 采纳：服务端读时派生 `onTime` + 展示态（枚举不变）；「逾期未交付 / 逾期已交付」由前端按 `onTime` + `displayStatus` 渲染；派生不出回落迁移值 | 契约已入（Push 70）；前端需改：删本地 `lateDeliveryLabel` 派生、逾期判定改用真实日期（原型按「M月D日」比较） |

> 清单原文见 `前端功能需求.md` §3.8；决议过程见 PR #40 评审记录。
> 2026-09-18 px 补充：A1 排序缺省 `updatedAt:desc`；A2 选 b（随行 `managerName`）；A3 `accent` = hex + `accentText`。
> 2026-09-20 wmj 定案（A9，随 PR #42，Push 69）：排序白名单补 `createdAt`（`sort=createdAt:asc|desc`），默认仍 `updatedAt:desc`；前端「维度 × 方向」（按创建时间 / 最近活动时间）仍为后续项。
> 2026-09-20 wmj 定案（A12~A14，随 PR #42，Push 70）：任务写入口径 —— 状态基础三态可写（服务端联动进度 / 完成日期）、「已延期 / 提前完成」派生不可写且派生优先、`progress<1` 清空 `actual_end`、`progress=1` 缺省按当天、`onTime` 与逾期标签服务端读时派生（前端不做本地派生）。

### 7.2 M0 决议落点（2026-09-20 · Push 71，ADR-019 ~ ADR-029）

十五项逐项定案：11 项落 ADR（下表），#9 落文案入仓（`docs/rules/`），#8 / #10 / #15 为业务确认项（见 7.3）。ADR 全文见 `docs/adr/`（索引已更新至 ADR-029）。

| # | 事项 | 决议形式 | 决议落点 | 备注 |
|---|---|---|---|---|
| 1 | 蓝图组织 | ADR | ADR-019 | 按项目类型各一份 + 默认模板兜底；修订 ADR-015「全局一份」口径 |
| 2 | 节点权限 | ADR | ADR-020 | 模板管理员 / 项目内增删项目经理 / 完成回退平权；修订 ADR-015「不做权限控制」口径 |
| 3 | 负责人 id 化 | 契约 + 前端改造 | ADR-021 | users.id 关联 + 显示名随行；负责人可空（待分配），撤回 A10 兜底描述 |
| 4 | 项目「时间」语义 | ADR | ADR-022 | updated_at 触发事件集冻结（主数据 / 成员 / 阶段 / 任务；文件日报与调度不触发） |
| 5 | 阶段推进 | ADR | ADR-023 | 手动 + 门禁 + 缺项明细（422）；回退限相邻阶段并需原因 |
| 6 | 成果文件多选 | 契约 + 迁移 | ADR-024 | 任务侧 deliverable_types；门禁唯一来源 = node_requirements |
| 7 | 「进行中」置位 | ADR | ADR-025 | 每日 00:05 + task_events 来源 system；不触发项目时间与提醒 |
| 8 | 变更阶段映射 | 业务确认 | 见 7.3 | 按建议值先行（可修订） |
| 9 | R01~R07 文案 | M0-04 | `docs/rules/R01-R07-内置规则文案.md` | 逐字入仓 + 金标用例骨架 |
| 10 | 项目类型 + 主题色 | 业务确认 | 见 7.3 | 字典元数据下发（A3 已入契约） |
| 11 | 问题 SLA | ADR + 迁移 | ADR-026 | due_at + 按归类配置时限；T+1 提醒 / T+3 升级 |
| 12 | 归档快照 | ADR | ADR-027 | 冻结状态位 + 引用式清单；不做全库快照表 |
| 13 | 时区口径 | ADR | ADR-028 | Asia/Shanghai + 应用层日界 + ClockService 注入 |
| 14 | 搜索分词 | PoC 结论 | ADR-029（可修订，复审 = M7 开工前） | pg_trgm 先行，zhparser 备选 |
| 15 | 演示字段来源 | 业务确认 | 见 7.3 | 模板默认值 + 用户填写 |

### 7.3 业务确认清单（4 项，按建议值先行、可修订）

| # | 事项 | 先行口径（建议值） | 待确认内容 | 影响面 |
|---|---|---|---|---|
| 8 | 变更阶段映射 | 采购备货 → 加工采购；发货 → 组装发货；实施 → 硬件实施 ~ 软件部署；验收 → 验收（四值 → 九阶段字典） | 历史数据导入的映射确认（原文待业务最终确认） | M8 迁移映射（PoC 7） |
| 10 | 项目类型取值 + 主题色 | 业务给清单与色值；字典元数据下发（`metadata.accent` / `accentText`，A3 已入契约） | 类型清单与色值未给前，前端主题色继续用本地映射 | M0-03 字典种子 / M1 字典接口 |
| 15 | 演示字段来源 | `headcount` / `priority` / `files` 由模板默认值 + 用户填写，明确优先级 | 三个字段的默认来源与填写优先级 | M0-03 种子 / M3 任务创建 |
| 16 | 数据范围枚举口径（C3-02 ↔ v0.2 §4.1） | 以 §4.1 六角色实际使用的五值为准：`all` / `managed_projects` / `involved_projects` / `own_stakeholders` / `granted`（0007 已落 CHECK；种子 #6a 按此六角色） | C3-02 措辞（全部 / 本部门 / 我参与的 / 我负责的 / 自定义项目集）与五值不一致：「本部门」无角色使用、「自定义项目集」≈ `granted`（近似映射）；h6 矩阵已按五值落地（未新增角色），收敛动作（补角色或改功能书措辞）仍待业务 / px 确认 | C3-09 管理界面；改枚举值需新迁移（0008），改措辞需 `系统功能书.md` 变更（提出人 + wmj / px 复核） |

> 业务回执后：7.3 各行状态改为「已确认」，取值回写 `字段对照清单.md` 与种子清单（M0-03）；未确认期间不得在契约与实现中固化未经确认的取值。

---

## 参考

- `技术设计v0.2-架构与数据模型.md`（§1 进程与边界、§2 领域模型、§3 蓝图与门禁、§4 权限、§5 文件与预览、§6 规则引擎、§7 契约与错误、§8 首页分类、§9 实施顺序、§11 增补）
- `系统功能书.md` V1.1（A1~D5 功能点与定档口径）
- `前端功能需求.md`（Push 40：页面、数据模型、接口需求、待确认问题）
- `团队分工.md` v1.1（模块归属、协作流程、PoC 分工、运维值班）
- `CONTRIBUTING.md`（§5 工作流、§11 开发日志、§14 生成物、§15 高风险、§16 发布、§17 DoD）
- `docs/adr/ADR-018-后端实施里程碑与卡片流水线.md`

---

（本文为实施与验收设计讨论稿；表结构与接口最终形态以 `database/migrations/` 与 `shared/` 契约为准。）

> 2026-09-23 px 补充（Push 175，业务口径「这个 time 改成 按创建时间 和更新时间排序 默认按创建时间」「这个筛选是按照项目创建时间筛选」）：① 列表 `sort` 缺省由 `updatedAt:desc` 改为 **`createdAt:desc`**；② `filter[timeFrom] / filter[timeTo]` 的取数维度由 `updated_at` 改为 **`created_at`（项目创建时间）**；③ 前端 TIME 升级为「维度（创建时间 / 更新时间）× 方向（降序 / 升序）」四枚按钮（§4 前端约定）；④ 上述三项契约描述 / 服务端 `parseProjectSort()` + `buildProjectFilter()` / 前端同批改动，**无 DDL、无迁移**；请 wmj 复核契约与后端。
> 2026-09-23 px 补充（Push 176，业务口径「把 time 去除了 然后把更新时间 创建时间放上去 字体也是偏灰的 并且可以点击 点击一次换一下不同的时间」）：首页工具条排序控件的**形态收敛** —— TIME 标识下线，排序维度由「两枚按钮」改为一枚**灰字可点文本**（显示当前维度，点一次在创建时间 / 更新时间之间切换），方向仍为「降序 / 升序」两枚按钮；**纯前端展示层**，§4 的 `sort` 参数口径、缺省 `createdAt:desc`、URL 与偏好记忆均不变（无契约 / 后端 / 字段改动）。
> 2026-09-23 px 补充（Push 177，业务口径「取消按更新时间排序吧 不要这个 只保留创建时间」）：排序**维度固定创建时间**（工具条上改为不可点的灰字说明，`text-zinc-400`），更新时间维度下线、只留「降序 / 升序」两枚方向按钮；前端状态层同步收敛 —— `useHashRoute.ts` 删 `SortField` 与 `ListQueryState.sortField`，`parseSortValue()` → `parseSortDesc()`（只读方向），`buildListHash()` 仅升序写 `sort=createdAt:asc`；`homePrefs.ts` 记忆项去掉 `sortField`；`projectApi.ts` 固定拼 `sort=createdAt:desc|asc`。契约 / 服务端 / 字段 / DDL / 迁移零改动（白名单 `updatedAt` 保留作旧链接兼容，前端不再产生）。

> 2026-09-23 px 补充（Push 178，业务口径「我们当前这个系统就不要考虑权限」，替 wmj 线落地并按跨线流程请 wmj 复核）：**权限判定开关 `PERMISSION_ENFORCED`（默认 `false` = 一期不判权限）接线** —— 唯一注入点 = `RoleService.getActorAuthorization()`：`false` 时返回**等效管理员画像**（`roleCodes=[admin]` + `dataScopes=[all]` + 契约全量权限位，不读 `user_roles`），`true`（二期）按库判；h6 判定链（can / 记录级可见集 / 字段级投影 / 五出口）与 task / flow / file 三处直读 `roleCodes` 的硬检查**零改动即整体放开**，`GET /api/v1/permissions/me` 与前端管理入口随之自动收敛（前端零代码改动）。**只影响裁定、不动数据**（用户偏好仍按账号各存一行）。无契约 / 无迁移 / 无 DDL；单测 +8（server 全量 492 例），真机对照 15 项（A 站默认 / B 站 `PERMISSION_ENFORCED=true` 两态同账号：画像 / 403 / 列表裁剪 / 偏好一致 / 角色绑定与临时会话零残留）。
