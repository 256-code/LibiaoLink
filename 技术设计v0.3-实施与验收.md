# 市场项目管理系统 · 技术设计 v0.3（实施与验收篇）

- 版本：v0.3 讨论稿，2026-09-18
- 入仓：仓库根 `技术设计v0.3-实施与验收.md`（新建）；数据模型与契约增补见《技术设计 v0.2》§11
- 输入基线：《技术设计v0.2-架构与数据模型.md》v0.2.6、《系统功能书.md》V1.1（定档版，含 2026-09-18 修订）、《前端功能需求.md》（Push 40 现状）、《团队分工.md》v1.1、《CONTRIBUTING.md》第 5、11、14~17 节
- 本文范围：后端落地的执行流程（卡片流水线）、里程碑与依赖、逐里程碑详细设计、横切机制、协作与联调节奏、验收与风险、M0 待定案口径清单
- 不在本文范围：业务功能点定义（系统功能书）、页面与交互（前端功能需求）、技术选型理由（技术设计 v0.1）、表结构最终形态（以 `database/migrations/` 为准）

**修订记录**

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.3 | 2026-09-18 | 首稿：卡片流水线与 DoR/DoD、M0~M8 里程碑与依赖、九项 PoC 落点、逐里程碑详细设计、横切机制、协作与联调、待定案口径清单（含建议值）、验收与风险 |

---

## 1. 执行模型

### 1.1 交付单位与三条轨道

- 交付单位 = 卡片（card）：一个可独立验收的纵切片，端到端穿透「契约 → 迁移 → schema → 模块 → 测试 → 文档 → 联调」；不做「先把所有表建完、再统一写接口」的分层式交付。
- 三条轨道并行，契约先于实现：
  - 契约与数据轨道（wmj 主责 / lan 评审）：`shared/` 契约、`database/` 迁移、ADR 与口径决议；
  - 领域轨道（wmj）：identity、project、blueprint/node、task、report/issue、stakeholder、automation、admin（字典 / 审计）；
  - 平台轨道（lan）：file、preview、notify、outbox / 队列 / 调度、search/dashboard、迁移工具链。
- 前端（px）在契约冻结后接入联调；契约未冻结期间继续使用内存模型开发，不手写与契约重复的第三份类型。

### 1.2 卡片流水线（八步，缺一步不算完成）

| 步 | 动作 | 产出物 | 负责 | 门禁 |
|---|---|---|---|---|
| 1 | 口径确认 | 决议记录（ADR / 技术设计增补 / 开发日志条目） | 提出人 + wmj | 影响契约、字段口径、权限的必须有结论 |
| 2 | 契约 | `shared/src/modules/*.ts` + openapi 注册 + 生成物 | wmj | `npm run typecheck` + `npm run check` 无漂移 |
| 3 | 迁移 | `database/migrations/000N_*.sql`（只追加） | 卡主责 | `migrate.mjs --dry-run` + 空库跑通 + 对端评审 |
| 4 | Schema | `server/src/db/schema/*.ts` | 卡主责 | `npm run check:db-schema` 无漂移 |
| 5 | 模块实现 | controller / service / repository / events + `index.ts` | 卡主责 | `typecheck` + `check:boundaries` |
| 6 | 测试 | 单测 + 集成 + 权限允许 / 拒绝 + 金标 | 卡作者 | `npm run test` 全绿；权限类卡片必须有拒绝用例 |
| 7 | 联调 | 前端替换 mock + 实测证据 | px + 卡主责 | 验收清单逐条勾选 |
| 8 | 文档与推送 | 需求 / 字段 / 设计 / 日志同步 + PR | 卡作者 | `CONTRIBUTING.md` §17 DoD + 非作者评审 |

### 1.3 DoR 与 DoD

- DoR（可开工）：口径已决；依赖卡片已合并；契约草案已评审；测试数据或样本已就绪（蓝图节点、成果文件样本、企微测试企业等）。
- DoD（在 `CONTRIBUTING.md` §17 之上，后端增补两条）：
  1. 空库迁移跑通：`node database/scripts/migrate.mjs` 在全空库可一次跑通，且 `npm run check:db-schema` 无漂移；
  2. 权限矩阵：涉及数据的接口至少各有一条允许与一条拒绝用例，且拒绝路径为 404 语义（防 IDOR）。

### 1.4 卡片模板（写进 PR 描述）

- 背景与目标 / 范围与非目标 / 口径决议引用 / 契约变更（增量或破坏性）/ 迁移与前滚方案 / 接口清单 / 事务与并发 / 幂等键 / 权限与字段策略 / 错误码 / 测试点 / 联调步骤 / 验收证据 / 文档同步项。

### 1.5 编号与既有编号映射

- 本文里程碑用 M0~M8 编号，与既有编号的映射：
  - 定档任务卡片 `g1`~`g6`（ADR、契约包、数据库基线、server 骨架、CI、auth 会话后端化）落在 M0 / M1；
  - `shared/README.md` 的「阶段 6 纵切 h1~h4」= M2 / M3；
  - 「阶段 7（文件与预览、自动化、日报与问题）」= M4 / M5 / M6；
  - 「阶段 8（通知、搜索、统计）」= M5 / M7；
  - 「阶段 9（迁移工具链）」= M8。

---

## 2. 里程碑与依赖

### 2.1 总览

| 里程碑 | 内容 | 主要卡片 | 依赖 | 预计 | 出口标准 |
|---|---|---|---|---|---|
| M0 | 口径冻结与种子数据 | 决议、契约切片表、种子清单、规则文案入仓 | — | 1 周 | 待定案清单（§7）全部落 ADR / 文档 |
| M1 | 身份与平台底座 | auth 会话、用户 / 组织 / 角色、字典、审计、幂等、outbox / jobs、权限策略 | M0 | 3 周 | 前端真实登录；字典接口可用；杀 worker 不丢消息 |
| M2 | 项目纵切（h1 / h2） | 项目 CRUD、建项目快照、阶段推进、facets、成员、视图、关注 | M1 | 3 周 | 首页全部走真实接口；facets 与列表一致 |
| M3 | 任务纵切（h3 / h4） | 任务列表 / 详情、进度与状态、门禁、批量、性能 | M1、M2 | 3 周 | 门禁拒绝可复现；1 万行 p95 达标 |
| M4 | 文件与预览（阶段 7） | 上传会话、版本 / 定档 / 回收站、关联、变更、预览管道、CAD PoC | M1、M3 | 5 周 | 200MB 续传；转换成功率 ≥95% |
| M5 | 自动化与通知（阶段 7~8） | 规则引擎、调度、企微、站内信 + SSE、R01~R07 | M1、M3、M4 | 5 周 | 金标回放全绿；限速实测 |
| M6 | 日报 / 问题 / 干系人 / 工作台 | 日报闭环、问题四态 + SLA、干系人、工作台 | M1~M3 | 4 周 | 填报 → 问题 → 关闭全链路可演示 |
| M7 | 搜索 / 仪表盘 / 导出 | 搜索投影、B4 / B5 统计、导出作业、归档 | M2~M6 | 4 周 | 搜索与权限一致；导出审计可查 |
| M8 | 迁移与上线（阶段 9） | 迁移工具链、全量演练、部署备份、验收 | M1~M7 | 4 周 | 真实数据导入 + 对账报告 + 恢复演练 |

### 2.2 依赖与关键路径

~~~text
M0 ─▶ M1 ─▶ M2 ─▶ M3 ─┬─▶ M4 ─▶ M5 ─┐
                       │                ├─▶ M8
                       └─▶ M6 ──────────┘
                                M4 / M6 ─▶ M7 ─┘
~~~

- 关键路径：M0 → M1 → M2 → M3 → M4 → M5 → M8；任何一项延期都直接推迟上线。
- M6 在 M3 之后即可开工（wmj 领域线），与 M4 / M5 并行；M7 在 M4 / M6 数据就绪后并行（lan 平台线）。
- M5 的规则框架可在 M3 后先行（不依赖文件），仅「文件事件类规则」需等待 M4 的 outbox 主题就位。

### 2.3 并行策略与瓶颈

- 并行瓶颈：文件管道与自动化（`技术设计v0.1-选型分析.md` §7.5.1 已指出）。对策：PoC 1 / 4 / 5 提前到 M1~M2 期间完成，M4 期间由 wmj 承接 M6。
- 平台线与领域线按模块边界解耦：跨模块只经 `index.ts`，副作用走同事务 Outbox，避免互相等待。
- 前端不等待全部后端：每个里程碑结束后即可接入对应页面（M2 首页、M3 任务表与抽屉、M4 文件库、M5 消息中心、M6 日报与问题、M7 搜索与仪表盘）。

### 2.4 九项 PoC 落点（`团队分工.md` §6）

| PoC | 内容 | 落点 |
|---|---|---|
| 1 | 文件直传 + 断点续传 + 转换预览 | M4 |
| 2 | 企微打通（应用消息 + 群机器人 + 限速） | M5 |
| 3 | 规则时间语义（R03~R07 金标回放） | M5 |
| 4 | Outbox 投递语义（杀 worker 不丢、重复不重发） | M1 |
| 5 | PG 队列并发（50 并发领取无重复无死锁） | M1 |
| 6 | 权限矩阵与脱敏（五类出口自动化用例） | M1 |
| 7 | 历史数据迁移（真实数据 + 附件全量） | M8 |
| 8 | 二维 CAD 本地转换与渲染（条件性） | M4 |
| 9 | 流程节点门禁与蓝图导入（round-trip + 拒绝留痕） | M2 / M3 |

---

## 3. 逐里程碑详细设计

### 3.1 M0 口径冻结与种子数据（1 周）

**卡片**

| 卡片 | 内容 | 主责 | 产出 |
|---|---|---|---|
| M0-01 | 待定案口径决议会（§7 十五项，逐项定案） | wmj | ADR-019~（按项拆分或合并）+ 技术设计增补 |
| M0-02 | 契约切片表：每张卡新增哪些端点 / 字段 / 错误码 | wmj | `shared/README.md` 切片段更新 |
| M0-03 | 种子数据清单与格式（九阶段、十类成果文件、问题归类、角色权限矩阵、蓝图节点） | wmj | 种子脚本规格（可重跑，与迁移分离） |
| M0-04 | R01~R07 文案入仓 + 金标用例骨架 | wmj | `docs/rules/` 或系统功能书附录 + 金标测试骨架 |
| M0-05 | 前端联调清单：每个页面需要哪些接口 / 字段 / 错误码 | px | 前端功能需求 §三 回写 |

**出口标准**

- §7 十五项全部有结论（未决项按建议值先行，并在 ADR 标注「可修订」与复审时间）；
- 蓝图节点清单与项目类型枚举定稿，能支撑 M2 建项目快照；
- R01~R07 文案逐字入仓，规则引擎可通过金标比对验收。
- 【状态 2026-09-20 · Push 71】M0-01 决议已落 ADR-019 ~ ADR-029；M0-04 文案与金标骨架已入仓 `docs/rules/`；M0-02 契约切片表与 M0-03 种子脚本规格为下一批产出。

### 3.2 M1 身份与平台底座（3 周）

**已落地（g6 · Push 43，2026-09-18）**：`/auth/*` 四条路径的契约与实现（identity 模块）、`0004_identity.sql`（users / sessions）、CSRF 同步 token 与会话守卫；本里程碑剩余部分为组织 / 角色 / 字典 / 审计 / 幂等 / outbox / 权限策略。

**新增表（`database/migrations/0004+`）**

| 表 | 关键字段 | 说明 |
|---|---|---|
| users（已随 g6 落地 0004） | casdoor_id（唯一）、username（工号，唯一）、display_name、email、owner、status | 以 SSO 为准；首次登录按 claims 归一化 upsert；一期不落 wecom_userid（需要时再加列） |
| departments | name、parent_id、source_id、status | 企微 / Casdoor 同步 |
| roles / role_permissions / user_roles | 角色、权限位、用户绑定 | 功能权限 + 数据范围 |
| sessions（已随 g6 落地 0004） | user_id、token_hash（sha256，唯一）、id_token（仅登出 id_token_hint）、expires_at、last_seen_at、revoked_at | 会话落库（无 Redis）；Cookie 值只存哈希，不存 refresh / access token |
| idempotency_keys | key + user + endpoint、request_hash、response_snapshot、status | 写接口幂等 |
| audit_logs | actor、action、object、before / after / diff、entry、trace_id、result | 追加写，保留 ≥6 个月 |
| dict_types / dict_items | code、name、sort、enabled、metadata（含 accent） | C9 字典唯一口径；接口形态见 A3（Push 49） |
| user_preferences | user_id（主键）、prefs（jsonb）、updated_at | 用户级 UI 偏好（A4）；与项目视图 project_views 分离 |
| jobs | kind、run_at、cron、last_run_at、status、locked_by | 定时 / 单例任务 |

**接口**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /auth/login | 生成 state + PKCE，302 到 Casdoor（已随 g6 落地） |
| GET | /auth/callback | 校验 state、换令牌、建会话（已随 g6 落地） |
| GET | /auth/me | 验签 id_token / 会话校验，返回用户字段（已随 g6 落地） |
| GET | /auth/logout | 清会话，302 到 Casdoor 登出（已随 g6 落地） |
| GET | /users | 用户目录（项目经理下拉 / 任务负责人候选；只返回启用用户）——**已入契约（A2，Push 49）** |
| GET | /dicts / GET /dicts/{type} | 字典下发（一期 region / projectType，含 metadata.accent；阶段 / 成果文件类型 / 紧急重要度走契约枚举）——**已入契约（A3，Push 49）** |
| GET / PATCH | /users/me/preferences | 用户偏好（PATCH 合并语义；一期键 taskTableHiddenColumns）——**已入契约（A4，Push 49）** |
| POST | /auth/mfa/verify | 本地账号 TOTP（D1-05，高风险操作重认证） |

**关键设计**

- `/auth/*` 为生产实现（当前仅 Vite 中间件，见 `frontend/README.md`）：state 双提交 Cookie + PKCE S256 + nonce；JWKS 缓存与 kid 轮换；时钟偏移容忍；id_token 验签后才采信。
- 会话 Cookie：`HttpOnly + Secure（生产）+ SameSite=Lax + Path=/`；30 分钟闲置超时（滑动续期）+ 12 小时绝对上限（对齐《开发者接入注意事项》第三章）；**会话校验比对 `users.status`：非 active 即撤销该用户全部会话**（覆盖离职踢线，g6 现状）——一期不设 `auth_version`，将来若需「改密 / 越权修复后强制全端下线」再补迁移。
- 组织同步（D1-02）：worker 定时拉取，产出差异报告；停用 / 离职触发会话撤销 + 审计；调岗触发数据范围重算（D1-07）。
- 权限策略服务（ADR-011 落地形态）：`can(actor, action, resource)` / `buildScopeWhere(actor)` / `fieldPolicy(entity)` / `exportPolicy`；角色与字段策略表驱动、内存缓存 + 版本号失效；Guard + 查询注入统一出口，保证「列表 / 详情 / 导出 / 搜索 / 通知」五出口一致（v0.1 §4.12 不变量）。
- 审计：api 角色仅授 INSERT（无 UPDATE / DELETE）+ 按月清理任务（保留 ≥6 个月）；越权尝试单独标记并告警。
- 幂等：`Idempotency-Key` 表 + 请求指纹；同键并发串行化并返回首次结果。
- Outbox / jobs：`FOR UPDATE SKIP LOCKED` 领取、指数退避、最大次数、死信告警；`jobs` + advisory lock 单例调度；ClockService 注入（规则代码禁取系统时间）。

**出口标准**

- 前端登录 → 会话建立 → `/auth/me` 返回用户；未登录 401 触发前端重走 SSO；
- 字典接口驱动项目类型主题色（前端移除硬编码映射，见 `前端功能需求.md` §二.1）；
- 权限矩阵 CI 用例（每条涉及数据的路由允许 + 拒绝）；
- PoC 4 / 5 / 6 证据入开发日志。

### 3.3 M2 项目纵切（3 周，h1 / h2）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M2-01 | 项目 CRUD：编号唯一（409 PROJECT_CODE_EXISTS）、seq_no 分配、乐观锁、软删 / 归档写保护 | wmj |
| M2-02 | 建项目事务：按项目类型蓝图快照生成 stages + nodes + requirements（PoC 9 前半） | wmj |
| M2-03 | 阶段推进：门禁校验 + 推进 / 回退 + 留痕 | wmj |
| M2-04 | 首页列表 / facets / 时间区间 / 排序（与前端 URL 参数全对齐） | wmj |
| M2-05 | 项目成员与记录级权限（非成员 404 语义） | wmj |
| M2-06 | 视图（个人 / 公共）、关注订阅、用户偏好 | wmj |
| M2-07 | 前端联调：首页 mock 下线、错误态、字典驱动主题色 | px |

**新增表**

| 表 | 关键字段 |
|---|---|
| project_members | project_id、user_id、role_in_project、joined_at |
| project_views | owner_id / scope（个人 / 公共）、filters、columns、sort、group、is_default |
| follows | user_id、object_type（project / task）、object_id、created_at |

**接口**

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /projects | 已入契约；补「导入蓝图版本」与成员初始化 |
| GET | /projects/{id} | 详情（含 stageKey / status / description / updatedAt） |
| PATCH | /projects/{id} | 已入契约；乐观锁 version |
| DELETE | /projects/{id} | 软删（**已入契约，A5 · Push 49**）：`If-Match` 回传 version 防误删；seq_no 不回收、code 唯一性保留（同编号再建仍 409） |
| GET | /projects/{id}/stages | 九阶段状态与完成度 |
| POST | /projects/{id}/stages/{key}/advance | 阶段推进（422 返回缺项明细） |
| GET / POST / DELETE | /projects/{id}/members | 成员维护 |
| GET | /projects/facets | 与列表同口径计数（权限过滤后） |
| GET / POST / PATCH / DELETE | /views | 视图 CRUD |
| GET / POST / DELETE | /follows | 关注 / 取关 |

**关键设计**

- 建项目事务（单事务）：校验 code 唯一 → `nextval` 分配 seq_no → 取该项目类型「已发布蓝图版本」→ 生成 stages / nodes / requirements（node_key 唯一防重，重复导入幂等）→ 写审计 → 同事务写 outbox（启动通知）。
- 阶段推进算法（**ADR-023**：项目经理手动触发 + 门禁校验 + 缺项明细，不做自动推进）：锁 stage → 校验该阶段全部任务完成且必要成果文件满足（复用 GateService）→ `project_stages.status = done`、下一阶段 active、`projects.stage_key` 前移 → 审计 + outbox；失败返回缺项清单（422），不部分推进。
- 列表与 facets 一致性：列表与计数共用一个 QueryBuilder（同 filter 构造器 + 同权限 JOIN），禁止两套 SQL；`filter[timeFrom] / filter[timeTo]` 按 Asia/Shanghai 日界截断（**已入契约，A1 · Push 49**）；排序支持 `updatedAt` / `createdAt` / `seqNo`（**A9 · Push 69**：白名单补 `createdAt`，默认序不变 = `updatedAt:desc`）；`q` 覆盖编号 / 名称 / 客户 / 序号。
- 项目「时间」语义按 **ADR-022** 执行（`updated_at` 触发事件集：主数据 / 成员 / 阶段 / 任务；文件日报与系统调度不触发），所有列表排序 / 时间筛选依赖该口径。
- 记录级权限：非成员项目在列表 / 详情 / facets / 搜索统一按 404 语义处理。

**出口标准**

- 首页筛选 URL 参数（region / projectType / managerId / timeFrom / timeTo / q / sort）直传后端，facets 与列表计数一致；
- 建项目 → 节点生成 → 阶段推进 → 门禁拒绝可复现；
- 前端移除项目经理 / 主题色硬编码假数据。

### 3.4 M3 任务纵切（3 周，h3 / h4）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M3-01 | 任务列表 / 详情：分页、筛选、排序、快筛、字段裁剪 | wmj |
| M3-02 | 进度与状态：联动、事件留痕、系统置位调度、逾期派生 | wmj |
| M3-03 | 完成门禁：任务侧三入口统一校验（PoC 9 后半） | wmj |
| M3-04 | 批量操作：批量指派 / 改状态 / 改日期 / 批量完成，部分失败清单 | wmj |
| M3-05 | 模板锁定与例外调整（管理员修正 + 原因留痕） | wmj |
| M3-06 | 性能：1 万行数据集压测与索引调优 | wmj |
| M3-07 | 前端联调：表格 15 列、抽屉、进度点击、409 冲突提示 | px |

**接口**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /projects/{id}/tasks | 已入契约；返回 `TaskListItem`（内联 ownerName / changeSummary / fileSummary，**A7 · Push 49**）；补快筛参数（我的 / 今天到期 / 本周 / 逾期 / 未完成 / 未上传成果文件） |
| GET | /projects/{id}/tasks/{taskId} | 任务详情（抽屉全字段 + 文件清单）——**已入契约（A7，Push 49）** |
| PATCH | /projects/{id}/tasks/{taskId}/progress | 已入契约；**A13 · Push 70**：`progress<1` 清空 `actual_end`（清除完成日期的唯一方式）、`progress=1` 缺省按当天；进度离散五档 |
| PATCH | /projects/{id}/tasks/{taskId} | 任务编辑（A10）；**A12 · Push 70** 开放可选 `status`（基础三态，服务端同事务联动进度与完成日期；「已延期 / 提前完成」派生不可写） |
| PATCH | /projects/{id}/tasks/batch | 批量更新（字段白名单 + 逐条校验 + 失败清单） |
| POST | /projects/{id}/tasks/{taskId}/complete | 完成提交（含门禁；未通过 422 + missing） |
| DELETE | /projects/{id}/tasks/{taskId} | 软删；有日报 / 问题 / 变更引用时拒绝 |

**关键设计**

- 状态与派生：存储基础态三值 + 查询期派生展示五态（不写回，见 v0.2 §2.4）；「进行中」自动置位由每日调度任务写入（**ADR-025**：每日 00:05，进度至少 1 格，不触发项目时间与提醒），`task_events` 标注来源 system（A1-10）；人工修改与回退留痕。**任务写入口径（A12~A14 · Push 70）**：状态只收基础三态（pending / active / done），服务端同事务联动进度与完成日期；进度离散五档、`progress<1` 清空 `actual_end`、`progress=1` 缺省按当天；`onTime` 与展示态服务端读时派生，「逾期未交付 / 逾期已交付」由前端按 `onTime` + `displayStatus` 渲染（派生优先：未完成已过 `plannedEnd` 一律「已延期」）。
- 门禁：`checkTaskCompletion(taskId)` 返回 `{ ok, missing[], warnings[] }`；按 `node_requirements` 逐 doc_type 统计 `files.status ∈ (final, changed)`；存在未定档（draft）时放行但返回 warning 并触发 R02；拒绝写审计（A4-20）。表格 / 看板 / 详情三个入口调用同一服务，服务端为准。
- 批量：预校验 → 分批执行 → 汇总失败清单，避免长事务；整批写一条批量审计；逐条校验权限与模板锁定。
- 性能：索引 `ix_tasks_project_stage (project_id, stage_key)`（0001 基线现有）+ 稳定排序键；**一期不新增 `tasks.seq`**；默认序 = 阶段序 + 组内 `plannedStart ASC NULLS LAST, created_at ASC, id ASC`（**A8 · Push 49**，id 兜底保证分页不跳行）；files 用随行 fileSummary 聚合、避免 N+1；limit 上限；1 万行首屏 50 行 p95 < 500ms 基线，压测脚本入仓。
- 成果文件多选按 **ADR-024** 执行：任务侧 `deliverable_types`（数组），门禁唯一来源 = `node_requirements`（无节点任务按自身类型兜底）。

**出口标准**

- 门禁拒绝可复现且留痕；三入口判定一致；
- 批量部分失败返回清单且不影响成功项；
- 1 万行压测报告入开发日志。

### 3.5 M4 文件与预览（5 周，阶段 7，lan 主线）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M4-01 | 上传会话：分片、断点续传、秒传、过期清理（PoC 1） | lan |
| M4-02 | 版本 / 定档 / 回溯 / 回收站 + 到期清理任务 | lan |
| M4-03 | 多态关联与文件库查询（项目 / 任务 / 日报 / 问题 / 变更双向跳转） | lan |
| M4-04 | 变更申请即通过：事务、R01 回写接口、变更记录与统计 | lan |
| M4-05 | 预览管道：converter 沙箱、队列、缓存、失败降级、短时签名 | lan |
| M4-06 | 二维 CAD 本地转换 PoC（条件性交付，PoC 8） | lan |
| M4-07 | 前端联调：文件库、预览入口、变更申请与记录 | px |

**新增表**

| 表 | 关键字段 |
|---|---|
| upload_sessions | project_id、node_id / task_id、doc_type、name、size、hash、mime、status、expires_at、created_by |
| ~~upload_parts~~ | **不落表**（评审定案，2026-09-18）：分片状态以对象存储 ListParts 为唯一真相，避免双写漂移 |
| file_links | file_id、object_type（project / task / node / report / issue / change）、object_id |
| preview_artifacts | file_id、version_id、pipeline_version、target、status、object_key、error、generated_at |
| change_items | change_request_id、file_id、before_version_id、after_version_id、doc_type |

**接口**

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /files/upload-sessions | 声明元数据 + 权限校验 + 秒传判定，返回分片签名 |
| GET | /files/upload-sessions/{id} | 断点续传状态（已收分片） |
| POST | /files/upload-sessions/{id}/complete | 校验分片、合并、哈希比对、建版本与关联 |
| GET / POST | /files / /files/{id}/versions | 文件库查询与版本链 |
| POST | /files/{id}/finalize | 定档锁版 |
| POST | /files/{id}/rollback | 回溯生成新版本 |
| DELETE / POST | /files/{id}（回收站）/ /files/{id}/restore | 软删与恢复（30 天） |
| GET | /files/{id}/preview | 预览状态 + 短时签名 URL |
| POST / GET | /changes | 变更申请（申请即通过）与记录检索 |
| GET | /files/{id}/download | 下载（鉴权 + 审计） |

**关键设计**

- 上传：api 只做签名与元数据，分片直传 MinIO；complete 时校验分片完整性、合并、哈希比对后写 `file_versions` + `file_links` + 审计 + outbox（预览任务）。断点续传 = 查询已收分片；过期会话与孤儿分片由 MinIO lifecycle + 定时任务清理。
- 版本与状态机：`draft → final → changed → archived / recycled`；定档锁 `current_version`；回溯生成新版本不删历史；回收站到期清理任务（默认 30 天，可配置）。
- 变更（A4-13 / A4-14）：单事务完成 `change_requests` + `change_items` + 新版本 + `files.status = changed` + 任务 `change_ref` 回写（R01）+ 审计 + outbox（通知）；缺「变更后文件」直接 422（CHANGE_FILE_REQUIRED）；幂等键 = 申请表单指纹。
- 预览：worker 领 outbox `preview.job` → 调 converter（无 DB、无外网、只读根文件系统、超时与资源限额）→ 产物回 MinIO → 更新 `preview_artifacts`；缓存键 = 内容哈希 + `pipelineVersion`；失败记录 reason 并降级「请下载」（D2-05）；定档时预生成（P1）；预览地址短时签名 + 审计 + 禁匿名（D2-04）。
- 预览 / 下载 / 定档 / 变更全部写审计（C7）。

**出口标准**

- 200MB 续传演示、断网恢复、秒传命中、并发定档 409、回收站恢复；
- 转换成功率 ≥95% 且中文不乱码（PoC 1）；CAD 达标上线、不达标降级不阻塞（PoC 8）。

### 3.6 M5 自动化与通知（5 周，阶段 7~8，lan + wmj）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M5-01 | 规则模型 + 条件求值 + 执行器 + 回放器（PoC 3） | wmj |
| M5-02 | 调度 / 补发 / 幂等执行键 / 状态回退再生窗口 | lan |
| M5-03 | 企微通道：应用消息、群机器人、token 管理、限速（PoC 2） | lan |
| M5-04 | 站内信 + SSE + 稍后提醒 + 合并免打扰 + 每日上限 | lan |
| M5-05 | R01~R07 + A01 / A02 / A03 / A14 落地 + 金标用例 | wmj |
| M5-06 | 规则管理接口、发送记录、重发 | lan |
| M5-07 | 前端联调：消息中心、规则管理页 | px |

**新增表**

| 表 | 关键字段 |
|---|---|
| automation_rules | code、enabled、trigger、conditions、actions、version |
| automation_runs | rule_id、entity、dedupe_key、命中 / 求值 / 动作结果、channel、时间 |
| notifications | user_id、type、title、body、object_ref、status（未读 / 已读 / 已处理）、remind_at |
| notify_prefs | user_id、免打扰时段、每日上限、合并窗口 |

**关键设计**

- 规则引擎：JSON 规则模型（触发器 / 条件 / 动作）+ 白名单操作符求值器 + ClockService 注入 + 幂等执行键（规则 ID + 实体 ID + 触发窗口）+ 回放器（给定数据集与时间 → 应发送清单）+ 金标文案逐字比对（C2-02）。
- 触发：事件型（业务事务写 outbox → worker 消费）、调度型（cron + 窗口 + `last_run_at` 补发）、手动补跑；状态回退再生新窗口（避免重复轰炸）。
- 投递：ChannelAdapter（企微应用消息 / 群机器人 / 站内信 / 邮件预留）；access_token 缓存刷新；群机器人队列限速（20 条 / 分钟，实测确认）；失败降级站内信（C5-04）；合并窗口 + 免打扰次日补发 + 每人每日上限（C2-09）。
- 站内信：收件箱 / 分类 / 未读计数 / 已读 / 已处理 / 稍后提醒（定时生成新消息）/ 全量留档（含企微成功发送）。
- SSE：通知与导出进度共用；断线重连与幂等游标。

**出口标准**

- 杀 worker 不丢消息、重复领取不重发、限速实测不超限；
- R03~R07 金标回放全绿（跨天、节假日、改期、补发）。

### 3.7 M6 日报 / 问题 / 干系人 / 工作台（4 周，wmj 线，可与 M4 / M5 并行）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M6-01 | 日报：草稿 / 提交 / 补填、模板、19:00 汇总、漏填名单 | wmj |
| M6-02 | 日报回写任务进展与问题自动生成（幂等） | wmj |
| M6-03 | 问题闭环：四态、分派、SLA、升级、关闭、看板、统计 | wmj |
| M6-04 | 干系人：台账、项目关联、导入、隐私字段权限、导出 | wmj |
| M6-05 | 工作台：我的任务 / 待办 / 关注动态 / 快捷入口 | wmj |
| M6-06 | 前端联调：日报、问题看板、干系人、工作台 | px |

**新增表**

| 表 | 关键字段 |
|---|---|
| daily_reports | project_id、user_id、report_date、content、headcount、status（草稿 / 已提交 / 补填）、submitted_at |
| daily_report_files | report_id、file_id、kind（现场图 / 问题图） |
| issues | project_id、task_id、category、status（四态）、due_at、assignee、source_report_id、closed_by |
| issue_events | issue_id、from / to、actor、note、created_at |
| issue_dispatch_rules | category → department / assignee（一类对多） |
| stakeholders / project_stakeholders | 台账字段 + 多对多关联 |
| todos | user_id、title、object_ref、remind_at、repeat_rule、status |

**关键设计**

- 防重提交：同人 + 同项目 + 同日期唯一约束；补填保留原始记录并标注。
- 应填未填：项目成员 × 工作日历 × 当日未提交清单；提醒记录可查（A7-05）。
- 回写与生成幂等：日报 → 任务进展描述（来源标注 + 幂等）；日报 → 问题自动生成（`unique(source_report_id)` 兜底），重编辑不重复生成（A3-09）。
- SLA：`due_at` 按归类配置计算（§7 第 11 项）；T+1 提醒、T+3 升级（走规则引擎 + 工作日历）；超时未确认自动关闭并单独标注（A3-15）。
- 干系人隐私字段：列表 / 导出 / 搜索 / 消息内容同一策略，无权字段不返回（A5-07、C3-08）。

**出口标准**

- 填报 → 问题生成 → 分派 → 处理 → 关闭全链路演示；
- 漏填提醒与升级记录可查；跨节假日用例通过。

### 3.8 M7 搜索 / 仪表盘 / 导出（4 周）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M7-01 | 搜索投影 + 分词 PoC + 回填脚本 + 高亮 + 权限过滤 | lan |
| M7-02 | 仪表盘 B4 与专项统计 B5（含风险提示阈值） | lan |
| M7-03 | 导出作业框架 + 五处导出接入（C4-05、B5-05、A7-04、A5-09、A4-15） | lan |
| M7-04 | 归档：门禁、清单、只读保护、检索 | wmj |
| M7-05 | 前端联调：搜索、仪表盘、导出入口 | px |

**关键设计**

- 搜索：`search_docs`（doc_type、project_id、title、body、meta、权限标签、tsv）+ GIN 索引；业务写入同事务维护投影（C6-04）；回填 / 重建脚本；分词方案按 PoC 定案（§7 第 14 项）；`ts_headline` 高亮；结果与字段按权限裁剪。
- 仪表盘：B4 每张卡片定义 SQL 口径（按时交付率分母、平均解决时长、TOP N、7 日到期、四格分布、下钻明细）；B5 风险提示阈值可配置；统计与列表同权限过滤。
- 导出：统一异步作业（jobs kind = export → worker 生成 → MinIO 临时对象 → 短时下载 URL → 过期清理）；进度 SSE；字段级过滤 + 审计 + （二期）水印。
- 归档：归档动作 = 门禁校验 → 生成归档清单 / 目录 → 项目状态 archived → 全写接口只读拦截；归档检索；归档包二期（C4-04）。

**出口标准**

- 搜索、统计、导出三处与页面权限口径一致（同一策略服务）；
- 导出审计可查、临时对象按期清理。

### 3.9 M8 迁移与上线（4 周，阶段 9）

**卡片**

| 卡片 | 内容 | 主责 |
|---|---|---|
| M8-01 | 迁移工具链：导入模板、校验、干跑报告、分批入库、幂等重导、附件包 | lan |
| M8-02 | 全量演练与对账报告（PoC 7） | lan |
| M8-03 | 部署 / 备份 / 恢复演练 + 监控告警 + 值班 | px |
| M8-04 | 上线检查单、灰度与回滚预案 | px |
| M8-05 | 前端 E2E 全链路与验收 | px |

**关键设计**

- 导入：Excel / CSV 模板（任务、干系人、历史项目）→ 校验（必填、字典匹配、节点匹配模板）→ 干跑报告（脏数据处理建议）→ 分批入库 → 幂等重导 → 附件包上传与关联 → 对账报告（条数 / 文件数 / 金额）。
- 上线：空库迁移演练、全量演练、前滚修复预案、恢复演练（两人会操、季度一次）、告警接线（死信、转换失败、企微失败、outbox 积压）。

**出口标准**

- PoC 7 证据（真实数据 + 附件全量导入 + 报告可读）；
- 恢复演练与上线检查单完成签认。

---

## 4. 横切机制

### 4.1 契约变更三类处理

- 增量（新增可选字段 / 新端点）：单 PR 完成，源与生成物同批（`CONTRIBUTING.md` §14）。
- 破坏性（改必填 / 改名 / 改语义）：必须 ADR + 前后端同批改造 + 兼容窗口，禁止静默变更。
- 生成流程：改 `shared/src` → `npm run generate` → 提交 `generated/`；CI 的 `shared` job 漂移检查必须绿。

### 4.2 迁移与 schema 同步

- 迁移编号顺延 `0004+`，只追加、合并后不可修改（`CONTRIBUTING.md` §14）；采用 expand / contract（先扩展后收敛）。
- 种子数据（九阶段、成果文件十类、问题归类、角色权限、蓝图节点）用独立可重跑脚本，不进迁移。
- 应用角色 `libiaolink_api` 无 DDL 权限（`database/README.md`）；迁移由独立任务执行，应用启动不改结构。
- 每张含迁移的卡片必须跑：`node database/scripts/migrate.mjs --dry-run`（演练库）→ 空库全量 → `npm run check:db-schema`。

### 4.3 模块实现骨架与事务边界

- 四层：controller（HTTP / Guard / Zod 管道）→ service（用例与事务边界）→ repository（Drizzle）→ events（同事务写 outbox）；对外只经 `index.ts`（`npm run check:boundaries`）。
- 一个业务用例 = 一个事务：业务表 + outbox + 搜索投影（同库）同事务；外部副作用只能由 worker 执行（v0.2 §1.3）。

### 4.4 幂等、乐观锁与并发

- 写接口支持 `Idempotency-Key`：落表 + 请求指纹 + 响应快照；同键并发串行化并返回首次结果（契约错误码 IDEMPOTENT_REPLAY）。
- 更新携带 `version`：冲突 409（VERSION_CONFLICT）；节点/阶段/任务/项目统一。
- 队列并发：`FOR UPDATE SKIP LOCKED`；单例调度：advisory lock；批量为「预校验 + 分批执行 + 失败清单」，不打包长事务。

### 4.5 权限策略服务（ADR-011 落地形态）

- 四个出口：`can(actor, action, resource)`、`buildScopeWhere(actor)`、`fieldPolicy(entity, actor)`、`exportPolicy(actor, dataset)`。
- 策略表驱动 + 内存缓存 + 版本号失效；Guard 声明式接入；查询层统一注入数据范围，禁止各模块自写权限 SQL。
- 五出口一致：列表 / 详情 / 导出 / 搜索 / 通知内容；无权字段不返回；资源不存在与无权统一 404。
- 越权尝试单独留痕并告警（C7-03）；临时授权到期自动收回（C3-06）。

### 4.6 审计

- 统一 `audit_logs`：actor、IP、UA、action、object、before / after / diff、entry（页面 / 批量 / 系统 / API）、trace_id、result。
- 追加写：api 角色仅 INSERT；保留 ≥6 个月 + 清理任务；高风险导出重认证（C7-04）。
- 字段级修改历史（C7-02）由 service 层 diff 生成，任务详情可直接查询。

### 4.7 时间与时钟

- 存 UTC（timestamptz），展示与业务计算按 Asia/Shanghai；业务日期用 date。
- ClockService 注入：规则、调度、逾期判定禁止直接取系统时间，保证可回放与金标测试。
- 工作日历（D5）提供节假日 / 调休与 T-1 / T+1 计算，提醒顺延按配置规则执行。

### 4.8 错误码增补清单（在 v0.2 §7.2 之上）

| code | HTTP | 场景 |
|---|---|---|
| STAGE_GATE_NOT_PASSED | 422 | 阶段推进门禁未通过（含缺项明细） |
| TASK_REQUIRED_DOC_MISSING | 422 | 任务完成缺必交成果文件（含 missing） |
| CHANGE_FILE_REQUIRED | 422 | 变更缺「变更后文件」 |
| FILE_STATE_INVALID | 409 | 文件状态不允许该操作（已有，补回收站 / 回溯路径） |
| UPLOAD_INCOMPLETE | 409 | 上传会话分片未齐（需补传后重试） |
| UPLOAD_SESSION_EXPIRED | 410 | 上传会话过期，需重建 |
| FILE_HASH_MISMATCH | 422 | 合并后哈希与声明不一致 |
| DICT_VALUE_UNKNOWN | 422 | 字典取值不合法或已停用 |
| DUPLICATE_SUBMISSION | 409 | 同人同场景重复提交（日报等） |
| ISSUE_TRANSITION_INVALID | 409 | 问题状态流转不允许 |
| RATE_LIMITED | 429 | 触发限流（登录 / 导出 / 搜索 / 预览签名） |
| MFA_REQUIRED | 403 | 高风险操作需重认证 |

### 4.9 测试与 CI 分层

- 现有：`frontend` / `shared` / `server` / `database` 四 job（`.github/workflows/ci.yml`）；`database` 已并入「空库迁移 + Drizzle 漂移检查」红线（Push 48）。
- 待补（L2 余下）：文件管道与 outbox 的集成测试（真实约束与并发）。
- 权限矩阵作为必跑用例（每条数据路由允许 + 拒绝），失败即阻断合并。
- L3 夜间：E2E 关键路径、恢复演练、依赖漏洞扫描、性能基线（1 万行任务表、facets 聚合）。
- 规则引擎以「回放 + 金标文案」为准入测试，不依赖真实企微。

### 4.10 可观测性

- 指标：outbox 积压 / 死信数、转换队列深度与失败率、企微发送失败率与限速触发、慢查询 TOP、导出失败、会话与 401 突增。
- 日志：Pino JSON + traceId 透传（已有）；审计与业务日志分离。
- 告警路由按 `团队分工.md` §7（px 服务 / 部署、lan 数据与投递、wmj 领域逻辑）。

---

## 5. 协作节奏与联调

### 5.1 周节奏

| 时间 | 事项 |
|---|---|
| 周一上午 | 计划与口径会：集中处理待定案口径（默认 1 小时），确认本周卡片与依赖 |
| 周一~周四 | 开发；每日 11:00 / 17:00 两个合并窗口（`团队分工.md` §5） |
| 周三 | 联调日：前端接真、错误态回归 |
| 周五 | 验收演示 + 文档同步检查 + 非紧急合并冻结（17:00 后） |

### 5.2 联调流程

- 契约合并 → 前端接真（保留 mock 开关便于离线开发）→ 卡主责提供演示数据（种子 / 演示项目）→ px 按验收清单执行（含 404 / 409 / 422 / 429 错误态）→ 证据（截图 / 无头实测）进开发日志。
- 前端接入顺序：M2 首页 → M3 任务表与抽屉 → M4 文件库 / 预览 → M5 消息中心 → M6 日报 / 问题 → M7 搜索 / 仪表盘。

### 5.3 文档同步矩阵

| 变更类型 | 必须同步 | 责任 |
|---|---|---|
| 契约 / 字段 | `前端功能需求.md` §三 + `字段对照清单.md` + 生成物 | wmj + px |
| 业务口径 | `系统功能书.md`（与源文档双处同步） | 变更提出人 |
| 架构 / 数据模型 | `技术设计v0.2` / `技术设计v0.3` + ADR | wmj |
| 每次推送 | `开发日志.md` 顶部条目（日期 时间 / 作者 / 分支 / Push 序号） | 推送人 |

### 5.4 评审与高风险

- 高风险（迁移、鉴权、契约、CI / 部署、Secrets）按 `CONTRIBUTING.md` §15 必须非作者批准；`CODEOWNERS` 自动路由。
- ADR 状态随 PR 合并转「已采纳」；被取代时新增 ADR 并回写旧条目。

---

## 6. 验收与风险

### 6.1 里程碑出口标准汇总

| 里程碑 | 一句话验收 |
|---|---|
| M0 | 十五项口径全部有结论，蓝图 / 字典 / 规则文案定稿 |
| M1 | 真实登录可用；权限矩阵、幂等、outbox 三项证据齐 |
| M2 | 首页全真实数据；facets 与列表一致；建项目 + 阶段推进可演示 |
| M3 | 三入口门禁一致；批量失败清单可用；1 万行性能达标 |
| M4 | 200MB 续传 + 预览成功率 + 变更即通过全链路 |
| M5 | 金标回放全绿；限速与降级实测 |
| M6 | 日报 → 问题 → 关闭闭环；SLA 升级可查 |
| M7 | 搜索 / 统计 / 导出与页面权限一致；导出审计可查 |
| M8 | 真实数据导入 + 对账 + 恢复演练 + 上线检查单 |

### 6.2 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| M0 口径拖延 | 全部卡片阻塞 | 十五项带建议值开会，未决项按建议值先行并在 ADR 标注可修订 |
| R01~R07 文案未入仓 | 规则引擎无法验收 | M0-04 强制入仓 + 金标骨架先行 |
| 蓝图按项目类型 vs 全局 | 建项目事务返工 | 先定案再改契约与 DDL，再写 M2-02 |
| 文件管道人力单点（lan） | 关键路径 | PoC 1 / 4 / 5 提前；M4 期间 wmj 承接 M6 |
| M4 / M5 串行风险 | 上线推迟 | M5 规则框架在 M3 后先开工，文件事件类规则后接 |
| 迁移数据质量 | 上线延期 | M7 末尾先做一次干跑，M8 全量演练 |
| 前端 mock 与契约漂移 | 联调返工 | 契约冻结后前端只消费生成类型，禁止手写模型 |
| 企微限速 / 失败 | 消息丢失 | 队列限速 + 站内信兜底 + 死信告警（M1 已具备 outbox） |

### 6.3 上线检查单（最小集）

- 空库迁移、`check:db-schema`、权限矩阵、金标回放、恢复演练五项全绿；
- 备份（PG + MinIO）与恢复演练记录、告警接线、值班表；
- Secret 全部在环境变量或密钥管理（`CONTRIBUTING.md` §12），仓库无 Secret；
- 上线演练的对账报告与遗留问题清单签认。

---

## 7. 待定案口径清单（M0 输入，含建议值）

> **状态（2026-09-20 · Push 71，M0-01 第一批）**：十五项已逐项定案 —— 11 项落 ADR-019 ~ ADR-029（见 7.2），#9 文案入仓 `docs/rules/`（M0-04），#8 / #10 / #15 为业务确认项（见 7.3，按建议值先行、可修订）；M0-02 契约切片表与 M0-03 种子脚本规格待出。

| # | 事项 | 建议口径 | 决议形式 |
|---|---|---|---|
| 1 | 蓝图组织 | 按项目类型各一份蓝图（`blueprints` 增 `project_type` 维度 + 版本化），默认模板 + 类型覆盖 | ADR |
| 2 | 节点权限 | 模板仅管理员；项目内节点增删限项目经理；完成 / 回退平权 | ADR |
| 3 | 负责人 id 化 | 任务 `owner_id`；新增用户目录接口；前端 `owner / ownerEn` 改 id + 显示名 | 契约 + 前端改造 |
| 4 | 项目「时间」语义 | 主数据变更 + 阶段推进 + 任务变更触发 `updated_at`；文件 / 日报不触发 | ADR |
| 5 | 阶段推进 | 项目经理手动触发 + 门禁校验 + 缺项明细；不做自动推进 | ADR |
| 6 | 成果文件多选 | 任务侧 `deliverable_types`；门禁以 `node_requirements` 为准 | 契约 + 迁移 |
| 7 | 「进行中」置位 | 每日 00:05 调度批量置位，写 `task_events`（来源 system） | ADR |
| 8 | 变更阶段映射 | 采用 v0.2 §2.5 建议（采购备货 → 加工采购等） | 业务确认 |
| 9 | R01~R07 文案 | 需求说明书附录 C.1 七条原文入仓（`docs/rules/`） | M0-04 |
| 10 | 项目类型取值 + 主题色 | 业务给清单与色值；字典元数据下发 | 业务确认 |
| 11 | 问题 SLA | `issues.due_at` + 按归类配置处理时限（字典） | ADR + 迁移 |
| 12 | 归档快照 | 归档时点清单 + 冻结状态位 + 可选导出包，不做全库快照表 | ADR |
| 13 | 时区口径 | 统一 Asia/Shanghai 计算「今天 / 本周 / 逾期」，ClockService 注入 | ADR |
| 14 | 搜索分词 | PoC 二选一：zhparser vs pg_trgm 双字切分（看部署成本与质量） | PoC 结论 |
| 15 | 演示字段来源 | `headcount / priority / files` 由模板默认值 + 用户填写，明确优先级 | 业务确认 |

### 7.1 决议记录（对齐清单 A1~A14，Push 49 / 69 / 70）

| 编号 | 事项 | 决议 | 落点 |
|---|---|---|---|
| A1 | 首页「项目时间」区间 | 采纳：`filter[timeFrom]` / `filter[timeTo]`（DateOnly 闭区间，Asia/Shanghai 日界；一期映射 `updated_at`） | 契约已入（Push 49）；语义依赖 §7#4 ADR；列表 `sort` 缺省 = `updatedAt:desc`（最近活动在前） |
| A2 | 用户目录 | 采纳：`GET /users`（启用用户、`q` + 分页、默认工号升序）；选 b：`Project.managerName` 随行下发（列表 / 详情 / 创建与编辑返回；取不到 `null`，前端显示「—」） | 契约已入（Push 49）；数据依赖组织同步（D1-02）或 M0 种子 |
| A3 | 字典下发 | 采纳（收窄）：接口只下发 `region` / `projectType`；阶段 / 成果文件类型 / 紧急重要度留契约枚举 | 契约已入（Push 49）；取值与色值依赖 §7#10；`metadata.accent` 定为 CSS 颜色字符串（hex）、补 `metadata.accentText`（徽标文字色，缺省 `#fff`） |
| A4 | 用户偏好 | 采纳：`GET / PATCH /users/me/preferences`（合并语义；表 `user_preferences`） | 契约已入（Push 49） |
| A5 | 项目软删 | 采纳：`DELETE /projects/{id}` + `If-Match` 防误删；`code` 唯一性保留 | 契约已入（Push 49） |
| A6 | facets 五组 vs 侧栏四组 | 后端维持五组返回；侧栏一期不展示「阶段 / 状态」（px 已确认） | 契约不变 |
| A7 | 任务列表字段范围 | 采纳：`TaskListItem`（内联摘要）+ 任务详情接口；裁掉 `ownerEn` | 契约已入（Push 49） |
| A8 | 任务默认排序 | 采纳：阶段序 + `plannedStart ASC NULLS LAST, created_at ASC, id ASC`；`sort` 白名单 6 项 | 契约已入（Push 49）；一期不新增 `tasks.seq` |
| A9 | 项目列表排序白名单补 `createdAt`（PR #42 评审提请） | 采纳：`ProjectListQuery` 白名单 = `updatedAt` / `createdAt` / `seqNo`（`sort=createdAt:asc\|desc`）；缺省仍 `updatedAt:desc`（最近活动在前），服务端按既有列排序；前端把 TIME 升级为「维度 × 方向」仍为后续项（`前端功能需求.md` §3.8 A9 / 第六章 Q15） | 契约已入（Push 69，随 PR #42） |
| A12 | 任务状态可编辑 + 与进度条联动 | 采纳：`TaskUpdateBody` 开放 `status`（基础三态）；服务端同事务回填进度与完成日期；「已延期 / 提前完成」派生不可写、派生优先（与 `系统功能书.md` §52 一致） | 契约已入（Push 70）；前端需改：下拉不再盖掉「已延期」（原型 `statusOverride` 可删） |
| A13 | 实际完成日期可编辑 | 采纳：填 / 清都走 `/progress` —— 填 = `progress=1` + `actualEnd`（缺省当天）；清 = 进度写回 <1 档（服务端清空 `actual_end`，唯一清除方式）；进度归一为离散五档 | 契约已入（Push 70）；前端需改：点满四格 / 选「已完成」后显示服务端补的当天日期（原型现留「—」） |
| A14 | 「是否按时交付」的逾期派生 | 采纳：服务端读时派生 `onTime` + 展示态（枚举不变）；「逾期未交付 / 逾期已交付」由前端按 `onTime` + `displayStatus` 渲染；派生不出回落迁移值 | 契约已入（Push 70）；前端需改：删本地 `lateDeliveryLabel` 派生、逾期判定改用真实日期（原型按「M月D日」比较） |

> 清单原文见 `前端功能需求.md` §3.8；决议过程见 PR #40 评审记录。
> 2026-09-18 px 补充：A1 排序缺省 `updatedAt:desc`；A2 选 b（随行 `managerName`）；A3 `accent` = hex + `accentText`。
> 2026-09-20 wmj 定案（A9，随 PR #42，Push 69）：排序白名单补 `createdAt`（`sort=createdAt:asc|desc`），默认仍 `updatedAt:desc`；前端「维度 × 方向」（按创建时间 / 最近活动时间）仍为后续项。
> 2026-09-20 wmj 定案（A12~A14，随 PR #42，Push 70）：任务写入口径 —— 状态基础三态可写（服务端联动进度 / 完成日期）、「已延期 / 提前完成」派生不可写且派生优先、`progress<1` 清空 `actual_end`、`progress=1` 缺省按当天、`onTime` 与逾期标签服务端读时派生（前端不做本地派生）。

### 7.2 M0 决议落点（2026-09-20 · Push 71，ADR-019 ~ ADR-029）

十五项逐项定案：11 项落 ADR（下表），#9 落文案入仓（`docs/rules/`），#8 / #10 / #15 为业务确认项（见 7.3）。ADR 全文见 `docs/adr/`（索引已更新至 ADR-029）。

| # | 事项 | 决议形式 | 决议落点 | 备注 |
|---|---|---|---|---|
| 1 | 蓝图组织 | ADR | ADR-019 | 按项目类型各一份 + 默认模板兜底；修订 ADR-015「全局一份」口径 |
| 2 | 节点权限 | ADR | ADR-020 | 模板管理员 / 项目内增删项目经理 / 完成回退平权；修订 ADR-015「不做权限控制」口径 |
| 3 | 负责人 id 化 | 契约 + 前端改造 | ADR-021 | users.id 关联 + 显示名随行；负责人可空（待分配），撤回 A10 兜底描述 |
| 4 | 项目「时间」语义 | ADR | ADR-022 | updated_at 触发事件集冻结（主数据 / 成员 / 阶段 / 任务；文件日报与调度不触发） |
| 5 | 阶段推进 | ADR | ADR-023 | 手动 + 门禁 + 缺项明细（422）；回退限相邻阶段并需原因 |
| 6 | 成果文件多选 | 契约 + 迁移 | ADR-024 | 任务侧 deliverable_types；门禁唯一来源 = node_requirements |
| 7 | 「进行中」置位 | ADR | ADR-025 | 每日 00:05 + task_events 来源 system；不触发项目时间与提醒 |
| 8 | 变更阶段映射 | 业务确认 | 见 7.3 | 按建议值先行（可修订） |
| 9 | R01~R07 文案 | M0-04 | `docs/rules/R01-R07-内置规则文案.md` | 逐字入仓 + 金标用例骨架 |
| 10 | 项目类型 + 主题色 | 业务确认 | 见 7.3 | 字典元数据下发（A3 已入契约） |
| 11 | 问题 SLA | ADR + 迁移 | ADR-026 | due_at + 按归类配置时限；T+1 提醒 / T+3 升级 |
| 12 | 归档快照 | ADR | ADR-027 | 冻结状态位 + 引用式清单；不做全库快照表 |
| 13 | 时区口径 | ADR | ADR-028 | Asia/Shanghai + 应用层日界 + ClockService 注入 |
| 14 | 搜索分词 | PoC 结论 | ADR-029（可修订，复审 = M7 开工前） | pg_trgm 先行，zhparser 备选 |
| 15 | 演示字段来源 | 业务确认 | 见 7.3 | 模板默认值 + 用户填写 |

### 7.3 业务确认清单（3 项，按建议值先行、可修订）

| # | 事项 | 先行口径（建议值） | 待确认内容 | 影响面 |
|---|---|---|---|---|
| 8 | 变更阶段映射 | 采购备货 → 加工采购；发货 → 组装发货；实施 → 硬件实施 ~ 软件部署；验收 → 验收（四值 → 九阶段字典） | 历史数据导入的映射确认（原文待业务最终确认） | M8 迁移映射（PoC 7） |
| 10 | 项目类型取值 + 主题色 | 业务给清单与色值；字典元数据下发（`metadata.accent` / `accentText`，A3 已入契约） | 类型清单与色值未给前，前端主题色继续用本地映射 | M0-03 字典种子 / M1 字典接口 |
| 15 | 演示字段来源 | `headcount` / `priority` / `files` 由模板默认值 + 用户填写，明确优先级 | 三个字段的默认来源与填写优先级 | M0-03 种子 / M3 任务创建 |

> 业务回执后：7.3 各行状态改为「已确认」，取值回写 `字段对照清单.md` 与种子清单（M0-03）；未确认期间不得在契约与实现中固化未经确认的取值。

---

## 参考

- `技术设计v0.2-架构与数据模型.md`（§1 进程与边界、§2 领域模型、§3 蓝图与门禁、§4 权限、§5 文件与预览、§6 规则引擎、§7 契约与错误、§8 首页分类、§9 实施顺序、§11 增补）
- `系统功能书.md` V1.1（A1~D5 功能点与定档口径）
- `前端功能需求.md`（Push 40：页面、数据模型、接口需求、待确认问题）
- `团队分工.md` v1.1（模块归属、协作流程、PoC 分工、运维值班）
- `CONTRIBUTING.md`（§5 工作流、§11 开发日志、§14 生成物、§15 高风险、§16 发布、§17 DoD）
- `docs/adr/ADR-018-后端实施里程碑与卡片流水线.md`

---

（本文为实施与验收设计讨论稿；表结构与接口最终形态以 `database/migrations/` 与 `shared/` 契约为准。）
