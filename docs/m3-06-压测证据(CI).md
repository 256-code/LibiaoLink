# M3-06 压测证据（CI 真机执行）

> 卡片：M3-06「1 万行性能达标（列表 / 筛选 / 总览 / 甘特取数）+ 索引调优评估」（主责 wmj / 评审 lan）｜口径来源：`技术设计v0.3-实施与验收.md` §3.4 与 §6.1、`server/README.md`。
> 本文件是 CI 运行日志的逐行转写：仅剥离每行时间戳前缀，并加本说明与元信息表；正文数字与文字未做改动。本地复跑由脚本 `--out` 直接生成（文件名不带 (CI)）。

## 元信息

| 项 | 值 |
|---|---|
| 执行环境 | GitHub Actions `ubuntu-24.04` · job `database`（check run 106689028217） |
| 代码版本 | `fd213ac`（PR #126 头提交；squash 合并提交 `658ad14`） |
| 执行时间 | 2026-09-22 09:25:39Z ~ 09:26:34Z（北京时间 17:25 ~ 17:26） |
| 数据库 | `postgres:18` service 5432，空库迁移 0001 ~ 0023 之后执行 |
| 目标 api | `http://127.0.0.1:3011`（真实 NestJS 进程 + 真 PG，非 mock） |
| 脚本 | `server/scripts/m3-06-stress.mjs` / `server/scripts/m6-replay.mjs` |

## 结论（整理自下方原文）

- 数据集：单项目 10000 行（活跃 9800 + 软删 200），九阶段 + 未分组、8 位负责人、稀疏关键字 10 条可复现。
- 性能：9 个读取形状 p50 / p95 全部在阈值内（默认读序 p95 71.0 ms / 阈值 800 ms；甘特 44.0 ms / 阈值 900 ms）。
- 索引对照：DROP `ix_tasks_project_stage_order` 后 9 形状 p50 倍率 0.94x ~ 1.02x（阈值 1.5x）、下线后默认读序 p50 4.1 ms（阈值 800 ms）→ 旧索引可下线，读面由部分索引 `ix_tasks_active_group` 兜底（迁移 0024 随 Push 158 落地）。
- 全部断言 A1 ~ A15 通过（原文见下）。

## 运行日志原文

# M3-06 压测证据（1 万行任务数据集 + 索引调优评估）

> 卡片：M3-06 · 「1 万行性能达标（列表 / 筛选 / 总览 / 甘特取数）+ 索引调优评估」（主责 wmj，评审 lan）｜口径来源：技术设计v0.3-实施与验收.md §3.4 / §6.1、技术设计v0.2-架构与数据模型.md §2.3。

| 项 | 值 |
|---|---|
| 回放时间 | 2026-09-22 17:26:33 +08:00 |
| 目标 | http://127.0.0.1:3011 |
| 数据库 | postgresql://libiaolink_migrator@127.0.0.1:5432/libiaolink |
| 代码版本 | f88037d |
| 脚本 | server/scripts/m3-06-stress.mjs |
| 数据集 | 单项目 10000 行（含 200 条软删 · 活跃 9800 行 · 九阶段 + 未分组 · 8 位负责人） |
| 采样 | 每形状 6 轮（SQL 用 EXPLAIN ANALYZE 的 Execution Time，API 用端到端耗时，取 p50 / p95 / max） |

## 断言明细

- 合成项目 M3STRESS-20260922092630（seq_no=1）id=ddcc5cec-c0c9-479a-bb6a-35e747851aac
| PASS | A1 | 数据集规模：单项目 1 万行（含 200 条软删） | 期望：total=10000 active=9800 | 实际：total=10000 active=9800 | inserted=10000 softDeleted=200 |
| PASS | A2 | 关键字分布：稀疏关键字命中 10 条（mod 977） | 期望：n=10 | 实际：n=10 |
| L1 | 列表 · 默认读序第 1 页（limit 50） | 200 | 21.9 ms | 71.0 ms |
| PASS | A3 | 列表第 1 页：50 条 + total=活跃数 + 首行属首阶段且位次 0 | 期望：200 / 50 条 / total=9800 / 首行 presale #0 | 实际：200 / 50 条 / total=9800 / 首行 presale #0 |
| PASS | A4 | 列表 p95 阈值 | 期望：<= 800 ms | 实际：71.0 ms（p50 21.9 / max 71.0） | 6 轮 |
| L2 | 列表 · 阶段筛选（stage=design） | 200 | 12.6 ms | 13.5 ms |
| PASS | A5 | 阶段筛选 p95 阈值 + 全部命中 design | 期望：<= 800 ms + 全部 design | 实际：13.5 ms / 越界 0 条 / total=984 |
| L3 | 列表 · 负责人筛选（filter[ownerId]） | 200 | 15.7 ms | 17.2 ms |
| PASS | A6 | 负责人筛选 p95 阈值 + 全部含该负责人 | 期望：<= 800 ms + 全部命中 | 实际：17.2 ms / 越界 0 条 / total=1114 |
| L4 | 列表 · 关键字（装配 · 全量命中） | 200 | 29.7 ms | 30.7 ms |
| PASS | A7 | 关键字筛选 p95 阈值 + total=活跃数 | 期望：<= 800 ms + total=9800 | 实际：30.7 ms / total=9800 |
| L5 | 列表 · 展示态筛选（filter[status]=overdue） | 200 | 17.7 ms | 18.9 ms |
| PASS | A8 | 展示态筛选 p95 阈值 | 期望：<= 800 ms | 实际：18.9 ms / total=8398 |
| L6 | 项目总览四格（GET /summary） | 200 | 6.9 ms | 7.6 ms |
| PASS | A9 | 总览四格 p95 阈值 + total=活跃数 | 期望：<= 500 ms + total=9800 | 实际：7.6 ms / total=9800 |
| L7 | 阶段完成度（GET /stages） | 200 | 9.2 ms | 11.1 ms |
| PASS | A10 | 阶段完成度 p95 阈值 + 计数合计=带阶段活跃数（未分组不计） | 期望：<= 800 ms + 合计=8816 | 实际：11.1 ms / 合计=8816 |
| L8 | 甘特图 · 10 页 x 200 条（共 2000 行） | 200 | 33.5 ms | 44.0 ms |
| PASS | A11 | 甘特图单页（200 条）p95 阈值 | 期望：<= 900 ms | 实际：44.0 ms（10 页合计 348.7 ms） |

### SQL 计划与索引对照（EXPLAIN ANALYZE · 收尾还原库结构）

| 形状 | 说明 | p50 | p95 | 命中索引 / 计划 |
|---|---|---|---|---|
| S1 | 列表 · 默认读序第 1 页（limit 50） | 4.3 ms | 7.1 ms | SEQ SCAN tasks |
| S2 | 列表 · 阶段筛选（design）第 1 页 | 0.4 ms | 0.4 ms | ix_tasks_project_stage |
| S3 | 列表 · 负责人筛选（任一命中）第 1 页 | 2.3 ms | 2.4 ms | SEQ SCAN tasks |
| S4 | 列表 · 关键字（装配 · 全量命中）第 1 页 | 9.1 ms | 9.2 ms | SEQ SCAN tasks |
| S5 | 列表 · 关键字（M3K-777 · 稀疏命中）第 1 页 | 6.1 ms | 6.4 ms | SEQ SCAN tasks |
| S6 | 列表 · 展示态筛选（逾期）第 1 页 | 4.3 ms | 4.6 ms | SEQ SCAN tasks |
| S7 | 项目总览四格（total / done / overdue 三个计数） | 2.9 ms | 2.9 ms | SEQ SCAN tasks |
| S8 | 阶段完成度（按 stage_key 分组计数） | 2.8 ms | 2.8 ms | SEQ SCAN tasks |
| S9 | 甘特图 · 单页取数（limit 200 · 第 1 页） | 4.8 ms | 4.9 ms | SEQ SCAN tasks |
- 索引 ix_tasks_active_group：496 kB（507904 bytes）
- 索引 ix_tasks_project_stage_order：496 kB（507904 bytes）
| PASS | A12 | 对照前旧索引存在（迁移 0015 建立） | 期望：n=1 | 实际：n=1 | 马上 DROP 做前后对照 |

- 已 DROP ix_tasks_project_stage_order（对照态：任务表只用部分索引 ix_tasks_active_group 兜底）
| DS1 | 列表 · 默认读序第 1 页（limit 50） · 下线旧索引后 | 4.1 ms | 4.7 ms | SEQ SCAN tasks |
| DS2 | 列表 · 阶段筛选（design）第 1 页 · 下线旧索引后 | 0.4 ms | 0.5 ms | ix_tasks_project_stage |
| DS3 | 列表 · 负责人筛选（任一命中）第 1 页 · 下线旧索引后 | 2.3 ms | 2.5 ms | SEQ SCAN tasks |
| DS4 | 列表 · 关键字（装配 · 全量命中）第 1 页 · 下线旧索引后 | 9.3 ms | 9.4 ms | SEQ SCAN tasks |
| DS5 | 列表 · 关键字（M3K-777 · 稀疏命中）第 1 页 · 下线旧索引后 | 6.1 ms | 6.3 ms | SEQ SCAN tasks |
| DS6 | 列表 · 展示态筛选（逾期）第 1 页 · 下线旧索引后 | 4.1 ms | 4.2 ms | SEQ SCAN tasks |
| DS7 | 项目总览四格（total / done / overdue 三个计数） · 下线旧索引后 | 2.8 ms | 2.9 ms | SEQ SCAN tasks |
| DS8 | 阶段完成度（按 stage_key 分组计数） · 下线旧索引后 | 2.8 ms | 2.9 ms | SEQ SCAN tasks |
| DS9 | 甘特图 · 单页取数（limit 200 · 第 1 页） · 下线旧索引后 | 4.8 ms | 4.9 ms | SEQ SCAN tasks |

- p50 倍率（下线后 / 基线）：S1 0.94x / S2 0.99x / S3 1.00x / S4 1.02x / S5 1.01x / S6 0.95x / S7 0.97x / S8 1.00x / S9 0.99x
| PASS | A13 | 下线旧索引后最差形状的 p50 倍率 <= 1.5x | 期望：worst <= 1.5x | 实际：S4 1.02x |
| PASS | A14 | 下线旧索引后默认读序（S1）p50 阈值 | 期望：<= 800 ms | 实际：4.1 ms |
| PASS | A15 | 对照结束后把旧索引建回（库结构与迁移一致） | 期望：n=1 | 实际：n=1 |

## 汇总

- 全部断言通过：数据集 A1 / A2 + API 时延 A3 ~ A11 + 索引对照 A12 ~ A15。

## 验收对照（M3-06）

- 「1 万行数据集」= A1 / A2：单项目 1 万行任务落库（九阶段 + 未分组 · 8 位负责人 · 200 条软删），关键字与分布可复现。
- 「列表 / 筛选 / 总览 / 甘特取数性能」= A3 ~ A11：默认读序 50 条分页、阶段 / 负责人 / 关键字（全量与稀疏）/ 展示态筛选、项目总览四格、阶段完成度、甘特 10 页 x 200 条取数，p95 全部在阈值内。
- 「索引调优评估」= A12 ~ A15：DROP ix_tasks_project_stage_order 前后 9 个形状逐一对齐（p50 倍率 + EXPLAIN 命中计划），评估由部分索引 ix_tasks_active_group 取代旧索引的可行性；对照结束把旧索引建回（默认与迁移结构一致）。
- 复跑：cd server && M3_STRESS_DATABASE_URL=postgresql://libiaolink_migrator@127.0.0.1:55432/libiaolink node scripts/m3-06-stress.mjs --out ../docs/m3-06-压测证据.md


##[group]Run node scripts/m6-replay.mjs
[36;1mnode scripts/m6-replay.mjs[0m
shell: /usr/bin/bash -e {0}
env:
  M6_DATABASE_URL: postgresql://libiaolink_migrator@127.0.0.1:5432/libiaolink
  M6_BASE_URL: http://127.0.0.1:3011
##[endgroup]

## 附：迁移 0024 下线态复跑（Push 158 · CI 真机）

> 上文正文为 Push 157（`fd213ac`）旧索引对照态的逐行转写；本附注为 PR #127（`21d35b8` → 合并 `fb1ace7`）迁移 0024 落库后的**下线态**复跑，同一 `database` job 口径（仅剥离时间戳前缀）。

| 项 | 值 |
|---|---|
| 执行环境 | GitHub Actions `ubuntu-24.04` · job `database`（check run 106694066906） |
| 代码版本 | `21d35b8`（PR #127 头提交；squash 合并提交 `fb1ace7`） |
| 执行时间 | 2026-09-22 09:41:54Z ~ 09:42:44Z（北京时间 17:41 ~ 17:42） |
| 数据库 | `postgres:18` service 5432，空库迁移 0001 ~ 0024（含 0024 下线旧索引）之后执行 |
| 目标 api | `http://127.0.0.1:3011`（真实 NestJS 进程 + 真 PG，非 mock） |
| 脚本 | `server/scripts/m3-06-stress.mjs`（下线态分支） |

原文（索引对照段 · 逐行转写）：

```text
| 形状 | 说明 | p50 | p95 | 命中索引 / 计划 |
|---|---|---|---|---|
- 索引 ix_tasks_active_group：496 kB（507904 bytes）
| PASS | A12 | 旧索引已下线（迁移 0024 落库后 · 直接按下线态跑） | 期望：n=0（下线态） | 实际：n=0 | 无对照基线：改为逐形状压预算 + 记录计划 |

- 旧索引不存在（迁移 0024 已下线）：直接按「部分索引 ix_tasks_active_group 兜底态」跑 9 形状
| DS1 | 列表 · 默认读序第 1 页（limit 50） · 下线态 | 4.3 ms | 6.0 ms | SEQ SCAN tasks |
| DS2 | 列表 · 阶段筛选（design）第 1 页 · 下线态 | 0.4 ms | 0.5 ms | ix_tasks_project_stage |
| DS3 | 列表 · 负责人筛选（任一命中）第 1 页 · 下线态 | 2.5 ms | 2.7 ms | SEQ SCAN tasks |
| DS4 | 列表 · 关键字（装配 · 全量命中）第 1 页 · 下线态 | 9.4 ms | 9.6 ms | SEQ SCAN tasks |
| DS5 | 列表 · 关键字（M3K-777 · 稀疏命中）第 1 页 · 下线态 | 6.4 ms | 8.8 ms | SEQ SCAN tasks |
| DS6 | 列表 · 展示态筛选（逾期）第 1 页 · 下线态 | 4.3 ms | 4.4 ms | SEQ SCAN tasks |
| DS7 | 项目总览四格（total / done / overdue 三个计数） · 下线态 | 2.8 ms | 2.9 ms | SEQ SCAN tasks |
| DS8 | 阶段完成度（按 stage_key 分组计数） · 下线态 | 2.8 ms | 2.9 ms | SEQ SCAN tasks |
| DS9 | 甘特图 · 单页取数（limit 200 · 第 1 页） · 下线态 | 4.8 ms | 4.9 ms | SEQ SCAN tasks |

- 旧索引已下线（无基线对照）：逐形状 p50 / 预算 —— DS1 4.3 ms / 800 ms / DS2 0.4 ms / 800 ms / DS3 2.5 ms / 800 ms / DS4 9.4 ms / 800 ms / DS5 6.4 ms / 800 ms / DS6 4.3 ms / 800 ms / DS7 2.8 ms / 500 ms / DS8 2.8 ms / 800 ms / DS9 4.8 ms / 900 ms
| PASS | A13 | 旧索引已下线（0024）：9 形状逐条压预算 | 期望：各形状 p50 <= 预算 | 实际：DS1 4.3 / 预算 800 / DS2 0.4 / 预算 800 / DS3 2.5 / 预算 800 / DS4 9.4 / 预算 800 / DS5 6.4 / 预算 800 / DS6 4.3 / 预算 800 / DS7 2.8 / 预算 500 / DS8 2.8 / 预算 800 / DS9 4.8 / 预算 900 |
| PASS | A14 | 下线旧索引后默认读序（S1）p50 阈值 | 期望：<= 800 ms | 实际：4.3 ms |

- 旧索引本就不存在（迁移 0024 已下线），无需建回
```

结论：旧索引物理下线后 9 个读取形状 p50 全部在预算内（最慢 DS4 9.4 ms / 预算 800 ms，余量 85 倍），默认读序 p50 4.3 ms（阈值 800 ms）、A15 不再建回（库结构 = 迁移 0024 落库态）→ 读面由部分索引 `ix_tasks_active_group` 兜底，索引下线定案生效；回滚 = 迁移 0024 注释内的 `create index`。
