# 市场项目管理系统 · 功能点清单（定档版）

> 版本：V1.1（定档）｜编制日期：2026-09-17｜口径来源：《市场项目管理系统-系统功能书-V1.0》＋本次定档补充口径（模板化流程节点、ONLYOFFICE 在线编辑、成果文件必交校验、变更申请即通过、首页分类侧边栏）
> 修订：2026-09-17 按当前系统口径对齐字段并清理引用：A1-01 字段范围、A1-06 自动计算字段、A1-13 进度展示；A1-11 里程碑与依赖、A2-03 部门维度移至二期；A1-17 / C9-06 模板字段同步；去掉已删字段引用（A1-04、A1-12、A1-15、A1-16、A2-07、A2-09、B1-01、B2-03、B2-04、B3-01、B3-06、B3-09、B4-04、B4-08、B5-01、C9-07、D5-04）；星级进度统一改为四格进度。
> 修订（2026-09-18，Push 34）：按两角色口径更正——项目级唯一责任人为「项目经理」（projects.manager_id，必填），任务级为「任务负责人」（tasks.owner_id）；A1-18 分类维度「负责人」更正为「项目经理」，A1-12、B5-01 去掉「总负责人」表述。
> 修订（2026-09-18，项目序号）：A1-12 项目主数据新增系统分配的「项目序号」（全库唯一、不可修改、不回收；与项目编号一一对应同一项目；首页卡片右上角两位补零展示，可排序 / 检索）。
> 修订（2026-09-19，Push 67）：A1-01 任务表默认显示列按业务定稿调整 —— 默认只隐藏「预计所需施工人数」；「文件」「项目进展描述」由默认隐藏改为默认显示（其余列与「预计所需天数跟随两日期列」的口径不变）。同一批口径：逾期标注由「实际完成日期」列移到「是否按时交付」列（未完成超期 = 「逾期未交付」、完成晚于预计完成日期 = 「逾期已交付」）；「已延期」不因点四格进度条被改成「待开始 / 进行中」。
> 修订（2026-09-20，Push 82）：A1-04 分组视图落地口径 —— 项目详情页顶部导航定为三视图「项目总览 / 人员任务分配 / 任务进展」：**项目总览**保留九阶段分组任务表（阶段标签不再各占一格，仍可从阶段分组头开「任务节点 + 模板」卡片加任务）；**人员任务分配** = 按任务负责人分组（列头头像 + 姓名 + N 项，「待分配」垫底）；**任务进展** = 按任务状态分组，固定顺序 已延期 → 进行中 → 已完成 → 提前完成 → 待开始（空分组保留）。两块钱看板的任务状态取与任务表同一套派生（待开始 / 进行中 / 已完成 / 提前完成 / 已延期），卡片字段 = 任务描述（中英文）/ 预计完成日期（进展看板为开始日期）/ 任务状态（负责人看板）/ 任务负责人（进展看板）/ 项目进度 / 是否按时交付 / **所属阶段**（阶段色签；任务字段里没有「里程碑」这一项，卡片不写「阶段性里程碑」字样），卡片材质按业务样张（外层壳 + 内层板 + 细纹叠加 + 三层投影）。原型口径；源文档（V1.0）侧需人工同步同一条。
> 修订（2026-09-20，Push 101）：A1-13 任务详情抽屉里的**任务状态**与**实际完成日期**改为可改（业务反馈「抽屉里仍不能改任务状态 / 实际完成日期 这个也要可以修改」）—— 状态 = 五态色标签下拉，联动同任务表（完成态 = 四格全亮、进行中 = 至少一格、待开始 = 清零、已延期 = 保持格数；改成非完成态时实际完成日期一并清空）；实际完成日期 = 单日期小日历（填 = 完成、清 = 退回进行中）；抽屉进度条与条上四颗点统一为**绿色**（与任务表四格点同色，业务反馈「为什么是灰的 应该也要绿的」）；抽屉只读口径收窄为 是否按时交付 / 输出成果文件 / 文件 / 变更关联。源文档（V1.0）侧需人工同步同一条。
> 修订（2026-09-20，Push 98）：A1-13 四格进度第 4 档档位名由「完成了」改为「已完成」（业务口径；任务表 / 任务详情抽屉 / 看板卡片三处一致）。同一批口径：四格进度可在任务详情抽屉里直接改（进度条长度不变，**四颗点平均分布在条上**：1/4 / 2/4 / 3/4 / 末尾 = 刚开工 / 完成一半 / 快完成了 / 已完成）；看板卡片（人员任务分配 / 任务进展）的进度一栏由百分比改为中文档位，并新增「实际完成日期」一栏，可在卡片上直接点选修改（填 = 完成、清 = 退回进行中）。源文档（V1.0）侧需人工同步同一条。
> 修订（2026-09-20，Push 71）：C2-02 内置规则 R01~R07 文案逐字基准入仓 `docs/rules/R01-R07-内置规则文案.md`（M0-04）；M0 决议（ADR-019 ~ ADR-029）与本文定档口径冲突时，以 ADR 为准并回写本文。
> 修订（2026-09-21，Push 120）：项目详情页三视图（A1-04）的看板交互定档 —— ① 「人员任务分配 / 任务进展」两块看板的**拖动换列 = 编辑**沿用 B2-01（卡片拖拽换人）/ B2-02（拖拽变更状态）的既有口径：拖到别的列放开 = 改任务负责人 / 改任务状态，并沿用同一套联动（状态与四格进度、实际完成日期互写）；② 两处**合法空值**确认 —— 任务可以没有所属阶段（卡片显示「未分组」、项目总览落最后一组）、任务负责人可以为空（「待分配」列，「负责人待定」是合法中间状态，卡片拖进该列即清空负责人）；③ 看板**列内顺序**可拖动调整（同列内也能换位置），「添加 → 阶段任务」时可指定插入位置（该阶段最后 / 最前 / 在某张同阶段任务之后），顺序与「项目总览」任务表的组内顺序是**同一条**，且**持久保存**（刷新 / 换设备 / 换人打开都保持）。原型侧已按此实现；源文档（V1.0）侧需人工同步同一条。
> 修订（2026-09-21，Push 128）：A1-04 项目详情页顶部导航由**三视图扩为四视图** —— 在「项目总览 / 人员任务分配 / 任务进展」之后新增第四个视图「**日报及问题**」（业务口径「标签导航栏旁边新加『日报及问题』导航」）：入口指向 A3 日报与问题跟踪（日报字段同 A3-01、问题四态同 A3-10 未分组 → 未解决 → 处理中 → 已完成），按项目查看、与 A7 日报中心的项目口径一致；该视图内部再分**四块子视图**（业务口径「把这个部分做成页内导航栏 …… 第一个日报填写、第二个日报记录、第三个问题追踪（用表格记录日报填写的问题）、第四个问题看板」）：**页内导航栏**（四个键帽按钮，一律白键帽、排在主标签栏正下方；当前项黑字加粗、未选项灰字）从左到右 = **日报填写 / 日报记录 / 问题追踪 / 问题看板**，默认停在「日报填写」——①「日报填写」= A3-01 字段的日报表单（时间 + 当日完成工作必填，A3-04；「现场发现问题」非空时问题归类必填），提交 / 暂存草稿都写进「日报记录」，**提交时「现场发现问题」自动生成一条问题记录**（A3-09 幂等口径）；②「日报记录」= 按日期倒序的日报列表；③「问题追踪」= 问题表格（明细列出由日报生成的问题及处理状态）；④「问题看板」= 四态看板（未分组 → 未解决 → 处理中 → 已完成，空列保留）。列显隐（任务表）与底部横向滑块仍只在「项目总览」出现。原型侧日报 / 问题存浏览器内存（换项目 / 刷新即重置），问题的状态流转与解决方案回复待后续迭代；源文档（V1.0）侧需人工同步同一条。
> 修订（2026-09-22，Push 136）：业务口径「一个项目可以有多个项目经理和任务负责人」——① **项目级「项目经理」由单一改为可多位**：数组（至少一位、顺序 = 展示顺序），多位按「、」连接展示；新建 / 编辑项目弹窗、任务表行内、任务详情抽屉三处都是多选（取消最后一位不生效）；首页分类侧边栏项目数徽标每位各计一次、筛选「任一位命中即命中」、搜索任一姓名命中即命中 —— 原 Push 34「项目级唯一责任人」口径改为「至少一位」。② **任务级「任务负责人」由单一改为可多位**：两组同下标数组（可多位、**空集 = 待分配**，沿用 B2-01 / Push 120 的「待分配」合法态）；任务表列与任务详情抽屉都是多选，人员任务分配看板按每位负责人各出一列（同一任务在多列出现、列计数 = 该人手上的任务数），拖动换列 = 负责人整体换成拖入列那一位、拖进「待分配」= 清空。前端原型（内存态）已按此实现；**落库 / 迁移 / 契约的多位口径已同批落地**（迁移 `0017_multi_manager_and_owner.sql` + 契约 `managerIds` / `ownerIds`；`前端功能需求.md` §3.8 A22 / A23、第六章 Q16），源文档（V1.0）侧需人工同步同一条。
> 修订（2026-09-22，Push 138）：A1-18 首页分类侧边栏新增「**常用筛选**」——员工可把一组分类条件（地区 / 项目类型 / 项目经理 / 项目时间，任意多项）命名保存（名称自定义 ≤ 20 字、最多 20 组），保存后即出现在侧栏第一组、点一下复用该组合；交互 = 点「添加」→ 在下方分类勾选多项 → 命名 → 保存（保存即生效）；组合带命中项目数，筛选 / 计数口径与本节其余维度一致；**应用侧复用既有筛选参数、服务端无新接口**，跨设备记忆待后端定契约（`前端功能需求.md` §3.8 A24）。技术可行性由 wmj 复核、实现一致性由 px 复核（双签以 PR 评审记录为准）。
> 修订（2026-09-22，Push 141）：A1-01 任务表行悬停入口由「编辑任务」铅笔改为「删除任务」按钮（业务口径「不要这个笔作为编辑了 这个基本上不用 改成删除按钮吧」）—— 形态 = 静止为 24px 幽灵态圆形按钮（无底色、浅灰垃圾桶图标），悬停 / 键盘聚焦展开成 48px 小号红底「删除」胶囊（展开只吃任务描述列右侧预留的固定 48px 槽位、槽位右侧另留 16px 留白，四格进度条与描述文字不位移）；点删除只作用于该行（不打开任务详情抽屉），删除后任务从本项目列表移除、该阶段完成度与整体进度随之重算；编辑入口仍为整行点击 / 看板卡片 → 任务详情抽屉。同一批：**任务表列头吸顶**（业务反馈「这个标题栏要固定 鼠标移动可以依旧显示」）—— 列头移出横向滚动容器、固定在应用顶栏（64px）下方，纵向滚动始终可见；横向滚动（视口不够 / 底部滑块）时列头同步平移、与各列保持对齐。原型阶段删除为浏览器内存态（换项目 / 刷新复位），正式版走任务删除接口（`前端功能需求.md` §3.8 A25：建议软删 + `TASK_DELETED` / 404 口径待 wmj 定）。尺寸与色彩经同日三轮业务反馈收定（「这个状态下有点太大了 要再小一点」「鼠标触碰列表时候黑的太突兀了 还是要比较看不出来吧」「当项目经理大于两位时 就是会触碰到」——展开胶囊右缘原与「项目经理」列齐平，留白后不再贴列）；技术可行性由 wmj 复核、实现一致性由 px 复核（双签以 PR 评审记录为准）。
> 修订（2026-09-22，Push 145）：A1-04 项目详情页顶部导航由**四视图扩为五视图** —— 在「项目总览」之后新增「**甘特图**」（业务口径「我想做甘特图」「在标签导航栏加一个甘特图页面」，形态参考 VTable 甘特图基本用法）：左侧固定任务列表（行号 / 任务 / 开始日期 / 预计完成日期 / 进展）+ 右侧时间轴（月份 + 逐日两级表头、周末底纹、今天虚线）；按施工阶段分组（空阶段照出骨架行、非九阶段落「未分组」垫底、阶段可折叠），任务条 = 开始 → 预计完成（**青 = 已完成进度、橙 = 剩余工期**），单日任务标**里程碑菱形**（一期无「是否里程碑」字段 —— A1-11 / B3-02 在二期 —— 前端暂按单日派生，字段口径登记 `前端功能需求.md` §3.8 A28）、**实际完成日期 = 条上深色刻度**（B3-01 前半，未画第二条）、**已延期 = 条头一段红内嵌色条**（B3-05；条本身不画描边，见 B3-05 修订）、**今天 = 蓝底日期格 + 蓝色虚线**；工具条 = **粒度日 / 周 / 月**（标准档列宽：日 26px / 周 91px / 月约 138px）+ 三档缩放（每档粒度各一套每天宽度）、**甘特内筛选**（负责人含「待分配」/ 只看已延期 / 只看未排期 + 命中计数 + 清空筛选）、全部折叠 / 展开、回到今天。**B3-09「一期不实现视图、仅保留菜单入口位」按本次业务口径提前落地为原型视图**（B3 段内已同步），B3-03 依赖箭头 / B3-04 关键路径 / B3-07 导出仍不在本期；**B3-08 的拖动条形改期 / 拖边缘改工期已按业务口径「维持手绘尽量还原官方的功能」提前落地为原型**（差「依赖冲突自动校验」—— 依赖字段 A1-11 在二期），同一轮还补齐**条选中 / 悬停遮罩 / 进度圆点拖拽改进度 / 点左表行定位任务条 / 拖左表右边线 = 移动分隔线（左表列宽固定、被时间轴盖住超出的列）**（对应官方 `taskBar.selectable` / `hoverBarStyle` / 进度调节 / `gantt-locate-taskbar` / `frame.verticalSplitLineMoveable`），仍**不引第三方图表库**、拖动只写既有字段；数据与任务列表同一份，**无新增接口与字段**；末轮补**左表「开始日期 / 预计完成日期」两列**（对齐官方样例左表 start / end，业务口径「我这个移动也要有这些信息 现在没做」）—— 拖动条 / 拖两端 / 拖圆点时这两列跟着预览值实时更新，松手才写回、Esc 回位。技术可行性由 wmj 复核、实现一致性由 px 复核（双签以 PR 评审记录为准）。
> 修订（2026-09-22，Push 148）：① **任务删除要做**（业务确认）—— 任务软删（`DELETE /api/v1/projects/{id}/tasks/{taskId}`：列表 / 看板 / 甘特图 / 完成门禁均不可见 + 写留痕；重复删除的 404 / 409 口径由 wmj 定）列入落地范围，契约与实现归后端线，前端行内删除入口已就绪（原型内存态）；② 甘特图口径确认：未排期 = 开始日期或预计完成日期**任一未设定**、取数按现状量级（**不做「单项目 1 万行」专门设计**）、里程碑按「单日任务」派生（字段随 A1-11 二期）；③ 依赖三条（B3-03 / B3-04 / B3-08 冲突校验）**暂不做**。
> 修订（2026-09-22，Push 151）：① 甘特图里「单日任务（开始 = 预计完成）」的菱形**按状态取色**（业务口径「没有完成是灰色 延期是红色」）—— 已完成 / 提前完成 = 绿色、未完成 = 灰色、已延期 = 红色；图例项由「里程碑（单日任务）」改名为「**单日任务**」（色块仍为绿菱形，业务口径「任务栏还是绿色的要」）；② 「问题归类」的录入控件统一为站内下拉（与任务状态 / 紧急重要度同一套：白底描边胶囊 + 弹层绿勾选中项；未填「现场发现问题」时禁用），不再用浏览器原生 `<select>`。
> 修订（2026-09-22，Push 146）：A1-07 / A4-13 / A4-14 / A4-16「变更关联」**落点写清**（wmj 在 PR #111 评审点名：勿与「单列覆盖」两处表述打架）—— 业务要求「变更关联」列**展示多条**，规则 R01 的「追加＋去重」**直接落库多条**：变更生效时按「任务成果类型命中变更文件 doc_type」判定命中任务，命中即**追加 + 去重**写入 `tasks.change_refs uuid[]`（数组顺序 = 关联先后、末位 = 最近一次变更；空数组 = 无变更），任务列表 / 任务详情随行下发**多条**（变更 id / 变更原因短文本 / 生效时间），「变更关联」列按多条徽标渲染；原 `tasks.change_ref` 单值列（= 最近一次变更，覆盖式）随迁移 `0020_task_change_refs.sql`（原 0019，撞 main j6 的 `0019_stakeholders.sql` 后顺延）**作废**。技术可行性由 wmj 复核、实现一致性由 px 复核（双签以 PR 评审记录为准）。源文档（V1.0）侧需人工同步同一条。
> 修订（2026-09-22，Push 152）：**任务删除（A2-01 / A1-01）后端已落地**（Push 148 定案「要做」的实现口径）——① 契约 `DELETE /api/v1/projects/{id}/tasks/{taskId}`（软删，**不物理删行**，历史与留痕保留），响应 `{ id, deleted: true }`；② 可见性：列表 / 看板 / 甘特图 / 详情 / 完成门禁 / 阶段计数一律不可见（读面过滤 `deleted_at is null`）；③ **重复删除与已删任务上的任何操作 = 统一 404**（不引入 `TASK_DELETED`，沿用记录级 404 语义）；④ **A2-01「已产生日报 / 问题 / 变更的任务不允许删除」按现有数据面执行** —— 已有变更记录（`tasks.change_refs` 非空）→ 409 `TASK_HAS_REFERENCES`（提示「只能关闭或标记」），日报 / 问题两表随 M5 落表后在同处加判定；⑤ 删除后组内位次自动压缩（保持 0 起、密集），来源节点回到「可添加」；⑥ 留痕 = 审计（删除前快照）+ outbox `task.deleted`。
> 修订（2026-09-22，Push 153）：**锁定字段例外调整（A1-17 / C9-07）后端已落地** —— ① 锁定字段 = 任务描述 / 英文描述 / 输出成果文件：按流程节点模板生成后锁定，常规编辑（任务表行内 / 任务详情抽屉保存）与批量操作均不含该三项（**无常规编辑通道**，前端只读展示）；② 例外调整 = `PATCH /api/v1/projects/{id}/tasks/{taskId}/locked-fields`：**仅系统管理员**（非管理员 403），`reason` 必填（1~500）并留痕（审计 + outbox `task.locked_fields_adjusted`），至少一个实际变化（空调整 400 `VALIDATION_FAILED`）；③ 差异登记：「阶段性里程」一期 `tasks` 表无对应列，本期不开放该字段（A1-17 映射修订）；前端管理端接线随 M3-07。
> 修订（2026-09-22，Push 155）：**日报与问题（A3）后端已落地**（M6-01 ~ M6-03 第一刀，wmj 线；迁移 `0023_daily_reports_issues.sql` + 契约 `shared/src/modules/reports.ts` / `issues.ts` + 模块 `server/src/modules/report-issue/`）—— ① **接口路径为项目嵌套**：`GET|POST /api/v1/projects/{id}/reports`、`GET|PATCH …/reports/{reportId}`、`GET …/issues`、`GET|PATCH …/issues/{issueId}`（提案的扁平路径在 `ProjectAccessGuard` 下拿不到项目上下文，差异登记 `前端功能需求.md` §3.8 A21）；② 日报 = 一人一项目一天一条（重复 409 `REPORT_ALREADY_EXISTS`）、草稿 / 提交 / 补填共用同一行（补填由服务端按日期推导）、未来日期 400；③ 提交触发两件**幂等**副作用（A3-08 回写任务「项目进展描述」+ A3-09 自动生成「未分组」问题）；④ 问题四态**允许回退且留痕**（不设流转白名单，不做 `ISSUE_TRANSITION_INVALID`）、每次更新写一条处理过程事件（状态 / 解决方案 / 分派）；⑤ 差异登记：归类十项为契约固定枚举（C9 可维护随后）、`due_at` 一期仅落库（T+1 / T+3 随 M5）、一期不做日报版本历史、自动分派一期只覆盖部门名归类（原因类归「未分组」）、日报附图未落表（随 M4 关联切片）；⑥ 同批补齐 A2-01 删除引用守卫（日报 / 问题）。
> 修订（2026-09-22，Push 154）：A1-07「变更关联」的**前端多条展示已落地**（px 线，兑现 Push 146 修订段点名的「前端渲染由 px 线落地」）—— 任务表「变更关联」列 = 一枚琥珀色「变更」徽标（多条时徽标右侧「+N」计数、单行不换行，同「文件」列口径；悬浮显示全部「变更日期 + 变更原因」）；任务详情抽屉 = 每条一行（「变更 {日期}」徽标 + 原因文本）。**仅展示层改动**：落库 / 契约（`tasks.change_refs`、`Task.changeLinks`）与判定口径不变。
> 修订（2026-09-23，Push 168）：**地区新增改为「全站共享、非管理员也能加」**（业务定调：「要全站共享但非管理员也能加」）—— C9-02 补充：新增 `region` 条目任何登录用户都可提交（保存后全站可见、可在首页按它筛选；每次变更写审计留痕、记实际提交人）；`projectType` 等其余字典的新增、以及所有字典条目的改名 / 排序 / 删除仍归管理员（`dict.manage`）—— 治理权不放开。定档口径见 C9-02。
> 修订（2026-09-23，Push 169）：A1-18「常用筛选」的**记忆范围改为按账号、跨设备**（业务口径「这个筛选是针对用户自身的 所以不同用户的账号上应该不一样」）—— 组合落服务端用户偏好（A4：`GET / PATCH /api/v1/users/me/preferences` 的 `homeSavedFilters` 键，表 `user_preferences`）：**同一账号换设备可见；同一设备换账号互不可见**；原「退出登录清除」的本地口径随之取消（属账号隔离，不再按设备隔离）；上限仍 20 组 / 名称 20 字，保存 / 删除 = 整体替换、失败可见提示。技术可行性由 wmj 复核、实现一致性由 px 复核（双签以 PR 评审记录为准）。源文档（V1.0）侧需人工同步同一条。
> 修订（2026-09-23，Push 170）：A1-01「**列显隐**」的记忆范围定档 —— 勾选 / 重置按**账号**存服务端（用户偏好 `user_preferences.taskTableHiddenColumns`，键白名单 = 任务表**非锁定列**），同一账号换设备保持、同一设备换账号互不可见；「任务描述」仍常显不可隐藏、预计所需天数仍跟随开始 / 预计完成日期；默认列不变（仅**预计所需施工人数**隐藏）。技术可行性由 wmj 复核、实现一致性由 px 复核（双签以 PR 评审记录为准）。源文档（V1.0）侧需人工同步同一条。
> 修订（2026-09-23，Push 171）：「项目总览」的「**醒目模式**」开关（A1-04 · 前端功能需求 §6.13）记忆范围定档 —— 开关值按**账号**存服务端（用户偏好 `user_preferences` 的 `focusMode` 键，与列显隐 / 常用筛选同一端点 `GET / PATCH /api/v1/users/me/preferences`），**同一账号换设备 / 刷新 / 换项目保持、同一设备换账号互不可见**；默认关（无记录 = false）。原「原型内存态（刷新回默认关、正式版是否落本地记忆待业务定）」口径取消；开关只影响显示（打开 = 任务表整行铺该任务状态底色），不改任务字段、不写审计。
> 修订（2026-09-23，Push 172）：C9-01 / C9-02 补「地区 / 项目类型下拉里的增删 + 颜色模板」前端口径 —— ① **新增**：地区保持「任何登录用户可加」（Push 168 口径）；项目类型新增照旧仅管理员（`dict.manage`），新增时从**预置色板**（11 色）选颜色、写 `metadata.accent` / `accentText`（业务口径「颜色之前应该有模板」；不提供自由取色器）；② **删除 = 停用**（C9-02）：下拉每行尾部的隐式删除按钮调 `PATCH /api/v1/dicts/{type}/items/{code}`（`enabled: false`，仅 `dict.manage`）—— 不新增 DELETE 路由、契约零改动；停用不影响存量数据展示、只是不再进下拉候选；③ **项目卡片**：编辑入口改为悬停 / 键盘聚焦才显示（业务口径「要鼠标触碰才显示 不触碰不显示」），并在编辑右侧加任务表行内同款**隐式删除按钮**（删除项目 = A5 软删，`If-Match: version`，点一下先出底部确认条、第二下才真删）。
> 修订（2026-09-23，Push 173）：① **C9-02 字典删除改为「真删除」**（业务口径「删除了不就删除了吗 为什么叫已停用」「删除了就没有记忆了 为什么还要记着呢 逻辑有问题」）—— 前端下拉每行尾部的隐式删除按钮由 `PATCH enabled:false` 改调 `DELETE /api/v1/dicts/{type}/items/{code}`（仅 `dict.manage`）：服务端**从 `dict_items` 物理删行**、删除前快照写审计（`action=delete`，字段级 `code/name/sort/enabled/metadata → null`）；**删除无记忆** —— 同码可重新新增、按全新条目处理（本次所选颜色、排到末尾），界面不出现「已停用 / 恢复 / 此前已删除」字样；存量项目安全（`projects.region` / `project_type` 是冗余名、无外键），仍按原码 / 原名渲染；`enabled` / `includeDisabled` 降级为兼容字段与兼容参数（二期「临时下架」）。契约 + 后端 + 迁移（`0030_dict_item_hard_delete.sql`）+ 前端同批落地。② **建 / 改项目入口按权限收敛**（前端呈现层，服务端仍逐请求裁决）：无 `project.create` 时「新建项目」出禁用观感 + 提示条；无 `project.update` 时卡片编辑入口不渲染。③ **字典下拉浮层贴右弹出**、触发器箭头朝右（与弹出的落点同向）；新增项目类型的**颜色模板只列「尚未被启用类型占用」的颜色**（全用满回落完整色板）。技术可行性由 wmj 复核、实现一致性由 px 复核（双签以 PR 评审记录为准）。
> 修订（2026-09-23，Push 174）：**C9-02 字典删除加「引用守卫」**（业务口径「当存在项目的类型或者地区在卡片里面的时候 不允许在这里删除」）—— 条目正被**未删除项目**引用（`projects.region` / `projects.project_type` 命中该码）时**不允许删除**：① 下发侧：`GET /api/v1/dicts` 的每个条目带 `usageCount`（引用该码的未删除项目数；**软删项目不计**，归档项目仍占卡片、照数）；② 删除侧：`DELETE /api/v1/dicts/{type}/items/{code}` 在事务内先判引用，> 0 直接 **409 `DICT_ITEM_IN_USE`** —— 不删行、不 touchType、不写审计（引用解除后即可删，存量项目不受影响）；③ 前端：下拉里该行的删除位**置灰、点不动、悬停出原因**（「正被 N 个项目使用，不能删除」），缓存陈旧时由服务端 409 兜底。新错误码入 `shared/src/common/errors.ts`（回写技术设计 §7.2 错误模型表与 §4.8 增补清单）。技术可行性由 wmj 复核、实现一致性由 px 复核。
> 修订（2026-09-23，Push 175）：**首页列表排序改为「维度 × 方向」、侧栏「项目时间」区间筛选的维度改为项目创建时间**（业务口径：「我发现现在的这个根据时间排序没用了」「这个 time 改成 按创建时间 和更新时间排序 默认按创建时间」「这个筛选是按照项目创建时间筛选」）—— ① **排序**：工具条 TIME 由「仅切方向」升级为「**维度**（创建时间 `createdAt` / 更新时间 `updatedAt`）× **方向**（降序 / 升序）」，**缺省 = 创建时间 × 降序（最近创建的在前）**（原缺省 = 更新时间降序，作废）；维度与方向随账号记忆（用户偏好，与 A1-18 常用筛选同一端点 `GET / PATCH /api/v1/users/me/preferences`）。② **侧栏「项目时间」区间筛选的取数维度由「最近活动时间」改为「项目创建时间」**（`filter[timeFrom]` / `filter[timeTo]` 口径同步改；技术设计 v0.3 §7 第 4 项「最近活动时间」口径作废；侧栏 ⓘ 提示文案同步为「按项目创建时间筛选」）。同批修掉「排序切换无效」的缺陷（前端请求键未含排序参数，切方向不重取）。**无字段、无 DDL、无迁移**（只改查询维度与缺省排序）。技术可行性由 wmj 复核、实现一致性由 px 复核（双签以 PR 评审记录为准）。
> 修订（2026-09-23，Push 176）：**排序维度的控件形态收敛**（业务口径「把 time 去除了 然后把更新时间 创建时间放上去 字体也是偏灰的 并且可以点击 点击一次换一下不同的时间」）—— 首页工具条 TIME 标识**下线**，排序维度由「两枚维度按钮」改成一枚**灰字可点文本**：显示的即当前维度（创建时间 `createdAt` / 更新时间 `updatedAt`），**点一次换一个维度**，悬停提示写明当前口径与切换目标；「降序 / 升序」两枚方向按钮保留。**纯展示层改动**：请求参数、缺省 `createdAt:desc`、URL 与偏好记忆口径全部沿用 Push 175，无字段 / 契约 / 服务端 / DDL 变化。
> 修订（2026-09-23，Push 177）：**排序维度固定为创建时间，更新时间维度下线（工具条只留方向）**（业务口径「取消按更新时间排序吧 不要这个 只保留创建时间」）—— 首页工具条排序分组里的维度文本改为**不可点的灰色说明**（显示「创建时间」），原「点一次换个维度」交互取消；「降序 / 升序」两枚方向按钮保留（缺省降序 = 最近创建的在前）。前端只产生 `createdAt:desc` / `createdAt:asc`（缺省不写入 URL 的 `sort`），旧的 `sort=updatedAt:*` 链接仍可用（**忽略维度、只读方向**）；排序仍随账号偏好记忆（A4）。**纯展示层与前端状态口径改动**：契约白名单仍含 `updatedAt`（兼容保留、前端不再产生），无字段 / 服务端 / DDL / 迁移变化。

> 修订（2026-09-23，Push 178）：**一期权限口径 = 不判权限（环境开关落地）**（业务口径「我们当前这个系统就不要考虑权限」）—— 服务端新增开关 `PERMISSION_ENFORCED`（默认 `false`）：授权画像一律等效管理员（数据范围 `all` + 契约全量权限位 + `admin` 角色码），C3 权限矩阵（功能权限 / 记录级可见性 / 字段级脱敏 / 导出单独授权）与「任务手工创建仅管理员」（A1-13）、「节点增删与阶段推进仅项目经理」（ADR-020）、「文件彻底删除仅系统管理员」（A4-12）三处硬检查**一期一并放开**；开关置 `true` = 按 ADR-011 判定（二期打开，届时恢复本功能书 C3 各条口径）。只影响**裁定**、不动数据（用户偏好仍按账号各存一行）。技术可行性由 wmj 复核、实现一致性由 px 复核（双签以 PR 评审记录为准）。
> 修订（2026-09-24，Push 179）：**任务状态五态可写 + 汇总卡改「最慢 / 最新阶段」+ 紧急重要度回三档**（业务 2026-09-24 定案三连：「状态下拉都要有 要5态 但是他们的逻辑要和之前的一样」/「（`currentStage` 保持 = 项目当前阶段）有了最快最慢这个就不需要了」/「紧急重要度 和前端一致只有三档」）——① **A1-06 / A2-01**：任务状态写入由基础三态扩为**五态可写** —— 「已延期 / 提前完成」不再只是派生展示，而是**显式覆盖**（落新列 `tasks.status_override`，读时覆盖优先、边界 = 已延期仅未完成生效 / 提前完成仅已完成生效），联动沿用原型口径（已延期 = 保持当前格数与完成日期、提前完成 = 四格全亮 + 完成日期缺省当天；写进度 / 写基础三态 / 门禁完成即清覆盖）；② **项目总览汇总卡**：`currentStage`（项目当前阶段）下线，改 **最慢阶段**（九阶段序第一个存在未完成任务的阶段）+ **最新阶段**（已动工任务里阶段序最靠后者的所属阶段），均随任务派生（`projects.stage_key` 本身仍是「项目当前阶段」、阶段推进口径不变）；③ **紧急重要度**统一为三档「高 / 中 / 低」（四象限作废，存量由迁移 `0031` 折算）。技术可行性由 wmj 复核、实现一致性由 px 复核；真机回放 24 项断言全过。
> 编号规则：模块编号（A1~D5）＋两位序号。【定档】＝本次定档新增或调整的功能点；期次标注：一期 / 二期 / 预留。

---

## 一、A 业务核心模块

### A1 项目总览

流程参考（项目全生命周期，本模块功能点按该主线组织）：

    售前规划 → 设计开发 → 加工采购 → 组装发货 → 硬件实施 → 软件部署 → 试运行 → 生产阶段 → 验收 → 项目归档与结项

#### A1-01 任务全字段维护（一期 · 字段口径修订）
- 一行＝一个任务，支持按阶段分组；是全系统任务数据的唯一入口与主数据。
- 字段范围（当前系统口径，表格共 15 列）：任务描述（中英文两行）、项目经理、任务负责人（中英文）、任务状态、紧急重要度、是否按时交付、输出成果文件、文件、项目进展描述、开始日期、预计所需天数、预计完成日期、预计所需施工人数、实际完成日期、变更关联。
- 不作为独立列的字段：阶段性里程（表格按九阶段分组，在分组头展示完成情况）、项目进度（四格进度条内嵌在任务描述列，任务详情展示档位文字与进度条）、是否逾期（【修订 2026-09-19，Push 67】逾期标注改挂在「是否按时交付」列 / 字段：未完成且超过预计完成日期 = 「逾期未交付」，已完成但晚于预计完成日期 = 「逾期已交付」；「逾期」不进入任务状态枚举，任务状态列为派生 5 态：待开始 / 进行中 / 已完成 / 提前完成 / 已延期）。
- 列显隐：任务描述常显锁定；**预计所需施工人数**默认隐藏，其余默认显示（【修订 2026-09-19，Push 67】业务定稿：文件、项目进展描述由「默认隐藏」改为「默认显示」）；预计所需天数跟随开始日期与预计完成日期两列，二者任一隐藏时该列自动隐藏（见 A1-02）。【修订 2026-09-23，Push 170】**勾选结果按账号记忆、跨设备生效**（服务端用户偏好 `user_preferences.taskTableHiddenColumns`；同一账号换设备保持、同一设备换账号互不可见），「重置」= 回到本节默认列。
- 任务详情抽屉始终展示全部字段，不受表格列显隐影响。
- 任务描述、阶段性里程、输出成果文件由流程节点模板带出并锁定（见 A1-17），其余可编辑字段按 C3 权限控制。
- 【修订】按当前系统口径去掉：任务编号、所属项目、星级进度、实际开始日期、责任部门、协同部门、前置/后置任务、剩余天数、任务健康度、完成检查项、任务类型、是否里程碑、关注人、关联日报数/问题数/变更次数、归档状态、创建与更新审计字段；新增字段：预计所需施工人数。
- 每次修改写字段级留痕（谁、何时、从什么改成什么）。
- 【修订 2026-09-22，Push 136】「项目经理」「任务负责人」两列均可多位：项目经理 = 项目级数组（至少一位，多位按「、」连接展示，编辑入口三处都是多选、取消最后一位不生效）；任务负责人 = 任务级多位（空集 = 待分配，任务表列与任务详情抽屉都是多选）。展示 / 编辑 / 计数 / 筛选口径见 A1-04 与 A1-18；落库与契约多位口径已落地（`0017_multi_manager_and_owner.sql`：`projects.manager_ids` / `tasks.owner_ids` 数组列 + `filter[...]` 任一位命中 + facets 每位各计一次），见 `前端功能需求.md` §3.8 A22 / A23。**技术可行性已随本批契约与 0017 落库验证（wmj 线代落）；双签以 PR 评审记录为准**。
- 【修订 2026-09-22，Push 141】任务描述列的行悬停入口由「编辑任务」铅笔改为**「删除任务」按钮**：行悬停（或键盘聚焦）浮现，静止 = 24px 幽灵态圆形按钮（无底色、浅灰垃圾桶图标），悬停 = 展开成 48px 小号红底「删除」胶囊（展开只吃预留槽位、槽位右侧另留 16px 留白 —— 展开胶囊右缘不贴「项目经理」列，四格进度条与描述文字不位移）；点删除只作用于该行、不打开任务详情抽屉，删除后任务从本项目列表移除，阶段完成度与整体进度随之重算；编辑入口仍为整行点击 / 看板卡片打开任务详情抽屉。同批：**列头吸顶** —— 任务表列头（含「一键收起」）纵向滚动时固定在应用顶栏（64px）下方，横向滚动时同步平移与各列对齐。原型阶段为浏览器内存态（换项目 / 刷新复位）；正式版任务删除接口口径见 `前端功能需求.md` §3.8 A25（软删 + 级联口径待 wmj 定）。
- 【修订 2026-09-22，Push 148】业务确认「**任务删除要做**」：任务软删列入落地范围 —— 契约 `DELETE /api/v1/projects/{id}/tasks/{taskId}`（软删：列表 / 看板 / 甘特图 / 完成门禁均不可见 + 写留痕；对已删任务再次操作 404 还是 409 `TASK_DELETED` 由 wmj 定）与实现归后端线（wmj）；前端行内删除入口已实现（原型内存态），接口落地后把 §6.8 / §6.14 A25 的接线补上。
- 【修订 2026-09-22，Push 152】**任务删除后端已落地**（wmj 线）：`DELETE /api/v1/projects/{id}/tasks/{taskId}`（软删 + 留痕）—— 读面一律不可见（列表 / 看板 / 甘特图 / 详情 / 完成门禁）；**重复删除 = 统一 404**（不引入 `TASK_DELETED`）；**引用口径按 A2-01**：已产生变更记录的任务不允许删除（409 `TASK_HAS_REFERENCES`，`details[].code = change_ref`），日报 / 问题两表随 M5 落表后同处加判定；删除后来源节点回到「可添加」、组内位次自动压缩。前端行内入口随 M3-07 接线。

#### A1-02 表格视图（一期）
- 类 Excel 交互：行内编辑、批量粘贴、多选、列宽与列序自定义、冻结列、列显示隐藏。
- 单项目 1 万行量级下滚动与筛选流畅；分页或虚拟滚动加载。
- 所有单元格编辑遵守模板锁定与字段权限：无权限字段不展示、不返回。

#### A1-03 视图管理（一期）
- 保存个人视图与公共视图：我的任务、本周到期、已延期、按阶段分组、按负责人分组等。
- 视图内容＝筛选条件＋列配置＋排序＋分组方式的组合；支持设默认视图、共享给指定角色。
- 视图仅保存配置，不复制数据；打开视图实时反映最新数据。

#### A1-04 分组与折叠（一期）
- 支持按项目、阶段、负责人、状态分组，分组层可折叠展开。
- 分组头显示组内任务数量与完成情况统计；分组内排序规则可配置。
- 【修订 2026-09-21，Push 120】项目详情页三视图的看板口径（承 Push 82）：① **人员任务分配 / 任务进展** 两块看板的**拖动换列 = 编辑**（沿用 B2-01 / B2-02 手势）—— 拖到别的列放开 = 改任务负责人（拖进「待分配」= 清空负责人）/ 改任务状态，联动同 A1-13；② **合法空值**：任务阶段可为空（卡片「所属阶段」显示「未分组」、项目总览落最后一组）、任务负责人可为空（「待分配」列 = 「负责人待定」）；③ **列内顺序**可拖动调整（同列内也能换位），「添加 → 阶段任务」可指定插入位置（该阶段最后 / 最前 / 在某张同阶段任务之后），顺序与项目总览任务表的组内顺序是同一条并**持久保存**。
- 【修订 2026-09-21，Push 128】项目详情页顶部导航由三视图扩为**四视图**：新增「**日报及问题**」（排在「任务进展」之后），视图内部为**页内导航栏**（日报填写 / 日报记录 / 问题追踪 / 问题看板，默认「日报填写」）——① 日报填写 = A3-01 字段表单（必填校验同 A3-04），提交 / 暂存草稿写进日报记录，提交时「现场发现问题」自动生成一条「未分组」问题（A3-09 幂等）；② 日报记录 = 按日期倒序的日报列表（字段同 A3-01）；③ 问题追踪 = 问题表格（由日报生成的问题明细 + 处理状态）；④ 问题看板 = 四态看板（未分组 → 未解决 → 处理中 → 已完成，空列保留，口径同 A3-10）；列显隐（任务表）与底部横向滑块仍只在「项目总览」出现。原型侧日报 / 问题存浏览器内存（问题的状态流转与解决方案回复待后续迭代）。
- 【修订 2026-09-22，Push 136】「人员任务分配」看板按**每位任务负责人各出一列**（同一任务在多列出现，列计数 = 该人手上的任务数）；拖动换列 = 负责人整体换成拖入列那一位（多位 → 一位）、拖进「待分配」列 = 清空负责人（空集）。
- 【修订 2026-09-22，Push 145】项目详情页顶部导航由**四视图扩为五视图**：在「项目总览」之后新增「**甘特图**」（排在「项目总览」与「人员任务分配」之间）—— 左侧固定任务列表（行号 / 任务 / 开始日期 / 预计完成日期 / 进展）+ 右侧时间轴（月份 + 逐日两级表头、周末底纹、每周一 / 每月一号分隔线、今天虚线）；按施工阶段分组、组内顺序同项目总览，阶段行可折叠（另有全部折叠 / 展开）；任务条 = 开始日期 → 预计完成日期（青 = 已完成进度、橙 = 剩余工期），**单日任务标里程碑菱形**，**已延期 = 条头一段红内嵌色条**（条本身不画描边），**实际完成日期 = 条上深色刻度**，今天 = 蓝底日期格 + 蓝虚线；工具条含**粒度（日 / 周 / 月）**、三档缩放与**甘特内筛选（按负责人 / 只看已延期 / 只看未排期）**、「回到今天」。数据源与任务列表同一份（`GET /api/v1/projects/{id}/tasks`），**无新增接口 / 字段**；B3-01 双条对比只做「计划条 + 实际完成刻度」、B3-03 / B3-04 / B3-07 与日 / 周 / 月粒度切换（B3-06）不在本期；**B3-08 的拖动条形改期 / 拖边缘改工期已提前落地为原型**（另含条选中 / 悬停遮罩 / 进度圆点改进度 / 点左表行定位 / 拖左表右边线 = 移动分隔线（左表列宽固定、被时间轴盖住超出的列；分隔线把手与 2px 高亮线画在裁切层之外，拖动另有 window 级兜底），口径见 `前端功能需求.md` §6.14），差「依赖冲突自动校验」（依赖字段 A1-11 在二期）。
- 【修订 2026-09-23，Push 171】「项目总览」右上角的「**醒目模式**」开关记忆范围定档：**按账号存服务端**（用户偏好 `GET / PATCH /api/v1/users/me/preferences` 的 `focusMode` 键，布尔；与 A1-01 列显隐、A1-18 常用筛选同一端点 / 同一张 `user_preferences`）—— 同一账号换设备 / 刷新 / 换项目保持上次选择，同一设备换账号互不可见；默认关（无记录 = false）；开关只切显示态（打开 = 项目总览任务表每行铺该任务状态的底色，见《前端功能需求》§6.13），不改任务字段、不入审计。

#### A1-05 快速筛选（一期）
- 一键筛选：今天到期、本周到期、已逾期、未完成、我负责的、我关注的、未上传成果文件（【修订 2026-09-22，Push 136】任务负责人可多位：「我负责的」= 任务负责人名单内任一位是我）。
- 支持组合筛选与筛选条件回显、一键清空。
- 首页分类侧边栏筛选（A1-18）与快速筛选叠加生效。

#### A1-06 自动计算字段（一期 · 字段口径修订）
- 自动计算：项目进度（四格档位 0 / 25% / 50% / 75% / 100%）、是否按时交付、是否逾期、阶段完成率与项目整体完成率（完成数 ÷ 总数）。
- 是否按时交付口径（【修订 2026-09-19，Push 67】逾期标注位）：实际完成日期不晚于预计完成日期标记「按时交付」；未完成不标记；**未完成且已过预计完成日期 = 「逾期未交付」，已完成但晚于预计完成日期（或未填完成日期、但预计完成日期已过）= 「逾期已交付」**。
- 任务状态派生口径（展示 5 态）：完成时按实际完成日期与预计完成日期比较——早于 = 提前完成；不早于 = 已完成（逾期补完按已完成、是否按时交付 = 否，不回退「提前完成」）；未完成且已过预计完成日期 = 已延期；其余按基础态 待开始 / 进行中。
- 【修订 2026-09-24，Push 179】读时口径含**显式覆盖**（`tasks.status_override`）：有覆盖时优先取覆盖值（已延期仅未完成生效、提前完成仅已完成生效，边界不满足回落下面的派生），无覆盖（或边界不满足）时按上述派生；**五态均可写入**（写面契约 `TaskStatusWrite`）。
- 是否逾期口径（【修订 2026-09-19，Push 67】标注位调整）：未完成且预计完成日期早于今天 —— 红色「逾期未交付」标注落在「是否按时交付」列与任务详情，任务状态列同时展示「已延期」；「逾期」不是任务状态取值，也不再标注在实际完成日期列（该列只显示完成日期或「—」）。
- 项目经理可手动覆盖项目进度，覆盖必须填写原因并留痕。

#### A1-07 变更关联展示（一期）
- 展开任务即见全部变更记录：时间、阶段、前后内容、版本、原因、申请人。
- 变更关联由规则 R01 自动写入（追加＋去重），人工修改留痕。
- 【修订 2026-09-22，Push 146】「追加＋去重」的落点 = `tasks.change_refs uuid[]`（写面追加 + 去重、数组顺序 = 关联先后、末位 = 最近一次变更）；「变更关联」列**展示多条**（任务表列 / 任务详情抽屉两处；前端渲染由 px 线落地）。
- 【修订 2026-09-22，Push 154】前端多条展示已落地：任务表「变更关联」列 = 一枚琥珀「变更」徽标（多条时右侧「+N」计数、单行不换行，悬浮「变更日期 + 变更原因」）、任务详情抽屉 = 每条一行（徽标 + 原因）；演示数据 3 条（均 2026/8/11：包装 2 条 + 物料清单完整版 1 条）。
- 变更记录仅展示不覆盖历史；历史永久保留。

#### A1-08 批量操作（一期）
- 批量指派负责人、批量改状态/日期/紧急重要度、批量导出、批量生成节点任务（从模板，见 A1-16）。
- 批量操作逐条校验权限与模板锁定规则，部分失败时返回失败清单及原因。
- 批量操作整体写审计日志。

#### A1-09 相关记录联动（一期）
- 任务详情内可见关联的日报、问题、文件、待办、评论、变更记录，并可跳转对应模块。
- 关联关系支持双向查看：从日报/问题/文件也可回到所属任务。

#### A1-10 操作留痕（一期）
- 每个字段的修改历史可查：操作人、时间、修改前后值、修改入口（页面/批量/系统自动）。
- 系统自动流转（状态、实际日期、进度）单独标注来源为「系统」，可人工纠正并留痕。

#### A1-11 里程碑与依赖（二期 · 字段口径修订）
- 【修订】当前任务字段不含「是否里程碑」「前置/后置任务」，一期不做里程碑标注与依赖维护，数据模型字段一并后移。
- 里程碑：模板中的里程碑节点自动点亮，阶段里程碑状态随节点完成自动更新。
- 依赖：前置/后置任务维护与列表展示、依赖冲突提示（前置未完成、后置早于前置），关键路径计算与甘特图（B3）同期。

#### A1-12 项目建档与阶段推进（一期 · 定档调整）
- 项目建档维护项目主数据：项目编号、项目名称、客户名称、项目经理、项目相关群聊、项目实施阶段、计划开始/计划完成、项目状态、合同信息、干系人、项目进度（四格）。
- 【修订 2026-09-22，Push 136】项目主数据的「项目经理」可多位（数组、至少一位、顺序 = 展示顺序；多位按「、」连接展示）—— 原 Push 34「项目级唯一责任人」口径相应改为「至少一位」；落库 / 契约多位口径已落地（`0017_multi_manager_and_owner.sql`；`前端功能需求.md` §3.8 A22）；**技术可行性已随本批契约与 0017 落库验证（wmj 线代落），双签以 PR 评审记录为准**。
- 【定档】项目主数据新增「地区」「项目类型」两个分类字段（枚举取值由 C9 字典维护），供首页分类侧边栏（A1-18）与统计使用；未填写归入「未分类」。
- 【修订 2026-09-23，Push 172】「地区」「项目类型」两个分类字段的**取值集合可在新建 / 编辑弹窗内就地扩缩**：地区任何登录用户可加（Push 168 口径）、项目类型仅管理员可加（新增时从预置色板选颜色，C9-01）；两类条目均可由管理员「删除」（Push 173 起 = 物理删行，C9-02）—— 删除不影响存量项目按原值展示，只是不再出现在候选与首页筛选项里。**Push 174 起加引用守卫**：正被项目卡片使用的地区 / 项目类型不给删（删除位置灰 + 服务端 409 `DICT_ITEM_IN_USE`）。
- 【定档】项目主数据新增「项目序号」：项目创建时由系统自动分配（全库唯一正整数、不可修改、不回收——删除项目不重排、不重发），与「项目编号」一一对应同一个项目；首页项目卡片右上角以两位补零展示（≥100 原样），列表支持按序号排序与检索，便于快速定位与对账。
- 建任务：项目建档后按所属项目类型的流程节点模板生成任务清单（A1-16），节点顺序与阶段归属固定，不随项目随意增删。
- 阶段推进：当前阶段任务全部完成、且要求成果文件齐全后，方可推进到下一阶段；九阶段顺序固定：售前规划 → 设计开发 → 加工采购 → 组装发货 → 硬件实施 → 软件部署 → 试运行 → 生产阶段 → 验收；推进与回退均留痕。
- 阶段完成度＝该阶段已完成任务数 ÷ 该阶段任务总数，自动计算并展示。

#### A1-13 进度展示（四格进度，一期 · 字段口径修订）
- 项目进度以四格进度条展示：全灰＝未开始；1 格＝刚开工；2 格＝完成一半；3 格＝快完成了；4 格＝**已完成**（【修订 2026-09-20，Push 98】第 4 档中文档位名由「完成了」改为「已完成」，任务表 / 任务详情抽屉 / 看板卡片三处一致）。
- 四格进度条固定在任务描述列内，不随列筛选与横向滚动移动；悬停浮现档位文字与「项目进度：档位」提示。
- 点击第 N 格把进度设为该档（前 N 格点亮）；点击当前已点亮的最后一格回退一档（误点修正）。
- 任务详情同步展示档位文字与进度条；【修订 2026-09-20，Push 98】四格进度**在任务详情抽屉里也能直接改** —— 进度条长度不变，**四颗点平均分布在条上**（1/4 / 2/4 / 3/4 / 末尾 = 刚开工 / 完成一半 / 快完成了 / 已完成）点哪颗写哪档，联动口径与任务表一致（A1-13 上述双向联动照旧）。
- 【修订 2026-09-20，Push 98】看板卡片（人员任务分配 / 任务进展）的进度一栏由百分比改为**中文档位**（标签同上），并新增「实际完成日期」一栏 —— 卡片上直接点选小日历即可修改（填 = 完成、清 = 退回进行中），与 A1-13 的进度 / 状态联动口径一致。
- 【修订 2026-09-20，Push 101】任务详情抽屉里的**任务状态**与**实际完成日期**同样可直接改（原先只有任务表行内 / 看板卡片可改）—— 状态为五态色标签下拉，联动口径同 A1-13（改成非完成态时实际完成日期一并清空）；实际完成日期为单日期小日历（填 = 完成、清 = 退回进行中）。抽屉的进度条与条上四颗点统一用**绿色**（与任务表四格点同色），进度条只表达「做了多少」，状态色仍由状态签 / 色点表达；抽屉只读口径 = 是否按时交付 / 输出成果文件 / 文件 / 变更关联。

#### A1-14 成果文件挂接（一期）
- 任务挂接输出成果文件与过程文件，提供上传、关联与定档入口。
- 「输出成果文件」类型来自模板（A1-17），文件挂接时按类型归类，便于缺件检查与校验。
- 任务完成提交时执行必交校验（A4-20）。

#### A1-15 关注/订阅（一期）
- 关注项目或任务后，状态变更、延期、变更生效等关键事件实时收到通知。
- 关注清单在任务详情可见，支持批量关注/取消；关注关系单独存储，不作为任务字段。

#### A1-16 流程节点模板化建任务（一期 · 定档）
- 项目环节的流程节点采用固定模板：任务只能从所属项目类型对应的流程节点模板中选择生成；不支持自由新建节点，不支持手工输入任务描述。
- 选择方式：使用节点选择器（按九阶段分组、支持中英文名称搜索），可批量勾选多个节点一次生成；同一项目已生成过的节点默认不重复生成，重复选择时提示。
- 生成结果：按模板逐字段填充任务（填充映射见 A1-17）；节点顺序、阶段归属与模板一致。
- 非标准事项：不进入流程节点模板，使用「自定义待办」（C2-06）挂接到相关节点；确需创建非标准任务时仅管理员可建。
- 模板启用范围：新建项目默认套用所属项目类型的模板；历史项目可按管理员导入的历史任务建档，但节点仍须在模板中存在。

#### A1-17 模板内容填充与任务侧锁定（一期 · 定档）
- 具体模板＝流程节点模板，每个节点固定包含下列字段；生成任务时按映射表把模板内容填写到任务字段（重点：把模板的「节点名称（任务描述）」原样填写到任务描述）：

| 模板字段 | 写入任务字段 | 规则 |
| --- | --- | --- |
| 节点名称（任务描述，中英文） | 任务描述 | 原样写入；任务侧只读，不可随意编辑 |
| 阶段性里程 | 阶段性里程 | 锁定，不可编辑 |
| 要求输出成果文件（多选） | 输出成果文件 | 锁定；作为提交校验（A4-20）与缺件提醒依据 |
| 默认工期（天） | 预计所需天数 | 作为预计完成日期默认值计算依据 |
| 默认紧急重要度 | 紧急重要度 | 生成时可调整，调整留痕 |

- 【修订】映射同步当前任务字段口径：去掉责任部门/协同部门、前置任务、是否里程碑、完成检查项四行；默认工期写入「预计所需天数」。
- 任务侧锁定规则：任务描述、阶段性里程、输出成果文件生成后不可编辑；确需修正时由管理员修正模板（C9-07），并对当期任务执行例外调整，必须填写原因并留痕。【落地口径 2026-09-22 · Push 153】一期 `tasks` 表无「阶段性里程」列（映射修订），实际开放三项 —— 任务描述（`title`）/ 英文描述（`titleEn`）/ 输出成果文件（`deliverableTypes`）：常规编辑与批量均不可达；例外调整走 `PATCH …/tasks/{taskId}/locked-fields`，仅系统管理员、原因必填并留痕（审计 + outbox `task.locked_fields_adjusted`）。
- 未配置的模板字段不写入，对应任务字段留空或取系统默认值。
- 【修订 2026-09-24 · Push 187】**节点库侧映射的落地范围收口（业务定案）**：① 「默认工期（天）→ 预计所需天数」与「默认紧急重要度 → 紧急重要度」两行 —— **本期不做**：节点库（`task_nodes`）不加这两列，生成任务时工期留空、紧急重要度由调用方给（不按模板带出）；② 「要求输出成果文件（多选）→ 输出成果文件」一行 —— **要做但暂缓**（随文件功能 / 缺件校验一起做）：在此之前从节点库或模板生成的任务 `deliverableTypes` 为空数组，只有「项目流程节点」来源才按 `required_doc` 带出。前端侧登记见 `前端功能需求.md` A30 与变更记录 Push 187。

#### A1-18 首页分类侧边栏（一期 · 定档）
- 位置：首页（项目总览）左侧固定侧边栏，可折叠；右侧为项目列表与任务台账主体。
- 分类维度：地区、项目类型、项目经理，默认三项；维度与取值由 C9 字典维护，可扩展（如项目状态、实施阶段、客户）。
- 交互规则：单选/多选组合筛选；每个分类项显示项目数徽标；「全部」恢复默认视图；与关键字搜索、表格筛选叠加生效；记忆并恢复用户上次选择。
- 权限与口径：仅统计与展示当前用户数据范围内（C3）的项目，无权限项目不展示、不计数；「项目经理」取项目主数据字段（必填）；【修订 2026-09-22，Push 136】项目经理可多位（数组、至少一位）—— 每个分类项的项目数徽标按每位各计一次，筛选按「任一位命中即命中」。
- 数据依赖：项目主数据的「地区」「项目类型」字段（A1-12）；未填写的项目归入「未分类」。
- 【修订 2026-09-22，Push 138】新增「**常用筛选**」（业务口径「不同的员工可能注意的项目不一样 所以需要分类 点击添加后选择多项 别的普通分类 然后保存 即出现在常用筛选 筛选的名字可以自定义」）：侧栏第一组，每位员工可保存若干组「名称 + 分类条件」组合 —— 添加（「+ 添加」→ 就地编辑卡片）→ 勾选多项（可跨「地区 / 项目类型 / 项目经理 / 项目时间」，不影响当前列表）→ 命名（自定义，≤ 20 字）→ 保存（**立即出现在「常用筛选」并生效**）；组合可改名 / 可删除（悬停胶囊浮出铅笔与 ×），胶囊右侧显示命中项目数；上限 20 组；【修订 2026-09-23，Push 169】**记忆范围改为按账号、跨设备** —— 组合存服务端用户偏好（`user_preferences.homeSavedFilters`；同一账号换设备可见、同一设备换账号互不可见），保存 / 删除为整体替换、失败可见提示；原「退出登录清除」的本地口径取消（那是账号自己的记忆）。应用中的组合高亮（按条件等价判定）。
- 【修订 2026-09-23，Push 175】侧栏「项目时间」一组的区间筛选**按项目创建时间**取数（原「最近活动时间」口径作废）；首页列表排序由工具条 TIME 控制 = 维度（创建时间 / 更新时间）× 方向（降序 / 升序），**默认创建时间降序**，随账号记忆（同 A4 偏好）；**【修订 2026-09-23，Push 176】该控件的形态收敛为「一枚灰字可点文本（显示当前维度，点一次换一个）+ 两枚方向按钮」，TIME 标识下线**。**【修订 2026-09-23，Push 177】排序维度固定为创建时间（灰字文本改为不可点的说明，更新时间维度下线、工具条只留方向）**。

---

### A2 任务计划

流程参考（任务状态机，A2 与 A1 同源数据）：

    待开始 →（到达开始日期）→ 进行中 →（按预计日期完成）→ 已完成
                                        └（早于预计日期完成）→ 提前完成
       └（超过预计完成日期未完成）→ 已延期 →（补完）→ 已完成（是否按时交付 = 否）

- 【修订 2026-09-24，Push 179】上图的 5 态在**写入侧全部可选**：「已延期 / 提前完成」由用户显式指定并落 `tasks.status_override`（读时覆盖优先），其余三态走基础态同事务联动进度与完成日期（A12）；点进度条 / 写基础三态 = 清除覆盖、回到下面这套派生。
- 口径说明：任务状态为 5 态（待开始 / 进行中 / 已完成 / 提前完成 / 已延期），由日期字段派生；「逾期」不是状态取值——【修订 2026-09-19，Push 67】逾期标注落在「是否按时交付」列与任务详情（未完成超期 = 「逾期未交付」、完成晚于预计完成日期 = 「逾期已交付」），实际完成日期列只显示日期或「—」。

#### A2-01 任务新建与编辑（一期 · 定档调整）
- 【定档】新建任务只允许从流程节点模板选择（A1-16），不接受手工输入任务描述；行内编辑仅开放未被模板锁定的字段（A1-17）。
- 负责人指派：可从项目成员中选择，支持指派给一人为主责、多人协同；指派后自动通知（A05）。
- 并发编辑保护：乐观锁控制，冲突时返回提示并保留用户已输入内容，不静默覆盖。
- 任务删除为软删除并留痕；已产生日报/问题/变更的任务不允许删除，只能关闭或标记。**【修订 2026-09-22，Push 152 · 落地口径】**：软删 = `tasks.deleted_at` / `deleted_by`（不物理删行）；重复删除与已删任务上的任何操作统一 404；引用守卫已按现有数据面落地 —— 变更记录（`tasks.change_refs` 非空）→ 409 `TASK_HAS_REFERENCES`（`details[].code = change_ref`），**【修订 2026-09-22，Push 155】**：日报 / 问题两类引用**已随 M6-01 ~ M6-03 落表并在守卫处一并加判定** —— `daily_reports.task_ids`（GIN 索引）与 `issues.task_id` → 409 `TASK_HAS_REFERENCES`（`details[].code = report_ref` / `issue_ref`，带条数），任一非空即不软删、不压缩位次、不写留痕。

#### A2-02 时间维度排序（一期）
- 默认按开始日期/预计完成日期排序，支持按周、月分段展示。
- 支持自定义排序（拖拽或指定序号）并保存为视图（A1-03）。

#### A2-03 部门维度（二期 · 字段口径修订）
- 【修订】当前任务字段不含「责任部门 / 协同部门」，一期不做部门维度分组与筛选，字段后移。
- 启用时：责任部门、协同部门字段维护，支持按部门分组或筛选；部门取值取组织架构（D1），调整自动带入、可人工覆盖并留痕。

#### A2-04 表格能力补齐（一期）
- 筛选、批量操作、分组等能力面向单项目 1 万行量级设计。
- 支持导出当前视图（受 C3 权限与导出审计控制）。

#### A2-05 计划编制（一期 · 定档调整）
- 【定档】按项目类型的流程节点模板批量生成节点清单（一次生成或按阶段分批生成），生成结果包含节点顺序、阶段、成果文件要求（A1-16 / A1-17）。
- 批量导入：支持历史/线下计划导入；导入的任务描述必须匹配模板节点，不匹配的进入异常清单人工处理。
- 复制历史项目任务时，同样以模板节点为准校验。

#### A2-06 快速改期（一期）
- 表格内批量改期（开始/预计完成日期）；改期触发未发送提醒按新日期重算（C2）。
- 改期原因非必填，但批量改期整体留痕；（二期）甘特内拖动条形改期。

#### A2-07 计划/实际对比（一期）
- 计划（开始日期 → 预计完成日期）与实际完成日期并排展示；逾期高亮。
- 用于周会与专项统计（B5）取数。

#### A2-08 时间冲突提示（二期）
- 同一负责人同期任务过多时提示（规则 A18）；提示不阻断保存。

#### A2-09 状态与进度更新（一期）
- 任务状态（5 态：待开始 / 进行中 / 已完成 / 提前完成 / 已延期）与四格进度更新；实际完成日期自动或手动回填。
- 状态派生：「已延期」＝未完成且已过预计完成日期；回填实际完成日期后按日期派生为「已完成 / 提前完成」（晚于预计完成日期按已完成、是否按时交付 = 否）。
- 【定档】提交「已完成/提前完成」时执行成果文件必交校验（A4-20）：要求成果文件的任务无文件不允许提交，返回缺件清单。
- 状态变更联动：进度重算、看板列移动、喜报（R06）、成果文件提醒（R02）按规则执行。

#### A2-10 成果文件上传与定档（一期）
- 任务节点上传成果文件并挂接「文件」字段；支持多个文件、按类型归类。
- 定档锁版：定档后修改须走变更管理（A4-13）；未定档文件可直接替换/删除。
- 未上传成果文件的任务不允许提交完成（A4-20）；已上传未定档允许提交但立即提示定档。

#### A2-11 人员负荷热力（二期）
- 按人员在制任务数/人天展示负荷（规则 A16）。

---

### A3 日报与问题跟踪

流程参考（日报—问题闭环）：

    现场人员填日报 → 日报中心自动汇总 → 项目群推送
                         ├─ 无问题：归档为日报记录
                         └─ 有问题：自动生成问题记录 → 问题看板（未分组 → 未解决 → 处理中 → 已完成）
                                    → 按归类自动分派责任部门/责任人 → 处理并回复解决方案
                                    → 提出人确认关闭 → 回写项目/任务问题统计

#### A3-01 在线日报表单（一期）
- 字段：项目名称、时间、当日完成工作、今日施工人数、明日计划、现场工作附图、现场发现问题、问题归类、解决方案或建议、当前问题附图；另含提交人、提交时间、关联任务、状态系统字段。
- 【修订 2026-09-22，Push 151】「问题归类」的录入控件统一为站内**下拉组件**（与任务状态 / 紧急重要度、甘特内负责人筛选同一套：白底描边胶囊 + 弹层选项，选中项绿勾）；未填「现场发现问题」时**禁用**（灰底灰字、点不开）—— 与本条既有的「为空时置灰」一致，不再用浏览器原生下拉。
- 【修订 2026-09-22，Push 155 · 后端已落（M6-01 / M6-02）】表单全字段落库（契约 `DailyReport`：`date` / `headcount` / `doneWork` / `plan` / `foundIssue` / `issueCategory` / `suggestion` / `taskIds` + `authorId` / `submittedAt` / `state` / `version`）；「现场工作附图 / 当前问题附图」一期未落列（`daily_report_files` 未落表，随 M4 文件关联切片 —— 差异登记）。
- 桌面端填报为主入口；表单字段枚举一期取**契约固定枚举**（归类十项；C9 字典可维护随后）。

#### A3-02 草稿 / 提交 / 补填（一期）
- 【修订 2026-09-22，Push 155 · 后端已落（M6-01 / M6-02）】草稿 / 提交 / 补填共用同一行（一人一项目一天一条，`uq_daily_reports_author_date`）；重复填报 409 `REPORT_ALREADY_EXISTS`（提示改用编辑）；补填 = 对过去日期首次提交（`state` 由服务端推导为 `supplement`，客户端只提 `draft` / `submitted`）；已提交 / 补填行不允许退回草稿；一期不做版本历史 —— 「补填保留原始提交记录」以状态 + `submittedAt` 表达（差异登记）。

#### A3-03 智能带入（一期）
- 选择项目自动带入任务清单、负责人、阶段；施工人数默认带上次填报值，可修改。
- 关联任务可多选，用于回写任务进展。

#### A3-04 提交校验（一期）
- 必填校验：项目、日期、当日完成工作；「现场发现问题」非空时问题归类必填。
- 【修订 2026-09-22，Push 155 · 后端已落（M6-01 / M6-02）】必填校验 = 日期 + 当日完成工作（1~2000 字）；「现场发现问题」非空 → 问题归类必填（契约 superRefine + 库侧成对 CHECK `ck_daily_reports_issue_pairs`）；未来日期 400（只允许当天或补填过去日期）；提交失败不清空已填内容（前端行为不变）。

#### A3-05 常用模板（一期）
- 常用日报内容另存为模板，一键填充后再修改；模板按人可见，可共享给部门。

#### A3-06 一键转任务/待办（一期）
- 日报中提到的临时事项一键生成待办或（管理员）非标准任务，默认挂接到相关流程节点。

#### A3-07 移动端填报（二期）
- 拍照上传、弱网草稿本地保存（IndexedDB），联网后自动提交。

#### A3-08 关联任务回写（一期）
- 【修订 2026-09-22，Push 155 · 后端已落（M6-01 / M6-02）】提交后把「当日完成工作」追加到关联任务的「项目进展描述」（`tasks.note`）+ 写 `task_events(note_change)` 留痕，**幂等标记 = `【日报 <日期>】`**（同任务同日期只追加一次）；只动 `note`、不动任务乐观锁版本（与变更记录 R01 同口径）。

#### A3-09 问题自动生成（一期）
- 日报「现场发现问题」非空即自动生成问题记录，带入归类、附图、提出人、所属项目/任务。
- 【修订 2026-09-22，Push 155 · 后端已落（M6-01 / M6-02）】提交且「现场发现问题」非空 → 生成一条 `state = unassigned` 的问题（带入归类 / 提出人 / 所属项目 / 关联任务 / 提出日期），以 `uq_issues_source_report` 唯一约束兜底幂等（冲突即已生成，不重复写事件与审计）；标题超 500 字截短（原文仍在日报行）；同步写 `created` 事件 + 审计 + outbox `issue.created`。

#### A3-10 问题状态（一期）
- 【修订 2026-09-22，Push 155 · 后端已落（M6-01 / M6-02）】四态落库（`unassigned` / `open` / `in_progress` / `done`）；**允许回退且留痕** —— 不设流转白名单、不做 `ISSUE_TRANSITION_INVALID`（差异登记），每次变更写一条 `issue_events`（`state_change`）；关闭 = `state = done` + `closed_at` / `closed_by`（成对 CHECK `ck_issues_closed_pairs`），回退自动清空。
- 【修订 2026-09-22，Push 155 · 后端已落（M6-01 / M6-02）】「状态变化触发提醒」随 M5 规则引擎；统计更新随 M7 仪表盘（数据面已就位）。

#### A3-11 问题归类（一期）
- 【修订 2026-09-22，Push 155 · 后端已落（M6-01 / M6-02）】十类为**契约固定枚举**、库侧 CHECK 同源；C9 字典可维护随后（差异登记）。

#### A3-12 自动分派（一期）
- 【修订 2026-09-22，Push 155 · 后端已落（M6-01 / M6-02）】一期口径：归类为机械部 / 采购部 / 规划部 / 项目部 → 提交时自动落 `owner_department`；原因类（物流 / 供应商 / 客户 / 客观 / 生产 / 其它）不自动分派（归「未分组」兜底）；「映射在 C9 配置 / 一类对多部门」与「提示项目部」随 M5 通知与二期字典（差异登记）。

#### A3-13 处理过程留痕（一期）
- 【修订 2026-09-22，Push 155 · 后端已落（M6-01 / M6-02）】处理过程子表 `issue_events` 落库（`created` / `state_change` / `solution` / `assignment` 四类事件 + 操作人 + 时间 + 备注）；解决方案 / 分派 / 时限变化均留痕；附件留痕随 M4 关联切片；响应时长与关闭率统计随 M7 仪表盘。

#### A3-14 超期升级（一期）
- 超过处理时限：T+1 提醒责任人，T+3 升级项目经理（规则 A03）；升级记录可查。

#### A3-15 关闭确认（一期）
- 提出人确认关闭；超时未确认自动关闭并单独标注（规则 A04）；关闭确认人与时间留痕。

#### A3-16 问题看板（一期）
- 【修订 2026-09-22，Push 155 · 后端已落（M6-01 / M6-02）】看板与问题追踪表**同源**：`GET /api/v1/projects/{id}/issues`（筛选 = 状态 / 归类 / 任务 / 来源日报 + 关键字 + 分页）即四列数据源（四态固定顺序、空列保留由前端渲染）；拖拽流转 = `PATCH …/issues/{issueId}`（允许回退、带 `version` 乐观锁）。

#### A3-17 问题统计（一期）
- 【修订 2026-09-22，Push 155 · 后端已落（M6-01 / M6-02）】统计所需数据面已就位（四态 + 归类 + 提出 / 关闭时间 + 责任部门 / 责任人），聚合与导出随 M7-02 / M7-03。

#### A3-18 周报/月报自动生成（二期）
- 自动抽取完成事项、问题、下步计划生成周报草稿，供项目经理修改后发布。

---

### A4 文件与变更管理

流程参考（文件生命周期，一期口径：变更申请即通过）：

    上传（未定档）→ 定档（锁版）→ 变更申请 → 变更生效（一期：提交即通过；二期：审批通过）→ 新版本
    任意状态 → 删除（进入回收站，可恢复）；归档后 → 已归档（冻结）

#### A4-01 项目文件库（一期）
- 项目维度集中存放全部文件，按类型/阶段/任务分组；支持按上传人、时间、类型筛选。
- 文件与项目、任务、日报、问题、变更的关联关系可查，支持双向跳转。

#### A4-02 文件类型字典（一期）
- 机械设计图、Word、PDF、Excel、评审单等类型可扩展，统一由 C9 数据字典维护。

#### A4-03 任意文件上传（一期）
- 初期支持任意格式；单文件大小与格式限制可配置。
- 上传进度展示，失败可重试；上传成功即写入文件库并建立关联。

#### A4-04 大文件直传（一期）
- 分片上传＋断点续传（200MB 级为 PoC 验收标准）；弱网中断后可续传。
- 内容哈希去重提示（相同文件重复上传给出提示，用户可确认继续）。

#### A4-05 文件定档（一期）
- 任务节点上传成果文件后执行定档，锁定为正式交付版本；定档操作受权限控制。
- 定档后不可直接覆盖或替换；修改必须走变更管理（A4-13）。

#### A4-06 版本管理（一期）
- 同一文件多版本管理；版本号、上传人、时间、备注可查；版本链可视化展示。
- 任意历史版本可预览与下载（受权限控制）。

#### A4-07 版本对比与回溯（一期）
- 历史版本可对比查看；一键回溯生成新版本，不删除历史版本；回溯留痕。

#### A4-08 文件状态（一期）
- 状态：未定档 / 已定档 / 已变更 / 已归档，自动流转。
- 【定档】一期口径：变更提交即进入「已变更」，无「待审批」停留状态（审批能力二期）。

#### A4-09 关联关系（一期）
- 文件可关联项目、任务、变更记录、问题、日报；一处关联多处可见。

#### A4-10 下载与权限（一期）
- 离线下载受权限控制并记日志；敏感文件可加水印（二期）。

#### A4-11 在线预览（一期）
- Office/PDF/图片在线预览；CAD 图纸见 D2；三维文件见 D3。
- 预览失败降级为下载并记录原因，不影响下载能力。

#### A4-12 文件回收站（一期）
- 删除进入回收站，可恢复；默认保留 30 天，可配置。
- 彻底删除仅管理员可执行，操作留痕。

#### A4-13 变更申请（一期 · 定档调整）
- 定档后的文件发生修改，由此发起变更；单独设置文件变更板块。
- 字段：变更时间、变更阶段、变更文件、变更内容描述、变更前/变更后、变更原因、变更申请人、填写者、进度、变更后文件版本、变更后文件、关联、审批状态。
- 提交校验：必填字段齐全；【定档】必须上传「变更后文件」，无变更后文件不允许提交。
- 【定档】一期提交即生效，不进入审批环节；申请记录立即生成。

#### A4-14 变更生效（一期 · 定档调整）
- 【定档】一期取消审批：申请提交即通过。提交成功即执行：生成变更记录、新版本入库（版本号递增）、回写任务「变更关联」（规则 R01）、通知相关人；「审批状态」由系统置为「通过」。
- 二期启用审批流后，本功能点保留「审批通过后生效」路径；驳回退回申请人并保留申请记录。
- 生效结果可追溯：变更前后内容、版本、原因、时间、申请人与生效时间完整记录。

#### A4-15 变更记录（一期）
- 完整记录变更全过程，历史永久保留；支持按项目、任务、阶段、原因检索与导出（受权限控制）。

#### A4-16 变更自动关联（一期）
- 新增变更记录自动回写任务「变更关联」（规则 R01，追加＋去重）。
- 【修订 2026-09-22，Push 146】回写落点 = `tasks.change_refs uuid[]`（写面追加 + 去重，不覆盖既有值）。
- 无匹配任务时记日志并提示申请人；匹配多条时全部关联。

#### A4-17 变更统计（一期）
- 变更次数、按阶段/原因分布、影响工期分析；供仪表盘（B4）与专项统计（B5）取数。

#### A4-18 变更通知（一期）
- 变更生效后通知申请人、任务负责人及相关群；二期启用审批后增加驳回通知。

#### A4-19 在线编辑（二期 · 定档口径）
- 【定档】工具＝ONLYOFFICE（自托管部署，数据不出内网）。
- 支持 Word、Excel、PPT 等格式在线编辑；入口在文件详情/预览页。
- 未定档文件：在线编辑保存生成新版本；已定档文件：编辑保存必须生成新版本，不覆盖定档版本，并按变更管理规则登记（A4-13/A4-14）。
- 编辑权限受 C3 控制；打开、编辑、保存行为全部写审计（C7）；同一文件并发编辑时给出锁定或冲突提示。

#### A4-20 成果文件必交校验（一期 · 定档）
- 触发点：任务提交「已完成/提前完成」时、阶段推进时、项目归档时。
- 规则：任务的「输出成果文件」非空（即流程节点模板要求了成果文件）时，必须已上传并挂接对应类型的文件，否则不允许提交，并返回缺件清单（列明所缺的成果文件类型）。
- 拦截入口：表格编辑、看板拖拽（B2-02）、任务详情三种提交入口执行同一校验；未通过时提供直接上传入口。
- 已上传未定档：允许提交，但立即提示定档并触发提醒（联动 R02 / A15）。
- 变更提交的对应校验在 A4-13（无变更后文件不允许提交）。
- 拦截记录与缺件情况写入审计日志（C7）；无成果文件要求的节点不做拦截。

---

### A5 干系人管理

#### A5-01 干系人台账（一期）
- 字段：姓名、电话/WhatsApp/微信号、邮箱、职务/责任板块、所属公司、填写者、干系人角色、关联项目、最近更新时间。
- 干系人清单归属销售部管理，开放销售端口录入第三方信息；前期由工作组完成全部项目干系人的基础录入。

#### A5-02 公司分类（一期）
- 分类：立镖机器人 Libiao Robot / 供应商 supplier / 总包单位 / 客户 customer；可补充具体公司名称。

#### A5-03 项目关联（一期）
- 干系人可关联多个项目（多对多）；按项目查看联系人清单，按干系人反查参与项目。

#### A5-04 销售录入端口（一期）
- 销售专属入口：仅可维护干系人数据，不能修改项目计划与其他业务数据。

#### A5-05 批量导入（一期）
- Excel 批量导入（前期基础录入）；导入校验（必填、格式、判重：姓名＋手机号）。
- 导入结果给出成功/失败清单，失败项可修正后重导。

#### A5-06 去重合并（二期）
- 同名/同手机号提示重复，支持合并两条记录（保留合并前信息可追溯）。

#### A5-07 隐私字段权限（一期）
- 联系方式（电话/微信/邮箱）字段级权限：无关角色不可见；导出、搜索、消息内容与页面同一策略，无权字段不返回。

#### A5-08 维护提醒（二期）
- 超过 6 个月未更新的记录，提醒填写者/销售（规则 A11）。

#### A5-09 导出（一期）
- 按项目/公司类型导出联系人清单；导出受权限控制并写审计。

---

### A6 我的工作台

#### A6-01 我的任务（一期）
- 我负责/我参与的任务，按今日待办、即将到期、已逾期分组展示；支持一键进入任务详情（【修订 2026-09-22，Push 136】任务负责人可多位：「我负责的」= 任务负责人名单内任一位是我）。

#### A6-02 待我审批（一期）
- 变更、延期等待办入口（C8）；【定档】一期变更申请即通过，不产生变更审批待办；本区域保留入口位，二期启用审批后展示。

#### A6-03 我负责的问题（一期）
- 我处理/我提出的问题及状态，支持快速更新处理进展与关闭确认。

#### A6-04 最新提醒（一期）
- 站内信与未读消息聚合（C5）；点击直达对应业务对象。

#### A6-05 我的待办提醒（一期）
- 自定义待办清单（规则 A14）：标题、内容、关联项目/任务、提醒时间、重复规则。

#### A6-06 快捷入口（一期）
- 填日报、从模板新建任务、上传文件、发起变更。

#### A6-07 稍后提醒/已处理（一期）
- 消息支持「稍后提醒」与「标记已处理」；处理结果在消息中心同步。

#### A6-08 关注动态（一期）
- 我关注的项目/任务状态变更动态流；支持一键取消关注。

---

### A7 日报中心

#### A7-01 自动汇总（一期）
- 按项目/日期/人员自动汇总，替代人工「填写后添加到日报」；汇总口径与日报表单一一致。

#### A7-02 多维检索（一期）
- 按项目、日期区间、人员、关键词检索；支持按是否有问题、是否补填等状态筛选。

#### A7-03 日报推送（一期）
- 每日 19:00 汇总推送项目群（规则 A02）；推送失败降级站内信。

#### A7-04 导出与存档（一期）
- 按项目导出日报合集（Excel/PDF）；随项目归档留存。

#### A7-05 漏填提醒（一期）
- 应填未填自动提醒（规则 A01）；提醒记录可查。

#### A7-06 日报详情（一期）
- 查看附图、关联问题、关联任务与提交状态（草稿/已提交/补填）；支持从详情跳转问题与任务。

---

## 二、B 数据与展示模块

### B1 人员任务分配表

#### B1-01 人员泳道看板（一期）
- 每列/每行一个成员，卡片为任务，显示状态色与截止日期；支持按项目过滤。

#### B1-02 快速指派（一期）
- 卡片拖拽换人；多人协同任务有标识；换人后原/新负责人均收到通知（A05）。

#### B1-03 跨项目视图（一期）
- 成员参与多项目时，跨项目汇总其全部任务；支持按项目折叠查看。

#### B1-04 人天投入统计（一期）
- 结合日报「今日施工人数」按日累计统计人天；支持按项目、人员、时间区间统计。

#### B1-05 负荷热力（二期）
- 按在制任务数/人天展示负荷热力（规则 A16）；提示不阻断操作。

---

### B2 任务进展看板

#### B2-01 状态分列（一期）
- 五列：待开始 / 进行中 / 已延期 / 已完成 / 提前完成（与任务状态 5 态一致；「已延期」＝未完成且超过预计完成日期；【修订 2026-09-19，Push 67】「逾期」标注在「是否按时交付」列：逾期未交付 / 逾期已交付）。

#### B2-02 拖拽变更状态（一期 · 定档调整）
- 拖动卡片即改状态，系统自动补齐实际日期字段。
- 【定档】拖入「已完成/提前完成」时执行成果文件必交校验（A4-20）：未通过则卡片回弹并提示缺件清单，不允许落入完成列。
- 乐观更新＋冲突提示（409）；拖拽产生的联动与 A1/A2 一致（进度重算、喜报、提醒）。

#### B2-03 卡片信息（一期）
- 项目、任务描述、负责人、截止日期、紧急重要度、四格进度、附件数。

#### B2-04 多维过滤（一期）
- 按项目、负责人、紧急重要度、时间范围过滤；支持保存为个人视图。

#### B2-05 颜色与标识（一期）
- 逾期红色、临近到期橙色、重点任务星标；颜色规则可配置。

#### B2-06 统计条（一期）
- 每列顶部显示数量与占比；支持点击统计条快速过滤。

---

### B3 甘特图（二期）

#### B3-01 计划条＋实际条（二期）
- 双条对比：计划条（开始日期 → 预计完成日期）与实际完成日期标注。

#### B3-02 里程碑（二期）
- 阶段里程碑菱形标注，阶段完成自动点亮。
- 【修订 2026-09-22，Push 151】甘特图里的单日任务菱形**按状态取色**（业务口径「改成单日任务」「没有完成是灰色 延期是红色」「任务栏还是绿色的要」）：**已完成 / 提前完成 = 绿色、未完成（待开始 / 进行中）= 灰色、已延期 = 红色**；图例项由「里程碑（单日任务）」改名 **「单日任务」**，色块保持绿菱形 + 悬停补一句三态说明。

#### B3-03 任务依赖（二期）
- 前置/后置任务（FS/SS/FF/SF），自动画依赖箭头。

#### B3-04 关键路径（二期）
- 自动计算关键路径并标红。

#### B3-05 延期高亮（二期）
- 已延期变红、临近到期变橙。
- 【修订 2026-09-22，Push 145】甘特图原型落地时的实现口径（业务口径「维持手绘尽量还原官方的功能」+「就是在末端 或者说两种颜色交接的地方 还有橙色的边框」+「之前的颜色可以的 不用改啊 我只是让你改细节」）：**已延期 = 条头一段红内嵌色条**（3px、`red-500`，独立子层并抬到进度层之上 —— 直接挂 `inset` 阴影会被上面的青色进度层盖住），**条本身不画描边**（原 `ring-amber-600/40` 只压在橙色段上、看着像给橙色块单独镶边；去掉后青色进度与橙色剩余工期之间是硬直缝，与参考样例一致）；「临近到期变橙」未做（没有「临近」阈值的字段 / 天数口径，留二期）。

#### B3-06 缩放与筛选（二期）
- 日/周/月切换；按项目、负责人筛选。
- 【修订 2026-09-22，Push 145】**本条的粒度切换与筛选已提前在项目详情页「甘特图」视图落地为原型**（业务口径「先做这两个」）：**粒度 = 日 / 周 / 月**（官方 `timelineHeader.scales`）—— 只换表头分段与网格密度（周粒度下排逐周「M/D」、竖线改周线并去掉周末底纹；月粒度上排 = 年份、下排 = 「5月」，列宽按当月真实天数展开），列宽 = 一列跨几天 × 每天宽度，几何基准仍是「每天多少 px」，所以任务条 / 里程碑 / 实际完成刻度 / 今天线一律按天精确落点；**筛选 = 按负责人（含「待分配」）/ 只看已延期 / 只看未排期**，带命中计数与「清空筛选」，筛的是任务集合（时间轴范围、阶段汇总、左表行与行号跟着走，空阶段不再占行），筛空 / 筛得都没排期各有空态并带「清空筛选」。**筛选控件**（2026-09-22 同日补，业务口径「下拉框组件设计 保持一致 搜索下拉框」）—— 负责人筛选不用原生下拉，与人员下拉 / 任务表行内成员单元格同一套搜索下拉（搜索框「搜索成员」+ 头像 + 姓名 + 拼音 + 角色 + 选中绿勾 + 页脚；↑↓ / Enter 选、Esc 关）；工具条控件统一「液态玻璃」材质（业务口径「改成同款液体玻璃」，与首页分类筛选侧栏 / 任务表行内编辑单元格同口径）；**「回到今天」在筛选后仍可用**（业务口径「为什么筛选后不能选择回到今天」）—— 今天不在筛选后的时间轴范围内时，先把今天并进范围（画出今天虚线）再居中，只有画不出时间轴时才置灰；**按项目筛选**仍不在本期（视图本身已经绑定单个项目）；口径见 `前端功能需求.md` §6.14。

#### B3-07 导出（二期）
- 导出图片/PDF 用于汇报。

#### B3-08 拖动改期（二期）
- 拖动条形改期、拖边缘改工期，自动校验依赖冲突。
- 【修订 2026-09-22，Push 145】业务口径「维持手绘尽量还原官方的功能」：**本条的前两件事已提前在项目详情页「甘特图」视图落地为原型**（不引第三方图表库、手绘实现，对应官方 `taskBar.moveable` / `taskBar.resizable`）—— 拖条整体平移 = 改 `startDate` + `dueDate`（含首尾 `days`，工期不变）、拖条两端 = 只动被拖的那一端（单日任务拖左端仍是单日）；拖动中是预览态（条上方浮「开始 → 预计完成 · 共 N 天」），松手才写回，**Esc / 指针取消 = 不写回**；日期域夹在 `TASK_DATE_YEAR`（2026）内（数据里的「M月D日」不带年份），顶到年初 / 年末按工期回夹。写回走既有任务字段（`startDate` / `dueDate` / `days`，与表格行内编辑同一套换算），落 `ProjectDetail` 内存态覆盖表（原型，刷新复位；正式版走既有 `PATCH .../tasks/{taskId}`，无新接口）。**本条的第三件事「自动校验依赖冲突」仍缺** —— 依赖字段（前置 / 后置与类型 FS/SS/FF/SF）随 A1-11 二期启用，届时再做冲突校验；线索总览见 B3-09 修订与 `前端功能需求.md` §6.14。

#### B3-09 一期预留说明（一期 · 预留）
- 一期不实现视图，仅保留菜单入口位；依赖字段、里程碑字段随二期启用时一并补充（见 A1-11）。
- 【修订 2026-09-22，Push 148】甘特图口径经业务逐条确认：**未排期 = 开始日期或预计完成日期任一未设定**（两个都填才算已排期）；**取数按现状量级**（单项目几十～数百条，一次取全即可，**不针对「单项目 1 万行」做专门的全量 / 视窗设计**）；**里程碑仍按「单日任务」派生**（「是否里程碑」字段随 A1-11 二期）；**依赖三条（B3-03 / B3-04 / B3-08 冲突校验）暂不做**。
- 【修订 2026-09-22，Push 145】业务口径「我想做甘特图 / 在标签导航栏加一个甘特图页面」：**甘特图视图提前在项目详情页落地为原型视图**（顶部标签栏第二个标签，数据源 = 任务列表）—— 已做 **B3-01 的计划条 + 实际完成日期刻度**（未画第二条）、**B3-02 的里程碑菱形**（一期无「是否里程碑」字段，暂按「单日任务 = 开始与预计完成同一天」派生，字段口径见 `前端功能需求.md` §3.8 A28）、**B3-05 的延期高亮**（已延期 = 条头一段红内嵌色条、条本身不画描边）、**B3-06 的粒度切换与筛选**（日 / 周 / 月 + 按负责人 / 只看已延期 / 只看未排期；另有三档缩放）、**B3-09 的入口位**（由「仅保留菜单入口位」改为标签栏正式入口）；**B3-03 依赖箭头、B3-04 关键路径、B3-07 导出仍不在本期**；**B3-08 的拖动条形改期与拖边缘改工期已提前落地为原型**（见 B3-08 修订；差「依赖冲突自动校验」—— 依赖字段 A1-11 在二期）。**同轮补齐**（业务口径「维持手绘尽量还原官方的功能」）：条选中（官方 `taskBar.selectable`）、悬停遮罩（`hoverBarStyle`）、进度圆点拖拽改进度（四档吸附，与四格进度条同口径）、条内「任务名 + 进度」标签（`labelText`）、点左表任务行定位到条（`gantt-locate-taskbar`）、拖左表右边线 = 移动分隔线（左表列宽固定、被时间轴盖住超出的列；把手与 2px 高亮线画在裁切层之外、拖动另有 window 级 `pointermove` / `pointerup` 兜底，官方 `frame.verticalSplitLineMoveable` + `verticalSplitLineHighlight`）、左表「开始日期 / 预计完成日期」两列（官方样例左表 start / end：拖动条 / 拖两端 / 拖圆点时跟着预览值实时更新）。数据源与任务列表同一份（`GET /api/v1/projects/{id}/tasks`），无新增接口 / 字段。

---

### B4 仪表盘

#### B4-01 项目概览卡（一期）
- 项目总数、进行中、已延期、本月完成、按时交付率；点击下钻明细。

#### B4-02 阶段分布（一期）
- 各项目实施阶段分布（漏斗/柱状）；与九阶段字典一致。

#### B4-03 任务健康度（一期）
- 任务状态分布、逾期任务 TOP、7 日到期清单。

#### B4-04 进度概览榜（一期 · 字段口径修订）
- 各项目进度以四格展示，悬停显示档位文字与百分比。

#### B4-05 问题统计（一期）
- 未解决/处理中问题数、按归类分布、平均解决时长。

#### B4-06 变更统计（一期）
- 变更次数趋势、按阶段/原因分布。

#### B4-07 日报动态（一期）
- 今日已填/未填项目、最近日报摘要。

#### B4-08 阶段节点到期（一期 · 字段口径修订）
- 未来 30 天到期的阶段节点一览（按阶段性里程与预计完成日期取数）。

#### B4-09 组件下钻（一期）
- 所有组件可点击下钻到明细列表，并保持权限过滤。

#### B4-10 人员负荷（二期）
- 成员在制任务数/人天热力。

#### B4-11 ERP 对比（二期）
- 系统计划 vs ERP 实际对比图。

#### B4-12 数据大屏（二期 · 预留）
- 会议室/项目部大屏轮播。

---

### B5 专项统计界面

#### B5-01 项目信息汇总（一期）
- 全部在管项目：名称、项目经理、实施阶段＋阶段完成度、四格进度（【修订 2026-09-22，Push 136】项目经理可多位，多位按「、」连接展示）。

#### B5-02 相关群聊（一期）
- 项目群名称、群成员、跳转链接。

#### B5-03 干系人概览（一期）
- 客户/总包/供应商关键联系人（受字段级权限控制）。

#### B5-04 风险提示（一期）
- 延期、缺件、问题积压、变更频繁自动标红；缺件口径与 A4-20 校验一致。

#### B5-05 一键导出（一期）
- 导出统计报表（Excel/PDF）；导出受权限控制并写审计。

---

### B6 ERP 关联与数据对比（二期）

#### B6-01 适配层与快照表（二期）
- 读取 ERP 采购/发货/验收等节点数据，存快照；一期仅保留设计位。

#### B6-02 只读同步（二期）
- 仅读取 ERP，不改写 ERP 数据。

#### B6-03 对比图数据供给（二期）
- 供 B6/B4 渲染系统计划与 ERP 实际对比图。

---

## 三、C 支撑模块

### C1 表单收集引擎

#### C1-01 通用收集流程（一期）
- 「收集表单 → 表单收集结果」标准链路；各业务场景复用同一引擎。

#### C1-02 场景接入（一期）
- 日报填报、变更申请、干系人录入、待办创建等场景统一接入。

#### C1-03 字典联动（一期）
- 表单字段枚举统一取自 C9 数据字典（问题归类、成果文件类型、地区、项目类型等）。

#### C1-04 结果回流（一期）
- 提交结果自动写入对应业务模块并触发下游规则（问题生成、变更生效、通知等）。

#### C1-05 校验与防重（一期）
- 必填/格式校验；同人同场景防重复提交；提交失败保留已填内容。

#### C1-06 表单模板（一期）
- 常用表单内容模板化（如日报模板），一键填充。

---

### C2 自动化与通知中心

流程参考（自动化引擎四段式）：

    触发器 → 条件 → 动作 → 投递（站内信＋企业微信）

#### C2-01 规则管理（一期）
- 可视化配置触发方式、条件、动作、消息模板；支持启用/停用/试运行。

#### C2-02 内置规则 R01~R07（一期）
- 开箱可用、可启停、可配置渠道；消息文案逐字保留（R01~R07）。
- 【修订 2026-09-20，Push 71】文案逐字基准入仓：`docs/rules/R01-R07-内置规则文案.md`（M0-04）；金标回放以该文件为逐字基准，规则侧待确认项见该文件文末清单。

#### C2-03 扩展规则（一期/二期）
- A01~A18：一期实现 A01/A02/A03/A14，其余二期。

#### C2-04 消息模板（一期）
- 模板支持变量替换、多渠道差异化文案；变更留痕。

#### C2-05 群聊绑定（一期）
- 维护项目与群绑定（项目主数据维护）；部门群按组织配置。

#### C2-06 自定义待办提醒（一期）
- 任意节点挂待办：标题、内容、关联项目/任务、提醒时间、提醒对象、重复规则；非标准事项默认走此通道（A1-16）。

#### C2-07 发送记录（一期）
- 每条消息发送对象、渠道、时间、结果可查；支持管理员重发。

#### C2-08 投递保障（一期）
- 幂等去重、失败重试（默认 3 次）、错过补发、死信告警管理员。

#### C2-09 合并与免打扰（一期）
- 同期消息合并推送、免打扰时段静默（次日补发）、节假日静默、每人每日上限。

#### C2-10 群播报（一期）
- 喜报（R06）、阶段达成（A08）、日报汇总（A02）群内播报；按队列限速发送。

#### C2-11 渠道矩阵（一期）
- 企业微信应用消息＋群机器人＋站内信；邮件基础可用；短信/钉钉/飞书/Webhook 预留接口。

#### C2-12 规则可解释（一期）
- 每条规则保存命中记录、条件求值结果、动作结果与发送渠道，管理员可查询。

---

### C3 权限管理

#### C3-01 功能权限（一期）
- 模块与操作级：查看/编辑/删除/导出/下载/审批。

#### C3-02 数据范围（一期）
- 全部 / 本部门 / 我参与的 / 我负责的 / 自定义项目集。

#### C3-03 记录级权限（一期）
- 项目成员制：非项目成员不可见，统一 404 语义，防 IDOR 探测。

#### C3-04 字段级权限（一期）
- 可见 / 只读 / 隐藏脱敏（干系人联系方式、商务字段等）。

#### C3-05 操作级控制（一期）
- 导出、下载、预览、外链生成单独授权并审计。

#### C3-06 临时授权（一期）
- 临时成员/项目限时授权，到期自动收回。

#### C3-07 外链分享（二期）
- 限时/限权/可水印的只读链接或收集表单。

#### C3-08 统一策略出口（一期）
- 导出、搜索、消息内容、统计接口与页面一致；无权字段一律不返回。

#### C3-09 权限管理界面（一期）
- 角色模板＋授权界面＋权限自检报告。

#### C3-10 权限审计（一期）
- 授权、变更、越权尝试留痕（对接 C7）；越权尝试单独告警。

---

### C4 归档与导出

#### C4-01 变更留存（一期）
- 归档时输出完整变更历史，解决「无法及时变更留存」。

#### C4-02 项目归档（一期）
- 验收完成后一键归档，冻结数据快照（任务、文件、变更、日报、问题）；归档前执行成果文件齐全性检查（A4-20 口径）。

#### C4-03 基础归档（一期）
- 生成归档目录＋清单（文件按类型/阶段组织）；归档结果可检索。

#### C4-04 归档包导出（二期）
- 归档目录＋清单＋文件压缩导出（可选 PDF 目录册）。

#### C4-05 常规导出（一期）
- 任务表、日报、问题、干系人、变更记录按筛选条件导出 Excel。

#### C4-06 导出审计（一期）
- 导出行为记录日志；敏感数据导出可加水印或审批（二期）。

#### C4-07 归档检索（一期）
- 已归档项目按项目/客户/年份检索并重新下载。

---

### C5 消息中心（站内信）

#### C5-01 收件箱（一期）
- 站内消息列表，未读/已读/已处理状态。

#### C5-02 分类与筛选（一期）
- 按类型（提醒/审批/播报/系统）筛选；支持按时间区间、已读状态筛选。

#### C5-03 全量留档（一期）
- 所有推送消息留档可回溯（含企微成功发送的）。

#### C5-04 兜底通道（一期）
- 企微发送失败自动降级为站内信，保证不丢。

#### C5-05 稍后提醒（一期）
- 消息可「稍后提醒」设置新提醒时间；设置与触发记录可查。

---

### C6 全局搜索

#### C6-01 覆盖范围（一期）
- 项目、任务、文件（名称/类型/关联）、日报、问题、干系人、变更记录。

#### C6-02 权限过滤（一期）
- 按权限过滤，无权限记录与字段不返回（与 C3 同策略）。

#### C6-03 检索能力（一期）
- 中文分词检索（含编号/短词）、按对象类型过滤、结果高亮。

#### C6-04 数据一致性（一期）
- 业务数据变更时同事务维护搜索投影，避免搜不到或过期结果。

---

### C7 审计与操作日志

#### C7-01 关键操作留痕（一期）
- 新增/修改/删除、导出、下载、预览、外链生成、授权变更、规则触发。

#### C7-02 字段级修改历史（一期）
- 谁、何时、从什么改成什么；任务详情可直接查看。

#### C7-03 越权尝试记录（一期）
- 越权访问/导出尝试单独记录并告警管理员。

#### C7-04 管理员查询页（一期）
- 多条件筛选、导出；高风险导出需重认证。

#### C7-05 防篡改（一期）
- 日志追加写、不可修改删除；保留期不少于 6 个月。

---

### C8 审批中心（一期不启用 · 定档调整）

#### C8-01 变更审批（二期；一期不启用）
- 【定档】一期变更管理不做审批，申请即通过（A4-13 / A4-14），不产生变更审批待办；本功能为二期能力，一期仅保留数据结构与入口位。
- 二期启用后：支持多级审批与驳回，驳回需填写原因。

#### C8-02 延期申请审批（二期）
- 负责人发起（原因＋新预计完成日期）→ 项目经理审批 → 通过后更新日期并留痕，未发送提醒按新日期重算。

#### C8-03 待审批列表（二期；一期保留入口位）
- 与「我的工作台」打通；支持批量查看与处理。

#### C8-04 流程配置（二期）
- 审批人（项目经理/部门负责人）可配置；可设多级。

#### C8-05 审批留痕（二期）
- 审批意见、时间、审批人全留痕；驳回原因必填。

---

### C9 字典与模板

#### C9-01 数据字典（一期）
- 统一维护枚举：阶段、问题归类、成果文件类型、紧急重要度、公司类型、文件状态、地区、项目类型等。
- 【定档】新增「地区」「项目类型」字典，供项目主数据（A1-12）与首页分类侧边栏（A1-18）使用。
- 【Push 172 前端口径】项目类型条目携带主题色元数据（`metadata.accent` / `metadata.accentText`，新增时从**预置色板**选）；卡片 / 类型徽标 / 色点随条目颜色渲染（A3）。

#### C9-02 字典变更管理（一期）
- 变更留痕；历史数据保留原值，新增数据取新值；停用值不影响存量数据展示。
- 【修订 2026-09-23 · Push 168】**地区（region）是公共标签，任何登录用户都可以新增**（项目现场遇到字典里没有的地区，随手加一个，全站立刻可选、可筛）；新增同样写审计留痕、记录实际提交人。**项目类型等其余字典仍只能由管理员维护**；所有字典条目的改名 / 排序 / 删除仍归管理员（治理权不放开）—— 管理员可删除不合规的地区，删除不影响存量项目按原值展示。
- 【修订 2026-09-23 · Push 172】~~**删除字典条目 = 停用**（不物理删）~~（**已被 Push 173 修订**，见下条）：前端在地区 / 项目类型下拉的每行尾部提供隐式删除按钮（仅 `dict.manage` 可见），当时落 `PATCH /api/v1/dicts/{type}/items/{code}` 的 `enabled: false`；停用不影响存量数据展示，只是不再进下拉候选。项目类型新增同样仅管理员，并随条目落颜色模板（见 C9-01）。
- 【修订 2026-09-23 · Push 173】**删除字典条目 = 物理删行**（C9-02 口径修订，业务定调「删除了就没有记忆了」）：前端下拉每行尾部的隐式删除按钮调 `DELETE /api/v1/dicts/{type}/items/{code}`（仅 `dict.manage`），服务端从 `dict_items` 删行、删除前快照写审计（`action=delete`，字段级 `code/name/sort/enabled/metadata → null`，C7-02）。**删除无记忆**：同码可重新新增，按**全新条目**处理（本次所选颜色、排到末尾），界面不出现「已停用 / 恢复」字样。存量数据安全：`projects.region` / `projects.project_type` 是冗余名（无外键引用 `dict_items`），存量项目仍按原码 / 原名渲染。`enabled` 字段与 `includeDisabled` 参数降级为**兼容保留**（二期「临时下架」用），一期产品不再产生停用项 —— 迁移见 `database/migrations/0030_dict_item_hard_delete.sql`。
- 【修订 2026-09-23 · Push 174】**删除加「引用守卫」：有项目在用的条目不给删**（业务定调「当存在项目的类型或者地区在卡片里面的时候 不允许在这里删除」）—— 删除前先数引用：该码被**未删除项目**（`projects.region` / `projects.project_type`）用了几个，> 0 即 **409 `DICT_ITEM_IN_USE`**，不删行、不写审计；下发条目带 `usageCount` 供前端把删除位置灰（悬停说明「正被 N 个项目使用，不能删除」），缓存陈旧时以服务端裁决为准。引用（把项目改到别的地区 / 类型，或该项目被删）解除后即可正常删除。

#### C9-03 消息模板管理（一期）
- 模板变量、多渠道文案、版本留痕；发送文案以模板为准。

#### C9-04 表单模板（一期）
- 日报常用模板、变更申请模板等。

#### C9-05 自定义字段（二期）
- 任务/项目/日报/问题/干系人可扩展自定义字段。

#### C9-06 流程节点模板库（一期 · 定档）
- 【定档】项目环节的流程节点采用固定模板：模板按「项目类型 × 九阶段」组织，一个项目类型对应一套节点清单；新建项目按项目类型套用（A1-16）。
- 每个模板节点的字段：节点序号、阶段性里程、节点名称（任务描述，中英文）、要求输出成果文件（多选）、默认工期（天）、默认紧急重要度。
- 模板节点可启用/停用；停用只影响新建任务，不影响已生成任务。
- 模板即项目模板：按模板一键生成项目任务清单（计划编制，A2-05）。

#### C9-07 模板维护与锁定（一期 · 定档）
- 维护权限：仅系统管理员可在模板库新增、修改、停用节点；项目环节的流程节点不可随意编辑。
- 变更留痕：模板每次变更记录版本、修改人、时间与变更内容；删除节点改为停用。
- 任务侧锁定：任务描述、阶段性里程、输出成果文件生成后不可编辑（A1-17）；纠错由管理员修正模板，例外调整必须填写原因并留痕。【落地口径 2026-09-22 · Push 153】锁定三项（描述 / 英文描述 / 成果文件）无常规编辑通道；例外调整 `PATCH /api/v1/projects/{id}/tasks/{taskId}/locked-fields` 仅系统管理员、`reason` 必填并留痕；「阶段性里程」一期无列不开放。
- 影响范围提示：模板修改默认不影响已生成任务；需要同步时由管理员显式执行「应用到进行中项目」，逐条确认并留痕。
- 模板使用约束：任务只能从模板选择生成；非标准事项走自定义待办或（管理员）非标准任务。

#### C9-08 初始模板节点清单（一期 · 定档 · 初始值）
以下为定档初始模板（取自现网项目总览表的标准节点），节点名称即任务描述模板取值；管理员可在模板库中维护（新增/停用），维护须留痕。

| 序号 | 阶段性里程 | 节点名称（任务描述模板） | 要求输出成果文件 |
| --- | --- | --- | --- |
| 1 | 售前规划 | 布局定档Layout scheduling | cad图纸 |
| 2 | 售前规划 | 技术协议定档Technical agreement finalization | 技术协议 |
| 3 | 售前规划 | 合同签署Contract signing | 合同 |
| 4 | 售前规划 | 项目启动Project Startup | 评审单 |
| 5 | 设计开发 | 规划设计planning design | 设备清单 |
| 6 | 设计开发 | 机械设计Mechanical design | — |
| 7 | 设计开发 | 硬件研发Hardware R&D | — |
| 8 | 设计开发 | 软件开发Software | — |
| 9 | 设计开发 | 物料清单完整版 | 物料总清单 |
| 10 | 加工采购 | 采购Procurement | — |
| 11 | 加工采购 | 订单录入Order entry | — |
| 12 | 加工采购 | 到货入库Goods arrival and warehousing | — |
| 13 | 组装发货 | 生产Production | — |
| 14 | 组装发货 | 质检Quality inspection | — |
| 15 | 组装发货 | 包装Packing | 发货装箱单 |
| 16 | 组装发货 | 送仓Deliver to warehouse | — |
| 17 | 组装发货 | 运输Transportation | — |
| 18 | 组装发货 | 清关Customs clearance | — |
| 19 | 硬件实施 | 人员进场、场地检查、施工对接Personnel entry, site inspection, construction docking | 到货单 |
| 20 | 硬件实施 | 施工安全培训Construction Safety Training | — |
| 21 | 硬件实施 | 物料转运、清点分类Material transfer, inventory and classification | — |
| 22 | 硬件实施 | 货架组装Shelf Assembly | — |
| 23 | 硬件实施 | 货架检查Shelf inspection | — |
| 24 | 硬件实施 | 挂件安装Pendant Installation | — |
| 25 | 硬件实施 | 巷道导轨安装，铜丝镶嵌Tunnel rail installation, copper wire inlay | — |
| 26 | 硬件实施 | 飞箱机器安装Air Robot installed | — |
| 27 | 硬件实施 | 强弱电布线、接线，服务器机柜安装及理线Power and weak current wiring and connections, Server cabinet installation and cable management | — |
| 28 | 硬件实施 | 工作站安装及定位弹线Workstation Installation and Positioning Chalk Line | — |
| 29 | 硬件实施 | 飞箱电控箱及AP安装AirRobot electric control cabinet and AP installation | — |
| 30 | 硬件实施 | 货架条码黏贴Shelf barcode labeling | — |
| 31 | 硬件实施 | 接驳位弹线及魔毯黏贴，充电桩组装Docking position marking and magic carpet sticking, charging pile assembly | — |
| 32 | 硬件实施 | 导航柱弹线及安装Guide post marking and installation | — |
| 33 | 硬件实施 | 第一批空箱上架The first batch of empty boxes is on the shelves | — |
| 34 | 硬件实施 | 安全围栏安装及调试Safety Fence Installation and Commissioning | — |
| 35 | 硬件实施 | 第二批空箱上架The second batch of empty boxes is on the shelves | — |
| 36 | 硬件实施 | 第三批空箱上架The third batch of empty boxes is on the shelves | — |
| 37 | 软件部署 | 通电测试Power-on Test | — |
| 38 | 软件部署 | RCS部署及联动随机跑RCS Deployment and Random-linked Operation | — |
| 39 | 软件部署 | 工作站软件部署Workstation Software Deployment | — |
| 40 | 软件部署 | WES软件部署及与WMS联调；设备运行测试WES Software Deployment and Integration with WMS; Equipment operation test | — |
| 41 | 试运行 | 小批量实物测试Small batch of physical test | 安装完成证明 |
| 42 | 试运行 | 客户培训、上线Customer training, go alive | — |
| 43 | 生产阶段 | 产能爬坡Capacity climbing | — |
| 44 | 验收 | 验收交付Acceptance | 验收单 |

说明：以上节点与成果文件要求以现网表格为准整理；节点名称在实际展示中中英文连写，命名与拆分以管理员在模板库中的定稿为准。

---

## 四、D 平台与集成模块

### D1 用户与组织

#### D1-01 企微免登（一期）
- 企业微信 OAuth 网页授权免登，用户在企微内点开即登录；登录态与企微身份绑定。

#### D1-02 通讯录同步（一期）
- 定时拉取企微通讯录（部门、人员、离职状态）；维护 wecom_userid 与本地账号映射。

#### D1-03 离职即失效（一期）
- 离职/停用在同事务递增 auth_version 并撤销会话，权限即时失效。

#### D1-04 本地账号兜底（一期）
- 企微未覆盖人员、管理员应急使用；本地账号同样受 C3 权限与 C7 审计约束。

#### D1-05 管理员安全（一期）
- TOTP MFA＋高风险操作重认证（用户管理、权限变更、审计导出）。

#### D1-06 组织维护（一期）
- 部门树、岗位、兼职/多部门支持；部门调整同步影响数据范围（C3）与部门维度筛选（A2-03）。

#### D1-07 账号生命周期（一期）
- 入职、调岗、离职流程化处理；调岗触发数据范围重算。

#### D1-08 身份源预留（预留）
- AD / LDAP、OIDC 适配位，一期不实现。

---

### D2 在线预览服务

#### D2-01 Office/PDF 预览（一期）
- Word、Excel、PDF 在线预览（自建转换管道，图片型兜底）。

#### D2-02 图片预览（一期）
- 现场照片、影像在线预览与批量下载。

#### D2-03 CAD 图纸预览（一期 · PoC）
- dwg/dxf 本地转换查看（图层、线宽、中文字体、打印样式需 PoC 达标）；不达标时退化为下载。

#### D2-04 预览鉴权（一期）
- 短时签名地址＋审计；禁止对象存储匿名读取。

#### D2-05 失败降级（一期）
- 转换失败提示「暂不支持在线预览，请下载查看」并记录原因；不影响下载。

#### D2-06 转换缓存（一期）
- 同一文件只转换一次（内容哈希＋参数版本）；定档文件预生成预览。

#### D2-07 预览审计（一期）
- 预览计入「查看/下载」审计，与 C7 打通。

#### D2-08 手机端预览（二期）
- 移动端常见格式预览（现场核对图纸）。

#### D2-09 在线编辑服务（二期 · 定档口径）
- 【定档】采用 ONLYOFFICE 自托管文档服务（数据不出内网），与文件库（A4）打通。
- 服务能力：Word、Excel、PPT 等格式的在线打开与编辑、保存回调、协同编辑。
- 与文件版本联动：未定档文件编辑保存生成新版本；已定档文件编辑保存必须生成新版本（不覆盖定档版本），并按变更管理规则登记（A4-13 / A4-14）。
- 鉴权与审计：编辑会话鉴权与文件权限（C3）一致；打开、编辑、保存全部写审计（C7）。
- 许可与部署：按 ONLYOFFICE 许可要求部署与配置，纳入运维监控。

---

### D3 三维与图纸查看

#### D3-01 三维文件管理（一期）
- 三维文件上传、下载、关联管理（与二维图纸一致）。

#### D3-02 第三方云排除（一期）
- 数据不出内网：Autodesk 等云方案已排除。

#### D3-03 在线查看方案（二期）
- 二期评估：设计端导出轻量预览件（glTF/PDF/图片）＋自研查看器；私有化商业 SDK；仅下载（保底）。

#### D3-04 二维 CAD 优先（一期）
- 优先用本地转换 PoC 验证渲染质量；达标上线，不达标退化为下载。

#### D3-05 启动条件（待确认）
- 拿到格式清单、样本文件、交互要求（旋转/剖切/测量）与成本预算后启动方案选型。

---

### D4 ERP 集成（二期，一期留位）

#### D4-01 适配层与快照表（一期 · 预留）
- 一期仅保留设计位，不实现。

#### D4-02 只读同步（二期）
- 采购/发货/验收等节点数据只读同步；不改写 ERP。

#### D4-03 对比图数据供给（二期）
- 供 B6/B4 渲染系统计划与 ERP 实际对比图。

---

### D5 工作日历

#### D5-01 日历维护（一期）
- 管理员维护节假日、调休安排。

#### D5-02 顺延规则（一期）
- 提醒日期落在节假日时按规则顺延（可配置）。

#### D5-03 T-1/T+1 计算（一期）
- 为规则引擎提供日期计算依据（如「前 1 天 08:00」）。

#### D5-04 日历视图（二期）
- 任务、待办统一日历。
