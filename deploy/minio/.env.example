# 本地沙箱示例；真实值不落仓库（CONTRIBUTING §12）
# MinIO 要求口令 >= 8 位；这里只放本地占位值，生产另行管理
MINIO_ROOT_USER=libiaolink
MINIO_ROOT_PASSWORD=libiaolink-local-only
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
# 允许直传的浏览器来源（逗号分隔）；与 server 的 S3_CORS_ORIGINS 保持同一组
MINIO_CORS_ALLOW_ORIGIN=http://localhost:5173
