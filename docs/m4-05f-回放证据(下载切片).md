# M4-05f 回放证据（S7·file 下载切片）

> 卡片：M4-05「在线预览」的**下载切片**（主责 lan，评审 wmj）｜口径来源：系统功能书 A4-06（任意历史版本可预览与下载）/ A4-10（离线下载受权限控制并记日志 · 一期）｜技术设计v0.3 §3.5（预览 / 下载 / 定档 / 变更全部写审计；下载受 `file.download` 约束）｜ADR-006（对象存储禁匿名读取：短时签名投递）｜契约 `FileDownloadUrlResponse`。
> 落点说明：`docs/` 属 px 线；本文件由 lan 随 M4-05 下载切片卡片代记（回放脚本与断言同 PR，请 px 复核）。

| 项 | 值 |
|---|---|
| 回放时间 | 2026-09-24 09:14:30 +08:00 |
| 目标 | http://127.0.0.1:3011 |
| 数据库 | postgresql://libiaolink_migrator@127.0.0.1:55432/libiaolink |
| 对象存储 | libiaolink（本地 MinIO 沙箱：仅沙箱、不代表生产选型） |
| 签名窗口 | 下载 600s（`S3_DOWNLOAD_URL_TTL_SECONDS`）／预览 900s（`PREVIEW_URL_TTL_SECONDS`）—— 两键独立配置 |
| 代码版本 | 42efba6（回放时工作区含本卡未提交改动） |
| 权限形态 | 一期不判权限（`PERMISSION_ENFORCED=false`，缺省）：全员等效管理员画像 |
| 执行账号 | 管理员（建项目 / 上传 / 换签下载）；另有**非成员**（sales 角色）与**匿名**（401）各一条 |
| 脚本 | server/scripts/m4-download-replay.mjs |

## 断言明细

| PASS | S0 | api 可用（/healthz + /readyz：含存储探针） | 
  - 期望：200 / 200
  - 实际：200 / 200
| PASS | P1 | 建回放项目（为 file 提供 projectId） | 
  - 期望：201 + 项目可见
  - 实际：201 {"id":"6b645fae-7379-4bb9-a08d-42136e3f01d1","code":"M4DL-20260924011429"}
| PASS | F1 | 上传 v1（真实上传管道：init → 分片直传 → complete） | 
  - 期望：200 + 版本 v1
  - 实际：{"status":200,"seq":1}
| PASS | F2 | 追加 v2（intent=version + fileId：同一文件第二个版本） | 
  - 期望：200 + 版本 v2
  - 实际：{"status":200,"seq":2,"fileId":"6105bd27-5ece-4ec3-8a23-fdf62a790585"}
| PASS | D1 | 取 v1 下载地址 → 200 + 四字段（url / fileName / sizeBytes / expiresAt），窗口 ≈ S3_DOWNLOAD_URL_TTL_SECONDS（与预览窗口独立） | 
  - 期望：fileName=原名 + sizeBytes=262205 + TTL ≈ 600s（预览配置 900s）
  - 实际：{"status":200,"fileName":"下载回放-机械设计图纸.pdf","sizeBytes":262205,"ttlSeconds":600,"urlHost":"http://127.0.0.1:9000/libiaolink/projects/6b645fae-7379-4bb9-a08d-42136e3f01d1/f"}
| PASS | D2 | GET 签名地址 → 200 + 字节与 v1 **逐字节一致** + `Content-Disposition: attachment`（原名 URL 编码，浏览器落盘） | 
  - 期望：status=200 + sha256=ea607e61552f… + disposition 含 attachment 与 UTF-8 文件名
  - 实际：{"status":200,"hash":"ea607e61552f…","disposition":"attachment; filename=\"____-______.pdf\"; filename*=UTF-8''%E4%B8%8B%E8%BD%BD%E5%9B%9E%E6%94%BE-%E6%9C%BA%E6%A2%B0%E8%AE%BE%E8%AE%A1%E5%9B%BE%E7%BA%B8.pdf"}
| PASS | D3 | 写一条 `action = download` 审计（object_type = file、metadata.versionId = v1）；**不写 preview 行** | 
  - 期望：download 1 条（metadata.versionId = v1）+ preview 0 条
  - 实际：{"downloads":1,"versionId":"de235216-eeb3-44e1-9e95-b59e593568bc","objectType":"file","summary":"下载文件：下载回放-机械设计图纸.pdf（版本 v1）","previews":0}
| PASS | D4 | v2 下载：按 v2 对象键签名、返回 v2 体积 —— 下载内容不串版本（A4-06） | 
  - 期望：status=200 + sha256=b0f689f0e60e…（≠ v1）+ sizeBytes=393277
  - 实际：{"status":200,"hash":"b0f689f0e60e…","sizeBytes":393277,"sameAsV1":false}
| PASS | D5 | 两次下载 → download 审计两条（一条一次，不合并、不重复） | 
  - 期望：download 2 条
  - 实际：{"downloads":2}
| PASS | D6 | 版本不属于该文件（用另一文件的 versionId）→ 404（同形，不泄漏版本归属） | 
  - 期望：404
  - 实际：{"status":404,"code":"NOT_FOUND"}
| PASS | D7 | 文件不存在（随机 UUID）→ 404 | 
  - 期望：404
  - 实际：{"status":404,"code":"NOT_FOUND"}
| PASS | D8 | 无会话 → 401（D2-04 禁匿名：下载地址必须带会话换签） | 
  - 期望：401
  - 实际：{"status":401,"code":"AUTH_REQUIRED"}
| PASS | D9 | 权限形态探测（sales 角色用户画像）：与脚本声明形态一致（等效管理员画像 ⇔ `PERMISSION_ENFORCED=false`） | 
  - 期望：声明 = 一期不判权限（false）→ 画像应为等效管理员；画像键 31 个
  - 实际：{"enforced":false,"profileKeys":31,"hasFileDownload":true,"equivalentAdmin":true}
| PASS | D10 | 默认态（一期不判权限）：非名册成员画像 = 等效管理员 → 下载 200 + 写一条 download 审计（记录级不裁剪） | 
  - 期望：200 + 审计 2 → 3 条
  - 实际：{"mode":"phase1-off","status":200,"fileName":"下载回放-机械设计图纸.pdf","auditsBefore":2,"auditsAfter":3}

## 汇总

- ✅ 全部断言通过（14 项）：签名四字段与独立窗口 / 附件投递（字节逐字节一致 + Content-Disposition 原名）/ download 审计一条一次 / v1·v2 版本路由 / 跨文件版本 404 / 文件不存在 404 / 禁匿名 401 / 权限形态（一期不判权限 → 全员可下载）。

## 未覆盖 / 风险登记

- **运行形态（两态各跑一次）**：默认态（`PERMISSION_ENFORCED=false`）证「一期不判权限 → 全员可下载」；判权限态（`true`）证「非成员不可见 → 404、不写审计」——脚本按声明形态自适应断言，两态分别产出证据。
- **403（可见但缺 `file.download`）真机不可达**：`file.download` 在项目成员 / 项目经理的**隐含权限位**内（`permission.rules.ts`），而可见性本身来自名册 / 主数据责任人 —— 一期六角色矩阵下「可见但无权下载」构造不出来；该分支由单测覆盖（`test/file-download.test.ts`：缺权限 403 且不签名不审计）。二期角色矩阵（更细粒度授权）落地后可在真机复验。
- **压测未做**：并发 2~4、200MB 级长跑内存曲线不在本脚本范围（属 M4-05 压测 / M8 容量验证）。
- **签名地址的生命周期由对象存储保证**：脚本只证「签发后可取回」；过期后 403 由 S3 侧拒绝（`S3_DOWNLOAD_URL_TTL_SECONDS` 窗口外），未在本脚本做等待过期的慢断言。

## 复跑

```bash
cd server && npm run build && S3_DOWNLOAD_URL_TTL_SECONDS=600 PREVIEW_URL_TTL_SECONDS=900 npm run start:api &
# 判权限态复跑：api 与脚本两侧同加 PERMISSION_ENFORCED=true，D10 断言翻转为「非成员 404」
cd server && M4_DATABASE_URL=postgresql://libiaolink_migrator@127.0.0.1:55432/libiaolink \
  S3_DOWNLOAD_URL_TTL_SECONDS=600 PREVIEW_URL_TTL_SECONDS=900 node --env-file-if-exists=.env scripts/m4-download-replay.mjs --out "../docs/m4-05f-回放证据(下载切片).md"
```
