# 市场项目管理系统 · 技术设计 v0.2（架构与数据模型篇）

- 版本：v0.2.8 讨论稿，2026-09-21
- 入仓：仓库根 技术设计v0.2-架构与数据模型.md（2026-09-17 入仓，PR 评审、与《开发日志》同批同步；v0.1 同批入仓）
- 输入基线：《技术设计v0.1-选型分析.md》v0.1.7（含业务确认 Q1~Q29）
- 需求口径：《市场项目管理系统_功能设计与需求说明书_V1.0》+《系统功能书 V1.0》（评审稿）；功能书后续修订不阻塞本设计（业务负责）
- 本文范围：总体架构与进程边界、领域模型与实体关系、流程节点与蓝图（含 JSON schema）、权限模型细化、文件生命周期与预览管道、自动化规则引擎、接口契约与错误模型、首页分类筛选契约、阶段 0 映射与实施顺序
- 不在本文范围：部署细节与运维手册（另立）、前端页面与交互稿（另立）、功能书条目修订（不归本设计）

**修订记录**

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.2 | 2026-09-17 | 首稿：架构、数据模型、节点与蓝图 schema、接口契约 |
| v0.2.1 | 2026-09-17 | 入仓：本文档入仓库根（技术设计v0.2-架构与数据模型.md，与 v0.1 同批）。对齐实现：§2.3 补 tasks / task_events DDL 与 projects.manager_id；§2.2 关系、§2.4 任务状态（5 态派生，逾期标注在实际完成日期） |
| v0.2.2 | 2026-09-18 | 项目级人员字段收敛：删 projects.owner_id，manager_id 改必填（项目级唯一责任人为项目经理）；§4.1 角色、§8.1 维度表、§8.2 筛选与索引同步；首页筛选 filter[managerId]（多值逗号分隔）；facets 组名 ownerId → managerId（详见开发日志 Push 34） |
| v0.2.3 | 2026-09-18 | 项目主数据新增「项目序号」：projects.seq_no（全库唯一正整数，创建时由序列 projects_seq_no_seq 分配，不可修改、不回收；与项目编号一一对应同一项目）；契约 Project.seqNo 随列表 / 详情返回、创建请求不传；列表支持 sort=seqNo:asc\|desc；首页卡片右上角徽标两位补零展示。§2.3 DDL、§8.2 排序同步；迁移见 database/migrations/0003_projects_seq_no.sql |
| v0.2.4 | 2026-09-18 | g4 落地回写：§1.2 依赖规则实现为 `server/scripts/check-boundaries.mjs`（零新增依赖，违规退出码 1）；§1.1 的 api / worker 双入口与健康检查就位；§2.3 的 Drizzle schema 与 0001+0002+0003 基线对齐并有漂移检查；后端版本基线实测（NestJS 12 ESM / TS 6.0.x / Drizzle 0.45.2）见 ADR-017 |
| v0.2.5 | 2026-09-18 | g6 落地回写：§2.1 / §2.3 增补 users / sessions（0004 迁移；会话 Cookie 值只存 sha256 哈希，id_token 仅用于单点登出）；§7.1 增补 /auth/* 根路径会话约定；§7.2 增补 AUTH_CALLBACK_FAILED(400)；实现见 server/src/modules/identity/（ADR-010） |
| v0.2.6 | 2026-09-18 | 实施反推增补：新增 §11（M1~M7 需新增的表 / 需修订字段 / 需新增契约族与错误码登记；来源《技术设计v0.3-实施与验收.md》）；M0 待定案口径清单见 v0.3 §7 |
| v0.2.7 | 2026-09-20 | h8 落地回写：§1.2 模块表补 `calendar`（横切：工作日历 D5 —— 例外维护 / 顺延规则 / T-1·T+1 求值）与依赖规则第 4 条（横切模块领域与平台都可依赖）；§11.1 补 `calendar_days` / `calendar_settings`（0014 迁移） |
| v0.2.8 | 2026-09-21 | w2 落地回写（A15 / A18 / A19 / A20 · Push 122）：§2.3 补 `tasks.sort_index`（组内位次：一组 = 同一项目 + 同一阶段，`stage_key` 为空 = 「未分组」自成一组）+ CHECK + 默认读序索引 `ix_tasks_project_stage_order`；`tasks.stage_key` / `owner_id` 放宽可空（「未分组」/「待分配」是合法状态）；§2.2 任务与关系行补位次与可空说明（0015 迁移） |

---

## 1. 总体架构与进程边界

### 1.1 进程与组件

| # | 进程/组件 | 职责 | 关键约束 |
|---|---|---|---|
| 1 | web（Nginx） | TLS 终止、SPA 静态资源、/api 反向代理 | 仅此组件暴露端口 |
| 2 | api（NestJS） | HTTP API、会话与权限判定、业务事务、Outbox 写入、SSE（通知/导出进度） | 无状态、可水平扩展；不执行 CPU 密集任务 |
| 3 | worker（NestJS 独立入口） | Outbox 投递、调度（scheduler）、自动化规则执行、转换编排、报表导出、补发 | 同一镜像不同入口；advisory lock 保证单实例调度 |
| 4 | converter（转换沙箱） | LibreOffice headless / dwg 转换等预览产物生成 | 无外网、非 root、只读根文件系统、资源限额与超时；不可访问数据库与内网 |
| 5 | PostgreSQL 18 | 业务数据、Outbox、队列表、搜索投影、审计 | 单一事实源；备份以可恢复为验收标准 |
| 6 | MinIO（S3 协议） | 原文件、转换产物、归档包、备份清单 | 独立数据盘；开启版本控制；预览不允许匿名读取 |
| 7 | 公司 SSO（Casdoor） | OIDC 登录、组织与账号来源 | 外部资产；本系统只做 OIDC 客户端 |

~~~text
浏览器 ── HTTPS ─▶ Nginx ──┬─▶ SPA 静态资源
                            └─▶ api（/api/v1，SSE）
api ── SQL ─▶ PostgreSQL（业务表 + outbox + 作业队列 + 搜索投影）
api ── S3 ─▶ MinIO
worker ── SKIP LOCKED 领取 ─▶ 调度 / 规则 / 投递 / 转换编排
worker ── HTTPS ─▶ 企业微信（应用消息 / 群机器人）
converter ── S3（内网）─▶ MinIO        （worker 触发，容器隔离）
~~~

### 1.2 模块划分（模块化单体的边界规则）

| 模块 | 职责 | 对外接口（示例） |
|---|---|---|
| identity/org | SSO 接入、组织同步、用户、角色 | getUser、listDepartments |
| project | 项目主数据、成员、阶段、首页分类字段 | ProjectService、FacetService |
| blueprint | 蓝图（全局一份、版本化）、校验、导入 | BlueprintService、ImportService |
| node | 流程节点实例、约束、完成门禁、增删留痕 | NodeService、GateService |
| task | 任务主表、状态与进度聚合 | TaskService |
| report/issue | 日报、问题闭环（四态）、规则触发事件 | ReportService、IssueService |
| stakeholder | 干系人台账与项目关联 | StakeholderService |
| file | 文件、版本、定档、变更（申请即通过）、预览编排、回收站 | FileService、PreviewService、ChangeService |
| automation | 规则定义、执行、回放与留痕 | RuleEngine |
| notify | 站内信、企微投递、模板、合并与免打扰 | NotifyService |
| search | 搜索投影与查询 | SearchService |
| dashboard | 统计与下钻（基础仪表盘） | DashboardService |
| admin | 字典、审计、备份恢复、运维页 | DictService、AuditService |
| calendar（横切） | 工作日历（D5）：节假日 / 调休维护、顺延规则、T-1 / T+1 求值 | CalendarService |

依赖规则（写进仓库规则与 CI）：

1. 单向依赖：业务模块可依赖平台模块，反向禁止（project → file 允许；file → project 只允许读取项目快照接口）。
2. 跨模块禁止直接改表；跨模块副作用走同事务 Outbox 事件。
3. 模块内部结构统一：controller / service / repository / events；DTO 用 Zod 契约包生成。
4. 横切模块（`permission` / `calendar`）领域与平台都可依赖，不参与第 1 条判定（登记于 `server/scripts/check-boundaries.mjs`）。

### 1.3 事务、一致性与幂等

| 场景 | 设计 |
|---|---|
| 业务写入 | 单事务内写 业务表 + outbox_events + 搜索投影（同库） |
| 外部副作用 | 只能由 worker 执行；至少一次投递 + 消费幂等（dedupe_key） |
| 写接口幂等 | 客户端带 Idempotency-Key，服务端落幂等表，重复请求返回首次结果 |
| 并发控制 | 乐观锁：version 条件更新；队列：SKIP LOCKED；单例调度：advisory lock |
| 时间语义 | 存储 UTC，展示 Asia/Shanghai；业务日期用 date；时钟注入可测 |
| 大文件 | 直传 MinIO（分片 + 断点续传），api 只负责签名与元数据 |

### 1.4 非目标（一期明确不做）

- 微服务拆分、K8s、Redis、Elasticsearch、独立消息队列（沿用 PG 原生队列）
- 在线编辑（只读预览）、移动端专项、甘特图、ERP 集成实现、外部格式蓝图导入
- 流程节点体系与变更申请的角色化权限（平权 + 留痕；权限位预留）
---

## 2. 领域模型与数据字典

### 2.1 实体总览

| 聚合 | 主要表 | 说明 |
|---|---|---|
| 组织与权限 | users、sessions、departments、roles、user_roles | SSO 为准；业务授权在本系统；会话在 PostgreSQL（ADR-010） |
| 项目 | projects、project_members、project_stages | 项目主数据含首页分类字段（地区/项目类型/项目经理） |
| 流程 | blueprints、blueprint_versions、project_nodes、node_requirements | 蓝图全局一份、版本化；项目节点为快照实例 |
| 任务 | tasks、task_events | 状态由日期字段推导，事件表留痕；组内位次 `sort_index` 持久化（一组 = 同一项目 + 同一阶段，A19 / A20 · Push 122） |
| 日报与问题 | daily_reports、issues、issue_events | 日报可生成问题；同一日报同一问题只生成一次（来源字段） |
| 干系人 | stakeholders、project_stakeholders | 隐私字段走字段级权限 |
| 文件与变更 | files、file_versions、change_requests、change_items | 变更申请即通过（一期）；版本链不可覆盖 |
| 自动化与通知 | automation_rules、automation_runs、notifications、outbox_events | 规则参数化 + 启停；执行全程留痕 |
| 平台 | dict_types、dict_items、audit_logs、search_docs、jobs | 字典唯一口径；审计抗篡改 |

### 2.2 关系（核心外键）

~~~text
projects 1─n project_stages 1─n project_nodes 1─n node_requirements
projects 1─n project_members n─1 users
project_nodes 1─n tasks n─1 users(owner，可空 = 待分配)；projects n─1 users(manager，项目经理)
projects 1─n files 1─n file_versions
change_requests 1─n change_items n─1 files（一条变更可含多个文件）
projects 1─n daily_reports 1─n issues；issues n─1 tasks(可空)
projects 1─n project_stakeholders n─1 stakeholders
automation_rules 1─n automation_runs；外发消息经 outbox_events.dedupe_key 幂等
~~~

### 2.3 关键表 DDL 草案（一期核心）

约定：主键 uuid（gen_random_uuid()）；时间 timestamptz；乐观锁 version integer；文件与节点的软删除用状态字段表达；枚举取值见 2.5 字典。

~~~sql
-- 项目（首页分类字段就位）
create sequence projects_seq_no_seq;      -- 项目序号唯一来源
create table projects (
  id uuid primary key default gen_random_uuid(),
  seq_no integer not null unique default nextval('projects_seq_no_seq'),  -- 项目序号（服务端分配、全库唯一、不可修改、不回收；与项目编号一一对应同一项目；卡片右上角两位补零展示）
  code text not null unique,              -- 项目编号
  name text not null,
  customer text,
  region text not null,                   -- 项目落地地区（字典 region）
  project_type text not null,             -- 项目类型（字典 project_type）
  manager_id uuid not null,               -- 项目经理（项目级唯一责任人，必填；首页「项目经理」维度取值）
  stage_key text not null,                -- 当前阶段（九阶段字典）
  status text not null,                   -- active | paused | done | archived
  description text,
  version integer not null default 0,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
create index ix_projects_facets on projects (region, project_type, manager_id);
create index ix_projects_stage on projects (status, stage_key);
alter sequence projects_seq_no_seq owned by projects.seq_no;   -- 序列随列生命周期（v0.2.3）

-- 项目阶段（导入蓝图时生成）
create table project_stages (
  id uuid primary key default gen_random_uuid(),
  project_id uuid not null references projects(id),
  stage_key text not null,                -- 九阶段字典
  seq smallint not null,
  status text not null,                   -- pending | active | done
  planned_start date, planned_end date,
  actual_start date, actual_end date,
  version integer not null default 0,
  unique (project_id, stage_key)
);

-- 流程节点实例（快照 + 按模板增删 + 留痕）
create table project_nodes (
  id uuid primary key default gen_random_uuid(),
  project_id uuid not null references projects(id),
  stage_id uuid not null references project_stages(id),
  node_key text not null,                 -- 蓝图节点稳定键（模板节点池）
  name text not null,
  seq numeric(10,2) not null,             -- 排序，留插入空间
  status text not null,                   -- pending | active | done | deleted
  origin text not null,                   -- blueprint | added_by_user（一期仅 blueprint）
  done_at timestamptz, done_by uuid,
  deleted_at timestamptz, deleted_by uuid,
  source_blueprint_version integer not null,
  version integer not null default 0,
  created_at timestamptz not null default now(),
  unique (project_id, node_key)
);
create index ix_nodes_project_seq on project_nodes (project_id, seq);

-- 节点约束（一期实现 required_doc 门禁；其余类型预留）
create table node_requirements (
  id uuid primary key default gen_random_uuid(),
  node_id uuid not null references project_nodes(id),
  requirement_type text not null,         -- required_doc | field | dependency | deadline
  doc_type text,                          -- 成果文件字典（required_doc 时必填）
  min_count integer not null default 1,
  config jsonb,
  created_at timestamptz not null default now()
);
create index ix_node_req_node on node_requirements (node_id);

-- 任务（项目总览 15 列口径；status 为存储基础态，展示态派生，见 2.4）
create table tasks (
  id uuid primary key default gen_random_uuid(),
  project_id uuid not null references projects(id),
  stage_key text,                         -- 九阶段字典；可空 = 「未分组」（A15 · Push 122）
  node_id uuid references project_nodes(id),  -- 来源节点（模板生成；手工补充任务为空）
  title text not null,                    -- 任务描述（中文）
  title_en text,                          -- 任务描述（英文）
  owner_id uuid,                          -- 任务负责人；可空 = 「待分配」（A18 · Push 122）
  status text not null,                   -- pending | active | done（存储基础态；展示态见 2.4）
  progress numeric(3,2) not null default 0,   -- 四格进度：0 / 0.25 / 0.5 / 0.75 / 1
  planned_start date,                     -- 开始日期
  planned_end date,                       -- 预计完成日期
  actual_end date,                        -- 实际完成日期（空=未完成；逾期与按时交付按此列判定/标注）
  estimated_days smallint,                -- 预计所需天数（由两个日期推导）
  headcount smallint,                     -- 预计所需施工人数
  priority text,                          -- 紧急重要度（四象限字典）
  deliverable text,                       -- 输出成果文件（十类成果文件字典）
  note text,                              -- 项目进展描述
  on_time boolean,                        -- 是否按时交付（派生：actual_end 不晚于 planned_end）
  change_ref uuid,                        -- 变更关联（最近一次变更，指向 change_requests）
  version integer not null default 0,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  sort_index integer not null default 0,  -- 组内位次（A19 / A20 · Push 122）：一组 = 同一项目 + 同一阶段（stage_key 为空 = 未分组），组内 0 起、密集
  check (sort_index >= 0)
);
create index ix_tasks_project_stage on tasks (project_id, stage_key);
create index ix_tasks_project_stage_order on tasks (project_id, stage_key, sort_index); -- 默认读序（阶段序 → 位次 → id）索引，w2 · Push 122
create index ix_tasks_owner_due on tasks (owner_id, planned_end);
create index ix_tasks_due on tasks (project_id, actual_end, planned_end);

-- 任务事件（字段级留痕：状态 / 日期 / 进度 / 描述变更）
create table task_events (
  id bigserial primary key,
  task_id uuid not null references tasks(id),
  event_type text not null,               -- status_change | date_change | progress_change | note_change
  before_value text, after_value text,
  actor_id uuid not null,
  created_at timestamptz not null default now()
);
create index ix_task_events_task on task_events (task_id, created_at desc);

-- 文件（五态：draft | final | changed | archived | recycled）
create table files (
  id uuid primary key default gen_random_uuid(),
  project_id uuid not null references projects(id),
  node_id uuid references project_nodes(id),
  task_id uuid,
  doc_type text,                          -- 十类成果文件字典；普通附件为空
  name text not null,
  status text not null,
  current_version_id uuid,
  version integer not null default 0,
  created_by uuid not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
create index ix_files_node on files (node_id, status);
create index ix_files_project on files (project_id, status);

-- 文件版本（内容哈希去重；定档后不覆盖）
create table file_versions (
  id uuid primary key default gen_random_uuid(),
  file_id uuid not null references files(id),
  seq integer not null,
  object_key text not null,               -- MinIO 键
  size_bytes bigint not null,
  content_hash text not null,
  mime text,
  uploaded_by uuid not null,
  uploaded_at timestamptz not null default now(),
  change_request_id uuid,
  unique (file_id, seq)
);

-- 变更申请（一期：申请即通过、所有人平权、全程留痕）
create table change_requests (
  id uuid primary key default gen_random_uuid(),
  project_id uuid not null references projects(id),
  node_id uuid references project_nodes(id),
  stage_key text,                         -- 直接引用九阶段字典
  reason text not null,
  before_summary text,
  after_summary text,
  status text not null,                   -- applied（一期唯一终态）
  applied_by uuid not null,
  applied_at timestamptz not null default now(),
  created_at timestamptz not null default now()
);
create index ix_change_project on change_requests (project_id, created_at desc);

-- Outbox（外部副作用唯一出口）
create table outbox_events (
  id bigserial primary key,
  topic text not null,                    -- wecom_msg | notification | preview_job | rule_run | export
  payload jsonb not null,
  dedupe_key text not null unique,        -- 幂等键
  status text not null,                   -- pending | processing | done | dead
  available_at timestamptz not null default now(),
  attempts integer not null default 0,
  last_error text,
  created_at timestamptz not null default now()
);
create index ix_outbox_ready on outbox_events (status, available_at);

-- 身份与会话（0004；ADR-010：SSO-only 登录；会话 Cookie 值只存哈希）
create table users (
  id uuid primary key default gen_random_uuid(),
  casdoor_id text not null unique,        -- Casdoor 用户 ID（claims.id）
  username text not null unique,          -- 工号（claims.name）
  display_name text not null,             -- 姓名（claims.displayName）
  email text,
  owner text,                             -- 所属组织（claims.owner）
  status text not null default 'active',  -- active | disabled
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  constraint ck_users_status check (status in ('active','disabled'))
);

create table sessions (
  id uuid primary key default gen_random_uuid(),
  token_hash text not null unique,        -- sha256(会话令牌)；原始值只在浏览器 Cookie
  user_id uuid not null references users(id) on delete cascade,
  id_token text not null,                 -- 仅用于 Casdoor 单点登出（id_token_hint）
  created_at timestamptz not null default now(),
  last_seen_at timestamptz not null default now(),  -- 空闲超时基准（SESSION_IDLE_MINUTES，默认 30 分钟）
  expires_at timestamptz not null,        -- 绝对上限＝ID Token exp
  revoked_at timestamptz,
  constraint ck_sessions_expires check (expires_at > created_at)
);
create index ix_sessions_user on sessions (user_id, expires_at desc);
create index ix_sessions_expiry on sessions (expires_at);
~~~

### 2.4 状态机与派生态

| 对象 | 状态 | 规则 |
|---|---|---|
| 任务 | 待开始 / 进行中 / 已完成 / 已延期 / 提前完成 | 展示态由日期字段派生（存储基础态仅 pending/active/done，派生结果不写回）：完成时按 实际完成日期 与 预计完成日期 比较——早于=提前完成；等于/当天=已完成（按时）；晚于=已完成且按时=否（逾期补完不再回到"提前完成"）；未完成且已过预计日期=已延期。"逾期"不是状态取值：红色「逾期」标注落在实际完成日期（表格列与任务详情），状态列仅展示上述 5 态 |
| 问题 | 未分组 / 未解决 / 处理中 / 已完成 | 四态制；"超时未确认自动关闭"的规则待 Q28 业务确认后补充，不预置"已处理/待确认"中间态 |
| 文件 | draft / final / changed / archived / recycled | draft=草稿可替换；final=定档锁 current_version；changed=定档后发生过变更（历史版本保留）；archived=只读；recycled=回收站（30 天清理，可恢复） |
| 节点 | pending / active / done / deleted | 完成必须过门禁（见 3.5）；删除仅限模板节点池且留痕；已关联成果物的节点删除需先解绑或确认（产品口径待定） |
| 变更 | applied（一期唯一终态） | 申请即通过；多级审批模型保留，二期可启用 |
| 蓝图 | draft / published | 发布产生新版本；不自动影响已生成项目（快照） |

### 2.5 字典（C9）第一批

| 字典 | 取值 | 说明 |
|---|---|---|
| 阶段（九） | 售前规划 / 设计开发 / 加工采购 / 组装发货 / 硬件实施 / 软件部署 / 试运行 / 生产阶段 / 验收 | 项目、节点、变更阶段统一引用 |
| 成果文件（十类） | CAD图纸 / 技术协议 / 合同 / 评审单 / 设备清单 / 物料总清单 / 发货装箱单 / 到货单 / 安装完成证明 / 验收单 | 门禁 required_doc 只能引用此字典 |
| 变更阶段 | 直接引用九阶段字典 | 原四值映射建议：采购备货→加工采购；发货→组装发货；实施→硬件实施~软件部署；验收→验收（待业务最终确认） |
| 问题归类 | 机械部 / 采购部 / 规划部 / 项目部 / 物流原因 / 供应商原因 / 客户原因 / 客观原因 / 生产原因 / 其它 | 评审补充 |
| 紧急重要度 | 重要且紧急 / 紧急但不重要 / 重要不紧急 / 不紧急不重要 | |
| 地区 | 项目落地地区（业务给清单） | 首页分类用 |
| 项目类型 | 业务给清单 | 首页分类用 |

---

## 3. 流程节点体系与蓝图

### 3.1 概念与生命周期

- **蓝图**：按项目类型组织（每个项目类型一份 + 默认模板兜底；blueprint_versions 保存完整 JSON 与校验结果）；管理员维护，发布即递增 blueprintVersion。【2026-09-20 修订：原「全局唯一」口径改按 ADR-019 执行】
- **项目节点**：项目创建时按「当前已发布蓝图」导入生成快照；之后项目内可增删（仅模板节点池、留痕），不再受蓝图后续变更影响（导入即快照）。
- **约束**：节点约束存 node_requirements；一期实现 required_doc 门禁，field / dependency / deadline 数据结构就位、规则后置（Q28 定细则）。

### 3.2 蓝图 JSON schema（v1，示例）

~~~json
{
  "schemaVersion": 1,
  "blueprintVersion": 3,
  "name": "立镖市场项目全流程蓝图",
  "updatedAt": "2026-09-17T00:00:00Z",
  "stages": [
    {
      "key": "presale",
      "name": "售前规划",
      "seq": 10,
      "nodes": [
        {
          "key": "presale.requirement",
          "name": "需求澄清与现场勘察",
          "seq": 10,
          "constraints": []
        },
        {
          "key": "presale.contract",
          "name": "合同与技术协议评审",
          "seq": 20,
          "constraints": [
            { "type": "required_doc", "docType": "技术协议", "minCount": 1 },
            { "type": "required_doc", "docType": "合同", "minCount": 1 }
          ]
        }
      ]
    },
    {
      "key": "design",
      "name": "设计开发",
      "seq": 20,
      "nodes": [
        {
          "key": "design.mech",
          "name": "机械设计图纸",
          "seq": 10,
          "constraints": [
            { "type": "required_doc", "docType": "CAD图纸", "minCount": 1 }
          ]
        },
        {
          "key": "design.review",
          "name": "设计评审",
          "seq": 20,
          "constraints": [
            { "type": "required_doc", "docType": "评审单", "minCount": 1 }
          ]
        }
      ]
    }
  ]
}
~~~

说明：以上为两阶段示例；其余阶段同构（见下表节点示例），schema 支持任意增补。阶段 key 与节点 key 一经发布不可改名（稳定键），改名视为新增 + 废弃。

### 3.3 首批节点清单（建议稿，业务后补全）

| 阶段 key | 阶段 | 节点示例 |
|---|---|---|
| presale | 售前规划 | 需求澄清与现场勘察；方案与报价；合同与技术协议评审 |
| design | 设计开发 | 机械设计图纸；电气设计图纸；设计评审 |
| purchase | 加工采购 | 设备清单；物料总清单；采购订单确认 |
| assembly | 组装发货 | 发货装箱单；到货单 |
| install | 硬件实施 | 施工计划；安装完成证明 |
| deploy | 软件部署 | 部署方案；上线确认 |
| trial | 试运行 | 试运行记录 |
| production | 生产阶段 | 生产运行记录 |
| acceptance | 验收 | 验收申请；验收单 |

### 3.4 校验规则（保存/发布/导入时执行）

1. schemaVersion 匹配；必填字段齐全。
2. stage.key 与 node.key 全局唯一、非空、稳定（同一次校验内不得重复）。
3. seq 递增（允许 10/20/30 步长，便于插入）。
4. required_doc.docType 必须存在于成果文件字典（十类）。
5. 至少一个阶段、每个阶段至少一个节点。
6. 校验失败一次性返回全部 issues（路径 + 原因），不允许部分导入。

### 3.5 导入流程（项目创建时，事务内）

1. 校验项目参数（region / project_type / manager 必填）。
2. 读取该项目类型的 published 蓝图（缺失时回落默认模板；锁定 blueprintVersion）【ADR-019】。
3. 生成 project_stages + project_nodes + node_requirements（node_key 唯一约束防重）。
4. 写审计（谁、何时、从哪个蓝图版本导入）。
5. 同事务写 outbox（可选的启动通知 / 待办）。

幂等：重试时已存在的 node_key 跳过；导入失败整体回滚。

### 3.6 完成门禁（核心正确性）

规则：节点存在 required_doc 且未满足 → 不允许完成。

~~~text
begin
  select ... for update 锁定节点（检查 status=active 且 version 匹配）
  统计：files where node_id=? and doc_type=? and status in (final, changed) and current_version_id is not null
  若任一 required_doc 不满足：
    写审计（完成被拒 + 缺失明细）
    返回 422 NODE_REQUIRED_DOC_MISSING（含 missing 列表）
  否则：
    node.status=done, done_at=now(), done_by=当前用户, version+1
    写审计（完成）+ 同事务写 outbox（node.completed 事件）
commit
~~~

拒绝响应示例：

~~~json
{
  "code": "NODE_REQUIRED_DOC_MISSING",
  "message": "缺少必交成果文件，无法完成节点",
  "details": {
    "nodeId": "uuid",
    "missing": [
      { "docType": "技术协议", "required": 1, "found": 0 }
    ]
  }
}
~~~

前端体验：进入节点详情调用 can-complete 预检（同一 GateService），按钮置灰并展示缺失清单；最终判定以服务端为准（防绕过）。

### 3.7 接口（/api/v1）

| 方法 | 路径 | 用途 | 备注 |
|---|---|---|---|
| GET | /blueprint | 当前蓝图（含版本） | 只读 |
| PUT | /blueprint | 保存蓝图草稿 | 管理员；必须通过校验 |
| POST | /blueprint/publish | 发布新版本 | 递增 blueprintVersion；留痕 |
| GET | /blueprint/export | 导出 JSON | 自建格式 |
| POST | /blueprint/import | 导入 JSON（保存为草稿） | schema + 引用校验 |
| GET | /projects/{id}/flow | 项目流程（阶段 + 节点 + 约束 + 状态） | 详情/首页用 |
| POST | /projects/{id}/nodes | 增节点（仅模板节点池） | 留痕 |
| DELETE | /projects/{id}/nodes/{nodeId} | 删节点 | 留痕；关联成果物时先提示 |
| POST | /nodes/{id}/complete | 完成（过门禁） | 失败返回 422 + missing |
| GET | /nodes/{id}/can-complete | 完成预检 | UI 置灰依据 |

### 3.8 测试（金标用例）

- 门禁：缺 required_doc → 422；补齐 → 200；并发双完成 → 一个 200、一个 409（version 冲突）。
- 蓝图：非法 docType / 重复 node_key / 缺阶段 → 校验失败并一次性返回全部 issues。
- 导入：重复导入幂等（不重复生成节点）；导出 → 导入 round-trip 一致。
- 审计：完成/被拒/增删节点均有留痕且可查询。

---

## 4. 权限模型细化（承接 v0.1 第 4.12 节）

### 4.1 角色与数据范围（一期样例，最终以权限矩阵 PR 为准）

| 角色 | 数据范围 | 关键能力 | 限制 |
|---|---|---|---|
| 系统管理员 | 全部 | 字典与蓝图、审计、备份恢复、运维 | 不替代业务操作审批（一期无审批） |
| 项目经理 | 我管理的项目 | 建项目 / 导入蓝图、成员、节点增删、定档、变更 | 不可越项目范围 |
| 任务负责人 | 我参与的项目 | 我的任务、上传文件、发起变更、完成节点 | 干系人隐私字段按字段权限隐藏 |
| 项目成员 | 我参与的项目 | 查看、上传、日报、问题 | 删除 / 导出受操作级权限限制 |
| 销售（录入） | 我录入的干系人 | 干系人新增 / 编辑 | 仅干系人模块 |
| 只读 | 按授权 | 查看与受控导出 | 无写操作 |

### 4.2 判定出口一致性（五出口）

列表 / 详情 / 导出 / 搜索 / 通知内容 全部走同一策略服务；无权限字段不返回；导出与搜索投影使用同一权限过滤；预览与下载同样计入审计。

### 4.3 例外与预留（本轮确认）

- 流程节点体系与变更申请：**不按角色区分（平权）、全程留痕**（见 v0.1 Q24/Q25）。
- 策略键预留：node.create / node.delete / node.complete / change.approve，一期不启用，二期可开启。
- 审计范围：完成 / 被拒 / 回退 / 增删 / 定档 / 变更 / 导出 / 下载 / 预览 / 越权尝试。

---

## 5. 文件生命周期与预览管道

### 5.1 对象键与版本

~~~text
原文件：    projects/{projectId}/files/{fileId}/v{seq}/{contentHash}.{ext}
预览产物：  previews/{contentHash}/{pipelineVersion}/{target}
归档包：    archives/{projectId}/{archiveId}.zip
备份清单：  backups/manifests/{date}.json
~~~

规则：内容哈希去重（同哈希共用对象）；定档只锁 current_version；物理对象不覆盖；中文文件名保留在元数据；桶版本控制开启。

### 5.2 状态机

draft（草稿，可替换）→ final（定档，锁版）→ changed（定档后发生变更，current 指向新版本、历史版本保留）→ archived（归档后只读）→ recycled（回收站，30 天清理，可恢复）。

### 5.3 变更流程（一期：申请即通过、所有人平权）

1. 发起变更（选文件、填原因、变更前后摘要、关联节点/阶段）。
2. 服务端同事务：写 change_requests（status=applied）+ 生成新 file_version + 更新 files.current_version_id 与 status=changed + 写审计 + 写 outbox（通知相关人）。
3. 不产生审批任务、不等待审批；多级审批模型保留、二期可启用。

### 5.4 预览管道（只读、系统内直接查看、对标 ONLYOFFICE 查看体验）

| 类型 | 处理方式 | 说明 |
|---|---|---|
| Office（docx / xlsx / pptx） | LibreOffice headless 转 PDF；xlsx 另生成结构化渲染数据 | 产物按 哈希 + pipelineVersion 缓存；失败降级提示"请下载" |
| PDF / 图片 | 直出 | 前端 PDF 查看器 / 图片查看 |
| 二维 CAD（dwg / dxf） | 阶段 0 PoC：本地转换 → PDF / SVG | 达标才上线，否则降级仅下载（条件性交付） |
| 三维 | 一期仅上传 / 下载 / 关联 | 在线查看走自建路线：开源组件优先（three.js + occt-import-js / OpenCASCADE WASM），无合适组件则自研；不上第三方云 |

安全与体验：预览地址短时签名 + 鉴权 + 审计；converter 无外网；定档时预生成产物（P1），其余按需懒生成。

---

## 6. 自动化规则引擎（一期 R01~R07 + A01/A02/A03/A14）

### 6.1 规则模型（参数化 + 启停；不做可视化编排）

~~~json
{
  "code": "R03",
  "name": "预计完成日期前 1 天提醒",
  "enabled": true,
  "trigger": { "type": "schedule", "cron": "0 8 * * *", "window": "T-1" },
  "conditions": [
    { "field": "task.status", "op": "in", "value": ["待开始", "进行中"] },
    { "field": "task.planned_end", "op": "eqOffsetDays", "value": 1 }
  ],
  "actions": [
    { "type": "notify", "channel": "wecom", "to": "task.owner", "template": "T-1_REMIND" },
    { "type": "notification", "to": "task.owner", "template": "T-1_REMIND" }
  ]
}
~~~

### 6.2 执行保证

| 机制 | 设计 |
|---|---|
| 幂等执行键 | ruleId + entityId + 触发窗口（日期窗口 / 状态版本），落 dedupe_key 唯一约束兜底 |
| 调度 | worker 单例（advisory lock）；记录 last_run_at；重启或错过窗口按应执行清单补发 |
| 投递 | 同事务写 outbox → worker 发送 → 结果回写；失败指数退避 + 最大次数 + 死信告警 |
| 合并与免打扰 | 同人同时段消息合并；免打扰时段静默、次日补发；每人每日上限 |
| 留痕 | automation_runs 记录命中、条件求值、动作结果、渠道与时间 |
| 可测试 | 时钟注入（规则代码禁止直接取系统时间）+ 规则回放 + 金标文案逐字比对 |

### 6.3 事件触发（节点完成等）

节点完成（3.6）在事务内写 outbox（node.completed）→ worker 消费 → 触发相关规则（缺件提醒、喜报、阶段达成）。事件处理幂等规则同 6.2；消息内容与权限（无权限字段不出现）走同一策略。

---

## 7. 接口契约与错误模型

### 7.1 约定

- 前缀 /api/v1；JSON；Zod 契约唯一真相 → 生成 OpenAPI 与前端客户端。
- 分页：表格 page / limit + total；信息流用 cursor。
- 幂等：写操作支持 Idempotency-Key，重复提交返回首次结果。
- 并发：更新携带 version，冲突返回 409。
- 时间：ISO8601（UTC 存储，Asia/Shanghai 展示）；业务日期 YYYY-MM-DD。
- 筛选与排序统一：filter[key]=、sort=key:asc、fields= 稀疏字段。
- 认证与会话：`/auth/*`（根路径，浏览器直接导航，不进 /api/v1）：OIDC authorization_code + PKCE + state，HttpOnly Cookie 会话（空闲超时 SESSION_IDLE_MINUTES，默认 30 分钟）；`/auth/me` 契约见 shared/src/modules/identity.ts（ADR-010）。

### 7.2 错误模型

~~~json
{ "code": "VALIDATION_FAILED", "message": "参数校验失败", "details": [], "traceId": "uuid" }
~~~

| code | HTTP | 场景 |
|---|---|---|
| VALIDATION_FAILED | 400 | 参数 / 契约校验失败 |
| AUTH_REQUIRED / FORBIDDEN | 401 / 403 | 认证与授权（资源不存在与无权统一按 404 语义处理） |
| NOT_FOUND | 404 | 资源不存在或不可见 |
| VERSION_CONFLICT | 409 | 乐观锁冲突（并发完成 / 编辑） |
| PROJECT_CODE_EXISTS | 409 | 项目编号已存在（创建人填写的编号需全局唯一） |
| NODE_REQUIRED_DOC_MISSING | 422 | 完成门禁未通过（含 missing 明细） |
| NODE_ALREADY_DONE / NODE_DELETED | 409 | 节点状态不允许当前操作 |
| BLUEPRINT_SCHEMA_INVALID / BLUEPRINT_REF_UNKNOWN | 422 | 蓝图校验失败（含 issues 列表） |
| FILE_STATE_INVALID | 409 | 文件状态不允许该操作 |
| UPLOAD_INCOMPLETE | 409 | 上传会话分片未齐（需补传后重试） |
| UPLOAD_SESSION_EXPIRED | 410 | 上传会话已过期，需重建（命名对齐 技术设计v0.3 §4.8） |
| FILE_HASH_MISMATCH | 422 | 完成上传时内容哈希与登记值不一致（命名对齐 技术设计v0.3 §4.8） |
| PREVIEW_NOT_READY / PREVIEW_FAILED | 200 + 状态字段 | 预览为异步产物，前端轮询或订阅 |
| IDEMPOTENT_REPLAY | 200 | 幂等重放（返回首次结果） |
| AUTH_CALLBACK_FAILED | 400 | SSO 回调校验失败（state / code / 换令牌；浏览器导航场景） |

---

## 8. 首页分类与筛选契约

### 8.1 维度（一期）

| 维度 | 字段 | 说明 |
|---|---|---|
| 地区 | projects.region | 项目落地地区（字典） |
| 项目类型 | projects.project_type | 字典 |
| 项目经理 | projects.manager_id | 用户（必填） |
| 阶段 | projects.stage_key | 九阶段 |
| 状态 | projects.status | active / paused / done / archived |

### 8.2 接口与实现

- 列表：GET /api/v1/projects?filter[region]=..&filter[projectType]=..&filter[managerId]=..&filter[stageKey]=..&page=1&limit=50
- 计数：GET /api/v1/projects/facets（同筛选口径返回各维度计数，供侧边栏展示）
- 排序：默认 `updatedAt` 降序（最新的在前）；支持 `sort=seqNo:asc|desc`（项目序号，全库唯一，可作稳定定位——与卡片右上角徽标同值）。
- 筛选态写入 URL query（可分享、可收藏）；导出与统计沿用同一筛选模型。
- 索引：ix_projects_facets（region, project_type, manager_id）、ix_projects_stage（status, stage_key）；一期数据量小，计数实时聚合即可（如需再物化视图）。

### 8.3 历史数据

迁移时地区 / 项目类型缺失 → 置「未分类」，不阻塞上线；补录走管理员批量导入；部门 / 人员映射以迁移脚本的校验报告为准。

---

## 9. 阶段 0 映射与实施顺序（技术侧）

| 顺序 | 内容 | 依赖 | 验收口径（对应 v0.1 第 7.3 节） |
|---|---|---|---|
| 1 | 骨架：仓库结构、CI 骨架、SSO 登录、契约包 | 无 | 登录可用；空库迁移可跑 |
| 2 | 项目 / 阶段 / 节点 / 蓝图导入 + 门禁 | 1 | PoC #9；金标用例全绿 |
| 3 | 文件：上传 / 版本 / 定档 / 变更（即通过）/ 预览 | 1 | PoC #1 |
| 4 | 自动化：Outbox / 调度 / R01~R07 + A01/A02/A03/A14 | 2、3 | PoC #3 / #4 / #5 |
| 5 | 企微打通与消息合并 | 4 | PoC #2 |
| 6 | 首页分类 + 搜索投影 + 基础仪表盘 | 2、3 | 分类筛选与计数一致 |
| 7 | 迁移演练（历史数据） | 2~6 | PoC #7 |
| 条件 | 二维 CAD PoC（阶段 0 前置取样本） | 3 | PoC #8 |

---

## 10. 开放问题（业务侧，不阻塞本设计）

| # | 事项 | 归属 |
|---|---|---|
| Q28 | 节点约束细则（哪些节点必交哪类成果文件、能否回退）；项目类型枚举；历史补录规则 | 业务 |
| A06 | 延期审批期次（影响审批中心去留） | 业务 / 功能书 V1.1 |
| 映射 | 变更阶段四值 → 九阶段映射确认（2.5 已给建议） | 业务 |
| 蓝图内容 | 九阶段各节点明细清单（3.3 为建议稿） | 业务 |

---

## 11. 数据模型与契约增补（v0.3 实施输入）

本节汇总《技术设计v0.3-实施与验收.md》从功能书与前端需求反推的模型 / 契约缺口，作为 M1~M7 卡片的登记口径；表结构最终以 `database/migrations/` 为准。

### 11.1 需新增的表（按模块）

| 模块 | 表 | 要点 |
|---|---|---|
| identity/org | users（已随 g6 落地 0004）、sessions（已随 g6 落地 0004）、departments、roles、role_permissions、user_roles | 登录 / 会话已落地（0004 / g6）；组织 / 角色四表已落地（0007 · h1 / Push 74，含角色种子）；目录同步适配器与权限矩阵条目随 h1 剩余 / h6 |
| admin | audit_logs、dict_types、dict_items | 审计追加写；字典唯一口径（含 accent 元数据） |
| calendar | calendar_days、calendar_settings | 工作日历（D5）：只存例外（放假 / 调休上班）+ 单行顺延配置（已落 0014 · h8 / Push 99） |
| platform | idempotency_keys、jobs、search_docs | 幂等、调度、搜索投影（导出并入 jobs） |
| project | project_members、project_views、follows | 成员制、视图、关注 |
| file | upload_sessions、file_links、preview_artifacts、change_items（`upload_parts` **不落表**：分片状态以对象存储 ListParts 为唯一真相） | 分片上传、多态关联、预览产物、变更明细 |
| report/issue | daily_reports、daily_report_files、issues、issue_events、issue_dispatch_rules | 日报与问题闭环（含 SLA `due_at`） |
| stakeholder | stakeholders、project_stakeholders | 隐私字段走字段级权限 |
| notify/automation | notifications、notify_prefs、automation_rules、automation_runs | 站内信、免打扰、规则与留痕 |
| workspace | todos | 自定义待办（C2-06 / A14） |

### 11.2 需修订的既有表

| 表 | 变更 | 口径来源 |
|---|---|---|
| projects | `updated_at` 触发事件集；归档只读状态位 | v0.3 §7 第 4 / 12 项 |
| tasks | `deliverable_types`（多选）；`deleted_at`（软删） | v0.3 §7 第 6 项、A2-01 |
| project_stages | 推进 / 回退时间与操作人 | A1-12 |
| files | `purge_after`（回收站到期）；doc_type 对齐成果文件字典 | A4-12 |
| change_requests | 与 change_items 的明细关系 | A4-13 |

### 11.3 需新增的契约（端点族）

| 族 | 端点（示例） | 对应卡片 |
|---|---|---|
| auth | /auth/login、/auth/callback、/auth/me、/auth/logout（已随 g6 落地）；/auth/mfa/verify 待建 | M1 |
| users | /users、/projects/{id}/members | M1 / M2 |
| dicts | /dicts、/dicts/{type}（含管理 CRUD） | M1 |
| preferences | /users/me/preferences | M1 |
| stages | /projects/{id}/stages、/projects/{id}/stages/{key}/advance | M2 |
| views / follows | /views、/follows | M2 |
| tasks | 批量 PATCH、complete、快筛参数 | M3 |
| files | upload-sessions、versions、finalize、rollback、回收站、preview、download | M4 |
| changes | /changes | M4 |
| reports / issues | /reports、/issues（含看板流转与统计） | M6 |
| stakeholders | /stakeholders、导入、导出 | M6 |
| notifications | /notifications、/notifications/stream（SSE） | M5 |
| search | /search | M7 |
| exports | /exports、进度查询 | M7 |

### 11.4 错误码增补

见《技术设计v0.3-实施与验收.md》§4.8：STAGE_GATE_NOT_PASSED、TASK_REQUIRED_DOC_MISSING、CHANGE_FILE_REQUIRED、UPLOAD_INCOMPLETE、UPLOAD_SESSION_EXPIRED、FILE_HASH_MISMATCH、DICT_VALUE_UNKNOWN、DUPLICATE_SUBMISSION、ISSUE_TRANSITION_INVALID、RATE_LIMITED、MFA_REQUIRED。

### 11.5 与 §2.3 基线的关系

0001 基线为 10 张业务表（projects、project_stages、project_nodes、node_requirements、tasks、task_events、files、file_versions、change_requests、outbox_events）＋ schema_migrations，覆盖「项目 / 流程 / 任务 / 文件」核心；本节为其余模块的登记增补，随 M1~M7 卡片分批落迁移（0004 起）。
---

（本文为架构与数据模型讨论稿；表结构与字段以迁移脚本为准，本文提供设计口径与验收线索。）
